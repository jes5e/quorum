---
id: t1.ut9.29
type: t1
title: Extend /quo-spec-review emission with per-fix-path depth + enumerated fix paths
parent: b.ut9
status: drafted
created_at: '2026-05-20T04:33:43.414940'
schema_version: '0.1'
guid: ut92987icjtbr39ev3466vkih1njyni5
---

Extend `skills/quo-spec-review/SKILL.md` Step 4 (Generate Work Item List) so each finding emits per-fix-path depth tags (`trivial-tweak` / `refactor-locally` / `re-architect`) plus enumerated fix paths. Severity tags (`[blocker]` / `[suggestion]` / `[nit]`) are already emitted by this skill; this Epic adds the depth dimension and the fix-path enumeration only.

## Scope

- Extend the output-shape section (currently at lines 202–243) with the new depth and fix-path elements.
- Provide at least one worked example showing the existing severity tag PLUS the new depth + paths.
- Preserve the three existing trailer shapes (Shape 1 blockers / Shape 2 suggestions-nits / Shape 3 no-findings) — the new emission sits inside the work-item list; trailers are unchanged.
- Preserve the checklist contents (twelve PRD checks, ten SDD checks, cross-document checks) — only the finding-emission shape changes.

## Acceptance criteria

1. The skill's output-shape section documents per-fix-path depth tags AND enumerated fix paths alongside the existing severity tags.
2. At least one worked example renders in the skill prose with the new shape.
3. The three existing trailer shapes are preserved verbatim.
4. A live run of `/quo-spec-review` produces findings carrying both severity and per-fix-path depth tags.

Out of scope: checklist content changes (the twelve PRD checks and ten SDD checks stay intact — PRD non-goal), orchestrator routing (Epic 7).

