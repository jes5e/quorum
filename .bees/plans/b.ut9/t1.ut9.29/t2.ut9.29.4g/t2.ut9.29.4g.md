---
id: t2.ut9.29.4g
type: t2
title: Extend /quo-spec-review Step 4 emission with per-fix-path depth tags + enumerated fix paths
parent: t1.ut9.29
children:
- t3.ut9.29.4g.tr
reference_materials: null
created_at: '2026-05-29T01:46:47.249512'
status: done
schema_version: '0.1'
guid: ut9294gnfu17j1r5nmqfmqua5riu5dfr
---

## Context

`/quo-spec-review` **already** emits severity tags — in `[blocker]` / `[suggestion]` / `[nit]` **bracket** form (defined at `skills/quo-spec-review/SKILL.md` line 36; used in the Step 4 worked examples at lines 213–216 and 225–227). Unlike the three implementer-side review skills (`/quo-engineer-review`, `/quo-doc-writer-review`, `/quo-test-writer-review`), this is **NOT** a first-introduction-of-severity Epic. This Epic adds **only** the orthogonal per-fix-path depth dimension and fix-path enumeration on top of the existing severity tagging (per SR-1.1's `/quo-spec-review` clause and SR-1.2).

The output-shape section is Step 4 "Generate Work Item List" (lines ~202–243). `/quo-spec-review` has **three** trailer shapes (not two): Shape 1 blockers-present (lines 210–218), Shape 2 suggestions/nits-only (lines 222–230), Shape 3 no-findings (lines 234–240). Each trailer carries the two-step `TaskCreate` → prescribed-tool gate prose and counter-anchor clauses.

## What Needs to Change

- In `skills/quo-spec-review/SKILL.md` **Step 4 (Generate Work Item List)**, extend the finding emission shape so each finding carries **one or more enumerated fix paths**, each fix path carrying **its own depth tag** (`trivial-tweak` / `refactor-locally` / `re-architect`), **alongside the existing severity tag**. The finding-line shape becomes (preserving the existing `[severity]` bracket form):
  - finding line: `<n>. [<severity>] <doc + section anchor> <one or more fix-path lines> — <description>` where `[<severity>]` stays `[blocker]` / `[suggestion]` / `[nit]` (bracket form, unchanged)
  - fix-path line: `(<letter>) [depth:<trivial-tweak|refactor-locally|re-architect>] <description of that fix path>`
  - Single-path findings remain valid (one `(a)` block); the shape is uniform whether 1 or N paths.
- **Do NOT change the severity rendering** from the existing `[blocker]` bracket form to the backtick `` `blocker` `` form used by the three implementer-side emission Epics. This Epic's scope is the depth dimension + fix-path enumeration only; the severity-rendering asymmetry between `/quo-spec-review` (bracket) and the other three skills (backtick) is **pre-existing and SDD-acknowledged**, and is benign for the Phase-2 routing parser (which keys on `(num-paths, max-depth)` derived from the `(<letter>) [depth:...]` fix-path lines, NOT on severity rendering — PRD FR-2).
- Provide **at least one worked example** showing the existing severity tag PLUS the new depth + fix paths (per Epic AC #2). Per SR-1.2's calibration discipline, include at least one example per depth bucket (`trivial-tweak`, `refactor-locally`, `re-architect`) and at least one single-path and one multi-path example in the prose. Keep examples spec-review-flavored (PRD/SDD section gaps, measurability, cross-document consistency).
- **Example-placement guidance:** prefer placing the per-depth-bucket calibration examples in introductory prose; keep the three fenced ```markdown trailer-shape blocks minimal-but-shape-complete (do NOT cram all three depth buckets into them) — those fenced blocks double as verbatim output templates a reviewer copies.
- **Preserve all three trailer shapes verbatim** — the new emission shape sits *inside* the work-item list; the Shape 1 / Shape 2 / Shape 3 trailers (lines 210–240) are unchanged in form, including the `**Your next two tool calls MUST be (1) TaskCreate ... then (2) AskUserQuestion**` / `**Your next tool call MUST be bees update-ticket --status ready**` prescriptions, the counter-anchor clauses, and the two-step gate-task instructions.
- **Preserve the checklist contents verbatim** — the twelve PRD checks (Step 2 PRD checklist), the ten SDD checks (Step 2 SDD checklist), and the five cross-document consistency checks. Only the finding-emission shape (Step 4) changes; the checklist substance (PRD non-goal) does NOT.

## Why

Severity describes *how important fixing-at-all is*; depth describes *what fixing costs*. `/quo-spec-review` already carries severity; adding the depth dimension + fix-path enumeration makes its emission feed the Phase-2 orchestrator routing table (Epic `t1.ut9.no`) on the same `(num-paths, max-depth)` basis as the other three review skills. Depth tags become *emitted* here but are not yet *consumed* by any routing rule — zero new user gates, zero `b.wii` exposure (Phase 1).

## Success Criteria

- The Step 4 output-shape section documents per-fix-path depth tags AND enumerated fix paths alongside the existing severity tags.
- The severity rendering stays `[blocker]` / `[suggestion]` / `[nit]` bracket form (not changed to backtick).
- At least one worked example renders the existing severity tag PLUS the new depth + fix paths; at least one example per depth bucket and at least one single-path and one multi-path example appear in the prose.
- The three existing trailer shapes (Shape 1 / Shape 2 / Shape 3) are preserved verbatim, including their two-step gate prose and counter-anchor clauses.
- The twelve PRD checks, ten SDD checks, and cross-document consistency checks are preserved verbatim (only the emission shape changes).
- A live run of `/quo-spec-review` produces findings carrying both severity and per-fix-path depth tags.
- The change is pure prose: language-agnostic, project-neutral, no new shell snippets (so no OS-pairing obligation), no scratch-file writes.

## Cross-Epic contract (PM cross-check)

The **new** elements introduced here — the fix-path enumeration `(<letter>) ...` and the `[depth:<trivial-tweak|refactor-locally|re-architect>]` depth tags — MUST match the shape used by sibling emission Epics `t1.ut9.9c` / `t1.ut9.jn` / `t1.ut9.q2` so the Phase-2 orchestrator parser (Epic `t1.ut9.no`) derives the `(num-paths, max-depth)` tuple uniformly across all four review skills. The **severity** rendering is intentionally NOT unified — `/quo-spec-review` keeps its existing `[blocker]` bracket form while the other three use backticks; this is benign because routing keys on paths+depth, not severity (PRD FR-2). The integration-time cross-check that verifies the new-element uniformity (and notes the acceptable severity-rendering asymmetry) across all four emission Epics is tracked as Issue `b.11z`.

Note: `/quo-spec-review` has **no** reviewer-subagent wrapper under `agents/` (it is invoked by `/quo-plan` / `/quo-write-prd` / `/quo-write-sdd` as a quality gate, not dispatched as a subagent), so Epic `t1.ut9.2q` (which aligns `agents/code-reviewer.md` / `doc-reviewer.md` / `test-reviewer.md` frontmatter) does NOT touch this skill — no wrapper-alignment coupling here.

**Out of scope (belongs to sibling Epics, do NOT include here):** changes to other review skills (`9c` / `jn` / `q2`), the checklist contents (PRD non-goal — twelve PRD checks / ten SDD checks / cross-doc checks stay intact), the orchestrator routing table + anti-pattern callout (`no`), the compromise tracker (`po`), the end-of-run "Accepted compromises" summary (`od`), and the post-completion review reframe (`pw`).
