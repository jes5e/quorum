---
id: t3.ut9.29.4g.tr
type: t3
title: Add per-fix-path depth tags + fix-path enumeration to /quo-spec-review Step 4 emission shape
parent: t2.ut9.29.4g
reference_materials: null
created_at: '2026-05-29T01:48:57.828566'
status: done
schema_version: '0.1'
guid: ut9294gtrfa6bkymosuxjt8ic8u5buqy
---

## Context

`/quo-spec-review` (`skills/quo-spec-review/SKILL.md`) is the fresh-eyes Spec-Bee PRD/SDD review skill. Unlike the three implementer-side review skills (`/quo-engineer-review`, `/quo-doc-writer-review`, `/quo-test-writer-review`), it **already** emits severity tags — in `[blocker]` / `[suggestion]` / `[nit]` **bracket** form (severity ladder defined at line 36 and Step 3 lines 183-187; worked examples render the bracket form at lines 213-216 and 225-227). This Epic is therefore NOT a first-introduction-of-severity change for this skill; it adds ONLY the orthogonal per-fix-path **depth** dimension plus **fix-path enumeration** on top of the existing severity tagging.

The output-shape section is Step 4 "Generate Work Item List" (lines ~202-243). This skill has **three** trailer shapes (not two): Shape 1 blockers-present (fenced block lines 210-218), Shape 2 suggestions/nits-only (fenced block lines 222-230), Shape 3 no-findings (fenced block lines 234-240). Each findings-present trailer carries the two-step `TaskCreate` -> prescribed-tool gate prose and counter-anchor clauses.

The new elements (fix-path enumeration `(<letter>) ...` and `[depth:<trivial-tweak|refactor-locally|re-architect>]` tags) MUST match the shape used by sibling emission Epics `t1.ut9.9c` / `t1.ut9.jn` / `t1.ut9.q2` so the Phase-2 orchestrator parser (Epic `t1.ut9.no`) can derive the `(num-paths, max-depth)` tuple uniformly across all four review skills (PRD FR-2). Depth tags are *emitted* here but not yet *consumed* by any routing rule (Phase 1 — zero new user gates). The severity-rendering asymmetry (bracket here vs backtick in the other three) is pre-existing and SDD-acknowledged, and is benign because routing keys on paths+depth, not severity.

This is a skill-prose change in the quorum repo, so CLAUDE.md's three design rules, the scratch-file convention, and `## Review criteria for skill changes` apply: keep the edit language-agnostic, project-neutral, and free of new shell snippets (no OS-pairing obligation) and scratch-file writes.

## What Needs to Change

Edit **only** Step 4 (Generate Work Item List, lines ~202-243) of `skills/quo-spec-review/SKILL.md`:

1. **Document the extended finding-emission shape** so each finding carries one or more enumerated fix paths, each fix path carrying its own depth tag alongside the existing severity tag:
   - finding line: `<n>. [<severity>] <doc + section anchor> <one or more fix-path lines> — <description>`, where `[<severity>]` STAYS `[blocker]` / `[suggestion]` / `[nit]` (bracket form, unchanged).
   - fix-path line: `(<letter>) [depth:<trivial-tweak|refactor-locally|re-architect>] <description of that fix path>`.
   - Single-path findings remain valid (one `(a)` block); the shape is uniform whether 1 or N paths.

2. **Do NOT change the severity rendering** from the existing `[blocker]` bracket form to the backtick `` `blocker` `` form used by the three implementer-side emission Epics. This Epic's scope is the depth dimension + fix-path enumeration only. Add a short prose note that the bracket-vs-backtick severity asymmetry is intentional / pre-existing / SDD-acknowledged and benign for the Phase-2 routing parser, which keys on `(num-paths, max-depth)` derived from the `(<letter>) [depth:...]` fix-path lines, not on severity rendering.

3. **Add per-depth-bucket calibration examples in introductory prose** (per SR-1.2 calibration discipline): at least one example each for `trivial-tweak`, `refactor-locally`, and `re-architect`, AND at least one single-path and one multi-path example. Each example must render the existing severity tag PLUS the new depth + fix paths (Epic AC #2). Keep examples spec-review-flavored (PRD/SDD section gaps, measurability, cross-document consistency).

4. **Keep the three fenced ```markdown trailer-shape blocks minimal-but-shape-complete.** Update the Shape 1 and Shape 2 worked findings so they demonstrate the new fix-path + depth shape (the fenced blocks double as verbatim output templates a reviewer copies), but do NOT cram all three depth buckets into them — put the full per-bucket calibration set in the introductory prose per item 3. Shape 3 (no-findings) has no findings to reshape.

5. **Preserve all three trailer shapes verbatim in form.** The Shape 1 / Shape 2 / Shape 3 trailer prose below each fenced block's findings (the `**Your next two tool calls MUST be (1) TaskCreate ... then (2) AskUserQuestion**` prescriptions for Shapes 1-2, the `**Your next tool call MUST be bees update-ticket --status ready**` prescription for Shape 3, the counter-anchor clauses, the two-step gate-task instructions, the `docs/doc-writing-guide.md` references, and the `gate-*` task-completion notes) are UNCHANGED. Only the work-item list content that sits ABOVE each trailer changes.

6. **Preserve all checklist contents verbatim** (PRD non-goal) — the twelve PRD checks (Step 2 PRD checklist, lines 87-118), the ten SDD checks (Step 2 SDD checklist, lines 120-159), and the five cross-document consistency checks (lines 161-179). Only Step 4's finding-emission shape changes.

## Key Files

- `/Users/jesseg/code/quorum/skills/quo-spec-review/SKILL.md` — Step 4 "Generate Work Item List" section (lines ~202-243), including the three fenced ```markdown trailer-shape blocks (Shape 1 lines 210-218, Shape 2 lines 222-230, Shape 3 lines 234-240).

## Acceptance Criteria

- Step 4's output-shape section documents per-fix-path depth tags (`trivial-tweak` / `refactor-locally` / `re-architect`) AND enumerated fix paths (`(<letter>) [depth:...]`) alongside the existing severity tags.
- The severity rendering stays `[blocker]` / `[suggestion]` / `[nit]` bracket form — NOT changed to backtick form. A prose note records that this asymmetry vs the other three review skills is intentional and benign for the routing parser.
- At least one worked example renders the existing severity tag PLUS the new depth + fix paths; the introductory prose contains at least one example per depth bucket (trivial-tweak, refactor-locally, re-architect) and at least one single-path and one multi-path example.
- The fix-path enumeration `(<letter>) ...` and `[depth:<trivial-tweak|refactor-locally|re-architect>]` tags match the shape spec shared with sibling emission Epics `t1.ut9.9c` / `t1.ut9.jn` / `t1.ut9.q2` (so the Phase-2 parser derives `(num-paths, max-depth)` uniformly).
- All three trailer shapes (Shape 1 / Shape 2 / Shape 3) are preserved verbatim in form, including their two-step `TaskCreate` -> `AskUserQuestion` gate prose, the Shape 3 `bees update-ticket --status ready` prescription, the counter-anchor clauses, the `docs/doc-writing-guide.md` references, and the `gate-*` task-completion notes.
- The twelve PRD checks, ten SDD checks, and five cross-document consistency checks are preserved verbatim (only the emission shape changes).
- The change is pure prose: language-agnostic, project-neutral, introduces no new shell snippets (no OS-pairing obligation) and no scratch-file writes, satisfying CLAUDE.md's three design rules + scratch-file convention + `## Review criteria for skill changes`.
- The Format command (`echo 'no formatter configured for this repo'`) and Lint command (`python -m pyflakes skills/*/scripts/*.py`) from CLAUDE.md `## Build Commands` still run clean (this edit touches no Python; Compile/type-check is empty for this repo and is skipped).
