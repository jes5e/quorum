---
id: t3.ut9.po.oi.du
type: t3
title: Add session-scoped compromise tracker mechanism to quo-fix-issue and quo-execute SKILL.md
parent: t2.ut9.po.oi
reference_materials: null
created_at: '2026-05-29T02:56:29.113054'
status: done
schema_version: '0.1'
guid: ut9pooiduvjef8cy5uasxd4zqgfdcrki
---

## Context

This Subtask implements PRD FR-4 / SDD SR-3 — the **session-scoped compromise tracker** — landing near-identical prose in BOTH `skills/quo-fix-issue/SKILL.md` AND `skills/quo-execute/SKILL.md`. The tracker is a markdown file under `<tempdir>/.quorum/` that accumulates one entry per accepted compromise, so that a legitimately-accepted compromise (Defer / Accept / refactor-locally auto-route / post-completion override) stays visible and challengeable rather than baked silently into the new baseline.

The whole mechanism is ONE internally-coherent thing and is kept in a SINGLE Subtask deliberately: the prose must land near-identically across the two orchestrators, and **divergence between the two files is a defect** (SDD SR-3, Task Success Criterion 8). Authoring both files in one coherent pass — holding both in working memory at once — is the design that protects against drift; do NOT split the work file-by-file or trigger-by-trigger.

The authoritative contract is SDD `t1.tip.ux` (the Spec Bee SDD `t1=Doc` child, fetch via `bees show-ticket --ids t1.tip.ux`), specifically requirements SR-3.1 through SR-3.4, the routing decision SR-2.4, the recovery gate SR-6.7, and the `## Codebase exploration findings` → `### Data models` → `#### Compromise tracker entry shape` block. Author against the SDD; where this Subtask body paraphrases, the SDD wins.

**Cross-Epic timing.** This Epic depends on Epic `t1.ut9.no` (the routing table + scope-bounding gates that Triggers A/B/C fire from) and forward-references Epic `t1.ut9.pw` (the SR-6.7 recovery gate that Trigger D fires from). At implementation time, `no`'s gates and `pw`'s SR-6.7 gate MAY NOT YET be present in the two SKILL.md files. That is expected and handled: this Subtask writes the tracker DEFINITION block plus the four WRITE-MOMENT instructions, anchoring each trigger to its SDD-locked gate by name/location (e.g. "the scope-bounding gate per SR-5.2", "the SR-6.7 three-choice recovery gate") so the write logic and the sibling Epics' gates align when both land. Do NOT author the gates themselves — `no` owns the routing table + scope-bounding gate, `pw` owns the SR-6.7 gate; this Epic only owns the tracker file convention, the entry shape, and the four append-write instructions (including the Trigger-D `File follow-up Issue → amend original Trigger C entry` nuance).

**House conventions that govern this change** (CLAUDE.md): the three design rules; the scratch-file convention (`<tempdir>/.quorum/`, create-if-absent, never delete, no `rm`-with-allowlist); the bash-etiquette rules; and — because this Epic introduces NEW shell snippets (the tracker-file create-if-absent step) — design rule #2 (paired POSIX bash + Windows PowerShell, no bash-only fallback) IS engaged. Match the house pattern set by the existing deferral-hygiene Encode snippets in both files (the `mkdir -p /tmp/.quorum` / `New-Item -ItemType Directory -Force -Path "$env:TEMP\.quorum"` paired blocks).

## What Needs to Change

Add the compromise-tracker mechanism to both orchestrator SKILL.md files in near-identical prose, comprising the four pieces below.

**Piece 1 — Tracker file convention + entry shape (definition block; SR-3.1 / SR-3.3 / Data models).** Add a single self-contained block to each file (a `####` subsection under the per-run cleanup/handoff region is the natural site — in `quo-fix-issue` near Section 7 / 7.5; in `quo-execute` near Section 4.1 / 6.5 — pick a site that the four trigger write-sites can reference by name, and keep the site analogous across the two files). The block MUST document:
  - Canonical filename `compromises-YYYYMMDD-HHMM-<short-suffix>.md` under `<tempdir>/.quorum/` (`/tmp/.quorum/` on POSIX, `%TEMP%\.quorum` on Windows), where `YYYYMMDD-HHMM` is a UTC timestamp generated ONCE at the start of the run and `<short-suffix>` is a short collision-resistant random string. State the rationale verbatim-faithfully to SR-3.1: the timestamp prefix makes tracker files debuggably identifiable across runs accumulated in `<tempdir>/.quorum/`.
  - The paired POSIX + PowerShell create-if-absent snippets for the `.quorum` dir, matching the existing deferral-hygiene snippet shape in the same file (`mkdir -p /tmp/.quorum` / `New-Item -ItemType Directory -Force -Path "$env:TEMP\.quorum"`), each in an OS-labeled fenced block. The tracker file itself is authored/appended via the `Write` tool (no shell redirect), consistent with the bash-etiquette and scratch-file conventions.
  - The five-field entry shape, either reproduced verbatim from the SDD Data models `#### Compromise tracker entry shape` markdown block OR documented inline with an explicit reference to that SDD section. The five canonical fields are: **Finding (verbatim)** (severity tag + fix-path enumeration with depth tags + description); **Fix paths surfaced by reviewer** (the `(a)/(b)/...` lines with `[depth:<...>]` tags); **Decision** (one of: `User picked path (a|b|...)` / `User picked Defer to follow-up Issue` / `User picked Accept the limitation` / `Auto-routed (a) per single-path refactor-locally rule` / `User overrode auto-route after post-completion challenge (depth misjudgment)` / `User accepted under-enumeration after post-completion challenge`); **Rationale** (the user's stated reason if surfaced via `AskUserQuestion`, or the auto-route rule that fired); **Follow-up Issue** (ticket ID, else `none`). Include the `## Compromise <n>` section heading with the 1-based run-scoped counter (NOT a globally-unique id), per the Data models shape. The Decision enum carries **two** post-completion-override values — depth-misjudgment (SR-6.7) and under-enumeration (the SR-4.6 analog gate) — because Trigger D (Piece 5) is generalized to cover both kinds of post-completion override.
  - Persistence (SR-3.3): the tracker is a file-system artifact and persists across orchestrator-yield events within a single run inherently; a fresh timestamp + `<short-suffix>` is generated at the start of each run, so previous-run tracker files remain visible but are NEVER appended to. Never delete the tracker file (scratch-file convention) — do NOT instruct any `rm` / `Remove-Item`.

**Piece 2 — Trigger A (Defer to follow-up Issue at the scope-bounding gate; SR-3.2).** At the site where Epic `no`'s scope-bounding gate (per SR-5.2) handles the user picking `Defer to follow-up Issue`, instruct: append a tracker entry IMMEDIATELY AFTER `/quo-file-issue` returns successfully with the new Issue ticket ID, BEFORE the orchestrator continues with the soft-fix dispatch. Capture the follow-up Issue ID in the entry's `Follow-up Issue` field; `Decision: User picked Defer to follow-up Issue`. Since `no`'s gate may not be present yet, anchor the instruction to the SR-5.2 scope-bounding gate by name and note that the write fires from that gate's `Defer` branch when it lands.

**Piece 3 — Trigger B (Accept the limitation at the scope-bounding gate; SR-3.2).** At the same scope-bounding gate's `Accept the limitation` branch, instruct: append a tracker entry IMMEDIATELY AFTER the `AskUserQuestion` returns the user's choice, BEFORE the orchestrator continues without a fix. `Decision: User picked Accept the limitation`; `Follow-up Issue: none`.

**Piece 4 — Trigger C (auto-route single-path `(1, refactor-locally)`; SR-2.4 / SR-3.2).** This trigger has NO gate — wire the write into the auto-dispatch step itself. Instruct: at the MOMENT of implementer dispatch for a single-path `(1, refactor-locally)` finding (per `no`'s routing table SR-2.4), append a tracker entry in the SAME LOGICAL BLOCK as the auto-dispatch (NOT a separate post-gate block — Success Criterion 5 verifies this in prose). `Decision: Auto-routed (a) per single-path refactor-locally rule`; `Rationale:` the routing-table rule wording that fired; `Follow-up Issue: none`.

**Piece 5 — Trigger D, GENERALIZED to cover BOTH post-completion-override gates (SR-3.2 / SR-6.7 / SR-4.6).** Epic `pw` owns the post-completion recovery gates; THIS Epic owns only the tracker-WRITE logic that fires from them. Trigger D is the post-completion-override write trigger and covers **both** kinds of post-completion override that `pw`'s gates can produce: the SR-6.7 depth-misjudgment recovery gate AND the SR-4.6 under-enumeration analog gate. (Rationale: SDD SR-4.6 specifies its accept-branch "follows the same shape as SR-6.7" and Epic `pw` AC 4a requires its accept-branch to "append explicit-override tracker entry"; SR-3.2 originally enumerated only the depth-misjudgment override, so this Subtask generalizes Trigger D to be the single owner of all post-completion-override tracker writes, closing the cross-Epic gap surfaced during `pw`'s breakdown. The optional matching SDD spec-clarity tidy-up is tracked separately.) Add the write logic anchored to both gates (in `quo-fix-issue` Section 8, in `quo-execute` Section 6 — the post-completion review sections), branch-specific:

  **SR-6.7 depth-misjudgment recovery gate:**
  - `Accept the misjudgment and proceed` → append a NEW tracker entry IMMEDIATELY AFTER the user picks this choice, BEFORE the post-completion flow continues. `Decision: User overrode auto-route after post-completion challenge (depth misjudgment)`; `Rationale:` the post-completion reviewer's challenge text.
  - `File follow-up Issue to revisit the depth decision` → NO new entry is appended; instead the ORIGINAL Trigger C entry (already written at auto-dispatch time) has its `Follow-up Issue` field UPDATED to the new Issue ID. State this nuance explicitly (Success Criterion 6).
  - `Pause to discuss` → DEFER any tracker write until the discussion resolves and the user picks again (i.e., no write on this branch until a subsequent `Accept`/`File` pick).

  **SR-4.6 under-enumeration analog recovery gate (same Trigger D mechanism, under-enumeration variant):**
  - `Accept the under-enumeration and proceed` → append a NEW tracker entry IMMEDIATELY AFTER the user picks this choice, BEFORE the post-completion flow continues. `Decision: User accepted under-enumeration after post-completion challenge`; `Rationale:` the post-completion reviewer's under-enumeration challenge text.
  - `File follow-up Issue to surface the missing path` → file the follow-up Issue; capture its ID in the entry (this branch concerns a missing path the in-flow reviewer never surfaced, so there is no original Trigger C entry to amend — append a new entry with the under-enumeration Decision value and the new Issue ID in `Follow-up Issue`, OR if the under-enumeration relates to an existing auto-routed entry, update that entry's `Follow-up Issue` in place; the implementer should mirror the SR-6.7 File-branch's in-place-update behavior when an originating Trigger C entry exists, and append otherwise).
  - `Pause to discuss` → DEFER any tracker write until the discussion resolves and the user picks again.

  Note that since `pw`'s SR-6.7 and SR-4.6 gates may not be present yet, anchor the write logic to those gates by name and keep the choice labels byte-identical to the SDD / Epic `pw` so they line up with `pw`'s gates when both land. `pw` FIRES these writes; `po` (this Epic) OWNS the write mechanism for both override kinds — neither override-write falls between the two Epics.

After the edits, run the repo's **Lint** command from CLAUDE.md `## Build Commands` (no `Compile/type-check`, `Narrow test`, or `Full test` are meaningful for this markdown-only repo per CLAUDE.md). Re-read both files' tracker prose side-by-side to confirm near-identity before finishing.

## Key Files

- `/Users/jesseg/code/quorum/skills/quo-fix-issue/SKILL.md` — add the tracker definition block (near Section 7 / 7.5), Trigger A/B at the SR-5.2 scope-bounding gate site, Trigger C at the SR-2.4 auto-dispatch site, Trigger D write logic at Section 8 (post-completion review) anchored to the SR-6.7 gate.
- `/Users/jesseg/code/quorum/skills/quo-execute/SKILL.md` — the same mechanism in near-identical prose; definition block near Section 4.1 / 6.5, Trigger D write logic at Section 6 (post-completion review).
- `/Users/jesseg/code/quorum/CLAUDE.md` — governing conventions (three design rules, scratch-file convention, bash etiquette, review criteria). Read-only reference.
- SDD `t1.tip.ux` (bees `t1=Doc` child; `bees show-ticket --ids t1.tip.ux`) — authoritative contract: SR-2.4, SR-3.1–SR-3.4, SR-6.7, and the Data models `#### Compromise tracker entry shape`. Read-only reference.
- Existing deferral-hygiene Encode snippets in both files (the `mkdir -p` / `New-Item` paired blocks) — house-pattern precedent to match. Read-only reference.

## Acceptance Criteria

1. Both SKILL.md files document the tracker file path convention `compromises-YYYYMMDD-HHMM-<short-suffix>.md` under `<tempdir>/.quorum/`, with the SR-3.1 timestamp-prefix rationale.
2. Both orchestrators' prose names the four append triggers (Defer / Accept / refactor-locally auto-route / post-completion override) each with its precise moment-of-write per SR-3.2.
3. The five-field tracker entry shape (Finding / Fix paths surfaced / Decision / Rationale / Follow-up Issue, under a `## Compromise <n>` heading with a 1-based run-scoped counter) is documented in both files, either reproduced or referenced to the SDD Data models section.
4. Paired POSIX bash + Windows PowerShell create-if-absent snippets are present for the tracker dir, matching the existing deferral-hygiene snippet shape (design rule #2 honored; no bash-only fallback).
5. Trigger C's write fires at implementer-dispatch time — verifiable in prose: the tracker-write appears in the SAME logical block as the auto-dispatch, not a separate post-gate block.
6. Trigger D (generalized) covers BOTH post-completion-override gates. For the SR-6.7 depth-misjudgment gate: a NEW entry only on `Accept the misjudgment and proceed`; the `File follow-up Issue to revisit the depth decision` branch UPDATES the original Trigger C entry's `Follow-up Issue` field (no new entry); `Pause to discuss` defers. For the SR-4.6 under-enumeration analog gate: a NEW entry on `Accept the under-enumeration and proceed` (Decision value `User accepted under-enumeration after post-completion challenge`); `File follow-up Issue to surface the missing path` captures the Issue ID (in-place update when an originating Trigger C entry exists, else append); `Pause to discuss` defers. All branch behaviors for both gates are stated explicitly, and the Decision enum carries both post-completion-override values.
7. The two orchestrators' tracker prose is near-identical (definition block, trigger write-moments, entry shape); no avoidable divergence.
8. No `rm` / `Remove-Item` of the tracker file is instructed anywhere; the persistence note (fresh timestamp + suffix per run; previous-run files never appended-to) is present (SR-3.3, scratch-file convention).
9. The gates themselves are NOT authored here — Trigger A/B/C are anchored to Epic `no`'s SR-5.2 scope-bounding gate / SR-2.4 routing table by name, and Trigger D is anchored to Epic `pw`'s SR-6.7 recovery gate by name, with three-choice labels byte-identical to the SDD; no routing-table prose, no scope-bounding-gate definition, and no SR-6.7-gate definition are added by this Subtask.
10. CLAUDE.md's three design rules and `## Review criteria for skill changes` are satisfied: no hardcoded language-specific command/extension/manifest; no project-specific paths/IDs/workflow-specifics baked into skill prose; scratch writes stay under `<tempdir>/.quorum/` with the create-if-absent step and no cleanup instruction. The repo **Lint** command passes.
