---
id: t1.ut9.jn
type: t1
title: Extend /quo-doc-writer-review emission with severity + per-fix-path depth + enumerated fix paths
parent: b.ut9
children:
- t2.ut9.jn.ss
reference_materials: null
created_at: '2026-05-20T04:33:40.198979'
status: in_progress
schema_version: '0.1'
guid: ut9jnzovft4epkd42mqbe6uvao9ay8yq
---

Extend `skills/quo-doc-writer-review/SKILL.md` Step 6 (Output Work Items) so each finding emits a severity tag (`blocker` / `suggestion` / `nit`) AND one or more enumerated fix paths, each with its own depth tag (`trivial-tweak` / `refactor-locally` / `re-architect`). Symmetric to Epic 1's `/quo-engineer-review` extension. This is the *first* introduction of severity tagging to this skill — findings currently carry no severity tag.

## Scope

- Document the new output shape in the SKILL.md's output-shape section (currently at lines 110–137).
- Provide at least one worked example.
- Preserve the existing two-trailer-shape pattern.

## Acceptance criteria

1. The skill's output-shape section documents severity tags AND per-fix-path depth tags AND enumerated fix paths.
2. At least one worked example renders in the skill prose with the new shape.
3. The two existing trailer shapes are preserved verbatim.
4. A live run of `/quo-doc-writer-review` against any doc diff produces findings in the documented shape.

Out of scope: changes to other review skills (separate Epics), changes to `agents/doc-reviewer.md` (Epic 6), changes to orchestrator routing (Epic 7).
