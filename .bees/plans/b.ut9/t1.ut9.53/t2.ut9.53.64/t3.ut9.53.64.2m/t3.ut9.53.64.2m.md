---
id: t3.ut9.53.64.2m
type: t3
title: Add per-fix-path depth dimension + fix-path enumeration to quo-plan Step 5e embedded reviewer finding-shape contract
parent: t2.ut9.53.64
reference_materials: null
created_at: '2026-05-29T02:25:52.650562'
status: ready
schema_version: '0.1'
guid: ut953642m9sgiu213b92p7z7f5t6p2dw
---

## Context

`/quo-plan` Step 5e (the fresh-eyes plan-review gate) embeds a read-only reviewer **dispatch prompt** whose per-finding output contract and worked example live at `skills/quo-plan/SKILL.md` lines ~511–530. This is an **embedded dispatch-prompt output contract**, not a standalone review skill's output section. The reviewer ALREADY emits, per finding: a backtick severity tag (`` `blocker` `` / `` `suggestion` `` / `` `nit` ``, line 513), a `target:` tag (`PRD` / `SDD` / `Plan-Bee-body` / `Epic:<epic-id>`, lines 514–515), and a one-or-two-sentence description (lines 516–517), followed by a verdict trailer line (lines 532–547), a clean/no-findings shape (lines 549–553), and three verdict-conditional Shape A/B/C routing trailers (lines 561–609).

This Epic adds ONLY the orthogonal per-fix-path **depth** dimension and **fix-path enumeration** to the finding shape, so Step 5e's emission feeds the Phase-2 orchestrator routing table on the same `(num-paths, max-depth)` basis as the four in-flow review skills. Severity and the `target:` tag are already present and stay. Depth is emitted-but-not-yet-consumed in Phase 1 — add NO routing/consumption prose for it. The fix-path-line shape `(<letter>) [depth:...]` must match the cross-Epic contract shared with sibling emission Epics `t1.ut9.9c` / `t1.ut9.jn` / `t1.ut9.q2` / `t1.ut9.29` (none have landed yet; the shape spelled out below is authoritative). This is a pure skill-prose change in the quorum repo, governed by CLAUDE.md's three design rules (language-agnostic, OS-portability, project-neutral), the scratch-file convention, and `## Review criteria for skill changes` — though this edit introduces no shell snippets and no scratch-file writes, so only the project-neutral / language-agnostic rules bind in practice.

## What Needs to Change

In `skills/quo-plan/SKILL.md` Step 5e's embedded dispatch-prompt output contract, edit ONLY the finding-shape contract (lines ~511–517) and the worked example (lines ~519–530):

1. **Extend the per-finding contract (lines ~511–517).** Specify that each finding renders the existing backtick severity tag AND the existing `target:` tag AND one or more enumerated fix-path lines, each fix-path line carrying its own depth tag. State the extended finding shape so it is unambiguous:
   - finding line: `` <n>. `<severity>` target: <PRD|SDD|Plan-Bee-body|Epic:<epic-id>> — <description> `` followed by one or more fix-path lines
   - fix-path line: `(<letter>) [depth:<trivial-tweak|refactor-locally|re-architect>] <description of that fix path>`
   - Make explicit that the shape is uniform whether the finding has 1 or N fix paths: single-path findings render exactly one `(a)` block and remain valid.
   - Keep the existing severity-tag bullet (backtick severity, exactly one of `blocker` / `suggestion` / `nit`) and the existing `target:`-tag bullet (lines 514–515) intact — extend the list, do not replace these.
   - In the contract prose (NOT the single example block), include the full per-depth-bucket calibration guidance so the reviewer can distinguish `trivial-tweak` (a localized wording/value edit) from `refactor-locally` (a contained restructuring within one doc/Epic) from `re-architect` (a cross-cutting change touching multiple Epics or the PRD/SDD's core approach), with at least one illustrative phrasing per bucket. This keeps the worked-example block minimal-but-shape-complete while the per-bucket calibration set lives in prose.

2. **Update the worked example (lines ~519–530).** Rewrite each example finding so it renders severity + `target:` PLUS the new depth tags + fix paths. Keep examples plan-review-flavored (PRD/SDD/Plan-Bee-body/Epic-decomposition substance, consistent with the existing three examples). Across the example set: at least one example per depth bucket (`trivial-tweak`, `refactor-locally`, `re-architect`) and at least one single-path example AND at least one multi-path example must appear. Keep the block minimal-but-shape-complete (it doubles as a template) — do not bloat it; the exhaustive per-bucket calibration text belongs in the contract prose per item 1.

3. **Preserve verbatim — do NOT edit:**
   - The `target:`-tag contract bullet (lines 514–515).
   - The verdict trailer line and its semantics (lines 532–547).
   - The clean/no-findings shape (lines 549–553).
   - The three verdict-conditional routing trailers Shape A / Shape B / Shape C (lines 561–609) — including their two-step `TaskCreate` → `AskUserQuestion` prose, finite-choice lists, Recommended markers, counter-anchor clauses, and `docs/doc-writing-guide.md` references.
   - Step 5e's substantive holistic-judgment review lane (the `SUBSTANCE: critique the overall plan as a coherent solution` framing at lines ~492–502 and the EXPLICITLY-OUT-OF-SCOPE prose-quality exclusions at lines ~503–509) — PRD non-goal.
   - The orchestrator-side findings-surfacing / gate-routing prose (lines ~614–629) — Phase 1 emits depth without consuming it; add no consumption prose there.

   The new emission shape sits INSIDE the numbered findings list, ABOVE the verdict trailer; the trailer and routing shapes are unchanged in form.

## Key Files

- `/Users/jesseg/code/quorum/skills/quo-plan/SKILL.md` — Step 5e embedded dispatch-prompt output contract, lines ~511–530 only (finding-shape contract + worked example). Everything from line 532 onward (verdict trailer, clean shape, Shape A/B/C) and the substance lane at 492–509 stay verbatim.

## Acceptance Criteria

- The embedded dispatch prompt's per-finding contract requires the backtick severity tag + the `target:` tag + per-fix-path depth tags + enumerated fix paths.
- The fix-path line shape is exactly `(<letter>) [depth:<trivial-tweak|refactor-locally|re-architect>] <description>`, matching the cross-Epic contract shared with `t1.ut9.9c` / `t1.ut9.jn` / `t1.ut9.q2` / `t1.ut9.29`.
- Single-path findings (one `(a)` block) are stated as valid; the shape is uniform for 1 or N paths.
- The worked example renders severity + `target:` PLUS depth + fix paths; at least one example per depth bucket AND at least one single-path AND at least one multi-path example appear; the per-bucket calibration set lives in the contract prose, not bloating the example block.
- The verdict trailer line, the verdict semantics, the clean/no-findings shape, and the three verdict-conditional Shape A/B/C routing trailers are byte-for-byte unchanged.
- Step 5e's substantive holistic-judgment lane (lines ~492–509) and the orchestrator-side surfacing/routing prose (lines ~614–629) are unchanged (PRD non-goals; depth is emitted-not-consumed in Phase 1).
- The change is pure prose: language-agnostic, project-neutral, introduces no new shell snippet (no OS-pairing obligation triggered) and no scratch-file writes, per CLAUDE.md's design rules.
- The `Lint` command from CLAUDE.md `## Build Commands` (`python -m pyflakes skills/*/scripts/*.py`) still passes (no helper scripts touched).
