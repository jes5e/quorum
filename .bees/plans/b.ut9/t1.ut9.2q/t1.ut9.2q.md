---
id: t1.ut9.2q
type: t1
title: Align reviewer-subagent wrapper frontmatter with new wrapped-skill emission shape
parent: b.ut9
status: drafted
created_at: '2026-05-20T04:33:46.074976'
schema_version: '0.1'
guid: ut92qsfeqjmandn5bvpisnwgf24x8b5x
---

Update the reviewer-subagent wrapper frontmatter in `agents/code-reviewer.md`, `agents/doc-reviewer.md`, and `agents/test-reviewer.md` to mention the new depth dimension alongside the existing severity-tag mention. The wrappers continue to defer to their wrapped skills' output contracts; this is a pass-through documentation alignment, not a behavioral change.

## Scope

- Three files (`agents/code-reviewer.md`, `agents/doc-reviewer.md`, `agents/test-reviewer.md`) — frontmatter alignment only.
- Reference the extended emission shape from Epics 1, 2, 3 respectively.
- The wrappers' `Responsibility` / `Instructions` blocks (if present) stay unchanged.

## Acceptance criteria

1. All three wrapper files' frontmatter mention severity tags AND per-fix-path depth tags as part of the wrapped-skill output contract.
2. A `grep` for the new depth-dimension language across `agents/*-reviewer.md` returns three hits (one per file).
3. The wrappers' pass-through-to-wrapped-skill behavior is unchanged (no Instructions-block edits).

Out of scope: changes to wrapped-skill emissions (Epics 1, 2, 3), changes to other agent role files (only the three reviewer wrappers are in scope here).

