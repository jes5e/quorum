---
id: t3.ut9.2q.j5.a5
type: t3
title: Align reviewer-wrapper output-shape descriptor sentence to name per-fix-path depth dimension
parent: t2.ut9.2q.j5
reference_materials: null
created_at: '2026-05-29T02:33:04.614007'
status: ready
schema_version: '0.1'
guid: ut92qj5a5xowvy59h3g8h3auecrvmda8
---

## Context

The three reviewer-subagent wrapper files — `agents/code-reviewer.md`, `agents/doc-reviewer.md`, `agents/test-reviewer.md` — are thin pass-through wrappers that dispatch their wrapped review skill (`/quo-engineer-review`, `/quo-doc-writer-review`, `/quo-test-writer-review` respectively) and defer to the wrapped skill's output contract. Each wrapper's Instructions-block output-shape descriptor sentence currently describes that contract as naming severity tags (`blocker` / `suggestion` / `nit`), file:line references, suggested fixes, and a verdict, followed by a "defer to whatever the wrapped skill produces / do not redefine the output shape" clause.

Sibling emission Epics (`t1.ut9.9c` / `t1.ut9.jn` / `t1.ut9.q2`) make the wrapped skills additionally emit a per-fix-path depth dimension (`trivial-tweak` / `refactor-locally` / `re-architect`) plus enumerated fix paths. This Subtask (SR-1.4) makes the wrapper descriptor sentences literal again by naming that depth dimension alongside the existing severity-tag language. This is a pass-through documentation alignment, NOT a behavioral change — the wrappers continue to defer to the wrapped skills' output contracts.

Spec-wording nuance (resolved via the authoritative SDD): the Epic body's "frontmatter alignment" / "no Instructions-block edits" wording is imprecise. The severity-tag mention being aligned lives in each wrapper's Instructions-block output-shape descriptor sentence (line 23 in `code-reviewer.md` and `test-reviewer.md`; line 27 in `doc-reviewer.md`), NOT in the YAML frontmatter `description` (the `description` fields do not mention severity tags). SDD `t1.tip.ux §SR-1.4` is authoritative and references this as the "Output-shape contract at lines 22-23" — i.e., the Instructions-block descriptor sentence. The ONLY edit is adding the depth-dimension phrase to that single sentence; all pass-through behavior stays unchanged.

This is a skill/subagent-prose change in the quorum repo: it must honor the three design rules in CLAUDE.md and the `## Review criteria for skill changes`. The change is pure prose — language-agnostic, project-neutral, with no shell snippets (no OS-pairing obligation) and no scratch-file writes.

## What Needs to Change

In each of `agents/code-reviewer.md`, `agents/doc-reviewer.md`, `agents/test-reviewer.md`, edit the single output-shape descriptor sentence in the Instructions block — the one that currently reads "Return findings to the orchestrator as a structured list consistent with the wrapped skill's existing output contract: severity tags (`blocker` / `suggestion` / `nit`), file:line references, suggested fixes, and a verdict. Do not redefine the output shape — defer to whatever `/quo-<wrapped-skill>` produces.":

- Add a mention of the per-fix-path depth tags (`trivial-tweak` / `refactor-locally` / `re-architect`) and the enumerated fix paths to that sentence, alongside the existing severity-tag mention, as part of the wrapped-skill output contract.
- Keep the existing "Do not redefine the output shape — defer to whatever `/quo-<wrapped-skill>` produces" clause intact — the wrapper still defers; the descriptor now names both the severity dimension and the depth dimension.
- Reference the correct wrapped skill per file (these references already exist in the sentence and must be preserved): `code-reviewer.md` → `/quo-engineer-review`; `doc-reviewer.md` → `/quo-doc-writer-review`; `test-reviewer.md` → `/quo-test-writer-review`.

Do NOT edit anything else in any of the three files:

- The YAML frontmatter (`name` / `description` / `model` / `tools`).
- The Responsibilities block.
- The Cold-start invariant block.
- The `doc-reviewer.md` "Model default and runtime override" block.
- The other Instructions bullets (read scope from dispatch prompt / do not compute scope; invoke the wrapped skill / wrapped skill carries the criteria).

Use the exact depth-tag vocabulary `trivial-tweak` / `refactor-locally` / `re-architect` so the wrapper descriptor stays literal against what the emission Epics make the wrapped skills emit.

## Key Files

- `/Users/jesseg/code/quorum/agents/code-reviewer.md` — edit the output-shape descriptor sentence at line 23 (wrapped skill `/quo-engineer-review`).
- `/Users/jesseg/code/quorum/agents/test-reviewer.md` — edit the output-shape descriptor sentence at line 23 (wrapped skill `/quo-test-writer-review`).
- `/Users/jesseg/code/quorum/agents/doc-reviewer.md` — edit the output-shape descriptor sentence at line 27 (wrapped skill `/quo-doc-writer-review`).

## Acceptance Criteria

- All three wrapper files' output-shape descriptor sentence mentions severity tags AND per-fix-path depth tags (`trivial-tweak` / `refactor-locally` / `re-architect`), and the enumerated fix paths, as part of the wrapped-skill output contract.
- A grep for the new depth-dimension language (e.g., `re-architect` or `refactor-locally`) across `agents/*-reviewer.md` returns three hits (one per file).
- The "Do not redefine the output shape — defer to whatever `/quo-<wrapped-skill>` produces" clause remains present and intact in all three sentences, with the correct per-file wrapped-skill reference.
- The pass-through behavior is unchanged: the Responsibilities block, Cold-start invariant block, `doc-reviewer.md` "Model default and runtime override" block, YAML frontmatter (`name` / `description` / `model` / `tools`), and the other Instructions bullets are all byte-for-byte untouched.
- The change is pure prose: language-agnostic, project-neutral (no reference to this repo's paths, ticket IDs, or internal workflow specifics in the edited sentence beyond the existing `/quo-<wrapped-skill>` reference), no new shell snippets (so no POSIX/PowerShell pairing obligation), and no scratch-file writes — consistent with CLAUDE.md's three design rules and `## Review criteria for skill changes`.
