---
id: t1.jp2.q4
type: t1
title: Epic 2 — Shared fail-open recording guard in the orchestrators; two-option setup gate; opt-out removed from helper, skills, tests, and docs
up_dependencies:
- t1.jp2.z7
parent: b.jp2
reference_materials: null
created_at: '2026-09-03T00:30:26.881405'
status: drafted
schema_version: '0.1'
guid: jp2q4nk7fa9vjpgbhqikxq1rrjz1gc2q
---

## Outcome

All three orchestrators run one shared fail-open, size-aware boundary guard that records every reading; `/quo-setup` offers a two-option producer gate; the opt-out mechanism is gone from helper, skills, tests, and docs; and the cumulative PRD/SDD carry this feature's record.

## Scope

- **Helper opt-out removal** (SDD SR-2.1, here so callers and callee change together): remove `write-opt-out`, `cmd_write_opt_out`, `opt_out_marker_path()`, `OPT_OUT_MARKER_FILENAME`, `OPT_OUT_MARKER_BODY`, the `opt_out_marker_path` / `opt_out_marker_present` inspector keys, and their docstring sections. Tests: delete the nine opt-out tests (`test_opt_out_marker_path_*` ×3, `test_cli_inspect_statusline_reports_the_opt_out_marker`, `test_cli_write_opt_out_*` ×4, `test_cli_write_opt_out_then_inspect_reports_the_marker_present`); edit `test_statusline_constants_are_the_published_contract`, `INSPECT_KEYS` + the assert in `test_cli_inspect_statusline_absent_settings_file`, `test_cli_mutating_subcommands_delete_nothing`, `test_cli_help_names_the_mutating_subcommands`; add `--help` lists no `write-opt-out` and the inspector exact key set.
- **Procedure file** `skills/quo-setup/context-guard.md` (SDD SR-3.1, SR-3.2): purpose paragraph; Step 1 read `CLAUDE_CODE_SESSION_ID` (paired snippets, trim); Step 2 unset → skip, record `skipped`; Step 3 resolve `<this skill's base directory>/../quo-setup/scripts/context_gauge.py`, call `read --session-id <id>`, then `stop-threshold --session-id <id> --units <next-unit-task-count>` (omit `--units` when the invoking skill supplies none); Step 4 **record** — minimal manifest rewrite appending `<id>=<value>@<W_model>` to `**Context readings:**` (copying the existing series forward verbatim; `W_model` taken from `stop-threshold --explain`'s `model_window`) plus the transcript echo `Context reading after <id>: <value>@<W_model> (threshold <t>)` — before any decision; Step 5 branch table per SDD SR-1 (`missing` → one notice line naming `/quo-setup --configure-gauge-producer`, continue; integer ≥ threshold / `stale` / non-zero / empty → stop with `<resume-command>`; `no-reading` / integer < threshold → continue). A short record-only section for run-start, Task-boundary, and run-ending reads: call `read`, call `stop-threshold --session-id <id> --explain` only to obtain `model_window` (discard the integer), record, echo without the threshold, take no decision. The value vocabulary is defined here and only here. Placeholders: `<resume-command>`, `<next-unit-description>`, `<next-unit-task-count>`, `<manifest-path>`. Self-contained; paired snippets; no `docs/*`, `CONTRIBUTING.md`, or `CLAUDE.md ## <section>` references.
- **Orchestrators** (SDD SR-3.3, SR-3.4, SR-5.1–5.3, SR-5.5): shrink each guard section to ≤ ~20 lines — heading, invoking-path paragraph, placeholder values, one `Read` instruction; unreadable file → report path and stop. Task-count supply: `quo-execute` runs `bees show-ticket --ids <next-epic-id>` at the guard and passes `--units <len(children)>`, omitting `--units` when `children` is empty; `quo-fix-issue` passes `--units 1`; `quo-breakdown-epic` omits `--units`. Delete `quo-breakdown-epic`'s "Read `context_gauge.py` at runtime" sentence. Add the per-skill manifest field `- **Context readings:** start=<value>@<W_model>; ...` to each `#### Write the run-state manifest` block without touching the five shared lead statements; the manifest line shows the `<value>@<W_model>` shape and points at `context-guard.md` for the value vocabulary (so the orchestrators never contain the sentinel strings), and states that every rewrite carries the series forward. Add record-only reads: at run start (first manifest write), after each Task in `quo-execute`'s `### 4. Per-Task and Per-Epic Cleanup`, and on every run-ending path in all three.
- **`skills/quo-setup/SKILL.md`** (SDD SR-4.1–4.3): gate becomes **Configure it** / **Not now**; remove skip rule 1 and `#### What the opt-out marker means`; drop opt-out sentences from the fast-path offer and the direct-invocation section (delete the "Opt-out override" paragraph); drop the two keys from the `inspect-statusline` sample and prose; update Act and Next Steps.
- **Docs**: `README.md` — remove opt-out prose (176, 191, 195, 200, 249, 255, 257), state both temp-directory namespaces (helper-written gauge via `tempfile.gettempdir()`, macOS `$TMPDIR`; prose-written scratch files under literal `/tmp/.quorum`; Windows `%TEMP%\.quorum` for both; interim until Issue b.ebw) and reword line 206; `CLAUDE.md` — update bullets 14 and 79, delete the opt-out exception paragraph (81) and the opt-out clause at 85, add `skills/quo-setup/context-guard.md` to the repo-layout bullet as a shipped, sibling-resolved procedure file; `docs/doc-writing-guide.md` — correct the mid-turn sentence (249), update the lockstep list (251: drop `write-opt-out` and the marker path, add `--units` / `--explain` and the procedure file's placeholders), remove the `**Opt-out marker.**` paragraph (253), reduce `## The context-gauge file contract` to contributor rationale plus a pointer to the helper docstring; `docs/sdd.md` / `docs/prd.md` — correct the b.55r subsections' opt-out and threshold statements (629, 631; 369, 378–379) and author this feature's cumulative `### Feature: Simplify the context-window guard` subsection in each, including the derivation formula and the recalibration procedure (SDD SR-5.4).
- **Tests** (SDD SR-8.3): repository test asserting no file under `skills/`, `agents/`, `docs/`, `README.md`, `CLAUDE.md`, `CONTRIBUTING.md` contains `context-guard-opt-out` or `write-opt-out` (`tests/` excluded); procedure-file tests — file exists with paired `# POSIX` / `# Windows (PowerShell)` markers per command; each orchestrator references it by the sibling path and contains none of `no-reading`, `Never guard me`, `write-opt-out`; confirm `tests/test_shipped_artifact_portability.py` covers the new file.

## Out of scope for this Epic

The threshold derivation itself and `CONTRIBUTING.md` (Epic 1); the temp-directory unification (Issue b.ebw); the installer.

## Acceptance criteria

- `grep -c no-reading` on each orchestrator SKILL.md returns 0; each contains exactly one `../quo-setup/context-guard.md` reference; each guard section is ≤ ~20 lines; `quo-execute`'s guard names the `bees show-ticket` Task-count step and `quo-fix-issue`'s names `--units 1`.
- `grep -rn -E 'context-guard-opt-out|write-opt-out|opt_out_marker|Run unguarded|Never guard me' skills agents docs README.md CLAUDE.md CONTRIBUTING.md` returns nothing; `context_gauge.py --help` lists exactly `produce`, `read`, `stop-threshold`, `inspect-statusline`, `install-statusline`; `inspect-statusline` output has no `opt_out_*` keys.
- Each `#### Write the run-state manifest` block carries `**Context readings:**` and the carry-forward rule; a diff of the five shared lead statements across the three files is empty; `quo-execute`'s per-Task cleanup and all three run-ending paths name a record-only read.
- Walkthrough: `read` → `missing` yields one notice line and a continue; `stale` yields a stop naming the resume command; every path writes the reading to the manifest and echoes it first.
- `/quo-setup`'s producer gate prose presents exactly two options; README describes both namespaces and contains no opt-out prose.
- `docs/sdd.md` and `docs/prd.md` each contain exactly one `### Feature: Simplify the context-window guard` subsection; the SDD one includes the recalibration procedure.
- Full test suite passes; pyflakes clean.

