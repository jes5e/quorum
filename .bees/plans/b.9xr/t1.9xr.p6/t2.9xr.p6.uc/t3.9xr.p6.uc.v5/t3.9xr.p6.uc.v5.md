---
id: t3.9xr.p6.uc.v5
type: t3
title: 'Epic A acceptance demos: /bees-status parity, /bees-plan end-to-end smoke, codebase-green check (AC #1, #3, #5)'
up_dependencies:
- t3.9xr.p6.uc.jv
- t3.9xr.p6.xk.7s
- t3.9xr.p6.fs.t8
parent: t2.9xr.p6.uc
egg: null
created_at: '2026-05-02T15:06:14.433552'
status: ready
schema_version: '0.1'
guid: 9xrp6ucv5qy7a7w5svedepv4xfc249jd
---

## Context

Epic A's three remaining acceptance criteria require explicit demonstrations that aren't covered by the AC #2 grep + AC #4 pyflakes gate in Subtask 2.5:

- **AC #1**: `/bees-status` produces output byte-for-byte identical (modulo timestamps) before and after the migration.
- **AC #3**: `/bees-plan` for "Add a hello-world function" creates a Plan Bee + one Epic in the same shape as before.
- **AC #5**: Codebase green — only `skills/`, `docs/`, and `CLAUDE.md` paths touched. The `b.veq` open issue (review skills work) unaffected.

This subtask runs all three demos as the closing gate of Epic A. It depends on every other subtask reaching `done` — Tasks 1, 2 (subtasks 2.1–2.5 plus 2.D1/2.D2), 3, and 4 must all be complete before the demos are meaningful.

## What Needs to Change

- **AC #1 demo.** Capture `/bees-status` output once before this subtask runs and once after Subtasks 2.1–2.5 (and 2.D1/2.D2) have all closed. Diff the two captures, normalizing only timestamp tokens. Confirm zero non-timestamp differences. Document the diff command and result in the commit message.
- **AC #3 demo.** Run `/bees-plan "Add a hello-world function"` end-to-end. Confirm a Plan Bee + one Epic land in the Plans hive with the same JSON shape as a pre-migration baseline (compare via `bees show-ticket --ids <bee>` and `bees show-ticket --ids <epic>` against a captured baseline modulo `ticket_id`, `created_at`, `guid`). Document the comparison in the commit message.
- **AC #5 demo.** Run `git status` and `git diff --name-only main..HEAD` (or equivalent against the Epic's base commit) and confirm every modified path is under `skills/`, `docs/`, or is `CLAUDE.md`. Confirm `bees show-ticket --ids b.veq` still returns the Issue in `open` state with body unchanged. Document both checks in the commit message.

If any demo fails, route the failure back to the appropriate Task (1, 2, 3, or 4) for remediation — do not patch in this subtask.

## Key Files

- (none modified by this subtask in the happy path)

## Acceptance Criteria

- `/bees-status` output byte-for-byte identical (modulo timestamp tokens) before and after the migration. Demonstrated by a `diff` of the two captures with timestamp tokens normalized; zero non-timestamp differences.
- `/bees-plan "Add a hello-world function"` creates a Plan Bee + one Epic with the same JSON shape (modulo `ticket_id`, `created_at`, `guid`) as a pre-migration baseline.
- `git diff --name-only` against the Epic's base commit returns only paths under `skills/`, `docs/`, or `CLAUDE.md`.
- `bees show-ticket --ids b.veq` returns the Issue in `open` state with the body unchanged from before the Epic started.
- The commit message records all three demos' commands and outputs verbatim, satisfying Epic A AC #1, AC #3, and AC #5 in one place.
