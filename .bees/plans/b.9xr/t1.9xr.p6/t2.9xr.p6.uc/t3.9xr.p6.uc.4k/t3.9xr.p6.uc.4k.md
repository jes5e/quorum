---
id: t3.9xr.p6.uc.4k
type: t3
title: Verify the three review skills need no migration
up_dependencies:
- t3.9xr.p6.qf.3v
down_dependencies:
- t3.9xr.p6.uc.jv
parent: t2.9xr.p6.uc
egg: null
created_at: '2026-05-02T15:05:52.645151'
status: ready
schema_version: '0.1'
guid: 9xrp6uc4kqou5d5dfjvd6si35jkfq9nm
---

## Context

The pre-collected inventory shows that `bees-code-review`, `bees-doc-review`, and `bees-test-review` reference "bees ticket" descriptively in prose (e.g., "If a bees ticket ID was passed, derive the file/scope context from the ticket") but contain zero direct `bees <subcommand>` CLI calls. Epic A AC #2 still requires the dispatcher-coverage grep to return empty across `skills/*/SKILL.md`, so these three need to be verified — and any vestigial direct-bees reference (likely none) cleaned up.

## What Needs to Change

- Run a verification grep across all three review skills' `SKILL.md` files to confirm no direct `bees <subcommand>` invocations exist.
- If the grep finds anything, migrate it to the dispatcher using the verb names, flag names, and JSON output shape documented in `ticket_backend.py`'s module docstring as the single source of truth.
- If the grep is empty (expected outcome), document the verification result in the commit message and make no file changes.
- Do not touch the descriptive prose ("a bees ticket", "bees workflow") that refers to the workflow as a concept — that is not a CLI call and should remain.

## Key Files

- `skills/bees-code-review/SKILL.md` — verify, migrate only if vestigial CLI call exists.
- `skills/bees-doc-review/SKILL.md` — verify, migrate only if vestigial CLI call exists.
- `skills/bees-test-review/SKILL.md` — verify, migrate only if vestigial CLI call exists.

## Acceptance Criteria

- The literal verification grep `grep -nE "bees (execute-freeform-query|show-ticket|create-ticket|update-ticket|list-hives|colonize-hive|set-types|set-status-values)" skills/bees-code-review/SKILL.md skills/bees-doc-review/SKILL.md skills/bees-test-review/SKILL.md` returns zero matches.
- If no migration is needed, the commit message records the verification (the literal grep command and its zero-match output) so future maintainers see this Task didn't skip these skills.
- If migration was needed, the changed prose follows CLAUDE.md "Bash etiquette" (single literal command per call) and matches the dispatcher contract.
- Descriptive references to "bees ticket" or "bees workflow" remain unchanged — only CLI call sites are in scope.
