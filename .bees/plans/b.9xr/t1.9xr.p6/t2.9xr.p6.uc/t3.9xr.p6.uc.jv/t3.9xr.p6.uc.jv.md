---
id: t3.9xr.p6.uc.jv
type: t3
title: 'AC #2 dispatcher-coverage grep + AC #4 pyflakes verification gate'
up_dependencies:
- t3.9xr.p6.uc.hj
- t3.9xr.p6.uc.wf
- t3.9xr.p6.uc.or
- t3.9xr.p6.uc.4k
down_dependencies:
- t3.9xr.p6.uc.v5
parent: t2.9xr.p6.uc
egg: null
created_at: '2026-05-02T15:06:09.066855'
status: ready
schema_version: '0.1'
guid: 9xrp6ucjvsfqoeos3gttx97xz6zdx79b
---

## Context

Epic A AC #2 requires `grep -nE "bees (execute-freeform-query|show-ticket|create-ticket|update-ticket|list-hives|colonize-hive|set-types|set-status-values)" skills/*/SKILL.md` returns empty across all eleven skills after the migration. Epic A AC #4 requires `python -m pyflakes skills/_shared/scripts/*.py` exits 0 (extended here to also cover `skills/*/scripts/*.py` since Task 3 may have refactored helpers). This subtask is the final verification gate that confirms Subtasks 2.1–2.4 collectively achieve AC #2 — and catches any straggler call site missed during per-file migration.

## What Needs to Change

- Run the literal AC #2 grep against `skills/*/SKILL.md` (the union of all eleven skills).
- If the grep returns matches, route each match back through the appropriate per-skill subtask (2.1–2.4) for remediation; do not patch in-place inside this verification subtask.
- Run `python -m pyflakes skills/_shared/scripts/*.py skills/*/scripts/*.py` and confirm zero new errors (Epic A AC #4) — this verification subtask is also the right place to gate that lint check, since it's the final cross-cutting smoke before Subtask 2.6 runs the Epic-acceptance demos.
- Document both verification results (the empty grep and the clean pyflakes) in the commit message so the Epic-closing reviewer can confirm AC #2 and AC #4 in one place.

## Key Files

- (none modified by this subtask in the happy path)
- (any straggler skill flagged by the grep would be remediated under its owning subtask 2.1–2.4)

## Acceptance Criteria

- `grep -nE "bees (execute-freeform-query|show-ticket|create-ticket|update-ticket|list-hives|colonize-hive|set-types|set-status-values)" skills/*/SKILL.md` returns zero matches.
- `python -m pyflakes skills/_shared/scripts/*.py skills/*/scripts/*.py` exits 0.
- The commit message records both verification commands and their outputs verbatim.
- No file changes are made by this subtask in the happy path.
