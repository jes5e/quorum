---
id: t3.9xr.p6.xk.ed
type: t3
title: 'Add ## Backend dispatcher subsection to project-root CLAUDE.md'
up_dependencies:
- t3.9xr.p6.qf.3v
down_dependencies:
- t3.9xr.p6.xk.7s
parent: t2.9xr.p6.xk
egg: null
created_at: '2026-05-02T15:05:58.637289'
status: ready
schema_version: '0.1'
guid: 9xrp6xkedqh65nqjjtkfc4x9nkzdckfn
---

## Context

Task 4 of Epic A introduces a new design rule: skill prose and helper scripts must call the ticket backend through `skills/_shared/scripts/ticket_backend.py` rather than shelling out to `bees ...` directly. This rule is reviewable, applies to every skill-prose change going forward, and sets up Epic B's beads branch (which extends the dispatcher with a second backend, invisible to skill prose because routing is absorbed inside the dispatcher). The natural home is project-root `CLAUDE.md`, slotted between `## Bash etiquette in this repo` and `## Review criteria for skill changes` since it shares their concern: how skill code is allowed to talk to the bees CLI. AC #5 of Epic A is relaxed to permit this CLAUDE.md edit (CLAUDE.md is at the repo root, outside `skills/` and `docs/`).

## What Needs to Change

- Add a new `## Backend dispatcher` top-level subsection to `CLAUDE.md` placed between `## Bash etiquette in this repo` and `## Review criteria for skill changes` (so it sits next to the related Bash-etiquette and ticket-querying material without disrupting the contract-keys block that follows).
- The subsection states the new prose pattern: skill prose calls `python3 "<base-dir>/../_shared/scripts/ticket_backend.py" <verb> ...` instead of `bees <subcommand>` directly, with the same `<base-dir>` runtime-resolution convention `docs/doc-writing-guide.md` `## The lookup-key pattern` already documents.
- The subsection lists the seven verbs by name (`query`, `create`, `update`, `show`, `list-spaces`, `setup-spaces`, `resolve-spec`) and references the dispatcher's module docstring as the authoritative source for arg shapes and JSON output shapes — do not restate them in CLAUDE.md (single-sourced).
- The subsection states that direct `bees ...` calls are forbidden in `SKILL.md` prose, with the rationale: keeps skill prose backend-neutral, sets up Epic B (sibling) to introduce the beads branch without touching any skill prose.
- The subsection states that helper scripts under `skills/<name>/scripts/` that need to call the bees CLI must also go through the dispatcher (no direct `subprocess.run(["bees", ...])` either).
- The subsection notes that Epic A is bees-only at the dispatcher level; Epic B (sibling Epic, `t1.9xr.4e`) adds the beads branch and is invisible to skill prose because the dispatcher absorbs the routing — a forward pointer that motivates why the rule exists now even though the second backend doesn't yet.
- Cross-link to `## Bash etiquette in this repo` (which still governs how the dispatcher invocation is shaped at the Bash-tool level — single literal command, no compounds, etc.) and to `## Querying tickets` (which now points at the doc-writing guide's dispatcher-shaped recipes per Subtask 2.D1).

## Key Files

- `CLAUDE.md` (project root) — new `## Backend dispatcher` subsection.
- `skills/_shared/scripts/ticket_backend.py` — module docstring (read-only reference; cited as the authoritative contract).

## Acceptance Criteria

- A new top-level `## Backend dispatcher` subsection is present in `CLAUDE.md`, placed adjacent to `## Bash etiquette in this repo` and before `## Review criteria for skill changes`.
- The subsection prescribes the dispatcher invocation shape using the standard `<base-dir>/../_shared/scripts/...` runtime-resolution convention rather than baking in absolute paths.
- The seven verb names are listed by name; arg shapes and JSON output shapes are explicitly delegated to the dispatcher's module docstring rather than restated.
- The subsection forbids direct `bees ...` calls in both `SKILL.md` prose and `skills/<name>/scripts/` helper scripts, with rationale grounded in backend neutrality and the Epic B handoff.
- The subsection includes a brief forward note that Epic B adds the beads branch inside the dispatcher and is invisible to skill prose by design.
- Existing top-level subsections (`## Bash etiquette in this repo`, `## Review criteria for skill changes`, `## Contract keys ...`, `## Querying tickets`, etc.) are unchanged in this subtask except for any cross-link insertions; their content is preserved verbatim.
