---
id: t3.9xr.p6.qf.3v
type: t3
title: 'Lock down the public contract: lint clean, smoke parity, beads-branch stub error message'
up_dependencies:
- t3.9xr.p6.qf.7s
- t3.9xr.p6.qf.8s
down_dependencies:
- t3.9xr.p6.uc.hj
- t3.9xr.p6.uc.wf
- t3.9xr.p6.uc.or
- t3.9xr.p6.uc.4k
- t3.9xr.p6.uc.t8
- t3.9xr.p6.uc.p2
- t3.9xr.p6.fs.t8
- t3.9xr.p6.xk.ed
parent: t2.9xr.p6.qf
egg: null
created_at: '2026-05-02T15:05:40.963938'
status: done
schema_version: '0.1'
guid: 9xrp6qf3vd5o7oia8kn9e1c7sxhk3ma4
---

## Context

With all seven verbs implemented on the bees branch, this subtask hardens the dispatcher for hand-off to Task 2. The Epic's Acceptance Criteria #4 (lint clean) is demonstrated here as evidence the dispatcher is ready for skill-prose migration. The beads-branch stub error message is also pinned now so Task 2's prose can rely on a stable error string, and Epic B has a clear seam to extend.

This subtask also runs a pair of byte-parity smoke comparisons against this very repo as the final smoke test. AC #1 (`/bees-status` parity) and AC #3 (`/bees-plan` end-to-end smoke) are demonstrated downstream in Task 2's Subtask 2.6 — this subtask is the dispatcher-internal smoke gate.

## What Needs to Change

- Add a single shared "beads not yet supported in this build" error path that every verb's beads branch routes to (a private helper). The error message is stable and includes the verb name, e.g. `ticket_backend.py: backend 'beads' is not supported in this build (verb: <verb>). Set '## Ticket Backend' to 'bees' in CLAUDE.md, or wait for Epic B (b.9xr / t1.9xr.4e).`. Exit code 2 (distinct from bees-CLI propagation, which uses bees' own exit code).
- Run `python -m pyflakes skills/_shared/scripts/*.py` and resolve any warnings.
- Smoke-test the dispatcher against this repo using the canonical "all open Plan Bees" recipe from `docs/doc-writing-guide.md` `## Querying tickets`: run `python3 ticket_backend.py query --query-yaml '<recipe>'` and `bees execute-freeform-query --query-yaml '<recipe>'` for the same recipe and `diff` the two stdout streams. They must be byte-identical.
- Smoke-test `python3 ticket_backend.py list-spaces` against `bees list-hives` the same way: `diff` byte-identical.
- Smoke-test `python3 ticket_backend.py show --ids b.9xr` against `bees show-ticket --ids b.9xr`: `diff` byte-identical.
- Tighten the module docstring with a final once-over: each verb's input flags, stdout JSON shape, stderr/exit-code semantics, and the cross-Task contract pointer ("Tasks 2 and 4 of Plan Bee b.9xr Epic A consume this contract — do not change verb names, flag names, or JSON keys without updating those Tasks").

## Key Files

- `skills/_shared/scripts/ticket_backend.py` — beads-stub error path, docstring final pass.

## Acceptance Criteria

- `python -m pyflakes skills/_shared/scripts/*.py` exits 0 (Epic AC #4).
- Calling any verb with a CLAUDE.md whose `## Ticket Backend` is set to `beads` exits with code 2 and the stable error message above (with the correct verb name interpolated).
- The module docstring's verb table is the authoritative contract: every flag and JSON key Task 2's skill prose will use is documented exactly once and consistently.
- A `diff` between `python3 ticket_backend.py query --query-yaml '<canonical-recipe>'` and `bees execute-freeform-query --query-yaml '<canonical-recipe>'` for the "all open Plan Bees" recipe produces zero output (byte-identical) on this repo.
- A `diff` between `python3 ticket_backend.py list-spaces` and `bees list-hives` produces zero output on this repo.
- A `diff` between `python3 ticket_backend.py show --ids b.9xr` and `bees show-ticket --ids b.9xr` produces zero output on this repo.
- The cross-Task contract pointer is present in the docstring so future contributors editing the dispatcher see immediately that Tasks 2 and 4 read this file as a contract.
