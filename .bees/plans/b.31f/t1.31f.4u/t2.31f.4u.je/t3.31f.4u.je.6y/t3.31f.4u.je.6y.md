---
id: t3.31f.4u.je.6y
type: t3
title: 'Sections 4 and 5: PM dispatch prompts acknowledge bees-resolver + traceability citation format'
up_dependencies:
- t3.31f.4u.je.ad
parent: t2.31f.4u.je
reference_materials: null
created_at: '2026-05-06T19:13:04.098065'
status: done
schema_version: '0.1'
guid: 31f4uje6yrookeauszdq8hgsaj1djaxn
---

## Context

This Subtask is the second of two delivering the `skills/bees-breakdown-epic/SKILL.md` updates for parent Task `t2.31f.4u.je` (Epic `t1.31f.4u`). It covers Sections 4 (per-Task PM dispatch prompt) and 5 (Epic-wide Spec Traceability Review prose embedded in the PM dispatch prompt).

Depends on the sibling Subtask (Section 2 — bees-resolver branch + Scoped-marker skip + polymorphic `reference_materials` reading), which establishes the resolver-branch shape these PM dispatch prompts reference. Sibling Task `t2.31f.4u.j5` (`agents/pm.md`) holds the canonical PM lookup logic; this Subtask updates the breakdown skill's PM dispatch prompts to acknowledge the bees-resolver path and to cite traceability sources accordingly. The dispatch prompts must NOT contradict `agents/pm.md` and must defer to it as the authoritative spec for the lookup logic.

## What Needs to Change

In `skills/bees-breakdown-epic/SKILL.md`:

1. **Section 4 — `#### Per-Task PM dispatch` prompt (PM dispatch boundary).** The existing prose says the PM "reviews the proposed Subtask set against the Epic's spec source (the PRD/SDD linked from the Bee's `reference_materials`, or the Bee body itself when `reference_materials` is null/empty)." Update to:
   - Acknowledge that spec content may come from file-resolver paths OR from Spec Bee children via the bees resolver, depending on what the Plan Bee's `reference_materials` carries.
   - Defer to `agents/pm.md` as the authoritative spec for the resolution logic — do NOT duplicate the title-match / enumerate-children recipe inline. The PM agent's own contract handles it.
   - Preserve the existing Bee-body-as-spec fallback for null/empty `reference_materials`.

2. **Section 5 — Spec Traceability Review prose (Step 3, embedded in the Epic-wide PM dispatch prompt).** The existing prose has a two-branch source identification (`reference_materials` present → cite PRD/SDD section numbers; null/empty → cite Bee body bullets) and a sample traceability table with `Source` column entries `PRD §X / Bee body` and `SDD §Y`. Update to:
   - Recognise three source shapes, branched on resolver kind (mirroring the sibling Subtask's Section 2 changes and `agents/pm.md`):
     - **`reference_materials` with `resolver == "file-path"` (or default)**: cite `PRD §X` / `SDD §Y` as today.
     - **`reference_materials` with `resolver == "bees"`**: cite the Spec Bee child ticket ID and section/bullet within that ticket — e.g., `<prd-child-ticket-id> §<section>` / `<sdd-child-ticket-id> §<section>`. Document the citation format inline so PM agents executing the review use a consistent shape.
     - **`reference_materials` null/empty**: cite Bee body bullets as today.
   - Update the example traceability table to show all three citation forms (file-path, bees, Bee body) so the format is unambiguous.
   - Keep the Step-3 prose verbatim-embeddable in the PM dispatch prompt — it is the PM's review contract.

3. **Cross-skill consistency.** Both the per-Task PM dispatch prompt (Section 4) and the Epic-wide Spec Traceability Review prose (Section 5) must be consistent with `agents/pm.md`'s lookup logic (sibling Task `t2.31f.4u.j5`). If `agents/pm.md` describes the resolver branches one way and these dispatch prompts describe them another, the PM Agent will receive contradictory instructions at run-time — that is a defect.

4. **Preserve existing behaviour.** Section 4's reconciliation-loop scaffolding (TaskList naming, hub-and-spoke, recursive-delegation note, role-selection rules, dispatch shape, research-mode preamble) is unchanged. Section 5's gap-fill iteration loop, Step 1 PM dispatch shape, Step 2 `<scoped-marker-resolver-path>` own-skill resolution, Step 4 gap-fill mechanics, and Step 5 ticket-creation + status-transition mechanics are unchanged.

5. **Skill-prose constraints.** Project-neutral; paired POSIX + PowerShell snippets where applicable; single-literal-command Bash etiquette; no hardcoded this-repo paths or ticket IDs in the example traceability rows (use placeholders like `<prd-child-ticket-id>` and `<section>`).

## Cross-Subtask contracts

- **Depends on the Section 2 sibling Subtask** for the resolver-branch shape and polymorphic `reference_materials` reading. The PM dispatch prompts reference what Section 2 establishes.
- **Mirrors sibling Task `t2.31f.4u.j5` (`agents/pm.md`)** — the dispatch prompts must not contradict the PM agent's own lookup logic. Where this Subtask describes resolver branches, the descriptions must match `agents/pm.md`'s prose. Side-by-side reading must show consistent resolution shape across the two files.

## Key Files

- `skills/bees-breakdown-epic/SKILL.md` — modify Section 4's `#### Per-Task PM dispatch` subsection (the prose describing what the PM reviews against) and Section 5's Step 3 Spec Traceability Review prose (the verbatim review-contract prose embedded in the PM dispatch prompt). All other Section 4 / Section 5 prose is unchanged.

## Acceptance Criteria

- Section 4's `#### Per-Task PM dispatch` prompt acknowledges that spec content may come from file-resolver paths or Spec Bee children via the bees resolver, and defers to `agents/pm.md` as the authoritative spec for the resolution logic. Does not duplicate the title-match / enumerate-children recipe inline.
- Section 4 preserves the existing null/empty `reference_materials` → Bee-body-as-spec fallback.
- Section 5 Step 3's source-identification prose distinguishes three cases: `reference_materials` with `resolver == "file-path"` (cite `PRD §X` / `SDD §Y`); `reference_materials` with `resolver == "bees"` (cite Spec Bee child ticket IDs with section/bullet refs); `reference_materials` null/empty (cite Bee body bullets).
- The example traceability table in Step 3 illustrates all three citation forms so PM agents executing the review have an unambiguous format reference. Citation format is documented inline.
- Section 4 and Section 5 prose mirrors `agents/pm.md`'s lookup logic — no contradictions between the dispatch prompts and the PM agent's own contract. Verifiable by side-by-side reading.
- Sections 4 and 5 retain all unchanged scaffolding: reconciliation loop, TaskList naming convention, research-mode preamble, hub-and-spoke note, recursive-delegation note, gap-fill iteration loop, `<scoped-marker-resolver-path>` own-skill resolution prose, ticket-creation + status-transition mechanics in Step 5.
- Skill prose remains project-neutral; placeholders (e.g., `<prd-child-ticket-id>`, `<section>`) used in example rows rather than this-repo-specific IDs.
- Any shell snippets ship as paired POSIX bash + Windows PowerShell variants.
- Every Bash tool call is a single literal command.
