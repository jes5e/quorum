---
id: t3.31f.4u.3e.gu
type: t3
title: Update bees-execute PM dispatch prose to acknowledge bees-resolver path
parent: t2.31f.4u.3e
reference_materials: null
created_at: '2026-05-06T19:12:52.493406'
status: done
schema_version: '0.1'
guid: 31f4u3egua6ecfa1ombfb63zqwvkp8zy
---

## Context

Parent Task `t2.31f.4u.3e` updates the PM dispatch prompts in the two execution skills so they don't contradict the new two-hop `reference_materials` lookup landing in sibling Tasks (`agents/pm.md` in `t2.31f.4u.j5`, `skills/bees-breakdown-epic/SKILL.md` in `t2.31f.4u.je`). This Subtask is scoped to `skills/bees-execute/SKILL.md` only; the sibling Subtask handles `skills/bees-fix-issue/SKILL.md`, and the doc-writer separately handles `docs/doc-writing-guide.md`.

Today's bees-execute prose at the Product Manager role bullet (currently around line 245) reads:

> reviews the Task's work against the spec source (the PRD/SDD linked from the Bee's `reference_materials`, or the Bee body itself when `reference_materials` is null/empty)

This describes a file-paths-only world. After Epic `t1.31f.4u` lands, `reference_materials` may also use the `bees` resolver shape `[{"value":"<spec-bee-id>","resolver":"bees"}]`, in which case the PM walks the Spec Bee's `t1=Doc` children (PRD and SDD tickets) for spec content. The actual lookup logic lives in `agents/pm.md`; the dispatch prompt's job is just to not contradict it.

## What Needs to Change

Locate the Product Manager role bullet in `skills/bees-execute/SKILL.md` (currently line 245, in the section that lists the four roles dispatched per Task — Engineer, Test Writer, Doc Writer, Product Manager). Update the PM bullet's parenthetical so it:

- Acknowledges that the spec source may resolve via either the `file-path` resolver (PRD/SDD files on disk, the `/bees-plan-from-specs` flow) OR the `bees` resolver (a Spec Bee whose `t1=Doc` children carry the PRD and SDD bodies, the `/bees-plan` flow), OR fall back to the Bee body when `reference_materials` is null/empty.
- Defers to `agents/pm.md` as the authoritative spec for the lookup logic — the dispatch prompt MUST NOT duplicate the resolver-branching prose.
- Keeps the rest of the bullet's structure intact (model assignment, role responsibilities) — only the spec-source parenthetical changes.

Do NOT modify the `Per-Task PM dispatch` subsection or the `Scoped-marker PM dispatch wiring` subsection in this Subtask — Scoped-marker handling is orthogonal to the resolver branch and continues to apply on the `file-path` path. The `agents/pm.md` update (sibling Task `t2.31f.4u.j5`) owns the actual branching logic, including "skip Scoped-marker resolution when resolver is `bees`."

The change is minimal — one bullet's parenthetical phrasing. No new sections, no new shell snippets, no structural reorganization.

## Key Files

- `skills/bees-execute/SKILL.md` — Product Manager role bullet (currently around line 245).

## Acceptance Criteria

- The PM role bullet in `skills/bees-execute/SKILL.md` acknowledges both the `file-path` resolver path and the `bees` resolver path (Spec Bee with `t1=Doc` children) as valid sources for the spec content `reference_materials` may point at.
- The bullet defers to `agents/pm.md` as the authoritative spec for the resolution logic — no duplicated branching prose.
- The body-as-spec fallback (when `reference_materials` is null/empty) is preserved.
- The rest of the SKILL.md (Per-Task PM dispatch, Scoped-marker PM dispatch wiring, role lists, etc.) is unchanged.
- Prose remains project-neutral — no hardcoded this-repo Bee IDs, no language-specific commands, no machine-specific paths.
- No new shell snippets are added; if any existing snippet is touched, it must remain paired POSIX bash + Windows PowerShell.
- Every Bash tool call used while implementing this Subtask is a single literal command (no compounds, pipes between commands, mid-chain redirects, or diagnostic tails).
