---
id: t3.31f.4u.3e.wj
type: t3
title: Update quo-fix-issue PM dispatch prose to acknowledge bees-resolver path
parent: t2.31f.4u.3e
reference_materials: null
created_at: '2026-05-06T19:12:53.585092'
status: done
schema_version: '0.1'
guid: 31f4u3ewjpmbqzyufwqcfy4z1z79whwe
---

## Context

Parent Task `t2.31f.4u.3e` updates the PM dispatch prompts in the two execution skills so they don't contradict the new two-hop `reference_materials` lookup landing in sibling Tasks. This Subtask is scoped to `skills/quo-fix-issue/SKILL.md` only; the sibling Subtask handles `skills/quo-execute/SKILL.md`, and the doc-writer separately handles `docs/doc-writing-guide.md`.

`/quo-fix-issue` deals with Issues, not Plan Bees. Issues have no `reference_materials` field of their own — the spec source for a fix is the Issue body itself, plus PRD/SDD-equivalent paths read directly from CLAUDE.md `## Documentation Locations`, optionally Scoped-marker-narrowed via a Plan Bee found in the Issue's `up_dependencies`. Today's quo-fix-issue prose at the Product Manager role bullet (currently around line 240) describes that flow.

After Epic `t1.31f.4u` lands, when the PM agent walks `up_dependencies` and lands on a Plan Bee, that Plan Bee's `reference_materials` may use the `bees` resolver instead of `file-path`. The PM agent then performs the two-hop lookup (resolve to a Spec Bee, walk its `t1=Doc` children for PRD and SDD bodies). The dispatch prompt's job is to not contradict this — without duplicating the resolver-branching prose, which `agents/pm.md` owns.

## What Needs to Change

Locate the Product Manager role bullet in `skills/quo-fix-issue/SKILL.md` (currently line 240, in the section that lists the roles dispatched per Issue). Update the bullet's parenthetical so it:

- Continues to name the Issue body and the PRD/SDD-equivalent paths from CLAUDE.md `## Documentation Locations` as primary spec sources for a fix.
- Acknowledges that when the PM transitively consults a Plan Bee in the Issue's `up_dependencies` (e.g., to verify the fix doesn't diverge from the originating Plan), that Plan Bee's `reference_materials` may resolve via either the `file-path` resolver or the `bees` resolver — in the latter case, the PM walks the Spec Bee's `t1=Doc` children for PRD and SDD content.
- Defers to `agents/pm.md` as the authoritative spec for the lookup logic — the dispatch prompt MUST NOT duplicate the resolver-branching prose.
- Preserves the existing Complex-vs-Simple dispatch gate language (PM dispatched only for Complex fixes, etc.) — only the spec-source parenthetical changes.

Do NOT modify the `Per-issue PM dispatch` subsection or the `Scoped-marker PM dispatch wiring` subsection. The Path B selection logic (issue-id + `up_dependencies` shape selects Path B in `agents/pm.md`'s Scoped-marker handling) continues to apply on the `file-path` path; the `agents/pm.md` update owns the actual "skip Scoped-marker resolution when resolver is `bees`" behavior.

The change is minimal — one bullet's parenthetical phrasing. No new sections, no new shell snippets, no structural reorganization. Be careful not to over-edit — Issues themselves do not carry `reference_materials`, so prose must not imply they do; the resolver-aware language applies only to a Plan Bee transitively reached via `up_dependencies`.

## Key Files

- `skills/quo-fix-issue/SKILL.md` — Product Manager role bullet (currently around line 240).

## Acceptance Criteria

- The PM role bullet in `skills/quo-fix-issue/SKILL.md` continues to name the Issue body and the CLAUDE.md `## Documentation Locations` paths as primary spec sources.
- The bullet acknowledges that a transitively consulted Plan Bee (reached via the Issue's `up_dependencies`) may carry `reference_materials` using either the `file-path` or `bees` resolver, and that the latter implies a Spec Bee with `t1=Doc` children.
- The bullet defers to `agents/pm.md` as the authoritative spec for the resolution logic — no duplicated branching prose.
- The bullet does NOT imply that Issues themselves carry `reference_materials`.
- Existing Complex-vs-Simple dispatch gate, model assignment, and Path B language are preserved.
- The rest of the SKILL.md (Per-issue PM dispatch, Scoped-marker PM dispatch wiring, complexity-gate prose, etc.) is unchanged.
- Prose remains project-neutral.
- No new shell snippets are added; if any existing snippet is touched, it must remain paired POSIX bash + Windows PowerShell.
- Every Bash tool call used while implementing this Subtask is a single literal command.
