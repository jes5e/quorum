---
id: t3.31f.6w.u8.vp
type: t3
title: Update Doc Writer dispatch bullet in skills/quo-execute/SKILL.md
parent: t2.31f.6w.u8
reference_materials: null
created_at: '2026-05-06T19:26:41.251474'
status: done
schema_version: '0.1'
guid: 31f6wu8vpxdwzuugcdymxreg4tze667w
---

## Context

Parent Task `t2.31f.6w.u8` (Epic `t1.31f.6w`, Bee `b.31f`) extends the Doc Writer dispatch prompts in the two execution skills to acknowledge the cumulative-PRD/SDD-update responsibility being added to `agents/doc-writer.md` by sibling Task `t2.31f.6w.21`. This Subtask delivers the change to the execute-mode skill only; the fix-mode skill is owned by the sibling Subtask.

The relevant Doc Writer role bullet lives in `skills/quo-execute/SKILL.md` (currently around line 244 inside the `#### Roles dispatched by the orchestrator` block). Today's prose reads:

  - **Doc Writer** (`agents/doc-writer.md`) — implements documentation Subtasks and reviews the Engineer's diff for documentation gaps. Model: user's choice (Opus or Sonnet, selected at the start of the run).

The parent Task body specifies the dispatch prompt must (a) acknowledge the cumulative-PRD/SDD-update responsibility, (b) reference the categorization heuristic in `agents/doc-writer.md` without duplicating it, and (c) defer to `agents/doc-writer.md` as the authoritative spec.

## What Needs to Change

Edit `skills/quo-execute/SKILL.md`:

1. Locate the Doc Writer bullet inside `#### Roles dispatched by the orchestrator` (currently around line 244).
2. Extend the bullet's prose to acknowledge that the Doc Writer's responsibilities now include appending or updating `### Feature: <title>` subsections in the project's cumulative PRD and SDD post-implementation, in addition to the existing diff-gap review and customer-facing docs updates.
3. Reference the categorization heuristic in `agents/doc-writer.md` (the four-category table — pure-refactor / architecture-only / deployment-CI / user-facing). Do NOT duplicate the heuristic prose, the `<title>` resolution rule, or the idempotency rule inline in this SKILL.md — point at `agents/doc-writer.md` as the authoritative spec.
4. In execute mode the Doc Writer traverses the Grandparent Bee chain (Subtask → Task → Epic → Plan Bee) to find the Plan Bee whose title sources the `<title>`. The dispatch prompt prose may mention this traversal in a single sentence or rely solely on `agents/doc-writer.md` to spell it out — pick the lighter option, since the contract lives in the role file.
5. Do not modify any other bullet, section header, or shell snippet. The change is one bullet's prose only.

## Constraints

- The prose must remain project-neutral — no this-repo Bee IDs, paths, or workflow specifics baked into the bullet.
- No new shell snippets are required; if any are added they must ship as paired POSIX bash + Windows PowerShell variants.
- Use lookup-key references (e.g., "the `Project requirements doc (PRD)` and `Internal architecture docs (SDD)` keys in CLAUDE.md `## Documentation Locations`") rather than hardcoded `docs/prd.md` / `docs/sdd.md` paths — but again, prefer to defer to `agents/doc-writer.md` rather than restating the lookup-key recipe.
- Every Bash tool call performed during this Subtask is one literal command.
- Use `Edit` rather than `Write` to avoid rewriting the whole file.

## Cross-Subtask contract consumed

- Sibling Task `t2.31f.6w.21` is updating `agents/doc-writer.md` to own the cumulative-doc-update responsibility, the `<title>` resolution rule, the idempotency rule, and the categorization heuristic. This Subtask defers to that file as authoritative.

## Key Files

- `skills/quo-execute/SKILL.md` — Doc Writer bullet inside `#### Roles dispatched by the orchestrator`, currently around line 244.
- `agents/doc-writer.md` — referenced from the bullet (do not modify in this Subtask).

## Acceptance Criteria

- The Doc Writer bullet in `skills/quo-execute/SKILL.md` acknowledges the cumulative-PRD/SDD-update responsibility added by sibling Task `t2.31f.6w.21`.
- The bullet defers to `agents/doc-writer.md` as the authoritative spec for the responsibility.
- The bullet does NOT duplicate the categorization heuristic, the idempotency rule, or the `<title>` resolution recipe — those live in `agents/doc-writer.md`.
- The bullet's prose is project-neutral; no this-repo Bee IDs or hardcoded `docs/prd.md` / `docs/sdd.md` paths.
- No new shell snippets are added; if any are, they ship as paired POSIX bash + Windows PowerShell variants.
- No other bullet, section, or structural element of `skills/quo-execute/SKILL.md` is modified.
- Every Bash tool call performed during this Subtask is a single literal command.
