---
id: t2.31f.6w.u8
type: t2
title: Execution-skill Doc Writer dispatch prompt updates
up_dependencies:
- t2.31f.6w.21
parent: t1.31f.6w
children:
- t3.31f.6w.u8.vp
- t3.31f.6w.u8.we
reference_materials: null
created_at: '2026-05-06T19:22:38.784652'
status: done
schema_version: '0.1'
guid: 31f6wu8ruvg5fvvo38cpvoio8fd44a84
---

## Context

Epic `t1.31f.6w` expands the `doc-writer` agent's responsibilities to include cumulative-PRD/SDD updates post-implementation. Sibling Task 1 of this Epic updates `agents/doc-writer.md` with the canonical contract. This Task delivers the parallel updates to the Doc Writer dispatch prompts in `skills/quo-execute/SKILL.md` and `skills/quo-fix-issue/SKILL.md`.

The execution skills' Doc Writer dispatch prompts today focus on "review the Engineer's diff for doc gaps." Extend the prompts to acknowledge the cumulative-PRD/SDD-update responsibility, so when the Doc Writer is dispatched in execute mode or fix mode, the prompt prose reminds it of the responsibility and points at the categorization heuristic in `agents/doc-writer.md`.

The actual responsibility logic lives in `agents/doc-writer.md` (sibling Task 1); the dispatch prompts only need to NOT contradict the new logic and to point at `agents/doc-writer.md` as authoritative.

## What Needs to Change

Two files modified:

1. **`skills/quo-execute/SKILL.md`** — locate the Doc Writer role bullet in the per-Task role list (where the skill spawns a Doc Writer agent). Update the prose to:
   - Acknowledge that Doc Writer's responsibilities now include appending/updating `### Feature: <title>` subsections in the cumulative PRD and SDD post-implementation, in addition to the existing diff-gap review and customer-facing docs updates.
   - Reference the categorization heuristic in `agents/doc-writer.md` (do NOT duplicate the heuristic prose inline).
   - Defer to `agents/doc-writer.md` as the authoritative spec for the responsibility.

2. **`skills/quo-fix-issue/SKILL.md`** — same change. Locate the Doc Writer role bullet in the per-Issue role list. Apply the same prose update: acknowledge cumulative-doc-update responsibility, reference `agents/doc-writer.md`, defer to it as authoritative.

   Note: `/quo-fix-issue` deals with Issues. The doc-writer in fix mode finds the Plan Bee via the Issue's `up_dependencies` (per sibling Task 1's traversal recipe). Document this in the dispatch prompt prose so the agent knows which Plan Bee to read.

The change is minimal — one bullet's prose per file. No new sections, no new shell snippets, no structural reorganization.

## Why

Without these dispatch-prompt updates, the Doc Writer agents dispatched in execute mode and fix mode would not know they have a new cumulative-doc-update responsibility — they would only do the diff-gap review and customer-facing docs updates as today. The dispatch prompts are the runtime entry point that wires the agent's contract; if they don't acknowledge the new responsibility, it doesn't fire.

## Cross-Task contracts consumed by this Task

- **`agents/doc-writer.md` cumulative-doc-update contract** (sibling Task 1 of this Epic): the dispatch prompts defer to it as authoritative.
- **`<title>` resolution rule, idempotency rule, categorization heuristic** (sibling Task 1): named in the dispatch prompts but not duplicated.

## Key Files

- `skills/quo-execute/SKILL.md` — Doc Writer role bullet in the per-Task role list.
- `skills/quo-fix-issue/SKILL.md` — Doc Writer role bullet in the per-Issue role list.

## Acceptance Criteria

- `skills/quo-execute/SKILL.md`'s Doc Writer role bullet acknowledges the cumulative-PRD/SDD-update responsibility and defers to `agents/doc-writer.md` as the authoritative spec.
- `skills/quo-fix-issue/SKILL.md`'s Doc Writer role bullet has the same update applied. Documents the Plan Bee discovery via Issue's `up_dependencies`.
- Neither dispatch prompt duplicates the categorization heuristic, idempotency rule, or `<title>` resolution recipe — those live in `agents/doc-writer.md`.
- Both files' prose remains project-neutral; no hardcoded this-repo Bee IDs or paths.
- All shell snippets (if any new ones added) ship as paired POSIX bash + Windows PowerShell variants.
- Every Bash tool call is a single literal command.
- The execution-skill changes are minimal — only the Doc Writer role bullet prose is modified, not the skill's main workflow.
