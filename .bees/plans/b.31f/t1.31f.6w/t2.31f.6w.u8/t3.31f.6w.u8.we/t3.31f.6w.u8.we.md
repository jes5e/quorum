---
id: t3.31f.6w.u8.we
type: t3
title: Update Doc Writer dispatch bullet in skills/bees-fix-issue/SKILL.md
parent: t2.31f.6w.u8
reference_materials: null
created_at: '2026-05-06T19:26:42.309440'
status: done
schema_version: '0.1'
guid: 31f6wu8weoj2qon1bihyqio28gx3au8g
---

## Context

Parent Task `t2.31f.6w.u8` (Epic `t1.31f.6w`, Bee `b.31f`) extends the Doc Writer dispatch prompts in the two execution skills to acknowledge the cumulative-PRD/SDD-update responsibility being added to `agents/doc-writer.md` by sibling Task `t2.31f.6w.21`. This Subtask delivers the change to the fix-mode skill only; the execute-mode skill is owned by the sibling Subtask.

The relevant Doc Writer role bullet lives in `skills/bees-fix-issue/SKILL.md` (currently around line 239 inside the `#### Roles dispatched by the orchestrator` block). Today's prose reads:

  - **Doc Writer** (`agents/doc-writer.md`) — reviews the Engineer's diff for documentation gaps and updates customer-facing and internal docs as needed. Model: user's choice (Opus or Sonnet, selected at the start of the run).

The parent Task body adds a fix-mode-specific note: in fix mode the Doc Writer cannot use the execute-mode Grandparent Bee chain because Issues have no parent Bee chain. Instead, the agent finds the Plan Bee (when one exists) via the Issue's `up_dependencies` field. This traversal recipe is owned by `agents/doc-writer.md` (per sibling Task `t2.31f.6w.21`'s acceptance criteria), but the dispatch prompt prose should mention which discovery path applies in fix mode so the agent knows to follow the up_dependencies branch rather than the parent-chain branch.

## What Needs to Change

Edit `skills/bees-fix-issue/SKILL.md`:

1. Locate the Doc Writer bullet inside `#### Roles dispatched by the orchestrator` (currently around line 239).
2. Extend the bullet's prose to acknowledge that the Doc Writer's responsibilities now include appending or updating `### Feature: <title>` subsections in the project's cumulative PRD and SDD post-implementation, in addition to the existing diff-gap review and customer-facing docs updates.
3. Reference the categorization heuristic in `agents/doc-writer.md` without duplicating it inline.
4. Note the fix-mode discovery path: the Plan Bee (when one exists) is found via the Issue's `up_dependencies`, not via a parent-chain traversal. Keep this mention to one sentence — the canonical traversal recipe lives in `agents/doc-writer.md`.
5. Defer to `agents/doc-writer.md` as the authoritative spec for the responsibility.
6. Do not modify any other bullet, section header, or shell snippet. The change is one bullet's prose only.

## Constraints

- The prose must remain project-neutral — no this-repo Issue IDs, paths, or workflow specifics baked into the bullet.
- No new shell snippets are required; if any are added they must ship as paired POSIX bash + Windows PowerShell variants.
- Use lookup-key references (CLAUDE.md `## Documentation Locations` keys) rather than hardcoded paths — and prefer to defer to `agents/doc-writer.md` rather than restating the lookup-key recipe.
- Every Bash tool call performed during this Subtask is one literal command.
- Use `Edit` rather than `Write`.
- Acknowledge that in fix mode the Doc Writer is dispatched on a per-Issue basis (not a per-Subtask basis as in execute mode); the cumulative-doc update happens once per Issue when applicable, not per Subtask.

## Cross-Subtask contract consumed

- Sibling Task `t2.31f.6w.21` is updating `agents/doc-writer.md` to own the cumulative-doc-update responsibility, including the fix-mode `up_dependencies` traversal. This Subtask defers to that file as authoritative.

## Key Files

- `skills/bees-fix-issue/SKILL.md` — Doc Writer bullet inside `#### Roles dispatched by the orchestrator`, currently around line 239.
- `agents/doc-writer.md` — referenced from the bullet (do not modify in this Subtask).

## Acceptance Criteria

- The Doc Writer bullet in `skills/bees-fix-issue/SKILL.md` acknowledges the cumulative-PRD/SDD-update responsibility added by sibling Task `t2.31f.6w.21`.
- The bullet documents the fix-mode Plan Bee discovery path (Issue's `up_dependencies`) in one sentence, so the dispatched agent knows which discovery branch applies; the canonical traversal recipe stays in `agents/doc-writer.md`.
- The bullet defers to `agents/doc-writer.md` as the authoritative spec.
- The bullet does NOT duplicate the categorization heuristic, the idempotency rule, or the `<title>` resolution recipe — those live in `agents/doc-writer.md`.
- The bullet's prose is project-neutral; no this-repo Issue IDs or hardcoded `docs/prd.md` / `docs/sdd.md` paths.
- No new shell snippets are added; if any are, they ship as paired POSIX bash + Windows PowerShell variants.
- No other bullet, section, or structural element of `skills/bees-fix-issue/SKILL.md` is modified.
- Every Bash tool call performed during this Subtask is a single literal command.
