---
id: t3.31f.y2.uw.1h
type: t3
title: '/quo-plan Step 5a: bees-resolver reference_materials, drop Scoped-marker, body simplification'
down_dependencies:
- t3.31f.y2.uw.a8
parent: t2.31f.y2.uw
reference_materials: null
created_at: '2026-05-06T18:52:57.557500'
status: done
schema_version: '0.1'
guid: 31fy2uw1h322dicy1by5kfysh4tgg2z3
---

## Context

Parent Task `t2.31f.y2.uw` redesigns `/quo-plan` Step 5 (Plan Bee creation) and Step 7 (commit logic) to align with the Spec Bee + `bees`-resolver pattern adopted in parent Bee `b.31f`'s Decisions section. This Subtask owns Step 5a's three tightly-coupled sub-changes (the resolver-shape switch, the Scoped-marker drop, and the body simplification all edit the same Step 5a prose region and must land together to leave the skill in a coherent state). The sibling Subtask owns the Step 7 commit-logic verification.

Sibling Tasks (do NOT modify their files):
- `t2.31f.y2.9q` — Step 4 redesign (Spec Bee creation, delegated authoring). Lands the Spec Bee whose ID this Subtask will plug into Step 5a's `--reference-materials`.
- `t2.31f.y2.o7` — Steps 1/2 mid-conversation context awareness.

Cross-Epic contracts consumed:
- Specs hive `bees` resolver allowed (from sibling Epic `t1.31f.ho`'s Subtask `t3.31f.ho.8h.hx`). The Specs hive will accept `[{"value":"<spec-bee-id>","resolver":"bees"}]` in `reference_materials` by the time this Subtask is executed.
- Plan Bee body shape parity with parent Bee `b.31f` (which was authored in this style as a self-bootstrap example) — brief 2-3 sentence summary plus `## Anticipated doc impact` section.

## What Needs to Change

Edit `skills/quo-plan/SKILL.md` Step 5a (lines covering the temp-file authoring guidance, the reference_materials variants, and the example bees create-ticket invocations). Make the following changes in one logical edit pass:

1. **Switch `--reference-materials` to the `bees`-resolver shape.** Replace today's two variants (PRD/SDD file paths via the implicit `file-path` resolver, and the no-`reference-materials` body-as-spec branch) with a single shape:

   `--reference-materials '[{"value":"<spec-bee-id>","resolver":"bees"}]'`

   The `<spec-bee-id>` placeholder is the Spec Bee created in Step 4 (sibling Task `t2.31f.y2.9q`'s scope). Add prose noting the value comes from Step 4. Keep the `--body-file` / temp-file authoring guidance (the etiquette about command-injection guards and shell quoting still applies).

2. **Drop the file-paths variant entirely.** Remove the `[{"value":"<prd path>"},{"value":"<sdd path>"}]` shape, the PRD-then-SDD positional-order convention prose, and the relative-vs-absolute-paths discussion. Add a one-sentence pointer noting that the file-paths shape moves to `/quo-plan-from-specs` exclusively (which already supports it). Do NOT remove the body-as-spec fallback prose entirely — instead, replace it with prose explaining that `/quo-plan` always sets `reference_materials` to the Spec Bee now, so the body-as-spec variant no longer applies on this skill's path. If any user is still on the old behavior, they should re-run after Step 4 lands.

3. **Drop the Scoped-marker emission.** Today's Step 5a does NOT explicitly emit a `Scoped to '### Feature: <title>' from <prd-path> and <sdd-path>.` marker line in the Plan Bee body — that marker is currently emitted only by `/quo-plan-from-specs --feature` mode. Re-read Step 5a carefully and confirm this is the case. If the marker IS emitted by `/quo-plan` (e.g., in a body-template snippet), remove it. If it is NOT emitted, add a one-line clarifying note in Step 5a that `/quo-plan` does NOT emit a Scoped-marker line (because `/quo-plan` no longer co-mingles content into shared docs after this Epic), and that the Scoped-marker contract documented in `docs/doc-writing-guide.md` remains valid for `/quo-plan-from-specs --feature` only.

4. **Plan Bee body simplification.** Replace the existing body-shape guidance in Step 5a with a brief 2-3 sentence summary template plus an `## Anticipated doc impact` section. The template should match parent Bee `b.31f`'s own body shape (read it via `bees show-ticket --ids b.31f` if needed for a concrete example):
   - 2-3 sentence summary: what the feature is and the high-level scope.
   - `## Anticipated doc impact` section listing which cumulative project docs (PRD at the path in CLAUDE.md `## Documentation Locations`, SDD at the same, README, and any other customer-facing docs) Epic 6 (post-impl doc-writer) will need to update. Keep the language project-neutral — reference the contract keys in CLAUDE.md, do not hardcode `docs/prd.md` etc. into the skill prose (per repo design rule 1).
   - No more PRD/SDD content in the body. No more Scoped-marker line.

5. **Spec Bee status gating.** No code change needed — the existing Step 5d behavior (transition Plan Bee from `drafted` to `ready` after Epics are wired) already runs strictly after Step 4 (sibling Task `t2.31f.y2.9q` transitions the Spec Bee to `ready` at the end of Step 4). Add a one-sentence prose note in Step 5d clarifying that the Plan Bee's `ready` transition is gated on the Spec Bee being `ready`, which Step 4 ensures.

6. **Skill-prose constraints (non-negotiable per this repo's CLAUDE.md):**
   - Project-neutral. No language-specific commands, no hardcoded `docs/prd.md` / `docs/sdd.md` paths embedded in skill prose (use the CLAUDE.md `## Documentation Locations` lookup-key pattern).
   - Every shell snippet ships as paired POSIX bash + Windows PowerShell variants.
   - One literal command per Bash-tool example. No compound chains, no pipes, no heredocs, no shell-variable expansion, no command substitution, no backticks-in-double-quotes.
   - Scratch-file convention: any temp file goes under `<tempdir>/.quorum/`, no `rm` / `Remove-Item` instructions.

## Why

Parent Bee `b.31f`'s Decisions section ("Adopt apiary's Spec Bee + `reference_materials` pattern") commits to using the `bees` resolver to point Plan Bees at Spec Bees, which preserves the polymorphism the bees CLI already supports for `reference_materials` and matches apiary's cross-tooling parity. This Subtask is where that decision lands in `/quo-plan`'s Step 5a prose.

Dropping the Scoped-marker emission removes the cumulative-doc-coupling overhead the parent Bee's Problem 1 calls out. With `/quo-plan` no longer co-mingling content into shared docs, the marker is unnecessary on this path; `/quo-plan-from-specs --feature` retains it for hand-authored cumulative-doc flows.

Body simplification mirrors apiary's Plan Bee shape and matches the parent Bee `b.31f`'s own body (which was authored in this style as a self-bootstrap example), keeping the documentation surface area small now that `reference_materials` carries the spec content.

## Key Files

- `skills/quo-plan/SKILL.md` — modify Step 5a (and a one-line addition in Step 5d for the Spec Bee `ready`-gating note).

## Acceptance Criteria

- `skills/quo-plan/SKILL.md` Step 5a creates the Plan Bee with `--reference-materials '[{"value":"<spec-bee-id>","resolver":"bees"}]'`. The PRD/SDD file-paths variant is removed; a one-sentence pointer notes the file-paths shape moves to `/quo-plan-from-specs` exclusively.
- `skills/quo-plan/SKILL.md` Step 5a does not emit a `Scoped to '### Feature: <title>' from <prd-path> and <sdd-path>.` marker line in any Plan Bee body template. A clarifying note in Step 5a explicitly says `/quo-plan` does not emit a Scoped-marker (the marker remains valid for `/quo-plan-from-specs --feature` only).
- The Plan Bee body template documented in Step 5a is a brief 2-3 sentence summary plus an `## Anticipated doc impact` section that references CLAUDE.md `## Documentation Locations` contract keys (PRD, SDD, customer-facing docs) rather than hardcoded paths.
- Step 5d includes a one-line note that the Plan Bee's `ready` transition is gated on the Spec Bee being `ready`, which Step 4 (sibling Task `t2.31f.y2.9q`) ensures.
- All shell snippets in the modified Step 5a/5d ship as paired POSIX bash + Windows PowerShell variants.
- Every Bash example in the modified Step 5a/5d is a single literal command.
- Skill prose remains project-neutral — no language-specific commands, no hardcoded `docs/prd.md` paths embedded in the prose.
- Steps 1, 2, 3, 4, 6, and 7 of `/quo-plan` are NOT modified by this Subtask.
- Lint passes: `python -m pyflakes skills/*/scripts/*.py` (per CLAUDE.md `## Build Commands`).
