---
id: t3.31f.y2.9q.42
type: t3
title: Strip /quo-plan Step 4 doc-mutation prose
down_dependencies:
- t3.31f.y2.9q.p4
parent: t2.31f.y2.9q
reference_materials: null
created_at: '2026-05-06T18:52:54.818099'
status: done
schema_version: '0.1'
guid: 31fy29q42z38fgu23fe8sgzap5cuuk72
---

## Context

Parent Task `t2.31f.y2.9q` redesigns Step 4 of `/quo-plan` to drop plan-time doc mutation in favor of delegated spec authoring (Spec Bee + `/quo-write-prd` + `/quo-write-sdd`). The redesign happens in two halves: this Subtask removes the existing doc-mutation prose so the Step 4 region is empty and ready for the new flow; sibling Subtasks then layer the new flow in.

Today's Step 4 (`skills/quo-plan/SKILL.md`) contains:
- A `### 4. Determine Doc Updates` heading.
- A `#### 4a — If PRD/SDD exist` branch with the feature-categorization table (Product / Architecture-reliability / Deployment-infra / CI-testing / Internal-refactor) and a draft-and-apply flow that writes new `### Feature: <title>` subsections to `docs/prd.md` and `docs/sdd.md`.
- A `#### 4b — If PRD/SDD don't exist` branch with two options: seed PRD/SDD now from the feature scope (writing `docs/prd.md`, `docs/sdd.md`, and updating CLAUDE.md `## Documentation Locations`), or skip with body-as-spec.
- An always-check-the-README directive embedded in 4a.
- A user-approval gate confirming proposed doc updates before applying.

All of the above must go. Per the parent Task body, the README-update logic moves to sibling Epic `t1.31f.6w`'s doc-writer post-impl flow (NOT this Task's scope to re-author — only to remove from Step 4 here). The PRD/SDD write logic moves to the new `/quo-write-prd` and `/quo-write-sdd` skills (sibling Epics `t1.31f.by` and `t1.31f.5u`).

This Subtask is purely deletion + a placeholder marker. The new Step 4 prose lands in sibling Subtasks (Spec Bee creation, Skill-tool invocations, Spec Bee promotion).

## What Needs to Change

Edit `skills/quo-plan/SKILL.md`:

1. **Delete the entire `### 4. Determine Doc Updates` section.** This includes the section heading, the introductory paragraph ("First, check whether the project has PRD/SDD configured…"), the `#### 4a` and `#### 4b` subsections in full, the feature-categorization table, the always-check-the-README paragraph, the user-approval gate, the seeded-PRD and seeded-SDD markdown templates, the `mkdir -p docs` / `New-Item -ItemType Directory -Force -Path docs` snippets, and the CLAUDE.md `## Documentation Locations` update instructions.

2. **Leave a placeholder section heading + brief stub** that sibling Subtasks will fill in. Use a one-line stub like:

   ```markdown
   ### 4. Author Specs

   <!-- Filled in by sibling Subtasks: create Spec Bee, invoke /quo-write-prd and /quo-write-sdd via the Skill tool, promote Spec Bee to ready. -->
   ```

   The stub heading + comment is intentional — it preserves the section's anchor in the document outline (Steps 1, 2, 3 above and Steps 5, 6, 7 below remain numbered correctly) and gives sibling Subtasks an obvious insertion point. The stub comment can also be deleted by the last sibling Subtask if desired.

3. **Step 4 references elsewhere in the file.** Audit the rest of the SKILL.md for cross-references to the deleted Step 4 content. Notable spots that may need touch-ups (verify before editing):
   - Step 2's prose: `"If they say no or the docs don't exist, skip all doc-related steps later (step 4)…"` — the redirect to Step 4 may need updating once the new behavior lands, but for THIS Subtask's purposes leave the prose alone if the cross-reference still parses ("step 4" still exists, just emptied). Sibling Subtasks adjust this if needed when they author the new Step 4.
   - Step 5a's prose: `"If PRD/SDD exist for this feature (Step 4a, or Step 4b option 1 where you just drafted them)…"` — this whole branch is logically obsolete because the new Step 4 always produces a Spec Bee with PRD+SDD children. Leave the prose as-is in THIS Subtask; sibling Subtasks (or sibling Task `t2.31f.y2.uw`'s Plan Bee creation update) handle the Step 5 rewrite. The cross-reference parses (Step 4 still exists), so no breakage.

4. **Steps 1, 2, 3, 5, 6, 7 stay byte-identical** apart from the cross-reference touch-ups above (which this Subtask explicitly defers).

5. **No new prose, no new Bash snippets in this Subtask.** Pure deletion + the stub.

## Key Files

- `skills/quo-plan/SKILL.md` — delete the `### 4. Determine Doc Updates` section in full (subsections 4a and 4b, the table, the user-approval gate, the seeded-doc templates, the directory-creation snippets, the CLAUDE.md update instructions). Replace with a stub `### 4. Author Specs` heading + HTML-comment placeholder.

## Acceptance Criteria

- `git diff` on `skills/quo-plan/SKILL.md` shows the entire `### 4. Determine Doc Updates` section deleted: the section heading, both `#### 4a` and `#### 4b` subsections, the feature-categorization table, the README directive, the user-approval gate, the seeded-PRD/seeded-SDD markdown templates, the `mkdir -p docs` / `New-Item` directory-creation snippets, and the CLAUDE.md `## Documentation Locations` update instructions.
- The diff shows a single replacement stub `### 4. Author Specs` heading with an HTML-comment marker (or equivalent) noting that sibling Subtasks fill in the section.
- Steps 1, 2, 3, 5, 6, 7 are byte-identical to before this Subtask. Verifiable by `git diff` showing changes confined to the Step 4 region.
- No new Bash snippets are added by this Subtask.
- The skill prose remains project-neutral.
- After this Subtask lands, `/quo-plan` is in a temporarily-broken state: the workflow has a placeholder Step 4 that does nothing. This is intentional and documented inline via the stub comment so reviewers/maintainers reading the file mid-stack understand the deferred state. Sibling Subtasks (Spec Bee creation, Skill-tool invocations, Spec Bee promotion) restore working behavior.
- Project-level lint passes: `python -m pyflakes skills/*/scripts/*.py` succeeds.
