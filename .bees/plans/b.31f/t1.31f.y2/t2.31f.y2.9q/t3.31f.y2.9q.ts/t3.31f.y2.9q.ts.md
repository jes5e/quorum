---
id: t3.31f.y2.9q.ts
type: t3
title: 'Add new /quo-plan Step 4: promote Spec Bee drafted->ready after writers return'
up_dependencies:
- t3.31f.y2.9q.k9
parent: t2.31f.y2.9q
reference_materials: null
created_at: '2026-05-06T18:53:12.509043'
status: done
schema_version: '0.1'
guid: 31fy29qts1br73a8y47tsfpfc5ywbni7
---

## Context

The Spec Bee detect-or-create and the Skill-tool invocation sibling Subtasks land the bulk of the new Step 4. This Subtask closes the loop: after both writer skills return with their PRD and SDD child tickets in `ready` state, transition the Spec Bee itself from `drafted` to `ready` so downstream consumers (the Plan Bee's `reference_materials` and `bees`-resolver lookups in sibling Task `t2.31f.y2.uw`'s Step 5/7 work) see a Spec Bee in `ready` state.

Cross-Task contracts produced by this Subtask:
- **Spec Bee `drafted → ready` gating** — the Spec Bee transitions to `ready` only after BOTH PRD and SDD child tickets are `ready`. Sibling Task `t2.31f.y2.uw` (Plan Bee creation update) consumes this — the Plan Bee's `reference_materials` points at the Spec Bee, and the Plan Bee's transition to `ready` indirectly gates on the Spec Bee's `ready` state.

Final Spec Bee status check (defensive): the writer skills' inline-invocation contracts (sibling Subtasks `t3.31f.by.7h.xa` and `t3.31f.5u.f3.pt`) guarantee that on successful return, the PRD/SDD child tickets are in `ready`. This Subtask's prose can rely on that guarantee, but should still defensively check the captured final-status values from the previous sibling's output capture before promoting the Spec Bee — defense-in-depth against a writer-contract regression.

## What Needs to Change

Edit `skills/quo-plan/SKILL.md`. Add the Spec Bee promotion sub-step at the end of Step 4, after the `/quo-write-sdd` invocation and before Step 5's heading.

1. **Sub-step: Promote the Spec Bee.**

   - Document a defensive check: confirm that both the PRD final status and the SDD final status captured in the previous sibling sub-step are `ready` before promoting the Spec Bee. If either is not `ready`, surface the discrepancy as an error and do not promote (the writer-skill contracts guarantee `ready` on successful return, so a non-`ready` status here indicates a contract regression that must be investigated rather than papered over).
   - When both are `ready`, transition the Spec Bee from `drafted` to `ready`:

     ```bash
     # POSIX (bash / zsh):
     bees update-ticket --ids <spec-bee-id> --status ready
     ```

     ```powershell
     # Windows (PowerShell):
     bees update-ticket --ids <spec-bee-id> --status ready
     ```

     The bees CLI's update-ticket command is the same shape on both platforms here — no platform-specific syntax difference. Pair the snippets anyway per design rule 2 so the SKILL.md's structure stays uniform.

2. **Idempotent re-runs.** Document inline that if the Spec Bee is already in `ready` state at the start of this sub-step (e.g., a re-run of `/quo-plan` where the Spec Bee was already promoted in a prior run and the writer skills returned no-op updates), the `bees update-ticket --status ready` call is harmless — bees treats setting status to its current value as a no-op (the bees CLI's update-ticket only changes provided fields and accepts re-asserting an existing value). The implementer can optionally short-circuit the call when already-`ready` is detected, but it is not required.

3. **End-of-Step-4 summary.** Add a brief inline summary at the end of Step 4 stating what the user should now see: a Spec Bee in `ready` state with PRD and SDD `t1=Doc` children both in `ready` state. This orient-the-reader paragraph mirrors Step 5e's existing report pattern and helps the user verify Step 4 succeeded before proceeding to Step 5.

4. **Cross-reference forward to Step 5.** Add a one-sentence note that Step 5's Plan Bee creation (modified by sibling Task `t2.31f.y2.uw`) will set the Plan Bee's `reference_materials` to a `bees`-resolver pointing at the Spec Bee ID this sub-step just promoted. This is the explicit hand-off point between Tasks under the Epic. Place this forward-reference in the end-of-Step-4 summary section above; the previous sibling Subtask should NOT also emit a separate forward-reference.

5. **Stub cleanup.** If the strip Subtask left an HTML-comment stub under the `### 4. Author Specs` heading (`<!-- Filled in by sibling Subtasks: ... -->`), remove that comment now that all three sibling Subtasks have populated their pieces. The Step 4 section is now complete and the placeholder marker is no longer load-bearing.

6. **Scope discipline — do NOT modify Steps 1, 2, 3, 5, 6, 7.** This Subtask's edits are confined to the Step 4 region of `skills/quo-plan/SKILL.md`. Notable cross-reference touch-ups elsewhere in the file are explicitly OWNED BY OTHER SUBTASKS:
   - **Step 2's `(step 4)` cross-reference** (`"If they say no or the docs don't exist, skip all doc-related steps later (step 4)…"`) — owned by sibling Task `t2.31f.y2.o7`'s Subtask `t3.31f.y2.o7.qn`, which already rewrites Steps 1-2 for mid-conversation awareness. The conditional-skip wording becomes nonsensical after this Epic lands (Step 4 no longer branches on PRD/SDD presence; it always creates a Spec Bee), and `qn` absorbs that cleanup.
   - **Step 5a's `"If PRD/SDD exist for this feature (Step 4a, or Step 4b option 1 where you just drafted them)…"`** — sibling Task `t2.31f.y2.uw`'s Subtask `t3.31f.y2.uw.1h` rewrites Step 5a (Plan Bee creation with `bees`-resolver `reference_materials`) and absorbs that cross-reference rewrite.
   - **Step 7's `docs/` staging logic** — sibling Task `t2.31f.y2.uw`'s Subtask `t3.31f.y2.uw.a8` verifies and updates Step 7's commit logic.

   This Subtask MUST NOT touch any of these regions. Verifiable by `git diff` showing changes confined to Step 4 only.

7. **Constraint compliance.** Paired POSIX bash + Windows PowerShell snippets. Single-literal-command Bash etiquette. No `rm` / `Remove-Item` cleanup. Project-neutral prose.

## Key Files

- `skills/quo-plan/SKILL.md` — add the Spec Bee `drafted → ready` promotion sub-step at the end of Step 4. Remove the placeholder stub comment from the strip Subtask. Add an end-of-Step-4 summary with a forward-reference to Step 5's Plan Bee creation.

## Acceptance Criteria

- `skills/quo-plan/SKILL.md` Step 4 contains a final sub-step that confirms both PRD and SDD final statuses are `ready` (defensive check) and then transitions the Spec Bee from `drafted` to `ready` via `bees update-ticket --ids <spec-bee-id> --status ready`.
- The defensive-check rationale ("writer-skill contracts guarantee `ready` on successful return") is documented inline so future maintainers understand why the check is there.
- Idempotent re-runs are documented: if the Spec Bee is already `ready`, the `bees update-ticket` call is a harmless no-op.
- An end-of-Step-4 summary tells the user what they should see after Step 4 succeeds: Spec Bee in `ready`, PRD and SDD `t1=Doc` children both in `ready`.
- A one-sentence forward-reference to Step 5's Plan Bee `reference_materials` wiring (a `bees`-resolver pointing at the Spec Bee just promoted) appears inline in the end-of-Step-4 summary.
- The placeholder stub comment from the strip Subtask (if any) is removed — Step 4 is now complete.
- **Steps 1, 2, 3, 5, 6, 7 are NOT modified by this Subtask.** The Step 2 `(step 4)` cross-reference cleanup is owned by sibling Subtask `t3.31f.y2.o7.qn`. The Step 5a and Step 7 cross-reference rewrites are owned by sibling Subtasks `t3.31f.y2.uw.1h` and `t3.31f.y2.uw.a8`. Verifiable by `git diff` showing changes confined to Step 4 only.
- All shell snippets ship as paired POSIX bash + Windows PowerShell variants.
- Every Bash tool call shown is a single literal command.
- No `rm` / `Remove-Item` cleanup is instructed.
- Skill prose remains project-neutral.
- Project-level lint passes: `python -m pyflakes skills/*/scripts/*.py` succeeds.
- After this Subtask lands, `/quo-plan`'s Step 4 is fully restored to working state: Spec Bee created or reused, PRD and SDD authored via Skill-tool invocations, Spec Bee promoted to `ready`. The acceptance criteria from parent Task `t2.31f.y2.9q` are met.
