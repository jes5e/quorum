---
id: t3.31f.y2.9q.k9
type: t3
title: 'Add new /bees-plan Step 4: invoke /bees-write-prd and /bees-write-sdd via Skill tool'
up_dependencies:
- t3.31f.y2.9q.p4
down_dependencies:
- t3.31f.y2.9q.ts
parent: t2.31f.y2.9q
reference_materials: null
created_at: '2026-05-06T18:53:08.393427'
status: done
schema_version: '0.1'
guid: 31fy29qk95c5bqcy6j78mgbkwmrkdhc1
---

## Context

The Spec-Bee detect-or-create sibling Subtask lands the first piece of the new Step 4. This Subtask layers on the delegated spec authoring: invoke `/bees-write-prd` and then `/bees-write-sdd` via the Skill tool, passing the Spec Bee ID and the conversation-distilled scope, and capturing the returned PRD/SDD ticket IDs and final statuses.

Cross-Task contracts consumed (READ THESE BEFORE IMPLEMENTING):
- **`/bees-write-prd` Skill-tool inline-invocation contract** — defined in sibling Epic `t1.31f.by`'s Subtask `t3.31f.by.7h.xa`. The contract specifies:
  - **Input shape (caller → skill):** Spec Bee ID + distilled scope content from the planning conversation. The exact shape (free-text args string vs structured JSON-in-prose) is the writer-skill author's call but is documented as a stable contract in `skills/bees-write-prd/SKILL.md`'s `## Inline invocation via the Skill tool` section.
  - **Output shape (skill → caller):** the PRD ticket ID + final status (`drafted` vs `ready`).
  - **Behavioral guarantee:** when invoked inline, the skill always takes the distill branch (because `.y2`'s Step 4 always passes substantive distilled scope), produces the twelve required PRD sections, and transitions the PRD ticket from `drafted` to `ready` before returning.
- **`/bees-write-sdd` Skill-tool inline-invocation contract** — defined in sibling Epic `t1.31f.5u`'s Subtask `t3.31f.5u.f3.pt`. Same input/output shape as `/bees-write-prd`, deliberately mirrored so this Step 4 can dispatch both writers with identical args. Same behavioral guarantees apply (distill branch always taken, seven required SDD sections, `drafted → ready` transition before return).

**The Skill-tool invocation shape is `Skill(skill="<name>", args="<args>")`** per Claude Code's harness. The exact `args` payload encoding is whatever the writer-skill SKILL.md prose documents — read both `skills/bees-write-prd/SKILL.md` and `skills/bees-write-sdd/SKILL.md` (after their parent Subtasks land) and pass args verbatim per their documented input shape.

Error handling (parent Task body item 5): if either writer skill returns an error (e.g., the user cancels the approval gate inside the writer), `/bees-plan`'s Step 4 must surface the error and not proceed to subsequent Steps. The Spec Bee remains in `drafted` so the user can re-run.

## What Needs to Change

Edit `skills/bees-plan/SKILL.md`. Add the Skill-tool invocation prose immediately after the Spec Bee detect-or-create sub-step landed by the previous sibling Subtask, and before the Spec Bee promotion that the final sibling Subtask adds.

1. **Sub-step: Invoke `/bees-write-prd`.**

   - Use the Skill tool inline: `Skill(skill="bees-write-prd", args="<args per the contract in t3.31f.by.7h.xa>")`. The exact `args` payload depends on what `skills/bees-write-prd/SKILL.md`'s `## Inline invocation via the Skill tool` section documents as the canonical input shape; this Step 4 prose must follow that shape verbatim. Document the shape inline (so a reader of `/bees-plan`'s SKILL.md alone understands the handoff without having to read the writer SKILL.md), but defer to the writer SKILL.md as the authoritative spec.
   - At minimum, the args carry: (a) the Spec Bee ID captured in the previous sibling Subtask, (b) the distilled scope content from Steps 2 and 3 of `/bees-plan` (the conversation-distilled scope statement plus any prior-context the user shared in Step 1).
   - Capture the returned PRD ticket ID and final status. Per the writer's contract, the final status is `ready` on success.
   - **Error handling.** If the Skill tool call returns an error or the writer reports a non-`ready` final status, surface the error to the user and abort Step 4. Document inline that the Spec Bee remains in `drafted` so the user can re-run `/bees-plan` and pick `Reuse existing Spec Bee` from the heuristic prompt.

2. **Sub-step: Invoke `/bees-write-sdd`.**

   - Same shape, mirrored. `Skill(skill="bees-write-sdd", args="<args per the contract in t3.31f.5u.f3.pt>")` with identical `args` payload to the `/bees-write-prd` invocation (per the parent Task body cross-Task contracts: "the args passed to `/bees-write-prd` and `/bees-write-sdd` are identical"). Capture the returned SDD ticket ID and final status. Same error-handling rules as the PRD invocation.
   - Invoke `/bees-write-sdd` AFTER `/bees-write-prd` returns successfully. Sequential rather than parallel — the writer-skills both modify the same Spec Bee's children list, and the Skill tool's invocation model is single-shot rather than concurrent.

3. **Identical-args invariant.** Document inline that the args payload to both writer skills is intentionally identical, and that divergence between the two invocations would be a defect (per parent Task body cross-Task contracts).

4. **No `bees`-CLI handling of the writer's children.** The writer skills handle their own `bees create-ticket --parent <spec-bee-id> --hive specs --ticket-type t1` calls and their own `drafted → ready` transitions on the PRD/SDD child tickets. This Step 4 prose does NOT call `bees create-ticket` or `bees update-ticket` for the PRD or SDD child tickets directly — only for the Spec Bee parent (in the previous sibling for create and the next sibling for promotion).

5. **Forward-reference to Step 5: NOT in this Subtask's scope.** Sibling Subtask `t3.31f.y2.9q.ts` (the Spec Bee promotion / end-of-Step-4 summary Subtask) is the sole owner of the forward-reference to Step 5's Plan Bee `reference_materials` wiring. Do NOT add an inline note in this Subtask's prose pointing at Step 5 — `.ts` will author the single forward-reference in its end-of-Step-4 summary section. This avoids redundant prose between `.k9` and `.ts`.

6. **Constraint compliance.** Project-neutral prose. Single-literal-command Bash etiquette (relevant here mainly via the writer-skill internals; this Step 4 prose itself uses Skill-tool calls rather than Bash for the writer invocations). Paired POSIX bash + Windows PowerShell snippets if any new Bash snippets are added (likely none — the writer invocations are Skill-tool calls, not Bash). No `rm` / `Remove-Item`.

## Key Files

- `skills/bees-plan/SKILL.md` — add the `/bees-write-prd` and `/bees-write-sdd` Skill-tool invocation prose under Step 4, between the Spec Bee detect-or-create sub-step and the Spec Bee promotion sub-step.

## Acceptance Criteria

- `skills/bees-plan/SKILL.md` Step 4 invokes `/bees-write-prd` via the Skill tool with the Spec Bee ID and the conversation-distilled scope. The args shape matches the contract documented in `skills/bees-write-prd/SKILL.md`'s `## Inline invocation via the Skill tool` section (per sibling Subtask `t3.31f.by.7h.xa`).
- `skills/bees-plan/SKILL.md` Step 4 invokes `/bees-write-sdd` via the Skill tool with the same args shape, immediately after `/bees-write-prd` returns successfully (per the contract documented in `skills/bees-write-sdd/SKILL.md` per sibling Subtask `t3.31f.5u.f3.pt`).
- Step 4 captures the returned PRD and SDD ticket IDs and final statuses. The captured values are available for the next sibling Subtask's Spec Bee promotion gating.
- The args payload to `/bees-write-prd` and `/bees-write-sdd` is identical. The identical-args invariant is documented inline so future maintainers do not introduce divergence.
- Error handling: if either writer skill returns an error or a non-`ready` final status, Step 4 surfaces the error and aborts. The Spec Bee remains in `drafted` so the user can re-run. Documented inline.
- The writer skills are invoked sequentially (PRD then SDD), not in parallel. Documented inline.
- Step 4 prose does NOT call `bees create-ticket` or `bees update-ticket` for the PRD or SDD child tickets — only the writer skills do that.
- This Subtask does NOT add an inline forward-reference to Step 5's Plan Bee `reference_materials` wiring — sibling Subtask `t3.31f.y2.9q.ts` is the sole owner of that forward-reference (placed in its end-of-Step-4 summary).
- Skill prose remains project-neutral.
- Every Bash snippet (if any) ships as paired POSIX bash + Windows PowerShell variants. Every Bash tool call is a single literal command. No `rm` / `Remove-Item` cleanup.
- Project-level lint passes: `python -m pyflakes skills/*/scripts/*.py` succeeds.
