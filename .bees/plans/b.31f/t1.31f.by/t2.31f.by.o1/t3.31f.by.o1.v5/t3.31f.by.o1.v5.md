---
id: t3.31f.by.o1.v5
type: t3
title: Add /bees-write-prd row to README skill catalog
parent: t2.31f.by.o1
reference_materials: null
created_at: '2026-05-06T18:19:41.907509'
status: ready
schema_version: '0.1'
guid: 31fbyo1v5aa5zwx3x3rwgp5uwsnshnbo
---

## Context

A new skill, `/bees-write-prd`, is being authored under sibling Task `t2.31f.by.7h` (which produces `skills/bees-write-prd/SKILL.md`). The README's user-facing skill catalog is the single source of truth for the skill list (per CLAUDE.md `## When editing skills`), so it must gain a row for the new skill. This Subtask adds that row.

Preconditions:
- Sibling Task `t2.31f.by.7h` has produced `skills/bees-write-prd/SKILL.md` with a finalized `description:` field in its YAML frontmatter. The implementer of this Subtask MUST pull the row's description text from that frontmatter rather than paraphrasing — the frontmatter description is the source of truth.
- If this Subtask is somehow scheduled before sibling Task `t2.31f.by.7h` lands its SKILL.md, block on that Task first.

Cross-Epic note: sibling Epic `t1.31f.5u` (`/bees-write-sdd`) will independently add its own row to the same catalog. The two changes do not collide on row content (different skill names), but they DO both need to bump the running skill count on README.md line 166 (`The current 11 skills are the portable core …`). Whichever lands first bumps 11 → 12; the second bumps 12 → 13. Read the current value at execution time and increment by one.

## What Needs to Change

1. README.md skill-catalog table (currently lines 112-124): insert a new table row for `/bees-write-prd`.
   - Position: after the `/bees-plan-from-specs` row (currently line 116) and before the `/bees-breakdown-epic` row (currently line 117). Rationale: the catalog is ordered by workflow stage (setup → planning → breakdown → execute → issues → status → reviews), not alphabetically, and `/bees-write-prd` is a planning-stage skill that authors a PRD document — it belongs in the planning group alongside `/bees-plan` and `/bees-plan-from-specs`.
   - Row format (matches existing rows exactly): `| `/bees-write-prd` | <description> |`
   - `<description>` MUST be copied verbatim from the `description:` field in `skills/bees-write-prd/SKILL.md`'s YAML frontmatter (output of sibling Task `t2.31f.by.7h`). Do not paraphrase, summarize, or reword. If the frontmatter description is multi-sentence and reads cleanly as a one-line catalog cell, paste it as-is; if it contains a literal newline that would break Markdown table formatting, replace internal newlines with a single space (preserving sentence boundaries) but do not otherwise edit the wording.

2. README.md line 166: bump the running skill count.
   - Current text: `The current 11 skills are the portable core …`
   - At execution time, read the current count (it may already have been bumped by the sibling `/bees-write-sdd` Task) and increment by one. If the line currently reads `11 skills`, change to `12 skills`. If it currently reads `12 skills` (sibling already bumped), change to `13 skills`.

3. No other README sections need changes for this Subtask:
   - The pipeline diagram at lines 5-22 is intentionally high-level and does not enumerate every skill (it omits status / review / file-issue too); leave it alone.
   - The `## Coming soon: optional skills` section is for tmux-dependent and stack-specific helpers; `/bees-write-prd` is portable core, not optional.

## Key Files

- README.md — add one table row in the `## The skills` table (after the `/bees-plan-from-specs` row, before `/bees-breakdown-epic`); bump the skill count on line 166 by one.

## Acceptance Criteria

- README.md contains a table row for `` `/bees-write-prd` `` positioned between the `/bees-plan-from-specs` row and the `/bees-breakdown-epic` row in the `## The skills` table.
- The description cell text matches the `description:` field of `skills/bees-write-prd/SKILL.md`'s YAML frontmatter verbatim (modulo newline-to-space normalization for table-cell formatting only).
- The table renders as valid GitHub-flavored Markdown (column count matches the header `| Skill | What it does |`, no broken pipes, no stray backticks).
- The skill-count claim on README.md line 166 has been incremented by one relative to its pre-change value.
- No other lines in README.md are modified by this Subtask.
- `git diff README.md` is limited to the new row insertion plus the count bump on line 166 — nothing else.
