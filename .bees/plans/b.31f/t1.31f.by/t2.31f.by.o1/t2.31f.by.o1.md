---
id: t2.31f.by.o1
type: t2
title: README skill catalog update for /bees-write-prd
up_dependencies:
- t2.31f.by.7h
parent: t1.31f.by
children:
- t3.31f.by.o1.v5
reference_materials: null
created_at: '2026-05-06T18:12:57.379454'
status: done
schema_version: '0.1'
guid: 31fbyo1bdu8g97hbuukachtqo962tdhm
---

## Context

Epic `t1.31f.by` introduces a new `/bees-write-prd` skill (authored in sibling Task 1 of this Epic). The README's skill catalog table is the single source of truth for the user-visible skill catalog per CLAUDE.md `## When editing skills` — when a new skill is added, the README must reflect it. This Task adds a row for `/bees-write-prd` to the skill catalog table.

This Task is documentation-only — no SKILL.md prose, no helper script, no agent contract changes. Sibling Task 1 of this Epic owns the actual skill prose; this Task only documents the skill in the catalog.

Note about a parallel update: sibling Epic `t1.31f.5u` (`/bees-write-sdd` skill) will also add a row to the README skill catalog when it is broken down and executed. The two rows are independent (different skill names, different descriptions); they should be added in alphabetical order or in a logical group with the existing skill rows. Coordination is at the file level — both Tasks edit `README.md`'s skill catalog section, so if both run concurrently the second one to land must rebase. Per the parent Bee body, Epics in this Plan are executed sequentially per the dependency chain, so this should not surface as a real conflict. If it does, prefer rebase over force-resolution.

## What Needs to Change

`README.md` skill catalog (the user-visible table or list that enumerates the skills installed by this repo) — add a new row for `/bees-write-prd` describing:

- Skill name as `/bees-write-prd`.
- One-line description matching the SKILL.md frontmatter `description` field (which sibling Task 1 of this Epic produces). The README description should be terse — one sentence — and cite the skill's role: authors or revises a PRD ticket as a `t1=Doc` child of a Spec Bee; composable with `/bees-plan`.

Position the new row in alphabetical order with the existing skill rows (or in whatever ordering convention the existing table follows — match the precedent).

Do NOT touch:
- Any other section of the README (hive catalog, upgrading-from-older-versions, install instructions, etc.). Sibling Epic `t1.31f.ho`'s Task 3 already updated the README hive table for the Specs hive in a prior commit.
- Sibling Epic `t1.31f.5u`'s skill row for `/bees-write-sdd` — that row is owned by `.5u`'s own Task 2 (or equivalent).
- The SKILL.md content itself — that's sibling Task 1 of this Epic.

## Why

Without this README catalog update, a user enumerating the available skills (via the README, via Claude Code's own skill listing, or via `claude /help`) would not see `/bees-write-prd` listed alongside the other workflow skills, even though it exists in `skills/`. The catalog is also where users learn how to invoke the skill and what it does — leaving it absent from the catalog is a UX cliff.

## Cross-Task contracts consumed by this Task

This Task consumes the SKILL.md frontmatter `description` field that sibling Task 1 of this Epic produces. The README description should be derived from (or directly quote) the SKILL.md description so the two stay in sync.

## Cross-Epic coordination note

Sibling Epic `t1.31f.5u` will add a row for `/bees-write-sdd` to the same README catalog. The two rows can be added in alphabetical order or in a logical group; either is acceptable. The cross-Epic coordination is at the file level — both Tasks edit the same README section, so concurrent edits would conflict. The parent Bee's dependency graph executes Epics sequentially in dependency order, so this should not surface; if it does, rebase rather than force-resolve.

## Key Files

- `README.md` — single file in scope. Specifically, the skill catalog section.

## Acceptance Criteria

- `README.md`'s skill catalog includes a row for `/bees-write-prd` with a one-line description matching the SKILL.md frontmatter `description` field.
- The new row is positioned in alphabetical order (or in whatever ordering convention the existing table follows).
- No other sections of the README are modified.
- The diff is contained to a single addition in `README.md`.
- The wording is consistent with the rest of the README's terse, factual style.
