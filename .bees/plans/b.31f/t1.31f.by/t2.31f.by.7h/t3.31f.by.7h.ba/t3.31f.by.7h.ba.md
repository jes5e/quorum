---
id: t3.31f.by.7h.ba
type: t3
title: Scaffold skills/bees-write-prd/SKILL.md with solo-invocation flow
down_dependencies:
- t3.31f.by.7h.xa
parent: t2.31f.by.7h
reference_materials: null
created_at: '2026-05-06T18:19:36.427657'
status: done
schema_version: '0.1'
guid: 31fby7hbavj1nodcibzgikyvpg3dr2ew
---

## Context

This Subtask creates the new skill file `skills/bees-write-prd/SKILL.md` and lands the solo-invocation flow end-to-end: `/bees-write-prd <spec-bee-id>` invoked directly by the user against an existing Spec Bee creates or updates a `t1=Doc` PRD child whose body contains the twelve required sections. After this Subtask lands, the skill is functionally complete for the standalone-revision use case described in the parent Task body item 4. Mid-conversation awareness (Task body item 3) and the inline-invocation Skill-tool contract (Task body item 5) are layered on by the sibling Subtask, which depends on this Subtask's scaffold and solo-flow being in place.

The parent Task (`t2.31f.by.7h`) fixes the contract values this Subtask consumes verbatim from sibling Epic `t1.31f.ho`'s Subtasks: Specs-hive normalized name `specs`, child tier `t1` (friendly name `Doc`/`Docs`), allowed statuses `drafted` (during authoring) and `ready` (when authoring completes), PRD ticket title exactly `PRD` (case-sensitive — sibling Epic `t1.31f.4u`'s PM two-hop lookup keys on this exact title to differentiate PRD from SDD).

This repo's three non-negotiable design rules in CLAUDE.md (project-neutral prose; paired POSIX bash + Windows PowerShell snippets, no bash-only fallback; no baking-in of this repo's specific paths or ticket IDs) plus the scratch-file convention (write `--body-file` payloads under `<tempdir>/.quorum/` with `mkdir -p` / `New-Item -ItemType Directory -Force` create-if-absent; never `rm` after) and Bash etiquette (one literal command per Bash call; no compound chains, pipes, redirects, or shell-variable expansion in skill prose) all apply. `skills/bees-file-issue/SKILL.md` and `skills/bees-plan-from-specs/SKILL.md` are the closest precedent skills to mirror for shape and conventions.

## What Needs to Change

Create the new skill file with a complete solo-invocation workflow.

1. Create `skills/bees-write-prd/SKILL.md` with valid YAML frontmatter:
   - `name: bees-write-prd`
   - `description:` — specific enough to avoid mis-invocation per CLAUDE.md `## When editing skills`. Mention the Specs hive, the PRD-ticket-as-`t1=Doc`-child shape, and that the skill is composable (solo for revisions; inline from `/bees-plan` via the Skill tool). Keep it one sentence, similar in flavor to existing skill descriptions in this repo.
   - `argument-hint: "<spec-bee-id>"` — matches the solo-invocation calling convention.

2. Author an Overview section that states what the skill does in one paragraph and lists the two invocation paths (solo, inline). The inline-invocation prose is just a stub here pointing forward to the Skill-tool contract section that the sibling Subtask will fill in — wire it as a section with a one-line placeholder so the sibling only needs to populate, not restructure.

3. Author a Preconditions section that hard-fails with `Run /bees-setup first.` (plus a one-line note about what is missing) when:
   - The Specs hive is missing from `bees list-hives` (no entry whose `normalized_name` is `specs`).
   - CLAUDE.md does not contain `## Documentation Locations`.

   Mirror the precedent in `skills/bees-file-issue/SKILL.md` `## Preconditions` and `skills/bees-plan-from-specs/SKILL.md` `### Setup` for shape and wording. Note (as those skills do) that `## Build Commands` is not required at PRD-authoring time.

4. Author a Workflow section covering solo invocation:

   a. **Read the Spec Bee.** Use `bees show-ticket --ids <spec-bee-id>` to fetch the Spec Bee body for context.

   b. **Detect existing PRD child.** Enumerate the Spec Bee's children to detect whether a `t1=Doc` child titled exactly `PRD` already exists. The recipe: `bees execute-freeform-query --query-yaml '<yaml>'` with a `parent=<spec-bee-id>` + `title~^PRD$` filter (or equivalent). Confirm flag names against `bees execute-freeform-query --help` rather than guessing. Document the query inline in the skill prose so Claude Code can copy it verbatim at runtime.

   c. **Discovery.** When invoked solo with no substantive prior conversation context, conduct apiary-style discovery to gather the content for the twelve required sections. Use `AskUserQuestion` only for finite-choice prompts; use prose questions for free-text answers (per CLAUDE.md `## AskUserQuestion usage`). The exact questions are the skill author's call, but they must yield enough material to populate every required section without hallucinating. (Mid-conversation distillation — when prior context is rich — is added by the sibling Subtask.)

   d. **Author the PRD body.** Produce a single markdown body with these twelve sections in this order, every section always present (filled with substance or explicitly marked empty per Task body item 2 — never silently omitted):
      - `## Problem Statement`
      - `## Goals`
      - `## Non-Goals / Out of Scope`
      - `## Functional Requirements`
      - `## Edge Cases and Error Handling`
      - `## Non-Functional Requirements` (mark `(if applicable)` in the heading or note explicitly when not applicable)
      - `## UI/UX Requirements` (same `(if applicable)` treatment)
      - `## Acceptance Criteria`
      - `## Assumptions`
      - `## Open Questions`
      - `## Background and rationale` (NEW — must always be present, even if explicitly marked `none — this PRD has no captured rationale from prior conversation`)
      - `## Decisions and rejected alternatives` (NEW — same treatment)

      Document in the skill prose that the two NEW sections are mandatory for downstream cold-session agents and must never be silently omitted; explain the "author forgot vs no rationale" disambiguation rationale from the parent Task body.

      Apiary's quality-bar guidance (adapt the spirit, not copy-paste): reject implementation details (those belong to the SDD); reject vague language (e.g., "should be fast"); require measurable acceptance criteria. State this in a `## Quality bar` subsection or inline checklist within the workflow.

   e. **Write body to scratch file.** Use the `Write` tool to author the body to a path under the namespaced workflow scratch dir (`/tmp/.quorum/bees-body-<short-suffix>.md` on POSIX, `$env:TEMP\.quorum\bees-body-<short-suffix>.md` on Windows). Create the `.quorum` subdir if absent (`mkdir -p /tmp/.quorum` on POSIX, `New-Item -ItemType Directory -Force -Path "$env:TEMP\.quorum" | Out-Null` on Windows). Do NOT remove the file after the bees command exits. Mirror the snippet shapes used in `skills/bees-file-issue/SKILL.md` step 3 verbatim.

   f. **Create-or-update branching (idempotency).**
      - If no existing PRD child: `bees create-ticket --hive specs --ticket-type t1 --parent <spec-bee-id> --title PRD --body-file <path> --status drafted`. Show the command as paired POSIX bash + Windows PowerShell snippets (POSIX uses `\` line continuation; PowerShell uses backtick). Verify each flag name against `bees create-ticket --help` before authoring the prose.
      - If an existing PRD child: `bees update-ticket --ids <prd-id> --body-file <path>`. Default to full-rewrite semantics for body coherence. Note in prose that `bees append-ticket-body --ticket-id <prd-id> --chunk-file <path>` is available for explicit append-only revisions but is NOT the default. Verify against `bees update-ticket --help` and `bees append-ticket-body --help`.

   g. **Promote to `ready`.** When authoring completes (user has approved the draft), transition the PRD ticket from `drafted` to `ready` via `bees update-ticket --ids <prd-id> --status ready`. The Spec Bee's transition to `ready` is sibling Epic `t1.31f.y2`'s responsibility, not this skill's.

   h. **Idempotency proof.** Document explicitly that re-running `/bees-write-prd <same-spec-bee-id>` on the same Spec Bee updates the existing PRD ticket rather than creating a duplicate. This is the observable behavior the parent Task's acceptance criteria verifies via two consecutive runs.

5. Ensure every Bash tool call shown in the skill prose is a single literal command. No `&&`, `||`, `;`, pipes, redirects, `$VAR` expansion, command substitution, backticks-in-double-quoted-strings, or diagnostic tails. For sequenced commands (e.g., `mkdir -p` then write file then `bees create-ticket`), present them as separate Bash blocks/calls. If multi-step variable-bearing logic is needed (e.g., parsing the freeform-query JSON to extract the existing PRD ticket ID), prefer a Python helper script under `skills/bees-write-prd/scripts/` over inline shell. Bundled-helper precedent: `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`. The Python helper option is the skill author's call — only add it if shell control flow would otherwise be needed; otherwise keep the skill prose-only.

6. Preserve project-neutral prose: do NOT bake in this repo's specific Bee IDs, paths, or workflow specifics. The skill must work in any project that has installed quorum.

## Key Files

- `skills/bees-write-prd/SKILL.md` (NEW) — the skill prose itself, frontmatter through the solo-invocation workflow including the twelve required PRD sections, scratch-file usage, idempotent create-or-update branching, and `drafted → ready` promotion.
- `skills/bees-write-prd/scripts/<name>.py` (OPTIONAL — only if a Python helper is genuinely needed for parsing freeform-query output or similar multi-step logic; the skill author's call).

## Acceptance Criteria

- `skills/bees-write-prd/SKILL.md` exists with valid YAML frontmatter (`name: bees-write-prd`, specific `description`, `argument-hint`).
- The skill prose, when followed by Claude Code on a `/bees-write-prd <spec-bee-id>` invocation against an existing Spec Bee, would produce a `t1=Doc` child ticket titled exactly `PRD` (case-sensitive) on the Specs hive whose body contains all twelve required sections in the specified order. (Behavioral verification of the actual run depends on the Specs hive existing in the target repo, which sibling Epic `t1.31f.ho`'s Subtask `t2.31f.ho.8h` provides.)
- The PRD body always includes `## Background and rationale` and `## Decisions and rejected alternatives` sections — the skill prose explicitly forbids silent omission and documents the disambiguation rationale.
- Re-invoking the skill on the same Spec Bee updates the existing PRD ticket rather than creating a duplicate. Verifiable by reading the create-or-update branching prose: it must enumerate children first, detect the existing `PRD`-titled `t1=Doc` child, and route to `bees update-ticket` rather than `bees create-ticket`.
- The skill creates the PRD ticket at `status=drafted`, then transitions to `status=ready` when authoring completes. Verifiable by reading the workflow prose.
- Scratch-file usage targets `<tempdir>/.quorum/` with `mkdir -p` / `New-Item -ItemType Directory -Force` create-if-absent. No `rm` / `Remove-Item` instructions appear anywhere in the skill prose.
- Every shell snippet in the skill ships as paired POSIX bash + Windows PowerShell variants. No bash-only fallback.
- Every Bash tool call shown is a single literal command — no compound chains, pipes, redirects, or shell-variable expansion.
- The skill prose contains no hardcoded language-specific commands, file extensions, or manifest filenames. The skill is project-neutral — no references to this repo's specific paths or ticket IDs that would not make sense in a downstream project.
- Preconditions hard-fail with `Run /bees-setup first.` (plus a one-line note) when the Specs hive is missing or `## Documentation Locations` is absent from CLAUDE.md.
- All bees CLI flag names referenced in the prose match the actual flags reported by `bees create-ticket --help`, `bees update-ticket --help`, `bees show-ticket --help`, `bees execute-freeform-query --help`, and (if referenced) `bees append-ticket-body --help` — flag names are not hand-authored.
- README skill catalog update for `/bees-write-prd` is NOT in this Subtask's scope (sibling Task `t2.31f.by.o1` owns it).
- Mid-conversation awareness behavior and the inline-invocation Skill-tool contract are NOT in this Subtask's scope (the sibling Subtask layers them on).
- Project-level lint passes: `python -m pyflakes skills/*/scripts/*.py` succeeds (relevant only if the optional Python helper is added).
