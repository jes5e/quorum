---
id: t3.31f.ho.b2.fj
type: t3
title: Add Specs hive entry to README hive catalog (Status vocabulary section)
parent: t2.31f.ho.b2
reference_materials: null
created_at: '2026-05-06T18:03:13.328979'
status: done
schema_version: '0.1'
guid: 31fhob2fjuf6ap3pxjagvp619napz6qq
---

## Context

Epic `t1.31f.ho` introduces a third hive (`Specs`) alongside `Plans` and `Issues`. The README's hive catalog must describe the new hive so users who see it appear in `bees list-hives` (after re-running `/bees-setup`) can understand its purpose, shape, and statuses.

The README does not have a dedicated `## Hives` section today — the de-facto hive catalog is the `## Status vocabulary` section (`README.md` lines 128-138), whose table currently has two rows (`Plans`, `Issues`) and four lines of prose defining the four Plans-hive statuses. That table plus its trailing prose is the location to extend.

This Subtask is documentation-only; no skill prose, helper script, or agent contract changes. Sibling Task `t2.31f.ho.8h` owns the actual hive-colonization prose in `/bees-setup`, and sibling Task `t2.31f.ho.r2` owns the precondition-gate hard-fail prose. The cross-Task contracts those Tasks fix are consumed here verbatim: display name `Specs`, normalized name `specs`, child tier `t1=Doc/Docs`, statuses `drafted` and `ready`, allowed resolver `bees`.

## What Needs to Change

In `README.md`:

1. Extend the `## Status vocabulary` table (around lines 130-133) to add a third row for the Specs hive. The new row's left column should identify the hive and its child-tier shape consistently with the existing rows (which name the hive followed by its child tiers in parentheses, e.g., `**Plans** (Plan Bees, Epics, Tasks, Subtasks)`). For Specs, use `**Specs** (Spec Bees, Docs)` — Spec Bees are the top-level container, with `t1=Doc` children. The right column lists the two-state ladder: `drafted` -> `ready`.

2. Immediately after the table (and after the existing four status-definition prose lines at 135-138), add a short paragraph or bullet that captures the Specs hive's additional metadata that the table cannot express:
   - Display name `Specs`, normalized name `specs`.
   - Child tier shape: `t1=Doc/Docs`. PRD and SDD are both `t1=Doc` children differentiated by ticket title (`PRD` vs `SDD`), not by tier.
   - Allowed resolver: `bees` — so a Plan Bee's `reference_materials` can point at a Spec Bee.
   - One-line summary: holds Spec Bees, each containing per-feature spec docs as `t1=Doc` children.

Keep the prose strictly factual and scoped to the hive itself. Do NOT speculate about how the redesigned `/bees-plan` flow will use Spec Bees, or how PM will resolve the `bees` resolver — those are sibling Epics whose docs land when those Epics ship.

Match the existing README's concise, neutral tone. Use the capitalized form `Specs` (matching sibling Task `t2.31f.ho.r2`'s hard-fail vocabulary) and lowercase `specs` only when referring to the normalized hive name.

## Key Files

- `README.md` — extend the `## Status vocabulary` section (table + trailing prose around lines 128-138) with the Specs hive entry plus its metadata paragraph.

## Acceptance Criteria

- The `## Status vocabulary` table in `README.md` has three rows: `Plans`, `Issues`, and `Specs`. The Specs row identifies the hive's child tiers in its label and lists `drafted` -> `ready` as its two-state ladder.
- The prose immediately after the table documents, for the Specs hive: display/normalized name (`Specs` / `specs`), tier shape (`t1=Doc/Docs`, with PRD vs SDD differentiated by title), allowed resolver (`bees`), and a one-line summary of what the hive holds.
- The capitalized form `Specs` is used when naming the hive in user-facing prose; lowercase `specs` appears only as the normalized-name reference.
- No other sections of the README are modified by this Subtask — in particular the skill catalog table (`## The skills`) is unchanged.
- The diff is contained to `README.md`.
