---
id: t3.31f.ho.b2.97
type: t3
title: Add Specs-hive note to README upgrading-from-older-versions section
parent: t2.31f.ho.b2
reference_materials: null
created_at: '2026-05-06T18:03:14.428794'
status: done
schema_version: '0.1'
guid: 31fhob297yv5353f8fz2ei4z2hiso67i
---

## Context

Users with an existing quorum installation that already has `Plans` and `Issues` hives need to know that re-running `/quo-setup` after this Epic ships will detect the missing `Specs` hive and prompt for its path, without disturbing the existing two hives. Without this note in the README's upgrading-from-older-versions section, an upgrading user would see an unexpected new prompt for a hive they have never heard of.

The existing upgrading section (`README.md` lines 98-108, recently extended in commit `efd1093` to cover the `egg`-to-`reference_materials` migration) is the right home for this note. Its style is short, factual, and tells the user exactly which command to re-run.

This Subtask is documentation-only and pairs with the hive-catalog Subtask of this Task. The two can land in either order or together; there is no cross-dependency.

## What Needs to Change

In `README.md`'s `### Upgrading from older quorum versions` section (around lines 98-108):

- Add a one-line bullet or one short paragraph noting that re-running `/quo-setup` against a repo that already has `Plans` and `Issues` will detect the missing `Specs` hive, prompt for its path, and leave the existing hives untouched.
- Keep the addition consistent in tone and length with the existing entry about `bees update-config` — this is one bullet or one short paragraph, not a new subsection.
- Reference the skill by its slash-command form (`/quo-setup`) and the hive by its capitalized display name (`Specs`), aligning with sibling Task `t2.31f.ho.r2`'s `Run /quo-setup first.` hard-fail vocabulary.

Do NOT speculate about future flows owned by sibling Epics (the redesigned `/quo-plan`, the new `/quo-write-prd` and `/quo-write-sdd` skills, PM's `bees`-resolver behavior). The note documents only the upgrade step itself.

## Key Files

- `README.md` — extend `### Upgrading from older quorum versions` (around lines 98-108) with the Specs-hive one-liner.

## Acceptance Criteria

- The `### Upgrading from older quorum versions` section in `README.md` includes a one-line note that re-running `/quo-setup` adds the `Specs` hive without disturbing existing `Plans` or `Issues` hives.
- The note uses the capitalized hive name `Specs` and references the slash-command `/quo-setup`.
- The note's tone and length are consistent with the section's existing entries (concise, factual, action-oriented).
- No other sections of the README are modified by this Subtask.
- The diff is contained to `README.md`.
