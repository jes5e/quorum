---
id: t3.31f.ho.r2.4v
type: t3
title: Update SDD and doc-writing guide preconditions prose for Specs precondition gate
parent: t2.31f.ho.r2
reference_materials: null
created_at: '2026-05-06T18:03:11.772479'
status: done
schema_version: '0.1'
guid: 31fhor24vqhvqogv7b189u418otbt5be
---

## Context

Task `t2.31f.ho.r2` extends the Preconditions sections of `/bees-execute`, `/bees-fix-issue`, and `/bees-breakdown-epic` to hard-fail with `Run /bees-setup first. — Specs hive is not colonized for this repo.` when the Specs hive is missing. Two non-README docs in this repo currently describe the existing precondition-failure pattern in prose that becomes incomplete once this Task lands:

1. `docs/sdd.md` `## Contract keys` (line 59) only mentions CLAUDE.md section/key checks and only names `bees-execute` + `bees-fix-issue`.
2. `docs/doc-writing-guide.md` `## Hard-fail preconditions` (lines 205-209) only mentions CLAUDE.md section/key checks and only names `bees-execute` + `bees-fix-issue`.

Neither prose mentions the existing Plans-hive presence check that the engineer is mirroring for Specs, nor the new Specs-hive presence check, nor `bees-breakdown-epic` (which also receives the gate per this Task's body).

This Subtask updates those two prose sites so they accurately describe the precondition pattern after Task `t2.31f.ho.r2` lands. It does NOT update `docs/sdd.md` line 24 (`The workflow uses two hives ...`) — that update is more naturally owned by sibling Task `t2.31f.ho.8h` (Specs-hive colonization), which is the change that actually creates the third hive.

This Subtask does NOT touch `README.md` — sibling Task `t2.31f.ho.b2` of this Epic owns all README changes.

## What Needs to Change

**`docs/sdd.md` `## Contract keys`** (line 59 area): expand the single-sentence precondition summary to (a) include `bees-breakdown-epic` alongside `bees-execute` and `bees-fix-issue` in the list of skills that hard-fail with `Run /bees-setup first.`, and (b) note that the precondition check covers both the required CLAUDE.md sections AND the required hives (Plans, Issues, Specs). Wording should stay tight — one or two sentences, in the same paragraph shape as the existing prose.

**`docs/doc-writing-guide.md` `## Hard-fail preconditions`**: same expansion. The current paragraph reads `Execution skills (`bees-execute`, `bees-fix-issue`) hard-fail with `Run /bees-setup first.` when the target CLAUDE.md is missing either of the two required sections (`Documentation Locations`, `Build Commands`) or any required key inside them.` Update it to (a) add `bees-breakdown-epic` to the list of skills, (b) extend the trigger-condition list to include hive-presence checks (Plans, Issues, Specs) alongside the existing CLAUDE.md section/key checks, and (c) preserve the existing follow-up sentence about adding new required CLAUDE.md keys.

Both updates should match the wording of the actual hard-fail message string the engineer ships in the SKILL.md files (per the Task body's `Run /bees-setup first. — Specs hive is not colonized for this repo.` template), so the docs and the runtime message agree.

## Key Files

- `docs/sdd.md` — `## Contract keys` section, the one-sentence precondition summary (currently around line 59).
- `docs/doc-writing-guide.md` — `## Hard-fail preconditions` section (currently lines 205-209).

## Acceptance Criteria

- `docs/sdd.md` `## Contract keys` precondition summary names all three skills that hard-fail (`bees-execute`, `bees-fix-issue`, `bees-breakdown-epic`) and mentions both CLAUDE.md section/key checks AND hive-presence checks (Plans, Issues, Specs) as triggers. Verifiable by reading the section.
- `docs/doc-writing-guide.md` `## Hard-fail preconditions` paragraph names all three skills and both precondition classes. The follow-up sentence about adding new required CLAUDE.md keys is preserved.
- Neither file touches `README.md`, the `## Project terminology` two-hives line in `docs/doc-writing-guide.md` (line 256-257), or the `two hives in the target repo` line in `docs/sdd.md` (line 24) — those are sibling-Task scope.
- The hard-fail wording quoted in the docs matches the wording the engineer ships in the SKILL.md files for this Task (`Run /bees-setup first. — Specs hive is not colonized for this repo.` or equivalent template). Verifiable by string-matching across the SKILL.md changes and the doc updates.
- Minimal diff per file — only the precondition prose changes, no unrelated edits.
