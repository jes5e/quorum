---
id: t3.31f.ho.r2.og
type: t3
title: Add Specs-hive precondition gate to /bees-execute
parent: t2.31f.ho.r2
reference_materials: null
created_at: '2026-05-06T18:03:08.323338'
status: done
schema_version: '0.1'
guid: 31fhor2og3f1ui9qatmkxvenaw9h85w2
---

## Context

Epic `t1.31f.ho` requires that `/bees-execute` hard-fail with `Run /bees-setup first.` when the repo does not have the `Specs` hive colonized. Sibling Task `t2.31f.ho.8h` colonizes the hive in `/bees-setup` (using normalized name `specs`, display name `Specs`); sibling Task `t2.31f.ho.b2` updates the README. This Subtask only extends `/bees-execute`'s `## Preconditions` section to add a Specs-hive bullet alongside the existing Plans-hive bullet.

The existing Plans-hive bullet at line 30 of `skills/bees-execute/SKILL.md` reads:

`The Plans hive is colonized for this repo. Check via `bees list-hives` — the output must include a hive whose `normalized_name` is `plans`.`

This is the canonical pattern to mirror — same `bees list-hives` query approach, same `normalized_name` JSON-parse path, same prose shape. The new bullet must also direct the user to re-run `/bees-setup` so the Specs hive is added without disturbing existing hives.

## What Needs to Change

In `skills/bees-execute/SKILL.md`, in the `## Preconditions` section (starts at line 25), add a new bullet immediately after the existing Plans-hive bullet (line 30). The new bullet must use the same prose shape and the same `bees list-hives` / `normalized_name` lookup pattern as the Plans bullet.

Proposed bullet text (insert as a new line after line 30):

`- The Specs hive is colonized for this repo. Check via `bees list-hives` — the output must include a hive whose `normalized_name` is `specs`. If absent, hard-fail with `Run /bees-setup first. — Specs hive is not colonized for this repo. Re-run /bees-setup to add the Specs hive without disturbing existing hives (Plans, Issues).``

Use the lowercase `specs` for the `normalized_name` filter (matches what sibling Task `t2.31f.ho.8h` colonizes) and the capitalized `Specs` only in the user-facing hard-fail message (matches the README hive table sibling Task `t2.31f.ho.b2` adds).

Do NOT alter the existing Plans-hive bullet, the subagents bullet, the Documentation Locations bullet, the Build Commands bullet, the Rationale paragraph, or any other prose in the file. Diff must be minimal — a single new bullet line.

## Key Files

- `skills/bees-execute/SKILL.md` — `## Preconditions` section, immediately after the existing Plans-hive bullet at line 30.

## Acceptance Criteria

- `skills/bees-execute/SKILL.md` `## Preconditions` section contains a new bullet that mandates a hive with `normalized_name == "specs"` is present in `bees list-hives` output.
- The new bullet's hard-fail message string is exactly: `Run /bees-setup first. — Specs hive is not colonized for this repo. Re-run /bees-setup to add the Specs hive without disturbing existing hives (Plans, Issues).`
- The new bullet uses the same prose shape and `bees list-hives` / `normalized_name` lookup pattern as the existing Plans-hive bullet on line 30 — no subtle divergence.
- The bullet uses lowercase `specs` for the `normalized_name` filter; capitalized `Specs` appears only in the user-facing hard-fail message.
- No other content in the file is modified — only one new bullet added. Verifiable by `git diff` showing a single-line addition in the `## Preconditions` section.
- The Plans-hive bullet, subagents bullet, Documentation Locations bullet, Build Commands bullet, and Rationale paragraph are all preserved verbatim.
