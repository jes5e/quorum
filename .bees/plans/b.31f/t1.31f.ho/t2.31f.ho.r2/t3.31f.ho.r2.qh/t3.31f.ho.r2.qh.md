---
id: t3.31f.ho.r2.qh
type: t3
title: Add Specs-hive precondition gate to /quo-breakdown-epic
parent: t2.31f.ho.r2
reference_materials: null
created_at: '2026-05-06T18:03:10.598663'
status: done
schema_version: '0.1'
guid: 31fhor2qhixbsmt55jej99fyqr3up3ks
---

## Context

Epic `t1.31f.ho` requires that `/quo-breakdown-epic` hard-fail with `Run /quo-setup first.` when the repo does not have the `Specs` hive colonized. Sibling Task `t2.31f.ho.8h` colonizes the hive in `/quo-setup` (using normalized name `specs`, display name `Specs`); sibling Task `t2.31f.ho.b2` updates the README. This Subtask only extends `/quo-breakdown-epic`'s `## Preconditions` section to add a Specs-hive bullet alongside the existing Plans-hive bullet.

The existing Plans-hive bullet at line 16 of `skills/quo-breakdown-epic/SKILL.md` reads:

`The Plans hive is colonized for this repo. Check via `bees list-hives` — the output must include a hive whose `normalized_name` is `plans`.`

This is the canonical pattern to mirror — same `bees list-hives` query approach, same `normalized_name` JSON-parse path, same prose shape. The wording matches the equivalent bullet in `/quo-execute`'s SKILL.md, so the new Specs bullet should match that same prose shape exactly.

## What Needs to Change

In `skills/quo-breakdown-epic/SKILL.md`, in the `## Preconditions` section (starts at line 11), add a new bullet immediately after the existing Plans-hive bullet (line 16). The new bullet must use the same prose shape and the same `bees list-hives` / `normalized_name` lookup pattern as the Plans bullet.

Proposed bullet text (insert as a new line after line 16):

`- The Specs hive is colonized for this repo. Check via `bees list-hives` — the output must include a hive whose `normalized_name` is `specs`. If absent, hard-fail with `Run /quo-setup first. — Specs hive is not colonized for this repo. Re-run /quo-setup to add the Specs hive without disturbing existing hives (Plans, Issues).``

Use the lowercase `specs` for the `normalized_name` filter (matches what sibling Task `t2.31f.ho.8h` colonizes) and the capitalized `Specs` only in the user-facing hard-fail message (matches the README hive table sibling Task `t2.31f.ho.b2` adds). The hard-fail message string MUST be character-for-character identical to the message used in the sibling Subtasks for `/quo-execute` and `/quo-fix-issue`.

Do NOT alter the existing Plans-hive bullet, the subagents bullet, the Documentation Locations bullet, the Build Commands bullet, the Rationale paragraph, or any other prose in the file. Diff must be minimal — a single new bullet line.

## Key Files

- `skills/quo-breakdown-epic/SKILL.md` — `## Preconditions` section, immediately after the existing Plans-hive bullet at line 16.

## Acceptance Criteria

- `skills/quo-breakdown-epic/SKILL.md` `## Preconditions` section contains a new bullet that mandates a hive with `normalized_name == "specs"` is present in `bees list-hives` output.
- The new bullet's hard-fail message string is exactly: `Run /quo-setup first. — Specs hive is not colonized for this repo. Re-run /quo-setup to add the Specs hive without disturbing existing hives (Plans, Issues).` — character-for-character identical to the message in the sibling `/quo-execute` and `/quo-fix-issue` Subtasks.
- The new bullet uses the same prose shape and `bees list-hives` / `normalized_name` lookup pattern as the existing Plans-hive bullet on line 16 — no subtle divergence.
- The bullet uses lowercase `specs` for the `normalized_name` filter; capitalized `Specs` appears only in the user-facing hard-fail message.
- No other content in the file is modified — only one new bullet added. Verifiable by `git diff` showing a single-line addition in the `## Preconditions` section.
- The Plans-hive bullet, subagents bullet, Documentation Locations bullet, Build Commands bullet, and Rationale paragraph are all preserved verbatim.
