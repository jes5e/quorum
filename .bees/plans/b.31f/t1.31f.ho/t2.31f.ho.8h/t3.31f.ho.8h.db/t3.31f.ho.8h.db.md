---
id: t3.31f.ho.8h.db
type: t3
title: Update docs/sdd.md hive count and architecture overview for Specs hive
parent: t2.31f.ho.8h
reference_materials: null
created_at: '2026-05-06T18:03:04.136067'
status: done
schema_version: '0.1'
guid: 31fho8hdbjm5gtvhp4zxsj6jqn9b795k
---

## Context

Task `t2.31f.ho.8h` colonizes a new `Specs` hive in `/quo-setup` (display name `Specs`, normalized `specs`, tier shape `t1=Doc/Docs`, statuses `drafted`/`ready`, resolver `bees`). Once `quo-setup` writes the third hive, the SDD's existing claim that "the workflow uses two hives" becomes factually wrong. The SDD is internal architecture documentation that downstream contributors and reviewers read to understand the system; leaving the two-hive claim in place creates a contradiction with the live behavior of `/quo-setup`.

This Subtask is scope-bounded to non-README docs (sibling Task `t2.31f.ho.b2` of this Epic owns README changes). Spec-content authoring (the `t1=Doc` children) lives in sibling Epic `t1.31f.y2` and is NOT in scope here — this Subtask only updates SDD prose to reflect the hive's existence.

## What Needs to Change

`docs/sdd.md` — single file. Two locations:

1. **`## Architecture overview` paragraph (around line 24)** — current text reads:
   > The workflow uses two hives in the target repo: **Plans** (top-level, with t1/t2/t3 = Epic/Task/Subtask) and **Issues** (no children).

   Update to name three hives: **Plans** (top-level, t1/t2/t3 = Epic/Task/Subtask, statuses `drafted`/`ready`/`in_progress`/`done`), **Issues** (no children, statuses `open`/`done`), and **Specs** (top-level Spec Bees with `t1=Doc/Docs` children, statuses `drafted`/`ready`). Note that the Specs hive is colonized by `/quo-setup` as a placeholder for future Spec content; the `t1=Doc` children themselves are authored by other skills (the redesigned `/quo-plan`) and are not produced by `/quo-setup`. Keep the existing prose immediately following (about Plan Bees carrying `reference_materials`, body-as-spec mode) intact — it still applies to the Plans hive and is unchanged.

2. **`## Key components` → `skills/quo-setup/` bullet (around line 28)** — current text mentions "hives" generically. Update to explicitly enumerate the three hives that `quo-setup` colonizes (Plans, Issues, Specs) so a reader can confirm what the skill actually creates without grepping the SKILL.md. Phrase the Specs hive bullet as "colonized by `/quo-setup` for use by future skills; no content is authored here at setup time" so the boundary against sibling Epic `t1.31f.y2` is clear.

Do NOT touch:
- The `quo-fix-issue` bullet at line 33 — it correctly says "Issues live in the `issues` hive" and that is still true.
- The `quo-status` bullet at line 35 — "across all hives" is generic and remains accurate.
- The `## Per-feature design` section at the bottom — that documents the paused Test-strategy feature and is unrelated.

## Key Files

- `docs/sdd.md` — update the `## Architecture overview` two-hive claim and the `quo-setup` Key-components bullet to reflect the three-hive reality.

## Acceptance Criteria

- `docs/sdd.md` `## Architecture overview` no longer claims "two hives"; it names Plans, Issues, and Specs with their tier shapes and status vocabularies.
- The `skills/quo-setup/` Key-components bullet enumerates all three hives that `/quo-setup` colonizes, with a note that Specs is colonized empty at setup time and filled by other skills.
- The Specs hive's status vocabulary (`drafted`/`ready`) and tier shape (`t1=Doc/Docs`) are stated correctly per the cross-Task contracts in Task `t2.31f.ho.8h`.
- No README changes are made (sibling Task `t2.31f.ho.b2` owns those).
- No claims about Spec-content authoring or the `/quo-plan` redesign are added (those belong to sibling Epic `t1.31f.y2`).
- Existing prose about `reference_materials`, body-as-spec mode, and Marker-producer/consumer roles remains byte-identical.
