---
id: t3.31f.ho.8h.mk
type: t3
title: Update docs/prd.md plain-English-statuses bullet for Specs hive
parent: t2.31f.ho.8h
reference_materials: null
created_at: '2026-05-06T18:03:05.702829'
status: done
schema_version: '0.1'
guid: 31fho8hmk7mxdhahdjvmv2p1x5ai5t9a
---

## Context

`docs/prd.md` line 14 advertises the workflow's status vocabulary to readers as a Why-this-project bullet:

> **Plain-English statuses.** Plan-hive bees use `drafted` / `ready` / `in_progress` / `done`; issue-hive bees use `open` / `done`. No translation layer or insect-metaphor jargon to remember.

Once Task `t2.31f.ho.8h` lands, `/quo-setup` colonizes a third hive (`Specs`) whose status vocabulary is `drafted`/`ready` (a strict subset of the Plans-hive ladder, per the cross-Task contracts established by Task `t2.31f.ho.8h`). Leaving the bullet enumerating only two hives makes the PRD silently incomplete — a customer or contributor reading the PRD would not know the Specs hive exists. The PRD is part of the cumulative customer-facing-and-internal spec record, so it needs the third hive's status vocabulary added at the same moment the hive starts being colonized.

Scope-bounded: this is a non-README doc edit (sibling Task `t2.31f.ho.b2` owns README), and it does NOT pre-document Spec-content authoring (that belongs to sibling Epic `t1.31f.y2`). The PRD edit only updates the status-vocabulary bullet to enumerate three hives.

## What Needs to Change

`docs/prd.md` — single file, single location.

**`## Why` → "Plain-English statuses" bullet (line 14)** — extend the existing sentence to cover the third hive. Suggested wording (engineer should use exact contract values):

> **Plain-English statuses.** Plan-hive bees use `drafted` / `ready` / `in_progress` / `done`; issue-hive bees use `open` / `done`; spec-hive bees use `drafted` / `ready`. No translation layer or insect-metaphor jargon to remember.

Do NOT add any other Specs-related prose to the PRD in this Subtask:
- Don't add a `### Feature: <Specs hive>` subsection — the Specs hive is infrastructure colonized by `/quo-setup`, not a customer-facing feature, and the existing `## Per-feature scope` section is reserved for paused/active feature work (Test strategy etc.).
- Don't pre-document the `/quo-plan` redesign or `t1=Doc` children — those belong to sibling Epic `t1.31f.y2`.
- Don't touch the `## Existing scope` paragraph (line 5) — its existing description of skills installed into `~/.claude/skills/` is unaffected by the new hive.

## Key Files

- `docs/prd.md` — extend the "Plain-English statuses" bullet to enumerate three hives' status vocabularies.

## Acceptance Criteria

- `docs/prd.md` line 14's "Plain-English statuses" bullet enumerates three hives (Plans, Issues, Specs) with their respective status values.
- The Specs-hive status vocabulary listed matches the cross-Task contract from Task `t2.31f.ho.8h`: `drafted` and `ready`.
- The bullet's framing ("plain-English", "no translation layer") is preserved.
- No other PRD prose changes — no new `### Feature:` subsection, no edits to `## Out of scope`, no edits to `## Acceptance criteria (project-level)`.
- README is untouched (sibling Task `t2.31f.ho.b2` owns README).
