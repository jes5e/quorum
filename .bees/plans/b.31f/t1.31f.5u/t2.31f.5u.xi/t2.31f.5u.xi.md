---
id: t2.31f.5u.xi
type: t2
title: README skill catalog update for /bees-write-sdd
up_dependencies:
- t2.31f.5u.f3
parent: t1.31f.5u
children:
- t3.31f.5u.xi.r1
reference_materials: null
created_at: '2026-05-06T18:25:32.790936'
status: done
schema_version: '0.1'
guid: 31f5uximxp6n89ef6ayja4zc7ywatk7a
---

## Context

Epic `t1.31f.5u` introduces a new `/bees-write-sdd` skill (authored in sibling Task 1 of this Epic). The README's skill catalog table is the single source of truth for the skill list per CLAUDE.md `## When editing skills` — when a new skill is added, the README must reflect it. This Task adds a row for `/bees-write-sdd` to the catalog table.

This Task is documentation-only. Sibling Task 1 owns the SKILL.md content; this Task only documents the skill in the catalog.

Cross-Epic coordination note: sibling Epic `t1.31f.by` (`/bees-write-prd` skill) was broken down before this Epic and also adds a row to the same README catalog plus a skill-count line bump. The two rows are independent (different skill names); coordination is at the file level. The skill count claim on README.md line 166 (`The current 11 skills are the portable core …`) gets bumped by `.by`'s row addition first (11 → 12), then this Task bumps further (12 → 13). Read the current value at execution time and increment by one — do NOT hardcode either bump.

## What Needs to Change

1. README.md skill-catalog table: insert a new table row for `/bees-write-sdd`.
   - Position: after the new `/bees-write-prd` row (added by sibling Epic `t1.31f.by`'s catalog Subtask) and before `/bees-breakdown-epic`. Rationale: the catalog is workflow-stage-ordered, not alphabetical, and `/bees-write-sdd` is a planning-stage skill paired with `/bees-write-prd`. Keep the two write-* skills adjacent for readability.
   - Row format matches existing rows: `| `/bees-write-sdd` | <description> |`
   - `<description>` MUST be copied verbatim from the `description:` field in `skills/bees-write-sdd/SKILL.md`'s YAML frontmatter (output of sibling Task 1). Do not paraphrase. If the frontmatter description has internal newlines that would break Markdown table formatting, normalize newlines to spaces but preserve wording.

2. README.md line 166 (or wherever the skill-count claim lives at execution time): bump the running skill count by one.
   - Before sibling Epic `.by`'s catalog Subtask lands, the line reads `The current 11 skills`. After `.by` lands, it reads `12 skills`. After this Subtask lands, it should read `13 skills`. Read the current value first, increment by one — don't hardcode either value.

3. No other README sections need changes for this Task. The pipeline diagram is intentionally high-level; the `## Coming soon` section is for tmux/optional helpers; `/bees-write-sdd` is portable core.

## Why

Without this README catalog update, a user enumerating the skills (via the README, via Claude Code's skill listing, via `claude /help`) would not see `/bees-write-sdd`. The catalog is also where users learn what the skill does and how to invoke it.

## Cross-Task contracts consumed by this Task

This Task consumes the SKILL.md frontmatter `description` field that sibling Task 1 produces.

## Cross-Epic coordination

Sibling Epic `t1.31f.by` adds a `/bees-write-prd` row + the first count bump. The two Epics' README updates touch the same section but different lines; expected execution order is `.by` first (so `.5u` increments from 12 to 13). If executed in a different order, the read-current-and-increment logic still produces a correct count.

## Key Files

- `README.md` — single file in scope.

## Acceptance Criteria

- `README.md`'s skill catalog includes a row for `/bees-write-sdd` positioned adjacent to the `/bees-write-prd` row (which sibling Epic `t1.31f.by`'s catalog Subtask adds).
- The new row's description text matches the SKILL.md frontmatter `description` field verbatim (modulo newline-to-space normalization for table formatting).
- The skill-count claim on README.md is incremented by one relative to its pre-change value.
- No other README sections are modified.
- The diff is contained to README.md.
