---
id: t3.31f.5u.xi.r1
type: t3
title: Add /quo-write-sdd row to README skill catalog
parent: t2.31f.5u.xi
reference_materials: null
created_at: '2026-05-06T18:33:16.149380'
status: done
schema_version: '0.1'
guid: 31f5uxir14xam9d9uno4kf5mytkgbsi9
---

## Context

A new skill, `/quo-write-sdd`, is being authored under sibling Task `t2.31f.5u.f3` (which produces `skills/quo-write-sdd/SKILL.md`). The README's user-facing skill catalog is the single source of truth for the skill list (per CLAUDE.md `## When editing skills`), so it must gain a row for the new skill. This Subtask adds that row.

Preconditions:
- Sibling Task `t2.31f.5u.f3` has produced `skills/quo-write-sdd/SKILL.md` with a finalized `description:` field in its YAML frontmatter. The implementer of this Subtask MUST pull the row's description text from that frontmatter rather than paraphrasing — the frontmatter description is the source of truth.
- If this Subtask is somehow scheduled before sibling Task `t2.31f.5u.f3` lands its SKILL.md, block on that Task first.

Cross-Epic note: sibling Epic `t1.31f.by` (`/quo-write-prd`) independently adds its own row to the same catalog under sibling Subtask `t3.31f.by.o1.v5`. The two changes do not collide on row content (different skill names), but they DO both touch the running skill count on README.md (currently line 166: `The current 11 skills are the portable core …`). Whichever lands first bumps 11 → 12; the second bumps 12 → 13. Read the current value at execution time and increment by one — do NOT hardcode either bump.

## What Needs to Change

1. README.md skill-catalog table (currently lines 112-124): insert a new table row for `/quo-write-sdd`.
   - Position: after the `/quo-write-prd` row (added by sibling Epic `t1.31f.by`'s catalog Subtask `t3.31f.by.o1.v5`, which itself sits between `/quo-plan-from-specs` and `/quo-breakdown-epic`) and before the `/quo-breakdown-epic` row. Rationale: the catalog is ordered by workflow stage (setup → planning → breakdown → execute → issues → status → reviews), not alphabetically; `/quo-write-sdd` is a planning-stage skill that authors an SDD document and pairs naturally with `/quo-write-prd`. Keep the two write-* skills adjacent for readability.
   - If sibling Subtask `t3.31f.by.o1.v5` has not yet landed at execution time, fall back to inserting the `/quo-write-sdd` row directly after `/quo-plan-from-specs` and before `/quo-breakdown-epic` — adjacency to `/quo-write-prd` will be re-established when `.by`'s row lands.
   - Row format (matches existing rows exactly): `| `/quo-write-sdd` | <description> |`
   - `<description>` MUST be copied verbatim from the `description:` field in `skills/quo-write-sdd/SKILL.md`'s YAML frontmatter (output of sibling Task `t2.31f.5u.f3`). Do not paraphrase, summarize, or reword. If the frontmatter description is multi-sentence and reads cleanly as a one-line catalog cell, paste it as-is; if it contains a literal newline that would break Markdown table formatting, replace internal newlines with a single space (preserving sentence boundaries) but do not otherwise edit the wording.

2. README.md skill-count line (currently line 166): bump the running skill count by one.
   - Current text at the time this Epic was broken down: `The current 11 skills are the portable core …`
   - At execution time, read the current count (it may already have been bumped by sibling Subtask `t3.31f.by.o1.v5`) and increment by one. If the line currently reads `11 skills`, change to `12 skills`. If it currently reads `12 skills` (sibling already bumped), change to `13 skills`.
   - Use `Read` + `Edit` to perform the bump (read the current value, compute current+1, edit the literal old string to the new string). Do NOT hardcode `11 → 12` or `12 → 13` in advance — the order in which the two Epics' Subtasks land is not guaranteed.

3. No other README sections need changes for this Subtask:
   - The pipeline diagram at lines 5-22 is intentionally high-level and does not enumerate every skill (it omits status / review / file-issue too); leave it alone.
   - The `## Coming soon: optional skills` section is for tmux-dependent and stack-specific helpers; `/quo-write-sdd` is portable core, not optional.

## Key Files

- README.md — add one table row in the `## The skills` table (after the `/quo-write-prd` row when present, otherwise after `/quo-plan-from-specs`; in both cases before `/quo-breakdown-epic`); bump the skill count on the `The current N skills are the portable core …` line by one.

## Acceptance Criteria

- README.md contains a table row for `` `/quo-write-sdd` `` positioned adjacent to the `/quo-write-prd` row (or, if `.by`'s Subtask has not yet landed, between `/quo-plan-from-specs` and `/quo-breakdown-epic`) in the `## The skills` table.
- The description cell text matches the `description:` field of `skills/quo-write-sdd/SKILL.md`'s YAML frontmatter verbatim (modulo newline-to-space normalization for table-cell formatting only).
- The table renders as valid GitHub-flavored Markdown (column count matches the header `| Skill | What it does |`, no broken pipes, no stray backticks).
- The skill-count claim on the `The current N skills are the portable core …` line has been incremented by one relative to its pre-change value.
- No other lines in README.md are modified by this Subtask.
- `git diff README.md` is limited to the new row insertion plus the count-line bump — nothing else.
