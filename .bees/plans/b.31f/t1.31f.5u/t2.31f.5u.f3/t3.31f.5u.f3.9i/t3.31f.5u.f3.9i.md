---
id: t3.31f.5u.f3.9i
type: t3
title: Scaffold skills/bees-write-sdd/SKILL.md with solo-invocation flow
down_dependencies:
- t3.31f.5u.f3.pt
parent: t2.31f.5u.f3
reference_materials: null
created_at: '2026-05-06T18:33:09.287704'
status: done
schema_version: '0.1'
guid: 31f5uf39ic68ivabygz4a8ghs2zxonyf
---

## Context

This Subtask creates the new skill file `skills/bees-write-sdd/SKILL.md` and lands the solo-invocation flow end-to-end: `/bees-write-sdd <spec-bee-id>` invoked directly by the user against an existing Spec Bee creates or updates a `t1=Doc` SDD child whose body contains all required sections. After this Subtask lands, the skill is functionally complete for the standalone-revision use case described in the parent Task body items 5 (solo invocation) and 7 (quality bar). Mid-conversation awareness (Task body item 4) and the inline-invocation Skill-tool contract (Task body item 6) are layered on by the sibling Subtask, which depends on this Subtask's scaffold and solo-flow being in place.

The parent Task (`t2.31f.5u.f3`) fixes the contract values this Subtask consumes verbatim from sibling Epic `t1.31f.ho`'s Subtasks: Specs-hive normalized name `specs`, child tier `t1` (friendly name `Doc`/`Docs`), allowed statuses `drafted` (during authoring) and `ready` (when authoring completes), SDD ticket title exactly `SDD` (case-sensitive — sibling Epic `t1.31f.4u`'s PM two-hop lookup keys on this exact title to differentiate PRD from SDD).

Apiary's `skills/write-srd/skill.md` is the structural starting reference (renamed SRD → SDD throughout per parent Task body item 1 — frontmatter description, Overview prose, body section names, helper-script names if any, and the resulting child-ticket title). Apiary's `/write-srd` uses the Explore subagent for codebase research; this skill must do the same per parent Task body item 3 (`subagent_type=Explore`). Apiary's "RESEARCH NEEDED" flag pattern for ambiguous spots carries over so SDD authors can mark spots that need further investigation rather than fabricate guesses.

This repo's three non-negotiable design rules in CLAUDE.md (project-neutral prose; paired POSIX bash + Windows PowerShell snippets, no bash-only fallback; no baking-in of this repo's specific paths or ticket IDs) plus the scratch-file convention (write `--body-file` payloads under `<tempdir>/.bees-workflow/` with `mkdir -p` / `New-Item -ItemType Directory -Force` create-if-absent; never `rm` after) and Bash etiquette (one literal command per Bash call; no compound chains, pipes, redirects, or shell-variable expansion in skill prose) all apply. `skills/bees-file-issue/SKILL.md`, `skills/bees-plan-from-specs/SKILL.md`, and the sibling `skills/bees-write-prd/SKILL.md` (created by sibling Epic `t1.31f.by`'s Subtask `t3.31f.by.7h.ba`) are the closest precedent skills to mirror for shape and conventions.

## What Needs to Change

Create the new skill file with a complete solo-invocation workflow.

1. Create `skills/bees-write-sdd/SKILL.md` with valid YAML frontmatter:
   - `name: bees-write-sdd`
   - `description:` — specific enough to avoid mis-invocation per CLAUDE.md `## When editing skills`. Mention the Specs hive, the SDD-ticket-as-`t1=Doc`-child shape, and that the skill is composable (solo for revisions; inline from `/bees-plan` via the Skill tool). Keep it one sentence, similar in flavor to existing skill descriptions in this repo.
   - `argument-hint: "<spec-bee-id>"` — matches the solo-invocation calling convention.

2. Author an Overview section that states what the skill does in one paragraph and lists the two invocation paths (solo, inline). The inline-invocation prose is just a stub here pointing forward to the Skill-tool contract section that the sibling Subtask will fill in — wire it as a section with a one-line placeholder so the sibling only needs to populate, not restructure.

3. Author a brief one-line note in the prose explaining the SRD → SDD rename — per parent Task body item 10, this prevents future readers cross-referencing apiary docs from being confused that the upstream skill is named `/write-srd` while this skill is named `/bees-write-sdd`. Anchor the note in the Overview or in a leading comment near the section names that derived from apiary's SRD scaffolding.

4. Author a Preconditions section that hard-fails with `Run /bees-setup first.` (plus a one-line note about what is missing) when:
   - The Specs hive is missing from `bees list-hives` (no entry whose `normalized_name` is `specs`).
   - CLAUDE.md does not contain `## Documentation Locations`.

   Mirror the precedent in `skills/bees-file-issue/SKILL.md` `## Preconditions` and `skills/bees-plan-from-specs/SKILL.md` `### Setup` for shape and wording. Note (as those skills do) that `## Build Commands` is not required at SDD-authoring time.

5. Author a Workflow section covering solo invocation:

   a. **Read the Spec Bee.** Use `bees show-ticket --ids <spec-bee-id>` to fetch the Spec Bee body for context.

   b. **Detect existing SDD child.** Enumerate the Spec Bee's children to detect whether a `t1=Doc` child titled exactly `SDD` already exists. The recipe: `bees execute-freeform-query --query-yaml '<yaml>'` with a `parent=<spec-bee-id>` + `title~^SDD$` filter (or equivalent). Confirm flag names against `bees execute-freeform-query --help` rather than guessing. Document the query inline in the skill prose so Claude Code can copy it verbatim at runtime.

   c. **Codebase exploration via Explore agent.** Per parent Task body item 3, dispatch the Explore subagent (`subagent_type=Explore`) to research the codebase areas touched by the feature. Document the dispatch shape in the skill prose: what to ask the Explore agent (architecture, affected modules, existing patterns, data models, test fixtures, configuration), how to consume its findings, and how to cite real module/file names from the actual target repo (apiary's quality bar — no fabricated names). Carry over apiary's **RESEARCH NEEDED** flag pattern: when the Explore agent surfaces ambiguity that needs follow-up rather than a confident answer, the SDD body should mark the affected spot with a `RESEARCH NEEDED: <question>` tag rather than fabricate a guess.

   d. **Discovery questions (cold solo invocation).** When invoked solo with no substantive prior conversation context, conduct apiary-style discovery questions to fill in the requirement groups, test-fixture conventions, existing-behavior constraints, decisions, and rationale that the Explore agent's codebase findings cannot answer alone. Use `AskUserQuestion` only for finite-choice prompts; use prose questions for free-text answers (per CLAUDE.md `## AskUserQuestion usage`). The exact questions are the skill author's call, but they must yield enough material to populate every required section without hallucinating. (Mid-conversation distillation — when prior context is rich — is added by the sibling Subtask.)

   e. **Author the SDD body.** Produce a single markdown body with these sections in this order, every section always present (filled with substance or explicitly marked empty per parent Task body item 2 — never silently omitted):
      - `## Codebase exploration findings` — architecture, affected modules, existing patterns, data models, test fixtures, configuration. Content sourced from the Explore agent's output. Cite real module names from the target repo. Use `RESEARCH NEEDED: <question>` inline where the Explore agent surfaced ambiguity.
      - `## Requirements` — numbered SR-style requirement groups (e.g., `SR-1`, `SR-2`), organized by domain/concern. The exact numbering scheme is the skill author's call; document the chosen scheme inline so cold-session agents reading downstream can cite groups consistently.
      - `## Test Fixtures` — apiary requires this section; it documents test-fixture conventions explicitly so engineers cannot hardcode magic numbers past test-helper conventions. Required even when the section's content is `none — no test-fixture conventions apply` (must be explicit, never silently omitted).
      - `## Existing Behavior` — what must NOT change. Document the contracts the implementation must preserve.
      - `## Documentation` — what docs must be written after implementation lands. Lists the cumulative project docs (PRD/SDD/README) that the doc-writer agent will need to update post-implementation.
      - `## Background and rationale` (NEW — must always be present, even if explicitly marked `none — this SDD has no captured rationale from prior conversation`)
      - `## Decisions and rejected alternatives` (NEW — same treatment; captures architectural decisions and trade-offs considered, so downstream cold-session agents do not re-litigate design choices)

      Document in the skill prose that the two NEW sections are mandatory for downstream cold-session agents and must never be silently omitted; explain the "author forgot vs no rationale" disambiguation rationale from the parent Task body. Apply the same disambiguation logic to the Test Fixtures section.

      Apiary's quality-bar guidance (adapt the spirit, not copy-paste): cite real module names (no fabricated names); include test-fixture conventions explicitly so engineers cannot hardcode magic numbers; keep architecture decisions and trade-offs traceable. State this in a `## Quality bar` subsection or inline checklist within the workflow.

   f. **Write body to scratch file.** Use the `Write` tool to author the body to a path under the namespaced workflow scratch dir (`/tmp/.bees-workflow/bees-body-<short-suffix>.md` on POSIX, `$env:TEMP\.bees-workflow\bees-body-<short-suffix>.md` on Windows). Create the `.bees-workflow` subdir if absent (`mkdir -p /tmp/.bees-workflow` on POSIX, `New-Item -ItemType Directory -Force -Path "$env:TEMP\.bees-workflow" | Out-Null` on Windows). Do NOT remove the file after the bees command exits. Mirror the snippet shapes used in `skills/bees-file-issue/SKILL.md` step 3 verbatim.

   g. **Create-or-update branching (idempotency).**
      - If no existing SDD child: `bees create-ticket --hive specs --ticket-type t1 --parent <spec-bee-id> --title SDD --body-file <path> --status drafted`. Show the command as paired POSIX bash + Windows PowerShell snippets (POSIX uses `\` line continuation; PowerShell uses backtick). Verify each flag name against `bees create-ticket --help` before authoring the prose.
      - If an existing SDD child: `bees update-ticket --ids <sdd-id> --body-file <path>`. Default to full-rewrite semantics for body coherence. Note in prose that `bees append-ticket-body --ticket-id <sdd-id> --chunk-file <path>` is available for explicit append-only revisions but is NOT the default. Verify against `bees update-ticket --help` and `bees append-ticket-body --help`.

   h. **Promote to `ready`.** When authoring completes (user has approved the draft), transition the SDD ticket from `drafted` to `ready` via `bees update-ticket --ids <sdd-id> --status ready`. The Spec Bee's transition to `ready` is sibling Epic `t1.31f.y2`'s responsibility, not this skill's.

   i. **Idempotency proof.** Document explicitly that re-running `/bees-write-sdd <same-spec-bee-id>` on the same Spec Bee updates the existing SDD ticket rather than creating a duplicate. This is the observable behavior the parent Task's acceptance criteria verifies via two consecutive runs.

6. Ensure every Bash tool call shown in the skill prose is a single literal command. No `&&`, `||`, `;`, pipes, redirects, `$VAR` expansion, command substitution, backticks-in-double-quoted-strings, or diagnostic tails. For sequenced commands (e.g., `mkdir -p` then write file then `bees create-ticket`), present them as separate Bash blocks/calls. If multi-step variable-bearing logic is needed (e.g., parsing the freeform-query JSON to extract the existing SDD ticket ID), prefer a Python helper script under `skills/bees-write-sdd/scripts/` over inline shell. Bundled-helper precedent: `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`. The Python helper option is the skill author's call — only add it if shell control flow would otherwise be needed; otherwise keep the skill prose-only.

7. Preserve project-neutral prose: do NOT bake in this repo's specific Bee IDs, paths, or workflow specifics. The skill must work in any project that has installed bees-workflow.

## Key Files

- `skills/bees-write-sdd/SKILL.md` (NEW) — the skill prose itself, frontmatter through the solo-invocation workflow including the seven required SDD sections, Explore-agent dispatch + RESEARCH NEEDED pattern, scratch-file usage, idempotent create-or-update branching, and `drafted → ready` promotion.
- `skills/bees-write-sdd/scripts/<name>.py` (OPTIONAL — only if a Python helper is genuinely needed for parsing freeform-query output or similar multi-step logic; the skill author's call).

## Acceptance Criteria

- `skills/bees-write-sdd/SKILL.md` exists with valid YAML frontmatter (`name: bees-write-sdd`, specific `description`, `argument-hint`).
- The skill prose, when followed by Claude Code on a `/bees-write-sdd <spec-bee-id>` invocation against an existing Spec Bee, would produce a `t1=Doc` child ticket titled exactly `SDD` (case-sensitive) on the Specs hive whose body contains all seven required sections in the specified order: `Codebase exploration findings`, `Requirements`, `Test Fixtures`, `Existing Behavior`, `Documentation`, `Background and rationale`, `Decisions and rejected alternatives`. (Behavioral verification of the actual run depends on the Specs hive existing in the target repo, which sibling Epic `t1.31f.ho`'s Subtask `t2.31f.ho.8h` provides.)
- The SDD body always includes `## Background and rationale`, `## Decisions and rejected alternatives`, and `## Test Fixtures` sections — the skill prose explicitly forbids silent omission and documents the disambiguation rationale.
- The skill prose dispatches the Explore subagent (`subagent_type=Explore`) to gather the `Codebase exploration findings` section's content. Verifiable by inspection of the Workflow section.
- The skill prose carries over apiary's `RESEARCH NEEDED: <question>` flag convention for marking ambiguous spots in the SDD body rather than fabricating guesses.
- The skill prose includes a brief note explaining the SRD → SDD rename so future readers of apiary docs are not confused.
- Re-invoking the skill on the same Spec Bee updates the existing SDD ticket rather than creating a duplicate. Verifiable by reading the create-or-update branching prose: it must enumerate children first, detect the existing `SDD`-titled `t1=Doc` child, and route to `bees update-ticket` rather than `bees create-ticket`.
- The skill creates the SDD ticket at `status=drafted`, then transitions to `status=ready` when authoring completes. Verifiable by reading the workflow prose.
- Scratch-file usage targets `<tempdir>/.bees-workflow/` with `mkdir -p` / `New-Item -ItemType Directory -Force` create-if-absent. No `rm` / `Remove-Item` instructions appear anywhere in the skill prose.
- Every shell snippet in the skill ships as paired POSIX bash + Windows PowerShell variants. No bash-only fallback.
- Every Bash tool call shown is a single literal command — no compound chains, pipes, redirects, or shell-variable expansion.
- The skill prose contains no hardcoded language-specific commands, file extensions, or manifest filenames. The skill is project-neutral — no references to this repo's specific paths or ticket IDs that would not make sense in a downstream project.
- Preconditions hard-fail with `Run /bees-setup first.` (plus a one-line note) when the Specs hive is missing or `## Documentation Locations` is absent from CLAUDE.md.
- All bees CLI flag names referenced in the prose match the actual flags reported by `bees create-ticket --help`, `bees update-ticket --help`, `bees show-ticket --help`, `bees execute-freeform-query --help`, and (if referenced) `bees append-ticket-body --help` — flag names are not hand-authored.
- README skill catalog update for `/bees-write-sdd` is NOT in this Subtask's scope (sibling Task `t2.31f.5u.xi` owns it).
- Mid-conversation awareness behavior and the inline-invocation Skill-tool contract are NOT in this Subtask's scope (the sibling Subtask layers them on).
- Project-level lint passes: `python -m pyflakes skills/*/scripts/*.py` succeeds (relevant only if the optional Python helper is added).
