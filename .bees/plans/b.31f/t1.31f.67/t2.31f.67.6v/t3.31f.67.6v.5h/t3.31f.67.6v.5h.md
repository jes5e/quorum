---
id: t3.31f.67.6v.5h
type: t3
title: Verify file-resolver regression then add Overview positioning prose
parent: t2.31f.67.6v
reference_materials: null
created_at: '2026-05-06T19:19:11.504987'
status: done
schema_version: '0.1'
guid: 31f676v5hp17cw4vuwowgjjetp91v76u
---

## Context

Parent Task `t2.31f.67.6v` under Epic `t1.31f.67` (parent Bee `b.31f`, `reference_materials: null`, body-as-spec). This Subtask delivers the Task's two coupled deliverables in a single pass:

- **Deliverable A** — static (read-only) verification that sibling Epic `t1.31f.4u`'s implementations preserve the file-resolver path verbatim across `agents/pm.md`, `skills/bees-breakdown-epic/SKILL.md` (Sections 2/4/5), `skills/bees-execute/SKILL.md`, and `skills/bees-fix-issue/SKILL.md`.
- **Deliverable B** — small prose update to `skills/bees-plan-from-specs/SKILL.md` clarifying positioning relative to the redesigned `/bees-plan` (sibling Epic `t1.31f.y2`).

The verification gates the prose update: if the static read finds a regression, surface it and stop — do NOT proceed to Deliverable B until the sibling Epic ships a fix. Both deliverables are small and tightly coupled (the prose change reflects the same separation-of-concerns the verification confirms), so they ship together in this one Subtask.

Note: `skills/bees-plan-from-specs/SKILL.md` today has no `## Overview` section — it jumps from the `# PRD + SDD to Plan` title directly to `## Input`. Deliverable B introduces a small new positioning block (either a `## Overview` section inserted between the title and `## Input`, or a short paragraph immediately under the title — implementer's call, but pick the option that reads most naturally). The rest of the file stays byte-identical.

## What Needs to Change

### Step 1 — Static verification (Deliverable A, read-only)

Using the `Read` tool (no shell, no edits), inspect each component below and confirm the file-resolver path is intact. For each, write down a one-line PASS / FAIL verdict plus the line range you read.

1. **`agents/pm.md`** — Confirm:
   - The `file-path` resolver branch is preserved verbatim, not harmonized with the new `bees`-resolver branch.
   - The asymmetric Path A vs Path B handling for the Scoped-marker resolution on the file-resolver path is preserved unchanged.
   - The body-as-spec fallback for null/empty `reference_materials` is preserved unchanged.

2. **`skills/bees-breakdown-epic/SKILL.md` Section 2** — Confirm:
   - The `file-path` resolver branch in the polymorphic reading logic is preserved verbatim.
   - The Scoped-marker helper invocation runs unchanged on the file-resolver path (no skip).
   - The Plan-Bee-body fallback for null/empty `reference_materials` is preserved.

3. **`skills/bees-breakdown-epic/SKILL.md` Sections 4 and 5** — Confirm:
   - The PM dispatch prompt's file-resolver acknowledgement uses the cite-PRD-§X / cite-SDD-§Y format for the file-resolver path.
   - The Spec Traceability Review's three-shape source identification correctly handles the file-resolver case unchanged.

4. **`skills/bees-execute/SKILL.md` and `skills/bees-fix-issue/SKILL.md` PM dispatch prompts** — Confirm the file-resolver path is acknowledged correctly and not contradicted by the new `bees`-resolver prose.

5. **`skills/bees-plan-from-specs/SKILL.md` itself (pre-edit baseline)** — Confirm the Step prose, all `bees create-ticket --reference-materials` invocations, all Scoped-marker emission logic, and the marker grammar are byte-identical to today. Sibling Epic `.4u` should not have touched this file at all.

Produce a `## Verification report` block at the end of the Subtask (commit message body or Subtask body) listing each component checked, the line range read, and the PASS / FAIL verdict. Overall verdict is PASS only if every component is PASS.

**Halt-on-FAIL rule.** If any component fails, stop. Do not perform Step 2. Surface the regression in the Subtask body's `## Verification report` and exit so the orchestrating skill can route the regression back to the responsible sibling Epic. Mark the Subtask with the failure verdict; do not flip to `done`.

### Step 2 — Overview positioning prose (Deliverable B, only on PASS)

Edit `skills/bees-plan-from-specs/SKILL.md` to add a small positioning block at the top of the file. The block must convey three points, in this order:

1. `/bees-plan` is for "I want to design from scratch with Claude; specs land as tickets in the Specs hive."
2. `/bees-plan-from-specs` is for "I already have PRD and SDD on disk (authored by hand or by external tooling); turn them into a Plan Bee directly."
3. The cumulative-PRD/SDD pattern is now driven only by external authoring + `/bees-plan-from-specs`, not by `/bees-plan`. State this explicitly so users understand the historical pattern is no longer auto-generated.

Implementation choice (pick whichever reads more naturally; do not do both):

- **Option A** — Insert a new `## Overview` section between the `# PRD + SDD to Plan` title (line 7) and the existing `## Input` section (line 9). One short paragraph or a 3-bullet list covering the three points.
- **Option B** — Add a short paragraph immediately under the `# PRD + SDD to Plan` title, before `## Input`, without introducing a new heading. Same three points.

Keep the prose project-neutral (no language-specific commands, no this-repo paths, no this-repo ticket IDs — design rules 1 and 3). The new prose almost certainly contains no shell snippets, but if any are added they must be paired POSIX bash + Windows PowerShell variants and one-literal-command shaped (design rule 2 + bash etiquette).

### Step 3 — Diff sanity check

After the Overview edit, confirm via `git diff skills/bees-plan-from-specs/SKILL.md` that:

- The diff is bounded to the top-of-file region (the new Overview block).
- No other section of the file is modified — Step prose, `bees create-ticket --reference-materials` invocations, Scoped-marker emission logic, and marker grammar are all byte-identical to before.

The minimal-diff property is an explicit Acceptance Criterion on the parent Task; verify it before marking the Subtask done.

## Optional — manual end-to-end smoke tests (NOT required)

The parent Task body suggests two end-to-end manual runs as a thoroughness option. They are explicitly OPTIONAL — absence is not a Subtask failure. If the implementer wants to run them, the runs are:

- `/bees-plan-from-specs <prd-path> <sdd-path>` (no `--feature`) against single-feature on-disk docs.
- `/bees-plan-from-specs <prd-path> <sdd-path> --feature "<title>"` against cumulative on-disk docs.

Skip these by default. The static verification in Step 1 is sufficient for the Task's regression-protection goal.

## Key Files

- `skills/bees-plan-from-specs/SKILL.md` — Overview positioning prose added at top of file (Deliverable B). Only file edited.
- `agents/pm.md` — read-only inspection (Deliverable A).
- `skills/bees-breakdown-epic/SKILL.md` — read-only inspection of Sections 2, 4, 5 (Deliverable A).
- `skills/bees-execute/SKILL.md` — read-only inspection of PM dispatch prompt (Deliverable A).
- `skills/bees-fix-issue/SKILL.md` — read-only inspection of PM dispatch prompt (Deliverable A).

## Acceptance Criteria

- A `## Verification report` block exists (in the Subtask commit message body or the Subtask body) listing each of the five components from Step 1 with a PASS verdict and the line range read for each. Overall verdict is PASS.
- `skills/bees-plan-from-specs/SKILL.md` contains a small positioning block at the top of the file (Option A `## Overview` section, or Option B short paragraph under the title) covering the three points: `/bees-plan` purpose, `/bees-plan-from-specs` purpose, cumulative-PRD/SDD pattern provenance.
- `git diff skills/bees-plan-from-specs/SKILL.md` shows changes bounded to the top-of-file region. No edits to Step prose, `bees create-ticket` invocations, Scoped-marker emission, or marker grammar.
- No other source file in the repo is modified.
- New prose is project-neutral (no language-specific commands, no this-repo paths or ticket IDs).
- If Step 1 produces any FAIL verdict, the Subtask halts before Step 2, surfaces the regression in the verification report, and is NOT marked `done` — the regression must be fixed in the responsible sibling Epic before this Subtask can resume.
- The optional end-to-end manual smoke tests are documented as optional; their absence is not a failure.
- Compile-check is a no-op for this repo (CLAUDE.md `Compile/type-check` is empty); Lint is `python -m pyflakes skills/*/scripts/*.py` and need not be re-run because this Subtask edits no Python helpers, but running it once after the edit to confirm green is fine.
