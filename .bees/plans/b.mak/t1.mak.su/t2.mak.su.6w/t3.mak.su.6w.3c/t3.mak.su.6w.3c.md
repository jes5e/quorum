---
id: t3.mak.su.6w.3c
type: t3
title: Update agents/test-reviewer.md to reference bees-test-writer-review
parent: t2.mak.su.6w
reference_materials: null
created_at: '2026-05-07T21:53:51.963387'
status: ready
schema_version: '0.1'
guid: maksu6w3cd3ykhu1yaw6qazu2hy373ei
---

## Context

Second Subtask of Task `t2.mak.su.6w` (Epic `t1.mak.su`, Bee `b.mak`). Epic 1 (`t1.mak.eo`) renamed `skills/bees-test-review/` to `skills/bees-test-writer-review/` and rewrote that skill's `SKILL.md` frontmatter and body. The reviewer-agent contract `agents/test-reviewer.md` still references the old skill name in three places and must be updated so the wrapped-skill `Skill`-tool dispatch resolves to the new skill directory.

This Subtask covers SDD SR-3.2.

## What Needs to Change

Replace every occurrence of `bees-test-review` with `bees-test-writer-review` in `agents/test-reviewer.md`. The three known reference sites at Task-research time were on lines 3 (frontmatter `description:`), 22 (body `Skill`-tool invocation bullet), and 23 (body closing line that defers to the wrapped skill's output contract). Line numbers may have drifted; use `grep` to locate references before editing — the `grep` AC at the bottom is the authoritative success signal.

This is a mechanical textual substitution. Do NOT modify role contracts (Responsibilities, Instructions), filenames, the YAML frontmatter shape (other than the `description:` value), the structured-output contract, or any prose beyond the textual substitution itself.

## Key Files

- `agents/test-reviewer.md` — apply the substitution

## Acceptance Criteria

- `grep -nE "bees-test-review" agents/test-reviewer.md` returns no matches.
- `grep -nE "bees-test-writer-review" agents/test-reviewer.md` returns exactly three matches (the frontmatter `description:` line plus two body lines).
- The body's `Skill`-tool invocation bullet now references the `bees-test-writer-review` skill.
- File path / filename `agents/test-reviewer.md` is unchanged.
- No other content in the file is modified (Responsibilities, Cold-start invariant section, model/tools frontmatter fields, etc.).
