---
id: t3.mak.su.6w.9p
type: t3
title: Update agents/code-reviewer.md to reference bees-engineer-review
parent: t2.mak.su.6w
reference_materials: null
created_at: '2026-05-07T21:53:50.729461'
status: done
schema_version: '0.1'
guid: maksu6w9puhmtvycqroaisugb4xgw35r
---

## Context

First Subtask of Task `t2.mak.su.6w` (Epic `t1.mak.su`, Bee `b.mak`). Epic 1 (`t1.mak.eo`) renamed `skills/bees-code-review/` to `skills/bees-engineer-review/` and rewrote that skill's `SKILL.md` frontmatter and body. The reviewer-agent contract `agents/code-reviewer.md` still references the old skill name in three places and must be updated so the wrapped-skill `Skill`-tool dispatch resolves to the new skill directory.

This Subtask covers SDD SR-3.1.

## What Needs to Change

Replace every occurrence of `bees-code-review` with `bees-engineer-review` in `agents/code-reviewer.md`. The three known reference sites at Task-research time were on lines 3 (frontmatter `description:`), 22 (body `Skill`-tool invocation bullet), and 23 (body closing line that defers to the wrapped skill's output contract). Line numbers may have drifted; use `grep` to locate references before editing — the `grep` AC at the bottom is the authoritative success signal.

This is a mechanical textual substitution. Do NOT modify role contracts (Responsibilities, Instructions), filenames, the YAML frontmatter shape (other than the `description:` value), the structured-output contract, or any prose beyond the textual substitution itself.

## Key Files

- `agents/code-reviewer.md` — apply the substitution

## Acceptance Criteria

- `grep -nE "bees-code-review" agents/code-reviewer.md` returns no matches.
- `grep -nE "bees-engineer-review" agents/code-reviewer.md` returns exactly three matches (the frontmatter `description:` line plus two body lines).
- The body's `Skill`-tool invocation bullet now references the `bees-engineer-review` skill.
- File path / filename `agents/code-reviewer.md` is unchanged.
- No other content in the file is modified (Responsibilities, Cold-start invariant section, model/tools frontmatter fields, etc.).
