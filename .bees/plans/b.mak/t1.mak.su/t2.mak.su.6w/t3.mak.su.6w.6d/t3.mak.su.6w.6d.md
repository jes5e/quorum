---
id: t3.mak.su.6w.6d
type: t3
title: Update agents/doc-reviewer.md to reference bees-doc-writer-review
parent: t2.mak.su.6w
reference_materials: null
created_at: '2026-05-07T21:53:53.203719'
status: done
schema_version: '0.1'
guid: maksu6w6dozizegppbkd5vpukxzu65zi
---

## Context

Third Subtask of Task `t2.mak.su.6w` (Epic `t1.mak.su`, Bee `b.mak`). Epic 1 (`t1.mak.eo`) renamed `skills/bees-doc-review/` to `skills/bees-doc-writer-review/` and rewrote that skill's `SKILL.md` frontmatter and body. The reviewer-agent contract `agents/doc-reviewer.md` still references the old skill name in three places and must be updated so the wrapped-skill `Skill`-tool dispatch resolves to the new skill directory.

This Subtask covers SDD SR-3.3.

## What Needs to Change

Replace every occurrence of `bees-doc-review` with `bees-doc-writer-review` in `agents/doc-reviewer.md`. The three known reference sites at Task-research time were on lines 3 (frontmatter `description:`), 26 (body `Skill`-tool invocation bullet), and 27 (body closing line that defers to the wrapped skill's output contract). Note: this file has an extra `## Model default and runtime override` section that the other two reviewer files don't, which shifts the body line numbers compared to `code-reviewer.md` / `test-reviewer.md`. Line numbers may have drifted further; use `grep` to locate references before editing — the `grep` AC at the bottom is the authoritative success signal.

This is a mechanical textual substitution. Do NOT modify role contracts (Responsibilities, Instructions), filenames, the YAML frontmatter shape (other than the `description:` value), the `## Model default and runtime override` section, the structured-output contract, or any prose beyond the textual substitution itself.

## Key Files

- `agents/doc-reviewer.md` — apply the substitution

## Acceptance Criteria

- `grep -nE "bees-doc-review" agents/doc-reviewer.md` returns no matches.
- `grep -nE "bees-doc-writer-review" agents/doc-reviewer.md` returns exactly three matches (the frontmatter `description:` line plus two body lines).
- The body's `Skill`-tool invocation bullet now references the `bees-doc-writer-review` skill.
- File path / filename `agents/doc-reviewer.md` is unchanged.
- No other content in the file is modified (Responsibilities, Cold-start invariant section, Model default and runtime override section, model/tools frontmatter fields, etc.).
