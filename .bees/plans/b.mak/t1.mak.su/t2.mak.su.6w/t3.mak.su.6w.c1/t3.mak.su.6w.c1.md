---
id: t3.mak.su.6w.c1
type: t3
title: Update agents/pm.md to reference bees-engineer-review and bees-doc-writer-review
parent: t2.mak.su.6w
reference_materials: null
created_at: '2026-05-07T21:53:54.353078'
status: done
schema_version: '0.1'
guid: maksu6wc1yizmnzfa8mf9idyqbhjg64k
---

## Context

Fourth Subtask of Task `t2.mak.su.6w` (Epic `t1.mak.su`, Bee `b.mak`). Epic 1 (`t1.mak.eo`) renamed `skills/bees-code-review/` to `skills/bees-engineer-review/` and `skills/bees-doc-review/` to `skills/bees-doc-writer-review/`, and rewrote both `SKILL.md` files. The PM role contract `agents/pm.md` references both old skill names in six places and must be updated so the in-flight orchestration paths (`Skill`-tool dispatches from the PM during per-Task review) resolve to the new skill directories.

Note: per SDD `## Codebase exploration findings`, the PM does NOT invoke `bees-test-review` (renamed in Epic 1 to `bees-test-writer-review`) — that wrapped-skill orchestration is handled by the test-reviewer subagent only, not the PM. The PM file therefore has zero references to `bees-test-review` and this Subtask makes no edit on that pair.

This Subtask covers SDD SR-3.4.

## What Needs to Change

In `agents/pm.md`, perform two mechanical textual substitutions over the whole file:

1. Replace every occurrence of `bees-code-review` with `bees-engineer-review`.
2. Replace every occurrence of `bees-doc-review` with `bees-doc-writer-review`.

The six known reference sites at Task-research time were on lines 3 (frontmatter `description:`), 8 (role-overview paragraph explaining the `Skill`-tool allowlist rationale), 19 (Responsibilities bullet on in-flight review-skill orchestration), 202 (Instructions bullet titled "In-flight review-skill orchestration"), 203 (Instructions bullet titled "Time budget — short-circuit when reviews run hot"), and 204 (Instructions bullet on ignored-feedback reporting). Line numbers may have drifted; use `grep` to locate references before editing — the `grep` AC at the bottom is the authoritative success signal.

These are mechanical textual substitutions. Do NOT modify any other prose: not the Responsibilities list (beyond the one bullet's skill-name tokens), not the Instructions list (beyond the three bullets' skill-name tokens), not the `## Resolving reference_materials entries` section, not the `## Spec-source scoping (Scoped-marker integration)` section (Path A, Path B, asymmetric error-handling, helper-resolution-path strategy), not the `## Model default and runtime override` section, not the cross-Task / cross-Epic interaction-check bullets, not the final report contract, not the Shell-command etiquette bullet, not the YAML frontmatter shape (other than the `description:` value).

## Key Files

- `agents/pm.md` — apply the two substitutions

## Acceptance Criteria

- `grep -nE "bees-code-review|bees-doc-review" agents/pm.md` returns no matches.
- `grep -nE "bees-engineer-review" agents/pm.md` returns at least four matches (frontmatter description, role-overview paragraph, Responsibilities bullet, and the Instructions-list bullets where engineer-review previously appeared as code-review).
- `grep -nE "bees-doc-writer-review" agents/pm.md` returns at least four matches (same set of reference sites where doc-review previously appeared as doc-review).
- `grep -nE "bees-test-review|bees-test-writer-review" agents/pm.md` returns no matches (PM does not invoke the test-review wrapped skill; this verifies no accidental edits introduced a test-review reference).
- File path / filename `agents/pm.md` is unchanged.
- No other content in the file is modified — diff against pre-edit content shows only skill-name token substitutions in the six identified line regions.
