---
id: t1.mak.su
type: t1
title: Update callers in agents and execution skills
up_dependencies:
- t1.mak.eo
down_dependencies:
- t1.mak.84
- t1.mak.ka
parent: b.mak
reference_materials: null
created_at: '2026-05-07T15:56:57.801846'
status: drafted
schema_version: '0.1'
guid: maksuzs2mu9pvgtc6eu59n6v2b7ae6r5
---

# Epic 2: Update callers in `agents/` and execution skills

Covers SDD `## Requirements` SR-3.

## Scope

**Note on line numbers below:** Line numbers are illustrative as of Epic authoring and may have drifted by execution time. Use `grep` to locate references before editing — the `grep` AC checks at the bottom of this Epic are the authoritative success signal.

Update every caller-file reference to the renamed three skills.

1. `agents/code-reviewer.md` — lines 3 (`description:`), 22 (`Skill`-tool invocation), 23 (output-contract reference). Replace `bees-code-review` with `bees-engineer-review`.
2. `agents/test-reviewer.md` — lines 3, 22, 23. Replace `bees-test-review` with `bees-test-writer-review`.
3. `agents/doc-reviewer.md` — lines 3, 26, 27. Replace `bees-doc-review` with `bees-doc-writer-review`.
4. `agents/pm.md` — lines 3 (`description:`), 8 (allowlist rationale), 19 (responsibility statement), 202, 203, 204 (in-flight orchestration prose with time-budget short-circuit guidance). Replace `bees-code-review` with `bees-engineer-review` and `bees-doc-review` with `bees-doc-writer-review`.
5. `skills/bees-execute/SKILL.md` — line 246 (PM dispatch line), line 357 (ignored-feedback report template), lines 433 / 467 (anti-pattern callout). Substitute all three new names.
6. `skills/bees-fix-issue/SKILL.md` — lines 374 / 407 (anti-pattern callout). Substitute all three new names.

## Acceptance criteria

- `grep -r "bees-code-review\|bees-test-review\|bees-doc-review" agents/ skills/bees-execute/ skills/bees-fix-issue/` returns no matches.
- `agents/{code,test,doc}-reviewer.md` `Skill`-tool invocations call the renamed skill via `skill="bees-{engineer|test-writer|doc-writer}-review"` (file-readable check).
- The three anti-pattern callouts in `bees-execute/SKILL.md` and `bees-fix-issue/SKILL.md` continue to assert that all three review skills should NOT be invoked for cross-lane sweeps; only the names change.

## Dependencies

Up-blocks: Epic 1 (the renamed directories must exist before callers reference them).

## Commit boundary

Shared with Epic 1 — Epics 1 and 2 land in a single git commit per PRD `## Edge Cases and Error Handling` item 1, to avoid the brief mid-rename inconsistency window where callers reference one set of names while the directories carry the other.
