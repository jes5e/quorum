---
id: t1.mak.ka
type: t1
title: Open-ticket sweep + final verification
up_dependencies:
- t1.mak.eo
- t1.mak.su
- t1.mak.84
parent: b.mak
children:
- t2.mak.ka.nf
- t2.mak.ka.wx
reference_materials: null
created_at: '2026-05-07T15:56:59.824363'
status: ready
schema_version: '0.1'
guid: makkaphxwgjs6ijia9unntxq75wsrybh
---

# Epic 4: Open-ticket sweep + final verification

Covers SDD `## Requirements` SR-5 (open-ticket sweep) and SR-6 (verification).

## Scope

**Note on line numbers anywhere below:** Line numbers in the SR enumerations referenced by Epics 1–3 are illustrative as of Epic authoring and may have drifted. Use `grep` as the authoritative success signal.

1. Run `bees execute-freeform-query` against the Plans and Issues hives filtered to open/in-progress/drafted/ready statuses with bodies containing any of the three old skill names:

   ```yaml
   stages:
     - [hive=plans, status_in=[drafted,ready,in_progress]]
     - [hive=issues, status=open]
   filter:
     body_contains_any: [bees-code-review, bees-test-review, bees-doc-review]
   report: [ticket_id, hive, ticket_status, title]
   ```

   (Adjust the filter shape to match the bees CLI's actual filter vocabulary at execution time. The expected post-Epic-1-2-3 result is exactly the **eight self-referential tickets** that describe the rename narrative itself: Spec Bee `b.mam`, its two `t1=Doc` children `t1.mam.qr` (PRD) and `t1.mam.a2` (SDD), Plan Bee `b.mak`, and `b.mak`'s four Epics `t1.mak.eo`, `t1.mak.su`, `t1.mak.84`, `t1.mak.ka`. All eight correctly describe the rename and must not be modified.)

2. For any open/in-progress ticket the query surfaces **beyond** those eight self-references: update the body in place via `bees update-ticket --body-file <path>` with the new names substituted. Done/closed tickets and the eight self-references are not touched.

3. Run final repo-wide verification:
   - `python -m pyflakes skills/*/scripts/*.py` (the repo's `Lint` build command) — expected to pass without errors.
   - Repo-wide `grep -rE "bees-code-review|bees-test-review|bees-doc-review" .` — expected to return matches only inside (a) the rewritten `CONTRIBUTING.md` "Status / type renames history" entry, (b) closed/done bees ticket files under `.bees/`, and (c) this rename's own eight self-referential bees tickets under `.bees/plans/b.mak/` (`b.mak`, `t1.mak.eo`, `t1.mak.su`, `t1.mak.84`, `t1.mak.ka`) and `.bees/specs/b.mam/` (`b.mam`, `t1.mam.qr`, `t1.mam.a2`).

## Acceptance criteria

- The `bees execute-freeform-query` open-ticket sweep returns exactly the eight self-reference tickets enumerated in Scope item 1 (no fewer, no more).
- `python -m pyflakes skills/*/scripts/*.py` returns no errors.
- Repo-wide `grep` returns no old-name matches outside the three documented exceptions above (rewritten `CONTRIBUTING.md` history entry, closed/done bees tickets, the eight self-referential rename-narrative tickets).

## Dependencies

Up-blocks: Epic 1, Epic 2, Epic 3 (all source-file rename work must be complete for the verification to be meaningful).

## Commit boundary

Standalone commit (typically just records the verification result via a commit message; no source mutations expected).
