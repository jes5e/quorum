---
id: t3.mak.84.e5.xx
type: t3
title: 'Update README.md skill catalog: rename three review-skill rows and delete the dual-mode paragraph'
parent: t2.mak.84.e5
reference_materials: null
created_at: '2026-05-07T22:03:46.481500'
status: done
schema_version: '0.1'
guid: mak84e5xx1r2rudajihuw583dpvkq1x1
---

## Context

`README.md` carries a skill-catalog table (heading `## The skills`, lines ~127-146) listing every skill in the quorum set. Three of the four review-skill rows still carry the old skill names — `/bees-code-review`, `/bees-doc-review`, `/bees-test-review` — that Epic 1 (`t1.mak.eo`) has renamed via SKILL.md frontmatter. The `/quo-spec-review` row is unaffected and must be left exactly as is.

Immediately following the four-row review-skill block, a paragraph beginning `The four reviewers (...)` describes the standalone-vs-orchestrator-driven invocation modes that Epic 1's frontmatter rewrite removes from the skill descriptions. The paragraph is now stale and must be deleted in full.

This Subtask covers SDD SR-4.1.

## What Needs to Change

1. In the `## The skills` table, update the three review-skill row labels in the leftmost column from `/bees-code-review`, `/bees-doc-review`, `/bees-test-review` to the new names from the renamed skills' SKILL.md frontmatter `name:` field (read each renamed skill's frontmatter under `skills/<new-name>/SKILL.md` to confirm — Epic `t1.mak.eo` is the rename source). The new names per Epic 1's spec are `quo-engineer-review`, `quo-doc-writer-review`, `quo-test-writer-review`. Leave the `/quo-spec-review` row exactly as is.
2. The right-column descriptions for those three rows currently start with `Primary use - invoked by ...` and `Standalone use - ad-hoc review of ...` boilerplate that mirrors the deleted dual-mode paragraph. Update each row's right-column description to match the new SKILL.md `description:` frontmatter from the renamed skill (read it from the renamed SKILL.md and copy-paste). The `/quo-spec-review` row's description stays untouched.
3. Delete the dual-mode paragraph in full (the single paragraph immediately following the table block, beginning `The four reviewers (`). Also delete the surrounding blank-line padding if doing so leaves the markdown clean (do not leave a double-blank-line gap, but do not collapse the normal one-blank-line separator that follows section breaks).
4. Do not touch any other rows, the `## Status vocabulary` section, the workflow diagram at the top of the file, or any other prose.

## Key Files

- `README.md` — the only file edited in this Subtask. Focus area: the `## The skills` table rows for the three renamed review skills plus the dual-mode paragraph immediately following.
- `skills/quo-engineer-review/SKILL.md`, `skills/quo-test-writer-review/SKILL.md`, `skills/quo-doc-writer-review/SKILL.md` — read-only references for sourcing the new row text from frontmatter.

## Acceptance Criteria

- `grep -nE "bees-code-review|bees-test-review|bees-doc-review" README.md` returns no matches.
- The three renamed skills appear as rows in the `## The skills` table with their new names in the leftmost column and their new SKILL.md frontmatter `description:` text in the right column.
- The `/quo-spec-review` row is byte-identical to its pre-edit form.
- The dual-mode paragraph (the single paragraph beginning `The four reviewers (` immediately after the table) is gone.
- The markdown still renders cleanly — no double-blank-line gaps introduced by the paragraph deletion.
- The repo's Lint command (`python -m pyflakes skills/*/scripts/*.py`) still passes (this Subtask should not affect Python helpers, but run it to confirm no collateral damage).
