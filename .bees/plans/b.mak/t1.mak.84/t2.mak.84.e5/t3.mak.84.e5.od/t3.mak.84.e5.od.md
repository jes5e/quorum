---
id: t3.mak.84.e5.od
type: t3
title: 'Update CLAUDE.md ## Review criteria for skill changes section to reference renamed review skills'
parent: t2.mak.84.e5
reference_materials: null
created_at: '2026-05-07T22:03:48.052216'
status: done
schema_version: '0.1'
guid: mak84e5odnucoj7k61ajr92h36pr8zbd
---

## Context

This repo's own `CLAUDE.md` (not the target repo's) carries a `## Review criteria for skill changes` section. The opening sentence reads: `When `bees-code-review`, `bees-test-review`, or `bees-doc-review` runs against changes in *this* repo, the three design rules above plus the scratch-file convention are mandatory review criteria layered on top of each skill's standard checks.` The same line also references `quo-spec-review` later in the sentence (in a parenthetical noting its intentional exclusion); that `quo-spec-review` reference is unaffected.

Epic 1 (`t1.mak.eo`) renamed the three review skills. This section must be updated to use the new names so that when a future contributor reads the review-criteria prose, the names match the actual skill files on disk.

This Subtask covers SDD SR-4.2.

## What Needs to Change

1. In the opening sentence of the `## Review criteria for skill changes` section, replace the three old skill names (`bees-code-review`, `bees-test-review`, `bees-doc-review`) with the new names from the renamed skills' SKILL.md frontmatter `name:` field (per Epic 1's spec: `quo-engineer-review`, `quo-test-writer-review`, `quo-doc-writer-review`).
2. Preserve the backtick formatting around each name (the existing prose wraps each name in backticks).
3. Preserve the `quo-spec-review` reference later in the same sentence — it is the spec-review skill (unaffected by Epic 1) and must be left as-is.
4. Do not touch the four bulleted criteria below the opening sentence — they reference design rules, not skill names, and are unchanged.
5. Do not touch the closing paragraph (`These criteria are additive...`).
6. Do not touch any other section of `CLAUDE.md`. Specifically: the `## Documentation Locations` and `## Build Commands` contract-key sections must remain byte-identical; the `## Bash etiquette in this repo` section must remain byte-identical; the `## Scratch-file convention` section must remain byte-identical.

## Key Files

- `CLAUDE.md` — the only file edited in this Subtask. Focus area: opening sentence of the `## Review criteria for skill changes` section.
- `skills/quo-engineer-review/SKILL.md`, `skills/quo-test-writer-review/SKILL.md`, `skills/quo-doc-writer-review/SKILL.md` — read-only references for sourcing the new names from frontmatter.

## Acceptance Criteria

- `grep -nE "bees-code-review|bees-test-review|bees-doc-review" CLAUDE.md` returns no matches.
- The opening sentence of `## Review criteria for skill changes` names the three new skills (with backtick formatting preserved).
- The `quo-spec-review` reference later in the same sentence is byte-identical to its pre-edit form.
- The four bulleted criteria, the closing paragraph, and every other section of `CLAUDE.md` are byte-identical to their pre-edit form (run `git diff CLAUDE.md` and confirm only the opening-sentence skill-name tokens changed).
- The repo's Lint command (`python -m pyflakes skills/*/scripts/*.py`) still passes.
