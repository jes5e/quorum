---
id: t2.mak.84.e5
type: t2
title: Update README.md skill catalog and CLAUDE.md review-criteria section
parent: t1.mak.84
children:
- t3.mak.84.e5.xx
- t3.mak.84.e5.od
reference_materials: null
created_at: '2026-05-07T21:59:22.139824'
status: done
schema_version: '0.1'
guid: mak84e5moqozfcjtvpp6fuhtgfp4rbas
---

# Task 1: Update `README.md` and `CLAUDE.md` to reference renamed review skills

Covers SDD `## Requirements` SR-4.1 (`README.md`) and SR-4.2 (`CLAUDE.md`).

## Context

First Task of Epic `t1.mak.84`. The customer-facing skill catalog in `README.md` and the review-criteria layered-on-top section in this repo's `CLAUDE.md` both name the three old review skills. Both files must be updated to the new names so the customer-facing catalog and the review-criteria contract reflect the renamed reality.

`README.md` carries a `## Skill catalog` table whose rows include the four review skills (the three renamed ones plus `bees-spec-review`). After Epic 1's rename, three of those rows still carry the old names, and a "dual-mode paragraph" immediately following the four-row block (line 143) describes the standalone-vs-orchestrator-driven invocation modes — that mode story is removed by Epic 1's frontmatter rewrite, so the paragraph is now stale and must be deleted.

`CLAUDE.md` (this repo's own — distinct from a target-repo `CLAUDE.md`) carries a `## Review criteria for skill changes` section that explicitly names `bees-code-review`, `bees-test-review`, and `bees-doc-review` when describing the layered-on-top design rules. After Epic 1's rename, those names are stale.

**Note on line numbers.** Line numbers cited in SDD SR-4 are illustrative and may have drifted; use `grep` to locate references before editing.

## What Needs to Change

1. **`README.md`** (per SDD SR-4.1):
   - Lines 138-140 illustrative — update the three review-skill catalog rows to the new names (`bees-engineer-review`, `bees-test-writer-review`, `bees-doc-writer-review`). Each row's name field plus any descriptive text mentioning the old name must be substituted; surrounding row structure (table column count, header row, the `bees-spec-review` row at line 141) is unchanged.
   - Line 143 illustrative — delete the dual-mode paragraph in its entirety. This is the prose explaining standalone-vs-orchestrator-driven invocation that the Epic 1 frontmatter rewrite makes obsolete. Per PRD `## Acceptance Criteria` #8, the prose explaining standalone vs orchestrator-driven invocation no longer appears.
   - Line 141 (the `bees-spec-review` row) is **unaffected** — `bees-spec-review` is not being renamed.

2. **`CLAUDE.md`** (per SDD SR-4.2):
   - Line 81 illustrative — update the `## Review criteria for skill changes` section to name the three new skills. The substitution is mechanical (`bees-code-review` → `bees-engineer-review`, etc.); the surrounding prose about review criteria layering is unchanged.

## Cross-Task contracts

- **Up-blocked at the Epic level by `t1.mak.eo` and `t1.mak.su`** (Epics 1 and 2). The renamed skill directories and the updated callers must exist before the customer-facing catalog and the review-criteria contract reference the new names.
- **No CONTRIBUTING.md edits.** That is Task 2 of this Epic.
- **No docs/sdd.md or docs/prd.md edits.** That is Task 3 of this Epic.
- **No source-code edits.** Sibling Epics `t1.mak.eo` (skill internals) and `t1.mak.su` (callers) are predecessors; their work is done.
- **No bees ticket edits.** Sibling Epic `t1.mak.ka`'s scope.

## Why

Per SDD SR-4.1 / SR-4.2 and PRD `## Functional Requirements` #8 / `## Acceptance Criteria` #8 / #9. Without these updates, the customer-facing catalog row names mismatch the actual skill directory names (a user who follows the README to invoke `/bees-code-review` finds nothing); the dual-mode paragraph dangles on prose that Epic 1 deleted from the SKILL.md frontmatter; and `CLAUDE.md`'s review-criteria layered-on-top design rules name skills that no longer exist by those names.

## Success Criteria

- `grep -nE "bees-code-review|bees-test-review|bees-doc-review" README.md CLAUDE.md` returns no matches.
- The three review-skill rows in `README.md`'s skill catalog name `bees-engineer-review`, `bees-test-writer-review`, and `bees-doc-writer-review` respectively. The `bees-spec-review` row is unchanged. The catalog table's column count and header row are unchanged.
- The dual-mode paragraph that previously followed the four-row block in `README.md` is gone (per PRD AC#8).
- `CLAUDE.md`'s `## Review criteria for skill changes` section names `bees-engineer-review`, `bees-test-writer-review`, and `bees-doc-writer-review`. The surrounding prose about review-criteria layering is unchanged.
