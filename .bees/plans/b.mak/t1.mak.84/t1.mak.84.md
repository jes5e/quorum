---
id: t1.mak.84
type: t1
title: Update top-level docs
up_dependencies:
- t1.mak.eo
- t1.mak.su
down_dependencies:
- t1.mak.ka
parent: b.mak
children:
- t2.mak.84.e5
- t2.mak.84.22
- t2.mak.84.ji
reference_materials: null
created_at: '2026-05-07T15:56:58.932203'
status: done
schema_version: '0.1'
guid: mak849qzqephewvziwni8at8p43amr99
---

# Epic 3: Update top-level docs

Covers SDD `## Requirements` SR-4 (including SR-4.5 for `docs/prd.md`).

## Scope

**Note on line numbers below:** Line numbers are illustrative as of Epic authoring and may have drifted by execution time. Use `grep` to locate references before editing — the `grep` AC checks at the bottom of this Epic are the authoritative success signal.

1. **`README.md`** lines 138–140 — update the three review-skill catalog rows to the new names. Delete line 143 (the dual-mode paragraph that follows the four-row block). Line 141 (the `bees-spec-review` row) is unaffected.
2. **`CLAUDE.md`** line 81 — update the review-criteria layered-on-top section to name the three new skills (`bees-engineer-review`, `bees-test-writer-review`, `bees-doc-writer-review`).
3. **`CONTRIBUTING.md`**:
   - Lines 13–16 (POSIX bash install symlink loop): update the skill-name list to the new names. Maintain alphabetical order; new names re-sort within the loop.
   - Lines 20–26 (PowerShell install symlink loop): same update.
   - Line 79 (rejected-suggestion note about `bees-test-review`'s read-everything alternative): update the skill name to `bees-test-writer-review`.
   - Line 92 ("Status / type renames history" entry that documents the prior `code-review` → `bees-code-review` rename): **rewrite** (not append) to record the new decision and reasoning. Replace the load-bearing "standalone invocation remains supported via the existing dual-mode prose" precedent with prose that captures: misinvocation surface from `/<x>-review` substring matches generic prompts; standalone path's value didn't justify it; renamed to role-based names mirroring `agents/<role>.md`.
4. **`docs/sdd.md`** — update to the new names at: line 22 (opening context paragraph naming the four review skills), line 38 (key components / architecture entry for the three review-skill directories), lines 48–50 (agent → skill mapping table rows), lines 76–77 (TaskList progress-UI example plus time-bounded review iteration prose), and line 195 (the `bees-spec-review` `## Architecture` paragraph that mentions the three by reference when describing structural mirroring).
5. **`docs/prd.md`** lines 84, 92, 94, 111 — update parallel-review-skill discussion to the new names (per SR-4.5 — added because AC#14's repo-wide grep would otherwise fail).

## Acceptance criteria

- `grep -E "bees-code-review|bees-test-review|bees-doc-review" README.md CLAUDE.md CONTRIBUTING.md docs/sdd.md docs/prd.md` returns matches only inside the rewritten `CONTRIBUTING.md` line 92 history entry (where the prior `code-review` → `bees-code-review` rename is being narrated).
- `README.md` line 143 (the dual-mode paragraph) is gone.
- The rewritten `CONTRIBUTING.md` history entry records the new decision and reasoning per the bullet above.

## Dependencies

Up-blocks: Epic 1 + Epic 2 (the docs reference the new names; the new state of `skills/` and `agents/` must be consistent before the docs can land coherently).

## Commit boundary

Standalone commit.
