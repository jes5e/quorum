---
id: t3.mak.84.22.c4
type: t3
title: Update CONTRIBUTING.md install loops and rejected-suggestion note with new review-skill names
down_dependencies:
- t3.mak.84.22.qy
parent: t2.mak.84.22
reference_materials: null
created_at: '2026-05-07T22:03:50.214814'
status: done
schema_version: '0.1'
guid: mak8422c4e96rr1xf9gnd7yuu4gdidkg
---

## Context

`CONTRIBUTING.md` carries three contexts that name the old review-skill directories (`bees-code-review`, `bees-doc-review`, `bees-test-review`) and must be updated to the new role-based names from Epic `t1.mak.eo` (`quo-engineer-review`, `quo-doc-writer-review`, `quo-test-writer-review`). This Subtask covers three of the four contexts in Task `t2.mak.84.22`; the fourth (the "Status / type renames history" entry rewrite) is handled by the sibling Subtask within this Task.

The three contexts here are mechanical textual substitutions. They are bundled into a single Subtask because they touch the same file and have no editorial complexity — each is a name swap with surrounding prose left intact.

This Subtask covers SDD SR-4.3 (in part — install loops + rejected-suggestion note).

## What Needs to Change

1. **POSIX bash install loop** (in the `## This is the canonical source` section, inside the bash-fenced block beginning `for s in ...`): substitute the three old names with the three new names, maintaining alphabetical ordering across the full skill list. The current loop lists `quo-breakdown-epic bees-code-review bees-doc-review quo-execute quo-file-issue quo-fix-issue quo-plan quo-plan-from-specs quo-setup quo-status bees-test-review`. After substitution, the list must read alphabetically with the new names inserted at their alphabetical positions: `quo-breakdown-epic quo-doc-writer-review quo-engineer-review quo-execute quo-file-issue quo-fix-issue quo-plan quo-plan-from-specs quo-setup quo-status quo-test-writer-review`. Re-flow the line wrapping to keep lines roughly the same length as the existing wrap.

2. **PowerShell install loop** (in the same `## This is the canonical source` section, inside the powershell-fenced block beginning `$skills = ...`): identical substitution and alphabetical re-sort to (1). Re-flow line wrapping similarly.

3. **Rejected-suggestion note** (in the `## Considered and rejected` section, the bullet beginning `**Unconditional 'read ALL test files' in bees-test-review**`): replace the literal string `bees-test-review` with `quo-test-writer-review`. Leave the surrounding prose unchanged.

## Key Files

- `CONTRIBUTING.md` — install loops in the `## This is the canonical source` section, rejected-suggestion bullet in the `## Considered and rejected` section.

## Acceptance Criteria

- POSIX install loop lists the new names in alphabetical order alongside the unchanged skill directories; no occurrence of `bees-code-review`, `bees-doc-review`, or `bees-test-review` remains in the loop.
- PowerShell install loop matches the POSIX loop's skill set in alphabetical order; no occurrence of the three old names remains.
- Rejected-suggestion bullet uses `quo-test-writer-review` and not `bees-test-review`.
- The history entry in `## Status / type renames history` is **not** modified by this Subtask — it is the deliverable of the sibling Subtask. `grep -nE "bees-code-review|bees-test-review|bees-doc-review" CONTRIBUTING.md` after this Subtask completes will still match inside the history entry (the rename narrative there references old names as historical context); that is expected and the sibling Subtask will rewrite the entry.
- The repo's Lint command (`python -m pyflakes skills/*/scripts/*.py`) still passes — no helper scripts are modified, but run it to confirm.
