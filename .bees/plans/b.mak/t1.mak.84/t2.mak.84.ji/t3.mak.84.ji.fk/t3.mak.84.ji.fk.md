---
id: t3.mak.84.ji.fk
type: t3
title: Update docs/sdd.md to reference renamed review skills
parent: t2.mak.84.ji
reference_materials: null
created_at: '2026-05-07T22:03:51.133477'
status: ready
schema_version: '0.1'
guid: mak84jifkdupw5fwwqxh4jsxwh8eiym8
---

## Context

SDD `## Requirements` SR-4.4 mandates that `docs/sdd.md` reference the three renamed review skills (`bees-engineer-review`, `bees-test-writer-review`, `bees-doc-writer-review`) in place of the legacy names (`bees-code-review`, `bees-test-review`, `bees-doc-review`). The skills themselves are renamed under sibling Epics `t1.mak.eo` (skill directories) and `t1.mak.su` (frontmatter `name` fields and other internal references); this Subtask is the doc-side mirror.

The rename is mechanical: every occurrence of the three old names in `docs/sdd.md` becomes its corresponding new name, with no semantic shifts in the surrounding prose. `bees-spec-review` is NOT renamed and must remain untouched everywhere it appears (notably in the line that currently lists all four reviewers, and in its own dedicated `## Architecture` paragraph and `## Key components` entry).

## What Needs to Change

Apply the three substitutions across all sites in `docs/sdd.md`. Sites observed in the current file at research time (line numbers approximate; do a final grep before claiming `done`):

- The `## Architecture overview` paragraph naming the four review skills as dual-mode (around line 22). Replace `bees-code-review`, `bees-doc-review`, `bees-test-review`; leave `bees-spec-review` alone.
- The `## Key components` entry that groups the three review-skill bullets (around line 38, the `**`skills/bees-code-review/`**, **`skills/bees-doc-review/`**, **`skills/bees-test-review/`** — dual-mode reviewers.` line). Rename the three skill paths inside the bolded markers; the entry's prose stays.
- The agent → skill mapping table rows (around lines 48-50): `agents/code-reviewer.md` row's Purpose column references `/bees-code-review`; `agents/doc-reviewer.md` row references `/bees-doc-review`; `agents/test-reviewer.md` row references `/bees-test-review`. Rename only the slash-command tokens; the agent-file column (`agents/code-reviewer.md` etc.) is owned by sibling Epic `t1.mak.su` and stays as-is on this Subtask.
- The `## Orchestration in execution skills` site (around lines 76-77 — SR-4.4 names both as one combined site). Two distinct grep matches inside the section:
  - Line 76 illustrative — TaskList progress-UI example containing `running /bees-code-review (~5 min)` (or similar). Rename `/bees-code-review` to `/bees-engineer-review`.
  - Line 77 illustrative — `Time-bounded review iteration` bullet that names `/bees-code-review` and `/bees-doc-review`. Rename both.
  Use `grep -nE "bees-code-review|bees-test-review|bees-doc-review" docs/sdd.md` to confirm both line matches before editing — line numbers may have drifted, but the two distinct contexts (TaskList example vs. time-bounded bullet) are both inside the same `## Orchestration in execution skills` section.
- The `### Feature: Add /bees-spec-review skill (apiary /req-review analog)` `## Architecture` paragraph (around line 195 in the SDD's reference, currently around line 211) that names the three structural analogs (`/bees-code-review`, `/bees-doc-review`, `/bees-test-review`). Rename all three.

Do not touch `bees-spec-review` references anywhere. Do not touch any `agents/*-reviewer.md` filename tokens — those are the role files. Preserve table structure, code-fence delimiters, surrounding prose, and section headers exactly.

## Key Files

- `docs/sdd.md` — the only file modified by this Subtask.

## Acceptance Criteria

- `grep -nE "bees-code-review|bees-test-review|bees-doc-review" docs/sdd.md` returns no matches.
- `grep -nE "bees-engineer-review|bees-test-writer-review|bees-doc-writer-review" docs/sdd.md` returns matches at every site enumerated in `## What Needs to Change`.
- `bees-spec-review` references in `docs/sdd.md` are unchanged (`grep -n "bees-spec-review" docs/sdd.md` returns the same line set as before this Subtask).
- The agent → skill mapping table maps `agents/code-reviewer.md` → `/bees-engineer-review`, `agents/doc-reviewer.md` → `/bees-doc-writer-review`, `agents/test-reviewer.md` → `/bees-test-writer-review`. Agent-file column entries are unchanged.
- All other prose in `docs/sdd.md` is unchanged. The diff for this Subtask shows only the three skill-name string substitutions and nothing else.
