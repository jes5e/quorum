---
id: t2.mak.84.ji
type: t2
title: Update docs/sdd.md and docs/prd.md to reference renamed review skills
parent: t1.mak.84
children:
- t3.mak.84.ji.fk
- t3.mak.84.ji.56
reference_materials: null
created_at: '2026-05-07T21:59:24.690003'
status: done
schema_version: '0.1'
guid: mak84ji819r9qjxu75wso3nr1emp5stt
---

# Task 3: Update `docs/sdd.md` and `docs/prd.md` to reference renamed review skills

Covers SDD `## Requirements` SR-4.4 (`docs/sdd.md`) and SR-4.5 (`docs/prd.md`).

## Context

Third Task of Epic `t1.mak.84`. The internal architecture doc (`docs/sdd.md`) and the project requirements doc (`docs/prd.md`) both reference the three old review-skill names in several distinct contexts. Both must be updated so the architecture description and the parallel-review-skill discussion reflect the renamed reality.

`docs/sdd.md` references the old names at five distinct sites per SDD `## Codebase exploration findings`:

- Line 22 illustrative — the opening context paragraph naming the four review skills.
- Line 38 illustrative — a key-components / architecture entry for the three review-skill directories.
- Lines 48-50 illustrative — agent → skill mapping table rows showing each reviewer agent wraps a corresponding skill.
- Lines 76-77 illustrative — TaskList progress-UI example plus time-bounded review iteration prose in the orchestration-mechanics section.
- Line 195 illustrative — the `quo-spec-review` `## Architecture` paragraph that mentions the three review skills by reference when describing structural mirroring.

`docs/prd.md` references the old names at four distinct sites:

- Lines 84, 92, 94, 111 illustrative — parallel-review-skill discussion. Per SDD SR-4.5, this file was added to the rename's scope because PRD AC#14's repo-wide grep would otherwise fail.

**Note on line numbers.** Line numbers cited in SDD SR-4.4 / SR-4.5 are illustrative and may have drifted by execution time; use `grep` to locate references before editing — the `grep` AC at the bottom is the authoritative success signal regardless of line drift.

## What Needs to Change

Update two files. Per-file substitutions:

1. **`docs/sdd.md`** (per SDD SR-4.4): `bees-code-review` → `quo-engineer-review`, `bees-test-review` → `quo-test-writer-review`, `bees-doc-review` → `quo-doc-writer-review` everywhere they appear (five distinct sites enumerated above; use `grep -nE "bees-code-review|bees-test-review|bees-doc-review" docs/sdd.md` to locate the current set). The agent → skill mapping table's row count, column structure, and the surrounding prose about agent-to-skill wrapping are unchanged. The TaskList progress-UI example's structure is unchanged. The opening context paragraph's other prose is unchanged.

2. **`docs/prd.md`** (per SDD SR-4.5): same three substitutions everywhere they appear (four distinct lines illustrative). The parallel-review-skill discussion's substantive points are unchanged; only the skill-name strings are substituted.

For both files, the substitutions are mechanical textual replacements. Do NOT modify any other prose, section headers, or document structure.

## Cross-Task contracts

- **Up-blocked at the Epic level by `t1.mak.eo` and `t1.mak.su`** (Epics 1 and 2).
- **No README.md, CLAUDE.md, or CONTRIBUTING.md edits.** Tasks 1 and 2 of this Epic.
- **No source-code edits.** Sibling Epics `t1.mak.eo` and `t1.mak.su`' scopes.
- **No bees ticket edits.** Sibling Epic `t1.mak.ka`'s scope.

## Why

Per SDD SR-4.4 / SR-4.5 and PRD `## Functional Requirements` #8 / `## Acceptance Criteria` #11 / #14. Without these updates, the architecture doc's agent → skill mapping table and the orchestration-mechanics example dangle on skill names that no longer exist; the PRD's parallel-review-skill discussion does the same; and AC#14's repo-wide grep would surface old-name matches in both files outside of the documented exception classes.

## Success Criteria

- `grep -nE "bees-code-review|bees-test-review|bees-doc-review" docs/sdd.md docs/prd.md` returns no matches.
- `docs/sdd.md`'s agent → skill mapping table (illustrative lines 48-50) names `quo-engineer-review`, `quo-test-writer-review`, `quo-doc-writer-review` mapped to `code-reviewer`, `test-reviewer`, `doc-reviewer` respectively. Table column count and structure are unchanged.
- `docs/sdd.md`'s opening context paragraph (illustrative line 22) names the three new skills plus `quo-spec-review` (still four review skills total).
- `docs/sdd.md`'s `quo-spec-review` `## Architecture` paragraph (illustrative line 195) names the three new skills when describing structural mirroring.
- `docs/prd.md`'s parallel-review-skill discussion uses the new names. The substantive points about parallelism are unchanged; only skill-name strings are substituted.
