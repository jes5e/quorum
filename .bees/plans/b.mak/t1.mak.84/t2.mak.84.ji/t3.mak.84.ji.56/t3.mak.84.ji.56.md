---
id: t3.mak.84.ji.56
type: t3
title: Update docs/prd.md to reference renamed review skills
parent: t2.mak.84.ji
reference_materials: null
created_at: '2026-05-07T22:03:51.856578'
status: done
schema_version: '0.1'
guid: mak84ji56vj1a1tjwuw4566sngusad7a
---

## Context

SDD `## Requirements` SR-4.5 mandates that `docs/prd.md` reference the three renamed review skills (`quo-engineer-review`, `quo-test-writer-review`, `quo-doc-writer-review`) in place of the legacy names (`bees-code-review`, `bees-test-review`, `bees-doc-review`). This Subtask is the PRD-side mirror of the SDD update done under the sibling Subtask in this Task.

The rename is mechanical: every occurrence of the three old names in `docs/prd.md` becomes its corresponding new name, with no semantic shifts in the surrounding prose. `quo-spec-review` is NOT renamed and must remain untouched.

## What Needs to Change

Apply the three substitutions across all sites in `docs/prd.md`. Sites observed in the current file at research time (line numbers approximate; do a final grep before claiming `done`):

- The `### Feature: Side-effect-free /quo-plan and /quo-file-issue with preserved context` `**Out of scope.**` bullet that lists deferred work (around line 84): `parallel to /bees-code-review, /bees-test-review, /bees-doc-review`. Rename all three.
- The `### Feature: Add /quo-spec-review skill (apiary /req-review analog)` `**What.**` paragraph (around line 92) that names the three structural analogs (`/bees-code-review`, `/bees-doc-review`, `/bees-test-review`). Rename all three.
- The same feature's `**Why.**` paragraph (around line 94) that names the four-reviewer parallel: `/bees-code-review`, `/bees-doc-review`, `/bees-test-review`, `/quo-spec-review`. Rename the first three; leave `/quo-spec-review` alone.
- The same feature's `**Out of scope.**` bullet (around line 111) about the cumulative project-level PRD/SDD: `/bees-doc-review's territory`. Rename to `/quo-doc-writer-review's territory`.

Do not touch `quo-spec-review` references anywhere. Preserve list structure, surrounding prose, and section headers exactly.

## Key Files

- `docs/prd.md` — the only file modified by this Subtask.

## Acceptance Criteria

- `grep -nE "bees-code-review|bees-test-review|bees-doc-review" docs/prd.md` returns no matches.
- `grep -nE "quo-engineer-review|quo-test-writer-review|quo-doc-writer-review" docs/prd.md` returns matches at every site enumerated in `## What Needs to Change`.
- `quo-spec-review` references in `docs/prd.md` are unchanged (`grep -n "quo-spec-review" docs/prd.md` returns the same line set as before this Subtask).
- All other prose in `docs/prd.md` is unchanged. The diff for this Subtask shows only the three skill-name string substitutions and nothing else.
