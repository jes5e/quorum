---
id: t3.mak.eo.zp.23
type: t3
title: Substitute renamed skill references in skills/bees-spec-review/SKILL.md
parent: t2.mak.eo.zp
reference_materials: null
created_at: '2026-05-07T21:43:43.156987'
status: done
schema_version: '0.1'
guid: makeozp23o7b18r8cujwg9vfhzb855dr
---

## Context

Third and final phase of Epic `t1.mak.eo` (renaming the three review skills). Sibling Tasks rename `bees-code-review` → `bees-engineer-review`, `bees-test-review` → `bees-test-writer-review`, and `bees-doc-review` → `bees-doc-writer-review`. The `bees-spec-review` skill itself is NOT being renamed, but its prose references the three renamed skills in two places. Per SDD SR-2.5 and AC#14 (repo-wide grep returns no old-name matches), this Subtask updates those references so the post-rename grep is clean.

Research confirmed the references at the line numbers cited in SDD SR-2.5 (line numbers may drift; use grep at execution time):

- Line 9 — `## Overview` paragraph naming all three old skills together as conceptual analogs.
- Lines 50, 51, 52 — three out-of-scope bullets, one per line, each naming a single old skill (`/bees-code-review`'s territory, `/bees-test-review`'s territory, `/bees-doc-review`'s territory).

No dependency on the sibling Epics' implementation order — the rename targets are static strings, so this Subtask can run independently of `t1.mak.su`, `t1.mak.84`, and `t1.mak.ka`.

## What Needs to Change

In `skills/bees-spec-review/SKILL.md`, perform exact-string substitutions for all four occurrences:

- `bees-code-review` → `bees-engineer-review` (appears on lines 9 and 50)
- `bees-test-review` → `bees-test-writer-review` (appears on lines 9 and 51)
- `bees-doc-review` → `bees-doc-writer-review` (appears on lines 9 and 52)

Use `grep -nE 'bees-code-review|bees-test-review|bees-doc-review' skills/bees-spec-review/SKILL.md` to locate the exact current line set before editing in case line numbers have drifted from the SDD's illustrative citations.

Substitution-only scope:

- Preserve the surrounding prose verbatim — the conceptual-analogs paragraph and three out-of-scope bullets must remain semantically intact, only the skill names change.
- Preserve leading slashes where present (the line-9 paragraph cites the skills with leading slashes as `/bees-code-review`, `/bees-test-review`, `/bees-doc-review`; lines 50-52 also use leading slashes). The substitution is on the bare skill name; the slash prefix stays attached.
- Do NOT make any other edits — no frontmatter changes, no section reordering, no other prose tweaks.

Out of scope (handled by sibling Epics, do NOT touch):

- Caller skills' references (`t1.mak.su`).
- Top-level docs like README.md (`t1.mak.84`).
- Ticket-body sweep across the bees workspace (`t1.mak.ka`).

## Key Files

- `skills/bees-spec-review/SKILL.md` — the only file edited.

## Acceptance Criteria

- `grep -nE 'bees-code-review|bees-test-review|bees-doc-review' skills/bees-spec-review/SKILL.md` returns no matches (exit code 1 / no output).
- `grep -nE 'bees-engineer-review|bees-test-writer-review|bees-doc-writer-review' skills/bees-spec-review/SKILL.md` returns matches on the conceptual-analogs paragraph (three new names on the same line) plus on each of the three out-of-scope bullets (one new name per line). Use `grep -c` for per-line count and `grep -o ... | wc -l` for per-occurrence count as needed.
- The conceptual-analogs paragraph (formerly line 9) reads as a coherent sentence with the three new skill names in place — no leftover slashes, dangling commas, or missing words.
- Each of the three out-of-scope bullets (formerly lines 50, 51, 52) names exactly one new skill name and otherwise reads identically to the pre-edit bullet.
- Repo-wide spot check: `grep -rE 'bees-code-review|bees-test-review|bees-doc-review' skills/bees-spec-review/` returns no matches (this file is the only place under `skills/bees-spec-review/` where these names appear; the broader repo-wide AC#14 sweep is owned by sibling Epic `t1.mak.ka`).
