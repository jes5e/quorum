---
id: t1.a82.tx
type: t1
title: Rewrite README for ticket-agnostic framing and drop Apiary references
parent: b.a82
status: drafted
created_at: '2026-05-08T15:13:40.366725'
schema_version: '0.1'
guid: a82txmm9dvdfecduhbeodao97ffexxk9
---

Localized rewrite of `README.md` to:

1. Reframe the lead paragraph (currently around line 3) as portable + ticket-system-agnostic, naming bees as the supported backend today and beads as planned future support (link: `https://github.com/gastownhall/beads`). Plan Bee `b.9xr` ("Optional beads backend") tracks the actual code work for beads support; this rewrite is the user-facing announcement of that direction.
2. Rewrite the `## Why this exists` section (currently lines 41-52) to stand on its own merits, without comparative framing against any other skill set or project. Retain the five priority bullets (cumulative project-level docs, language-agnostic, cross-platform, plain-English statuses, PRD/SDD-first or scope-first, idempotent setup) substantively; editorial polish allowed but not required.
3. Drop the Apiary fallback line in `## Coming soon: optional skills` (currently around line 192). Retain the two listed optional-skill capabilities (async-worktree session management, multi-repo orchestration).
4. Drop the Apiary clause in `## Credits` (currently around line 209) while retaining the bees credit (bees is the current supported backend).
5. Re-verify the workflow diagram (currently lines 5-37) against the current skill set. The Explore agent's audit at SDD-authoring time found zero drift — all 10 referenced skills exist under `skills/`. Update only if new drift is surfaced during implementation.
6. Re-verify the skill catalog table (currently lines 129-144) against `skills/` and each `SKILL.md` frontmatter. The Explore agent's audit found 14 rows / 14 directories / zero semantic drift. Update only if new drift is surfaced during implementation.

The PRD `t1.43w.jj` and SDD `t1.43w.nj` under Spec Bee `b.43w` (referenced via the parent Plan Bee's `reference_materials`) carry the full requirement and design context.

## Acceptance Criteria

(Carried forward verbatim from PRD `## Acceptance Criteria` so this Epic carries the same gating bar that the PRD declares.)

1. `grep -i "apiary" README.md` returns zero matches.
2. The first paragraph after the H1 names quorum as portable, names structured ticket systems (or equivalent agnostic framing), names bees as supported today, and names beads as planned future support with a link.
3. The `## Why this exists` section contains no comparative framing against other skill sets (no `alternative`, `vs`, or `compared to` phrasing pointing at a different project).
4. The skill catalog table's row count equals the count of `skills/quo-*` directories under `skills/` (excluding `_shared/`). At time of authoring this Epic that count is 14.
5. For each skill catalog row, the row's first sentence is substantively consistent with the corresponding `SKILL.md` frontmatter `description` field (paraphrase OK; semantic divergence is not).
6. The workflow diagram references only skills that exist in `skills/` at the time of merge.
7. The `## Coming soon: optional skills` section retains a roadmap of planned optional skills and contains no Apiary fallback line.
8. The Credits section drops the Apiary mention; the bees credit remains.
9. The cross-platform install commands (POSIX bash + Windows PowerShell) are both present, both well-formed, and both reflect the current install path.
10. No file other than `README.md` is modified by this work.

## Constraints

- README edits only. No skill prose, helper script, agent file, or other doc files touched.
- Markdown structure preserved (no top-level `##` heading added or removed).
- The `## Status vocabulary`, `## Where docs live`, and `## Contributing` sections are not modified.
- The cumulative-doc fold step at end of `/quo-execute` should be skipped for this Plan Bee — see Plan Bee body's `## Anticipated doc impact` for rationale.

## Dependencies

None. This Epic has no `up_dependencies`.

## Notes for breakdown

The work is small enough that it likely fits a small handful of Tasks (one per affected README section, or possibly one Task with several Subtasks for an even tighter grouping). Engineer dispatched by `/quo-execute` will edit the README; Test Writer's role is light (no automated tests apply — verification is the manual checks in Acceptance Criteria); Doc Writer's role at end-of-Bee should be a no-op fold step per the Plan Bee's `## Anticipated doc impact` section.

