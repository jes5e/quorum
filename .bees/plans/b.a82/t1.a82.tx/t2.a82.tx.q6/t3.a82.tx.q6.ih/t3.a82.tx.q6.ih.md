---
id: t3.a82.tx.q6.ih
type: t3
title: Rewrite Why this exists section to drop comparative Apiary framing
up_dependencies:
- t3.a82.tx.q6.i9
down_dependencies:
- t3.a82.tx.q6.o5
parent: t2.a82.tx.q6
reference_materials: null
created_at: '2026-05-08T15:25:28.693010'
status: done
schema_version: '0.1'
guid: a82txq6ih3r8b7njgx5wog66xnbq7ss9
---

## Context

Second Subtask of the README rewrite under Task `t2.a82.tx.q6`. The current `## Why this exists` section (lines 41-52) opens by positioning quorum as an alternative to Apiary and closes by directing readers to Apiary if they want a different experience. Per Spec Bee `b.43w` SR-2 and the Task body item 2, this section must be rewritten to stand on its own merits, with all comparative-against-Apiary framing dropped.

Depends on the lead-paragraph rewrite Subtask completing first to avoid concurrent edits to the same file. The five priority bullets (cumulative project-level docs, language-agnostic, cross-platform, plain-English statuses, PRD/SDD-first or scope-first, idempotent) must be retained substantively; editorial polish is allowed but not required.

## What Needs to Change

In `/Users/jesseg/code/quorum/README.md`:

- **Lines 43-44** (the section opener): drop the `[Apiary](https://github.com/gabemahoney/apiary), built by the bees creator, is the original bees skill set and remains a great fit for many projects.` sentence and the `**quorum is an alternative**, shaped by these priorities:` lead-in. Replace with an opener that introduces the priorities list on quorum's own merits (no `alternative`, `vs`, or `compared to` phrasing pointing at a different project).
- **Lines 45-50** (the five priority bullets): retain substantively. Bullet content does not need to change. Editorial polish is allowed but not required.
- **Line 52** (the comparative closing paragraph `If you want the lightweight, ephemeral-spec, async-team-spawning experience of Apiary, use Apiary...`): drop entirely.
- The `## Why this exists` H2 heading itself stays.

## Key Files

- `/Users/jesseg/code/quorum/README.md` — rewrite lines 41-52 (the `## Why this exists` section).

## Acceptance Criteria

- The `## Why this exists` H2 heading is still present.
- The section contains no comparative framing against other skill sets (no `alternative`, `vs`, or `compared to` phrasing pointing at a different project; no occurrence of `Apiary` in the section).
- The five priority bullets (cumulative project-level docs, language-agnostic, cross-platform, plain-English statuses, PRD/SDD-first or scope-first, idempotent) are still present, each substantively unchanged.
- The opener introduces the priorities list without referencing another project.
- The closing comparative paragraph (former line 52) is removed.
- No other section of the file is modified by this Subtask.
