---
id: t3.a82.tx.q6.2c
type: t3
title: Re-verify skill catalog table against SKILL.md frontmatter descriptions
up_dependencies:
- t3.a82.tx.q6.gc
parent: t2.a82.tx.q6
reference_materials: null
created_at: '2026-05-08T15:25:45.606241'
status: done
schema_version: '0.1'
guid: a82txq62ciwy7a9byux5paeyuo2cqo8c
---

## Context

Sixth and final Subtask of the README rewrite under Task `t2.a82.tx.q6`. Per Spec Bee `b.43w` SR-5 and Task body item 6, the skill catalog table (lines 129-144 of `/Users/jesseg/code/quorum/README.md`) must (a) have a row count equal to the number of `skills/quo-*` directories under `skills/` (excluding `_shared/`), and (b) for each row, have the row's first sentence be substantively consistent with the corresponding `SKILL.md` frontmatter `description` field.

The Explore agent's audit at SDD-authoring time confirmed the table is accurate, so this is a **verification-only** Subtask unless drift surfaces. There are exactly 14 `quo-*` skill directories at the time of Task creation (verified by `Grep` for `^name:` across `skills/**/SKILL.md`).

This Subtask is also the natural place to **affirmatively verify the post-rewrite well-formedness of the cross-platform install commands** (PRD AC#9 / SR-8.2's code-block-validity intent). No earlier Subtask edits the `## Install` section; AC#9 holds by non-modification, but it should still be explicitly confirmed at the end of the Subtask chain so a downstream reviewer can trace AC#9 to a Subtask rather than to an implicit guard.

Depends on the workflow-diagram verification Subtask completing first to keep all sequential README edits in a clean linear chain.

## What Needs to Change

In `/Users/jesseg/code/quorum/README.md`:

- Read the skill catalog table (lines 129-144 in the original file; line numbers may shift after earlier Subtasks).
- Count the table rows (excluding the header row and the `|---|---|` separator). Confirm the count equals 14.
- For each table row, identify the skill name in column 1 and the description text in column 2. Read the corresponding `skills/<skill-name>/SKILL.md` frontmatter `description` field.
- For each row, compare the row's first sentence in column 2 against the SKILL.md `description`. Confirm semantic consistency (paraphrase OK; semantic divergence is not).
- If zero drift surfaces in the catalog, no edit is needed; the Subtask completes by recording the verification as passed.
- If catalog drift surfaces (row count mismatch, or a row whose first sentence semantically diverges from its SKILL.md description), update the table to restore consistency — minimum-scope changes only. If the row count changes, **also update the `14 skills` prose claim at line 187 in lockstep** (per SR-5.3).
- **Affirmatively verify the `## Install` section's cross-platform install commands** (PRD AC#9): read the section and confirm both the POSIX bash code block and the Windows PowerShell code block are present, are syntactically valid markdown fenced blocks, and reflect the current install path (i.e., they have not been corrupted by any earlier Subtask's edits despite each Subtask asserting "no other section modified"). This is a no-modification verification step — if the section is intact (the expected case for this rewrite), no edit is made; if it has been disturbed (unexpected — would indicate a defect in an earlier Subtask), surface and fix.

## Key Files

- `/Users/jesseg/code/quorum/README.md` — skill catalog table block (around lines 129-144) and `## Install` section (around lines 60-127).
- `/Users/jesseg/code/quorum/skills/quo-*/SKILL.md` — frontmatter `description` field, one per skill (read-only reference).

## Acceptance Criteria

- The skill catalog table's row count equals 14 (matching the count of `skills/quo-*` directories under `skills/`, excluding `_shared/`).
- For each skill catalog row, the row's first sentence is substantively consistent with the corresponding `SKILL.md` frontmatter `description` field (paraphrase OK; semantic divergence is not).
- The `14 skills` prose claim at line 187 is consistent with the actual table row count post-rewrite (per SR-5.3).
- The `## Install` section retains both code blocks (POSIX bash + Windows PowerShell), both well-formed and syntactically valid, both reflecting the current install path (PRD AC#9 affirmative check).
- If no drift was found in either the catalog or the install section, both blocks are byte-for-byte identical to their pre-Subtask state.
- If drift was found, the changes are minimum-scope.
- After this Subtask completes, all ten parent-Task `## Acceptance Criteria` items hold (overall-Task gate; this Subtask is the last one in the chain).
- No file other than `/Users/jesseg/code/quorum/README.md` is modified by this Subtask (or by the Task as a whole).
