---
id: t3.a82.tx.q6.gc
type: t3
title: Re-verify workflow diagram against current skills directory
up_dependencies:
- t3.a82.tx.q6.7y
down_dependencies:
- t3.a82.tx.q6.2c
parent: t2.a82.tx.q6
reference_materials: null
created_at: '2026-05-08T15:25:39.736998'
status: done
schema_version: '0.1'
guid: a82txq6gc6c79pbdq69a9sbp6221eik9
---

## Context

Fifth Subtask of the README rewrite under Task `t2.a82.tx.q6`. Per Spec Bee `b.43w` SR-4 and Task body item 5, the workflow diagram (lines 5-37 of `/Users/jesseg/code/quorum/README.md`) must reference only skills that exist in `skills/` at the time of merge. The Explore agent's audit at SDD-authoring time confirmed the diagram is accurate against the present-day `skills/` directory, so this is a **verification-only** Subtask unless drift surfaces.

Depends on the Apiary-removal Subtasks completing first so that this verification runs over the post-rewrite state of the file (no compounding-edit risk).

## What Needs to Change

In `/Users/jesseg/code/quorum/README.md`:

- Read the workflow diagram (lines 5-37 in the original file; line numbers may shift after earlier Subtasks).
- Enumerate every skill name referenced in the diagram (e.g., `/quo-setup`, `/quo-plan`, `/quo-plan-from-specs`, `/quo-write-prd`, `/quo-write-sdd`, `/quo-spec-review`, `/quo-breakdown-epic`, `/quo-execute`, `/quo-file-issue`, `/quo-fix-issue`).
- Confirm each referenced skill has a corresponding `skills/<name>/SKILL.md` directory in `/Users/jesseg/code/quorum/skills/` (use `Grep` for `^name:` across `skills/**/SKILL.md` to enumerate the actual catalog — there should be 14).
- If zero drift surfaces, no edit is needed; the Subtask completes by recording the verification as passed.
- If drift surfaces (a referenced skill is missing, or an existing skill is omitted from the diagram in a way that breaks the workflow narrative), update the diagram to match reality — but make only the minimum change needed to restore accuracy.

## Key Files

- `/Users/jesseg/code/quorum/README.md` — workflow diagram block (around lines 5-37).
- `/Users/jesseg/code/quorum/skills/` — reference for verification (read-only).

## Acceptance Criteria

- Every skill name referenced in the workflow diagram corresponds to an existing `skills/<name>/SKILL.md` in the repo.
- Every skill that should logically appear in the workflow narrative (the Plan -> Breakdown -> Execute -> File-issue/Fix-issue chain) is present in the diagram.
- If no drift was found, the diagram is byte-for-byte identical to its pre-Subtask state.
- If drift was found, the change is minimum-scope and matches the actual `skills/` directory.
- No other section of the file is modified by this Subtask.
