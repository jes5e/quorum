---
id: t2.a82.tx.q6
type: t2
title: Apply the README rewrite per SR-1 through SR-7
parent: t1.a82.tx
children:
- t3.a82.tx.q6.i9
- t3.a82.tx.q6.ih
- t3.a82.tx.q6.o5
- t3.a82.tx.q6.7y
- t3.a82.tx.q6.gc
- t3.a82.tx.q6.2c
reference_materials: null
created_at: '2026-05-08T15:20:31.889437'
status: ready
schema_version: '0.1'
guid: a82txq6k7vpbruru33ii7j9i3emi8zmw
---

## Context

This Task implements the README rewrite scope captured in the parent Epic `t1.a82.tx`. The Spec Bee `b.43w` (PRD `t1.43w.jj`, SDD `t1.43w.nj`) carries the full requirements; the Plan Bee `b.a82` body carries the `## Anticipated doc impact` note (no cumulative-PRD/SDD fold needed for this Plan Bee — the work IS the customer-facing-doc change).

The Explore agent's audit at SDD-authoring time confirmed the workflow diagram (lines 5–37) and skill catalog table (lines 129–144) are accurate against the present-day `skills/` directory and each `SKILL.md` frontmatter. So SR-4 (workflow diagram) and SR-5 (skill catalog) are **verification-only** requirements unless drift surfaces during implementation.

## What Needs to Change

Localized rewrite of `README.md`:

1. **Lead paragraph** (currently around line 3): reframe as portable + ticket-system-agnostic, naming **bees** as the supported backend today and **beads** (link: `https://github.com/gastownhall/beads`) as planned future support. The phrase "structured ticket systems" (or equivalent agnostic framing) must appear; the cross-platform / cross-language portability story should be retained.
2. **`## Why this exists` section** (currently lines 41–52): rewrite to stand on its own merits. Drop the comparative "vs Apiary" framing in the opener (lines 43–44: `[Apiary](...), built by the bees creator, ...` and `**quorum is an alternative**, shaped by these priorities:`). Drop the comparative closing paragraph at line 52 (`If you want the lightweight, ephemeral-spec, async-team-spawning experience of Apiary, use Apiary...`). Retain the five priority bullets substantively (cumulative project-level docs, language-agnostic, cross-platform, plain-English statuses, PRD/SDD-first or scope-first, idempotent). Editorial polish allowed but not required.
3. **`## Coming soon: optional skills` section** (currently around line 192): drop the Apiary fallback line. Retain the two listed optional-skill capabilities (async-worktree session management, multi-repo orchestration) and the closing stack-specific-helpers paragraph.
4. **`## Credits` section** (currently around line 209): drop the Apiary clause. Retain the bees credit (`Built on top of the [bees](...) ticket management system by Gabe Mahoney.`).
5. **Re-verify the workflow diagram** (lines 5–37) against the current `skills/` directory. Update only if drift surfaces beyond what the SDD-time audit found.
6. **Re-verify the skill catalog table** (lines 129–144) against the current `skills/` directory and each skill's `SKILL.md` frontmatter `description` field. Update only if drift surfaces beyond what the SDD-time audit found.

## Key Files

- `/Users/jesseg/code/quorum/README.md` — the only file modified by this Task.

## Acceptance Criteria

(Carried forward from PRD `## Acceptance Criteria`; the Task is complete only when every criterion holds.)

1. `grep -i "apiary" README.md` returns zero matches.
2. The first paragraph after the H1 names quorum as portable, names structured ticket systems (or equivalent agnostic framing), names bees as supported today, and names beads as planned future support with a link.
3. The `## Why this exists` section contains no comparative framing against other skill sets (no `alternative`, `vs`, or `compared to` phrasing pointing at a different project).
4. The skill catalog table's row count equals 14 (matching the count of `skills/quo-*` directories under `skills/`, excluding `_shared/`).
5. For each skill catalog row, the row's first sentence is substantively consistent with the corresponding `SKILL.md` frontmatter `description` field (paraphrase OK; semantic divergence is not).
6. The workflow diagram references only skills that exist in `skills/` at the time of merge.
7. The `## Coming soon: optional skills` section retains a roadmap of planned optional skills and contains no Apiary fallback line.
8. The Credits section drops the Apiary mention; the bees credit remains.
9. The cross-platform install commands (POSIX bash + Windows PowerShell) are both present, both well-formed, and both reflect the current install path.
10. No file other than `README.md` is modified by this Task.

## Constraints / Cross-Task contracts

- **Doc-only Task.** README edits only. No skill prose, helper script, agent file, or other doc files touched. The Engineer subagent should NOT be dispatched at execute time for this Task — the work is markdown editing, owned by the Doc Writer.
- **Markdown structure preserved.** No top-level `##` heading added or removed. The `## Status vocabulary`, `## Where docs live`, and `## Contributing` sections are not modified.
- **Doc-writer agent dispatched at end of `/quo-execute`** should explicitly **skip** the cumulative-PRD/SDD fold step for this Plan Bee. Rationale lives in Plan Bee `b.a82` `## Anticipated doc impact`: the work IS the customer-facing-doc change; there is no functional artifact to fold into cumulative project docs.
- **No automated test fixtures apply** (per SDD `## Test Fixtures`). Verification is the manual `## Acceptance Criteria` checks; the Test Writer subagent should NOT be dispatched at execute time for this Task.
