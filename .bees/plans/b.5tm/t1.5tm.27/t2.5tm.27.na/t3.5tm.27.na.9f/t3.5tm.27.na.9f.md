---
id: t3.5tm.27.na.9f
type: t3
title: Extend README install commands and intro prose for subagents/
parent: t2.5tm.27.na
egg: null
created_at: '2026-05-03T11:46:31.859306'
status: done
schema_version: '0.1'
guid: 5tm27na9f56mnv7eh9j6wm8u2zp3gksv
---

## Context

The README's `## Install` section (heading at line 61) currently documents two install patterns — Option A (global, recommended for single-user machines) and Option B (per-project) — each with paired POSIX bash and Windows PowerShell snippets. Each pattern's snippet has one line that copies `skills/*` into `~/.claude/skills/` (Option A) or `<repo>/.claude/skills/` (Option B). With the introduction of the new top-level `subagents/` directory in T1/T2/T3, the install section must also instruct users to copy `subagents/*` into the corresponding `~/.claude/subagents/` (Option A) or `<repo>/.claude/subagents/` (Option B) location.

The four edit sites are at:
- Option A POSIX (around line 70) — existing `cp -r` line for `skills/`
- Option A PowerShell (around line 74) — existing `Copy-Item` line for `skills/`
- Option B POSIX (around line 83) — existing `cp -r` line for `skills/`
- Option B PowerShell (around line 86) — existing `Copy-Item` line for `skills/`

The intro prose (around lines 65 and 79) currently references only "skills"; after this edit each prose intro must also acknowledge that subagent definitions are part of the install — a small parallel adjustment, sentence-level, not a paragraph rewrite.

**Sibling-Epic guard:** Epic F (t1.5tm.fy) will DELETE the "Required: enable Agent Teams" and "Display backend" sections (around lines 30-59 in the current README) and add a one-sentence replacement near the workflow diagram. This subtask MUST NOT touch those sections; doing so prematurely removes Agent Teams setup prose while sibling Epics B/C/D are still using Agent Teams.

## What Needs to Change

- Edit `README.md` `## Install` section only. No edits anywhere else in the file.
- For each of the four install snippets, add a parallel copy line for `subagents/*` immediately below the existing `skills/*` line. The new lines mirror the existing line's syntax exactly:
  - Option A POSIX: add a `cp -r ...subagents/* ~/.claude/subagents/` line below the existing skills copy line.
  - Option A PowerShell: add a `Copy-Item -Recurse ...subagents\* $HOME\.claude\subagents\` line below the existing skills copy line.
  - Option B POSIX: add a `cp -r ...subagents/* /path/to/your/repo/.claude/subagents/` line below the existing skills copy line.
  - Option B PowerShell: add a `Copy-Item -Recurse ...subagents\* C:\path	o\your\repo\.claude\subagents\` line below the existing skills copy line.
- Adjust the intro prose for each option (around lines 65 and 79) to acknowledge subagent definitions as part of the install. Keep it sentence-level; do not rewrite the paragraphs.
- **Optionally**, add a one-sentence orientation note inside `## Install` explaining the purpose of `subagents/` (role-definition files used by `/bees-execute` and `/bees-fix-issue`) — only if it does not duplicate prose elsewhere in README. If it would duplicate, drop the orientation note (acceptable to ship without it).
- Preserve the existing OS-conditional shell-block convention: each snippet stays in its existing fenced block with the same `# POSIX (bash / zsh):` and `# Windows (PowerShell):` header comments. Do NOT re-flow the block structure or change the comments.
- Do NOT touch the `## Tech stack` section, the `## Required: enable Agent Teams` section, the `## Display backend` section, the `### After install` subsection, the `## The skills` table, or any other content outside the four install snippets and the two intro sentences.
- Verify by grep: any `--feature` flag mentions or `Scoped` references added by b.51d must remain unchanged in the diff.
- Apply the project's general doc-writing conventions per `docs/doc-writing-guide.md`: backtick file paths and command names, no emojis.

## Key Files

- `README.md` — edit the `## Install` section only. Four shell-snippet edits (one new line each) plus two intro-sentence edits, plus optionally a one-sentence orientation note.

## Acceptance Criteria

- The `## Install` section's Option A POSIX snippet contains both the existing `skills/*` copy line and a new parallel `subagents/*` copy line targeting `~/.claude/subagents/`.
- The `## Install` section's Option A PowerShell snippet contains both the existing skills line and a new parallel `subagents` line targeting `$HOME\.claude\subagents\`.
- The `## Install` section's Option B POSIX snippet contains both the existing skills line and a new parallel `subagents` line targeting `<repo>/.claude/subagents/`.
- The `## Install` section's Option B PowerShell snippet contains both the existing skills line and a new parallel `subagents` line targeting `<repo>\.claude\subagents\`.
- The Option A and Option B intro sentences acknowledge that subagent definitions are part of the install.
- An optional one-sentence orientation note explaining the purpose of `subagents/` is included if and only if it does not duplicate prose elsewhere in the README. If it would duplicate, it is intentionally omitted.
- No edits exist outside the `## Install` section: the `## Required: enable Agent Teams` section, the `## Display backend` section, the `## Tech stack` section, the `### After install` subsection, the `## The skills` table, and every other section of the README is byte-for-byte unchanged.
- The existing `skills/*` install lines are preserved (NOT deleted) at all four sites.
- Each shell snippet retains its existing OS-conditional fenced-block structure and header comments.
- The new `subagents/*` lines mirror the syntax of the existing `skills/*` lines on the same OS exactly (same flag, same path style, same trailing slash / backslash convention).
- README diff shows zero deletions outside the `## Install` section.
- No file other than `README.md` is modified.
- b.51d additions (`--feature` flag mentions, `Scoped` references) are unchanged in the diff.
