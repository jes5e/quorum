---
id: t3.5tm.27.qw.sj
type: t3
title: Author subagents/test-writer.md
parent: t2.5tm.27.qw
created_at: '2026-05-03T11:46:23.755134'
status: done
schema_version: '0.1'
guid: 5tm27qwsj4h93z4h7h8nd2zuyoa2zeuu
reference_materials: null
---
## Context

The Test Writer subagent writes or updates tests against the artifacts the Engineer produced. Its current inline Instructions block lives in `skills/bees-execute/SKILL.md` Step 3 (Test Writer entry); `bees-fix-issue` has an analogous block. The Test Writer's gating precondition in the old substrate was "Engineer subtask is `status=done`"; in the new substrate that becomes "the Engineer Agent has returned and the orchestrator dispatched this Test Writer Agent" — so the gating-precondition prose is dropped during the lift, but the artifact-level handoff (read what the Engineer landed; verify via narrow test + lint) is preserved.

## What Needs to Change

- Ensure the top-level `subagents/` directory exists at the repo root (`mkdir -p subagents/` is idempotent).
- Create `subagents/test-writer.md` with YAML frontmatter and a markdown body.
- Frontmatter:
  - `name: test-writer`
  - `description`: lead with the verb; state scope as authoring or updating tests for a Subtask of test changes (or, in fix-issue mode, ad-hoc tests covering an Engineer's diff), against the project's test writing/review guides.
  - `model: opus` (Test Writer is always Opus per CLAUDE.md).
  - `tools: [Bash, Edit, Read, Write, Grep]`.
- Body lifts the Test Writer Instructions from `skills/bees-execute/SKILL.md` Step 3, retaining:
  - Use the test writing guide and test review guide referenced in CLAUDE.md `## Documentation Locations`.
  - Execute test subtasks (in execute mode) or write tests against the Engineer's diff (in fix mode). Cover gaps the engineer's pre-planned test subtasks may have missed.
  - Mark each Subtask `status=in_progress` and `status=done` (artifact-based handoff signal).
  - Test-scope discipline: Narrow test + Lint while iterating; defer Full test to the Task's authoritative `.T` subtask. Reference lookup keys by exact contract names.
  - Long-running command guidance: use the Bash `timeout` parameter (max 600000 ms = 10 min) for foreground; use `run_in_background: true` for runs >10 min; do not write polling loops.
  - Shell-command etiquette block (same as Engineer's).
- Strip during the lift (parent Task's Agent-Teams-prose-stripping convention):
  - "Self-trigger" bullet.
  - "Surface blocked state via `blocked_on` metadata" bullet and any `TaskUpdate` references.
  - Any "do not wait for further pings from the team-lead" framing.
  - Any named-team-scoped agent-naming prose.
- Keep the body project-neutral.

## Key Files

- `subagents/test-writer.md` — new file, YAML frontmatter + lifted-and-stripped body.

## Acceptance Criteria

- `subagents/test-writer.md` exists with frontmatter containing the four required keys, `model: opus`, and `tools` exactly `[Bash, Edit, Read, Write, Grep]`.
- The body retains use-test-guides / execute-test-subtasks / mark-status / test-scope-discipline / long-running-command / shell-etiquette content from the source Test Writer Instructions block, with Agent-Teams-specific prose stripped.
- The body contains no `metadata.blocked_on`, `TaskUpdate`, "self-trigger", named-team-scoped agent names, or escalation-ladder references.
- The body contains no this-repo-specific paths, ticket IDs, or workflow specifics.
- Any shell snippet in the body ships in paired POSIX bash + Windows PowerShell labelled blocks.
