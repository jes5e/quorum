---
id: t2.5tm.27.qw
type: t2
title: Author engineer.md, test-writer.md, doc-writer.md doer subagents
down_dependencies:
- t2.5tm.27.na
- t2.5tm.27.ek
parent: t1.5tm.27
children:
- t3.5tm.27.qw.af
- t3.5tm.27.qw.sj
- t3.5tm.27.qw.rq
egg: null
created_at: '2026-05-03T11:40:28.952913'
status: done
schema_version: '0.1'
guid: 5tm27qw2reayy163e5oq6cvy46npeiic
---

## Context

Foundational deliverable for the three "doer" subagent roles. These role definitions are read by Claude Code's harness when the orchestrator (Epics B and C) dispatches `Agent(subagent_type="engineer"|"test-writer"|"doc-writer", ...)`. Role prose is lifted from inline Instructions blocks currently embedded in `skills/bees-execute/SKILL.md` Step 3 (Engineer / Test Writer / Doc Writer Instructions blocks) and `skills/bees-fix-issue/SKILL.md` analogous blocks.

This Task is purely additive — no existing skill SKILL.md is touched. All 11 existing skills continue to work via the old Agent Teams substrate.

## What Needs to Change

Create three subagent definition files under a new `subagents/` directory at the repo root:

- `subagents/engineer.md` — YAML frontmatter (`name`, `description`, `model`, `tools`) + body lifting the Engineer Instructions block.
- `subagents/test-writer.md` — same shape; lifted Test Writer Instructions block.
- `subagents/doc-writer.md` — same shape; lifted Doc Writer Instructions block, with a short body note explaining the model-default + runtime-override interaction.

Tool allowlists per Epic body:
- engineer.md, test-writer.md: `Bash, Edit, Read, Write, Grep`
- doc-writer.md: `Read, Edit, Write, Grep` (no Bash by design)

Models per CLAUDE.md `## Model assignment in execution skills`:
- engineer.md, test-writer.md: `model: opus` (always — not user-configurable)
- doc-writer.md: `model: opus` (shipped default; runtime override to `sonnet` is handled by the orchestrating execution skill in Epic B/C — record the deferral in a body note)

**Agent-Teams-prose-stripping convention** (applies uniformly to T1/T2/T3):
- "Self-trigger" / "top of every turn" prose — strip
- `metadata.blocked_on` / `TaskUpdate metadata.blocked_on` references — strip
- "team-lead might miss your ping" / graduated-escalation-ladder prose — strip
- Team-Lead message-flow choreography prose — strip
- Named-team-scoped agent-naming prose (e.g., `engineer-<bee-id>`) — strip

**Preserve** (load-bearing across the lift):
- Compile-check discipline using `Compile/type-check` lookup-key from CLAUDE.md `## Build Commands`
- Narrow-test + Lint scoping rules
- Shell-command etiquette block (engineer + test-writer; doc-writer's allowlist excludes Bash so the block is irrelevant)
- CLAUDE.md `## Documentation Locations` and `## Build Commands` lookup-key references
- The fix-mode-vs-execute-mode divergence note in doc-writer.md (preserved as-is)

## Why

These three "doer" subagent definitions are the substrate that Epics B, C, D will reference when they rewrite the execution skills off Agent Teams. Authoring them up front (additively, before any SKILL.md is touched) means existing skills keep working while the new substrate is built.

## Cross-Task contracts (surfaced for sibling Tasks and downstream Epics)

- **Frontmatter schema contract**: `name`, `description`, `model`, `tools` in that order. Sibling Tasks T2 (reviewer subagents) and T3 (pm.md) MUST follow the same schema. Tool allowlists are inter-Epic contracts — sibling Epics B/C/D will dispatch these subagent types and their prompts must work with the allowlists set here. Agents cannot acquire tools at runtime.
- **Agent-Teams-prose-stripping convention** defined here applies identically to sibling Tasks T2 and T3.
- **Doc Writer model-default note pattern**: doc-writer.md ships `model: opus` and explains that runtime override to `sonnet` is handled by the orchestrating execution skill in Epic B/C. Sibling subtasks T2.S3 (doc-reviewer.md) and T3.S1 (pm.md) follow the same pattern verbatim.
- **File-location contract**: all 7 subagent files at `subagents/<role>.md` at repo root, NOT under `skills/`. Sibling Tasks T2, T3, T4 depend on this.
- **`Skill` tool absent from T1 allowlists**: engineer/test-writer/doc-writer do not invoke other skills. Only T2 reviewers and T3 PM get `Skill`.

## Success Criteria

- `subagents/` directory exists at repo root.
- `subagents/engineer.md` has frontmatter `name: engineer`, `model: opus`, `tools: [Bash, Edit, Read, Write, Grep]`, and body retains lifted Engineer Instructions with stripped Agent-Teams prose.
- `subagents/test-writer.md` has frontmatter `name: test-writer`, `model: opus`, `tools: [Bash, Edit, Read, Write, Grep]`, and body retains lifted Test Writer Instructions with stripped Agent-Teams prose.
- `subagents/doc-writer.md` has frontmatter `name: doc-writer`, `model: opus` (shipped default), `tools: [Read, Edit, Write, Grep]` (no Bash), and body retains lifted Doc Writer Instructions plus a short model-default + runtime-override note.
- No file references `metadata.blocked_on`, `TaskUpdate`, "self-trigger", named-team-scoped agent names, escalation-ladder rungs, or Team-Lead-message-flow-choreography.
- No file references this-repo-specific paths, ticket IDs, or workflow specifics that would not make sense in a different installed project.
- Any shell snippet in any body follows paired POSIX bash + Windows PowerShell labelled blocks per `docs/doc-writing-guide.md` `## OS-conditional shell blocks`.
- All 11 existing skills continue to work unchanged (no skill SKILL.md is touched).
