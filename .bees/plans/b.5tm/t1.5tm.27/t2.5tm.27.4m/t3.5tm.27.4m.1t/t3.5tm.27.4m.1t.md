---
id: t3.5tm.27.4m.1t
type: t3
title: Author subagents/test-reviewer.md
parent: t2.5tm.27.4m
created_at: '2026-05-03T11:46:26.488505'
status: done
schema_version: '0.1'
guid: 5tm274m1t9315k4pjc3vsi2k5vw9jbxh
reference_materials: null
---
## Context

The Test Reviewer subagent wraps the existing `/bees-test-review` skill in the same thin wrapper pattern as the Code Reviewer. Same fresh-eyes / always-cold rule per the PRD. Same Opus-pinned model rule per CLAUDE.md. The current inline Test Reviewer Instructions blocks in `skills/bees-execute/SKILL.md` Step 5 and `skills/bees-fix-issue/SKILL.md` Step 4 are again thin (single "Invoke the /bees-test-review skill" bullet).

## What Needs to Change

- Ensure the top-level `subagents/` directory exists at the repo root (idempotent).
- Create `subagents/test-reviewer.md` with YAML frontmatter and a markdown body.
- Frontmatter:
  - `name: test-reviewer`
  - `description`: lead with the verb; state scope precisely as performing a fresh-eyes review of the test work just produced by the Test Writer, via the project's `/bees-test-review` skill, returning structured findings to the orchestrator.
  - `model: opus` (always — not user-configurable per CLAUDE.md).
  - `tools: [Bash, Read, Grep, Skill]`.
- Body: thin wrapper, concise. Same shape as Code Reviewer:
  - State the role's purpose (review the output of the Test Writer; provide feedback where the work was not up to standards).
  - Direct the subagent to invoke the `/bees-test-review` skill via the `Skill` tool against the scope indicated in the orchestrator's prompt.
  - Return findings to the orchestrator in a structured form (numbered list with severity tags) consistent with the wrapped skill's existing output contract.
  - State explicitly that the reviewer always runs cold.
- Strip during the lift (parent Task's Agent-Teams-prose-stripping convention): self-trigger, `blocked_on` / TaskUpdate, ping / escalation, named-team-scoped agent names, warm-Agent / SendMessage prose.
- Keep the body project-neutral; the only project-specific reference allowed is the wrapped skill name `/bees-test-review`.
- Any shell snippet must follow paired POSIX + PowerShell labelled blocks.

## Key Files

- `subagents/test-reviewer.md` — new file, YAML frontmatter + thin wrapper body.

## Acceptance Criteria

- `subagents/test-reviewer.md` exists with frontmatter containing the four required keys, `model: opus`, and `tools` exactly `[Bash, Read, Grep, Skill]`.
- The body invokes the `/bees-test-review` skill via the `Skill` tool against the scope indicated in the orchestrator's prompt.
- The body explicitly states that the reviewer runs cold (fresh eyes).
- The body returns findings in a structured form consistent with the wrapped skill's existing output contract.
- The body contains no `metadata.blocked_on`, `TaskUpdate`, "self-trigger", named-team-scoped agent names, escalation-ladder, warm-Agent, or `SendMessage` references.
- The body contains no this-repo-specific paths, ticket IDs, or workflow specifics beyond the wrapped skill's name `/bees-test-review`.
- The body is concise.
- No modifications to `skills/bees-test-review/SKILL.md` (PRD scope-fence).
