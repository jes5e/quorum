---
id: t2.5tm.kn.vk
type: t2
title: Stale-reference cleanup in CLAUDE.md, CONTRIBUTING.md, and docs/doc-writing-guide.md
down_dependencies:
- t2.5tm.kn.6j
parent: t1.5tm.kn
children:
- t3.5tm.kn.vk.eh
- t3.5tm.kn.vk.2g
- t3.5tm.kn.vk.5q
egg: null
created_at: '2026-05-05T11:50:31.337510'
status: done
schema_version: '0.1'
guid: 5tmknvkm5z96rgx4xcwhfmu4bhk966by
---

## Context

Epic `t1.5tm.kn` removes the Agent Teams setup ceremony from `bees-setup/SKILL.md` and deletes the two now-unused helper scripts. The pre-deletion grep audit must return zero hits across `skills/`, `docs/`, `README.md`, `CONTRIBUTING.md`, and this repo's `CLAUDE.md` before Task 3 deletes the scripts.

This Task cleans up the contributor-facing docs that B/C/D didn't touch and that are **not** in Epic `t1.5tm.fy`'s scope:

- This repo's `CLAUDE.md` — has stale bullets in `## Repo layout (only what isn't obvious)` and `## Bash etiquette in this repo`'s "bundled-helper precedent" line, plus an entire `## Agent Teams` section that is wrong post-orchestration-rewrite. Also has historical context in `## Contract keys that downstream skills depend on` (the "Bundled helper scripts are NOT contract keys" paragraph) referencing `force_clean_team.py` as one of two example scripts.
- `CONTRIBUTING.md` — has a helper-script example using `force_clean_team.py` and a "rejected: path-traversal sanitization in `force_clean_team.py`" rationale entry that becomes moot once the script is gone.
- `docs/doc-writing-guide.md` — has a helper-script example using `force_clean_team.py`, a glossary entry for "Agent Teams" (which now describes a removed dependency), and precondition guidance for `bees-execute` / `bees-fix-issue` (lines ~265-266) describing the old `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` check rather than the new seven-subagent registry check.

Epic `t1.5tm.fy`'s scope is `docs/sdd.md` non-feature sections, `README.md`, and the `b.gar` Plan Bee body refresh — it does **not** cover the three contributor-facing docs in this Task. The per-feature PRD subsection (`### Feature: Ephemeral-Agent Orchestration` in `docs/prd.md`) and the per-feature SDD subsection stay intact in either Epic.

## What Needs to Change

In **`/Users/jesseg/code/bees-workflow/CLAUDE.md`** (this repo's CLAUDE.md, NOT a target repo's):

1. Update the `## Repo layout (only what isn't obvious)` bullet that lists "Five exist today" helper scripts (currently around line 14) — drop `force_clean_team.py` and `check_agent_teams.py`, and reword the count. Three remain: `file_list_resolver.py`, `detect_fast_path.py`, and `scoped_marker_resolver.py`.
2. Update the `## Bash etiquette in this repo` "bundled-helper precedent" bullet (currently around line 47) — drop `force_clean_team.py` and `check_agent_teams.py` from the precedent list. Three remain.
3. Update the "Bundled helper scripts are NOT contract keys" paragraph (currently around line 109) — replace `bees-execute/scripts/force_clean_team.py` with a script that still exists (e.g., `bees-breakdown-epic/scripts/scoped_marker_resolver.py`) so the historical example references a real path. The b.963 historical record stays.
4. **Delete the entire `## Agent Teams` section** (currently around lines 130-132). Replace with no-op (no replacement section needed); the orchestration story is now distributed across the relevant SKILL.md files and the per-feature SDD subsection.

In **`/Users/jesseg/code/bees-workflow/CONTRIBUTING.md`**:

5. Update the "Helper scripts" example bullet (currently around line 43) — replace the `bees-execute/scripts/force_clean_team.py` example with a script that still exists post-deletion (e.g., `bees-breakdown-epic/scripts/scoped_marker_resolver.py`).
6. Remove or recast the "Path-traversal sanitization in `force_clean_team.py`" rejected-design entry (currently around line 78). The entry was about a script that no longer exists; either delete it (preferred — historical rejected-design notes for removed code add no value) or rewrite it generically about "path-traversal sanitization in helper scripts" if the rationale still applies to surviving helpers.

In **`/Users/jesseg/code/bees-workflow/docs/doc-writing-guide.md`**:

7. Update the "Bundled helper scripts (e.g., `force_clean_team.py`)" example (currently around line 62) — replace with a still-extant script (e.g., `scoped_marker_resolver.py`).
8. Update the "Agent Teams" glossary entry (currently around line 258) — either delete it entirely (the term no longer describes anything in the workflow) or rewrite it as a historical reference. Recommended: delete; the workflow no longer uses Agent Teams, so a glossary entry for it is misleading.
9. Update the precondition-check guidance for `bees-execute` and `bees-fix-issue` (currently around lines 265-266) — replace the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` env-var check guidance with the seven-subagent registry check guidance (which is what the rewritten skills actually do post-Epic-B/C). Both bullets need this rewrite.
10. Sweep the rest of `docs/doc-writing-guide.md` for any other Agent Teams or removed-helper references — the Scoped-marker contract section (cited explicitly by Epic `t1.5tm.fy`) stays intact, but the file has other prose that may need updating.

## Key Files

- `/Users/jesseg/code/bees-workflow/CLAUDE.md` — repo-level project instructions
- `/Users/jesseg/code/bees-workflow/CONTRIBUTING.md` — contributor guide (engineering best practices per `## Documentation Locations`)
- `/Users/jesseg/code/bees-workflow/docs/doc-writing-guide.md` — doc-writing guide (per `## Documentation Locations`)

## Acceptance Criteria

- `grep -rn "force_clean_team\|check_agent_teams" CLAUDE.md CONTRIBUTING.md docs/doc-writing-guide.md` returns zero hits.
- `grep -rn "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" CLAUDE.md CONTRIBUTING.md docs/doc-writing-guide.md` returns zero hits — except for any prose explicitly cast as historical ("formerly used Agent Teams"). Per-feature historical records belong in `docs/prd.md` / `docs/sdd.md` per-feature subsections (Epic `t1.5tm.fy`'s domain), not in contributor-facing guides.
- `## Documentation Locations` and `## Build Commands` contract sections in this repo's `CLAUDE.md` are preserved unchanged (this is `t1.5tm.kn`'s acceptance criterion 5).
- The historical b.963 record about the removed `## Skill Paths` section in `CLAUDE.md` is preserved with an accurate (still-existing) script as the example.
- All references to `force_clean_team.py` / `check_agent_teams.py` are gone from contributor-facing docs in this repo, leaving the path clear for Task 3's pre-deletion grep audit to return zero hits in these files.

## Cross-Task contracts

- **Gates Task `t2.5tm.kn.6j` (helper-script deletion).** The pre-deletion grep audit must return zero hits across `CLAUDE.md`, `CONTRIBUTING.md`, and `docs/doc-writing-guide.md` before deletion proceeds; this Task is responsible for clearing all such references in those three files.
- **Out of scope.** `README.md`, `docs/sdd.md` non-feature sections, the `b.gar` Plan Bee body refresh, and `docs/prd.md` per-feature subsection — these belong to Epic `t1.5tm.fy` (`README.md`, SDD non-feature, `b.gar`) or stay intact as historical record (`docs/prd.md` and `docs/sdd.md` per-feature subsections). Do not edit them in this Task.
- **Sibling Task `t2.5tm.kn.e9`** (bees-setup/SKILL.md cleanup) operates on a disjoint file set. The two Tasks can land in either order; both gate Task `t2.5tm.kn.6j`.
