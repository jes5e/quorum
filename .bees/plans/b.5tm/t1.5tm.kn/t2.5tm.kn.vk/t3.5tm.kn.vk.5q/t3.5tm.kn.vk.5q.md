---
id: t3.5tm.kn.vk.5q
type: t3
title: 'docs/doc-writing-guide.md: replace deleted-helper example, remove Agent Teams glossary, rewrite bees-fix-issue precondition guidance, sweep stragglers'
parent: t2.5tm.kn.vk
egg: null
created_at: '2026-05-05T12:05:40.931807'
status: done
schema_version: '0.1'
guid: 5tmknvk5qv51452urgn756toq1put1j8
---

## Context

`/Users/jesseg/code/bees-workflow/docs/doc-writing-guide.md` has the most updates of the three contributor docs in this Task: a deleted-helper example, a glossary entry that no longer describes anything in the workflow, precondition guidance that describes the old `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` env-var check rather than the new seven-subagent-registry check, and a stray reference inside the Scoped-marker contract section. All updates must land before sibling Task `t2.5tm.kn.6j` deletes the helper scripts.

The Scoped-marker contract section (lines ~165-201) is cited as in-scope of Epic `t1.5tm.fy` for substantive content changes — this Subtask only touches it to remove the now-stale parenthetical comparison to `check_agent_teams.py` (a script that will not exist post-deletion), not to rewrite the contract itself.

Line numbers below were verified against the file at the time of authoring.

## What Needs to Change

1. **Line 62** — `## The lookup-key pattern (no hardcoded language commands)` paragraph that begins "Bundled helper scripts (e.g., `force_clean_team.py`)". Replace `force_clean_team.py` with a script that still exists post-deletion (recommended: `scoped_marker_resolver.py`). The surrounding prose about "deliberately *not* on this list" and runtime-resolution from skill base directory stays intact.

2. **Line 197** — Inside the `## The Scoped-marker contract` section, the parenthetical "(same shape as `bees-fix-issue`'s existing sibling-resolution of `check_agent_teams.py`)". The comparison anchor is being deleted by `t2.5tm.kn.6j`, so the parenthetical becomes stale. Recommended: delete the parenthetical entirely (the surrounding sentence still stands on its own — it already states the resolved path explicitly). Do NOT rewrite or restructure the rest of the Scoped-marker contract section; that section's substantive content is in scope of Epic `t1.5tm.fy`, not this Task.

3. **Line 258** — `## Project terminology` glossary entry for "Agent Teams". The workflow no longer uses Agent Teams (per the Epic-B/C ephemeral-Agent rewrite). Recommended action: delete the bullet entirely — a glossary entry for a term that no longer describes anything in the workflow is misleading. Alternative: rewrite as a historical note ("formerly used; replaced by ephemeral per-Subtask Agent dispatch"), but the deletion is cleaner since no surviving prose references the term.

4. **Lines 265-266** — `## When you're updating an existing skill` step 3 (`Substrate is required, not optional`) has two sub-bullets, one for `bees-fix-issue` (line 265) and one for `bees-execute` (line 266). The current `bees-execute` sub-bullet (line 266) already describes the new seven-subagent-registry precondition correctly — leave that one in place. The `bees-fix-issue` sub-bullet (line 265) still describes the old `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` Agent-Teams check — rewrite it to mirror the `bees-execute` sub-bullet's structure: per-Subtask ephemeral Agent dispatch as the substrate, with a subagent-registry precondition check that hard-fails when any of the seven required custom subagent types (`engineer`, `test-writer`, `doc-writer`, `pm`, `code-reviewer`, `test-reviewer`, `doc-reviewer`) is missing from the running Claude Code session. Drop the `if-Teams-off` conditional-branch warning; replace with a parallel "don't introduce conditional fallback branches" warning.

   Note: re-verify the current line-266 content against the file before editing — the rest of the sub-bullet structure (intro line for step 3, lead-in sentence about the precondition gate) is correct and should be preserved.

5. **Sweep pass** — read the rest of `docs/doc-writing-guide.md` end-to-end after edits 1-4 are in place and remove any other stale references to Agent Teams, `teammateMode`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `force_clean_team`, or `check_agent_teams`. The grep audit in the acceptance criteria below is the canonical check.

## Key Files

- `/Users/jesseg/code/bees-workflow/docs/doc-writing-guide.md` — apply edits 1-5 above.

## Acceptance Criteria

- `grep -rn "force_clean_team\|check_agent_teams" /Users/jesseg/code/bees-workflow/docs/doc-writing-guide.md` returns zero hits.
- `grep -rn "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" /Users/jesseg/code/bees-workflow/docs/doc-writing-guide.md` returns zero hits — except for any prose explicitly cast as historical ("It is no longer a ...", "formerly used ...", etc.). The current line-266 prose `It is no longer a \`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1\` env-var check` is the canonical example of an allowed historical reference and should be preserved unchanged. This exception mirrors the parent Task's AC wording.
- The `## The lookup-key pattern` paragraph still explains why bundled-helper paths are not contract keys, but uses `scoped_marker_resolver.py` (or another extant script) as the example.
- The `## Project terminology` section no longer contains a glossary entry for "Agent Teams". The other terminology bullets are preserved unchanged.
- The `## When you're updating an existing skill` step 3 sub-bullet for `bees-fix-issue` describes per-Subtask ephemeral Agent dispatch and a seven-subagent-registry precondition check (matching the existing `bees-execute` sub-bullet's structure), and no longer mentions `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`.
- The `## When you're updating an existing skill` step 3 sub-bullet for `bees-execute` is preserved unchanged (it already describes the seven-subagent-registry check correctly).
- The `## The Scoped-marker contract` section's substantive content (marker grammar, subsection extraction rule, hard-fail rules, bundled-parser path, `bees-fix-issue` consumer note) is preserved unchanged — only the stale `check_agent_teams.py` parenthetical comparison on line 197 is removed.
- All other top-level sections in `docs/doc-writing-guide.md` are preserved unchanged.
