---
id: t3.5tm.kn.vk.eh
type: t3
title: 'CLAUDE.md (this repo): drop deleted-helper references and remove ## Agent
  Teams section'
parent: t2.5tm.kn.vk
created_at: '2026-05-05T12:05:38.339576'
status: done
schema_version: '0.1'
guid: 5tmknvkeh27no9ofdzibef4hsamh4i5w
reference_materials: null
---
## Context

This repo's `/Users/jesseg/code/quorum/CLAUDE.md` (NOT a target-repo CLAUDE.md) has stale references to two helper scripts being deleted in sibling Task `t2.5tm.kn.6j` (`quo-execute/scripts/force_clean_team.py` and `quo-execute/scripts/check_agent_teams.py`), plus a `## Agent Teams` section that is wrong post-orchestration-rewrite. This Subtask updates `CLAUDE.md` so it lands before `t2.5tm.kn.6j` deletes the scripts and the pre-deletion grep audit returns zero hits.

The `## Documentation Locations` and `## Build Commands` contract sections must be preserved unchanged — they are read by every other skill via exact-key match.

Line numbers below were verified against the file at the time of authoring; if drift has occurred, locate the matching prose and update it in place.

## What Needs to Change

1. **Line 14** — `## Repo layout (only what isn't obvious)` bullet that says "Five exist today". Drop the two helper scripts being deleted (`quo-execute/scripts/force_clean_team.py` and `quo-execute/scripts/check_agent_teams.py`) and reword the count to "Three exist today". The remaining three are:
   - `quo-setup/scripts/file_list_resolver.py` (the egg resolver)
   - `quo-setup/scripts/detect_fast_path.py` (new-machine fast-path detection)
   - `quo-breakdown-epic/scripts/scoped_marker_resolver.py` (Scoped-marker parser/scoper, sibling-resolved by `quo-execute` and `quo-fix-issue`)

2. **Line 47** — `## Bash etiquette in this repo` "Bundled-helper precedent" line. Drop `force_clean_team.py` and `check_agent_teams.py`. The remaining list is `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`.

3. **Line 109** — "Bundled helper scripts are NOT contract keys" paragraph in `## Contract keys that downstream skills depend on`. The historical b.963 record stays, but replace the example path `quo-execute/scripts/force_clean_team.py` with a script that still exists post-deletion (recommended: `quo-breakdown-epic/scripts/scoped_marker_resolver.py`). The example pair should still illustrate that the removed `## Skill Paths` section once held absolute paths to bundled scripts.

4. **Lines 130-132** — Delete the entire `## Agent Teams` section (heading line plus blank line plus the body paragraph that describes `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS = "1"`, `teammateMode`, and Agent-Teams agent naming). No replacement section is needed — the orchestration story now lives in the relevant `SKILL.md` files and the per-feature SDD subsection. Make sure the surrounding section spacing (blank line between `## Egg resolver` and the next surviving section, `## Model assignment in execution skills`) is correct after deletion.

## Key Files

- `/Users/jesseg/code/quorum/CLAUDE.md` — apply edits 1-4 above.

## Acceptance Criteria

- `grep -rn "force_clean_team\|check_agent_teams" /Users/jesseg/code/quorum/CLAUDE.md` returns zero hits.
- `grep -rn "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" /Users/jesseg/code/quorum/CLAUDE.md` returns zero hits.
- The `## Repo layout` bullet now says "Three exist today" and lists exactly `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py` (with their owning skill prefixes and one-line descriptions).
- The `## Bash etiquette` "Bundled-helper precedent" list contains exactly three filenames: `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`.
- The b.963 paragraph still exists and still describes the historical removal of `## Skill Paths`, but its example pair references `quo-breakdown-epic/scripts/scoped_marker_resolver.py` (or another script that still exists in the repo) instead of `quo-execute/scripts/force_clean_team.py`.
- The `## Agent Teams` section is gone — no heading and no body paragraph remain.
- `## Documentation Locations` and `## Build Commands` sections are byte-for-byte unchanged (exact section bodies and key spellings preserved — they are a string contract).
- `## Model assignment in execution skills` is preserved unchanged (assignments are load-bearing for output quality).
- `python -m pyflakes skills/*/scripts/*.py` (the repo's `Lint` command from `## Build Commands`) still passes — this Subtask does not touch Python.
