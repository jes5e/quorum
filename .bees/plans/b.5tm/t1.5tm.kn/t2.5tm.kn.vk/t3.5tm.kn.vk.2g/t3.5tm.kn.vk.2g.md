---
id: t3.5tm.kn.vk.2g
type: t3
title: 'CONTRIBUTING.md: replace force_clean_team.py example and drop rejected-design
  entry'
parent: t2.5tm.kn.vk
created_at: '2026-05-05T12:05:39.526475'
status: done
schema_version: '0.1'
guid: 5tmknvk2g76eb3bvzmt341scx5dj8r3v
reference_materials: null
---
## Context

`/Users/jesseg/code/bees-workflow/CONTRIBUTING.md` references two helper scripts being deleted in sibling Task `t2.5tm.kn.6j`. One is a concrete example illustrating the "Helper scripts" convention and one is a "Considered and rejected" rationale entry. Both must update before `t2.5tm.kn.6j` deletes the scripts so the pre-deletion grep audit returns zero hits.

Line numbers below were verified against the file at the time of authoring.

## What Needs to Change

1. **Line 43** — `## Skill conventions` `Helper scripts:` bullet uses `bees-execute/scripts/force_clean_team.py` as the in-prose example for "Ship inside the owning skill's `scripts/` directory". Replace with a script that still exists post-deletion. Recommended: `bees-breakdown-epic/scripts/scoped_marker_resolver.py`. Keep the surrounding sentence structure intact (the example just plugs into the existing prose).

2. **Line 78** — `## Considered and rejected` entry titled "Path-traversal sanitization in `force_clean_team.py`". The script no longer exists post-deletion, so a rejected-design note about hardening it adds no value. Recommended action: delete the entire bullet (preferred — historical rejected-design notes for removed code add no value). Alternative: rewrite generically as "Path-traversal sanitization in helper scripts" if the rationale still applies to a surviving helper, but this is unlikely to be true since the original rationale was specific to `~/.claude/teams/<name>` and `~/.claude/tasks/<name>` paths that only `force_clean_team.py` touched.

## Key Files

- `/Users/jesseg/code/bees-workflow/CONTRIBUTING.md` — apply edits 1-2 above.

## Acceptance Criteria

- `grep -rn "force_clean_team\|check_agent_teams" /Users/jesseg/code/bees-workflow/CONTRIBUTING.md` returns zero hits.
- `grep -rn "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" /Users/jesseg/code/bees-workflow/CONTRIBUTING.md` returns zero hits.
- The `Helper scripts:` bullet under `## Skill conventions` still illustrates the "Ship inside the owning skill's `scripts/` directory" rule, but with an example path pointing at a script that exists in the repo (recommended: `bees-breakdown-epic/scripts/scoped_marker_resolver.py`).
- The `## Considered and rejected` section no longer contains a bullet about path-traversal sanitization in `force_clean_team.py`. The other rejected-design entries are preserved unchanged.
- All other top-level sections in `CONTRIBUTING.md` are preserved unchanged.
