---
id: t3.5tm.kn.e9.np
type: t3
title: 'bees-setup/SKILL.md final audit: zero-hit grep + end-to-end walk + design-rule sweep'
up_dependencies:
- t3.5tm.kn.e9.w2
- t3.5tm.kn.e9.66
parent: t2.5tm.kn.e9
egg: null
created_at: '2026-05-05T11:59:10.221465'
status: ready
schema_version: '0.1'
guid: 5tmkne9npc43pezvwx3vzd81i1xoqre1
---

## Context

The two prior Subtasks under Task `t2.5tm.kn.e9` edit independent regions of `skills/bees-setup/SKILL.md`:

- The Agent Teams / teammateMode / Helper deletion Subtask removes the fast-path condensed prompt, the slow-path `### Per-machine Claude Code settings` section (with both `####` children), and the now-orphaned shared `#### Helper: write a single key to `~/.claude/settings.json`` subsection, plus repairs cross-references at lines 129 and 250.
- The bundled-script reference cleanup Subtask drops `force_clean_team.py` / `check_agent_teams.py` from the inventory paragraph at line ~454, the sibling-resolution example at line ~462, and the *Egg Resolver* parenthetical at line ~560.

This final-audit Subtask exists to validate that the union of those two changes satisfies the parent Task's acceptance criteria, that the surviving prose still walks `/bees-setup` end-to-end, and that the parent Task's gating contract on the sibling script-deletion Task `t2.5tm.kn.6j` is honored.

This Subtask makes no source-code edits unless the audit surfaces a regression. Its primary output is the audit result; if a regression is found, fix it inline and re-run the audit until it is clean.

## What Needs to Change

In `skills/bees-setup/SKILL.md`:

1. **Run the parent Task's full grep audit.** Execute this exact grep against the file:

   ```
   grep -n "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\|iTerm2\|force_clean_team\|check_agent_teams" skills/bees-setup/SKILL.md
   ```

   Expected result: zero hits. If any hit remains, identify whether it belongs in the first sibling Subtask's scope (Agent Teams / teammateMode / `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` / iTerm2) or the second sibling Subtask's scope (`force_clean_team` / `check_agent_teams`), fix it inline in this Subtask, and re-run the grep until it is clean.

2. **Read the post-edit file end-to-end** and confirm the skill prose still walks `/bees-setup` coherently from *Overview* through *Next Steps*. Pay particular attention to:
   - The fast-path detection branch (around lines 88-250) — it should now flow detect → diagnose → re-register hives → confirm-and-exit, with no per-machine Claude Code settings step in the middle.
   - The slow-path sequence — after the deletions, *Resolve bundled helper script paths* should be the first section after the fast-path branch. Confirm the section ordering is *Resolve bundled helper script paths* → *Egg Resolver* → *Hive Configuration* → *Documentation Locations* → *Bootstrap PRD/SDD from existing codebase* → *Build Commands* → *Next Steps*.
   - All cross-references in the surviving prose resolve to sections that still exist. Specifically: any remaining `*<section name>*` italicized cross-reference must point at a heading that is present in the file. The two cross-references repaired in the first sibling Subtask (lines ~129 and ~250) are the high-risk ones; sweep the rest of the file for any other italicized cross-reference and verify it still resolves.

3. **Re-confirm the three non-negotiable design rules** for the post-edit file:
   - **Rule 1 (language-agnostic).** No language-specific commands, file extensions, or manifest filenames are referenced in skill prose outside the *Build Commands* `Detect the stack` table (which is intentionally stack-aware and unchanged by this Task).
   - **Rule 2 (OS-paired snippets).** Every shell snippet remaining in the file ships as labeled OS-conditional blocks (POSIX bash + Windows PowerShell at minimum). Confirm no orphaned bash-only snippets were left behind by the deletions.
   - **Rule 3 (project-neutral).** No references to this repo's specific paths, ticket IDs, or internal workflow specifics were introduced.

4. **Re-confirm the scratch-file convention.** No new transient `--body-file` (or similar) writes were introduced by the edits, so this rule is trivially preserved — but verify by inspection that no helper or snippet writes outside `<tempdir>/.bees-workflow/` was accidentally introduced.

5. **Re-confirm the Bash etiquette rule** from this repo's `CLAUDE.md` `## Bash etiquette in this repo`. Every shell snippet in the post-edit file must be a single literal command — no compound chains (`&&` / `||` / `;`), no pipes between commands, no diagnostic tails, no `$VAR` / `$?` / `$(...)` expansion, no inline heredocs.

## Key Files

- `skills/bees-setup/SKILL.md` — read-only audit, with inline fixes only if a regression is found.

## Acceptance Criteria

- `grep -n "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\|iTerm2\|force_clean_team\|check_agent_teams" skills/bees-setup/SKILL.md` returns zero hits. (This is the parent Task's load-bearing acceptance criterion and the gate for sibling Task `t2.5tm.kn.6j`.)
- The skill prose walks `/bees-setup` end-to-end through hives colonization, CLAUDE.md `## Documentation Locations` and `## Build Commands` writes, and the optional PRD/SDD bootstrap. No step depends on Agent Teams or `teammateMode`.
- The fast-path detection step (`detect_fast_path.py`) prose is unchanged — its rationale and Python helper invocation prose stay intact.
- Every italicized cross-reference (`*<section name>*`) in the post-edit file resolves to a heading that is still present in the file.
- The three non-negotiable design rules (language-agnostic, OS-paired POSIX + PowerShell snippets, project-neutral) and the scratch-file convention are preserved across the post-edit file.
- Every shell snippet remaining in the file is a single literal command per `CLAUDE.md` `## Bash etiquette in this repo` — no compound chains, no pipes between commands, no diagnostic tails, no `$VAR` / `$?` / `$(...)` expansion, no inline heredocs.
- If `python -m pyflakes skills/*/scripts/*.py` is run as the repo's *Lint* contract, it returns no new errors introduced by this Task. (This Task edits prose only, so Lint is expected to be a no-op.)
