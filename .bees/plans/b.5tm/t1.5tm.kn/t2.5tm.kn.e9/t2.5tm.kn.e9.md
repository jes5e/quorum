---
id: t2.5tm.kn.e9
type: t2
title: 'bees-setup/SKILL.md cleanup: remove Agent Teams setup ceremony and stale helper-script references'
down_dependencies:
- t2.5tm.kn.6j
parent: t1.5tm.kn
children:
- t3.5tm.kn.e9.w2
- t3.5tm.kn.e9.66
- t3.5tm.kn.e9.np
egg: null
created_at: '2026-05-05T11:50:29.401057'
status: done
schema_version: '0.1'
guid: 5tmkne95tfqtght5iv4cmhpc2ieo521r
---

## Context

Epic `t1.5tm.kn` removes the Agent Teams setup ceremony from `bees-setup/SKILL.md` and deletes the two now-unused helper scripts. This Task is the first half of the SKILL-prose cleanup: edit `skills/bees-setup/SKILL.md` so a fresh `/bees-setup` walk-through no longer touches Agent Teams or `teammateMode`, and so the bundled-scripts paragraph reflects the post-deletion state.

The companion Tasks `t2.5tm.kn.vk` (cleanup of contributor-facing docs in this repo) and `t2.5tm.kn.6j` (delete the helper scripts) handle the rest of the Epic. **Task `t2.5tm.kn.6j` (script deletion) is gated on this Task completing** — its pre-deletion grep audit must return zero hits across `skills/`, so the references this Task removes from `bees-setup/SKILL.md` need to be gone first.

Epic B (`t1.5tm.8s`), C (`t1.5tm.fk`), and D (`t1.5tm.o1`) shipped their SKILL.md rewrites before this Epic; their precondition checks are already off `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` and onto the seven-subagent registry check. So `bees-setup/SKILL.md` is the only execution-time SKILL.md that still configures Agent Teams. Customer-facing README sections and `docs/sdd.md` non-feature sections are explicitly Epic `t1.5tm.fy`'s scope — do **not** touch them here.

## What Needs to Change

In `skills/bees-setup/SKILL.md`:

1. **Remove the entire `#### Claude Code Agent Teams (required)` subsection** (currently around lines 256-285). This includes the section header, the "required for both skills" prose, the detect-current-state logic, the AskUserQuestion block, and the three branch handlers. Nothing about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` should remain.

2. **Remove the entire `#### teammateMode display backend` subsection** (currently around lines 287-352). This includes the section header, the iTerm2-prompt rationale, the per-terminal detection table, the AskUserQuestion choice block (including the `"in-process"` / `"tmux"` / `"auto"` options), the iTerm2 install-path prose, and the write-the-key step. Nothing about `teammateMode` should remain.

3. **Re-author the umbrella subsection that introduces these two settings** (currently around lines 254-255 — the "two settings ... live in the user's per-machine Claude Code settings file" intro). Two acceptable shapes:
   - Delete the umbrella entirely if no other `~/.claude/settings.json` configuration step belongs there post-deletion (likely; `bees-setup` no longer writes per-machine Claude Code settings).
   - If the surrounding step structure prefers to keep an empty container, replace its body with a one-line note stating that `bees-setup` no longer writes per-machine Claude Code settings; `bees-execute` and `bees-fix-issue` orchestrate via subagents loaded from `~/.claude/agents/` and `<repo>/.claude/agents/`, which is a separate install step covered by the README's `## Install` section.

4. **Update the fast-path detection prose** so the combined-prompt logic for these two settings (currently around lines 222-232 — "If either or both are missing: present a single combined `AskUserQuestion`") is removed. The fast-path is now scoped to hive existence + CLAUDE.md sections (and any other repo-state checks already there); per-machine Claude Code settings are not part of `bees-setup` anymore.

5. **Update the shared Python one-liner example block** (currently around line 444 — "Replace `<key>` and `<value>` ... e.g., `\"CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\"` `\"1\"` ... `\"teammateMode\"` `\"in-process\"`"). If the helper one-liner is no longer used by any remaining `bees-setup` step, delete the entire helper subsection. If it is used by some remaining step (e.g., a future settings-write that's not Agent Teams-related), keep the helper but update the example callsite to a non-Agent-Teams example.

6. **Drop bundled-script references in two paragraphs** (currently around lines 454 and 462). Specifically:
   - Line 454's "five exist today" sentence becomes a "three exist today" sentence after this Epic ships, listing `file_list_resolver.py`, `detect_fast_path.py`, and `scoped_marker_resolver.py`.
   - Line 462's parenthetical that mentions `bees-execute` computing `force_clean_team.py` / `check_agent_teams.py` paths must be rewritten — the surviving sibling-resolution example is `bees-execute` and `bees-fix-issue` resolving `scoped_marker_resolver.py` from `bees-breakdown-epic`'s `scripts/` directory.

7. **Drop the parenthetical at line 560** that says "(This is the same convention `bees-execute` and `bees-fix-issue` use for their `force_clean_team.py` references.)". Replace with a still-valid example or drop the parenthetical entirely.

8. **Confirm there are no other Agent Teams / teammateMode / iTerm2 references** in `bees-setup/SKILL.md` after the edits. The grep `grep -n "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\|iTerm2\|force_clean_team\|check_agent_teams"` should return zero hits inside this file when this Task completes.

## Key Files

- `skills/bees-setup/SKILL.md` — sole source of changes for this Task

## Acceptance Criteria

- `grep -n "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\|iTerm2\|force_clean_team\|check_agent_teams" skills/bees-setup/SKILL.md` returns zero hits.
- The skill prose still walks `/bees-setup` end-to-end through hives colonization, CLAUDE.md `## Documentation Locations` and `## Build Commands` writes, and the optional PRD/SDD bootstrap. None of those steps depend on Agent Teams or `teammateMode`.
- The fast-path detection step (`detect_fast_path.py`) is unchanged — its rationale and Python helper invocation prose stay intact.
- The skill prose follows the three non-negotiable design rules from this repo's CLAUDE.md (language-agnostic, OS-paired POSIX + PowerShell snippets, project-neutral) and the scratch-file convention. The deletions reduce surface but must not introduce a regression on any of these rules.
- Bash etiquette is preserved across edits — every shell snippet remains a single literal command (no compound chains, no pipes, no diagnostic tails) per CLAUDE.md `## Bash etiquette in this repo`.

## Cross-Task contracts

- **Gates Task `t2.5tm.kn.6j` (helper-script deletion).** The pre-deletion grep audit (`grep -r "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md`) must return zero hits across `skills/` before deletion proceeds; this Task is responsible for clearing all `bees-setup/SKILL.md` hits.
- **Out of scope.** Customer-facing README sections (`## Required: enable Agent Teams`, `## Display backend`, the workflow-diagram replacement sentence, the skill-table description), `docs/sdd.md` non-feature sections (Tech stack, Key components, Team orchestration, External dependencies), and the `b.gar` Plan Bee body refresh — these belong to Epic `t1.5tm.fy`. Do not edit them in this Task.
- **Per-feature PRD/SDD subsections (`### Feature: Ephemeral-Agent Orchestration`)** stay intact — they are the historical record of what this Bee delivers; do not edit those subsections in this Task.
