---
id: t3.5tm.kn.e9.w2
type: t3
title: 'bees-setup/SKILL.md: remove Agent Teams + teammateMode setup and shared Helper
  subsection; repair cross-refs at lines 129/250'
down_dependencies:
- t3.5tm.kn.e9.np
parent: t2.5tm.kn.e9
created_at: '2026-05-05T11:59:06.063490'
status: done
schema_version: '0.1'
guid: 5tmkne9w2jee4k3vvehiwsnkcixi5bch
reference_materials: null
---
## Context

Epic `t1.5tm.kn` removes the Agent Teams setup ceremony from `/bees-setup`. Epics B / C / D have already migrated `bees-execute` / `bees-fix-issue` / `bees-breakdown-epic` off the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var and onto a seven-subagent registry check, so `bees-setup/SKILL.md` is the only execution-time SKILL.md still configuring per-machine Claude Code settings related to Agent Teams. After this Subtask, a fresh `/bees-setup` walk-through must no longer touch Agent Teams, `teammateMode`, the iTerm2 prompt, or the user's `~/.claude/settings.json` file.

Three edit zones are coupled in this Subtask because they all cross-reference each other:

1. The fast-path subsection `#### Configure per-machine Claude Code settings (condensed prompt)` (currently around lines 221-236).
2. The slow-path section `### Per-machine Claude Code settings` and its two `####` children (`#### Claude Code Agent Teams (required)` and `#### teammateMode display backend`) plus all `#####` sub-steps under teammateMode (currently around lines 252-373).
3. The shared `#### Helper: write a single key to ` `` `~/.claude/settings.json` `` `` subsection (currently around lines 375-444).

After the slow-path section is deleted, two cross-references elsewhere in the file become dangling and must be repaired in the same Subtask:

- Line 129: "If `fast_path_eligible` is **false**, fall through to the existing slow path starting at *Per-machine Claude Code settings*; if those are already configured, the slow path continues to *Resolve bundled helper script paths* — there is no behavior change for first-time setup or for already-fully-configured machines."
- Line 250: "If option 2: continue with the existing slow path starting at *Resolve bundled helper script paths* (*Per-machine Claude Code settings* was already configured by the condensed prompt above; we skip ahead to the next slow-path section)."

The shared Helper subsection (lines 375-444) only existed to write Agent Teams / teammateMode keys into `~/.claude/settings.json`; with both callers gone there is no remaining caller in the skill, so the helper itself is also deleted.

## What Needs to Change

In `skills/bees-setup/SKILL.md`:

1. **Delete the fast-path subsection `#### Configure per-machine Claude Code settings (condensed prompt)` in its entirety** (currently around lines 221-236). The fast path no longer prompts for any per-machine Claude Code settings; the only fast-path post-conditions are hive re-registration plus the existing CLAUDE.md sanity check that `detect_fast_path.py` already performs.

2. **Delete the slow-path section `### Per-machine Claude Code settings` and both of its `####` children in their entirety** (currently around lines 252-373). This includes:
   - The umbrella intro paragraph at lines 252-254 ("The two settings below — `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` and `teammateMode` — live in the user's per-machine Claude Code settings file...").
   - The full `#### Claude Code Agent Teams (required)` subsection (lines 256-285), including the detect / AskUserQuestion / three branch handlers / tmux optional-later-installs blockquote.
   - The full `#### teammateMode display backend` subsection (lines 287-373), including the iTerm2 rationale, the `##### Detect the terminal` table (with the `iTerm.app` row), the `##### Prompt the user` AskUserQuestion block (with the `"in-process"` / `"tmux"` / `"auto"` options), the `##### Apply the choice` step, and the `##### If the user picked "tmux", verify the backend is installed` step (with all `iTerm2` / `it2` / `tmux` install prose).

   The Task body permits an alternative shape — keep an empty `### Per-machine Claude Code settings` container with a one-line note. Pick the **delete entirely** shape: post-Epic, `bees-setup` writes nothing to `~/.claude/settings.json`, so an empty container would just be confusing scaffolding. Subagent-loading from `~/.claude/agents/` and `<repo>/.claude/agents/` is already covered by the README's `## Install` section — no need to repeat it here.

3. **Delete the `#### Helper: write a single key to `~/.claude/settings.json`` subsection in its entirety** (currently around lines 375-444), including both POSIX bash and Windows PowerShell variants of the Python one-liner and the trailing "Replace `<key>` and `<value>` at the call site..." instructional paragraph. With both callers (Agent Teams, teammateMode) deleted, this helper has no remaining caller in `bees-setup/SKILL.md`. The horizontal-rule separator (`---`) on line 446 should also be removed if it now floats with no preceding subsection.

4. **Repair the dangling cross-reference at line 129** in the *Detect* subsection of fast-path detection. Change the sentence so it no longer references *Per-machine Claude Code settings*. Replacement target: the slow path now begins at *Resolve bundled helper script paths*. Suggested rewrite (engineer may refine wording while preserving intent): "If `fast_path_eligible` is **false**, fall through to the existing slow path starting at *Resolve bundled helper script paths* — there is no behavior change for first-time setup or for already-fully-configured machines."

5. **Repair the dangling cross-reference at line 250** in the fast-path *Confirm and exit* subsection's option-2 branch. Change the sentence so it no longer references *Per-machine Claude Code settings* (which no longer exists). Suggested rewrite: "If option 2: continue with the existing slow path starting at *Resolve bundled helper script paths*."

6. **Confirm there are no other Agent Teams / teammateMode / iTerm2 references** introduced by surrounding prose. The grep target for this Subtask is `Agent Teams|teammateMode|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS|iTerm2|iTerm\.app`. Note: `force_clean_team` and `check_agent_teams` are handled in the sibling Subtask; do not address those here.

## Key Files

- `skills/bees-setup/SKILL.md` — delete three subsections (fast-path condensed prompt, slow-path Per-machine Claude Code settings + both `####` children, shared Helper one-liner) and repair two cross-references at lines 129 and 250.

## Acceptance Criteria

- `grep -n "Agent Teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\|iTerm2\|iTerm\.app" skills/bees-setup/SKILL.md` returns zero hits.
- The fast-path branch no longer mentions `~/.claude/settings.json`, `%USERPROFILE%\.claude\settings.json`, or any per-machine Claude Code settings step. After re-registering on-disk hives, it proceeds straight to *Confirm and exit*.
- The string `### Per-machine Claude Code settings` does not appear as a section header anywhere in the file.
- The string `#### Helper: write a single key to ` `` `~/.claude/settings.json` `` `` does not appear as a section header anywhere in the file.
- The cross-reference on line ~129 in *Fast-path detection / Detect* points readers at *Resolve bundled helper script paths* (not the deleted *Per-machine Claude Code settings*).
- The cross-reference on line ~250 in *Fast-path detection / Confirm and exit* points readers at *Resolve bundled helper script paths* (not the deleted *Per-machine Claude Code settings*).
- The fast-path detection step (`detect_fast_path.py`) prose is unchanged — its rationale and Python helper invocation snippets stay intact.
- Adjacent slow-path sections (*Resolve bundled helper script paths*, *Egg Resolver*, *Hive Configuration*, *Documentation Locations*, *Build Commands*, *Bootstrap PRD/SDD from existing codebase*, *Next Steps*) read coherently end-to-end with no orphaned forward/backward references introduced by the deletions.
- The skill prose still walks `/bees-setup` end-to-end through hives colonization, CLAUDE.md `## Documentation Locations` and `## Build Commands` writes, and the optional PRD/SDD bootstrap. None of those steps depend on Agent Teams or `teammateMode`.
- The three non-negotiable design rules (language-agnostic, OS-paired POSIX + PowerShell snippets, project-neutral) and the scratch-file convention are preserved across the edits — no regressions introduced.
- Bash etiquette is preserved across edits — every shell snippet remaining in the file remains a single literal command (no compound chains, no pipes, no diagnostic tails).
