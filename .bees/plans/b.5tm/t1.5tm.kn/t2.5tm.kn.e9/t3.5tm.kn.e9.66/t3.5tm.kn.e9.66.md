---
id: t3.5tm.kn.e9.66
type: t3
title: 'quo-setup/SKILL.md: drop force_clean_team.py / check_agent_teams.py from
  helper-script paragraphs (~454, ~462, ~560)'
down_dependencies:
- t3.5tm.kn.e9.np
parent: t2.5tm.kn.e9
created_at: '2026-05-05T11:59:06.306834'
status: done
schema_version: '0.1'
guid: 5tmkne966q71cb82phq6huf738s8xdhk
reference_materials: null
---
## Context

Epic `t1.5tm.kn` deletes the two helper scripts `quo-execute/scripts/force_clean_team.py` and `quo-execute/scripts/check_agent_teams.py` (sibling Task `t2.5tm.kn.6j`). The script-deletion Task is gated on this Subtask completing — its pre-deletion grep audit (`grep -r "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md`) must return zero hits inside `skills/` before deletion proceeds, so this Subtask is responsible for clearing all remaining references inside `skills/quo-setup/SKILL.md`.

Three references must be cleaned up:

1. The bundled-scripts inventory paragraph at line ~454 in *Resolve bundled helper script paths* — currently lists four scripts (`file_list_resolver.py`, `detect_fast_path.py`, `force_clean_team.py`, `check_agent_teams.py`). Note the paragraph already correctly omits `scoped_marker_resolver.py`; this repo's `CLAUDE.md` count-of-record ("five exist today") covers the broader skill-set inventory across multiple `SKILL.md` files. After this Epic ships, the surviving inventory is `file_list_resolver.py`, `detect_fast_path.py`, and `scoped_marker_resolver.py` (three scripts, owned by `quo-setup`, `quo-setup`, and `quo-breakdown-epic` respectively).

2. The sibling-resolution example parenthetical at line ~462 — currently shows `quo-execute` computing `force_clean_team.py` / `check_agent_teams.py` paths and `quo-fix-issue` resolving the same scripts via `<quo-fix-issue base>/../quo-execute/scripts/`. After Epic `t1.5tm.kn` ships, the surviving sibling-resolution example is `quo-execute` and `quo-fix-issue` resolving `scoped_marker_resolver.py` from `quo-breakdown-epic`'s `scripts/` directory (a one-hop sibling resolution that is the same shape as the one being replaced).

3. The parenthetical at line ~560 in *Egg Resolver* — "(This is the same convention `quo-execute` and `quo-fix-issue` use for their `force_clean_team.py` references.)". The `force_clean_team.py` file is being deleted, so this concrete example must be replaced or the parenthetical dropped.

## What Needs to Change

In `skills/quo-setup/SKILL.md`:

1. **Update the bundled-scripts inventory paragraph at line ~454** (in *Resolve bundled helper script paths*). Replace the four-script list `file_list_resolver.py`, `detect_fast_path.py`, `force_clean_team.py`, `check_agent_teams.py` with the three-script list `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`. The surrounding sentence shape ("Helper scripts (...) ship bundled inside their owning skill's `scripts/` directory. Each skill that needs a bundled script computes the path at runtime from its own base directory — no probing, no persistence to CLAUDE.md.") stays intact. The inventory paragraph in this skill is illustrative, not authoritative — the goal is project-neutral skill prose, not an exhaustive registry. Listing three representative scripts is sufficient.

2. **Rewrite the sibling-resolution example parenthetical at line ~462** (in *Resolve bundled helper script paths*). Replace the current parenthetical:

   > "(e.g., `quo-execute` computes `<quo-execute base>/scripts/force_clean_team.py` and `<quo-execute base>/scripts/check_agent_teams.py`; `quo-fix-issue` computes `<quo-fix-issue base>/../quo-execute/scripts/force_clean_team.py` and `<quo-fix-issue base>/../quo-execute/scripts/check_agent_teams.py`)."

   …with a parenthetical that uses the surviving `scoped_marker_resolver.py` cross-skill resolution as the concrete example. Suggested rewrite (engineer may refine wording while preserving intent and OS-paired path shape):

   > "(e.g., `quo-execute` and `quo-fix-issue` compute `<quo-breakdown-epic base>/scripts/scoped_marker_resolver.py` from their own base directory — a one-hop sibling resolution into another skill's `scripts/` subtree)."

   The cross-skill (sibling-skill) shape — one skill resolving another skill's bundled script via `<sibling-base>/scripts/` — is the load-bearing point of the example, and `scoped_marker_resolver.py` is the post-Epic representative of that shape.

3. **Drop or rewrite the parenthetical at line ~560** in *Egg Resolver*: "(This is the same convention `quo-execute` and `quo-fix-issue` use for their `force_clean_team.py` references.)". Two acceptable shapes:
   - **Drop entirely.** The surrounding paragraph already explains the inline-the-literal convention without needing the cross-reference. Cleanest result.
   - **Rewrite** to point at the surviving `scoped_marker_resolver.py` cross-skill resolution, e.g. "(This is the same convention `quo-execute` and `quo-fix-issue` use for their `scoped_marker_resolver.py` references.)".

   Pick whichever produces the cleanest final paragraph. The dropping shape is preferred unless an immediate cross-reference materially helps the reader.

4. **Confirm there are no other `force_clean_team` / `check_agent_teams` references** in the file. The grep target for this Subtask is `force_clean_team\|check_agent_teams`.

## Key Files

- `skills/quo-setup/SKILL.md` — three small edits (inventory paragraph at ~454, sibling-resolution parenthetical at ~462, *Egg Resolver* parenthetical at ~560).

## Acceptance Criteria

- `grep -n "force_clean_team\|check_agent_teams" skills/quo-setup/SKILL.md` returns zero hits.
- The bundled-scripts inventory paragraph in *Resolve bundled helper script paths* lists exactly the three scripts `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`.
- The sibling-resolution example parenthetical in *Resolve bundled helper script paths* references `scoped_marker_resolver.py` and demonstrates the same one-hop cross-skill shape (`<sibling-skill-base>/scripts/<script-name>.py`) that the previous example demonstrated.
- The *Egg Resolver* paragraph at line ~560 either omits the cross-reference parenthetical entirely or references `scoped_marker_resolver.py` instead of the deleted `force_clean_team.py`.
- The three non-negotiable design rules (language-agnostic, OS-paired POSIX + PowerShell snippets, project-neutral) and the scratch-file convention are preserved.
- Bash etiquette is preserved — no shell snippets are added or restructured by this Subtask, so existing single-literal-command shapes remain intact.
