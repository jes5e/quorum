---
id: t3.5tm.kn.vt.by
type: t3
title: Static verification of post-cleanup bees-setup/SKILL.md against AC3 and AC4 (forbidden-token grep, required-prose presence, fast-path preservation, deletion confirmation, design-rule sweep, Lint)
down_dependencies:
- t3.5tm.kn.vt.vx
parent: t2.5tm.kn.vt
egg: null
created_at: '2026-05-05T12:19:45.701074'
status: done
schema_version: '0.1'
guid: 5tmknvtby5pjrw739ffmhimewsj58tzo
---

## Context

After Tasks `t2.5tm.kn.e9` (`bees-setup/SKILL.md` cleanup), `t2.5tm.kn.vk` (contributor-facing docs cleanup), and `t2.5tm.kn.6j` (helper-script deletion) land, the rewritten `skills/bees-setup/SKILL.md` must pass a battery of grep-driven static checks before the demo runbook can be reliably authored against it. Per the bees-workflow demo-runbook precedent (Epic `t1.5tm.o1` Task `t2.5tm.o1.7t`, Subtask `t3.5tm.o1.7t.i5`), this Subtask is dispatched-Agent static verification — observable findings are appended to the parent Task body's `## Probe results` section under a `### Static verification` subheading after the Agent runs.

The static checks come from two sources:

- Epic `t1.5tm.kn` AC3 (no Agent Teams / `teammateMode` / `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` prompts or settings writes in `bees-setup/SKILL.md`).
- Epic `t1.5tm.kn` AC4 (`detect_fast_path.py` invocation prose intact; helper not regressed).
- The repo's three non-negotiable design rules and the scratch-file convention from `CLAUDE.md`.

This Subtask is READ-ONLY. If any check FAILs, the finding is surfaced in the Probe results with file/line references and routed back to Task 1/2/3 as a new Issue or pre-`ready` rework cycle per the parent Task body's Cross-Task contracts — do NOT patch in this Task.

## What Needs to Change

Execute the following static checks against `skills/bees-setup/SKILL.md` and `skills/bees-setup/scripts/detect_fast_path.py`. Findings get appended to the parent Task body's `## Probe results` section under `### Static verification`.

**Forbidden-token grep checks (Epic AC3) — zero hits required in `skills/bees-setup/SKILL.md`:**

- `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`
- `Agent Teams`
- `teammateMode`
- `iTerm2 Split Pane`
- `force_clean_team`
- `check_agent_teams`
- `it2 backend` / references to the iTerm2 hard-prompt workaround prose

**Settings-write absence checks — zero hits required in `skills/bees-setup/SKILL.md`:**

- No reference to writing `~/.claude/settings.json` for `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` or `teammateMode` (the `Helper: write a single key to ~/.claude/settings.json` Python one-liner section, the `Per-machine Claude Code settings` slow-path section, and the `Configure per-machine Claude Code settings (condensed prompt)` fast-path subsection should all be gone OR repurposed without those two keys).
- No `AskUserQuestion` prompt about Agent Teams enablement.
- No `AskUserQuestion` prompt about `teammateMode` / display backend.

**Required-prose presence checks — at least one occurrence required in `skills/bees-setup/SKILL.md`:**

- `bees colonize-hive` invocation for the `issues` hive with the canonical scope/`--egg-resolver` shape.
- `bees colonize-hive` invocation for the `plans` hive with the canonical scope/`--egg-resolver` shape and `bees set-types` with the t1/t2/t3 child-tier JSON.
- `## Documentation Locations` section-write prose with all seven contract-key bullets (`Project requirements doc (PRD)`, `Internal architecture docs (SDD)`, `Customer-facing docs`, `Engineering best practices`, `Test writing guide`, `Test review guide`, `Doc writing guide`).
- `## Build Commands` section-write prose with all five contract-key bullets (`Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`).
- Optional PRD/SDD bootstrap step (the `Bootstrap PRD/SDD from existing codebase` subsection) is still present and functional.

**Fast-path detection preservation (Epic AC4):**

- `detect_fast_path.py` invocation prose is present in the SKILL.md, including paired POSIX (`python3 "<bees-setup-base-dir>/scripts/detect_fast_path.py" --repo-root "$(pwd)"`) and PowerShell (`python "<bees-setup-base-dir>\scripts\detect_fast_path.py" --repo-root "$((Get-Location).Path)"`) snippets.
- The JSON-payload schema documentation (the `repo_root` / `on_disk_hives` / `any_registered_for_repo` / `registered_hive_names` / `claude_md_doc_locations_set_up` / `claude_md_build_commands_set_up` / `fast_path_eligible` keys) is documented in the prose.
- The three-condition `fast_path_eligible` decision rule is documented (on-disk hives non-empty, no scope covers repo, both CLAUDE.md sections populated).
- `skills/bees-setup/scripts/detect_fast_path.py` exists at exactly that path.
- `git diff <epic-start-ref> -- skills/bees-setup/scripts/detect_fast_path.py` returns empty output (no helper-content changes from pre-Epic state). Compute `<epic-start-ref>` at runtime as the SHA of the commit immediately before this Epic's first commit on the working branch (e.g., the parent of the first commit on `bee/b.5tm` whose subject references Epic `t1.5tm.kn`).

**Helper-script deletion checks (Epic AC2 cross-Task confirmation):**

- `skills/bees-execute/scripts/force_clean_team.py` does NOT exist on disk.
- `skills/bees-execute/scripts/check_agent_teams.py` does NOT exist on disk.
- Repo-wide grep for `force_clean_team` and `check_agent_teams` across `skills/`, `docs/`, `README.md`, `CONTRIBUTING.md`, and `CLAUDE.md` returns zero hits in the **disallowed locations** (allowed remaining hits per `t2.5tm.kn.6j`'s allow-list: `docs/prd.md` per-feature subsection, `docs/sdd.md` per-feature subsection, `docs/sdd.md` non-feature sections, `README.md` — Epic `t1.5tm.fy` scope).
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists at exactly that path (Epic body's preserved-cross-cutting-work guarantee — must NOT have been deleted by Task 3).
- `skills/bees-setup/scripts/file_list_resolver.py` exists at exactly that path (out-of-scope helper, must NOT have been deleted).

**Design-rule and convention checks:**

- All shell snippets in the file appear as paired POSIX (bash / zsh) + Windows PowerShell variants (design rule 2). No bash-only snippets without a PowerShell counterpart.
- No language-specific commands, file extensions, or manifest filenames hardcoded in the prose (design rule 1) — e.g., no literal `cargo test` / `npm install` / `go test` etc.
- No references to this repo's own paths, ticket IDs, or internal workflow specifics that wouldn't make sense in a different target project (design rule 3).
- Any transient scratch-file write the prose instructs (e.g., for a `bees update-ticket --body-file` payload) writes under `<tempdir>/.bees-workflow/` with `mkdir -p` / `New-Item -ItemType Directory -Force` create-if-absent, and does NOT instruct the caller to `rm` / `Remove-Item` the scratch file (scratch-file convention).
- Bash-tool calls follow `CLAUDE.md` `## Bash etiquette in this repo`: no compound chains (`&&`, `||`, `;` between commands), no pipes between commands, no diagnostic tails (`; echo exit=$?`), no shell-variable expansion across snippets (`$VAR`, `$(...)`).

**Lint check:**

- Run the project Lint command from `CLAUDE.md` `## Build Commands` (currently `python -m pyflakes skills/*/scripts/*.py`) once at the end of the Subtask. Confirm zero pyflakes findings (the helper-script deletion in Task 3 should not have left a broken import in the remaining helpers).

## Key Files

- `skills/bees-setup/SKILL.md` — target of all SKILL.md grep checks (read-only)
- `skills/bees-setup/scripts/detect_fast_path.py` — preservation check (path-existence + git-diff against pre-Epic SHA)
- `skills/bees-execute/scripts/force_clean_team.py` — must NOT exist (deletion-confirmation check)
- `skills/bees-execute/scripts/check_agent_teams.py` — must NOT exist (deletion-confirmation check)
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` — must exist (preserved-helper check)
- `skills/bees-setup/scripts/file_list_resolver.py` — must exist (out-of-scope helper check)
- `docs/`, `README.md`, `CONTRIBUTING.md`, `CLAUDE.md` — repo-wide grep targets for residual `force_clean_team` / `check_agent_teams` references

## Acceptance Criteria

- All seven forbidden-token grep checks return zero hits in `skills/bees-setup/SKILL.md`.
- No `AskUserQuestion` prompt or settings-file write for Agent Teams or `teammateMode` remains in the SKILL.md.
- All required-prose presence checks (hive colonization for `issues` and `plans`, `## Documentation Locations` with all seven contract-key bullets, `## Build Commands` with all five contract-key bullets, optional PRD/SDD bootstrap) PASS.
- Paired POSIX + PowerShell `detect_fast_path.py` invocation snippets are present.
- The JSON-payload schema and three-condition `fast_path_eligible` decision rule are documented in the prose.
- `skills/bees-setup/scripts/detect_fast_path.py` exists and is unchanged from pre-Epic state (`git diff <epic-start-ref> -- ...` returns empty).
- `skills/bees-execute/scripts/force_clean_team.py` and `skills/bees-execute/scripts/check_agent_teams.py` both do NOT exist on disk.
- Repo-wide grep for `force_clean_team` / `check_agent_teams` across the disallowed locations (`skills/` excluding the deleted targets themselves, `CLAUDE.md`, `CONTRIBUTING.md`, `docs/doc-writing-guide.md`) returns zero hits.
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` and `skills/bees-setup/scripts/file_list_resolver.py` both exist (preserved helpers untouched).
- Design-rule and convention checks (paired snippets, no stack-specific hardcoding, no project-specific references, scratch-file convention adherence, Bash etiquette adherence) all PASS.
- Project Lint (`python -m pyflakes skills/*/scripts/*.py`) returns zero findings.
- All findings (PASS / FAIL per check, with file/line references for any FAILs) are appended to the parent Task body's `## Probe results` section under a `### Static verification` subheading per the bees-workflow demo-runbook precedent.
- If any check FAILs, the Subtask report explicitly names the affected Task (`t2.5tm.kn.e9`, `t2.5tm.kn.vk`, or `t2.5tm.kn.6j`) so the regression can be routed back via a new Issue or pre-`ready` rework cycle — do NOT patch in this Task.
