---
id: t2.5tm.kn.vt
type: t2
title: 'Live-demo verification: /bees-setup runs end-to-end without Agent Teams prompts;
  detect_fast_path.py still works'
up_dependencies:
- t2.5tm.kn.6j
parent: t1.5tm.kn
children:
- t3.5tm.kn.vt.by
- t3.5tm.kn.vt.vx
created_at: '2026-05-05T11:50:55.911727'
status: done
schema_version: '0.1'
guid: 5tmknvt7u12nuvbea9wqb8p2f3qkiu89
reference_materials: null
---
## Context

Epic `t1.5tm.kn`'s acceptance criteria 3 and 4 require live-demo verification:

- AC3: `/bees-setup` runs end-to-end on a fresh repo with no Agent Teams or display-backend prompts and no prompts about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`.
- AC4: The fast-path detection (`skills/bees-setup/scripts/detect_fast_path.py`) still works on multi-machine clones — it's not Agent Teams-related, but the cleanup should not regress it.

This Task gates the Epic's "ready for shipped" status. It runs after Tasks 1 (`bees-setup/SKILL.md` cleanup), 2 (contributor-facing docs cleanup), and 3 (helper-script deletion) land.

The verification follows this repo's demo-runbook pattern: dispatched research Agents do **static verification** (read the post-cleanup `bees-setup/SKILL.md` end-to-end, simulate the walk-through against the Build Commands and Documentation Locations contract keys, confirm no remaining settings-write step, confirm `detect_fast_path.py` invocation prose is intact) **plus author a manual runbook** the user runs from a fresh Claude Code session against a temp-directory fresh repo. Live execution is the user's responsibility from a fresh session — the orchestrator does not run it from inside this skill invocation.

## What Needs to Change

1. **Static verification (research-mode dispatch deliverable).** Read `skills/bees-setup/SKILL.md` end-to-end after Task 1 lands and walk through it as if a fresh user were invoking `/bees-setup` for the first time on a fresh repo. Confirm:
   - No step prompts the user about Agent Teams.
   - No step prompts the user about `teammateMode`.
   - No step writes to `~/.claude/settings.json` for `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` or `teammateMode`.
   - Hive-colonization, `## Documentation Locations` write, `## Build Commands` write, and the optional PRD/SDD bootstrap step are all still present and functional.
   - `detect_fast_path.py` invocation prose (helper script + bash/PowerShell snippets) is unchanged.
   - The skill prose still follows the three non-negotiable design rules (language-agnostic, OS-paired POSIX + PowerShell snippets, project-neutral) and the scratch-file convention from `CLAUDE.md`.
   - The skill prose's Bash-tool calls follow the `CLAUDE.md` `## Bash etiquette in this repo` etiquette (single literal command per call, no compound chains, no diagnostic tails, etc.).
2. **Author a manual end-to-end runbook** under the namespaced workflow scratch dir per `CLAUDE.md` `## Scratch-file convention` — `/tmp/.quorum/<task-id>-runbook.md` on POSIX, `$env:TEMP\.quorum\<task-id>-runbook.md` on Windows — that the user can execute from a fresh Claude Code session. **The runbook is a transient artifact and is NOT committed to the repo** (matches the demo-runbook precedent on Epic `t1.5tm.o1` Task `t2.5tm.o1.7t`). The runbook must include:
   - Steps to set up a temp-directory fresh repo (`git init` in a fresh `<tempdir>/.quorum/`-shape directory) so `/bees-setup` runs against a non-colonized state.
   - The `/bees-setup` invocation and the expected interactive flow (which prompts to expect, which to NOT see).
   - A checklist of observable outcomes mapped to AC3 and AC4.
   - A multi-machine clone simulation step that exercises `detect_fast_path.py` (e.g., set up a parallel clone with hives already colonized in the original tree, run `/bees-setup` in the second clone, observe the fast-path branch fires).
   - Per the demo-runbook precedent, **observations from the live run are appended to this Task body's `## Probe results` section** (NOT to the runbook file). The orchestrator records the user-reported observations back into the bees-stored Task body as the closing artifact for the Epic.
3. **Surface any spec divergence found during static verification** to the orchestrator in the JSON-structured findings — for example, if a Task 1 / Task 2 / Task 3 review-cycle gap left a stale reference, the verification finds it before the user wastes a fresh-session run on it.

The dispatched Agents are research-only — they do not modify `bees-setup/SKILL.md` or run `/bees-setup`. They produce the runbook (a markdown file) and the JSON-structured findings.

## Key Files

- `skills/bees-setup/SKILL.md` — read-only, source of truth for the static verification.
- `skills/bees-setup/scripts/detect_fast_path.py` — read-only, source of truth for the fast-path verification.
- `<tempdir>/.quorum/<task-id>-runbook.md` — the runbook authored for the user to execute manually. Lives in the namespaced workflow scratch dir per `CLAUDE.md` `## Scratch-file convention`; not committed to the repo; never deleted by the skill.
- This Task's body in bees (`bees show-ticket --ids t2.5tm.kn.vt`) — the home of the `## Probe results` section that captures user-reported live-run observations.

## Acceptance Criteria

- A reviewable runbook is authored at `<tempdir>/.quorum/<task-id>-runbook.md` (POSIX `/tmp/.quorum/<task-id>-runbook.md`, Windows `$env:TEMP\.quorum\<task-id>-runbook.md`) covering both AC3 (no Agent Teams / teammateMode prompts) and AC4 (fast-path detection works). The runbook is a transient scratch file and is NOT committed to the repo (matches the demo-runbook precedent on Epic `t1.5tm.o1` Task `t2.5tm.o1.7t`).
- After the user manually executes the runbook from a fresh Claude Code session, observations are appended to **this Task's body** at a `## Probe results` section (mirrors how the live demo for Epic `t1.5tm.o1` was recorded — see commit `453abae` for shape).
- Static verification finds no Agent Teams / `teammateMode` / `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` references in `skills/bees-setup/SKILL.md`.
- Static verification finds `detect_fast_path.py` invocation prose intact (no regression introduced by the cleanup).
- The runbook follows the **scratch-file convention** for any temp directories it instructs the user to create (`<tempdir>/.quorum/` namespace; no rm/Remove-Item cleanup instructions).
- The runbook's bash and PowerShell snippets follow design rule 2 (paired POSIX + PowerShell).

## Cross-Task contracts

- **Depends on Task `t2.5tm.kn.e9`** (bees-setup/SKILL.md cleanup), **Task `t2.5tm.kn.vk`** (contributor-facing docs cleanup), and **Task `t2.5tm.kn.6j`** (helper-script deletion). All three must land before this Task's static verification and runbook-authoring can proceed against an accurate post-cleanup state.
- **Reads-only verification.** This Task does not change `bees-setup/SKILL.md` or any source code. If verification surfaces a regression, route the fix back to Task 1 / 2 / 3 as appropriate via a new Issue or a pre-`ready` rework cycle on the affected Task — do not patch in this Task.
- **Demo-runbook pattern.** Per the `quorum` demo-runbook precedent (Epic `t1.5tm.o1`'s live-demo Probe results recorded by commit `453abae` on 2026-05-03), the runbook is authored by dispatched Agents but executed by the user manually from a fresh Claude Code session; observations are committed back into the runbook's `## Probe results` section as the closing artifact.

## Probe results

### Static verification

Recorded by the dispatched Engineer for Subtask `t3.5tm.kn.vt.by` on 2026-05-05 against branch `bee/b.5tm` at the post-Task-1/2/3 state (Epic-start SHA `0719c3a`). Each check below maps 1:1 to a check enumerated in the Subtask's "What Needs to Change" section.

**Forbidden-token grep against `skills/bees-setup/SKILL.md`** (Epic AC3 — zero hits required for: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `Agent Teams`, `teammateMode`, `iTerm2`, `force_clean_team`, `check_agent_teams`, `it2 backend`): **PASS** — `grep -n "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS\|Agent Teams\|teammateMode\|iTerm2\|force_clean_team\|check_agent_teams\|it2 backend" skills/bees-setup/SKILL.md` returned zero matches.

**Settings-write absence (no `~/.claude/settings.json` writes for `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` or `teammateMode`)**: **PASS** — `grep -n "settings.json" skills/bees-setup/SKILL.md` returned zero matches. The `Helper: write a single key to ~/.claude/settings.json` Python one-liner section, the `Per-machine Claude Code settings` slow-path section, and the `Configure per-machine Claude Code settings (condensed prompt)` fast-path subsection are all gone.

**No `AskUserQuestion` prompt about Agent Teams enablement**: **PASS** — All 11 `AskUserQuestion` occurrences in the file (lines 185, 187, 223, 411, 446, 462, 513, 544, 641, 693, 703, 732) are scoped to: hive re-registration, doc-path strategy, doc-type picking, bootstrap option picking, build-command slot walk, and next-step recommendation. None reference Agent Teams.

**No `AskUserQuestion` prompt about `teammateMode` / display backend**: **PASS** — Same evidence as above; no `AskUserQuestion` prose mentions `teammateMode`, display backend, iTerm2, or in-process.

**Required-prose presence — `bees colonize-hive` for `issues` hive**: **PASS** — Present at SKILL.md line 161 (POSIX) and line 172 (PowerShell) in the *Re-register each on-disk hive* fast-path block, and again at line 419 (POSIX) and line 423 (PowerShell) in the slow-path *Hive Configuration* block. Both invocations carry the canonical scope (`<repo-root>/**`) and `--egg-resolver` (`<bees-setup-base-dir>/scripts/file_list_resolver.py`) shape.

**Required-prose presence — `bees colonize-hive` for `plans` hive plus `bees set-types` with t1/t2/t3 child-tier JSON**: **PASS** — Present at SKILL.md line 165 (POSIX) and line 176 (PowerShell). The accompanying `bees set-types` calls at lines 166 and 177 use the canonical `'{"t1":["Epic","Epics"],"t2":["Task","Tasks"],"t3":["Subtask","Subtasks"]}'` payload.

**Required-prose presence — `## Documentation Locations` write with all seven contract-key bullets**: **PASS** — Section template at SKILL.md lines 491-503 carries all seven bullets (`Project requirements doc (PRD)`, `Internal architecture docs (SDD)`, `Customer-facing docs`, `Engineering best practices`, `Test writing guide`, `Test review guide`, `Doc writing guide`). The walk-through table at lines 448-456 covers the same seven keys row-by-row.

**Required-prose presence — `## Build Commands` write with all five contract-key bullets**: **PASS** — Section template at SKILL.md lines 716-724 carries all five bullets (`Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`). The walk-through enumeration at lines 695-699 covers the same five slots in order.

**Required-prose presence — Optional PRD/SDD bootstrap step**: **PASS** — `### Bootstrap PRD/SDD from existing codebase (optional)` section is present at SKILL.md lines 505-662 with all five Steps (A: Explore the codebase, B: Ask the questions code can't answer, C: Generate the seed docs, D: Show drafts and apply, E: Tell the user how the docs grow from here) intact and functional.

**Fast-path detection — paired POSIX + PowerShell `detect_fast_path.py` invocation snippets**: **PASS** — POSIX snippet at SKILL.md lines 99-101 (`python3 "<bees-setup-base-dir>/scripts/detect_fast_path.py" --repo-root "$(pwd)"`) and PowerShell snippet at lines 104-106 (`python "<bees-setup-base-dir>\scripts\detect_fast_path.py" --repo-root "$((Get-Location).Path)"`).

**Fast-path detection — JSON-payload schema documentation**: **PASS** — JSON example at SKILL.md lines 110-121 documents all required keys: `repo_root`, `on_disk_hives`, `any_registered_for_repo`, `registered_hive_names`, `claude_md_path`, `claude_md_doc_locations_set_up`, `claude_md_build_commands_set_up`, `fast_path_eligible`.

**Fast-path detection — three-condition `fast_path_eligible` decision rule documented**: **PASS** — The three-condition decision rule is enumerated at SKILL.md lines 123-127 (1: on-disk hive markers exist; 2: no registered scope covers repo; 3: both CLAUDE.md sections populated with required contract bullets).

**Fast-path detection — `skills/bees-setup/scripts/detect_fast_path.py` exists at the expected path**: **PASS** — `ls skills/bees-setup/scripts/detect_fast_path.py` returns the file.

**Fast-path detection — `git diff 0719c3a -- skills/bees-setup/scripts/detect_fast_path.py` returns empty**: **PASS** — `git diff 0719c3a -- skills/bees-setup/scripts/detect_fast_path.py` returned empty output (no helper-content changes since the pre-Epic SHA).

**Helper-script deletion — `skills/bees-execute/scripts/force_clean_team.py` does NOT exist on disk**: **PASS** — File is absent. The entire `skills/bees-execute/scripts/` directory was removed by Task 3 (only `SKILL.md` remains under `skills/bees-execute/`).

**Helper-script deletion — `skills/bees-execute/scripts/check_agent_teams.py` does NOT exist on disk**: **PASS** — File is absent (same evidence as above).

**Helper-script deletion — repo-wide audit grep**: **PASS** — `grep -rn "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md` returned hits ONLY in the allowed locations: `README.md` (1 hit at line 153 — the helper-bundle bullet listing), `docs/prd.md` (2 hits at lines 64, 75 — per-feature subsection), `docs/sdd.md` (7 hits at lines 6, 37, 38, 52, 193, 203, 204 — mix of per-feature subsection at 193/203/204 and non-feature sections at 6/37/38/52). Zero hits in disallowed locations (`skills/`, `CLAUDE.md`, `CONTRIBUTING.md`, `docs/doc-writing-guide.md`). All hits are within the allow-list scoped by Task `t2.5tm.kn.6j` and Epic `t1.5tm.fy`.

  - Borderline note (informational, NOT a FAIL): `README.md:153` lists `force_clean_team.py` as one of three helper-script examples in the bundle. Since Task 3 deleted that script, the example is now stale. This sits in `README.md` which is the `t1.5tm.fy` Epic's domain (customer-facing-doc cleanup), not Epic `t1.5tm.kn`'s — flagging it here only as visibility for the parent orchestrator. No regression to route back to Task 1 / 2 / 3 of this Epic.

**Preserved-helper check — `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists**: **PASS** — `ls skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` returns the file. Cross-cutting helper preserved per the Epic body's preserved-cross-cutting-work guarantee.

**Preserved-helper check — `skills/bees-setup/scripts/file_list_resolver.py` exists**: **PASS** — `ls skills/bees-setup/scripts/file_list_resolver.py` returns the file. Out-of-scope helper preserved.

**Design rule 2 — paired POSIX + PowerShell snippets**: **PASS** — Every fenced `bash` / shell snippet in the file has a paired `powershell` snippet within a few lines. Spot-checked at: lines 99/104 (detect_fast_path.py invocation), 148/153 (resolver-existence guard), 160/171 (fast-path hive registration), 204/212 (unknown-hive registration), 261/296 (Skill Paths migration), 352/357 (Egg Resolver guard), 364/378 (Egg Resolver Python one-liner), 418/422 (slow-path hive colonization), 565/568 (docs/ creation in bootstrap). Plus mixed POSIX/PowerShell/cmd.exe block at lines 50-58 for the `which bees` / `Get-Command` / `where bees` pattern. No bash-only snippets without PowerShell counterpart.

**Design rule 1 — no language-specific commands hardcoded as prescriptive prose**: **PASS** — The only literal language-tool names in the file (`cargo`, `npm`, `tsc`, `pytest`, `go test`, `mvn`, etc.) appear inside the stack-detection table at SKILL.md lines 676-687. That table is *proposed defaults* presented to the user, not prescriptive prose, and the actual prescriptive lookup happens at runtime via the CLAUDE.md `## Build Commands` contract keys (line 726 explicitly calls out the contract). This matches the design-rule expectation: skills detect-stack-and-suggest, but contractually read commands from CLAUDE.md at use-time.

**Design rule 3 — no references to this repo's own paths, ticket IDs, or internal workflow specifics**: **PASS** — No mentions of `bee/b.5tm`, `t1.5tm.*`, `t2.5tm.*`, `t3.5tm.*`, this repo's own `docs/prd.md` or `docs/sdd.md` paths, etc. The skill prose remains project-neutral throughout.

**Scratch-file convention adherence**: **PASS (vacuously)** — `skills/bees-setup/SKILL.md` does not author any transient scratch files via `bees update-ticket --body-file` or similar (this skill writes `CLAUDE.md`, `docs/prd.md`, `docs/sdd.md`, and `~/.bees/config.json`, all of which are persistent target-repo or per-machine config files, not scratch files). `grep -n "tempdir\|.quorum\|/tmp\|TEMP\|rm \|Remove-Item" skills/bees-setup/SKILL.md` returned zero matches. No cleanup instructions present.

**Bash etiquette adherence (CLAUDE.md `## Bash etiquette in this repo`)**: **PASS with one acknowledged shape** — The file has two `||` short-circuits at SKILL.md line 149 and line 353, both inside a single-call shape `test -f "<path>" || { echo "..."; exit 1; }` used as a precondition guard for the resolver-existence check. This shape is one logical Bash call — a guarded `test`-then-error — and was present in the original pre-Epic file (it is not a regression introduced by Task 1 / 2 / 3). The skill prose does not chain multiple operations across `&&`/`||`/`;` for normal flow, has no diagnostic tails, no shell-variable expansion across snippets, no pipes between commands, and no inline heredocs.

**Lint check — `python -m pyflakes skills/*/scripts/*.py`**: **PASS** — Zero pyflakes findings against the three remaining helper scripts (`skills/bees-setup/scripts/detect_fast_path.py`, `skills/bees-setup/scripts/file_list_resolver.py`, `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`). Helper-script deletion did not leave a broken import in the remaining helpers.

**Overall**: All checks PASS. No regressions found, no FAILs to route back to Task `t2.5tm.kn.e9`, `t2.5tm.kn.vk`, or `t2.5tm.kn.6j`. The post-cleanup state is ready for the live-demo runbook authoring and execution under sibling Subtask `t3.5tm.kn.vt.vx`.

### Live demo

Recorded by the human executor on 2026-05-05 against branch `bee/b.5tm` at the post-Task-1/2/3/PM-approval state. Demo executed from a fresh Claude Code session against fresh temp-dir repo `/private/tmp/.quorum/setup-demo-kn1` (slow path) and `/private/tmp/.quorum/setup-demo-kn1-clone` (fast path / multi-machine clone simulation).

#### Slow-path observations

- **Observation 1 — No Agent Teams prompt fires:** **PASS** — `/bees-setup` invocation went straight from `detect_fast_path.py` (which reported `fast_path_eligible: false` for the empty repo) to "Fast-path not eligible — proceeding with full setup." No `AskUserQuestion` about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, no enable-Agent-Teams prompt, no mention of "Agent Teams is currently disabled".
- **Observation 2 — No teammateMode / display-backend prompt fires:** **PASS** — No `AskUserQuestion` about `teammateMode`. No terminal-detection table walk. No iTerm2 / tmux / it2 backend / Apple Terminal probing. No iTerm2 Split Pane Setup mention.
- **Observation 3 — No write to `~/.claude/settings.json` for the two removed keys:** **PASS** — `diff /tmp/.quorum/settings-before.json ~/.claude/settings.json` returned empty output; no changes to `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` or `teammateMode`.
- **Observation 4 — Hive colonization fires for both `issues` and `plans`:** **PASS** — Both `bees colonize-hive --name issues ...` and `bees colonize-hive --name plans ...` invocations fired with the canonical scope (`/private/tmp/.quorum/setup-demo-kn1/**`) and `--egg-resolver` (`/Users/jesseg/.claude/skills/bees-setup/scripts/file_list_resolver.py`). `bees set-types` for the plans hive carried the canonical t1/t2/t3 child-tier JSON. `bees set-status-values` followed for both hives. Post-run `bees list-hives` from the demo-repo path showed both hives registered with the canonical scope.
- **Observation 5 — `## Documentation Locations` written with all 7 contract-key bullets:** **PASS** — `grep -nE "^- \*\*(Project requirements doc|Internal architecture docs|Customer-facing docs|Engineering best practices|Test writing guide|Test review guide|Doc writing guide)" /tmp/.quorum/setup-demo-kn1/CLAUDE.md` returned 7 matches at lines 5-11. All values empty (each row was skipped individually per the demo flow). Section rows present.
- **Observation 6 — `## Build Commands` written with all 5 contract-key bullets:** **PASS** — `grep -nE "^- \*\*(Compile/type-check|Format|Lint|Narrow test|Full test)" /tmp/.quorum/setup-demo-kn1/CLAUDE.md` returned 5 matches at lines 15-19. All values are the placeholder stub `echo "not configured"` (the user picked "Use placeholder echo commands" in the Build Commands flow per the demo intent). Section rows present.
- **Observation 7 — Optional PRD/SDD bootstrap step offered, decline-flow proceeds cleanly:** **PASS with caveat** — The slow-path flow went directly from "Writing CLAUDE.md with empty Documentation Locations rows" → CLAUDE.md write → Build Commands prompt → Next Steps prompt → clean exit. The `### Bootstrap PRD/SDD from existing codebase (optional)` step did not surface as an explicit `AskUserQuestion`. Most likely cause: the demo repo is empty (no manifest files, no source files), so the bootstrap step's "explore the codebase" precondition has nothing to offer to bootstrap from, and the skill silently skipped the offer. Functionally equivalent to a successful decline (no error, run proceeded to Build Commands and exit cleanly), but the explicit decline-prompt path was not exercised. Worth verifying on a non-empty repo in a future demo to confirm the bootstrap step still surfaces when there IS code to derive specs from.
- **Observation 8 — Run completes without error and prints setup-complete confirmation:** **PASS** — Final Next Steps `AskUserQuestion` offered three options ("Starting from an idea (no PRD/SDD yet)", "I have finalized PRD and SDD docs", "I'm done for now"). User picked option 3 ("I'm done for now"). Run exited cleanly with no exceptions, no hard-fails, no abandoned subagents.

#### Fast-path observations (multi-machine clone simulation)

- **Observation 1 — `detect_fast_path.py` invoked upfront with the clone path:** **PASS** — Invocation: `python3 "/Users/jesseg/.claude/skills/bees-setup/scripts/detect_fast_path.py" --repo-root "$(pwd)"` with `$(pwd)` resolving to `/private/tmp/.quorum/setup-demo-kn1-clone`. JSON output's `repo_root` confirmed `/private/tmp/.quorum/setup-demo-kn1-clone`.
- **Observation 2 — JSON payload reports `fast_path_eligible: true`:** **PASS** — JSON payload showed `on_disk_hives` non-empty (both `issues` and `plans` markers carried over from the slow-path commit), `any_registered_for_repo: false`, `claude_md_doc_locations_set_up: true`, `claude_md_build_commands_set_up: true`, `fast_path_eligible: true`. (Output truncated in the harness preview at +16 lines but the visible keys plus the orchestrator's subsequent fast-path-only behavior confirm all four eligibility conditions met.)
- **Observation 3 — Fast-path Diagnose paragraph printed verbatim:** **PASS** — The orchestrator printed: "Looks like this repo was already set up for bees on another machine. The on-disk hive markers are here but they're not registered in your machine's bees config. I can re-register them for you and you'll be ready to go. CLAUDE.md will not be touched." Matches the fast-path Diagnose prose in `skills/bees-setup/SKILL.md`.
- **Observation 4 — Hive re-registration fires for both `issues` and `plans`; CLAUDE.md NOT touched:** **PASS** — Both `bees colonize-hive --name issues ...` and `bees colonize-hive --name plans ...` re-fired with the clone path's canonical scope (`/private/tmp/.quorum/setup-demo-kn1-clone/**`) and the same egg-resolver. Plans hive received `bees set-types` + `bees set-status-values`; issues hive received `bees set-status-values`. No `## Documentation Locations` re-prompt, no `## Build Commands` re-prompt, no edit to CLAUDE.md. Post-run `diff /tmp/.quorum/setup-demo-kn1/CLAUDE.md /tmp/.quorum/setup-demo-kn1-clone/CLAUDE.md` returned empty output (byte-identical).
- **Observation 5 — No Agent Teams or teammateMode prompt fires in fast-path branch:** **PASS** — Same negative checks as slow-path Observations 1 and 2: no `AskUserQuestion` about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, no `teammateMode` prompt, no iTerm2 / tmux / display-backend mention.
- **Observation 6 — Fast-path run completes with re-registration message and clean Confirm-and-exit:** **PASS with caveat — actual completion message differs from runbook's forecast literal** — Actual orchestrator message: "Setup complete. The fast path skipped the full walk-through because your repo was already configured. Anything else?" with options "Exit now (Recommended)" and "Continue with full setup walk". Runbook had forecast the literal "Re-registered N hives. You're ready to go." Functional behavior matches the AC (clean re-registration completion + Confirm-and-exit prompt with Exit-now option), but the SKILL.md's actual completion-message prose has drifted from the runbook's forecast. Worth a one-line edit to the runbook (or a runbook regeneration) to sync the expected literal — non-blocking for this Bee since the AC itself is met. User picked option 1 (Exit now); run exited cleanly.

#### Spot-check

- **(a) Slow-path repo `CLAUDE.md` contains both contract sections with all required bullets:** **PASS** — Confirmed via the two grep verifications recorded under slow-path Observations 5 and 6 (7 + 5 contract-key bullets present).
- **(b) `bees list-hives` from both repo paths shows `issues` and `plans` registered:** **PASS** — From `/private/tmp/.quorum/setup-demo-kn1` and from `/private/tmp/.quorum/setup-demo-kn1-clone`, both invocations returned exactly two hives (`issues` and `plans`) with paths and scopes matching the respective repo path.
- **(c) `~/.claude/settings.json` was NOT modified for the two removed keys:** **PASS** — `diff /tmp/.quorum/settings-before.json ~/.claude/settings.json` returned empty output after both runs.

#### Overall verdict

**PASS.** Demo confirms the post-cleanup `/bees-setup` matches Epic `t1.5tm.kn`'s AC3 (no Agent Teams / `teammateMode` / settings-write prompts) and AC4 (fast-path detection works on multi-machine clones). Two non-blocking caveats recorded:

1. Slow-path Observation 7 — bootstrap step did not surface an explicit decline prompt on the empty demo repo. Likely auto-skipped because there's nothing to bootstrap from. Verify on a non-empty repo in a future demo.
2. Fast-path Observation 6 — actual SKILL.md completion-message prose ("Setup complete. The fast path skipped the full walk-through because your repo was already configured.") differs from the runbook's forecast literal ("Re-registered N hives. You're ready to go."). Functional equivalence holds; sync runbook on next regeneration.

