---
id: t1.5tm.kn
type: t1
title: quo-setup cleanup and helper-script deletion
up_dependencies:
- t1.5tm.8s
- t1.5tm.fk
- t1.5tm.o1
down_dependencies:
- t1.5tm.fy
parent: b.5tm
children:
- t2.5tm.kn.e9
- t2.5tm.kn.vk
- t2.5tm.kn.6j
- t2.5tm.kn.vt
created_at: '2026-05-03T01:16:46.555729'
status: done
schema_version: '0.1'
guid: 5tmkn9j8f1vn2v6xykw1jv5xsfb9j7iy
reference_materials: null
---
## What this Epic delivers

Remove Agent Teams setup ceremony from `quo-setup/SKILL.md` and delete the now-unused helper scripts.

## Acceptance criteria

1. `quo-setup/SKILL.md` no longer contains: Agent Teams precondition step, `teammateMode` configuration step, iTerm2 hard-prompt workaround prose, references to `force_clean_team.py` or `check_agent_teams.py`.
2. The two helper scripts no longer exist on disk:
   - `skills/quo-execute/scripts/force_clean_team.py` deleted
   - `skills/quo-execute/scripts/check_agent_teams.py` deleted
3. `/quo-setup` runs end-to-end on a fresh repo: hives colonized, CLAUDE.md `## Documentation Locations` and `## Build Commands` sections written, optional PRD/SDD bootstrap. No Agent Teams or display backend prompts. No prompts about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`.
4. The fast-path detection (`detect_fast_path.py`) still works on multi-machine clones — it's not Agent Teams-related.
5. CLAUDE.md retains both required contract sections (`## Documentation Locations`, `## Build Commands`); the Agent Teams precondition is removed from skills' precondition checks.

## Codebase-green check

`/quo-setup` runs cleanly on a fresh repo. All execution skills (post-B/C/D) run without Agent Teams. `/quo-status`, `/quo-plan`, `/quo-plan-from-specs`, `/quo-file-issue`, the three review skills unaffected.

## Implementation notes

- **Sequencing-critical.** This Epic must NOT ship before B, C, and D — removing the Agent Teams precondition before all three execution skills are rewritten leaves users in a state where `quo-setup` says "no special setup needed" but `quo-execute` (or one of its siblings) still hard-fails for lack of Teams. The dependency wiring on this Epic enforces this.
- **Pre-deletion grep audit.** Before deleting the two scripts, grep across the repo for any remaining references: `grep -r "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md`. The grep should return zero hits before deletion (post-B/C/D world).
- **`quo-fix-issue`'s sibling-resolved reference to `check_agent_teams.py`** (current `quo-fix-issue/SKILL.md` resolves the script as a sibling of `quo-execute/scripts/`) — this reference disappears as part of Epic C, so by the time Epic E runs the script has no callers.

## Preserved cross-cutting work (b.mu9 / b.51d shipped 2026-05-03)

The helper-script deletion criterion in this Epic is restricted to **two files only**:

- `skills/quo-execute/scripts/force_clean_team.py` — DELETE
- `skills/quo-execute/scripts/check_agent_teams.py` — DELETE

The following helper, also bundled with this skill set, MUST NOT be deleted, moved, or renamed by this Epic:

- `skills/quo-breakdown-epic/scripts/scoped_marker_resolver.py` — **KEEP** (sibling-resolved by `quo-execute` and `quo-fix-issue`; part of the cumulative-PRD scoping work shipped by b.mu9 / b.51d on 2026-05-03)

The other two helpers (`skills/quo-setup/scripts/file_list_resolver.py` and `skills/quo-setup/scripts/detect_fast_path.py`) are out of scope for this Epic — leave them alone.

Pre-deletion grep audit (mandatory): `grep -r "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md` should return zero hits before deletion. If any references remain post-Epics B/C/D, surface them and pause this Epic until they're cleaned up.
