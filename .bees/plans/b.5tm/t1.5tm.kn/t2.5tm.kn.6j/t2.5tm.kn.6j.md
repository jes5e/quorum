---
id: t2.5tm.kn.6j
type: t2
title: Delete force_clean_team.py and check_agent_teams.py from skills/bees-execute/scripts/
up_dependencies:
- t2.5tm.kn.e9
- t2.5tm.kn.vk
down_dependencies:
- t2.5tm.kn.vt
parent: t1.5tm.kn
children:
- t3.5tm.kn.6j.mo
egg: null
created_at: '2026-05-05T11:50:47.780561'
status: ready
schema_version: '0.1'
guid: 5tmkn6je3utw6e4ksv2dmuwq3k1k2tkp
---

## Context

Epic `t1.5tm.kn` deletes the two now-unused helper scripts after the SKILL-prose and contributor-doc cleanup is done:

- `skills/bees-execute/scripts/force_clean_team.py` — force-cleans stuck Claude Code teams; no callers remain post-Epics B/C/D.
- `skills/bees-execute/scripts/check_agent_teams.py` — verifies the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` precondition; replaced post-Epics B/C/D by the seven-subagent registry check.

The `scoped_marker_resolver.py` helper at `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` is **NOT** in this Task's deletion scope — it is the cumulative-PRD scoping helper shipped by b.mu9 / b.51d on 2026-05-03 and is sibling-resolved by `bees-execute` and `bees-fix-issue`. The other two surviving helpers (`bees-setup/scripts/file_list_resolver.py` and `bees-setup/scripts/detect_fast_path.py`) are also out of scope.

This Task is gated on Task `t2.5tm.kn.e9` (bees-setup/SKILL.md cleanup) and Task `t2.5tm.kn.vk` (contributor-facing docs cleanup) completing — those Tasks remove the references this Task's pre-deletion grep audit checks for.

## What Needs to Change

1. **Pre-deletion grep audit (mandatory).** Run `grep -rn "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md` from the repo root. The expected post-Tasks-1-and-2 surface is:
   - **Allowed remaining hits**: per-feature subsection content in `docs/prd.md` `### Feature: Ephemeral-Agent Orchestration` (around lines 64, 75) and `docs/sdd.md` `### Feature: Ephemeral-Agent Orchestration` (around lines 162, 193, 195, 203-208, 218, 220, 230, 234, 235). These are intentional historical records of what this Bee delivers and stay intact across this Epic and Epic `t1.5tm.fy`.
   - **Allowed remaining hits in non-feature sections** that Epic `t1.5tm.fy` will clean: `docs/sdd.md` lines 6, 8, 38, 56 (Tech stack, External dependencies, Key components, Team orchestration in execution skills); `README.md` lines 43-58, 114, 153 (Required: enable Agent Teams + Display backend + skill table description + helper-script mention).
   - **Disallowed**: any hit in `skills/` (excluding the two scripts themselves), in `CLAUDE.md`, in `CONTRIBUTING.md`, or in `docs/doc-writing-guide.md`. If any disallowed hit appears, surface it back to the orchestrator (the parent caller), pause this Task, and route the gap-fill back through the appropriate sibling Task before resuming.
2. **Delete `skills/bees-execute/scripts/force_clean_team.py`** from disk via `git rm`.
3. **Delete `skills/bees-execute/scripts/check_agent_teams.py`** from disk via `git rm`.
4. **Post-deletion grep audit.** Re-run the same grep and confirm only the allowed remaining hits are present (per-feature PRD/SDD subsections + Epic `t1.5tm.fy`-scoped non-feature sections).
5. **Run the project lint** (`python -m pyflakes skills/*/scripts/*.py` per `CLAUDE.md` `## Build Commands` `Lint`) over the surviving scripts to confirm none of them imported anything from the deleted scripts. Should be a no-op given the deleted scripts had no callers post-Epics B/C/D, but the lint sweep is cheap.

## Key Files

- `skills/bees-execute/scripts/force_clean_team.py` — DELETE
- `skills/bees-execute/scripts/check_agent_teams.py` — DELETE
- (no edits to other files in this Task — references already cleaned by Tasks 1 and 2)

## Acceptance Criteria

- Post-deletion grep `grep -rn "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md` returns hits only in `docs/prd.md` `### Feature: Ephemeral-Agent Orchestration`, `docs/sdd.md` `### Feature: Ephemeral-Agent Orchestration` and SDD non-feature sections (Epic `t1.5tm.fy` will handle the latter), and `README.md` (Epic `t1.5tm.fy` will handle).
- `skills/bees-execute/scripts/force_clean_team.py` and `skills/bees-execute/scripts/check_agent_teams.py` no longer exist on disk (verified by `ls`).
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`, `skills/bees-setup/scripts/file_list_resolver.py`, and `skills/bees-setup/scripts/detect_fast_path.py` still exist on disk (regression check — these are out of scope).
- `python -m pyflakes skills/*/scripts/*.py` runs cleanly (no broken imports introduced by the deletion).

## Cross-Task contracts

- **Depends on Task `t2.5tm.kn.e9`** (bees-setup/SKILL.md cleanup) and **Task `t2.5tm.kn.vk`** (contributor-facing docs cleanup). Both must land before this Task's pre-deletion grep audit can pass.
- **Gates the live-demo verification Task** (the fourth Task under this Epic). The verification Task runs `/bees-setup` end-to-end on a fresh repo and expects no Agent Teams or `teammateMode` prompts — that behavior depends on this Task plus the two upstream cleanup Tasks all having shipped.
- **Out of scope.** `README.md`, `docs/sdd.md` non-feature sections, and the `b.gar` body refresh — Epic `t1.5tm.fy`. Per-feature `docs/prd.md` and `docs/sdd.md` `### Feature: Ephemeral-Agent Orchestration` subsections — stay intact as historical record.
