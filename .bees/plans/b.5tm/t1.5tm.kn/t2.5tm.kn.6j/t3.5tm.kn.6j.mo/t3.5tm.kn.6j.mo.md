---
id: t3.5tm.kn.6j.mo
type: t3
title: Pre-audit, delete force_clean_team.py and check_agent_teams.py via git rm,
  post-audit, lint sweep
parent: t2.5tm.kn.6j
created_at: '2026-05-05T12:10:51.005234'
status: done
schema_version: '0.1'
guid: 5tmkn6jmo5parptx8spqyaf7bjinsqih
reference_materials: null
---
## Context

This Subtask carries out the entire deletion Task `t2.5tm.kn.6j` under Epic `t1.5tm.kn` in one commit-sized unit: it audits that no disallowed references remain to the two helper scripts (sibling Tasks `t2.5tm.kn.e9` and `t2.5tm.kn.vk` will have removed them), deletes the two scripts via `git rm`, re-audits to confirm only allowed historical / Epic-`t1.5tm.fy`-scoped hits remain, and runs the project lint over the surviving helper scripts to confirm no callers broke.

Preconditions assumed:

- Sibling Task `t2.5tm.kn.e9` (`bees-setup/SKILL.md` cleanup) is `done` — references at `skills/bees-setup/SKILL.md` lines 454, 462, 560 must be gone.
- Sibling Task `t2.5tm.kn.vk` (CLAUDE.md, CONTRIBUTING.md, docs/doc-writing-guide.md cleanup) is `done` — references at `CLAUDE.md` lines 14, 47, 109, 130-132; `CONTRIBUTING.md` lines 43, 78; `docs/doc-writing-guide.md` lines 62, 197, 258, 265 must be gone.
- The two scripts have no remaining Python callers post-Epics B/C/D (verified at research time: zero hits across `skills/bees-execute/SKILL.md` and `skills/bees-fix-issue/SKILL.md`; the three surviving helpers contain no docstring/comment references either).

## What Needs to Change

1. **Pre-deletion grep audit (mandatory gate).** Run `grep -rn "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md` from the repo root.
   - **Allowed remaining hits** (do not act on these; they stay):
     - `docs/prd.md` per-feature subsection content under `### Feature: Ephemeral-Agent Orchestration` (around lines 64, 75) — intentional historical record.
     - `docs/sdd.md` per-feature subsection content under `### Feature: Ephemeral-Agent Orchestration` (around lines 162, 193, 195, 203-208, 218, 220, 230, 234, 235) — intentional historical record.
     - `docs/sdd.md` non-feature sections (around lines 6, 8, 37, 38, 52, 56: Tech stack, External dependencies, Key components, Build process, Team orchestration in execution skills) — Epic `t1.5tm.fy` scope.
     - `README.md` (around lines 43-58, 114, 153: Required: enable Agent Teams + Display backend + skill table description + helper-script mention) — Epic `t1.5tm.fy` scope.
     - The two scripts themselves at `skills/bees-execute/scripts/force_clean_team.py` and `skills/bees-execute/scripts/check_agent_teams.py` — the deletion targets.
   - **Note on line numbers.** Line numbers above are approximate forecasts captured at planning time; documents may have shifted by the time this Subtask executes. Trust the actual `grep` output and the **location-class** allow-list (per-feature subsections of `docs/prd.md`/`docs/sdd.md`, `docs/sdd.md` non-feature sections, `README.md`, plus the two deletion targets themselves), not the literal line numbers.
   - **Disallowed** (any hit in these locations means a precondition was missed; do NOT proceed with the deletions): any hit in `skills/` other than the two scripts themselves, in `CLAUDE.md`, in `CONTRIBUTING.md`, or in `docs/doc-writing-guide.md`. If any disallowed hit appears, stop, surface the hit back to the orchestrator, and route the gap-fill back through the appropriate sibling Task before resuming.
2. **Delete `skills/bees-execute/scripts/force_clean_team.py`** via `git rm skills/bees-execute/scripts/force_clean_team.py`.
3. **Delete `skills/bees-execute/scripts/check_agent_teams.py`** via `git rm skills/bees-execute/scripts/check_agent_teams.py`.
4. **Post-deletion grep audit.** Re-run the same `grep -rn "force_clean_team\|check_agent_teams" skills/ docs/ README.md CONTRIBUTING.md CLAUDE.md` and confirm only the allowed remaining hits are present (per-feature PRD/SDD subsections + Epic-`t1.5tm.fy`-scoped non-feature sections in `docs/sdd.md` and `README.md`). The two scripts themselves should no longer be hit because they no longer exist on disk.
5. **Run the project lint** per `CLAUDE.md` `## Build Commands` `Lint`: `python -m pyflakes skills/*/scripts/*.py`. This sweeps the three surviving helpers (`bees-setup/scripts/file_list_resolver.py`, `bees-setup/scripts/detect_fast_path.py`, `bees-breakdown-epic/scripts/scoped_marker_resolver.py`) and confirms none of them imported anything from the deleted scripts. Should be a no-op given the deleted scripts had no Python callers, but the sweep is cheap and is the explicit Acceptance Criterion.

## Key Files

- `skills/bees-execute/scripts/force_clean_team.py` — DELETED via `git rm`.
- `skills/bees-execute/scripts/check_agent_teams.py` — DELETED via `git rm`.
- `skills/bees-setup/scripts/file_list_resolver.py` — read-only; lint sweep target (no edits).
- `skills/bees-setup/scripts/detect_fast_path.py` — read-only; lint sweep target (no edits).
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` — read-only; lint sweep target (no edits).

## Acceptance Criteria

- Pre-deletion grep shows zero disallowed hits (no hits in `skills/` other than the two deletion targets, no hits in `CLAUDE.md`, `CONTRIBUTING.md`, or `docs/doc-writing-guide.md`). If disallowed hits appear, the Subtask is paused and routed back to the appropriate sibling Task, not advanced to the deletion step.
- After `git rm`, both `skills/bees-execute/scripts/force_clean_team.py` and `skills/bees-execute/scripts/check_agent_teams.py` no longer exist on disk and are staged as deletions in `git status`.
- The three out-of-scope helpers — `skills/bees-setup/scripts/file_list_resolver.py`, `skills/bees-setup/scripts/detect_fast_path.py`, and `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` — still exist on disk and are unmodified.
- Post-deletion grep returns hits only in allowed locations: `docs/prd.md` per-feature subsection, `docs/sdd.md` per-feature subsection, `docs/sdd.md` Epic-`t1.5tm.fy`-scoped non-feature sections, and `README.md` Epic-`t1.5tm.fy`-scoped sections.
- `python -m pyflakes skills/*/scripts/*.py` exits cleanly (no warnings, no errors) over the three surviving scripts.
