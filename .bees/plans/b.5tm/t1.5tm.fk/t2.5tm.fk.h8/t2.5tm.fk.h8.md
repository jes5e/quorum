---
id: t2.5tm.fk.h8
type: t2
title: End-to-end demo + record probe results in Epic body
up_dependencies:
- t2.5tm.fk.8r
parent: t1.5tm.fk
children:
- t3.5tm.fk.h8.29
- t3.5tm.fk.h8.3f
- t3.5tm.fk.h8.gs
- t3.5tm.fk.h8.k9
created_at: '2026-05-04T14:22:22.879789'
status: done
schema_version: '0.1'
guid: 5tmfkh8umjvf2i51mk1mpvpii324vbba
reference_materials: null
---
## Context

PRD AC #6 of the Ephemeral-Agent Orchestration Bee calls for an end-to-end demo of the rewritten `bees-fix-issue`: `/bees-fix-issue <id>` runs end-to-end on a real (or stand-in) issue without manual prods, AND `all` mode runs against 2-3 dummy issues to validate batch-mode behavior. PRD AC #4 requires the optional-PM behavior to be preserved (simple skips PM, complex includes PM). Mirror Epic 8s Task `.cr`'s shape: probe the rewritten skill end-to-end and record the outcome in this Epic's body.

## What Needs to Change

### Demo 1 — single-issue mode

- Pick a real `open` Issue OR create a stand-in fixture Issue (e.g., a small README typo fix, a one-line config change, a docstring correction). The fixture Issue must be small enough to complete cleanly but large enough to exercise: at least one Agent dispatch (Engineer or Doc Writer), the Section 4 reviewer loop, and the Section 7 commit.
- Run `/bees-fix-issue <id>` from a fresh Claude Code session. Verify start-to-finish completion without manual prods. Intended interaction points (worktree-vs-branch isolation question, post-completion-review handling option) do NOT count as "manual prods" — they are designed user touchpoints.
- Capture observations matching each Epic AC:
  - **AC #1 (forbidden-token grep audit)**: confirm zero hits in the pre-demo SKILL.md for: `TeamCreate`, `TeamDelete`, `Agent Teams`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `force_clean_team`, `check_agent_teams`, `teammateMode`, `metadata.blocked_on`, `graduated escalation`.
  - **AC #2 (subagent type references)**: confirm skill prose names the seven subagent types literally.
  - **AC #3 (reconciliation loop / warm-cold / TaskList progress)**: observe issue-scoped TaskList naming (`engineer-<issue-id>`, etc.) and event-driven tick (no `/loop` / `ScheduleWakeup` / `CronCreate` invocations during the run).
  - **AC #4 (mode-selection UX preserved)**: single-issue mode runs cleanly; the model-choice question fires once at the start, the isolation question fires once.
  - **AC #5 (optional-PM behavior preserved)**: if the demo Issue is simple, observe NO `pm-<issue-id>` TaskList task; if complex, observe one. The complexity decision is orchestrator-direct (no Agent dispatched to make it).
  - **AC #6 (demo)**: end-to-end completion confirmed.
  - Scoped-marker PM dispatch wiring (when PM is dispatched): observe the orchestrator passing the helper path placeholder into the PM's dispatch prompt before invocation.
  - Conditional reviewer spawn (only-if-corresponding-implementer-ran): observe the reviewer set matches the implementer set.
  - Section 7 commit procedure: observe NO `.bees/` files in the staged set (per Task 4's no-`.bees/`-staging discipline).
  - Section 8 post-completion fresh-eyes sweep: observe a `general-purpose` Agent dispatched against the diff, returning either "no issues found" or a structured findings list.

### Demo 2 — `all` mode on 2-3 dummy Issues

- Create 2-3 `open` Issues (or pick existing ones). Each should be small enough to complete cleanly. Mix simple and complex if practical (e.g., one doc-only, one source-code-only, one mixed) so the optional-PM gate is exercised across issues.
- Run `/bees-fix-issue all` from a fresh Claude Code session. Verify:
  - Pre-validation step on the issue list (`bees show-ticket --ids <id1> <id2> ...`) before any fixes start.
  - Sequential fix loop, one commit per Issue.
  - Between Issues, no team-cleanup ceremony in the orchestrator's prose (cold dispatches already exited at each Section 7).
  - Final post-completion review covers the cumulative diff for all Issues fixed (the fresh-eyes generalist sees `git diff <pre-session-sha>..HEAD` covering all commits).

### Record results in this Epic's body

Mirror the structure Epic 8s Task `.cr` used. Add a `## Probe results` (or `## Demo results`) section to this Epic's (`t1.5tm.fk`) body containing:

- Outcome summary (PASS / DEFERRED / FAIL per demo).
- Probe environment provenance: host OS, Claude Code version, target repo, demo branch SHA, whether the demo branch was merged or kept separate, fixture Issue IDs created (with statuses at start and after demo).
- Captured observations per AC (matching the bullets above).
- Demo branch reference (e.g., `demo/<issue-id>` or whatever branch was used) so a future maintainer can re-walk the diff.
- Cross-OS coverage gap: POSIX-only on the demo host's machine; Windows PowerShell coverage is a documented gap and downstream-Epic obligation (Epic E `bees-setup` cleanup, Epic F doc cleanup) — same gap-acknowledgment pattern as Epic 8s.
- Implications for downstream Epics (E `bees-setup` and F doc-cleanup): if any cross-cutting behavior surfaced during the demo that those Epics must preserve, surface it here.

### Cleanup

Mark fixture Issues as `done` after the demo so they don't pollute future `/bees-fix-issue all` runs. If the demo fixture Issues were created specifically for the demo, note their IDs in the probe results so a future reader can verify the cleanup landed.

## Why

Demonstrates the rewrite works end-to-end and locks in the probe evidence the Epic's PRD calls for. Without this Task, AC #6 is not closed and Epic 8s' "test it on a real Bee" precedent isn't honored at issue scope.

## Cross-Task contracts

- This Task is the **integration test** for Tasks 1-5. It must execute LAST and must not start until 1-5 are landed (`up_dependencies` reflects this).
- **Probe artifacts** (stand-in Issues, dummy Issues for `all` mode) must be cleaned up or marked `done` after the demo so they don't pollute future `/bees-fix-issue all` runs in this repo.
- **No regression**: the demo must not surface any contradictions to the patterns established in Tasks 1-5. If a demo observation contradicts a sibling Task's contract, fix the contradiction here (re-dispatch the relevant implementer) rather than papering over it in the probe results.

## Acceptance Criteria

- Single-issue demo run completed start-to-finish without manual prods (other than intended interaction points).
- `all`-mode demo run completed on 2-3 dummy Issues without manual prods between Issues.
- This Epic's body has a `## Probe results` (or `## Demo results`) section with: outcome summary, environment provenance, per-AC observations, cross-OS gap acknowledgment, downstream-Epic implications.
- AC #1 (forbidden-token grep audit) verified zero-hits in the rewritten skill.
- AC #2-5 verified by demo observation.
- AC #6 verified by demo completion.
- Fixture Issues created for the demo are marked `done` after demo (or noted as out-of-scope cleanup for follow-up).
