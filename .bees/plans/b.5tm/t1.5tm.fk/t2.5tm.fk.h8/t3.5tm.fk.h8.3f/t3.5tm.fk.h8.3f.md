---
id: t3.5tm.fk.h8.3f
type: t3
title: Run single-issue demo and capture observations
up_dependencies:
- t3.5tm.fk.h8.29
down_dependencies:
- t3.5tm.fk.h8.gs
parent: t2.5tm.fk.h8
created_at: '2026-05-04T14:23:08.458263'
status: done
schema_version: '0.1'
guid: 5tmfkh83f6jmmcx9nfqwspkkhr4ca154
reference_materials: null
---
## Context

This Subtask is the second of four in Task 6 of Epic t1.5tm.fk. With Subtask 6.1's fixture set in hand, this Subtask runs `/quo-fix-issue <id>` (single-issue mode) end-to-end against one of the fixtures and captures the observations Subtask 6.4 will record in the Epic body.

The demo must validate each Epic AC (#1-#6) by direct observation:

- **AC #1 (forbidden-token grep audit)**: Subtask 5.2 already covered this; pre-demo confirmation of zero hits is sufficient.
- **AC #2 (subagent type references)**: confirm skill prose names the seven subagent types literally.
- **AC #3 (reconciliation loop / TaskList progress)**: observe issue-scoped TaskList naming (`engineer-<issue-id>`, `code-reviewer-<issue-id>`, etc.) and event-driven tick (no `/loop`, `ScheduleWakeup`, `CronCreate` invocations during the run).
- **AC #4 (mode-selection UX preserved)**: single-issue mode runs cleanly; the model-choice question fires once at the start; the isolation question fires once.
- **AC #5 (optional-PM behavior preserved)**: if the demo fixture is simple, observe NO `pm-<issue-id>` TaskList task; if complex, observe one. The complexity decision is orchestrator-direct.
- **AC #6 (demo)**: end-to-end completion confirmed.

Plus secondary observations:

- Scoped-marker PM dispatch wiring (when PM is dispatched): observe the orchestrator passing the helper path placeholder into the PM's dispatch prompt before invocation (Path B selection).
- Conditional reviewer spawn (only-if-corresponding-implementer-ran): observe the reviewer set matches the implementer set.
- Section 7 commit procedure: observe NO `.bees/` files in the staged set (per Task 4's no-`.bees/`-staging discipline).
- Section 8 post-completion fresh-eyes sweep: observe a `general-purpose` Agent dispatched against the diff, returning either "no issues found" or a structured findings list.

Intended interaction points (worktree-vs-branch isolation question, post-completion-review handling option) do NOT count as "manual prods" — they are designed user touchpoints.

## What Needs to Change

This is a research/observation Subtask. No source files are modified.

- From a fresh Claude Code session, invoke `/quo-fix-issue <id>` against one of the fixtures from Subtask 6.1.
- Verify start-to-finish completion without manual prods (other than intended interaction points). If any prod was required, record what and why.
- Capture observations into a notes artifact for Subtask 6.4 to consume:
  - **Forbidden-token zero-hits**: confirm a pre-demo grep across the rewritten SKILL.md returned zero hits for the nine prohibited tokens (this is Subtask 5.2's deliverable; cite it).
  - **Subagent type references**: confirm the seven subagent types are named literally in the skill prose.
  - **TaskList naming**: capture the TaskList task names observed during the run; verify they follow the `<role>-<issue-id>` convention.
  - **Event-driven tick**: confirm no `/loop`, `ScheduleWakeup`, or `CronCreate` invocations during the run.
  - **Mode-selection UX**: confirm the model-choice question fired once at the start and the isolation question fired once.
  - **Optional-PM behavior**: record whether a `pm-<issue-id>` TaskList task appeared, and whether that matches the fixture's predicted complexity.
  - **Scoped-marker dispatch (if PM dispatched)**: capture the helper-path placeholder the orchestrator passed in the PM dispatch prompt.
  - **Conditional reviewer spawn**: record which reviewer Agents were dispatched and confirm the set matches the implementer set.
  - **Commit procedure**: run `git show --stat <commit-sha>` (or equivalent) and confirm no `.bees/` files in the staged set.
  - **Post-completion fresh-eyes sweep**: record the result ("no issues found" or the findings list).
  - **Host platform**: POSIX or Windows.
- Do NOT modify the rewritten SKILL.md or any other source file. If a defect is found in the rewritten skill, surface it via the Subtask's status report rather than fixing it in-band.

## Key Files

- No source files modified. Observations captured into notes for Subtask 6.4.

## Acceptance Criteria

- `/quo-fix-issue <id>` ran end-to-end on a fixture without manual prods (or any prods captured with cause).
- Observations matching each Epic AC (#1-#6) are captured in the notes artifact:
  - Zero forbidden-token hits in pre-demo grep.
  - Seven subagent types referenced literally.
  - Issue-scoped TaskList naming observed.
  - No clock primitives invoked during the run.
  - Mode-selection UX preserved (one model question, one isolation question).
  - Optional-PM behavior matched the fixture's predicted complexity.
  - End-to-end completion confirmed.
- Secondary observations captured: Scoped-marker dispatch wiring (or "n/a — fixture is simple, no PM dispatched"); conditional reviewer spawn; no-`.bees/`-staging at commit time; post-completion fresh-eyes sweep result.
- Host platform recorded.
- No SKILL.md, helper script, README, SDD, or `agents/<role>.md` file was modified.
