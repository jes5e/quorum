---
id: t1.5tm.fk
type: t1
title: bees-fix-issue rewrite to ephemeral-Agent orchestration
up_dependencies:
- t1.5tm.27
down_dependencies:
- t1.5tm.kn
parent: b.5tm
children:
- t2.5tm.fk.4u
- t2.5tm.fk.jg
- t2.5tm.fk.2j
- t2.5tm.fk.gu
- t2.5tm.fk.8r
- t2.5tm.fk.h8
egg: null
created_at: '2026-05-03T01:16:44.272720'
status: in_progress
schema_version: '0.1'
guid: 5tmfkndcym1tjj2ccomrpjnzek71avam
---

## What this Epic delivers

Rewrite `skills/bees-fix-issue/SKILL.md` using the same reconciliation-loop + ephemeral-Agent pattern from Epic B, applied at issue scope.

## Acceptance criteria

1. `bees-fix-issue/SKILL.md` no longer contains any of: `TeamCreate`, `TeamDelete`, `Agent Teams`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `force_clean_team`, `check_agent_teams`, `teammateMode`, `metadata.blocked_on`, "graduated escalation".
2. Skill prose references the new subagent types by name.
3. Same reconciliation-loop, warm/cold, and TaskList-progress patterns as Epic B, scoped to one issue per cycle (vs. one Epic per cycle).
4. Single-issue, list, and `all` modes all work as before — no UX regression in mode selection.
5. Optional-PM behavior preserved: simple fixes skip PM, complex fixes include it. The decision logic moves from `bees-fix-issue` skill prose to a small orchestrator-direct check (no Agent needed for the decision itself).
6. **Demo:** `/bees-fix-issue <id>` runs end-to-end on a real (or stand-in) issue without manual prods. Run also for `all` mode against 2-3 dummy issues to validate batch-mode behavior.

## Codebase-green check

`bees-fix-issue` runs end-to-end. `bees-execute` (post-Epic B) and `bees-breakdown-epic` (still on Agent Teams pre-Epic D) unaffected. Other skills unaffected.

## Implementation notes

- **Issue-scoped agent names.** `engineer-<issue-id>` (e.g., `engineer-veq` for issue b.veq). Mirror Epic B's scoping pattern.
- **No `.bees/` state mutation in commits.** Current behavior preserved — only the issue's actual code/test/doc changes get committed; no ticket-status diff is staged. (This differs from `bees-execute` which DOES stage `.bees/` because Tasks/Subtasks have status changes.)
- **Branch-isolation prompt** (current lines ~83-100): preserved as orchestrator-direct AskUserQuestion — no Agent involved.
- **Pre-validation of issue IDs** (batch mode) preserved as orchestrator-direct — no Agent involved.

## Preserved cross-cutting work (b.mu9 / b.51d shipped 2026-05-03)

The current `bees-fix-issue/SKILL.md` PM-role block (around line 275 post-b.51d) carries a Scoped-marker note that documents the check as a **no-op for today's Issue schema** (Issues have no canonical parent-Plan-Bee field; only `up_dependencies` for blockers) but preserves the resolution path to the bundled helper at `<base>/../bees-breakdown-epic/scripts/scoped_marker_resolver.py` for a future `derived_from`-style schema extension. This documented-no-op behavior MUST survive the rewrite:

- The PM subagent (when invoked from the rewritten `bees-fix-issue` orchestrator) must continue to read spec docs in full as today.
- The note about WHY it's a no-op (Issue schema gap) and the path to the helper must be preserved so a future schema extension can wire in marker consumption without re-deriving the contract.
- Do NOT delete the marker-related prose just because it's "currently no-op" — its presence is load-bearing for future-proofing the cumulative-PRD scoping work b.mu9/b.51d shipped.

If Epic A authors `pm.md` faithfully (with both the active marker-check from `bees-execute` AND the no-op note from `bees-fix-issue` carried forward), Epic C mostly inherits this for free.

## Post-Step-8-review correction (b.51d polish, commit 9c97dd1, 2026-05-03)

The Step 8 post-completion reviewer of b.51d flagged that the `bees-fix-issue` Scoped-marker check should not stay a no-op. The fix landed in commit `9c97dd1` ("Polish b.51d Step 8 review items"). **It supersedes the cross-cutting note above.**

**Correction.** The `bees-fix-issue` PM marker check is no longer a documented no-op — it is **active via `up_dependencies` opportunistic discovery** (lines 275-305 of the post-9c97dd1 `bees-fix-issue/SKILL.md`). This active behavior MUST survive the rewrite:

- The PM iterates the Issue's `up_dependencies`, identifies `plans`-hive Bees via `bees execute-freeform-query` (filter `[type=bee, hive=plans]`), runs the helper against each Bee body, and scopes spec docs to the marker's feature subsection if found. **First-marker-wins** on tie-break; **best-effort fallback** to full-doc on missing/malformed/no-marker results.
- The **dual-use of `up_dependencies`** (blocker AND optional scope-context source) is deliberate and load-bearing. The rewrite must carry the explicit dual-use note so future maintainers don't conflate the two roles.
- The body-extraction-from-JSON-envelope instruction (`tickets[0].body` extracted from the query result, NOT the raw envelope written to the temp file) and the paired POSIX/PowerShell temp-file cleanup snippets are part of the procedure and must be preserved in any restructuring.
- The "future `derived_from` schema extension" framing in the original cross-cutting note above is **obsolete** — the schema extension shipped via the `up_dependencies` semantic broadening, not via a new typed field. The rewrite should not carry forward language anticipating a `derived_from` field.

**SDD impact.** SDD's `bees-fix-issue` Key components tag is now "Marker consumer (via up_dependencies-link)" — Epic F must preserve this and not regress to the old "no-op today" wording.

If Epic A authors `pm.md` faithfully against the post-9c97dd1 prose (per Epic A's matching correction), Epic C inherits the active marker check for free.

## Precondition reminder (from Epic A probe finding)

Epic A's `## Probe results` section documents that custom subagent types under `~/.claude/subagents/` (or `<repo>/.claude/subagents/`) are loaded by Claude Code only at session start — mid-session installs are NOT hot-reloaded. The rewritten skill's `## Preconditions` section MUST therefore include a hard-fail check that the subagent types this skill dispatches against (e.g. `engineer`, `pm`, `code-reviewer`, etc.) are registered in the running CC session. If any required subagent_type is missing, hard-fail with a clear message instructing the user to (a) verify the subagents/ install per `README.md` `## Install`, and (b) restart Claude Code. The README's `### After install` section already documents the restart step (commit `69890ea`); the precondition check is what surfaces the missing-restart symptom at run-time. Do NOT silently fall back to `general-purpose` or skip the dispatch — that masks the install gap.

When breaking this Epic down, treat this precondition as a Task-level deliverable in addition to whatever other preconditions the rewrite needs.

## Probe results

**Outcome summary**: PASS (static verification) + DEFERRED (live demo)

- AC #1-#5: PASS via static verification of `skills/bees-fix-issue/SKILL.md` prose at HEAD `4c7da3c` (Subtasks `.3f` + `.gs` static-AC pass; Subtask `.63` 9-token grep audit zero-hits in operative use).
- AC #6: DEFERRED — live end-to-end demo runs are scaffolded as user-executed runbooks (single-issue + all-mode); contents reproduced below so they survive `/tmp` cleanup. Demo will land on this branch (`bee/b.5tm`) once the user runs the runbooks from a fresh Claude Code session.

A sub-agent cannot spawn fresh Claude Code sessions, so Subtasks `.3f` and `.gs` did NOT execute live demos. Instead they statically verified every AC item that can be checked from prose and authored two paste-executable runbooks. This `## Probe results` section reflects that reality honestly: AC #1-#5 PASS via static verification, AC #6 DEFERRED to user-runbook-execution. Runbook contents and recommended fixture inputs are reproduced below.

### Probe environment provenance

- Host OS: macOS 26 / Darwin 25.3.0 (`Darwin Kernel Version 25.3.0: Wed Jan 28 20:54:46 PST 2026; root:xnu-12377.91.3~2/RELEASE_ARM64_T6000 arm64`).
- Claude Code version: `2.1.126`.
- Target repo: `bees-workflow` (this repo).
- Branch: `bee/b.5tm`.
- HEAD at probe authoring: `4c7da3c78a6c50a3f52a3312b74238da6068ffd5` (`4c7da3c`).
- Pre-session SHA for live demo: `bee/b.5tm` tip at time of user invocation (the runbooks compute this dynamically — see Section 1.3 of each runbook).
- Demo branch: `bee/b.5tm` itself (no separate `demo/` branch — Epic 8s used one because team-display-backend conflicts; this Epic does not need one).
- Fixture Issues:
  - `b.pn4` — newly authored by Subtask `.29`; simple; doc-only fix against `README.md`; predicted PM-skip path. Status at probe authoring: `open`. Will be `done` after `.3f` runbook execution.
  - `b.dkw` — pre-existing; complex; multi-file (`CLAUDE.md`, `README.md`, multiple `SKILL.md`); predicted PM-included path. Status at probe authoring: `open`. Will be `done` after `.gs` runbook execution.
  - `b.ehy` — pre-existing; complex; substrate-gated; predicted PM-included path. Status at probe authoring: `open`. Will be `done` after `.gs` runbook execution.

### Static AC verification

For each AC item, verdict + evidence (file:line in `skills/bees-fix-issue/SKILL.md`):

- **AC #1 (forbidden-token grep audit)**: PASS. Subtask `.63` confirmed zero hits across the 9-token forbidden list (`TeamCreate`, `TeamDelete`, "Agent Teams", `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `force_clean_team`, `check_agent_teams`, `teammateMode`, `metadata.blocked_on`, "graduated escalation") in **operative** use. Note: `.3f`'s static check found `/loop`, `ScheduleWakeup`, `CronCreate`, `SendMessage` token hits — these are in the "Anti-pattern: no clock primitives" callout (lines 181-183) and the divergence-callout citing Issue `b.x9w` (line 213). They are intentional negative references, not operative invocations; the audit's "operative-use only" framing (per Subtask `.63`) is what passes.
- **AC #2 (seven subagent types named literally)**: PASS. Lines 19, 31, 194, 237-242, 309-311 enumerate `engineer`, `test-writer`, `doc-writer`, `pm`, `code-reviewer`, `test-reviewer`, `doc-reviewer`.
- **AC #3a (TaskList naming convention prose)**: PASS. Lines 264-268 define `<role>-<issue-id>` issue-scoped naming (stripped suffix form, e.g., `engineer-veq` for `b.veq`); line 354 references the same convention at issue close-out.
- **AC #3b (no clock primitives)**: PASS. Lines 177-186 enumerate `/loop`, `ScheduleWakeup`, `CronCreate`, polling.
- **AC #4 (mode-selection UX)**: PASS. Section 1's model-choice (lines 69-77) and isolation (lines 79-101) `AskUserQuestion`s present and orchestrator-direct.
- **AC #5 (optional-PM gate)**: PASS. Section 3 Simple-vs-Complex classification (lines 138-151) preserved verbatim; PM dispatch gated at lines 207, 240, 246.
- **All-mode pre-validation**: PASS. Line 58 mandates `bees show-ticket --ids ...` upfront on the full list before any per-issue dispatch.
- **Sequential fix loop, one commit per issue**: PASS. Lines 52-54, 349, 367.
- **No inter-issue cleanup ceremony**: PASS. Line 59 explicit substrate-neutral wording; line 354 reinforces ("There is no Agent shutdown to perform — the per-issue cold dispatches established in Section 3 already complete-and-exit when each Agent returns").
- **Final post-completion review covers cumulative diff**: PASS. Lines 371, 377, 384-387.
- **Conditional reviewer spawn**: PASS. Lines 298-303 ("If the X Agent ran during this issue's implementation pass, dispatch the X-reviewer Agent now").
- **No-`.bees/`-staging**: PASS. Line 352 explicit divergence-from-`bees-execute` note.
- **Section 8 fresh-eyes sweep dispatch**: PASS. Line 379 `subagent_type=general-purpose, run_in_background=true`; lines 373, 406-409 anti-pattern callouts (no `/bees-code-review`, `/bees-doc-review`, `/bees-test-review` invocation in the dispatched Agent's prompt).
- **Scoped-marker PM dispatch wiring (Path B)**: PASS. Lines 270-292 (Issue ID + `up_dependencies` shape signaled, no Grandparent Bee available; resolver path passed as `<scoped-marker-resolver-path>` placeholder).

### Live demo (DEFERRED to user runbook execution)

Two runbooks have been authored and are reproduced verbatim below so they survive `/tmp` cleanup. The user runs each from a fresh Claude Code session in the host repo's working tree. (Outer fences below use four backticks because the inner runbook prose contains triple-backtick fences.)

#### Single-issue runbook (target: `b.pn4`)

````markdown
# bees-fix-issue single-issue mode demo runbook (fixture: `b.pn4`)

**Authored by:** Engineer for Subtask `t3.5tm.fk.h8.3f`
**Date:** 2026-05-04
**Companion:** `/tmp/bees-fix-issue-demo-fixtures.md` (fixture-set authored by Subtask `.29`)
**Source skill under demo:** rewritten `skills/bees-fix-issue/SKILL.md` (Epic `t1.5tm.fk`)
**Target fixture:** `b.pn4` — *Document bundled-helper-script location in README's 'Where docs live' section*
**Predicted classification:** **Simple** (doc-only, single file: `README.md`)
**Predicted dispatch shape:** Doc Writer only (no Engineer, no Test Writer); **NO** PM Agent dispatched
**Predicted reviewer shape:** Doc Reviewer only (no Code Reviewer, no Test Reviewer)

This runbook is paste-executable. The user runs each block from a **fresh Claude Code session** in the host repo's working tree, without dispatching agents to do the run on their behalf. The user fills in the observation checklist as the run progresses, then pastes the filled-in template back to the orchestrator.

---

## 0. Static AC verification (already done — for reference only)

Performed against `skills/bees-fix-issue/SKILL.md` at HEAD `4c7da3c`. Verdicts:

| AC | Verdict | Evidence |
|---|---|---|
| #1 — Forbidden tokens used only as anti-pattern callouts | PASS | `SKILL.md:181-183` enumerates `/loop`, `ScheduleWakeup`, `CronCreate` as anti-patterns; `:200`, `:213` reference `SendMessage` in the warm-Agent-intent contrast. No operative invocations. |
| #2 — Seven subagent types named literally | PASS | `engineer`, `test-writer`, `doc-writer`, `pm`, `code-reviewer`, `test-reviewer`, `doc-reviewer` enumerated literally at `SKILL.md:19`, `:31`, `:237-242`, `:309-311`. |
| #3a — Issue-scoped TaskList naming `<role>-<issue-id>` | PASS | `SKILL.md:264-268`. Stripped suffix form (`engineer-veq` for `b.veq`). |
| #3b — No clock primitives | PASS | `SKILL.md:177-186`. |
| #4 — Mode-selection UX preserved | PASS | Model-choice at `:69-77`; isolation at `:79-101`. |
| #5 — Optional-PM gate preserved | PASS | `:138-151` Simple-vs-Complex; `:207, 240, 244-246` gate PM on Complex. |
| Conditional reviewer spawn | PASS | `:298-303`. Doc-only corollary at `:303`. |
| No-`.bees/`-staging discipline | PASS | `:352` divergence-from-bees-execute note. |
| Section 8 fresh-eyes sweep | PASS | `:379` `subagent_type=general-purpose, run_in_background=true`. Anti-pattern at `:373`. |
| Scoped-marker PM dispatch wiring (Path B) | PASS | `:270-292`; Issue ID + `up_dependencies` shape signal Path B. |

**Issue-ID-suffix decision:** stripped (no `b.` prefix). For `b.pn4`: `doc-writer-pn4`, `doc-reviewer-pn4`.

---

## 1. Pre-flight checks

### 1.1 Confirm working tree state

POSIX (bash / zsh): `git -C /Users/jesseg/code/bees-workflow status --short`
Windows (PowerShell): `git -C C:\path	o\bees-workflow status --short`

**Expected:** working tree may have un-tracked `.bees/plans/...` files but no `M ` (modified) source files outside `.bees/`.

### 1.2 Confirm branch

POSIX: `git -C /Users/jesseg/code/bees-workflow branch --show-current`
PowerShell: `git -C C:\path	o\bees-workflow branch --show-current`

**Expected:** `bee/b.5tm`.

### 1.3 Record pre-session HEAD

POSIX: `git -C /Users/jesseg/code/bees-workflow rev-parse HEAD`
PowerShell: `git -C C:\path	o\bees-workflow rev-parse HEAD`

**Action:** copy SHA into observation template (`<pre-session-sha>`).

### 1.4 Confirm `b.pn4` is `open`

POSIX / PowerShell: `bees show-ticket --ids b.pn4`. **Expected:** `"ticket_status": "open"`.

### 1.5 Confirm a fresh Claude Code session

Quit any existing Claude Code session in this repo and start a new one before invoking the skill (so the precondition fast-fail can confirm the seven custom subagent types are registered via `claude agents` per `SKILL.md:31`).

---

## 2. Invocation

Type exactly: `/bees-fix-issue b.pn4`

The skill should:

1. Run preconditions.
2. Resolve to single-issue mode.
3. Ask the model-choice question.
4. Ask the isolation-strategy question (may be silent on feature branch + single-issue mode per `SKILL.md:88`).
5. Validate `b.pn4`.
6. Assess complexity → **Simple**.
7. Dispatch the Doc Writer Agent (only).
8. Section 4 review loop → Doc Reviewer Agent (only).
9. Section 6 spec-vs-code check → orchestrator-direct (load-bearing on simple-fix path per `SKILL.md:329`).
10. Section 7 commit (one commit, no `.bees/` files).
11. Section 8 fresh-eyes sweep → `general-purpose` Agent against the diff.
12. (Conditional) Post-completion-review handling question.

### 2.1 Model-choice prompt

Per `SKILL.md:69-77`. Recommended pick: **Opus**. Record verbatim text + selection (#A).

### 2.2 Isolation-strategy prompt

Per `SKILL.md:79-101`. On feature branch + single-issue mode, prompt MAY be silent (`SKILL.md:88`). If it fires, recommended pick: **Work on current branch**. Record (#B).

### 2.3 Post-completion-review handling prompt

Per `SKILL.md:422-428`. Fires only if Section 8 sweep flags issues. Recommended pick: **Skip**. Record (#C).
````

(continuation of single-issue runbook reproduction)

````markdown
## 3. Live observation checklist

**TaskList tasks expected:**

- [ ] `doc-writer-pn4` appears — `pending` → `in_progress` → `completed` (per `SKILL.md:206-207`).
- [ ] `engineer-pn4` does **NOT** appear (no source code touched).
- [ ] `test-writer-pn4` does **NOT** appear (no tests touched).
- [ ] `pm-pn4` does **NOT** appear — load-bearing for AC #5 (Simple → no PM per `SKILL.md:207, 240, 246`).
- [ ] `doc-reviewer-pn4` appears after `doc-writer-pn4` completes (per `SKILL.md:301`).
- [ ] `code-reviewer-pn4` does **NOT** appear (no Engineer ran per `SKILL.md:299`).
- [ ] `test-reviewer-pn4` does **NOT** appear (no Test Writer ran per `SKILL.md:300`).

**Naming sanity:** stripped suffix only (`doc-writer-pn4`, NOT `doc-writer-b.pn4`).

**Reconciliation-loop ticks:** no `/loop`, no `ScheduleWakeup`, no `CronCreate`, no polling pattern.

**Section 6 (spec-vs-code):** orchestrator-direct check. Reports either "Docs verified accurate — no changes needed" or "Updated <doc path> §X.Y to reflect <change>".

**Section 7 commit:** one commit, descriptive message mentioning `b.pn4`, NO `.bees/...` files in `git show --stat HEAD`, only `README.md` (and possibly formatter-touched siblings) appears.

**Section 8 sweep:** background Agent invocation `subagent_type=general-purpose, run_in_background=true` with `git diff <pre-session-sha>..HEAD` and Issue ID `b.pn4`. Agent does NOT invoke `/bees-code-review`, `/bees-doc-review`, `/bees-test-review` (per `SKILL.md:373` and `:406-409`).

---

## 4. Mid-run intervention guidance

Intended interaction points: model-choice, isolation (if it fires), post-completion-review handling (if sweep flags). Anything else is a "manual prod" — record verbatim in #D.

If the skill hard-fails or a dispatched Agent reports a non-recoverable error, record in #E and stop. Do NOT manually patch the SKILL.md in-band.

---

## 5. Post-run command set

### 5.1 One new commit
POSIX: `git -C /Users/jesseg/code/bees-workflow log --oneline <pre-session-sha>..HEAD`
PowerShell: `git -C C:\path	o\bees-workflow log --oneline <pre-session-sha>..HEAD`

### 5.2 Commit's staged set
POSIX: `git -C /Users/jesseg/code/bees-workflow show --stat HEAD`
PowerShell: `git -C C:\path	o\bees-workflow show --stat HEAD`

Load-bearing: only `README.md` (+ formatter-touched siblings); NO `.bees/...` paths anywhere; diff content adds 3-5 sentences under `## Where docs live`.

### 5.3 `b.pn4` now `done`
POSIX / PowerShell: `bees show-ticket --ids b.pn4`. **Expected:** `"ticket_status": "done"` per `SKILL.md:348`.

### 5.4 Note host platform: POSIX or Windows.

---

## 6. Observation template — paste back

Fill in each blank, then paste the entire block back to the orchestrator.

# bees-fix-issue single-issue demo (fixture b.pn4) — observations

- Run date / Host platform / Pre-session SHA / Post-run SHA / Branch / Source skill SHA at run time
- #A Model-choice prompt (fired? selected? verbatim?)
- #B Isolation-strategy prompt (fired? selected? verbatim if fired?)
- #C Post-completion-review handling prompt (fired? selected?)
- TaskList observations (each role-Issue task with checkbox + naming form observed)
- Reconciliation-loop observations (no `/loop`, no `ScheduleWakeup`, no `CronCreate`, no polling)
- Section 6 verdict
- Section 7 commit observations (`git show --stat HEAD` verbatim, NO `.bees/...`, commit message verbatim)
- Section 8 sweep observation (Agent dispatched as `general-purpose, run_in_background=true`, diff/issue-id correct, no `/bees-*-review` invocation, verdict)
- Status confirmation (`b.pn4` post-run = `done`)
- #D Manual prods, #E Errors
- Defects in rewritten skill (surface only — do not fix in-band)
- Per-AC verdict roll-up (#1-#6)
- Secondary-observation roll-up (scoped-marker PM dispatch wiring n/a here, conditional reviewer spawn, no-`.bees/`-staging, sweep result)

---

## 7. If the demo fails partway

Hard-fail at precondition gate: record error in #E, run `claude agents`, restart Claude Code or `/agents` to hot-reload, restart from Section 1.

Dispatched Agent fails: record in #E, do NOT manually fix in-band, stop and surface.

Defect observed but skill completes: complete runbook, record in defects list, do NOT modify SKILL.md (Subtask brief forbids).

---

## 8. Done

Once filled-in template is pasted back, runbook is done.
````

#### All-mode runbook (targets: `b.dkw`, `b.ehy`)

````markdown
# bees-fix-issue all-mode (or list-mode) demo runbook (fixtures: `b.dkw` + `b.ehy`)

**Authored by:** Engineer for Subtask `t3.5tm.fk.h8.gs`
**Date:** 2026-05-04
**Companions:**
- `/tmp/bees-fix-issue-demo-fixtures.md` (fixture-set authored by Subtask `.29`)
- `/tmp/bees-fix-issue-demo-runbook-single.md` (single-issue runbook authored by Subtask `.3f`)

**Source skill under demo:** rewritten `skills/bees-fix-issue/SKILL.md` (Epic `t1.5tm.fk`)
**Target fixtures:** `b.dkw` + `b.ehy` (both **Complex**, both expected to dispatch a per-issue PM)
**Predicted classifications:**
- `b.dkw` — Complex (multi-file: CLAUDE.md + README + multiple `SKILL.md`); PM dispatched
- `b.ehy` — Complex (PRD/SDD-described-behavior rule + non-obvious side effects); PM dispatched

**Predicted dispatch shape per issue:** Engineer + Doc Writer + PM (both fixtures); Test Writer iff Issue body calls for tests (likely no for either).
**Predicted reviewer shape per issue:** code-reviewer-`<id>` + doc-reviewer-`<id>` (test-reviewer skipped iff no Test Writer ran).

---

## 0. Static AC verification (already done — for reference only)

Performed against `skills/bees-fix-issue/SKILL.md` at HEAD on `bee/b.5tm`. All-mode-specific verdicts:

| All-mode AC | Verdict | Evidence |
|---|---|---|
| Pre-validation step on issue list before per-issue dispatch | PASS | `SKILL.md:58` — list-mode bullet: "before starting any fixes, `bees show-ticket --ids <id1> <id2> ...` on the full list." |
| Sequential fix loop, one commit per issue | PASS | `SKILL.md:52-54` (sequential per-issue), `:349` ("Create one git commit"), `:367` ("proceed to the next issue"). |
| No inter-issue cleanup ceremony in orchestrator prose | PASS | `SKILL.md:59` ("Between issues, no inter-issue cleanup ceremony is needed"); `:354` ("There is no Agent shutdown to perform"). Zero occurrences of `TeamDelete`, `force_clean_team`, "shutdown" in inter-issue context. |
| Final post-completion review covers cumulative diff | PASS | `SKILL.md:371` ("After all issues are fixed ... run a final fresh-context generalist sweep"); `:377` (`<pre-session-sha>` capture); `:384-387` (sweep prompt scope = cumulative diff against all issue bodies). |

---

## 1. Pre-flight checks

### 1.1 Working tree state
POSIX: `git -C /Users/jesseg/code/bees-workflow status --short`
PowerShell: `git -C C:\path	o\bees-workflow status --short`
Expected: untracked `.bees/plans/...` only; no modified source files outside `.bees/`. Demo will land **two new commits**.

### 1.2 Branch
POSIX: `git -C /Users/jesseg/code/bees-workflow branch --show-current`
PowerShell: `git -C C:\path	o\bees-workflow branch --show-current`
Expected: `bee/b.5tm`.

### 1.3 Pre-session HEAD
POSIX: `git -C /Users/jesseg/code/bees-workflow rev-parse HEAD`
PowerShell: `git -C C:\path	o\bees-workflow rev-parse HEAD`
Action: copy SHA into observation template.

### 1.4 Confirm `b.pn4` is `done` (gating: `.3f` runbook ran first)
POSIX / PowerShell: `bees show-ticket --ids b.pn4`. Expected: `"ticket_status": "done"`. If `open`, run the single-issue runbook first.

### 1.5 Confirm `b.dkw` and `b.ehy` are `open`
POSIX / PowerShell: `bees show-ticket --ids b.dkw b.ehy`. Expected: both `open`.

### 1.6 Confirm no unrelated `open` Issues exist (decides mode: `all` vs list)
POSIX / PowerShell — single literal command:
`bees execute-freeform-query --query-yaml 'stages: [type=issue, status=open]
report: [title, ticket_status]'`

- If exactly two open issues are listed and both are `b.dkw` and `b.ehy`: clean environment → use `all` mode (`/bees-fix-issue all`).
- If `b.x9w` is also open (deliberately excluded re-probe Issue): use list mode (`/bees-fix-issue b.dkw b.ehy`).
- If any other open issue appears: use list mode to keep blast radius scoped.

Record the chosen mode (and why) in the observation template (#G).

### 1.7 Fresh Claude Code session

Quit any existing Claude Code session in this repo and start a new one (subagent registration verification at session-start per `SKILL.md:31`).

---

## 2. Mode decision and invocation

- `all` mode (only `b.dkw` and `b.ehy` open): type `/bees-fix-issue all`
- List mode (other open Issues exist): type `/bees-fix-issue b.dkw b.ehy`

Both paths trigger the batch fix loop (`SKILL.md:52-54`) and no-inter-issue-cleanup-ceremony language (`:59`).

The skill should:

1. Run preconditions.
2. Resolve to `all` mode or list mode.
3. **Pre-validate** the issue list with `bees show-ticket --ids b.dkw b.ehy` (load-bearing — observe it).
4. Ask model-choice **once at the start**, NOT per-issue.
5. Ask isolation-strategy **once at the start**. Per `SKILL.md:89`, on feature branch in batch mode, the prompt SHOULD fire (different from single-issue mode).
6. For each issue (in order):
   1. Validate the Issue.
   2. Assess complexity → **Complex** for both fixtures.
   3. Dispatch Engineer + Doc Writer + PM (Test Writer iff body calls for tests).
   4. Section 4 review loop.
   5. Section 6 spec-vs-code (informational on Complex path — PM already covered it).
   6. Section 7 commit (one commit, no `.bees/` files).
7. After both land: Section 8 fresh-eyes sweep against `git diff <pre-session-sha>..HEAD` (cumulative — both commits in scope).
8. (Conditional) Post-completion-review handling question if sweep flags issues.

### 2.1 Model-choice prompt (session-level, ONCE at start)

Per `SKILL.md:69-77` and `:77` ("Ask once at the start, not per issue"). Recommended pick: **Opus**. Record (#A) — including verification it fired exactly once at start.

### 2.2 Isolation-strategy prompt (session-level, ONCE at start)

Per `SKILL.md:79-101`. On feature branch + batch mode (per `:89`), should fire. Should state: cwd, branch name (`bee/b.5tm`), "local only" semantics, issue count = 2. Recommended pick: **Work on current branch**. Record (#B).

### 2.3 Post-completion-review handling prompt (conditional)

Per `SKILL.md:422-428`. Fires only if Section 8 flags issues. Recommended pick: **Skip**.
````

(continuation of all-mode runbook reproduction)

````markdown
## 3. Live observation checklist

### 3.1 Pre-validation (load-bearing for all-mode AC)

- [ ] Before any per-issue dispatch, orchestrator reports running `bees show-ticket --ids b.dkw b.ehy` (or equivalent batch validation against open-issues list in `all` mode).
- [ ] Pre-validation prose mentions confirming each ID exists, is in `issues` hive, has `ticket_status=open` (per `SKILL.md:58`).

### 3.2 First issue — TaskList tasks

Record `<first-issue-id>` = ____________.

For first issue (one of `b.dkw` or `b.ehy`):

- [ ] `engineer-<first-issue-stripped>` appears, transitions cleanly.
- [ ] `doc-writer-<first-issue-stripped>` appears, transitions cleanly.
- [ ] `pm-<first-issue-stripped>` appears, transitions cleanly. **Load-bearing for AC #5 (Complex → PM required per `SKILL.md:207, 244-246`).**
- [ ] `test-writer-<first-issue-stripped>` appears iff Issue body calls for tests (probably NOT; record actual).
- [ ] `code-reviewer-<first-issue-stripped>` appears after Engineer (per `SKILL.md:299`).
- [ ] `doc-reviewer-<first-issue-stripped>` appears after Doc Writer (per `SKILL.md:301`).
- [ ] `test-reviewer-<first-issue-stripped>` appears iff Test Writer ran (per `SKILL.md:300`).

**Naming-convention sanity:** stripped suffix only (`engineer-dkw`, NOT `engineer-b.dkw`) per `SKILL.md:266`.

### 3.3 First issue — commit observation

- [ ] One new commit lands.
- [ ] NO `.bees/...` files in `git show --stat` (per `SKILL.md:352`).
- [ ] Files staged match Issue scope:
  - `b.dkw`: `CLAUDE.md`, `README.md`, multiple `skills/*/SKILL.md`.
  - `b.ehy`: `skills/bees-breakdown-epic/SKILL.md`.
- [ ] Commit message mentions `<first-issue-id>` or its title (per `SKILL.md:353`).

### 3.4 Between issues — NO cleanup ceremony (load-bearing for all-mode AC)

After first commit lands and before second issue starts:

- [ ] Orchestrator prose does NOT mention "shutting down agents", "disbanding team", "TeamDelete", or `force_clean_team`.
- [ ] No team-cleanup tool, helper, or skill invoked between commit and next dispatch.
- [ ] Transition prose is brief — typically Section 7 summary block followed directly by Section 2 validation for next issue (per `SKILL.md:59`).

### 3.5 Second issue — TaskList tasks

Record `<second-issue-id>` = ____________. Mirror Section 3.2 with second issue's stripped suffix.

### 3.6 Second issue — commit observation

- [ ] One new commit lands (cumulative session total: 2).
- [ ] NO `.bees/...` files in stat.
- [ ] Files staged match second issue's scope.
- [ ] Commit message mentions `<second-issue-id>`.

### 3.7 Reconciliation-loop ticks (across both issues)

- [ ] No `/loop`, no `ScheduleWakeup`, no `CronCreate`, no polling pattern.

### 3.8 Section 8 sweep (cumulative — load-bearing for all-mode AC)

After both commits land, Section 8 fires **once for the session** (not per-issue):

- [ ] Single new background Agent invocation: `subagent_type=general-purpose, run_in_background=true` (per `SKILL.md:379`).
- [ ] Agent prompt includes `git diff <pre-session-sha>..HEAD` — cumulative diff covering BOTH commits.
- [ ] Agent prompt includes BOTH issue IDs (e.g., `b.dkw b.ehy`).
- [ ] Agent does NOT invoke `/bees-code-review`, `/bees-doc-review`, `/bees-test-review` (per `SKILL.md:373`, `:406-409`).

### 3.9 Final summary

- [ ] Orchestrator outputs per-issue summary block (per `SKILL.md:357-365`) — two summary blocks total.
- [ ] After Section 8 returns, orchestrator presents synthesized findings (per `SKILL.md:418`).

---

## 4. Mid-run intervention guidance

Intended interaction points: model-choice (once), isolation (once on feature branch + batch mode), post-completion-review handling (once at end if sweep flagged). Anything else between issues is a "manual prod" — record verbatim in #D, especially re-prompts of model-choice, isolation, or "ready for next issue?".

---

## 5. Post-run command set

### 5.1 Two new commits, in order
POSIX: `git -C /Users/jesseg/code/bees-workflow log --oneline <pre-session-sha>..HEAD`
PowerShell: `git -C C:\path	o\bees-workflow log --oneline <pre-session-sha>..HEAD`
Expected: exactly two lines.

### 5.2 Each commit's staged set
POSIX: `git -C /Users/jesseg/code/bees-workflow show --stat <first-commit-sha>` and `git -C /Users/jesseg/code/bees-workflow show --stat <second-commit-sha>`
PowerShell: same with `C:\path	o\bees-workflow`.

Load-bearing: NO `.bees/...` paths in either stat; each commit scoped to its own issue's files (no cross-contamination).

### 5.3 Both fixtures `done`
POSIX / PowerShell: `bees show-ticket --ids b.dkw b.ehy`. Expected: both `done`.

### 5.4 Note host platform: POSIX or Windows.

---

## 6. Observation template — paste back

# bees-fix-issue all-mode demo (fixtures b.dkw + b.ehy) — observations

- Run date / Host platform / Pre-session SHA / Post-run SHA / Branch / Source skill SHA at run time
- #G Mode (`all` or list) and reason; open-Issues query output verbatim
- Pre-validation observation (orchestrator's prose verbatim)
- #A Model-choice prompt (fired once at start? selected? verbatim?)
- #B Isolation prompt (fired? once at start? stated cwd/branch/local-only/issue-count? selected? verbatim?)
- #C Post-completion-review handling prompt (fired? selected?)
- First issue observations (TaskList tasks, naming form, commit SHA, `git show --stat` verbatim, no `.bees/...`, commit message)
- Inter-issue boundary observation (NO shutdown/teamdelete/force_clean_team language; transition prose verbatim)
- Second issue observations (mirror first)
- Reconciliation-loop observations
- Section 8 sweep observation (single dispatch; cumulative diff; both issue IDs in prompt; no `/bees-*-review` invocation; verdict)
- Per-issue summary blocks verbatim
- Status confirmation (`b.dkw` and `b.ehy` post-run = `done`)
- #D Manual prods, #E Errors
- Defects in rewritten skill (surface only)
- Per-AC verdict roll-up
- Secondary observations (scoped-marker PM dispatch wiring, conditional reviewer spawn, no-`.bees/`-staging, sweep result)

---

## 7. If demo fails partway

Hard-fail at precondition gate: record in #E, `claude agents`, restart Claude Code, restart from Section 1.

First issue fails: record in #E, do NOT manually fix in-band, stop and surface. All-mode AC verdicts cannot be filled — record `BLOCKED`.

First completes but second fails: record in #E. Some AC verdicts (pre-validation, first-commit landing, inter-issue cleanup absence) still assessable. Cumulative-diff AC for Section 8 = `BLOCKED — Section 8 did not run`.

Defect observed but skill completes: complete runbook, record defect, do NOT modify SKILL.md.

---

## 8. Done

Once filled-in template is pasted back, runbook is done.
````

### Secondary observations

- **Scoped-marker dispatch wiring**: Static-verified at `SKILL.md:270-292` (Path B branch — Issue ID + `up_dependencies` shape, no Grandparent Bee). For the deferred live demo, this fires only on the Complex fixtures (`b.dkw`, `b.ehy`) where the PM Agent is dispatched; the Simple-fixture run (`b.pn4`) is n/a because no PM is dispatched.
- **Conditional reviewer spawn**: Static-verified at `SKILL.md:298-303` ("If the X Agent ran during this issue's implementation pass, dispatch the X-reviewer Agent now"). Live observation deferred — for `b.pn4` only `doc-reviewer-pn4` should fire; for `b.dkw` and `b.ehy` `code-reviewer-<id>` and `doc-reviewer-<id>` should fire (and `test-reviewer-<id>` only iff Test Writer ran for that Issue).
- **No-`.bees/`-staging at commit time**: Static-verified at `SKILL.md:352`. Live observation deferred — runbooks check `git show --stat HEAD` for absence of any `.bees/...` paths.
- **Post-completion fresh-eyes sweep results**: Deferred to live demo. The sweep dispatches as `subagent_type=general-purpose, run_in_background=true` (per `SKILL.md:379`) and must NOT invoke `/bees-code-review`, `/bees-doc-review`, or `/bees-test-review` (per `SKILL.md:373`, `:406-409`).
- **Batch-mode pre-validation step**: Static-verified at `SKILL.md:58`. Live observation deferred — runbook Section 3.1 instructs the user to confirm the orchestrator runs `bees show-ticket --ids b.dkw b.ehy` (or equivalent) before any per-issue dispatch.

### Fixture cleanup

Per the original Subtask body: "Mark fixture Issues as `done` after the demo so they don't pollute future `/bees-fix-issue all` runs." Since the LIVE demo is deferred to user runbook execution, fixture Issues are NOT being marked `done` at probe authoring time. Fixture Issues `b.pn4`, `b.dkw`, `b.ehy` remain `open` pending live runbook execution; the runbooks themselves close them via `/bees-fix-issue`'s normal flow (per `SKILL.md:348` — "Mark the issue's bees ticket `status=done` before committing").

### Cross-OS coverage gap (acknowledged)

POSIX-macOS only at probe authoring time (Darwin 25.3.0 arm64). Windows PowerShell coverage is a documented gap and downstream-Epic obligation (Epic E `bees-setup` cleanup; Epic F doc cleanup). Same gap-acknowledgment pattern as Epic 8s' Probe results section on Bee `b.kw3`. The runbooks themselves carry paired POSIX (bash / zsh) + Windows (PowerShell) command blocks per the cross-OS rule, so a Windows user can re-walk the same demo.

### Implications for downstream Epics

- **Epic E (`bees-setup` cleanup)**: may delete `check_agent_teams.py` and `force_clean_team.py` from disk now that no skill references them in prose. The static audit confirms zero references in `bees-fix-issue/SKILL.md`; verify same for `bees-execute/SKILL.md` (already shipped via Epic 8s) and `bees-breakdown-epic/SKILL.md` (Epic D scope) before deletion.
- **Epic F (doc cleanup)**: `docs/sdd.md` and `README.md` still carry residual `Agent Teams`, `force_clean_team`, `check_agent_teams`, `teammateMode`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `TeamCreate`, `TeamDelete` token references — explicit Epic F scope per `docs/sdd.md` line 235.
- **Future literal-token grep readers**: a "literal-token grep" on `bees-fix-issue/SKILL.md` may flag the negative-reference hits at lines 181-183 and 213 (e.g., `/loop`, `ScheduleWakeup`, `SendMessage`, `CronCreate`). The intent of the 9-token forbidden audit is "operative-use" only, not "literal-token absence" — record this distinction so a future audit doesn't trigger spurious blockers. (This is the only stylistic observation surfaced by `.3f`.)

### Cumulative-resource accounting

N/A. The substrate switch is a prose change, not a resource-acquisition change. No bounded resources introduced or compounded.

### Symmetric-change accounting

N/A. No new resource class introduced.

### Probe authoring fingerprint

This `## Probe results` section was authored by Subtask `t3.5tm.fk.h8.k9` on `bee/b.5tm` at HEAD `4c7da3c`, before the user's manual runbook execution. Live observations from the user's runbook execution will be appended as a follow-up `## Live demo observations` section in a separate commit if the user chooses to do so (or simply captured in the user's session log).
