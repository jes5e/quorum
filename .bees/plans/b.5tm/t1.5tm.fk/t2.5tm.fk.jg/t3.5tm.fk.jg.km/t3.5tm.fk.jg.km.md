---
id: t3.5tm.fk.jg.km
type: t3
title: 'Section 3: TaskList progress UI prose + issue-scoped naming + Scoped-marker
  PM dispatch wiring (Path B)'
up_dependencies:
- t3.5tm.fk.jg.fa
parent: t2.5tm.fk.jg
created_at: '2026-05-04T14:23:05.449528'
status: done
schema_version: '0.1'
guid: 5tmfkjgkmd9szxn814qu2yvjes1d7ab4
reference_materials: null
---
## Context

This Subtask is the second half of Task 2 in Epic t1.5tm.fk. Subtask 2.1 lands the Section 3 skeleton: reconciliation loop, per-issue cold dispatch, hub-and-spoke note, recursive-delegation finding, role pointers. This Subtask adds the two concrete pieces Tasks 3-4 will reference: the TaskList progress-UI prose with issue-scoped naming convention, and the Scoped-marker PM dispatch wiring (Path B — `up_dependencies`-based opportunistic discovery, best-effort fallback).

Carrying-forward architectural decisions from the canonical pattern:

- **TaskList as progress UI.** The orchestrator creates one TaskList task per concurrent Agent invocation; status transitions (`pending → in_progress → completed`) update as Agents start/finish. `metadata.activity` carries finer-grained progress.
- **Issue-scoped naming.** Per the scope difference noted in Subtask 2.1, the naming convention is `<role>-<issue-id>` (e.g., `engineer-veq` for issue `b.veq`, `pm-veq` for the PM on the same issue, `code-reviewer-veq` for the code reviewer in Section 4). This is THE canonical contract Tasks 3 (reviewer dispatches) and 4 (TaskList completion at issue close-out) will consume.
- **Scoped-marker check stays inside `agents/pm.md`.** The orchestrator's role is to pass the helper path placeholder `<scoped-marker-resolver-path>` in the PM dispatch prompt; pm.md owns Path B logic. Do NOT inline the marker grammar, parsing, or helper invocation in skill prose.

`agents/pm.md` distinguishes Path A (bees-execute, Grandparent Bee, hard-fail) from Path B (bees-fix-issue, `up_dependencies`-based, best-effort fallback) by the context the orchestrator provides in its dispatch prompt: an Issue ID and the issue's `up_dependencies` array, with no Grandparent Bee. The dispatch prompt's context must signal Path B selection.

## What Needs to Change

In `skills/bees-fix-issue/SKILL.md` Section 3 (building on Subtask 2.1's skeleton):

- Add a "TaskList as progress UI" subsection at the placeholder Subtask 2.1 left in the dispatch-contract prose. Establish:
  - One TaskList task per concurrent Agent invocation, transitioning `pending → in_progress → completed`.
  - The naming convention is **issue-scoped**: `<role>-<issue-id>` for every role (implementer, PM, reviewer). Examples: `engineer-veq`, `test-writer-veq`, `doc-writer-veq`, `pm-veq` (when PM dispatched for complex fixes), and (in Section 4) `code-reviewer-veq`, `test-reviewer-veq`, `doc-reviewer-veq`. Naming is deterministic and unambiguous across concurrent runs.
  - `metadata.activity` is the carrier for finer-grained progress (e.g., `"running narrow tests on package X"`, `"resolving Scoped-marker via up_dependencies iteration"`).
  - TaskList replaces the Agent Teams display backend; native Claude Code TaskList rendering is the visual UI; no separate setup required.
- Add Scoped-marker PM dispatch wiring to the per-issue PM dispatch prose:
  - When the orchestrator dispatches the PM Agent for a complex fix, the dispatch prompt must include the helper path placeholder `<scoped-marker-resolver-path>` resolved at runtime as `<this skill's base directory>/../bees-breakdown-epic/scripts/scoped_marker_resolver.py`. The path-resolution discipline matches the existing sibling-skill helper resolution pattern.
  - The dispatch prompt's context must signal **Path B** selection: it names an Issue ID and the issue's `up_dependencies` array, with no Grandparent Bee. (Path A is for `bees-execute`; Path B is for `bees-fix-issue`.) Do not inline the path-selection logic in skill prose — that lives in `agents/pm.md`.
  - The orchestrator does NOT inline the Scoped-marker grammar, the temp-file recipe for staging the Bee body, or the helper invocation. Those live in `agents/pm.md`.
  - Provide POSIX bash and Windows PowerShell snippets showing the resolved helper path the orchestrator embeds in the PM dispatch prompt, mirroring the rewritten `bees-execute/SKILL.md` Section 3's "Scoped-marker PM dispatch wiring" subsection.
- Update the dispatch-contract prose Subtask 2.1 wrote so its forward references to "the TaskList progress UI" and "the Scoped-marker dispatch wiring" now point at concrete subsections.
- Cross-Task contract artifact: the issue-scoped TaskList naming convention is the canonical reference for Tasks 3 (reviewer dispatches) and 4 (TaskList completion at issue close-out). Document it once in Section 3.

Do NOT edit `agents/pm.md`, do NOT edit any helper script, do NOT touch Sections 1, 2, 4-8.

## Key Files

- `skills/bees-fix-issue/SKILL.md` — Section 3, building on Subtask 2.1: add "TaskList as progress UI" subsection (with issue-scoped naming convention), add Scoped-marker PM dispatch wiring (path placeholder, sibling-skill resolution pattern, paired POSIX bash + Windows PowerShell snippets, Path B selection prose). Update forward references.

## Acceptance Criteria

- Section 3 contains a "TaskList as progress UI" subsection naming the per-concurrent-Agent task creation, the `pending → in_progress → completed` transitions, the `metadata.activity` finer-progress carrier, and the issue-scoped naming convention (`<role>-<issue-id>` for every role).
- The TaskList naming convention is stated once in Section 3 as the canonical reference Tasks 3 and 4 will consume.
- Section 3 contains Scoped-marker PM dispatch wiring stating the orchestrator passes `<scoped-marker-resolver-path>` resolved as `<this skill's base directory>/../bees-breakdown-epic/scripts/scoped_marker_resolver.py` in the PM dispatch prompt.
- The Scoped-marker prose explicitly names **Path B** (Issue ID + `up_dependencies`, no Grandparent Bee) as the path the dispatch prompt's context signals, and states that `agents/pm.md` owns the helper invocation, the temp-file recipe, and the marker grammar.
- The Scoped-marker subsection ships paired POSIX bash + Windows PowerShell labeled snippets showing the resolved helper path.
- The orchestrator does NOT inline the Scoped-marker grammar, the temp-file write recipe, or the helper invocation in Section 3.
- Subtask 2.1's placeholder reference to "the TaskList progress UI (see below)" now resolves to a concrete in-section subsection.
- Section 3 still contains zero occurrences of: `TeamCreate`, `TeamDelete`, `Agent Teams`, `metadata.blocked_on`, `blocked_on`, "graduated escalation", `force_clean_team`, "self-trigger", "task-scoped agent names".
- Project-neutrality, language-agnosticism, POSIX + Windows PowerShell pairing preserved.
- `agents/pm.md` is not modified.
- Sections 1, 2, 4-8 not modified.
