---
id: t2.5tm.8s.cr
type: t2
title: End-to-end demo + record probe results in Epic body
up_dependencies:
- t2.5tm.8s.9b
parent: t1.5tm.8s
children:
- t3.5tm.8s.cr.o6
- t3.5tm.8s.cr.c3
created_at: '2026-05-03T15:57:52.982233'
status: done
schema_version: '0.1'
guid: 5tm8scr52p4btaffw2tzsybr8q8rkpf5
reference_materials: null
---
## Context

Epic 8s AC #7 demands an end-to-end demo: `/bees-execute` runs end-to-end on a small real or stand-in Bee (1-2 Epics, 2-3 Tasks, 4-6 Subtasks total) without manual prods. Concurrent specialist work observable in TaskList (Engineer Subtask N+1 running while Test Writer Subtask N runs).

Epic 8s AC #6 demands recursive-delegation probe result recorded in Epic body. Per docs research during this breakdown: recursive delegation is NOT supported (sub-agents docs: "Subagents cannot spawn other subagents"). Records as docs-resolved finding with URL citation.

PRD AC #2 calls for a multi-Epic Bee (4+ Epics, 12+ Tasks, 60+ Subtasks). Task 7's small-demo size is below that threshold; the gap is acknowledged in writing in the probe-results record (either justified as smoke-test sufficient signal, or flagged as "AC#2 pending validation against a larger Bee" follow-up — full validation does not have to land in this Epic).

## What Needs to Change

- Run `/bees-execute` against a small real or stand-in Bee (1-2 Epics, 2-3 Tasks, 4-6 Subtasks total). Capture: (a) start-to-finish completion without user prods (worktree-isolation gate excepted); (b) at least one observation of concurrent specialist work in TaskList; (c) per-Task commit count matches Task count; (d) PM Scoped-marker dispatch fires correctly (orchestrator-passed helper path honored, OR "n/a — demo Bee has no Scoped-marker"); (e) post-completion fresh-eyes sweep returns a finding (or "no issues found"); (f) host platform (POSIX/Windows).
- Append `## Probe results` section to Epic 8s body (`t1.5tm.8s`) via `bees append-ticket-body` documenting:
  - **Recursive-delegation finding (docs-resolved)**: cite https://code.claude.com/docs/en/sub-agents and quote "Subagents cannot spawn other subagents". State that the rewritten skill ships flat orchestration with Epic-boundary context-clear discipline as the bound on context growth.
  - **End-to-end demo result**: PASS/FAIL with demo Bee ID, count of Epics/Tasks/Subtasks completed, Subtask 7.1's captured observations.
  - **AC#2 sizing-gap acknowledgment**: explicit note that the demo Bee size is below PRD AC#2's 4+ Epics / 12+ Tasks / 60+ Subtasks threshold; either justify as sufficient smoke-test signal OR flag as "AC#2 pending validation against a larger Bee" follow-up.
  - **Cross-OS coverage**: host platform; documented gap if untested platform.

## Why

The demo is the empirical proof that the rewrite works end-to-end. The probe-results record provides the audit trail Epic A's `## Probe results` pattern established as the canonical artifact for this Bee.

## Cross-Task Contract

Final Task in Epic 8s. Depends on Tasks 1-6 all having landed (the SKILL.md must be fully rewritten before the demo can run).

## Success Criteria

- `/bees-execute` runs end-to-end on a small Bee without manual prods (worktree-isolation gate excepted).
- Concurrent specialist work observable in TaskList during the demo.
- Per-Task commits land cleanly.
- PM Scoped-marker dispatch fires correctly during the demo (or "n/a — demo Bee has no Scoped-marker" recorded).
- `## Probe results` section appended to Epic 8s body documenting: recursive-delegation finding (docs-resolved with URL citation); end-to-end demo result; AC#2 sizing-gap acknowledgment; cross-OS coverage.
- Cross-OS coverage gap explicitly documented.
