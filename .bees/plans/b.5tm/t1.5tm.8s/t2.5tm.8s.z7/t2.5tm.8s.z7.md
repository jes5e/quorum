---
id: t2.5tm.8s.z7
type: t2
title: 'Section 4.1: replace team shutdown with TaskList completion (preserve commit procedure)'
up_dependencies:
- t2.5tm.8s.mp
down_dependencies:
- t2.5tm.8s.11
parent: t1.5tm.8s
children:
- t3.5tm.8s.z7.1n
egg: null
created_at: '2026-05-03T15:57:47.714898'
status: ready
schema_version: '0.1'
guid: 5tm8sz7cicdmh9heqd2q5ojfuzg7wb2c
---

## Context

Section 4.1 of `skills/bees-execute/SKILL.md` ("After Each Task") covers the per-Task commit procedure: mark Task done → run Format → git status → selective staging (Plans-hive path resolution + agent-reported files) → commit → "send shutdown requests, delete completed tasks" (Agent Teams ceremony) → continue. With Task 2's substrate switch, the shutdown ceremony is replaced by TaskList completed-tasks cleanup; the rest is preserved verbatim.

## What Needs to Change

- Replace "Send shutdown requests to all current agents. Delete completed tasks from the task list." with TaskList completed-tasks cleanup wording matching Task 2's TaskList naming convention.
- Update trailing prose ("continue to the next Task (spawning new agents with new task-scoped names on the same team)") to drop team / named-agent / on-the-same-team semantics; describe dispatching fresh ephemeral implementer Agents per Section 3's dispatch shape.
- Preserve verbatim: Format command lookup; git status; Plans-hive path resolution snippets (POSIX + PowerShell); selective `git add` (no blind `git add -A`); descriptive commit message; per-Task summary template.
- Remove from Section 4.1: "team", "TeamDelete", "force_clean_team.py", "task-scoped agent names" residue.
- Preserve the **NEVER push to remote — committing only** UX contract.
- Preserve the "Mark the Task as `status=done`. **Do this before committing** so the `.bees/` status changes are included in the commit." ordering invariant.

## Why

Per-Task commit procedure is one of the cross-cutting work-to-preserve items the rewrite must keep. Only the team-shutdown ceremony obsoletes; the commit procedure itself is unchanged.

## Cross-Task Contract

Assumes Task 2's TaskList naming convention is in place (`engineer-<task-id>-subtask-<id>`, etc.). The TaskList completed-tasks cleanup wording must reference Task 2's convention rather than introducing a new one or restating it.

This Task does NOT touch Section 4.2 (Task 4's scope) or Section 5 (Task 5's scope).

## Success Criteria

- Section 4.1 no longer contains: "Send shutdown requests", "TeamDelete", "force_clean_team", "task-scoped" (in the Agent-naming sense), "on the same team".
- Section 4.1 retains the Format → git status → selective stage → commit → summary procedure verbatim.
- Section 4.1's TaskList completed-tasks cleanup wording references Task 2's naming convention.
- Per-Task summary template is preserved verbatim.
- The `NEVER push to remote — committing only` UX contract is preserved verbatim.
- The "Mark the Task as `status=done`. Do this before committing..." ordering invariant is preserved verbatim.
- Lint passes.
