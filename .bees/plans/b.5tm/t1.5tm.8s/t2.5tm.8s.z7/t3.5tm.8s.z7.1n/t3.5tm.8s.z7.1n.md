---
id: t3.5tm.8s.z7.1n
type: t3
title: Replace team shutdown ceremony with TaskList completed-tasks cleanup in Section 4.1
parent: t2.5tm.8s.z7
egg: null
created_at: '2026-05-03T16:05:47.883698'
status: done
schema_version: '0.1'
guid: 5tm8sz71n6p2awbhzicai19xv45wgfdg
---

## Context

This Subtask handles Task 3 of Epic t1.5tm.8s as a single section-cleanup pass. Section 4.1 ("After Each Task") of `skills/bees-execute/SKILL.md` contains the per-Task commit procedure: mark Task done → run Format → git status → selective staging (Plans-hive path resolution + agent-reported files, no blind `git add -A`) → descriptive commit → "send shutdown requests to current agents, delete completed tasks from the task list" (Agent Teams ceremony) → continue.

Epic 8s's substrate switch obsoletes only the shutdown ceremony. Everything else in Section 4.1 — Format lookup against CLAUDE.md `## Build Commands`, the Plans-hive path resolution snippets in paired POSIX bash + Windows PowerShell form, the selective staging discipline, the descriptive commit message, the per-Task summary template, the `NEVER push to remote` UX contract, and the "mark Task done before committing so the `.bees/` status changes are included in the commit" ordering invariant — survives the substrate change unchanged and must be preserved verbatim.

The TaskList completed-tasks cleanup wording must reference the naming convention Subtask 2.2 establishes as the canonical Section 3 reference. This Subtask does not introduce a new naming convention; it consumes the one from Section 3.

## What Needs to Change

In `skills/bees-execute/SKILL.md` Section 4.1:

- Remove the line/passage "Send shutdown requests to all current agents. Delete completed tasks from the task list." Replace it with TaskList completed-tasks cleanup wording: at the per-Task boundary, the orchestrator marks the per-Task TaskList tasks (per the naming convention established in Section 3 by Subtask 2.2) as `completed` and clears them from the active set. There is no Agent shutdown to perform — the per-Subtask cold dispatches established in Section 3 already complete-and-exit when the Agent returns.
- Update the trailing prose ("continue to the next Task (spawning new agents with new task-scoped names on the same team)") to reflect the new substrate: the orchestrator advances to the next Task and dispatches fresh ephemeral implementer Agents per the Section 3 dispatch shape. No team, no task-scoped names, no on-the-same-team semantics.
- Remove every Section 4.1 occurrence of: "send shutdown requests", "delete completed tasks from the task list" (as the Agent Teams shutdown idiom), `TeamDelete`, `force_clean_team`, "task-scoped", "on the same team", "the team", "current agents" in the team-shutdown sense.
- Preserve verbatim: Format command lookup against CLAUDE.md `## Build Commands` (and rationale that workspace test suite is NOT re-run); `git status` instruction; Plans-hive path resolution snippets in paired POSIX bash + Windows PowerShell labeled blocks; "Do NOT blindly `git add -A`" admonition; descriptive commit message; per-Task summary template; `NEVER push to remote — committing only` UX contract; "Mark the Task as `status=done`. **Do this before committing**..." ordering invariant.
- Preserve project-neutrality, language-agnosticism, and POSIX + Windows PowerShell pairing throughout.

Do NOT edit Sections 1, 2, 3, 4.2, 5, 6-10. Do NOT edit any helper script. Do NOT touch any other SKILL.md.

## Key Files

- `skills/bees-execute/SKILL.md` — Section 4.1: replace shutdown-ceremony passage with TaskList completed-tasks cleanup wording referencing Section 3's naming convention; update next-Task transition prose to drop team/named-agent semantics; preserve Format → git status → selective stage → commit → summary procedure verbatim.

## Acceptance Criteria

- Section 4.1 contains zero occurrences of: "Send shutdown requests", `TeamDelete`, `force_clean_team`, "task-scoped", "on the same team".
- Section 4.1's commit procedure preserves verbatim: Format command lookup against CLAUDE.md, `git status` instruction, paired POSIX + PowerShell Plans-hive path resolution snippets, "Do NOT blindly `git add -A`" admonition, descriptive commit message instruction, per-Task summary template.
- The `NEVER push to remote — committing only` UX contract is preserved verbatim.
- The "Mark the Task as `status=done`. Do this before committing..." ordering invariant is preserved verbatim.
- Section 4.1's TaskList completed-tasks cleanup wording references the naming convention established in Section 3 by Subtask 2.2 (rather than introducing a new one or restating it).
- The next-Task transition prose describes dispatching fresh ephemeral implementer Agents per Section 3's dispatch shape, with no team / named-agent / on-the-same-team semantics.
- Project-neutrality, language-agnosticism, and POSIX + Windows PowerShell pairing rules preserved.
- Lint (`python -m pyflakes skills/*/scripts/*.py`) passes.
- Sections 1, 2, 3, 4.2, 5, 6-10 are not modified.
