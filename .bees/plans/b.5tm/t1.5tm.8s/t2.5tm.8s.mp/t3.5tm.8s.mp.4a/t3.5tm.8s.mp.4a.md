---
id: t3.5tm.8s.mp.4a
type: t3
title: 'Section 3: TaskList progress UI prose + Scoped-marker PM dispatch wiring'
up_dependencies:
- t3.5tm.8s.mp.4k
down_dependencies:
- t3.5tm.8s.mp.ip
- t3.5tm.8s.mp.yr
parent: t2.5tm.8s.mp
created_at: '2026-05-03T16:05:42.789540'
status: done
schema_version: '0.1'
guid: 5tm8smp4aib3iq7r2wkovwp65g1xtah6
reference_materials: null
---
## Context

This Subtask is the second half of Task 2 in Epic t1.5tm.8s. Subtask 2.1 lands the Section 3 skeleton: reconciliation loop, per-Subtask cold dispatch, hub-and-spoke note, recursive-delegation finding, role pointers. This Subtask adds the two concrete pieces Tasks 3-5 will reference: the TaskList progress-UI prose with naming convention, and the Scoped-marker PM dispatch wiring per Epic A's verified T3 contract.

Carrying-forward architectural decisions:

- **TaskList as progress UI.** The orchestrator creates one TaskList task per concurrent Agent invocation; status transitions (`pending → in_progress → completed`) update as Agents start/finish. `metadata.activity` carries finer-grained progress.
- **Scoped-marker check stays inside `agents/pm.md`.** Epic A lifted it. The orchestrator's role is to pass the helper path placeholder `<scoped-marker-resolver-path>` in the PM dispatch prompt; pm.md honors it verbatim per Epic A's verified T3 contract.

The TaskList naming convention established here is the cross-Task contract Tasks 3 and 5 will reference. Suggested shape: implementer Agents named `<role>-<task-id>-subtask-<id>`; PM Agents named `pm-<task-id>` for per-Task review; reviewer Agents at Bee scope named `<reviewer>-<bee-id>`. The Engineer authoring this Subtask may refine, but it must be deterministic.

## What Needs to Change

In `skills/bees-execute/SKILL.md` Section 3 (building on Subtask 2.1's skeleton):

- Add a "TaskList as progress UI" subsection at the placeholder Subtask 2.1 left in the dispatch-contract prose. Establish:
  - One TaskList task per concurrent Agent invocation, transitioning `pending → in_progress → completed`.
  - The naming convention covering implementer Agents at Subtask scope, PM Agents at Task scope, and reviewer Agents at Bee scope. Deterministic, unambiguous across concurrent invocations, consumable by Tasks 3 and 5.
  - `metadata.activity` as the carrier for finer-grained progress (e.g., `"running narrow tests on package X"`).
  - TaskList replaces the Agent Teams display backend; native Claude Code TaskList rendering is the visual UI; no separate setup required.
- Add Scoped-marker PM dispatch wiring to the per-Task PM dispatch prose:
  - When the orchestrator dispatches the PM Agent for a Task review, the dispatch prompt must include the helper path placeholder `<scoped-marker-resolver-path>` resolved at runtime as `<this skill's base directory>/../bees-breakdown-epic/scripts/scoped_marker_resolver.py`. The path-resolution discipline matches the existing sibling-skill helper resolution pattern.
  - The orchestrator does NOT inline the Scoped-marker grammar, the temp-file recipe, or the helper invocation. Those live in `agents/pm.md` per Epic A's lift.
  - Preserve POSIX + Windows PowerShell pairing for any shell snippet showing path resolution.
- Update the dispatch-contract prose Subtask 2.1 wrote so its forward references to "the TaskList progress UI" and "the Scoped-marker dispatch wiring" now point at concrete subsections.
- Cross-Task contract artifact: TaskList naming convention is the canonical reference for Tasks 3-5. Document it once in Section 3.

Do NOT edit `agents/pm.md`, do NOT edit any helper script, do NOT touch Sections 4.1, 4.2, 5, 6-10.

## Key Files

- `skills/bees-execute/SKILL.md` — Section 3, building on Subtask 2.1: add "TaskList as progress UI" subsection (with naming convention), add Scoped-marker PM dispatch wiring (path placeholder, sibling-skill resolution pattern, paired OS shell example if shown). Update forward references.

## Acceptance Criteria

- Section 3 contains a "TaskList as progress UI" subsection naming the per-concurrent-Agent task creation, the `pending → in_progress → completed` transitions, the `metadata.activity` finer-progress carrier, and a deterministic naming convention covering implementer Agents at Subtask scope, PM Agents at Task scope, and reviewer Agents at Bee scope.
- The TaskList naming convention is stated once in Section 3 as the canonical reference Tasks 3-5 will consume.
- Section 3 contains Scoped-marker PM dispatch wiring stating the orchestrator passes `<scoped-marker-resolver-path>` resolved as `<this skill's base directory>/../bees-breakdown-epic/scripts/scoped_marker_resolver.py` in the PM dispatch prompt, and stating that `agents/pm.md` owns the helper invocation, the temp-file recipe, and the marker grammar.
- The orchestrator does NOT inline the Scoped-marker grammar, the temp-file write recipe, or the helper invocation in Section 3.
- Subtask 2.1's placeholder reference to "the TaskList progress UI (see below)" now resolves to a concrete in-section subsection.
- Section 3 still contains zero occurrences of: `TeamCreate`, `TeamDelete`, `Agent Teams`, `metadata.blocked_on`, `blocked_on`, "graduated escalation", "Team-lead message-flow choreography", `force_clean_team`, "self-trigger", "task-scoped agent names".
- "Testing discipline" and "Running long commands" subsections remain preserved.
- Project-neutrality, language-agnosticism, POSIX + Windows PowerShell pairing preserved.
- Lint passes.
- `agents/pm.md` is not modified.
- Sections 4.1, 4.2, 5, 6-10 not modified.
