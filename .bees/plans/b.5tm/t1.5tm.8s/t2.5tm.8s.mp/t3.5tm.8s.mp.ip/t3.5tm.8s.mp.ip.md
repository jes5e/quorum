---
id: t3.5tm.8s.mp.ip
type: t3
title: 'doc-writing-guide: revise Project terminology Agent Teams entry'
up_dependencies:
- t3.5tm.8s.mp.4a
parent: t2.5tm.8s.mp
egg: null
created_at: '2026-05-03T16:05:44.241428'
status: done
schema_version: '0.1'
guid: 5tm8smpipbk4fn12jmev4gexfigz4vcp
---

## Context

Epic 8s rewrites `skills/bees-execute/SKILL.md` to dispatch fresh per-Subtask `Agent(subagent_type=<role>, run_in_background=true)` calls instead of using Agent Teams primitives. Once Section 3 of the SKILL.md is rewritten under Task 2 (Subtasks 2.1 + 2.2), the doc writing guide's `## Project terminology` "Agent Teams" entry becomes inaccurate — it currently asserts that `bees-execute` uses Agent Teams for parallel Engineer / Test Writer / Doc Writer / PM execution. `bees-fix-issue` still uses Agent Teams (out of scope for Epic 8s; pre-Epic-C state), so the entry must be **revised, not deleted**, to reflect that only `bees-fix-issue` retains Agent-Teams-based execution.

This Subtask ships in the same commit as Subtask 2.1 + 2.2 (Section 3 SKILL.md rewrite). Same-Task atomicity is the right default — locking the guide updates to the substrate-flip Task keeps the SKILL.md substrate prose and the guide's substrate description in sync at every Task-boundary commit.

## What Needs to Change

Update the "Agent Teams" entry under `## Project terminology` in `docs/doc-writing-guide.md` so it accurately describes which skills in the current repo state use Agent Teams as their orchestration substrate. The revised entry should reflect that `bees-fix-issue` uses Agent Teams; `bees-execute` does not. Avoid baking in implementation detail beyond what was already in the prior entry — this is a terminology entry, not a substrate explainer.

## Key Files

- `docs/doc-writing-guide.md` — `## Project terminology` "Agent Teams" bullet revision

## Acceptance Criteria

- The "Agent Teams" entry no longer claims `bees-execute` uses Agent Teams.
- The entry continues to accurately describe Agent Teams as Claude Code's experimental concurrent-agent feature gated by `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`.
- The entry continues to accurately describe `bees-fix-issue`'s use of Agent Teams.
- No other terminology entries are altered.
- The change ships in the same commit as the Section 3 SKILL.md rewrite (Subtasks 2.1 + 2.2) for Task 2, so the guide and the SKILL.md are consistent at every Task-boundary state.
