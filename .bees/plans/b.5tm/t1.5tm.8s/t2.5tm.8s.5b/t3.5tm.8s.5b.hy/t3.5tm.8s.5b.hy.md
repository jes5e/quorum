---
id: t3.5tm.8s.5b.hy
type: t3
title: Replace review Team formation with three concurrent ephemeral reviewer Agents
parent: t2.5tm.8s.5b
created_at: '2026-05-03T16:05:51.638301'
status: done
schema_version: '0.1'
guid: 5tm8s5bhy18poxsmdjghrcwhepruc5i4
reference_materials: null
---
## Context

This Subtask handles Task 5 of Epic t1.5tm.8s as a single section-rewrite pass. Section 5 ("Final Bee-level Code, Doc and Eng reviews") of `skills/bees-execute/SKILL.md` currently:

- Calls `TeamDelete` on the last Epic's team (with `force_clean_team.py` fallback) before forming a new review Team.
- Forms a new review Team with bee-scoped agent names (`code-reviewer-<bee-id>`, etc.).
- Conditionally spawns reviewer roles (Code Reviewer if Engineer was used; Test Reviewer if Test Writer was used; Doc Reviewer if Doc Writer was used).
- Carries inline Instructions blocks for Code Reviewer / Test Reviewer / Doc Reviewer (each "Invoke the /bees-<role>-review skill").
- Runs an iteration loop: "If feedback requires action, reform or re-use the first team to do the work" with optional-PM-on-iteration logic.

Epic 8s's substrate switch replaces team formation with three concurrent `Agent(subagent_type=<reviewer>, run_in_background=true)` invocations against the `code-reviewer`, `test-reviewer`, `doc-reviewer` subagent types Epic A landed in `agents/`. The conditional spawn logic is preserved (only spawn reviewers whose corresponding implementer was dispatched). The iteration loop is preserved with the dispatch shape updated. Optional-PM-on-iteration and ignore-feedback UX contract are preserved verbatim.

The TaskList naming convention for reviewers consumes Section 3's bee-scoped pattern as established in Subtask 2.2 — `code-reviewer-<bee-id>`, `test-reviewer-<bee-id>`, `doc-reviewer-<bee-id>`. This Subtask references it rather than restating it.

The reviewer role-Instructions blocks are owned by `agents/<role>-reviewer.md` per Epic A's lift. Section 5 replaces them with one-line summaries plus pointers to the agent definition files.

## What Needs to Change

In `skills/bees-execute/SKILL.md` Section 5:

- Remove the opening `TeamDelete` call on the last Epic's team and its `force_clean_team.py` fallback chain.
- Replace "Form a new review Team (e.g., `bee-review-<bee-id>`) to check their work. Use bee-scoped agent names ..." with: "Dispatch three concurrent ephemeral reviewer Agents per Section 3's dispatch shape, one per reviewer role: `Agent(subagent_type=\"code-reviewer\", run_in_background=true)`, `Agent(subagent_type=\"test-reviewer\", run_in_background=true)`, `Agent(subagent_type=\"doc-reviewer\", run_in_background=true)`. Track each via a TaskList task per Section 3's naming convention (bee-scoped form: `code-reviewer-<bee-id>`, `test-reviewer-<bee-id>`, `doc-reviewer-<bee-id>`)."
- Reframe the conditional spawn logic to substrate-neutral wording: "If Engineer Agents were dispatched during the Epic loop, dispatch the code-reviewer Agent now." Same for Test Writer / test-reviewer and Doc Writer / doc-reviewer.
- Replace the inline Instructions blocks for Code Reviewer / Test Reviewer / Doc Reviewer with one-line role summaries plus pointers to `agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md`.
- Update the iteration loop: "Reform or re-use the first team to do the work" → "If feedback requires action, dispatch fresh ephemeral implementer Agents per Section 3's dispatch shape (Engineer / Test Writer / Doc Writer / PM as needed). Stay in delegate mode."
- Preserve verbatim: optional-PM-on-iteration logic ("If the feedback was minor enough, you may choose to **NOT** spawn the Product Manager on this iteration"); the "**IMPORTANT** Stay in delegate mode and do not do the work yourself." UX contract; ignore-feedback UX contract ("Note: This could create an infinite loop so you may ignore feedback so long as you present it in Final Review."); "Get the feedback, and make a judgement call about whether that work must be done" framing.
- Remove every Section 5 occurrence of: `TeamCreate`, `TeamDelete`, `force_clean_team`, "team-scoped" (Agent-naming sense), "Form a new review Team", "Reform the implementation team", "the just-disbanded pattern", "bee-scoped agent names" (the convention is preserved as a TaskList naming pattern referenced from Section 3, not as Agent names).
- Preserve project-neutrality, language-agnosticism, and POSIX + Windows PowerShell pairing.

Do NOT edit `agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md`. Do NOT edit Sections 1, 2, 3, 4.1, 4.2, 6-10. Do NOT edit any helper script.

## Key Files

- `skills/bees-execute/SKILL.md` — Section 5: remove `TeamDelete` opener and team-formation paragraph; replace with three concurrent ephemeral reviewer Agent dispatches per Section 3's dispatch shape; preserve conditional spawn logic with updated dispatch verb; replace reviewer role-Instructions blocks with one-line summaries and pointers to `agents/<role>-reviewer.md`; preserve iteration loop, optional-PM-on-iteration, delegate-mode UX contract, and ignore-feedback UX contract.

## Acceptance Criteria

- Section 5 contains zero occurrences of: `TeamCreate`, `TeamDelete`, `force_clean_team`, "team-scoped" (Agent-naming sense), "Form a new review Team", "Reform the implementation team", "the just-disbanded pattern".
- Section 5 dispatches three concurrent ephemeral reviewer Agents using subagent type names `code-reviewer`, `test-reviewer`, `doc-reviewer` per Section 3's dispatch shape with `run_in_background=true`.
- Each reviewer dispatch is tracked via a TaskList task following Section 3's naming convention in its bee-scoped form (`code-reviewer-<bee-id>`, `test-reviewer-<bee-id>`, `doc-reviewer-<bee-id>`).
- The conditional spawn logic ("If Engineer Agents were dispatched during the Epic loop, dispatch the code-reviewer Agent now"; same for Test Writer / test-reviewer and Doc Writer / doc-reviewer) is preserved with the substrate-neutral dispatch verb.
- The inline Instructions blocks for Code Reviewer / Test Reviewer / Doc Reviewer are replaced with one-line role summaries plus pointers to `agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md`. No reviewer role prose duplicated into Section 5.
- The iteration loop's dispatch wording is "If feedback requires action, dispatch fresh ephemeral implementer Agents per Section 3's dispatch shape (Engineer / Test Writer / Doc Writer / PM as needed). Stay in delegate mode."
- Optional-PM-on-iteration logic preserved verbatim.
- "**IMPORTANT** Stay in delegate mode and do not do the work yourself." UX contract preserved verbatim.
- "Note: This could create an infinite loop so you may ignore feedback so long as you present it in Final Review." UX contract preserved verbatim.
- Project-neutrality, language-agnosticism, POSIX + Windows PowerShell pairing rules preserved.
- Lint passes.
- `agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md` not modified.
- Sections 1, 2, 3, 4.1, 4.2, 6-10 not modified.
