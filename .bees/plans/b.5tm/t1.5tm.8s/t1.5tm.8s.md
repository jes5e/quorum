---
id: t1.5tm.8s
type: t1
title: bees-execute rewrite to ephemeral-Agent orchestration
up_dependencies:
- t1.5tm.27
down_dependencies:
- t1.5tm.kn
parent: b.5tm
children:
- t2.5tm.8s.6z
- t2.5tm.8s.mp
- t2.5tm.8s.z7
- t2.5tm.8s.11
- t2.5tm.8s.5b
- t2.5tm.8s.9b
- t2.5tm.8s.cr
egg: null
created_at: '2026-05-03T01:16:43.450904'
status: in_progress
schema_version: '0.1'
guid: 5tm8s5x9fzgab7hxmwk9nwtmqtrrxhpy
---

## What this Epic delivers

Rewrite `skills/bees-execute/SKILL.md` to use the reconciliation-loop pattern with background `Agent` invocations against the subagent types defined in Epic A. Drop all Agent Teams primitives, the message-flow choreography, the `blocked_on` signal, the graduated-escalation ladder, and the helper-script references. Probe recursive delegation early; pick flat or nested orchestration based on the result.

## Acceptance criteria

1. `bees-execute/SKILL.md` no longer contains any of: `TeamCreate`, `TeamDelete`, `Agent Teams`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `force_clean_team`, `check_agent_teams`, `teammateMode`, `metadata.blocked_on`, "graduated escalation", "Team-lead message-flow choreography".
2. Skill prose references the new subagent types by name (`subagent_type: "engineer"`, etc.) rather than inlining role instructions.
3. The reconciliation-loop tick is documented: read state (bees ticket query + TaskList) → reconcile (dispatch ready, persist completed, advance) → yield. Explicit guidance: no `/loop`, no `ScheduleWakeup`, no `CronCreate`, no polling.
4. Warm/cold cold-start guidance documented:
   - Engineer + Test Writer use named Agents + SendMessage across Subtasks within a Task
   - PM, Code Reviewer, Doc Reviewer, Test Reviewer always fresh
   - Doc Writer fresh by default with opt-in warm for multi-doc-Subtask Tasks
   - Task scope end: orchestrator sends "complete" SendMessage to each warm Agent so it returns and exits
5. TaskList-as-progress-UI documented: orchestrator creates one TaskList task per concurrent Agent invocation; status transitions (`pending → in_progress → completed`) and `metadata.activity` are updated as Agents start/finish.
6. **Recursive-delegation probe completed and recorded.** Skill prose includes either:
   - (a) An Epic-level sub-orchestrator pattern that fires `Agent(subagent_type=epic-orchestrator)` for each Epic, or
   - (b) Flat orchestration with documented Epic-boundary context-clear discipline (the existing pattern, kept).
   Probe result is recorded in this Epic's done-status notes.
7. **Demo:** `/bees-execute` runs end-to-end on a small real or stand-in Bee (1-2 Epics, 2-3 Tasks, 4-6 Subtasks total) and completes without manual prods. Concurrent specialist work is observable in the TaskList UI (e.g., Engineer Subtask 1 + Test Writer Subtask 0 running in parallel).

## Codebase-green check

`bees-execute` runs end-to-end on a Bee. `bees-fix-issue` and `bees-breakdown-epic` still work (still use Agent Teams) but are unaffected by this Epic. `bees-setup` still configures Agent Teams (Epic E hasn't shipped yet) — that's fine; the precondition is set, `bees-execute` no longer needs it but its sibling skills do.

## Implementation notes

- **Probe shape.** The recursive-delegation probe is a real `Agent` invocation that internally calls `Agent` again — minimal test, measure success or specific failure mode. If subagents cannot spawn further subagents, fall back to flat. PRD/SDD already acknowledge both outcomes.
- **Hub-and-spoke preserved.** Workers do not communicate with each other. Engineer's commits become Test Writer's input via git diff; Test Writer's output becomes PM's input via diff + ticket state. No SendMessage between workers.
- **Cross-Task / cross-Epic interaction checks.** PM responsibility (per current SKILL.md lines ~377-382). Move into the PM subagent's body in Epic A; invoke from this skill's orchestrator at the appropriate tick.
- **Time-bounded review iteration** (~10 work items / ~5 turns short-circuit). Preserve in the PM subagent body.
- **Worktree isolation prompt** (current lines ~110-122) preserved as orchestrator-direct AskUserQuestion — no Agent involved.
- **One commit per Task** (current Section 4.1) preserved — orchestrator-direct, no Agent.
- **Inter-Epic interaction checkpoint** (current Section 4.2 ~lines 442-454) preserved — orchestrator-direct, no Agent.

## Preserved cross-cutting work (b.mu9 / b.51d shipped 2026-05-03)

The current `bees-execute/SKILL.md` Step 4 PM section (around line 359 post-b.51d) carries a Scoped-marker check that runs `<base>/../bees-breakdown-epic/scripts/scoped_marker_resolver.py` against the Grandparent Bee body and scopes spec-compare to a single `### Feature: <title>` subsection when the marker is present. This functionality is orthogonal to the Agent Teams substrate and MUST survive the rewrite:

- The reconciliation-loop's PM-dispatch logic (or the PM subagent's body authored in Epic A) must invoke the marker check before running per-Task spec-compare review.
- The OS-paired helper invocation (POSIX bash + Windows PowerShell) must be preserved.
- The error-handling contract — helper exits 2 on malformed marker / missing doc / missing heading; orchestrator must surface the error and stop, **NOT** silent-fallback to the full doc — must be preserved.
- The reference to `docs/doc-writing-guide.md` `## The Scoped-marker contract` as the source of truth for marker grammar must be preserved.

If Epic A lifts the PM Instructions block into `pm.md` faithfully (with the marker-detection prose included), Epic B's rewrite of `bees-execute/SKILL.md` mostly just needs to ensure the orchestrator dispatches the PM Agent at the right ticks — the marker logic lives inside the subagent definition, not in the orchestrator's prose.

If recursive delegation is used (per this Epic's probe), the Epic-level sub-orchestrator Agent must still defer the marker check to per-Task PM Agents — the scoping decision is consulted at every spec-compare, not just once at Bee start.

## Precondition reminder (from Epic A probe finding)

Epic A's `## Probe results` section documents that custom subagent types under `~/.claude/subagents/` (or `<repo>/.claude/subagents/`) are loaded by Claude Code only at session start — mid-session installs are NOT hot-reloaded. The rewritten skill's `## Preconditions` section MUST therefore include a hard-fail check that the subagent types this skill dispatches against (e.g. `engineer`, `pm`, `code-reviewer`, etc.) are registered in the running CC session. If any required subagent_type is missing, hard-fail with a clear message instructing the user to (a) verify the subagents/ install per `README.md` `## Install`, and (b) restart Claude Code. The README's `### After install` section already documents the restart step (commit `69890ea`); the precondition check is what surfaces the missing-restart symptom at run-time. Do NOT silently fall back to `general-purpose` or skip the dispatch — that masks the install gap.

When breaking this Epic down, treat this precondition as a Task-level deliverable in addition to whatever other preconditions the rewrite needs.

## Probe results

Probes for Epic 8s acceptance criteria #6 (recursive-delegation finding) and #7 (end-to-end demo) ran post-Task-6 on 2026-05-04 against the rewritten `skills/bees-execute/SKILL.md`.

### Recursive-delegation finding (docs-resolved)

**Status: docs-resolved, no probe required.** Per CONTRIBUTING.md `## Verifying external-system contracts`, authoritative-source research is preferred over empirical probing when the docs are decisive. Claude Code's official sub-agents documentation states verbatim: **"Subagents cannot spawn other subagents."** Source: https://code.claude.com/docs/en/sub-agents.

The rewritten `skills/bees-execute/SKILL.md` Section 3 ships **flat orchestration** as the consequence: a single top-level orchestrator dispatches per-Subtask, per-Task, and per-Bee Agents directly, with no nested sub-orchestrators. The bound on flat-orchestration context growth is documented in Section 4.2's **Epic-boundary context-clear discipline** paragraph (~25-30% of the 1M context window per Epic, per SDD line 197), with a forward-pointer from Section 3's "Recursive delegation: not supported" note.

### End-to-end demo result: **PASS**

Demo Bee: **`b.kw3`** — "Demo: CHANGELOG.md skeleton with Epic 8s entry" — synthetic stand-in created post-Task-6 to validate the rewrite end-to-end.

**Structure completed:** 1 Epic (`t1.kw3.dy`), 2 Tasks (`t2.kw3.dy.iu`, `t2.kw3.dy.je`), 4 Subtasks (`t3.kw3.dy.iu.h1`, `t3.kw3.dy.iu.9a`, `t3.kw3.dy.je.ne`, `t3.kw3.dy.je.nv`).

**Run host:** POSIX (macOS Darwin 25.3.0).

**Demo branch:** `demo/b.kw3` branched from `bee/b.5tm` HEAD (commit `68235db`) prior to demo start. Three commits landed on the demo branch (one per Task plus the Bee close-out):
- `5d889e3` Add CHANGELOG.md skeleton with Unreleased section (Task 1)
- `b715afc` Add Epic 8s entry to CHANGELOG Unreleased section (Task 2)
- `deb0b20` Close out Bee b.kw3 and Epic t1.kw3.dy

**Captured observations:**

- **Start-to-finish completion: PASS, no manual prods.** The orchestration ran from `/bees-execute b.kw3` invocation through Section 8 mark-Bee-complete without manual prods beyond the worktree-vs-branch isolation question and the Final Output AC sign-off / Bee-close confirmation (both intended interaction points).

- **Concurrent specialist work in TaskList: PASS, two sightings.**
  - **Sighting 1 (Task 1, two Engineers in parallel):** TaskList showed `engineer-t3.kw3.dy.iu.h1` and `engineer-t3.kw3.dy.iu.9a` both at `in_progress` simultaneously while CHANGELOG.md was being skeleton'd. Orchestrator reported "*Two Engineer Agents in flight, both in_progress — concurrent dispatches against CHANGELOG.md for Task t2.kw3.dy.iu. Yielding for completion notifications.*" — verbatim phrasing matching Section 3's reconciliation-loop tick.
  - **Sighting 2 (Section 5, two Bee-level reviewers in parallel):** `code-reviewer-b.kw3` and `doc-reviewer-b.kw3` both at `in_progress` simultaneously during the Bee-level review. `test-reviewer` correctly skipped per Section 5's conditional-spawn logic (no Test Writer was dispatched during the Epic loop).
  - **Note (serial-dispatch adaptation in Task 2):** the orchestrator chose to dispatch Subtasks `t3.kw3.dy.je.ne` and `t3.kw3.dy.je.nv` serially rather than concurrently because Subtask `nv`'s body explicitly references the bullet `ne` creates. Reasonable inferred-dependency adaptation, not a defect — the per-Subtask cold dispatch shape was still exercised, just sequentially. Per-Subtask completion notifications continued to drive the reconciliation loop tick correctly.

- **Per-Task commits: PASS.** Exactly 2 Task commits landed on `demo/b.kw3` (`5d889e3` for Task 1, `b715afc` for Task 2), matching the Task count. The Section 4.1 commit procedure was honored end-to-end: Format command run (no-op for this repo), `git status` inspected, **selective staging discipline working** (orchestrator explicitly noted *"I'll stage only b.kw3-related files plus CHANGELOG.md, leaving .claude/ and the b.5tm change untouched"* — correctly identifying the pre-existing in-progress `t2.5tm.8s.cr.md` flip as unrelated to the demo Bee), descriptive commit message emitted, per-Task summary template printed verbatim.

- **PM Scoped-marker dispatch: PASS, helper-path verification observed.** During Task 1's PM dispatch, the orchestrator ran `test -f /Users/jesseg/.claude/skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` BEFORE invoking `pm(PM: t2.kw3.dy.iu)`. That confirms Section 3's `<scoped-marker-resolver-path>` placeholder wiring fires as designed (the orchestrator resolves the sibling-skill helper path and passes it to the PM Agent in the dispatch prompt). The demo Bee body has no Scoped-marker line, so the marker check itself returned `scoped: false` (n/a — demo Bee is unscoped); the helper-path pass-through contract is what was being exercised.

- **Section 6 post-completion fresh-eyes sweep: PASS, no issues found.** The orchestrator dispatched `Agent(subagent_type=general-purpose, run_in_background=true)` as `post-completion-sweep-b.kw3` after the Bee-level reviewers returned clean. The fresh-eyes generalist returned **"no issues found"** and additionally observed (correctly) that "the Bee status is `in_progress` at HEAD — that's expected because Section 8 (mark Bee complete) runs after Section 7 sign-off" — defense-in-depth reasoning showing the generalist understood the bees-execute workflow's ordering.

**CHANGELOG.md final state (6 lines, all four Bee ACs satisfied):**

```
# Changelog

## Unreleased

- Migrated `/bees-execute` from the Agent Teams substrate to ephemeral Agent dispatch (b.5tm Epic 8s).
  - The new substrate uses a reconciliation-loop pattern — see Section 3 of `skills/bees-execute/SKILL.md`.
```

### AC#2 sizing-gap acknowledgment

PRD AC#2 calls for a multi-Epic Bee (4+ Epics, 12+ Tasks, 60+ Subtasks). The demo Bee `b.kw3` (1 Epic / 2 Tasks / 4 Subtasks) is well below that threshold — **deliberately so**: this demo is a focused smoke-test validating the substrate-switch's correctness end-to-end, not the full PRD AC#2 scale validation. The smoke-test signal is sufficient for Epic 8s acceptance: the per-Subtask cold dispatch shape, concurrent specialist work (twice), reconciliation-loop tick, hub-and-spoke (no inter-worker messaging), TaskList-as-progress-UI naming convention, conditional-reviewer-spawn logic, Section 4.1 commit procedure with selective staging, Section 4.2 three-branch classification, Section 5 three-concurrent-reviewer dispatch, Section 6 post-completion sweep, Section 8 defense-in-depth precondition check, Final Summary template — all were exercised and PASSed.

PRD AC#2's full multi-Epic-Bee validation is **deferred** as a follow-up; it does not have to land in Epic 8s. The natural opportunity to close that gap is the next workstream that runs `/bees-execute` on a multi-Epic Bee in the bees-workflow project (e.g., subsequent Epics in `b.5tm` itself, or downstream users running the rewritten skill on larger feature Bees).

### Cross-OS coverage gap

Demo ran on **POSIX (macOS Darwin 25.3.0)**. **Windows PowerShell coverage is a documented gap**, expected to be exercised by downstream users on Windows hosts. The substrate change does not introduce new OS-specific surfaces beyond what already shipped under Agent Teams (the OS-paired snippets in Section 1's `claude agents` precondition check, Section 4.1's Plans-hive path resolution, and Section 3's Scoped-marker helper invocation are all preserved verbatim from the pre-rewrite skill). The gap is acceptable to record rather than block on, consistent with Epic A's `## Probe results` cross-OS-gap pattern.

### Cross-Task contract verification (incidental observations)

The demo run incidentally confirmed three cross-cutting contracts established by Tasks 1-6:

- **TaskList naming convention (Section 3 → Sections 4.1 / 5).** The bee/task/subtask-scoped names observed during the run (`engineer-t3.kw3.dy.iu.h1`, `engineer-t3.kw3.dy.iu.9a`, `pm-t2.kw3.dy.iu`, `pm-t2.kw3.dy.je`, `code-reviewer-b.kw3`, `doc-reviewer-b.kw3`, `post-completion-sweep-b.kw3`) all matched the deterministic patterns documented in Section 3's "TaskList naming convention" subsection.

- **Substrate-neutral dispatch verb (Section 3 → 4.2 → 5).** The orchestrator's prose throughout the run used substrate-neutral phrasing — "dispatching the per-Task PM", "dispatch a fresh ephemeral Engineer Agent", "dispatching code-reviewer and doc-reviewer concurrently" — never "spawning a teammate", "forming a team", "TeamCreate", or any Agent Teams residue.

- **Conditional reviewer spawn logic (Section 5).** The orchestrator emitted *"Engineer + Doc Writer were both dispatched; Test Writer was not. Dispatching code-reviewer and doc-reviewer concurrently."* — verbatim demonstration of Section 5's substrate-neutral conditional-spawn rule.

### Doc Writer tool-allowlist friction (incidental observation)

The Doc Writer subagent flagged twice during the demo (and twice earlier in this Bee's own execution under the prior Agent Teams substrate) that it cannot run `bees update-ticket` because Bash is not in its tool allowlist (`agents/doc-writer.md` ships with `tools: [Read, Edit, Write, Grep]` by design). The orchestrator handled the bee-Subtask status flips on the Doc Writer's behalf cleanly. Not a defect — the allowlist is intentional and the orchestrator-driven status update is the documented path. Recording here for awareness; no action required.

