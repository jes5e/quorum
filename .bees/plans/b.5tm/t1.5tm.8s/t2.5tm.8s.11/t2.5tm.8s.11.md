---
id: t2.5tm.8s.11
type: t2
title: 'Section 4.2: remove TeamDelete + force_clean_team; preserve checkpoint and
  classification'
up_dependencies:
- t2.5tm.8s.z7
down_dependencies:
- t2.5tm.8s.5b
parent: t1.5tm.8s
children:
- t3.5tm.8s.11.gy
created_at: '2026-05-03T15:57:48.962831'
status: done
schema_version: '0.1'
guid: 5tm8s114nxtdafza5rikedghvnyc55xk
reference_materials: null
---
## Context

Section 4.2 of `skills/bees-execute/SKILL.md` ("Find next Epic or move to Final Review") contains three pieces:
1. The inter-Epic interaction checkpoint (Director runs directly, no new team — diff scan, contract drift, resource compounding, symmetric-change gaps).
2. The three-branch classification of post-Epic state (drafted/blocked-on-drafted; workable; all-done) including user-prompt branches.
3. The team-disbanding ceremony (`TeamDelete` calls, `force_clean_team.py` fallback, "task-scoped names", "shutdown timing" framing).

Epic 8s's substrate switch obsoletes only piece 3. Pieces 1 and 2 are preserved verbatim. The replacement for the disbanding ceremony is the Epic-boundary context-clear discipline — at an Epic boundary, the orchestrator clears its working context. There is no team to disband because there are no named persistent Agents; per-Subtask cold dispatches complete-and-exit naturally.

## What Needs to Change

- Remove every `TeamDelete` call (including the fallback chains involving `force_clean_team.py` and the "(1) run `force_clean_team.py` ... (2) call `TeamDelete` again" recovery pattern) throughout Section 4.2's three classification branches and the "If the user declines" path.
- Replace "Clear your context window and go back to step 2 (which will create a new team for the next Epic)." with: "Clear your working context per the Epic-boundary context-clear discipline established in Section 3, then return to step 2." (No team creation.)
- Update "Reform the implementation team" / "spin up the Engineer (via a fresh team)" → "dispatch a fresh ephemeral Engineer Agent per Section 3's dispatch shape".
- Add an explicit Epic-boundary context-clear discipline paragraph naming the ~25-30% / 1M-window budget per SDD line 197, stating the context-clear is what bounds flat-orchestration context growth (this is the load-bearing prose Task 2's forward-pointer points at).
- Remove residual Section 4.2 references to `TeamDelete`, `force_clean_team.py`, "task-scoped names", "shutdown timing", "the just-disbanded pattern".
- Preserve verbatim: three-branch classification; inter-Epic interaction checkpoint procedure (Director runs directly — no new team); user-prompt branches; precedence note (branch 1 > branch 2); `bees execute-freeform-query` recipe; `up_dependencies` batch-lookup recipe.

## Why

The inter-Epic checkpoint and three-branch classification are load-bearing — they were hard-won across multiple prior tickets. Verbatim preservation matters. Only the team-disbanding ceremony is obsolete with the substrate switch.

## Cross-Task Contract

Builds on Task 2's reconciliation-loop dispatch shape. The "fresh ephemeral Engineer Agent" dispatch (used in inter-Epic checkpoint when issues are found) uses Task 2's dispatch shape. Lands the load-bearing prose Task 2's forward-pointer references (Epic-boundary context-clear paragraph naming the ~25-30% / 1M-window budget).

This Task does NOT touch Section 5 (Task 5's scope).

## Success Criteria

- Section 4.2 contains zero occurrences of: `TeamDelete`, `force_clean_team`, "task-scoped" (in the Agent-naming sense), "shutdown timing", "the just-disbanded pattern", "Reform the implementation team", "fresh team".
- Three-branch classification (drafted/blocked-on-drafted; workable; all-done), inter-Epic checkpoint mechanics, and user-prompt branches are preserved verbatim.
- The "next Epic" transition uses Epic-boundary context-clear discipline language matching Task 2's forward-pointer.
- Section 4.2 contains an explicit Epic-boundary context-clear paragraph naming the ~25-30% / 1M-window budget per SDD line 197.
- "Reform team" / "fresh team" → "dispatch a fresh ephemeral Engineer Agent per Section 3's dispatch shape".
- `force_clean_team.py` is still present on disk (deletion belongs to Epic E).
- Lint passes.
