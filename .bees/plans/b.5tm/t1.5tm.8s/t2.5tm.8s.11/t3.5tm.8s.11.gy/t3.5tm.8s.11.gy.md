---
id: t3.5tm.8s.11.gy
type: t3
title: Remove team-disbanding ceremony from Section 4.2; preserve checkpoint and classification;
  add Epic-boundary context-clear paragraph
parent: t2.5tm.8s.11
created_at: '2026-05-03T16:05:49.896034'
status: done
schema_version: '0.1'
guid: 5tm8s11gyywxzawxt9rm19hvb43ydwf9
reference_materials: null
---
## Context

This Subtask handles Task 4 of Epic t1.5tm.8s as a single section-rewrite pass. Section 4.2 ("Find next Epic or move to Final Review") of `skills/quo-execute/SKILL.md` contains three pieces:

1. The inter-Epic interaction checkpoint (Director runs directly, no new team — diff scan, contract drift, resource compounding, symmetric-change gaps).
2. The three-branch classification of post-Epic state (drafted/blocked-on-drafted; workable; all-done) including user-prompt branches.
3. The team-disbanding ceremony (`TeamDelete` calls, `force_clean_team.py` fallback, "task-scoped names", "shutdown timing" framing).

Epic 8s's substrate switch obsoletes only piece 3. Pieces 1 and 2 are preserved verbatim. The replacement for the disbanding ceremony is the Epic-boundary context-clear discipline established in Section 3 (which Subtask 2.1 documents as part of the recursive-delegation / flat-orchestration finding) — at an Epic boundary, the orchestrator clears its working context.

This Subtask also lands the load-bearing prose Task 2's forward-pointer points at: an explicit Epic-boundary context-clear discipline paragraph naming the ~25-30% / 1M-window budget per SDD line 197.

The "fresh ephemeral Engineer Agent" path used inside the inter-Epic checkpoint (when issues are found and a fix is needed) consumes Section 3's dispatch shape from Subtask 2.1.

## What Needs to Change

In `skills/quo-execute/SKILL.md` Section 4.2:

- Remove every `TeamDelete` call (including the fallback chains involving `force_clean_team.py` and the "(1) run `force_clean_team.py` ... (2) call `TeamDelete` again" recovery pattern) throughout Section 4.2's three classification branches and the "If the user declines" path.
- Replace "Clear your context window and go back to step 2 (which will create a new team for the next Epic)." with: "Clear your working context per the Epic-boundary context-clear discipline established in Section 3, then return to step 2." Do NOT mention team creation.
- Update "Reform the implementation team" / "spin up the Engineer (via a fresh team using the just-disbanded pattern)" wording → "dispatch a fresh ephemeral Engineer Agent per Section 3's dispatch shape." Same shape change for any sibling phrasing.
- **Add an explicit Epic-boundary context-clear discipline paragraph naming the ~25-30% / 1M-window budget per SDD line 197**, stating the context-clear is what bounds flat-orchestration context growth across Epics. This is the load-bearing prose Task 2's forward-pointer references; without it, Task 2's no-recursion design rationale lives nowhere durable.
- Remove every Section 4.2 occurrence of: `TeamDelete`, `force_clean_team`, "task-scoped" (in the Agent-naming sense), "shutdown timing", "the just-disbanded pattern", "the just-finished Epic's team", "stuck agents".
- Preserve verbatim: the inter-Epic interaction checkpoint procedure (Director-runs-directly framing; diff scan via `git log --oneline <previous-epic-last-commit>..HEAD`; contract-drift / resource-compounding / symmetric-change-gaps checks; "additional commits, clearly labeled" instruction); the three-branch classification structure; the user-prompt instructions in each branch; the precedence note (branch 1 > branch 2); the `bees execute-freeform-query` recipe with its YAML stages; the `up_dependencies` batch-lookup recipe via `bees show-ticket --ids`.
- Preserve the "Mark the just-completed Epic as `status=done`" instruction and its placement after the checkpoint passes.
- Preserve project-neutrality, language-agnosticism, and POSIX + Windows PowerShell pairing.

Do NOT edit Sections 1, 2, 3, 4.1, 5, 6-10. Do NOT edit any helper script (force_clean_team.py deletion is Epic E's scope).

## Key Files

- `skills/quo-execute/SKILL.md` — Section 4.2: remove all `TeamDelete` and `force_clean_team.py` ceremony; replace context-clear-plus-team-creation language with Section 3 Epic-boundary context-clear reference; replace "Reform team" / "fresh team" wording with "fresh ephemeral Agent per Section 3's dispatch shape"; add Epic-boundary context-clear paragraph naming the ~25-30% / 1M-window budget; preserve checkpoint procedure, three-branch classification, user-prompt branches, and bees query recipes verbatim.

## Acceptance Criteria

- Section 4.2 contains zero occurrences of: `TeamDelete`, `force_clean_team`, "task-scoped" (Agent-naming sense), "shutdown timing", "the just-disbanded pattern", "the just-finished Epic's team", "Reform the implementation team", "fresh team".
- The inter-Epic interaction checkpoint procedure is preserved verbatim, including: Director-runs-directly framing; `git log --oneline <previous-epic-last-commit>..HEAD` diff scan; contract-drift / resource-compounding / symmetric-change-gaps checks; "additional commits, clearly labeled" instruction.
- The three-branch classification is preserved verbatim, including: branch 1 ("Drafted or blocked-on-drafted Epics remain") with its stop-and-tell-user message and the precedence note; branch 2 ("Workable Epic remains") with the user prompt; branch 3 ("All Epics under this Bee are done"); the precedence ordering (branch 1 > branch 2).
- The `bees execute-freeform-query` recipe with its YAML stages and the `bees show-ticket --ids <dep-id-1> <dep-id-2> <...>` batch-lookup recipe are preserved verbatim.
- The "Mark the just-completed Epic as `status=done`" instruction is preserved with its placement after the checkpoint passes.
- The replacement for "Clear your context window and go back to step 2 (which will create a new team for the next Epic)." references the Epic-boundary context-clear discipline established in Section 3 and does NOT mention team creation.
- The "fresh ephemeral Engineer Agent per Section 3's dispatch shape" wording is used inside the checkpoint's issue-found branch in place of "Reform the implementation team" / "fresh team using the just-disbanded pattern".
- **Section 4.2 contains an explicit Epic-boundary context-clear discipline paragraph naming the ~25-30% / 1M-window budget per SDD line 197, stating the context-clear is what bounds flat-orchestration context growth.** This paragraph is what Task 2's forward-pointer points at.
- Project-neutrality, language-agnosticism, and POSIX + Windows PowerShell pairing rules preserved.
- Lint passes.
- `skills/quo-execute/scripts/force_clean_team.py` is still present on disk (deletion belongs to Epic E).
- Sections 1, 2, 3, 4.1, 5, 6-10 not modified.
