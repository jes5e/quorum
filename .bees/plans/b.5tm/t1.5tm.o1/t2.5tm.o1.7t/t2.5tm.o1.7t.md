---
id: t2.5tm.o1.7t
type: t2
title: Static-verify rewrite + author runbook for end-to-end demo
up_dependencies:
- t2.5tm.o1.hs
parent: t1.5tm.o1
children:
- t3.5tm.o1.7t.i5
- t3.5tm.o1.7t.sz
egg: null
created_at: '2026-05-04T21:52:30.361756'
status: done
schema_version: '0.1'
guid: 5tmo17tned3jcp8dau9t6d587a4wuqy4
---

## Context

After Tasks 1 and 2 land, the rewritten `skills/bees-breakdown-epic/SKILL.md` must pass a battery of grep-driven static checks (Epic AC#1) and a fresh-session demo run (Epic AC#5) before the Epic is verified complete.

Per the team-lead's saved demo runbook pattern: dispatched Agents do static verification + author a markdown runbook; the user executes the runbook manually from a fresh Claude Code session; observations get appended to this Task body's `## Probe results` section. This pattern matches Epic B's `## Probe results` outcome on `t1.5tm.8s`.

## What Needs to Change

- **Static verification (Subtask 3.1)**: Run grep-driven checks for AC#1 forbidden-token absence, subagent-type-name presence, Mandatory Subtask Description Template presence, Step 1 marker-detection prose preservation (paired POSIX + PowerShell snippets, three JSON-result paths, no-silent-fallback contract), `docs/doc-writing-guide.md` `## The Scoped-marker contract` reference present, `<scoped-marker-resolver-path>` placeholder in PM dispatch prose, helper-script preservation (path-existence + git-diff against pre-Epic SHA). Findings appended to this Task body's `## Probe results`.
- **Runbook authoring (Subtask 3.2)**: Author a markdown runbook at `/tmp/.bees-workflow/<task-id>-runbook.md` (POSIX) / `$env:TEMP\.bees-workflow\<task-id>-runbook.md` (Windows) per CLAUDE.md `## Scratch-file convention`. Runbook describes Preflight, Demo execution (5 observations: subagent-precondition fires, research-mode dispatch shape observed, TaskList naming convention follows pattern, traceability-via-PM-Agent runs before `bees create-ticket`, Mandatory Subtask Description Template applied to all Subtasks), Spot-check, and Recording-observations steps.
- **User executes runbook**: from a fresh Claude Code session, runs `/bees-breakdown-epic <epic-id>` against a synthetic stand-in Bee/Epic. Observations recorded in this Task body's `## Probe results` section.

## Why

Epic AC#1 + AC#5: verify the rewrite passes grep-driven sanity checks and produces same-quality Tasks/Subtasks via demo run. Static-verify catches drift cheaply; the manual demo provides end-to-end coverage of the substrate switch.

## Cross-Task contract

- **Task 1 + Task 2 → Task 3 (verification target).** Static checks run against the post-Task-1+2 file state. Subtask 3.1 depends on Subtask 2.6 (post-coherence-pass file state).
- **Subtask 3.2 → Subtask 3.1.** Runbook authored only after static checks pass — otherwise the runbook would describe a demo against broken code.

## Cross-Epic contract

- **Epic E (`t1.5tm.kn`) — DRAFTED**: Epic E will delete `force_clean_team.py` and `check_agent_teams.py` ONLY. Subtask 3.1 explicitly verifies `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` survives (the Epic body's preserved cross-cutting work locks this from this Epic's side).
- **Demo runbook lives in the namespaced workflow scratch dir** per CLAUDE.md `## Scratch-file convention` — not in the repo, not committed, never deleted.

## Success Criteria

- All AC#1 token-grep checks return zero hits in `skills/bees-breakdown-epic/SKILL.md`.
- All required subagent-type-name literals present (`engineer`, `pm` at minimum; `doc-writer` and `test-writer` if the rewrite documents downstream-repo dispatch shapes for them).
- Mandatory Subtask Description Template four-section structure + framing sentence present verbatim.
- Step 1 marker-detection prose preserved (paired POSIX + PowerShell helper-invocation snippets; three JSON-result exit paths covered; no-silent-fallback contract present).
- `docs/doc-writing-guide.md` `## The Scoped-marker contract` reference present.
- `<scoped-marker-resolver-path>` placeholder present in PM dispatch prose.
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists at exactly that path.
- `git diff <pre-Epic-SHA> -- skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` returns empty output (no helper-content changes).
- Runbook authored at the namespaced scratch path with all four runbook sections (Preflight, Demo execution, Spot-check, Recording-observations).
- Runbook documents both POSIX and Windows PowerShell paths/commands.
- Runbook does NOT instruct the user to delete the runbook file or any other file under `<tempdir>/.bees-workflow/`.
- User-recorded observations appended to this Task body's `## Probe results` after manual demo run.

## Probe results

### Static verification — Subtask t3.5tm.o1.7t.i5 (2026-05-05)

Resolved `<epic-start-ref>` SHA: `1149a070a91086ba8dc05dd7c57b39b407c546fa` (parent of `b33e273`, the first Epic-`t1.5tm.o1` commit on `bee/b.5tm`).

Target files inspected:
- `skills/bees-breakdown-epic/SKILL.md` (51,619 bytes, mtime 2026-05-05)
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` (6,653 bytes)

**Token grep checks (Epic AC#1) — PASS (5/5).** All five forbidden Agent-Teams substrate tokens return zero occurrences in `skills/bees-breakdown-epic/SKILL.md`:

| Token | Hits |
|---|---|
| `TeamCreate` | 0 |
| `TeamDelete` | 0 |
| `Agent Teams` | 0 |
| `mode: "plan"` | 0 |
| `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` | 0 |

**Subagent-name presence-grep — PASS (4/4).** All four `subagent_type: "<name>"` literals present:

- `subagent_type: "engineer"` — lines 230, 237, 417.
- `subagent_type: "pm"` — lines 233, 240, 364.
- `subagent_type: "doc-writer"` — lines 232, 239, 419.
- `subagent_type: "test-writer"` — lines 231, 238, 418.

**Mandatory Subtask Description Template presence — PASS.** All four heading strings present at line start as `##` headings (lines 328, 331, 334, 337). Framing sentence present verbatim at line 325: "Every subtask description MUST include all of the following sections. Do not omit any section." (followed by an additional sentence, "Do not use abbreviated or one-line descriptions." — extension does not break verbatim presence).

**Step 1 marker-detection prose preserved — PASS.**

- POSIX `mkdir -p /tmp/.bees-workflow` snippets present at lines 98 and 294.
- POSIX `python3 ... scoped_marker_resolver.py ...` invocation present at line 110.
- PowerShell `New-Item -ItemType Directory -Force ...` snippets present at lines 103 and 299.
- PowerShell `python ... scoped_marker_resolver.py ...` invocation present at line 115.
- Three JSON-result exit paths documented in a single contiguous paragraph at line 118: exit 0 / `"scoped": false` (proceed with full egg-resolved doc), exit 0 / `"scoped": true` (use `docs` array's scoped subsection content), exit 2 (clear error on stderr; surface and stop).
- No-silent-fallback contract language present at line 118: "surface that error to the user and stop; do not silent-fallback to the full doc."

**Reference checks — PASS.**

- `docs/doc-writing-guide.md` `## The Scoped-marker contract` referenced at line 118 ("The Scoped-marker grammar and the helper contract are documented in `docs/doc-writing-guide.md` `## The Scoped-marker contract`.").
- `<scoped-marker-resolver-path>` placeholder present in PM dispatch prose at lines 378 and 381 (own-skill resolution explanation).

**Helper preservation — PASS.**

- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists at the documented path.
- `git diff 1149a070a91086ba8dc05dd7c57b39b407c546fa -- skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` returns empty output (zero changes from pre-Epic SHA).

**Lint (`python -m pyflakes skills/*/scripts/*.py`) — see Subtask report.** Run once at the end of the Subtask per project Lint contract.

**Overall static-verification result: PASS** for all categories listed in Epic AC#1 and the Epic body's `## Preserved cross-cutting work`. The rewrite is clear for the runbook-authoring Subtask (`t3.5tm.o1.7t.sz`) to proceed.

### Demo run — manual fresh-session execution (2026-05-05)

Demo executed in a fresh Claude Code session against a synthetic stand-in Bee `b.fio` ("Demo: bees-setup deprecation warning") with single Epic `t1.fio.pn`. Initial Bee body (deprecation-warning framing on bees-setup) was caught by the orchestrator's pre-dispatch coherence check as misaligned with the actual codebase — bees-setup has no contract-key precondition gate (the gate lives in the consumer skills) and there is no "legacy auto-detection fallback" to deprecate. Spec was amended to a coherent variant: enrich the existing `Run /bees-setup first.` hard-fail message in the three execution skills' `## Preconditions` sections to name the specific missing CLAUDE.md section/key. Demo re-run against the amended Epic.

Synthetic ticket fixtures (`b.fio`, `t1.fio.pn`, `t2.fio.pn.1v`, `t3.fio.pn.1v.5y`, `.8v`, `.ak`) deleted post-demo via `bees delete-ticket` and the `.bees/plans/b.fio/` working-tree directory removed via `git clean -fd`. No commits made against the synthetic Bee.

#### Observation 1 — Subagent-precondition check fires upfront

**PASS.** Orchestrator invoked `claude agents` upfront (visible in transcript before any `Agent(...)` dispatch), enumerated the seven required subagent names, confirmed all present, and proceeded to Section 1. The orchestrator additionally re-confirmed the precondition gates inline (Plans hive colonized; CLAUDE.md `## Documentation Locations` and `## Build Commands` with all five keys present) — reading the precondition section as a checklist rather than a one-shot enumeration. Hard-fail path was not exercised on this run because the install was correctly in place.

#### Observation 2 — Research-mode dispatch shape

**PASS.** Section 4 dispatched two `Agent(...)` invocations in parallel for Task `t2.fio.pn.1v` with `run_in_background=true`: `subagent_type=engineer` and `subagent_type=doc-writer`. `subagent_type=test-writer` was correctly skipped because the project's CLAUDE.md `## Build Commands` `Narrow test`/`Full test` keys are stubbed (`echo 'no test suite for this repo'`) — the role-selection rule in Section 4 ("If unit-test code needs to be changed, dispatch `subagent_type: \"test-writer\"`. If not, the Test Writer is optional.") drove the elision. Workers exhibited READ-ONLY RESEARCH MODE behavior in their JSON findings (Doc Writer searched all five registered docs without mutating files; Engineer returned proposed Subtask bodies without running `bees create-ticket`).

#### Observation 3 — TaskList naming convention

**PASS.** Observed entries: `engineer-research-t2.fio.pn.1v`, `doc-writer-research-t2.fio.pn.1v` (implementer Task scope), `pm-research-t1.fio.pn` (per-Task PM review of `t2.fio.pn.1v`, Epic scope), `pm-research-t1.fio.pn` (Epic-wide Spec Traceability Review, Epic scope; reused name after the previous PM entry transitioned to `completed`). All entries match the documented `<role>-research-<task-id>` (implementer) and `pm-research-<epic-id>` (PM) shapes. Status transitions observed: `pending` → `in_progress` → `completed`. The two PM entries sharing a name is correct per the spec ("the orchestrator creates a new PM research Agent per Task-level review boundary, but the TaskList name disambiguates by Epic"); they were sequential, not concurrent.

#### Observation 4 — Spec Traceability Review (PM Agent) runs BEFORE `bees create-ticket` for gap-fill

**PASS.** Section 5 ordering verified: the Epic-wide PM `Agent(...)` dispatch (TaskList: `pm-research-t1.fio.pn`, Epic-wide scope) landed before any gap-fill `bees create-ticket` invocation. PM signed off with all six traceability rows at `OK` and zero gaps, so the gap-fill loop was a no-op on this run — the ordering assertion still holds because no gap-fill `bees create-ticket` was needed. After PM sign-off, the orchestrator transitioned Epic + Task + 3 Subtasks to `ready` via four `bees update-ticket --ids <id> --status ready` calls, matching the Section 5 Step 5 ordering.

#### Observation 5 — Mandatory Subtask Description Template applied to every Subtask

**PASS.** All three created Subtasks (`t3.fio.pn.1v.5y`, `.8v`, `.ak`) were authored via `bees create-ticket --body-file <path>` with bodies written to `/tmp/.bees-workflow/bees-body-st-{be,fi,bd}-fio.md` (namespaced scratch dir per CLAUDE.md `## Scratch-file convention`). Each Subtask body contained all four `## Context` / `## What Needs to Change` / `## Key Files` / `## Acceptance Criteria` sections, with substantive content (line numbers, file paths, canonical message templates) — not abbreviated or one-line descriptions. The Engineer additionally added a `## Cross-Subtask wording contract` section because of the Task-level uniformity contract.

#### Spot-check

- (a) Four template sections present in all Subtasks: **PASS**
- (b) All Tasks and Subtasks at status `ready`: **PASS**
- (c) Epic at status `ready`: **PASS**

#### Notable findings beyond the five observations

- **Coherence check at orchestrator level (positive surprise).** Before dispatching any research Agents, the orchestrator independently verified the spec is consistent with the actual codebase (Section 2's "Check external-system contracts against authoritative docs" prose working as designed, applied here to in-repo contracts rather than third-party docs). The first synthetic Bee body was caught as misaligned with the actual code at the cheapest possible point (zero agent dispatches burned). The rewritten skill correctly performs this pre-dispatch sanity check.
- **Yield discipline (positive).** The orchestrator yielded after every dispatch and waited for completion notifications rather than polling. No `/loop`, `ScheduleWakeup`, `CronCreate`, or sleep-wait cycles observed. Section 4's "Anti-pattern: no clock primitives" callout is being followed.
- **CLI flag-name memory error (minor, recovered).** At the Section 5 Step 5 status-transition step, the orchestrator initially used `--ticket-id <id>` (wrong flag) on `bees update-ticket` and got a clear CLI error (`the following arguments are required: --ids`). It self-corrected to `--ids <id>` within one tick and re-issued all four updates successfully. Not a regression of the rewrite — the bees CLI's error message named the right flag, and the orchestrator's recovery was clean. Worth surfacing as a low-priority polish item: the rewrite could optionally name `--ids` (vs `--ticket-id`) explicitly in Section 5 Step 5 prose.
- **HTML-entity cleanup in JSON findings (minor, handled).** The Engineer's returned JSON used `&lt;` / `&gt;` HTML entities for angle brackets in placeholders. The orchestrator noticed this when authoring the Subtask body files and converted them to literal `<` `>` before writing. Reasonable handling; not a regression.

#### Overall verdict

**PASS** — Demo confirms the rewritten `/bees-breakdown-epic` matches Epic `t1.5tm.o1`'s acceptance criteria. All five observations exhibit the documented behavior. Minor polish items (CLI flag-name documentation, HTML-entity handling note) recorded above for future iteration; neither is a blocker for closing the Epic.
