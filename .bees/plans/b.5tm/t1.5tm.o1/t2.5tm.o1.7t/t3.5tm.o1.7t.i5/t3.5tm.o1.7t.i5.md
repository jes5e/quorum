---
id: t3.5tm.o1.7t.i5
type: t3
title: Static verification of rewritten SKILL.md against AC#1 and preservation contracts
up_dependencies:
- t3.5tm.o1.hs.ee
down_dependencies:
- t3.5tm.o1.7t.sz
parent: t2.5tm.o1.7t
created_at: '2026-05-04T22:41:16.231725'
status: done
schema_version: '0.1'
guid: 5tmo17ti5s3szf77spbxcr6pwuvb2wkt
reference_materials: null
---
## Context

After Tasks 1 and 2 land, the rewritten `skills/bees-breakdown-epic/SKILL.md` must pass a battery of grep-driven static checks before the demo runbook can be reliably authored. Per the team-lead's saved demo-runbook pattern, this Subtask is dispatched-Agent static verification — observable findings are appended to this Task body's `## Probe results` section after the Agent runs.

The static checks come from two sources:

- Epic AC#1 (zero hits for the forbidden Agent-Teams substrate tokens).
- The Epic body's `## Preserved cross-cutting work` (helper-script preservation, Step 1 marker-detection prose preservation).

The "Helper preservation" check needs an anchor SHA for "unchanged from pre-Bee state". Use `git rev-parse <epic-start-ref>` where `<epic-start-ref>` is the orchestrator-recorded SHA at the start of `t1.5tm.o1` execution (typically `git merge-base main bee/b.5tm` or the SHA of the commit immediately before this Epic's first Task commit). The Subtask body should not hardcode the SHA — the executing static-verify Agent computes it at runtime from the bee branch's git log.

## What Needs to Change

Execute the following static checks against `skills/bees-breakdown-epic/SKILL.md` and `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`. Findings get appended to this Task body's `## Probe results` section.

**Token grep checks (Epic AC#1):**

- Zero occurrences of `TeamCreate` in `skills/bees-breakdown-epic/SKILL.md`.
- Zero occurrences of `TeamDelete` in the same file.
- Zero occurrences of `Agent Teams` in the same file.
- Zero occurrences of `mode: "plan"` in the same file.
- Zero occurrences of `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` in the same file.

**Subagent-name presence-grep checks:**

- At least one literal occurrence of `subagent_type: "engineer"` (or equivalent dispatch-prose form like `subagent_type=engineer`) in the file.
- At least one literal occurrence of `subagent_type: "pm"` (or equivalent).
- If the rewrite documents downstream-repo dispatch shapes for them: at least one literal occurrence of `subagent_type: "doc-writer"` and `subagent_type: "test-writer"`.

**Mandatory Subtask Description Template presence-grep:**

- The four template heading strings appear in the file: `## Context`, `## What Needs to Change`, `## Key Files`, `## Acceptance Criteria`.
- The framing sentence "Every subtask description MUST include all of the following sections. Do not omit any section." appears verbatim.

**Step 1 marker-detection prose preserved:**

- The file contains both POSIX (`mkdir -p /tmp/.bees-workflow` and `python3 ... scoped_marker_resolver.py ...`) and Windows PowerShell (`New-Item -ItemType Directory -Force ...` and `python ... scoped_marker_resolver.py ...`) helper-invocation snippets.
- The file contains the JSON-result-handling prose covering all three exit paths: exit 0 / `scoped: false`, exit 0 / `scoped: true`, exit 2 / hard-fail with stderr surfaced.
- The file contains the no-silent-fallback contract language explicitly.

**Reference checks:**

- The file references `docs/doc-writing-guide.md` `## The Scoped-marker contract` as the marker-grammar source of truth.
- The file's PM dispatch prose contains the literal placeholder `<scoped-marker-resolver-path>`.

**Helper preservation:**

- The file `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists at exactly that path.
- `git diff <epic-start-ref> -- skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` returns empty output (no changes to the helper from pre-Epic state). Compute `<epic-start-ref>` at runtime as the SHA of the commit immediately before this Epic's first commit on the working branch.

## Key Files

- skills/bees-breakdown-epic/SKILL.md — target of all static-verification grep checks
- skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py — preservation check (path-existence + git-diff against pre-Epic SHA)

## Acceptance Criteria

- All five token-grep checks return zero hits in `skills/bees-breakdown-epic/SKILL.md`.
- All required subagent-type literals are present: `engineer`, `pm` at minimum; `doc-writer` and `test-writer` if the rewrite documents downstream-repo dispatch shapes.
- All four Mandatory Subtask Description Template heading strings are present.
- The Mandatory Subtask Description Template framing sentence is present verbatim.
- Both POSIX and Windows PowerShell Step 1 marker-detection helper-invocation snippets are present.
- All three JSON-result-handling exit paths (exit 0 / scoped:false, exit 0 / scoped:true, exit 2) are documented in the rewrite.
- No-silent-fallback contract language is present.
- `docs/doc-writing-guide.md` `## The Scoped-marker contract` reference is present.
- `<scoped-marker-resolver-path>` placeholder is present in the PM dispatch prose.
- `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists at the documented path.
- `git diff <epic-start-ref> -- skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` returns empty (no helper-content changes).
- All findings are appended to this Task body's `## Probe results` section per the saved demo-runbook pattern.
