---
id: t2.5tm.o1.4d
type: t2
title: Add Preconditions section with subagent-registration hard-fail
down_dependencies:
- t2.5tm.o1.hs
parent: t1.5tm.o1
children:
- t3.5tm.o1.4d.mu
egg: null
created_at: '2026-05-04T21:52:26.760207'
status: done
schema_version: '0.1'
guid: 5tmo14ddm8qx7w8n2ding2xnyvp9w9x8
---

## Context

The rewritten `skills/bees-breakdown-epic/SKILL.md` (under Task 2) dispatches research Agents against the seven custom subagent types installed at `~/.claude/agents/` by Epic A (`t1.5tm.27`). Claude Code loads custom subagents only at session start; mid-session installs require a Claude Code restart (or `/agents` to hot-reload). Without an explicit precondition gate, a fresh-install user who runs `/bees-breakdown-epic` before restarting hits a confusing `Agent type 'X' not found`-style error mid-dispatch with no actionable guidance.

The Epic body's `## Precondition reminder` paragraph references the stale path `~/.claude/subagents/`. The verified-correct canonical path (per Epic A's post-restart Probe results) is `~/.claude/agents/` (global) and `<repo>/.claude/agents/` (per-project). This Task uses the corrected path, NOT the path from the Epic body's stale paragraph.

## What Needs to Change

- Add a new top-level `## Preconditions` section to `skills/bees-breakdown-epic/SKILL.md` immediately before `## Workflow` (and before Section 0).
- Mirror `bees-execute/SKILL.md`'s Preconditions section pattern (~lines 25-53) — including both the procedural-gate-at-first-dispatch and the upfront `claude agents` fast-fail.
- Require all seven custom subagent type literal names (`engineer`, `test-writer`, `doc-writer`, `pm`, `code-reviewer`, `test-reviewer`, `doc-reviewer`) — uniform with bees-execute / bees-fix-issue.
- Hard-fail message must direct user to (a) verify install per `README.md` `## Install` and (b) restart Claude Code or run `/agents` to hot-reload.
- All install-path references use `~/.claude/agents/` (global) and `<repo>/.claude/agents/` (per-project). NOT `subagents/`.
- Include the existing CLAUDE.md `## Documentation Locations` + `## Build Commands` checks (with all five required keys: `Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`) and the Plans-hive check for parity with bees-execute Preconditions.

## Why

Per Epic body's `## Precondition reminder`: the rewritten skill MUST hard-fail with a clear "restart Claude Code" message rather than silently falling back to `general-purpose` or failing mid-dispatch. Per Epic A's verified Probe results, sessions cache the subagent registry at startup.

## Cross-Task contract

**Task 1 → Task 2.** Task 2's reconciliation-loop dispatch prose (Subtask 2.1) **references this Preconditions section's hard-fail message** as the procedural-gate fallback when first dispatch hits an `Agent type 'X' not found`-style error. Task 1 must land first or in parallel with Task 2's Subtask 2.1.

## Cross-Epic contract

- Mirror `bees-execute/SKILL.md` and `bees-fix-issue/SKILL.md` Preconditions sections — same seven required subagent type names, same procedural-gate + upfront-`claude agents` hybrid pattern, same hard-fail message structure.
- Use the corrected `~/.claude/agents/` path (Epic A post-probe correction), NOT the stale `~/.claude/subagents/` path that still appears in this Epic's body precondition-reminder paragraph.

## Success Criteria

- `skills/bees-breakdown-epic/SKILL.md` contains a top-level `## Preconditions` section before `## Workflow`.
- The section enumerates all seven subagent type names as literal strings.
- Both the upfront `claude agents` enumeration check (paired POSIX + PowerShell) and the procedural-gate fallback are documented.
- Hard-fail message names both the README install instructions and the Claude Code restart / `/agents` remediation.
- Zero occurrences of the path substring `subagents/` (with trailing slash) in the new section.
- The Plans-hive check + CLAUDE.md `## Documentation Locations` + `## Build Commands` checks (with all five required keys) are documented.
