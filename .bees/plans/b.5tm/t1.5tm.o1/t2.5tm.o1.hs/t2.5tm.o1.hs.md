---
id: t2.5tm.o1.hs
type: t2
title: Wholesale SKILL.md rewrite to ephemeral-Agent research orchestration
up_dependencies:
- t2.5tm.o1.4d
down_dependencies:
- t2.5tm.o1.7t
parent: t1.5tm.o1
children:
- t3.5tm.o1.hs.69
- t3.5tm.o1.hs.38
- t3.5tm.o1.hs.yw
- t3.5tm.o1.hs.rg
- t3.5tm.o1.hs.23
- t3.5tm.o1.hs.ee
created_at: '2026-05-04T21:52:29.396942'
status: done
schema_version: '0.1'
guid: 5tmo1hssh3cq1exdcms119dj6ir4prcx
reference_materials: null
---
## Context

The current `skills/bees-breakdown-epic/SKILL.md` Section 4 (lines ~145-289) is built on Agent Teams primitives — `mode: "plan"`, "Spawn one persistent team", "team-lead" / "team" vocabulary, inline role instructions, `#### Team Composition` block. Section 5 (lines ~291-346) creates tickets first, then runs the Spec Traceability Review afterward.

Per Epic AC#1, all Agent-Teams primitives must be removed. Per Epic AC#2, the new prose references the Epic-A subagent types by name with a "research mode" instruction in the dispatch prompt. Per Epic AC#3, Spec Traceability Review (PM subagent in research mode) must run BEFORE ticket creation. Per Epic AC#4, the Mandatory Task/Subtask description template is preserved.

This Task is the wholesale rewrite of Section 4 + reorder of Section 5, plus verification that Sections 0, 1, 2, 3, 6, 7 (which Subtasks 2.1 / 2.2 / 2.3 do not touch) survive verbatim. The substrate switch and the traceability reorder are tightly coupled — splitting them would create an inconsistent intermediate-state file.

## What Needs to Change

- **Section 4 substrate switch (Subtask 2.1)**: Replace Agent-Teams prose with reconciliation-loop + research-mode ephemeral-Agent dispatch. Reference subagent types by literal name (`engineer`, `doc-writer`, `test-writer`, `pm`); link each to `agents/<role>.md`. Document the research-mode dispatch-prompt preamble. Document TaskList naming convention (`engineer-research-<task-scope-id>`, `pm-research-<epic-id>`, etc.).
- **Section 5 reorder (Subtask 2.2)**: PM Agent (research mode) Spec Traceability Review runs BEFORE `bees create-ticket`, with gap-fill iteration loop. PM dispatch prompt includes `<scoped-marker-resolver-path>` placeholder (own-skill resolution — `<this skill's base directory>/scripts/scoped_marker_resolver.py`, NOT sibling-resolved). After PM sign-off, orchestrator creates tickets via `--body-file`. After creation, set statuses to `ready` (Epic + Tasks + Subtasks).
- **Section 4 callouts (Subtask 2.3)**: Add anti-patterns (no `/loop`, `ScheduleWakeup`, `CronCreate`, polling), hub-and-spoke via substrate, recursive-delegation-not-supported (cite Claude Code sub-agents docs URL).
- **Sections 0, 1, 2, 3, 6, 7 preservation (Subtask 2.4)**: Verify these sections survive verbatim — including Step 1 marker-detection prose with paired POSIX + PowerShell snippets, JSON-result handling, no-silent-fallback contract, `docs/doc-writing-guide.md` `## The Scoped-marker contract` reference, sibling-overlap check, stale-Epic check, Plans-hive-aware commit logic, six-option next-steps menu with reshape-risk-aware Recommended badge.
- **Mandatory Subtask Description Template + temp-file body-authoring prose preservation (Subtask 2.5)**: Verify the four-section template (`## Context`, `## What Needs to Change`, `## Key Files`, `## Acceptance Criteria`) and the temp-file `--body-file` body-authoring prose survive verbatim at architecturally-coherent points in the rewrite.
- **Final coherence pass (Subtask 2.6)**: Verify section numbering is contiguous, cross-references resolve, vocabulary is uniform, no orphan references to deleted concepts.

## Why

The substrate switch is the entire purpose of this Epic — Agent Teams is being retired across all three execution skills. Research-mode prompt-prefix dispatch is the design pattern Epic body's Implementation note specifies ("Research mode signaled via prompt, not separate subagent type"). Spec-traceability before ticket creation prevents create-then-delete churn when the PM's gap-fill loop changes the proposed Subtask set.

## Cross-Task contract

- **Task 1 → Task 2 (procedural-gate fallback).** Subtask 2.1's Section 4 reconciliation-loop dispatch prose **references Task 1's `## Preconditions` section** for the procedural-gate fallback at first dispatch's `Agent type 'X' not found`-style error.
- **Task 2 → Task 3 (verification target).** This Task establishes the dispatch shape, the `<scoped-marker-resolver-path>` placeholder, the TaskList naming convention, and the subagent_type literals — all of which Task 3's static-verify Subtask grep-checks against and the runbook's manual-demo observations look for.
- **Internal Subtask dependencies.** 2.2 → 2.1 (Section 5 references the Section 4 dispatch scaffold). 2.3 → 2.1 (callouts live inside the Section 4 scaffold). 2.4 → 2.1, 2.2, 2.3 (verification pass over their edits). 2.5 → 2.1, 2.2 (verifies content survived their wholesale section-replacements). 2.6 → 2.1, 2.2, 2.3, 2.4, 2.5 (final coherence pass after all parallel edits).

## Cross-Epic contract

- **Mirror Epic B's bees-execute Section 3** at smaller research-mode scope — same TaskList naming-convention vocabulary, same recursive-delegation-not-supported callout citing the [Claude Code sub-agents docs](https://docs.claude.com/en/docs/claude-code/sub-agents), same anti-pattern callout list. Diverge only where research-mode demands (per-Task TaskList names use `*-research-*` infix; orchestrator alone runs `bees create-ticket`).
- **Helper-resolution path divergence from Epic B/C pattern.** bees-breakdown-epic's PM dispatch resolves `<scoped-marker-resolver-path>` against THIS skill's own `scripts/` dir (`<this skill's base directory>/scripts/scoped_marker_resolver.py`), NOT sibling-resolved against `bees-breakdown-epic/scripts/` from another skill's base. Subtask 2.2 must call out this divergence so a reader doesn't apply the bees-execute pattern by analogy.
- **Epic E (`t1.5tm.kn`) territory**: `scoped_marker_resolver.py` is preserved (NOT deleted/moved/renamed). This Task does not touch the helper.
- **Epic F (`t1.5tm.fy`) territory**: SDD non-feature sections, README — this Task does not touch them. Only this skill's SKILL.md is in scope.

## Success Criteria

- All AC#1 forbidden tokens absent from `skills/bees-breakdown-epic/SKILL.md`: `TeamCreate`, `TeamDelete`, `Agent Teams`, `mode: "plan"`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`.
- New Section 4 references subagent_type literals (`engineer`, `doc-writer`, `test-writer`, `pm`).
- Section 5 reorder lands: PM Agent (research mode) runs Spec Traceability Review BEFORE `bees create-ticket`; gap-fill iteration loop documented; ticket creation only after PM sign-off; statuses set to `ready` after creation.
- Sections 0, 1, 2, 3, 6, 7 verbatim-preserved (Step 1 marker-detection prose intact; `scoped_marker_resolver.py` reference intact; doc-writing-guide reference intact).
- Mandatory Subtask Description Template (4-section: `## Context`, `## What Needs to Change`, `## Key Files`, `## Acceptance Criteria`) preserved verbatim.
- Temp-file `--body-file` body-authoring prose preserved (paired POSIX + PowerShell, never-delete-after-exit instruction).
- File reads as a coherent whole post-rewrite (no orphan references, contiguous section numbering, uniform dispatched-research-Agent vocabulary).
