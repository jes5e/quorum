---
id: t3.5tm.o1.hs.23
type: t3
title: Verify Mandatory Subtask Description Template + temp-file body-authoring prose preserved
up_dependencies:
- t3.5tm.o1.hs.69
- t3.5tm.o1.hs.38
down_dependencies:
- t3.5tm.o1.hs.ee
parent: t2.5tm.o1.hs
egg: null
created_at: '2026-05-04T22:41:07.425386'
status: done
schema_version: '0.1'
guid: 5tmo1hs23t8oqb25pjhconvyuepajeca
---

## Context

Two preservation items deserve their own Subtask because Epic AC#4 explicitly calls them out and they are the most easily-dropped items during a wholesale-section-replacement edit:

- The **Mandatory Subtask Description Template** (currently `#### Mandatory Subtask Description Template`, lines ~268-285) — Context / What Needs to Change / Key Files / Acceptance Criteria. Epic AC#4: "Mandatory Task/Subtask description template applied (current behavior preserved)." This template lived inside the old Section 4 (which Subtask 2.1 replaces wholesale), so there is meaningful risk of it getting dropped during the substrate switch. It must survive the rewrite at an equivalent point in the new architecture (likely as a top-level subsection in the new Section 4, or as a standalone subsection between Sections 4 and 5, so both the new Section 4's research-mode dispatch prose and the existing Section 5's Spec Traceability Review can reference it).

- The **temp-file body-authoring prose** (currently the long `**Authoring Task and Subtask bodies**` paragraph at line ~166) — for every `bees create-ticket` the orchestrator runs for a Task or Subtask, author the body to a temp file via the `Write` tool (under the namespaced workflow scratch dir `/tmp/.bees-workflow/bees-body-<short-suffix>.md` POSIX / `$env:TEMP\.bees-workflow\bees-body-<short-suffix>.md` Windows; create the `.bees-workflow` subdir if absent; never delete after bees command exits) and pass `--body-file <path>`. This is the scratch-file convention from CLAUDE.md `## Scratch-file convention` applied to this skill. It must survive the rewrite at a point where the orchestrator's `bees create-ticket` invocations are documented (likely Section 5, where ticket creation now happens after PM sign-off per Subtask 2.2).

## What Needs to Change

Verify both items survived Subtasks 2.1 and 2.2's edits at equivalent points in the new architecture. If dropped, restore at the appropriate location:

- The Mandatory Subtask Description Template MUST appear in `skills/bees-breakdown-epic/SKILL.md` with all four section headings preserved verbatim: `## Context`, `## What Needs to Change`, `## Key Files`, `## Acceptance Criteria`, plus the introductory and per-section explanatory prose currently at lines ~270 and ~273-284. The framing sentence "Every subtask description MUST include all of the following sections. Do not omit any section. Do not use abbreviated or one-line descriptions." must remain.
- The temp-file body-authoring prose MUST appear at a point where the orchestrator's `bees create-ticket` invocations are documented (the new Section 5, post-PM-sign-off ticket-creation per Subtask 2.2 is the natural spot). Required content: Write tool, temp-file path under `<tempdir>/.bees-workflow/`, `mkdir -p` / `New-Item -ItemType Directory -Force` create-if-absent step, paired POSIX + PowerShell path examples, never-delete-after-exit instruction, the `--body-file` flag rationale (multi-section markdown trips Claude Code's command-injection guard if inlined as `--body "..."`).

## Key Files

- skills/bees-breakdown-epic/SKILL.md — verify Mandatory Subtask Description Template and temp-file body-authoring prose

## Acceptance Criteria

- The Mandatory Subtask Description Template appears in the rewritten SKILL.md as a labeled subsection with all four headings literal: `## Context`, `## What Needs to Change`, `## Key Files`, `## Acceptance Criteria`.
- The framing sentence "Every subtask description MUST include all of the following sections. Do not omit any section. Do not use abbreviated or one-line descriptions." appears verbatim in the rewritten SKILL.md.
- The temp-file body-authoring prose names the namespaced scratch directory `<tempdir>/.bees-workflow/`, includes paired POSIX (`mkdir -p /tmp/.bees-workflow`) and PowerShell (`New-Item -ItemType Directory -Force -Path "$env:TEMP\.bees-workflow" | Out-Null`) directory-creation snippets, and explicitly instructs the caller NOT to remove the temp file after the bees command exits.
- The temp-file prose includes the `--body-file` flag rationale (multi-section markdown trips Claude Code's command-injection guard if inlined as `--body "..."`).
- Both items are positioned at architecturally-coherent points: the Mandatory Subtask Description Template is referenced (or located) where research-mode Subtask authoring is described; the temp-file body-authoring prose is located where the orchestrator's `bees create-ticket` invocations are documented.
