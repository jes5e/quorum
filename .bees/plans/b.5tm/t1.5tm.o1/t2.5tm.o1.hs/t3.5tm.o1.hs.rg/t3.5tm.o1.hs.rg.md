---
id: t3.5tm.o1.hs.rg
type: t3
title: Verify Sections 1-3 and 6-7 preserved verbatim through the rewrite
up_dependencies:
- t3.5tm.o1.hs.69
- t3.5tm.o1.hs.38
- t3.5tm.o1.hs.yw
down_dependencies:
- t3.5tm.o1.hs.ee
parent: t2.5tm.o1.hs
egg: null
created_at: '2026-05-04T22:41:05.463085'
status: ready
schema_version: '0.1'
guid: 5tmo1hsrgimy3wcej9biyb6fkhwqgcsx
---

## Context

Per the Epic body's `## Preserved cross-cutting work` and the Task plan's "What's preserved" list, large sections of the current SKILL.md must survive the rewrite verbatim or at the equivalent point in the new architecture. This Subtask is a static-verification + targeted-restoration pass that confirms the preserved sections survived Subtasks 2.1-2.3's edits unchanged. Subtasks 2.1-2.3 should not have touched these sections, but a verification pass catches accidental drift.

In particular, the current SKILL.md's Section 2 Step 1 marker-detection prose (currently lines ~62-90, `**Check for the Scoped-marker on the parent Bee.**` paragraph and the paired POSIX bash + Windows PowerShell helper-invocation snippets) is **load-bearing** per the Epic body's `## Preserved cross-cutting work` paragraph: it is the canonical Marker consumer for the breakdown skill and the helper script `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` MUST NOT be moved or renamed. This Subtask verifies the prose and the helper-path reference are intact.

## What Needs to Change

Verify the following sections of `skills/bees-breakdown-epic/SKILL.md` survived Subtasks 2.1-2.3's edits unchanged in content (formatting whitespace adjustments are fine):

- **Section 0 — `### 0. Choose agent model preference`** (currently lines ~13-22): Opus-vs-Sonnet support-roles question, "core implementation-shaping role always uses Opus" line. Preserve verbatim.
- **Section 1 — `### 1. Determine Which Epic to Break Down`** (currently lines ~24-52): caller-arg branching (Epic ID / Bee ID / no args), drafted-Epic enumeration query, AskUserQuestion-driven Epic pick. Preserve verbatim.
- **Section 2 — `### 2. Fetch and Analyze Epic`** (currently lines ~54-103): all bullets — Read parent Bee + egg sources, Plan-Bee-body-as-spec branch, Step 1 marker-detection prose with paired POSIX + PowerShell snippets and JSON-result handling and no-silent-fallback contract, doc-writing-guide reference for Scoped-marker grammar source-of-truth, "Check external-system contracts against authoritative docs" prose, "Find any Epics this Epic depends on" prose, "Check for sibling overlap" prose. Preserve verbatim.
- **Section 3 — `### 3. Break Epic into Tasks`** (currently lines ~106-143): Tasks design rules, Cross-Task contract documentation prose with the four canonical contract examples, Example task. Preserve verbatim.
- **Section 6 — `### 6. Commit New Ticket Files`** (currently lines ~348-389): Plans-hive-aware git logic, paired POSIX bash + Windows PowerShell snippets, out-of-repo skip-and-surface-note rule. Preserve verbatim.
- **Section 7 — `### 7. Offer Next Steps`** (currently lines ~391-441): six-option menu, reshape-risk-aware Recommended badge logic, per-option "best when …" rationales. Preserve verbatim.

If any of these sections has drifted from its pre-Bee state, restore the original content. Subtask 2.2's Section 5 reorder does not change section numbering itself; references such as Section 7's mention of "Step 6 found the Plans hive lives outside the repo" remain correct (Section 6 is unchanged at #6). Only update numerical cross-references if Subtask 2.2's renumbering changed any (it should not).

## Key Files

- skills/bees-breakdown-epic/SKILL.md — verify (and restore if drifted) Sections 0, 1, 2, 3, 6, 7

## Acceptance Criteria

- Section 0's Opus-vs-Sonnet AskUserQuestion shape matches pre-Bee content verbatim (including the "core implementation-shaping role always uses Opus" line).
- Section 1's no-args / Bee ID / Epic ID branching, the drafted-Epic-enumeration `bees execute-freeform-query` recipe, and the AskUserQuestion-driven Epic pick are unchanged in content.
- Section 2's Step 1 marker-detection prose is intact: both POSIX bash and Windows PowerShell helper-invocation snippets present, JSON-result handling (`scoped: false` / `scoped: true` / exit 2) intact, no-silent-fallback contract intact, the `<this skill's base directory>/scripts/scoped_marker_resolver.py` resolution path intact.
- Section 2 references `docs/doc-writing-guide.md` `## The Scoped-marker contract` as the source-of-truth for marker grammar.
- Section 2's "Check external-system contracts against authoritative docs" paragraph (`WebSearch` / `WebFetch` upfront-doc-check rationale) is intact.
- Section 2's "Find any Epics this Epic depends on" and "Check for sibling overlap" bullets are intact.
- Section 3's Tasks design-rules bullets, Cross-Task contract documentation prose with all four contract-example bullets (Ordering / Resource-sharing / Invariant assumptions / Symmetric-lifecycle), and the Example task block are intact.
- Section 6's Plans-hive-aware git logic with paired POSIX + PowerShell snippets is intact, and the out-of-repo skip-and-surface-note rule is intact.
- Section 7's six-option menu is intact: whole-Bee execute, this-Epic-first execute, start-at-specific-Epic, break-down-next-Epic, review-first, done-for-now. Reshape-risk-aware Recommended badge logic is intact. Each option carries its "best when …" clause.
- The bundled helper at `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py` exists at exactly that path (not moved, not renamed) — this is the inter-skill contract documented in the Epic body's `## Preserved cross-cutting work`.
