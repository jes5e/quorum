---
id: t3.5tm.o1.hs.yw
type: t3
title: Add anti-patterns, hub-and-spoke, and recursive-delegation-not-supported callouts
  to Section 4
up_dependencies:
- t3.5tm.o1.hs.69
down_dependencies:
- t3.5tm.o1.hs.rg
- t3.5tm.o1.hs.ee
parent: t2.5tm.o1.hs
created_at: '2026-05-04T22:41:01.054272'
status: done
schema_version: '0.1'
guid: 5tmo1hsyw9bvz6vk9mgvnhkyj34i192c
reference_materials: null
---
## Context

Subtask 2.1 establishes the reconciliation-loop substrate in Section 4. Three architectural-discipline callouts must accompany that substrate switch — they appear in `quo-execute/SKILL.md` Section 3 (subsections `##### Anti-pattern: no clock primitives`, `#### Hub-and-spoke via substrate`, `#### Recursive delegation: not supported`) and must appear at equivalent points in `quo-breakdown-epic/SKILL.md`'s new Section 4 for cross-skill consistency. These are independent textual additions that complement Subtask 2.1's substrate switch.

The recursive-delegation callout cites the [Claude Code sub-agents docs](https://docs.claude.com/en/docs/claude-code/sub-agents) ("Subagents cannot spawn other subagents"). quo-execute uses the same citation; reuse the URL and quoted phrase verbatim for cross-skill consistency.

## What Needs to Change

In `skills/quo-breakdown-epic/SKILL.md` Section 4 (the new reconciliation-loop section authored by Subtask 2.1), add three subsection callouts:

- **Anti-pattern: no clock primitives** — explicit prohibition on `/loop`, `ScheduleWakeup`, `CronCreate`, polling. Mirror quo-execute Section 3's `##### Anti-pattern: no clock primitives` subsection (lines ~196-204) verbatim in content; only the surrounding context-setting language adapts to research-mode scope.
- **Hub-and-spoke via substrate** — workers do not message each other; the orchestrator is the hub; each dispatched research Agent is a spoke that reads its prompt, returns findings as text, and exits. The diff-as-handoff phrasing from quo-execute's hub-and-spoke subsection adapts to "the JSON-structured findings text is the handoff to the orchestrator's next reconciliation tick" for research mode.
- **Recursive delegation: not supported** — flat orchestration only; every research Agent invocation originates from this skill's reconciliation loop, never from a worker. Cite the [Claude Code sub-agents docs](https://docs.claude.com/en/docs/claude-code/sub-agents) ("Subagents cannot spawn other subagents"). Use the same URL and quoted phrase as quo-execute Section 3's `#### Recursive delegation: not supported` subsection.

## Key Files

- skills/quo-breakdown-epic/SKILL.md — add three subsections inside the new Section 4 authored by Subtask 2.1

## Acceptance Criteria

- New Section 4 contains an `Anti-pattern: no clock primitives` subsection that names `/loop`, `ScheduleWakeup`, `CronCreate`, and polling as forbidden.
- New Section 4 contains a `Hub-and-spoke via substrate` subsection that explicitly states workers do not message each other and the orchestrator is the only dispatch source.
- New Section 4 contains a `Recursive delegation: not supported` subsection that quotes "Subagents cannot spawn other subagents" and links to `https://docs.claude.com/en/docs/claude-code/sub-agents` (same URL quo-execute uses).
- Wording, structure, and citation match quo-execute Section 3's three corresponding subsections at the research-mode scope (so a reader of either skill recognizes the pattern).
