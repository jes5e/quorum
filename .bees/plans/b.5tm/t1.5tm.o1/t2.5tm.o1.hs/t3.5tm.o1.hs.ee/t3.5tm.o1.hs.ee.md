---
id: t3.5tm.o1.hs.ee
type: t3
title: Final coherence pass on the rewritten SKILL.md
up_dependencies:
- t3.5tm.o1.hs.69
- t3.5tm.o1.hs.38
- t3.5tm.o1.hs.yw
- t3.5tm.o1.hs.rg
- t3.5tm.o1.hs.23
down_dependencies:
- t3.5tm.o1.7t.i5
parent: t2.5tm.o1.hs
created_at: '2026-05-04T22:41:11.143745'
status: done
schema_version: '0.1'
guid: 5tmo1hsee12jd1o32eenwyb1otori23f
reference_materials: null
---
## Context

Subtasks 2.1-2.5 land independent edits on different parts of `skills/bees-breakdown-epic/SKILL.md`. After they all land, a single coherence pass is needed to ensure the file reads as a unified whole — section numbering is contiguous, cross-references between sections all resolve, vocabulary is uniform across sections (e.g., "research-mode Agent" terminology used consistently), and there are no orphan references to deleted concepts (`mode: "plan"`, "team-lead", "Agent Teams", `TeamCreate`, `TeamDelete`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`).

This Subtask is the final Engineer-lane gate before the file is considered done.

## What Needs to Change

Read `skills/bees-breakdown-epic/SKILL.md` end-to-end and fix any cross-section coherence issues introduced by the parallel edits in Subtasks 2.1-2.5:

- Section numbering is contiguous (`### 0`, `### 1`, ..., `### 7`) with no gaps or duplicates.
- Cross-references between sections all resolve (e.g., if Section 5 says "see the dispatch shape established in Section 4", Section 4 actually contains that dispatch-shape prose).
- Vocabulary is uniform across sections — pick one phrasing for the dispatched-research-Agent ("research-mode Agent" vs. "research Agent" vs. "ephemeral research Agent") and use it consistently throughout the file.
- The file's H1 (`# Epic to Tasks`) and the introductory line `Your job is to break down an Epic ticket into Tasks and Subtasks.` are intact.
- The new `## Preconditions` section (Task 1) appears immediately before the `## Workflow` section, with no orphaned content between them.
- Zero occurrences of any of the forbidden tokens introduced by removed substrate: `TeamCreate`, `TeamDelete`, `Agent Teams`, `mode: "plan"`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`. (Task 3's static-verify Subtask covers this same grep externally; this Subtask catches it as part of the coherence pass.)
- The `#### Checklist Before Returning` items match the new ordering — every item still corresponds to an actual step in the rewritten flow.
- Each `### N. <heading>` corresponds to the substantive workflow step its prose describes (no leftover heading prose mismatched to a different concern).

## Key Files

- skills/bees-breakdown-epic/SKILL.md — coherence-pass edits across the whole file

## Acceptance Criteria

- Section numbering is contiguous, no duplicates, no gaps.
- Zero occurrences of forbidden tokens: `TeamCreate`, `TeamDelete`, `Agent Teams`, `mode: "plan"`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`.
- All inter-section references resolve (the section the reference points to actually contains the cited content).
- Dispatched-research-Agent vocabulary is uniform across the file (single phrasing chosen and used consistently).
- The `## Preconditions` section sits immediately before `## Workflow`.
- The `#### Checklist Before Returning` enumerates items that match the rewritten flow (no orphan items, no missing items).
- Reading the file end-to-end produces a coherent narrative: Preconditions → choose model → determine Epic → fetch-and-analyze Epic (with Step 1 marker detection) → break Epic into Tasks → reconciliation-loop research-Agent dispatch → PM-driven Spec Traceability Review with gap-fill loop → ticket creation → set status to ready → commit → next-steps menu.
