---
id: t2.5tm.fy.hj
type: t2
title: 'SDD cleanup: non-feature sections + cold-start-hybrid paragraph'
parent: t1.5tm.fy
children:
- t3.5tm.fy.hj.v9
created_at: '2026-05-05T14:51:50.582519'
status: done
schema_version: '0.1'
guid: 5tmfyhj1icf55jhd73c2o77d8g52cvan
reference_materials: null
---
## Context

The Ephemeral-Agent Orchestration rewrite (parent Bee `b.5tm`) has shipped — sibling Epics A through E (Subagent definitions, the three SKILL.md rewrites, and `bees-setup` cleanup with helper-script deletion) are all `done`. `docs/sdd.md` still describes the pre-rewrite Agent Teams architecture in its non-feature sections and in one paragraph of the per-feature design, so the SDD no longer matches the shipped state. This Task is the SDD half of the final cleanup pass. The README half lives in a sibling Task; the `b.gar` Plan Bee body refresh and PRD/SDD `Status: paused` prefix cleanup live in two further sibling Tasks.

The shipped architecture is documented authoritatively in `docs/sdd.md` `## Per-feature design` `### Feature: Ephemeral-Agent Orchestration` (the planning-stage feature subsection — kept intact by this Task except for the targeted cold-start-hybrid paragraph rewrite below) and in `docs/prd.md` `## Per-feature scope` `### Feature: Ephemeral-Agent Orchestration`. Use those two subsections as the source of truth for the descriptive prose this Task adds back into the non-feature sections.

Three helper scripts remain on disk after Epic E's deletions: `skills/bees-setup/scripts/file_list_resolver.py`, `skills/bees-setup/scripts/detect_fast_path.py`, and `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`. The Tech stack helper-scripts bullet in this Task must list exactly three.

The `agents/<role>.md` definition files shipped by sibling Epic A live at `<repo>/agents/` (and install at `~/.claude/agents/` or `<repo>/.claude/agents/`). The seven role files are: `engineer.md`, `test-writer.md`, `doc-writer.md`, `pm.md`, `code-reviewer.md`, `doc-reviewer.md`, `test-reviewer.md`.

The cumulative-PRD scoping work shipped by `b.mu9` / `b.51d` left two annotations on the SDD that this Task must preserve untouched: each affected skill's Key components bullet carries a "**Marker producer**" or "**Marker consumer**" annotation, and `docs/doc-writing-guide.md` `## The Scoped-marker contract` is already in place and out of scope for this Task. Only Agent-Teams-related prose is removed by this Task; the marker annotations and surrounding cumulative-PRD prose stay verbatim.

## What Needs to Change

All edits land in `docs/sdd.md` and form one git commit.

**Tech stack section.** Drop the experimental Agent Teams requirement language from the Runtime host bullet. Replace with a description of `Agent`-tool background invocations as the orchestration substrate, with the seven `agents/<role>.md` definition files as the role contracts. Update the helper-scripts bullet to list exactly three scripts (the two `bees-setup` scripts plus `scoped_marker_resolver.py`); remove the entries for `force_clean_team.py` and `check_agent_teams.py`. Do not change wording about anything other than the Runtime host and helper-scripts bullets.

**Key components section.** Remove the two bulleted entries for `skills/bees-execute/scripts/force_clean_team.py` and `skills/bees-execute/scripts/check_agent_teams.py`. Add an entry describing the top-level `agents/` directory and what each `<role>.md` file is for (lift the role table from `### Feature: Ephemeral-Agent Orchestration` `**Custom subagent types**` rather than re-deriving). Preserve every existing **Marker producer** / **Marker consumer** annotation and the surrounding prose verbatim — only Agent-Teams content is removed. The `bees-execute` and `bees-fix-issue` bullets must lose their references to spawning a team / `TeamDelete` / `force_clean_team.py` while keeping their marker-consumer prose.

**Team orchestration section → Orchestration in execution skills.** Replace the entire `## Team orchestration in execution skills` section (heading and body) with a new `## Orchestration in execution skills` section that describes: the reconciliation-loop pattern (read state / reconcile / yield); hub-and-spoke as a structural property of ephemeral background subagents (workers don't message each other); cold dispatch for all roles; the TaskList progress UI as the visual progress mechanism; and the explicit absence of clock primitives (`/loop`, `ScheduleWakeup`, `CronCreate`, polling). The section subsumes the still-valid intentional design rationale that survived the substrate change (state-aware dispatch, verbatim ticket-body quoting, time-bounded review iteration) — re-state that rationale in the new prose where it still applies. Remove the `blocked_on` metadata convention and the four-rung graduated-escalation ladder, both of which are gone in the shipped implementation.

**External dependencies section.** Drop the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` requirement from the `Claude Code` bullet. The bees CLI / Python 3 / tmux bullets are unchanged. Do not add new dependencies — the `agents/` directory is install-time content, not a runtime dependency.

**Per-feature `### Feature: Ephemeral-Agent Orchestration` `**Cold-start hybrid (warm vs fresh Agents)**` paragraph.** The currently-documented warm-Agent pattern (named Engineer / Test Writer Agents reused across Subtasks via `SendMessage`) is unavailable without `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, which the rewrite removes. The shipped implementation uses **per-Subtask cold dispatch** for Engineer, Test Writer, and Doc Writer; coherence across dependent Subtasks is preserved through the diff (each later Engineer reads the prior Engineer's commit), matching the hub-and-spoke "diff is the handoff" pattern. Concurrent specialist work is preserved: Engineer-Subtask-N+1 runs in parallel with Test-Writer-Subtask-N once the orchestrator's reconciliation loop ticks past Engineer-Subtask-N's completion. Token cost is bounded by Claude Code's prompt cache (5-min TTL). Replace the warm-Engineer / Test-Writer prose accordingly while keeping the existing PM / Code Reviewer / Doc Reviewer / Test Reviewer cold-fresh prose unchanged. Add a short paragraph documenting the divergence as intentional and reference follow-up Issue **`b.x9w`** (`Re-probe SendMessage-without-Agent-Teams as warm-Agent token-cost optimization`) by name so a future maintainer reading the SDD finds the action path. The Issue is intended to stay open under "periodic re-test" semantics — closed only when (a) a future Claude Code release makes `SendMessage` available without Agent Teams and a follow-up Bee implements warm-Agent dispatch as a token-cost optimization, or (b) we decide to drop the optimization permanently.

**No CLAUDE.md edits.** Verify by running `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' CLAUDE.md` and confirming zero matches before declaring this Task done. Do not edit CLAUDE.md.

## Why

`docs/sdd.md` is the internal architecture doc that future contributors read to understand how the workflow is built. Leaving four sections out-of-date with the shipped state would mislead anyone reading the SDD before re-reading the shipped skills. The cold-start-hybrid paragraph in the per-feature design is the one place inside the SDD's planning record where a maintainer would look to understand the warm-vs-cold trade-off; documenting the divergence and pinning the follow-up Issue ID prevents the warm-Agent optimization from being silently lost.

## Cross-Task contract

This Task ships SDD updates that the per-feature design subsection of `docs/sdd.md` `### Feature: Test strategy for the skills repo` will reference downstream. Sibling Task 4 (PRD/SDD `Status: paused` prefix cleanup) edits a different subsection of `docs/sdd.md` (`### Feature: Test strategy for the skills repo`) and depends on Task 3 (`b.gar` body refresh) but **not** on this Task — the two SDD sections do not overlap. Sibling Task 2 (README cleanup) edits `README.md` and is independent of this Task. The cold-start-hybrid paragraph's reference to Issue **`b.x9w`** is load-bearing for the SDD's coherence: do not generalize the Issue ID, do not invent a new one, and do not drop the reference.

## Success Criteria

- `docs/sdd.md` `## Tech stack` Runtime host bullet no longer mentions `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` or Agent Teams; helper-scripts bullet lists exactly three scripts (no mention of `force_clean_team.py` or `check_agent_teams.py`).
- `docs/sdd.md` `## Key components` no longer has bulleted entries for `force_clean_team.py` or `check_agent_teams.py`; a new entry describes the seven `agents/<role>.md` definition files; every existing **Marker producer** / **Marker consumer** annotation is preserved.
- `docs/sdd.md` `## Team orchestration in execution skills` is renamed to `## Orchestration in execution skills` and rewritten to describe the reconciliation-loop / cold-dispatch / TaskList-UI pattern; `blocked_on` and the four-rung escalation ladder are absent.
- `docs/sdd.md` `## External dependencies` Claude Code bullet no longer references `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`.
- `docs/sdd.md` `### Feature: Ephemeral-Agent Orchestration` `**Cold-start hybrid (warm vs fresh Agents)**` paragraph is rewritten to describe per-Subtask cold dispatch and references Issue `b.x9w` by name.
- `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' CLAUDE.md` returns zero matches (no edit was needed).
- The Lint command from `## Build Commands` (`python -m pyflakes skills/*/scripts/*.py`) still passes.
- One git commit, message subject `Doc: SDD cleanup for Ephemeral-Agent Orchestration shipped state (t2.5tm.fy.<short-suffix>)`.
