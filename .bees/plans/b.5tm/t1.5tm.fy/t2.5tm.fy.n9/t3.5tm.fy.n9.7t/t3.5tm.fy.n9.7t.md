---
id: t3.5tm.fy.n9.7t
type: t3
title: Refresh b.gar Plan Bee body and reconcile up_dependencies
parent: t2.5tm.fy.n9
egg: null
created_at: '2026-05-05T15:03:20.724612'
status: done
schema_version: '0.1'
guid: 5tmfyn97tx4zuiwbs4pwgimcpkst5xap
---

## Context

Plan Bee `b.gar` ("Test strategy for the skills repo") was authored before the Ephemeral-Agent Orchestration rewrite (parent Bee `b.5tm`) was planned and before the abandoned beads-backend Plan Bee (`b.9xr`) was paused. Its current body still describes a three-layer test strategy whose Layer 2.5 (a backend-equivalence harness validating the bees and beads adapter paths in `ticket_backend.py`) presumes a dual-backend world that no longer exists on `main`. The body also wires `up_dependencies: ["b.9xr"]`. Without a refresh, a future `/bees-breakdown-epic b.gar` invocation against the post-orchestration `main` would walk into stale assumptions and propose Tasks against nonexistent `ticket_backend.py` code.

The canonical specs at `docs/prd.md` `### Feature: Test strategy for the skills repo` (line 33 onward) and `docs/sdd.md` `### Feature: Test strategy for the skills repo` (line 88 onward) carry `**Status: paused as of 2026-05-03.**` prefixes whose narrative explicitly points at this Plan Bee body update as the place where the bees-only re-scoping lands. Sibling Task 4 (cleaning up those `Status: paused` prefixes) runs after this Task on the up_dependencies chain.

Current `b.gar` body (verbatim, from `bees show-ticket --ids b.gar`):

> Add a layered test strategy for the bees-workflow repo itself: pytest unit tests on bundled helpers (Layer 1), a structural SKILL.md linter (Layer 2), and a backend-equivalence harness validating the bees and beads adapter paths in ticket_backend.py (Layer 2.5). All three layers wire to a single `make test` entrypoint. Layer 3 (live Claude Code end-to-end smoke) is explicitly out of scope.
>
> See egg (PRD + SDD) for full scope, motivation, acceptance criteria, and architecture. Blocks on b.9xr ("Optional beads backend").

Current `b.gar` `up_dependencies`: `["b.9xr"]`. Current `b.gar` `egg`: `["/Users/jesseg/code/bees-workflow/docs/prd.md", "/Users/jesseg/code/bees-workflow/docs/sdd.md"]` (the egg-resolver's resolved form — preserve the field shape exactly as-is). `b.9xr` is currently `status=ready` but its body documents itself as paused (`**Status: paused as of 2026-05-03.**`).

## What Needs to Change

This Subtask mutates bees state via the `bees update-ticket` CLI; it does not edit any file on disk. The change is committed implicitly as part of the Epic's bees-state commit (the Plans hive's tracked file will show the new body in `git status`).

**1. Author the new body.** Write the new body to a temp file under the scratch namespace per the project's scratch-file convention (CLAUDE.md `## Scratch-file convention`). Required shape:

```bash
# POSIX (bash / zsh):
mkdir -p /tmp/.bees-workflow
# Then write the body to /tmp/.bees-workflow/bees-bee-body-<short-suffix>.md via the Write tool.
```

```powershell
# Windows (PowerShell):
New-Item -ItemType Directory -Force -Path "$env:TEMP\.bees-workflow" | Out-Null
# Then write the body to $env:TEMP\.bees-workflow\bees-bee-body-<short-suffix>.md via the Write tool.
```

Do NOT delete the scratch file after the bees command exits — the convention forbids cleanup steps.

The new body must:

- **Open with one short paragraph** acknowledging that the Ephemeral-Agent Orchestration rewrite (parent Bee `b.5tm`) has shipped on `main`, and the beads-backend feature (Plan Bee `b.9xr`) remains paused. This sets the architectural context — the bees-workflow repo is now a bees-only project running on top of Claude Code's stable Agent tool.
- **State the test-strategy goal** preserved from the original body: a layered automated-testing approach for the bees-workflow repo itself, replacing the current single-check `python -m pyflakes` floor.
- **Re-state the layered approach for the bees-only world**:
  - **Layer 1** (per-helper pytest unit tests on every bundled Python helper) still applies. The bundled helpers today are `skills/bees-setup/scripts/file_list_resolver.py`, `skills/bees-setup/scripts/detect_fast_path.py`, and `skills/bees-breakdown-epic/scripts/scoped_marker_resolver.py`.
  - **Layer 2** (a structural `SKILL.md` linter validating the project's design rules from CLAUDE.md — language-agnostic, POSIX + Windows PowerShell, project-neutral, plus the scratch-file convention) still applies.
  - **Layer 2.5** (backend-equivalence harness): pick one disposition explicitly and state it. Recommended wording: "deferred — there is no second backend on `main` to validate against. If `b.9xr` ever resumes, Layer 2.5 comes back into scope at that point; until then, this layer is out of scope for `b.gar`." (The Epic body offers "deferred or cut" as the choice; deferred-pending-`b.9xr` preserves optionality and matches the paused-not-abandoned status of `b.9xr`.)
  - **Layer 3** (live Claude Code end-to-end smoke harness) remains out of scope as before.
- **Reference the canonical spec source**: `docs/prd.md` and `docs/sdd.md` `### Feature: Test strategy for the skills repo` subsections, linked via the `egg` field. Note that those subsections currently carry `**Status: paused as of 2026-05-03.**` prefixes pointing at this body update as the bees-only re-scoping; sibling Task 4 cleans those prefixes up.
- Replace the original body's closing line `Blocks on b.9xr ("Optional beads backend").` with prose appropriate to the keep-or-clear `up_dependencies` decision (see step 2).

The body must be capped at 10,000 characters (the `--body-file` limit per `bees update-ticket --help`). The current body is well under that, and the refresh is content-shaped not size-shaped, so this should not be a constraint in practice.

**2. Decide keep-or-clear on `up_dependencies: ["b.9xr"]`.** The Epic body asks the executor to make a deliberate call and document it in the commit message. Recommended call: **clear it**. Rationale to record in the commit message: the refreshed body cuts/defers Layer 2.5 unconditionally for the bees-only world; the body no longer describes Layer 2.5 as something that requires `b.9xr` first; therefore `b.9xr`'s state no longer gates `b.gar`. A future `/bees-breakdown-epic b.gar` should be free to run against `main` without seeing a phantom `b.9xr` blocker. If `b.9xr` ever resumes and Layer 2.5 comes back into scope, the dependency can be re-added at that point.

If the executor instead chooses to keep the dependency, the rationale to record is: `b.9xr` remains a meaningful blocker because Layer 2.5 is *deferred-pending-`b.9xr`* (not cut outright), and a planning run that walks `up_dependencies` should still surface the link.

**3. Apply the body update.** One `bees update-ticket` invocation. Two shapes depending on the keep-or-clear call:

```bash
# POSIX (bash / zsh) — clear up_dependencies:
bees update-ticket --ids b.gar --body-file /tmp/.bees-workflow/bees-bee-body-<short-suffix>.md --up-deps null
```

```bash
# POSIX (bash / zsh) — keep up_dependencies (do not pass --up-deps):
bees update-ticket --ids b.gar --body-file /tmp/.bees-workflow/bees-bee-body-<short-suffix>.md
```

```powershell
# Windows (PowerShell) — clear up_dependencies:
bees update-ticket --ids b.gar --body-file "$env:TEMP\.bees-workflow\bees-bee-body-<short-suffix>.md" --up-deps null
```

```powershell
# Windows (PowerShell) — keep up_dependencies (do not pass --up-deps):
bees update-ticket --ids b.gar --body-file "$env:TEMP\.bees-workflow\bees-bee-body-<short-suffix>.md"
```

The `--up-deps null` form is the documented clear-array shape per `bees update-ticket --help` ("Pass null to JSON fields to clear them"). `--up-deps '[]'` would set the field to an empty list rather than clearing it — `null` is the explicit-clear shape and is preferred.

**4. Preserve the `egg` field.** Do NOT pass `--egg`. The egg field's current value (`["/Users/jesseg/code/bees-workflow/docs/prd.md", "/Users/jesseg/code/bees-workflow/docs/sdd.md"]`) is the egg-resolver's resolved form and must remain unchanged — the PRD/SDD `### Feature: Test strategy for the skills repo` subsections are still the canonical spec source for the eventual breakdown.**5. Verify.** Run `bees show-ticket --ids b.gar` and confirm:

- The new body appears verbatim (orchestration-rewrite acknowledgment paragraph; bees-only Layer 1 / Layer 2 / Layer 2.5 disposition / Layer 3 re-statement; PRD/SDD reference).
- The `egg` field is unchanged (still the two-element list above).
- The `up_dependencies` field reflects the keep-or-clear decision (`[]` if cleared via `--up-deps null`; `["b.9xr"]` if kept).
- The `parent`, `children`, `tags`, `ticket_status`, `created_at`, `schema_version`, and `guid` fields are unchanged.

## Key Files

- `/Users/jesseg/code/bees-workflow/docs/prd.md` lines 33-61 — `### Feature: Test strategy for the skills repo` (canonical PRD spec; the new body should reference this section by name).
- `/Users/jesseg/code/bees-workflow/docs/sdd.md` lines 88-158 — `### Feature: Test strategy for the skills repo` (canonical SDD spec; the new body should reference this section by name).
- `/Users/jesseg/code/bees-workflow/CLAUDE.md` `## Scratch-file convention` — governs the `/tmp/.bees-workflow/` namespace and the no-cleanup rule.
- No on-disk source-code or skill-prose files are edited by this Subtask. The `bees update-ticket` invocation mutates the Plans hive's tracked file, which `git status` will show.

## Acceptance Criteria

- `bees show-ticket --ids b.gar` returns a body that opens with the orchestration-rewrite acknowledgment and includes the bees-only re-statement of Layer 1 / Layer 2 / Layer 2.5 (deferred or cut, disposition stated explicitly) / Layer 3 (out of scope).
- The new body references `docs/prd.md` and `docs/sdd.md` `### Feature: Test strategy for the skills repo` as the canonical spec source.
- The new body no longer mentions `ticket_backend.py`, the bees+beads adapter pair, or any dual-backend Layer 2.5 architecture.
- `b.gar`'s `egg` field still equals `["/Users/jesseg/code/bees-workflow/docs/prd.md", "/Users/jesseg/code/bees-workflow/docs/sdd.md"]` (unchanged).
- `b.gar`'s `up_dependencies` field reflects the deliberate keep-or-clear decision.
- `b.gar`'s `parent`, `children`, `tags`, `ticket_status`, `created_at`, `schema_version`, and `guid` fields are unchanged.
- The scratch body file lives under `/tmp/.bees-workflow/` (POSIX) or `$env:TEMP\.bees-workflow\` (Windows) and is NOT deleted after the bees command exits.
- The Epic's commit message documents the keep-or-clear call on `up_dependencies` with rationale.
- The Plans hive's tracked file shows the body update in `git status` and is included in the Epic-close commit.
