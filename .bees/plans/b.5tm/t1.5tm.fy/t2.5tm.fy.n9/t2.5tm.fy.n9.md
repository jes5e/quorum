---
id: t2.5tm.fy.n9
type: t2
title: Refresh b.gar Plan Bee body for post-orchestration bees-only world
down_dependencies:
- t2.5tm.fy.mf
parent: t1.5tm.fy
children:
- t3.5tm.fy.n9.7t
egg: null
created_at: '2026-05-05T14:53:25.242717'
status: ready
schema_version: '0.1'
guid: 5tmfyn9vvnyacu95jaipnrm95bqoec3w
---

## Context

Plan Bee `b.gar` ("Test strategy for the skills repo") was authored before the Ephemeral-Agent Orchestration rewrite (parent Bee `b.5tm`) was planned and before the abandoned beads-backend branch (`b.9xr`) was paused. Its current body still describes a three-layer test strategy whose Layer 2.5 (a backend-equivalence harness validating the bees and beads adapter paths in `ticket_backend.py`) presumes a dual-backend world that no longer exists. The body is also wired to block on `b.9xr`. Without a refresh, a future `/bees-breakdown-epic b.gar` invocation would walk into stale assumptions and propose Tasks for code (`ticket_backend.py`) that doesn't exist on `main`.

The Epic body for `t1.5tm.fy` prescribes the refresh content explicitly: acknowledge the architecture change (orchestration rewrite landed, beads paused); preserve the test-strategy goal; re-state the layered approach for the bees-only world (Layer 1 helper unit tests and Layer 2 SKILL.md linter still apply; Layer 2.5 backend-equivalence harness is deferred or cut since there's no second backend; Layer 3 live Claude Code end-to-end smoke remains out of scope); note that `b.gar`'s PRD/SDD entries' "Status: paused" prefixes can stay with a small content tweak pointing at the updated body, or be removed entirely once the body update lands.

The current `b.gar` body for reference (verbatim, from `bees show-ticket --ids b.gar`):

> Add a layered test strategy for the bees-workflow repo itself: pytest unit tests on bundled helpers (Layer 1), a structural SKILL.md linter (Layer 2), and a backend-equivalence harness validating the bees and beads adapter paths in ticket_backend.py (Layer 2.5). All three layers wire to a single `make test` entrypoint. Layer 3 (live Claude Code end-to-end smoke) is explicitly out of scope.
>
> See egg (PRD + SDD) for full scope, motivation, acceptance criteria, and architecture. Blocks on b.9xr ("Optional beads backend").

`b.gar`'s `up_dependencies` field currently references `b.9xr`. The Epic body does not explicitly prescribe whether to clear that dependency, but a future planning run that walks `up_dependencies` to discover blockers would still see `b.9xr` even after the body refresh. The author of this Task should make a deliberate call: keep the `up_dependencies` link if `b.9xr` itself remains a meaningful blocker (it is paused, not abandoned, and the refreshed body should describe the bees-only Layer 2.5 disposition independently of `b.9xr`'s state); clear it if the new body cuts/defers Layer 2.5 unconditionally and `b.9xr`'s state no longer gates `b.gar`. Document the call in the commit message.

## What Needs to Change

This Task mutates bees state via the `bees update-ticket` CLI; it does not edit any file on disk. The change is therefore committed implicitly as part of the next bees-state commit (the new ticket files this Epic produces, plus any updates to existing tickets).

**Author the new `b.gar` body.** The new body should:

- Open with one short paragraph acknowledging that the orchestration rewrite (parent Bee `b.5tm`) has shipped and the beads-backend feature (Plan Bee `b.9xr`) remains paused.
- State the test-strategy goal (a layered automated-testing approach for the bees-workflow repo itself, replacing the current single-check `python -m pyflakes` floor) — preserved from the original body.
- Re-state the layered approach for the bees-only world. The shape: Layer 1 (per-helper pytest unit tests on every bundled Python helper) and Layer 2 (a structural `SKILL.md` linter validating the project's design rules from `CLAUDE.md`) both still apply. Layer 2.5 (backend-equivalence harness) is deferred or cut — there is no second backend to validate against; the body should pick one disposition and state it explicitly. Layer 3 (live Claude Code end-to-end smoke) remains out of scope as before.
- Reference the canonical spec source — the `### Feature: Test strategy for the skills repo` subsection of `docs/prd.md` and `docs/sdd.md` (linked via the `egg` field) — and note that the `Status: paused` prefix on each subsection points at this body for the bees-only re-scoping.
- Decide whether to keep or clear `up_dependencies: ["b.9xr"]` and document the call in the commit message.

**Apply the body update.** Write the new body to a temp file under `/tmp/.bees-workflow/` (POSIX) or `$env:TEMP\.bees-workflow\` (Windows) and call `bees update-ticket --id b.gar --body-file <path>`. If `up_dependencies` is being cleared, do that in the same `bees update-ticket` invocation via `--up-deps '[]'` (or whatever the supported clear-array shape is for that field — check `bees update-ticket --help`). If kept, do not pass `--up-deps`.

**Preserve the egg.** Do not touch `b.gar`'s `egg` field. The PRD/SDD `### Feature: Test strategy for the skills repo` subsections are still the canonical spec source for the eventual breakdown.

**Verification.** After the update, run `bees show-ticket --ids b.gar` and confirm the new body appears verbatim, the `egg` field is unchanged (still points at the PRD and SDD), and `up_dependencies` matches the deliberate call documented above.

## Why

Without this refresh, `b.gar` cannot be picked up by a future `/bees-breakdown-epic` invocation against the post-orchestration `main` without re-derivation: a Task author would either (a) propose Tasks against the dual-backend Layer 2.5 design (chasing nonexistent `ticket_backend.py` code), or (b) stall and ask the human caller to re-scope mid-breakdown. The body refresh is what makes `b.gar` resumable from a clean slate.

## Cross-Task contract

This Task is the prerequisite for sibling Task 4 (PRD/SDD `Status: paused` prefix cleanup). Task 4's edits to the two `### Feature: Test strategy for the skills repo` subsections reference the refreshed body — Task 4 must run after this Task completes. The `up_dependencies` between the two Tasks is wired so the executor processes them in order. The two SDD-touching Tasks (this Task does not touch SDD; sibling `t2.5tm.fy.hj` and Task 4 both edit different SDD subsections) do not collide on the same lines: `t2.5tm.fy.hj` edits non-feature sections plus the `### Feature: Ephemeral-Agent Orchestration` cold-start-hybrid paragraph; Task 4 edits the `### Feature: Test strategy for the skills repo` subsection; this Task edits no file on disk.

## Success Criteria

- `bees show-ticket --ids b.gar` returns a body that opens with the orchestration-rewrite acknowledgment and includes the bees-only re-statement of Layer 1 / Layer 2 / Layer 2.5 (deferred or cut) / Layer 3 (out of scope).
- The new body references `docs/prd.md` and `docs/sdd.md` `### Feature: Test strategy for the skills repo` as the canonical spec source.
- `b.gar`'s `egg` field still points at `["/Users/jesseg/code/bees-workflow/docs/prd.md", "/Users/jesseg/code/bees-workflow/docs/sdd.md"]` (unchanged from before this Task) — note the absolute paths are the egg-resolver's resolved form; preserve whatever shape the field currently has.
- `b.gar`'s `up_dependencies` field reflects the deliberate keep-or-clear decision; the commit message documents the call.
- The new body is committed to the Plans hive's tracked file alongside any other ticket-state changes the Epic produces (ticket files live inside this repo, so `git status` will show the change).
- One git commit at Epic close, message subject `Refresh b.gar Plan Bee body for post-orchestration bees-only world (t2.5tm.fy.<short-suffix>)`. The body of the commit message describes the keep-or-clear call on `up_dependencies`.
