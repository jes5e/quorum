---
id: t2.5tm.fy.q2
type: t2
title: 'README cleanup: delete Agent Teams + Display backend sections, add replacement sentence'
parent: t1.5tm.fy
children:
- t3.5tm.fy.q2.7s
egg: null
created_at: '2026-05-05T14:52:32.554742'
status: done
schema_version: '0.1'
guid: 5tmfyq2v5uc54jprrc7wqw2h2okxvpm9
---

## Context

The Ephemeral-Agent Orchestration rewrite (parent Bee `b.5tm`) has shipped — sibling Epics A through E are all `done` and the experimental Agent Teams substrate is gone. `README.md` still carries two top-level setup sections (`## Required: enable Agent Teams`, `## Display backend`) that describe the old substrate, plus stale references in the skill table and the helper-scripts paragraph. This Task is the README half of the final cleanup pass; the SDD half lives in sibling Task `t2.5tm.fy.hj`.

The cumulative-PRD scoping work shipped by `b.51d` may have added README mentions of the `--feature` flag and/or the `Scoped to ...` marker pattern (already-shipped feature). This Task's deletions and edits must not also delete those additions — verify before applying each change.

The README's workflow diagram lives near the top of the file, immediately after the introductory paragraph. The Epic body and the PRD's acceptance criteria both prescribe the exact replacement sentence to add near it: *"The skills orchestrate work via Claude Code's ephemeral background subagents — no special setup is required beyond the bees CLI and Claude Code itself."* Use that wording verbatim.

## What Needs to Change

All edits land in `README.md` and form one git commit.

**Delete `## Required: enable Agent Teams` section** — heading and body together. The section describes the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var and the `/bees-setup` configuration prompt for it. Both are gone in the shipped implementation.

**Delete `## Display backend` section** — heading and body together. The section describes the `teammateMode` setting, the `"in-process"` / `"tmux"` / `"auto"` choices, and the iTerm2 hard-prompt workaround. None of this applies post-rewrite.

**Add one replacement sentence near the workflow diagram.** Insert: *"The skills orchestrate work via Claude Code's ephemeral background subagents — no special setup is required beyond the bees CLI and Claude Code itself."* The exact placement (immediately under the workflow-diagram code block, or appended to the introductory paragraph above the diagram) is the editor's call — pick the spot that reads cleanly.

**Update the skill table's `/bees-setup` row.** The current row description ends with "Agent Teams + display backend, optional PRD/SDD bootstrap from existing codebase" — drop the "Agent Teams + display backend" phrase and re-flow the sentence so the bootstrap clause and the new-machine fast-path clause read cleanly without it.

**Update the helper-scripts paragraph.** The paragraph currently lists `force_clean_team.py` as one of the example helpers. Replace with one of the helpers that still exists (`detect_fast_path.py` is a good substitute since the existing two examples are `file_list_resolver.py` and `scoped_marker_resolver.py`); do not list a deleted helper. Verify by checking the example helpers against the on-disk set (`file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`).

**Preserve `b.51d` additions.** Before applying any deletion, grep `README.md` for `--feature` and for `Scoped to` to confirm whether the cumulative-PRD scoping work added README content. Any matches must survive untouched. The two section deletions named above are localized and should not collide with `--feature` / scoping prose; the targeted preservation grep is the safety net.

**No CLAUDE.md edits.** The Epic body explicitly notes that the target repo's CLAUDE.md does not carry Agent Teams precondition prose; verify by `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' CLAUDE.md` returning zero matches before declaring this Task done.

## Why

`README.md` is the customer-facing onboarding doc. New users read it top-to-bottom to learn what the skills require — leaving the Agent Teams setup ceremony in place would steer them through a broken setup that no longer matches the shipped skills. The replacement sentence near the workflow diagram preserves the customer's mental model: orchestration still happens; it's now a built-in capability rather than an opt-in experimental feature.

## Cross-Task contract

This Task is independent of the SDD cleanup (sibling `t2.5tm.fy.hj`), the `b.gar` body refresh (sibling Task 3), and the `Status: paused` prefix cleanup (sibling Task 4). README and SDD edits target different files; the b.gar work targets bees state and PRD/SDD `### Feature: Test strategy` subsections, none of which this Task touches.

## Success Criteria

- `README.md` no longer contains a `## Required: enable Agent Teams` heading or its body.
- `README.md` no longer contains a `## Display backend` heading or its body.
- `README.md` carries the new sentence "The skills orchestrate work via Claude Code's ephemeral background subagents — no special setup is required beyond the bees CLI and Claude Code itself." near the workflow diagram (verbatim wording).
- The `/bees-setup` row of the skill table no longer references "Agent Teams + display backend"; the row still reads cleanly.
- The helper-scripts paragraph examples list only helpers that exist on disk (`file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`); no reference to `force_clean_team.py` or `check_agent_teams.py`.
- Any `b.51d` additions (`--feature`, `Scoped to`) are preserved verbatim.
- `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' README.md` returns zero matches.
- `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' CLAUDE.md` returns zero matches (no edit was needed).
- One git commit, message subject `Doc: README cleanup for Ephemeral-Agent Orchestration shipped state (t2.5tm.fy.<short-suffix>)`.
