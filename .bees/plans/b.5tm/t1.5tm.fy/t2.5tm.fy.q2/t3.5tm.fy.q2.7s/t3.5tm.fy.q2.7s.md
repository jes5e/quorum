---
id: t3.5tm.fy.q2.7s
type: t3
title: Apply README cleanup edits for shipped Ephemeral-Agent Orchestration
parent: t2.5tm.fy.q2
created_at: '2026-05-05T15:02:49.182662'
status: done
schema_version: '0.1'
guid: 5tmfyq27s1mfmg8zpamq2vbxsqo6qdn8
reference_materials: null
---
## Context

Parent Task `t2.5tm.fy.q2` under Epic `t1.5tm.fy` (Plan Bee `b.5tm`, Ephemeral-Agent Orchestration). The rewrite has shipped and the experimental Agent Teams substrate is gone, but `/Users/jesseg/code/bees-workflow/README.md` still carries two top-level setup sections describing the old substrate plus stale references in the skill table and the helper-scripts paragraph. This Subtask is the entire README cleanup pass — one file, one commit.

The parent Task body prescribes the exact replacement sentence to add near the workflow diagram and explicitly warns about preserving `b.51d` additions (`--feature`, `Scoped to`) that may already exist in the README. A pre-edit grep step is mandatory and must run before any deletions are applied.

CLAUDE.md `## Documentation Locations` resolves `Customer-facing docs` to `README.md`, confirming this is the right file. CLAUDE.md itself does not carry Agent Teams precondition prose (verified — `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' CLAUDE.md` returns zero matches at research time), so no CLAUDE.md edit is required.

## What Needs to Change

All edits land in `/Users/jesseg/code/bees-workflow/README.md` and form one git commit.

**Step 1 — Pre-edit safety grep (mandatory; do not skip).** Before applying any deletion, run:

```bash
# POSIX (bash / zsh):
grep -nE -- '--feature|Scoped to' /Users/jesseg/code/bees-workflow/README.md
```

```powershell
# Windows (PowerShell):
Select-String -Path C:\path	o\README.md -Pattern '--feature|Scoped to'
```

Record every line number that matches. These lines are `b.51d` additions (cumulative-PRD scoping work) and must survive every subsequent edit untouched. At research time, two matches are known: line 32 (intro paragraph mentioning `Scoped to ...` and `--feature`) and line 116 (`/bees-plan-from-specs` skill-table row mentioning `--feature`). Neither line falls inside the deletion ranges, but the grep is the safety net.

**Step 2 — Delete `## Required: enable Agent Teams` section (heading + body together).** The section currently spans roughly lines 43-47 and describes the `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var and the `/bees-setup` configuration prompt for it. Both are gone in the shipped implementation. Delete the heading and the entire body through (but not including) the next top-level heading.

**Step 3 — Delete `## Display backend` section (heading + body together).** The section currently spans roughly lines 49-59 and describes the `teammateMode` setting, the `"in-process"` / `"tmux"` / `"auto"` choices, and the iTerm2 hard-prompt workaround. None of this applies post-rewrite. Delete the heading and the entire body through (but not including) the next top-level heading (`## Install`).

**Step 4 — Add one replacement sentence near the workflow diagram.** Insert verbatim, exactly as written, no paraphrasing:

> The skills orchestrate work via Claude Code's ephemeral background subagents — no special setup is required beyond the bees CLI and Claude Code itself.

The workflow diagram is the fenced code block near the top of the file (currently lines 5-22), immediately after the introductory paragraph and before `## Why this exists`. Pick whichever placement reads cleanly — appended to the introductory paragraph above the diagram, or as a standalone sentence immediately under the diagram code block. Editor's call; pick the spot that flows best.

**Step 5 — Update the `/bees-setup` row of the skill table.** The current row description (line 114) ends with "Agent Teams + display backend, optional PRD/SDD bootstrap from existing codebase". Drop the "Agent Teams + display backend" phrase and re-flow the sentence so the bootstrap clause and the new-machine fast-path clause read cleanly without it. Do not introduce new claims — only remove the now-stale phrase.

**Step 6 — Update the bundled-helper-scripts paragraph.** The paragraph at line 153 currently lists `force_clean_team.py` as one of three example helpers. Replace `force_clean_team.py` with `detect_fast_path.py` so the example list matches the on-disk set: `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`. Verify by listing `/Users/jesseg/code/bees-workflow/skills/bees-setup/scripts/` and `/Users/jesseg/code/bees-workflow/skills/bees-breakdown-epic/scripts/` before naming substitutes — do not list a deleted helper.

**Step 7 — Post-edit verification greps.** After all edits land, run:

```bash
# POSIX (bash / zsh):
grep -niE 'agent.teams|teammateMode|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' /Users/jesseg/code/bees-workflow/README.md
grep -niE 'agent.teams|teammateMode|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' /Users/jesseg/code/bees-workflow/CLAUDE.md
grep -nE 'force_clean_team|check_agent_teams' /Users/jesseg/code/bees-workflow/README.md
grep -nE -- '--feature|Scoped to' /Users/jesseg/code/bees-workflow/README.md
```

The first three must return zero matches. The fourth must return the same line set (or content-equivalent set, accounting for renumbering after deletions) recorded in Step 1 — every `b.51d` addition must still be present.

**Step 8 — Commit.** Stage `README.md` only and commit with the subject line specified in the parent Task's success criteria: `Doc: README cleanup for Ephemeral-Agent Orchestration shipped state (t2.5tm.fy.<short-suffix>)` where `<short-suffix>` is this Subtask's bees ID short suffix.

## Key Files

- `/Users/jesseg/code/bees-workflow/README.md` — only file edited; receives Steps 2-6 above
- `/Users/jesseg/code/bees-workflow/CLAUDE.md` — read-only check (Step 7 verification grep); no edit
- `/Users/jesseg/code/bees-workflow/skills/bees-setup/scripts/` — read-only (verify on-disk helper names for Step 6)
- `/Users/jesseg/code/bees-workflow/skills/bees-breakdown-epic/scripts/` — read-only (verify `scoped_marker_resolver.py` is still on disk)

## Acceptance Criteria

- Step 1 pre-edit grep was run and matching lines (`--feature`, `Scoped to`) were recorded before any deletion was applied.
- `README.md` no longer contains a `## Required: enable Agent Teams` heading or its body.
- `README.md` no longer contains a `## Display backend` heading or its body.
- `README.md` carries the new sentence "The skills orchestrate work via Claude Code's ephemeral background subagents — no special setup is required beyond the bees CLI and Claude Code itself." near the workflow diagram, verbatim wording with the em dash intact.
- The `/bees-setup` row of the skill table no longer references "Agent Teams + display backend"; the row still reads cleanly as a single sentence describing what `/bees-setup` does.
- The bundled-helper-scripts paragraph examples list only helpers that exist on disk: `file_list_resolver.py`, `detect_fast_path.py`, `scoped_marker_resolver.py`. No reference to `force_clean_team.py` or `check_agent_teams.py` remains.
- All `b.51d` additions (`--feature`, `Scoped to`) recorded in Step 1 are preserved verbatim.
- `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' README.md` returns zero matches.
- `grep -i 'agent.teams\|teammateMode\|CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' CLAUDE.md` returns zero matches (no edit was made; this is a no-op confirmation).
- One git commit was created, message subject `Doc: README cleanup for Ephemeral-Agent Orchestration shipped state (t2.5tm.fy.<short-suffix>)`. Only `README.md` is staged in that commit.
