---
id: t3.5tm.fy.mf.jm
type: t3
title: Update or remove Status:paused prefix in PRD/SDD Test-strategy subsections
parent: t2.5tm.fy.mf
egg: null
created_at: '2026-05-05T15:03:00.429281'
status: done
schema_version: '0.1'
guid: 5tmfymfjmzuauke6jyf3s1rm7mcr73hc
---

## Context

Both `docs/prd.md` `### Feature: Test strategy for the skills repo` (line 35) and `docs/sdd.md` `### Feature: Test strategy for the skills repo` (line 90) open with a parallel "**Status: paused as of 2026-05-03.**" prefix paragraph. Both prefixes describe `b.gar`'s Plan Bee body refresh as a future event ("is updated at the conclusion of the Ephemeral-Agent Orchestration feature"). After sibling Task `t2.5tm.fy.n9` (refresh `b.gar` body) lands, that future-tense framing is stale — the body refresh has happened.

The Epic body for `t1.5tm.fy` says: "`b.gar`'s PRD entry 'Status: paused' prefix updated or removed once the body is current." The choice between updating the prefix (keeping the paused framing but acknowledging the refresh has landed) and removing it entirely is the editor's call. Whichever choice is made must apply symmetrically to both PRD and SDD subsections — they have parallel wording today and must stay parallel.

This Subtask's parent Task is wired with `up_dependencies` to `t2.5tm.fy.n9`, so the executor can trust the prerequisite (refreshed `b.gar` body) is already in place when this Subtask runs.

## What Needs to Change

One git commit, two files.

**`docs/prd.md` `### Feature: Test strategy for the skills repo` opening prefix paragraph (currently around line 35).** Either:

- **Update**: rewrite to acknowledge `b.gar`'s body refresh has landed (it's no longer a planned future event) and to note that the bees-only re-scoping the body now describes is what governs resume planning. Keep the "paused" framing if the feature itself remains paused — the orchestration rewrite shipping does not auto-unpause Test strategy.
- **Remove**: drop the prefix paragraph entirely if it no longer carries useful information now that `b.gar`'s body is current.

**`docs/sdd.md` `### Feature: Test strategy for the skills repo` opening prefix paragraph (currently around line 90).** Apply the same choice (update vs. remove) symmetrically. Both prefixes use parallel wording today; the edit must keep them parallel (both updated, or both removed).

**Preserve the rest of each subsection.** Only the opening prefix paragraph is in scope. The downstream content of each subsection — `**What.**`, `**Why.**`, `**Acceptance criteria.**`, `**Architecture.**`, `**Sequencing.**`, `**Decomposition.**`, etc. — describes the originally-planned dual-backend test strategy and stays verbatim. The refreshed `b.gar` body in sibling Task `t2.5tm.fy.n9` already notes the subsection content "may be re-scoped on resume"; re-scoping is a future Test-strategy resume's job, not this Subtask's.

**Document the call in the commit message.** The commit message body must record the update-vs-remove decision and the reasoning so a future maintainer can trace why the choice was made.

## Key Files

- `/Users/jesseg/code/bees-workflow/docs/prd.md` — edit (or remove) the opening "Status: paused as of 2026-05-03." paragraph of `### Feature: Test strategy for the skills repo`. Leave the rest of the subsection unchanged.
- `/Users/jesseg/code/bees-workflow/docs/sdd.md` — apply the same choice symmetrically to the opening "Status: paused as of 2026-05-03." paragraph of `### Feature: Test strategy for the skills repo`. Leave the rest of the subsection unchanged.

## Acceptance Criteria

- `docs/prd.md` `### Feature: Test strategy for the skills repo` opening prefix paragraph is either updated to reflect `b.gar`'s now-current body or removed entirely.
- `docs/sdd.md` `### Feature: Test strategy for the skills repo` opening prefix paragraph applies the same choice symmetrically (both updated, or both removed — not one of each).
- The downstream subsection content in both files is unchanged byte-for-byte; only the opening prefix paragraph is touched.
- No other `### Feature:` subsections in either file are modified.
- The commit message body documents the update-vs-remove call and the reasoning.
- One git commit, message subject `Doc: PRD/SDD Test-strategy 'Status: paused' prefix cleanup post-b.gar refresh (t2.5tm.fy.<short-suffix>)`.
