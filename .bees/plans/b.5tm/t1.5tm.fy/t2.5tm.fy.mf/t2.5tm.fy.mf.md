---
id: t2.5tm.fy.mf
type: t2
title: PRD/SDD Test-strategy Status:paused prefix cleanup post-b.gar refresh
up_dependencies:
- t2.5tm.fy.n9
parent: t1.5tm.fy
children:
- t3.5tm.fy.mf.jm
egg: null
created_at: '2026-05-05T14:53:56.505825'
status: done
schema_version: '0.1'
guid: 5tmfymfrdjhghk713pt4uppdh34bvtvv
---

## Context

Both `docs/prd.md` `### Feature: Test strategy for the skills repo` and `docs/sdd.md` `### Feature: Test strategy for the skills repo` open with a "**Status: paused as of 2026-05-03.**" prefix paragraph that explains the feature was sequenced after the (also paused) beads-backend feature and notes that `b.gar`'s Plan Bee body will be refreshed at the conclusion of the Ephemeral-Agent Orchestration feature before Test strategy resumes. After sibling Task 3 (`t2.5tm.fy.n9`) refreshes the `b.gar` body, that prefix is stale: it still describes the body refresh as a future event.

The Epic body for `t1.5tm.fy` says explicitly: "**`b.gar`'s PRD entry 'Status: paused'** prefix updated or removed once the body is current." The choice between updating the prefix (with a small content tweak pointing at the now-current body) and removing the prefix entirely is the editor's call. Keep the choice symmetric across the PRD and SDD subsections — both edit at once.

This Task depends on `t2.5tm.fy.n9` (refresh `b.gar` body). The dependency is wired via `up_dependencies` so the executor processes them in order; do not attempt this Task before the body refresh has landed.

## What Needs to Change

Both edits land in one git commit, across `docs/prd.md` and `docs/sdd.md`.

**`docs/prd.md` `### Feature: Test strategy for the skills repo` opening "Status: paused" paragraph.** Either:

- **Update**: rewrite the paragraph to acknowledge the `b.gar` body refresh has landed (it's no longer a planned future event) and to note that the bees-only re-scoping the body now describes is what governs resume planning. Keep the "paused" framing if the feature itself remains paused — the orchestration rewrite shipping does not auto-unpause Test strategy.
- **Remove**: drop the prefix paragraph entirely if the editor judges it no longer carries useful information now that `b.gar`'s body is current.

**`docs/sdd.md` `### Feature: Test strategy for the skills repo` opening "Status: paused" paragraph.** Apply the same choice (update vs. remove) symmetrically. The PRD and SDD prefixes have parallel wording today; whichever choice this Task makes, both subsections must reflect it consistently.

**Preserve subsection content.** Only the opening prefix paragraph is in scope. The downstream body of each subsection (`**What.**`, `**Why.**`, `**Acceptance criteria.**`, `**Architecture.**`, etc.) is the originally-planned dual-backend test strategy and must stay verbatim — the refreshed `b.gar` body in sibling Task 3 already notes that the subsection content "may be re-scoped on resume." Re-scoping is a future Task strategy resume's job, not this Task's.

**Decision rationale in the commit message.** Document the update-vs-remove call in the commit message body so a future maintainer can see why the choice was made.

## Why

The "Status: paused" prefix paragraphs were added when the body refresh was a planned future event. After Task 3 lands, leaving them unedited means the PRD and SDD describe the body refresh in the future tense even though it has happened. Updating or removing the prefixes is a small consistency pass that prevents confusion for any future contributor reading the PRD/SDD before reading `b.gar`.

## Cross-Task contract

This Task depends on sibling Task 3 (`t2.5tm.fy.n9`, "Refresh b.gar Plan Bee body for post-orchestration bees-only world") via `up_dependencies`: the prefix update reasonably can only land after the body update it points at is current. This Task is independent of the SDD non-feature cleanup (sibling `t2.5tm.fy.hj`) — the two SDD edits target different `### Feature:` subsections and do not collide. The README cleanup (sibling `t2.5tm.fy.q2`) is also independent.

## Success Criteria

- `docs/prd.md` `### Feature: Test strategy for the skills repo` opening prefix paragraph is either updated to reflect `b.gar`'s now-current body or removed entirely.
- `docs/sdd.md` `### Feature: Test strategy for the skills repo` opening prefix paragraph applies the same choice symmetrically.
- The downstream subsection content in both files is unchanged (only the opening prefix paragraph is touched).
- The commit message body documents the update-vs-remove call and the reasoning.
- One git commit, message subject `Doc: PRD/SDD Test-strategy 'Status: paused' prefix cleanup post-b.gar refresh (t2.5tm.fy.<short-suffix>)`.
