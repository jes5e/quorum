---
id: t3.55r.9d.ds.is
type: t3
title: 'Verify the Task: full test suite, lint, and four-surface consistency read'
up_dependencies:
- t3.55r.9d.ds.ow
- t3.55r.9d.ds.58
parent: t2.55r.9d.ds
reference_materials: null
created_at: '2026-08-18T23:33:18.101693'
status: ready
schema_version: '0.1'
guid: 55r9ddsishvfednwdboj6mn7j9cb7boy
---

## Context

Terminal verification Subtask for this Task, added at the per-Task PM review's direction. The Task's dependency graph has two terminal lanes (the new test coverage and the README amendment) that can land in either order, and one sibling (the helper docstring/decision-record Subtask) edits the shipped source file after the test lane may already have run its suite — so without this Subtask nothing re-runs the project-wide checks, and nothing reads the four behavior-describing surfaces side-by-side, once the last lane lands. This Subtask owns the Task-scope Success Criterion "The test suite ... passes under the project Full test command; Lint is clean" and the cross-surface consistency obligation that the sibling doc Subtasks each check only from their own side.

Preconditions: all six sibling Subtasks of this Task have landed (implementation, docstring/decision record, existing-test updates, new coverage, guide rewrite, README amendment).

## What Needs to Change

No file edits. Two verification duties:

1. **Project-wide checks.** Run the project Full test command and the Lint command, both looked up from CLAUDE.md `## Build Commands` (`Full test`, `Lint`). Both must pass at repo scope. Note the Lint command covers the helper scripts but not `tests/`; additionally run the lint tool against `tests/test_context_gauge.py` as the test lane's own acceptance criteria already require, and confirm it is clean.
2. **Four-surface consistency read.** Read, side by side: (i) the `context_gauge.py` module docstring (Producer-mode block, Exit codes block, invariants/decision-record blocks), (ii) `docs/doc-writing-guide.md` `## The context-gauge file contract`, (iii) `README.md` `### The context-usage gauge file`, and (iv) the post-fix assertions in `tests/test_context_gauge.py`. Verify all four agree on: the exit code per input shape (`produce` exits 0 on every input shape, argparse usage errors aside), the retained single-line stderr diagnostic on unparseable stdin, the wrapped-display disposition of both former blanking shapes (empty/whitespace stdin; non-empty unparseable stdin), the unwrapped limit (the default display is legitimately empty when there is no payload to derive it from), and the gauge-write behavior matrix (no gauge write on either shape). A surface that is silent on a dimension is NOT a disagreement — some surfaces are legitimately capped by sibling design (the README stays at operator register: no exit-code table, no stdin-drain mechanics, no contributor-level internals) — only two surfaces making contradictory statements are reportable. Report any actual disagreement — naming the file, the location, and the two conflicting statements — rather than editing; fixes route back to the owning lane's Subtask.

## Key Files

- skills/quo-setup/scripts/context_gauge.py — read-only (surface i)
- docs/doc-writing-guide.md — read-only (surface ii)
- README.md — read-only (surface iii)
- tests/test_context_gauge.py — read-only (surface iv)

## Acceptance Criteria

- The project Full test command passes at repo scope, run after the last sibling Subtask landed.
- The project Lint command passes; the lint tool is additionally clean on `tests/test_context_gauge.py`.
- A recorded four-surface consistency verdict exists: either "all four surfaces agree" across the five checked dimensions, or a list of specific disagreements (file + location + the conflicting statements) reported for the owning lanes to fix.
- No file is modified by this Subtask.
