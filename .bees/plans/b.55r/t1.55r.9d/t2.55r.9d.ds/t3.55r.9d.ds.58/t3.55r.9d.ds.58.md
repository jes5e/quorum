---
id: t3.55r.9d.ds.58
type: t3
title: Amend the customer-facing gauge-contract section with the wrap display-preservation guarantee
up_dependencies:
- t3.55r.9d.ds.bo
down_dependencies:
- t3.55r.9d.ds.is
parent: t2.55r.9d.ds
reference_materials: null
created_at: '2026-08-18T23:25:17.820724'
status: done
schema_version: '0.1'
guid: 55r9dds58cvnkwho6iwjftee4ak8erkk
---

**Fix-path resolution (orchestrator note, 2026-08-18):** the implementation subtask for this Task implements refined path (b) — `produce` exits `0` on every input shape, always emits the display, and downgrades the unparseable-stdin `fail()` to a stderr-only `warn()`. Path (a) (emit-then-exit-2) was rejected because the harness blanks the status line on any non-zero exit regardless of stdout (verified against https://code.claude.com/docs/en/statusline: "Scripts that exit with non-zero codes or produce no output cause the status line to go blank"). Wherever the body below states per-path wording, the path-(b) wording applies; still verify against the landed diff rather than assuming.

## Context

The customer-facing docs (CLAUDE.md `## Documentation Locations` -> `Customer-facing docs` -> `README.md`) carry the operator-facing gauge contract at `### The context-usage gauge file` (README.md lines 200-237): Location, Required fields, Overwrite and freshness, a Restricted-or-pinned-environments paragraph, the `read`-based verification snippets, and a closing pointer to the contributor-facing guide section.

**Finding — the README does not currently describe the blanking behavior.** A read of lines 200-237 plus a grep for `blank` / `exit` / `unparseable` / `produce` over README.md confirms it: the section documents the *file* contract (path, fields, overwrite/freshness) and the `read` verification path, and never describes `produce --wrap-command`'s behavior on a bad payload. The only exit-code sentence in the section (line 235: "A nonconforming file is the exception — rather than printing one of those four values, the reader exits non-zero and describes the malformation on stderr, which is how you tell a broken producer from a merely quiet one.") is about the **reader**, not `produce`, and stays correct post-fix. So the Epic-body deferral's claim that the current behavior "is documented in the README" over-states it for this surface — the README's gap is an omission, not a wrong statement.

Consequently this subtask is **additive and small**: the Task's Success Criterion "The customer-facing docs' gauge-contract section ... describe the post-fix behavior" is met by adding the operator-level guarantee that a producer-side parse failure never blanks a wrapped status line, not by correcting an existing sentence. Task 2 of this Epic ships the `/quo-setup` step that installs `--wrap-command` wrappers, so operators will start encountering wrapped configurations; the guarantee needs a customer-facing home before that lands.

Assumes the sibling doc Subtask has already settled the contributor-facing wording in `docs/doc-writing-guide.md` `## The context-gauge file contract`; this section must agree with it and must not duplicate its depth (the README section's own closing sentence at line 237 already frames the guide as "optional depth, aimed at contributors").

## What Needs to Change

- Add ONE short bolded-lead-in paragraph to `### The context-usage gauge file` in `README.md`, matching the section's existing `**Bolded lead-in.** prose...` paragraph shape (as used by `**Location.**` line 204, `**Required fields.**` line 211, `**Overwrite and freshness.**` line 219, `**Restricted or pinned status-line environments.**` line 221). Suggested lead-in: `**Wrapping an existing status line.**` Place it after the `**Overwrite and freshness.**` paragraph (line 219) and before `**Restricted or pinned status-line environments.**` (line 221) — the restricted-environments paragraph already gestures at "the environment's own status-line command, a wrapper around it", so the wrap guarantee reads naturally just above it.
- Content, 2-3 sentences, operator register, no contributor-level depth:
  - `produce --wrap-command "<your existing status-line command>"` runs the operator's own command with the same status-line payload and re-emits its output verbatim, so the gauge is added to an existing status line rather than replacing it.
  - The guarantee: a payload the producer cannot parse — including an empty one, or one whose shape the harness changed — does not blank the wrapped display; the wrapped command still runs and its output is still what appears on the status line.
  - One sentence on how a malformed payload surfaces instead, worded per the landed fix path (read the landed `skills/quo-setup/scripts/context_gauge.py` and the sibling doc Subtask's guide wording; do not guess):
    - **Path (a) — SUPERSEDED (not implemented; retained for history only):** the producer still reports the problem on stderr and exits non-zero, so the display survives while the failure remains visible to anyone inspecting the status-line command.
    - **Path (b) — unparseable-is-a-no-op (the implemented path):** the producer keeps exiting 0 (reporting the problem only as a single stderr line) and the gauge simply stops being refreshed, so a malformed payload shows up as a `stale` (or `missing`) reading when you verify with the reader snippet below — point at the existing verification snippets at README.md lines 223-235 rather than restating them.
- Do NOT document how to enable the wrapper or the `/quo-setup` step's options and caveats. Task 3 (`t2.55r.9d.rp`) owns the setup how-to, the four choices (configure / wrap / opt out / decline), and the next-session-activation and pinned-environment caveats. This paragraph documents only the contract-level display-preservation guarantee.
- Do NOT modify the `**Location.**`, `**Required fields.**`, or `**Overwrite and freshness.**` paragraphs — path, field names, and overwrite/freshness semantics are the protected cross-Epic contract consumed by Epic 3 and are unchanged by this Task.
- Do NOT modify README.md line 235's reader-exit sentence; it is `read`-scoped and remains correct.
- Do NOT relocate or restyle the closing doc-writing-guide pointer at line 237 — Epic 3's body (`t1.55r.h9`, item 4) tracks that cosmetic relocation as its own doc-pass item.
- Do NOT add or extend a `### Feature:` subsection in `docs/prd.md` or `docs/sdd.md`; Epic 3 owns the cumulative feature subsections.

## Key Files

- `README.md` — `### The context-usage gauge file` (lines 200-237). One new bolded-lead-in paragraph inserted between line 219 (`**Overwrite and freshness.**`) and line 221 (`**Restricted or pinned status-line environments.**`). No other line in the section changes.
- `docs/doc-writing-guide.md` — READ-ONLY input. `## The context-gauge file contract`, as rewritten by the sibling doc Subtask; the source of truth for the post-fix semantics this paragraph summarizes, and the reference for not duplicating contributor-level depth.
- `skills/quo-setup/scripts/context_gauge.py` — READ-ONLY. `cmd_produce` (lines 361-389) and `run_wrapped` (lines 313-348) confirm which fix path landed.

## Acceptance Criteria

- `README.md` `### The context-usage gauge file` contains exactly one new bolded-lead-in paragraph, positioned between the `**Overwrite and freshness.**` and `**Restricted or pinned status-line environments.**` paragraphs; `git diff README.md` for this subtask shows insertions only, with no modified or deleted pre-existing lines in that section.
- The new paragraph states, in operator-facing terms, that `--wrap-command` preserves an existing status line AND that a payload the producer cannot parse (including an empty payload) does not blank that wrapped display.
- The paragraph states how a malformed payload surfaces instead, and that statement matches the landed implementation and the sibling doc Subtask's `docs/doc-writing-guide.md` wording — verified by reading both.
- The paragraph is at most three sentences and introduces no contributor-level detail (no exit-code table, no stdin-drain mechanics, no `cmd_produce` internals) that duplicates the doc-writing guide.
- The paragraph does not describe the `/quo-setup` step, its options, or its caveats (Task 3's scope).
- No sentence anywhere in `README.md` asserts that a parse failure or an empty payload blanks a *wrapped* status line. Scope the claim to the wrapped display — the unwrapped default display is legitimately empty when there is no payload to derive one from, so do not over-claim beyond the wrap path.
- `docs/prd.md` and `docs/sdd.md` are unmodified by this subtask.
