---
id: t3.55r.9d.zk.n2
type: t3
title: 'Verify the Task: full test suite, lint, and cross-surface consistency read'
up_dependencies:
- t3.55r.9d.zk.3c
- t3.55r.9d.zk.cj
parent: t2.55r.9d.zk
reference_materials: null
created_at: '2026-08-19T00:10:30.939839'
status: ready
schema_version: '0.1'
guid: 55r9dzkn2xpuameu5bojy44cmedewvf7
---

## Context

Terminal verification Subtask for this Task, mirroring Task 1's (added there at the per-Task PM review's direction; recommended here by the test lane for the same reason). Task 2's lanes end at the doc-sync Subtask and the second test Subtask, in either order — and both test Subtasks scope themselves to the Narrow test plus the lint tool on the touched test file, per the test-scope discipline. Without this Subtask, nobody owns the authoritative workspace-wide Full test + Lint run after the last lane lands, and nobody reads the behavior-describing surfaces side-by-side.

Preconditions: all eight sibling Subtasks of this Task have landed (inspection helper, install/write-opt-out helper, slow-path step prose, fast-path + direct entry point, marker-convention docs, enum sync, and both test Subtasks).

## What Needs to Change

No file edits. Three verification duties:

1. **Project-wide checks.** Run the project Full test command and the Lint command, both looked up from CLAUDE.md `## Build Commands` (`Full test`, `Lint`). Both must pass at repo scope. Additionally run the lint tool against `tests/test_context_gauge.py` (the Lint command's glob covers helper scripts, not `tests/`) and confirm it is clean.
2. **Cross-surface consistency read.** Read, side by side: (i) the `context_gauge.py` module docstring (the `inspect-statusline` / `install-statusline` / `write-opt-out` contract blocks and the decision record), (ii) the shipped `skills/quo-setup/SKILL.md` sections (the optional producer step, the fast-path subsection, the direct-invocation section), (iii) `CLAUDE.md` `## Scratch-file convention`'s third documented-exception paragraph, (iv) `docs/doc-writing-guide.md`'s opt-out-marker note and `**Lockstep rule.**` enumeration, (v) `docs/sdd.md`'s quo-setup and `context_gauge.py` `## Key components` bullets, and (vi) the test assertions in `tests/test_context_gauge.py`. Verify agreement on six dimensions: the opt-out marker path string and the `/quo-setup` direct-invocation producer-configuration argument (each byte-identical everywhere it is spelled — the argument across the frontmatter `argument-hint`, the direct-invocation section's trigger, and the doc-writing guide's Lockstep-rule enumeration), the helper's subcommand and flag surface (enumerated identically everywhere it is enumerated), the wrap-don't-replace and always-exit-0 producer semantics, the fast-path decision (stated the same way in the skill prose and the SDD bullet), the idempotency skip rules (already-configured-and-current / opted-out, plus the direct-path opt-out override), and snippet pairing — every shell snippet added to `skills/quo-setup/SKILL.md` by this Task ships as a paired POSIX bash block and a Windows PowerShell block with no bash-only fallback (no mechanical test covers OS pairing; this read is the only guard). A surface that is silent on a dimension is NOT a disagreement — several are legitimately capped by sibling design — only two surfaces making contradictory statements are reportable. Report any actual disagreement (file + location + the two conflicting statements) rather than editing; fixes route back to the owning lane's Subtask.
3. **Shipped-artifact portability.** Confirm `tests/test_shipped_artifact_portability.py` passes within the Full test run — it is the mechanical half of the no-repo-only-references review criterion over the new SKILL.md prose and helper docstring text.

## Key Files

- skills/quo-setup/scripts/context_gauge.py — read-only (surface i)
- skills/quo-setup/SKILL.md — read-only (surface ii)
- CLAUDE.md — read-only (surface iii)
- docs/doc-writing-guide.md — read-only (surface iv)
- docs/sdd.md — read-only (surface v)
- tests/test_context_gauge.py — read-only (surface vi)

## Acceptance Criteria

- The project Full test command passes at repo scope, run after the last sibling Subtask landed.
- The project Lint command passes; the lint tool is additionally clean on `tests/test_context_gauge.py`.
- A recorded consistency verdict exists: either "all surfaces agree" across the six checked dimensions, or a list of specific disagreements (file + location + the conflicting statements) reported for the owning lanes to fix.
- No file is modified by this Subtask.
