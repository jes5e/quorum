---
id: t3.55r.9d.zk.3c
type: t3
title: Sync the internal architecture docs and repo-layout enumerations with the shipped quo-setup statusline step
up_dependencies:
- t3.55r.9d.zk.uc
- t3.55r.9d.zk.q5
down_dependencies:
- t3.55r.9d.zk.n2
parent: t2.55r.9d.zk
reference_materials: null
created_at: '2026-08-18T23:58:27.195743'
status: done
schema_version: '0.1'
guid: 55r9dzk3c9qvdkwx5p2w2v3g1r5t1put
---

## Context

`docs/sdd.md` and `CLAUDE.md` both carry enumerations of what `/quo-setup` does and which bundled helper scripts exist. Task 2 changes both facts. The engineer design decision for this Task (recorded in the sibling implementation Subtasks) is: NEW argparse subcommands on the existing `context_gauge.py` (`inspect-statusline`, `install-statusline`, `write-opt-out`) — no new helper script. This subtask is a diff-review pass over the Engineer's shipped Task 2 change against enumerations that go stale in different ways; verify the shipped shape against the diff rather than assuming.

The specific enumerations at risk:

- `docs/sdd.md:28` — the `**skills/quo-setup/**` `## Key components` bullet, which lists quo-setup's responsibilities (colonize three hives, write the two CLAUDE.md sections, optional PRD/SDD bootstrap, fast-path re-registration). Task 2 adds a materially new one: the first settings-file write quo-setup has ever performed, plus a marker write. The bullet is not falsified but is now incomplete about quo-setup's side effects.
- `docs/sdd.md:6` (`## Tech stack` helper-script list) and `docs/sdd.md:55-58` (per-helper `## Key components` bullets, `context_gauge.py` at line 58, which enumerates its subcommands verbatim). With new subcommands on `context_gauge.py`, line 58's "Two argparse subcommands" count and enumeration go stale.
- `CLAUDE.md` `## Repo layout` — the `skills/<name>/scripts/` bullet enumerates all bundled helpers by path and purpose in closed-list form (the `context_gauge.py` purpose gloss goes stale under the recorded design and needs refreshing; a new-file entry is needed only if one shipped).
- `docs/doc-writing-guide.md:247` `**Lockstep rule.**` — enumerates "both subcommand names (`produce`, `read`), both flag names (`--wrap-command`, `--session-id`)". New subcommands or flags on `context_gauge.py` falsify this sentence directly.

Preconditions: the Engineer's Task 2 change has landed and is readable as a diff; Task 1's doc subtasks (`t3.55r.9d.ds.58`, `t3.55r.9d.ds.bo`) have already amended the gauge-contract sections, so this pass edits their post-Task-1 text. This subtask is sequenced after the sibling opt-out-marker doc subtask because both touch `CLAUDE.md` — and the `**Lockstep rule.**` this subtask extends is that sibling's POST-EDIT version (which already appended the opt-out marker path and the direct-invocation argument). Extend that single rule in place; do not create a second lockstep paragraph.

## What Needs to Change

Read the Engineer's Task 2 diff first, then update only the enumerations the diff actually falsified:

- **`docs/sdd.md:28`** — extend the `skills/quo-setup/` bullet with a clause covering the new optional statusline-producer step: that it is `AskUserQuestion`-gated and optional, that it writes/updates the user-settings `statusLine` to invoke the bundled gauge helper's `produce` subcommand (wrap-don't-replace when one already exists), that it can instead write the persistent opt-out marker, and — per the Task-2 requirement that the fast-path decision be documented, not left in review notes — whether the fast path offers the step or only the slow path. Record the fast-path decision as it actually shipped; read it from the skill prose rather than assuming.
- **`docs/sdd.md:6` and `docs/sdd.md:55-58`** — correct the `context_gauge.py` bullet's subcommand count and enumeration to the shipped surface (expected: `produce`, `read`, `inspect-statusline`, `install-statusline`, `write-opt-out`); if against expectation a new helper script shipped instead, add a new `## Key components` bullet in the established shape (purpose, argparse subcommands, output contract, failure convention `fail()` → one stderr line, exit 2) plus an entry in the `## Tech stack` list. Also refresh `docs/sdd.md:6`'s `## Tech stack` parenthetical for `context_gauge.py` when its purpose gloss no longer covers the shipped surface — under the recorded design it will not, for the same reason as the CLAUDE.md gloss above.
- **`CLAUDE.md` `## Repo layout`** — edit if the diff added a new helper file (add it to the `skills/<name>/scripts/` enumeration with a parenthetical purpose, matching the existing entries' shape) OR if an existing helper's parenthetical purpose description no longer covers its shipped surface. Under the recorded design the second trigger fires: `context_gauge.py`'s gloss ("context-usage gauge producer/reader") becomes incomplete once the helper also inspects and updates the operator's status-line settings and writes the opt-out marker — refresh the parenthetical.
- **`docs/doc-writing-guide.md:247`** (`**Lockstep rule.**`) — add the shipped new subcommands/flags on `context_gauge.py` to the protected-string enumeration so the lockstep warning covers the real surface.
- If the diff falsified **none** of a given enumeration (e.g. no new helper file), record that explicitly in the subtask's completion note rather than inventing an edit — `docs/sdd.md:28` will still need the step clause regardless.

**Scope fences.**

- Do **NOT** author the cumulative `### Feature: Context-window guard for long orchestrator runs` subsection under `docs/sdd.md` `## Per-feature design` (line 105) or under `docs/prd.md` `## Per-feature scope`. Epic 3 (`t1.55r.h9`) owns it, exactly once (Bee `b.55r` `## Anticipated doc impact`; Epic 3 acceptance criterion "Cumulative-doc ownership"). The edits above are to pre-existing non-`### Feature:` sections only.
- Do **NOT** touch `README.md`. Task 3 (`t2.55r.9d.rp`) owns every customer-facing surface for this step, including the skill-catalog row and the setup-flow description.
- Do **NOT** add a reference from any shipped artifact (`skills/*/SKILL.md`, `agents/*.md`, `skills/*/scripts/*.py`) to `docs/sdd.md`, `docs/doc-writing-guide.md`, `CONTRIBUTING.md`, or a repo-only `CLAUDE.md ## <section>` — `CLAUDE.md` `## Review criteria for skill changes` rule 3 (shipping-boundary extension) makes that a flaggable defect, and `tests/test_shipped_artifact_portability.py` enforces it mechanically.

## Key Files

- `docs/sdd.md` — line 28 (`skills/quo-setup/` `## Key components` bullet) always; line 6 (`## Tech stack` helper list) and lines 55-58 (per-helper bullets; `context_gauge.py` at 58). Line 105 (`## Per-feature design`) is out of scope.
- `CLAUDE.md` — `## Repo layout`, the `skills/<name>/scripts/` helper enumeration; edit expected (`context_gauge.py` gloss refresh), plus a new-file entry only if one shipped.
- `docs/doc-writing-guide.md` — line 247 `**Lockstep rule.**` subcommand/flag enumeration.
- `skills/quo-setup/SKILL.md` — read-only input (the shipped step prose and the fast-path decision).
- `skills/quo-setup/scripts/context_gauge.py` — read-only input (the shipped CLI surface).

## Acceptance Criteria

- `docs/sdd.md:28`'s `skills/quo-setup/` bullet names the optional statusline-producer step, states that it is optional and gated, states the wrap-don't-replace behavior, names the opt-out choice, and states the fast-path treatment (offered / slow-path-only) that actually shipped.
- Every bundled helper script present under `skills/*/scripts/` after Task 2 appears in all three enumerations: `docs/sdd.md` `## Tech stack`, `docs/sdd.md` `## Key components`, and `CLAUDE.md` `## Repo layout` — verifiable by listing `skills/*/scripts/*.py` and grepping each filename in each of the three locations — AND each enumeration's parenthetical purpose description for `context_gauge.py` covers the shipped surface (gauge producer/reader plus status-line settings inspection/installation and the opt-out writer); filename presence alone does not satisfy this criterion.
- Every argparse subcommand and flag on `skills/quo-setup/scripts/context_gauge.py` after Task 2 is named in `docs/sdd.md:58` and in `docs/doc-writing-guide.md`'s `**Lockstep rule.**`; the subcommand count in `docs/sdd.md:58` matches the shipped count.
- No `### Feature:` heading is added to `docs/sdd.md` or `docs/prd.md` by this subtask; `grep -c '^### Feature:' docs/sdd.md` is unchanged from its pre-subtask value.
- `README.md` is unmodified by this subtask.
- `python -m pytest tests/` passes and `python -m pyflakes skills/*/scripts/*.py` is clean.
