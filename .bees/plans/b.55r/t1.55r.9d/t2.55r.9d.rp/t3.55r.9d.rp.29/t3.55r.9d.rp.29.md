---
id: t3.55r.9d.rp.29
type: t3
title: Reflect the producer step and the direct-invocation argument in the customer-facing skill catalog and setup-flow prose
up_dependencies:
- t3.55r.9d.rp.79
down_dependencies:
- t3.55r.9d.rp.j2
parent: t2.55r.9d.rp
reference_materials: null
created_at: '2026-08-19T00:37:21.069064'
status: done
schema_version: '0.1'
guid: 55r9drp29kiwf2ga85w9ep7fbp8ukz5m
---

## Context

The customer-facing skill catalog is the single source of truth for the user-visible skill surface (CLAUDE.md `## When editing skills`; `docs/doc-writing-guide.md` `## The frontmatter contract`). Task 2 extends `/quo-setup`'s shipped `description` to cover producer configuration and adds an `argument-hint` for the direct-invocation path, so the catalog row and the setup-flow prose now understate what the skill does and accept.

Preconditions: all Task 2 Subtasks have landed, and the sibling Subtask **Author the customer-facing how-to for the optional /quo-setup gauge-producer step** has landed (this Subtask links to the section it creates). **Read the shipped `skills/quo-setup/SKILL.md` frontmatter and `## Overview` for the authoritative wording; do not copy from ticket text.**

## What Needs to Change

**`README.md`**

1. **`/quo-setup` catalog row** (line 124 in `### Skills you invoke`, pre-Task-2 snapshot; re-locate by the row's leading `` `/quo-setup` `` cell). Extend it so it mirrors the substance of the shipped extended `description` and the new `argument-hint`: the row must name the optional status-line gauge-producer step, name the producer-configuration argument, and say that the argument runs only that step (skipping the rest of setup, and usable on a machine with no configured repo). Keep the existing sentences — one-time configuration, CLAUDE.md sections, optional PRD/SDD bootstrap, idempotent/safe-to-re-run — and extend the existing fast-path sentence so it also mentions that the fast path offers the producer step. Link the new material to the how-to section's anchor, `#enabling-the-gauge-producer`.
2. **The after-install setup summary** (line 102, the paragraph beginning "It will colonize hives (Plans + Issues + Specs), write a `## Documentation Locations` …"). Add the optional producer step to that enumeration, in the same register and without disturbing the closing "Safe to re-run if you skip a step and want to come back to it later." sentence.
3. **Bundled helper list — two duties** (`### Where bundled helper scripts live`, line 189). Task 2 added subcommands to the existing `skills/quo-setup/scripts/context_gauge.py`, which the list already names, so no NEW name is expected. (a) Add a name only if a genuinely new script file shipped — verify against the landed diff (list the files under `skills/*/scripts/` touched by Epic 2); expected no. (b) Refresh the paragraph's `context_gauge.py` purpose gloss if it no longer covers the shipped surface — under the recorded design it will not: "producer/reader" omits that the helper now also inspects and updates the operator's status-line settings and writes the opt-out marker. Keep the gloss in the paragraph's existing one-line register. Record the outcome of both duties.

**Explicitly out of scope.** Do not change the ASCII workflow diagram at README.md lines 7-21 (it names entry points, not steps). Do not change the `## Recommended session settings` table's `/quo-setup` row — its rationale is about the CLAUDE.md contract keys and remains correct. Do not edit the internal-skills catalog table (lines 137-144). Do not restate the step's options, caveats, or opt-out semantics here — one copy of that prose lives in the how-to section; the catalog row points at it. Do not add or extend a `### Feature:` subsection in `docs/prd.md` or `docs/sdd.md`, and do not edit `CLAUDE.md`, `skills/*`, or `agents/*`.

## Key Files

- `README.md` — the `/quo-setup` catalog row (line 124); the after-install setup summary paragraph (line 102); `### Where bundled helper scripts live` (line 189; gloss refresh expected, new-file entry conditional and expected no-op).
- `skills/quo-setup/SKILL.md` — READ-ONLY. Frontmatter `description` and `argument-hint`, and `## Overview`, are the source of truth the catalog row mirrors.

## Acceptance Criteria

- The `/quo-setup` catalog row names the optional status-line gauge-producer step and names the producer-configuration argument, spelled identically to the argument the shipped `### Direct invocation: producer configuration only` section triggers on and consistent with the frontmatter `argument-hint` (hint text; not necessarily byte-identical to it).
- The catalog row states that the argument triggers a configure-only path that skips full setup and does not require a configured repo.
- The catalog row's fast-path sentence mentions the producer offer, and its pre-existing claims (one-time configuration, CLAUDE.md sections, optional PRD/SDD bootstrap, idempotent) survive unchanged.
- The catalog row's new material links to `#enabling-the-gauge-producer`, and that anchor resolves to a heading that exists in the same file.
- The after-install setup summary paragraph enumerates the optional producer step alongside hives, the two CLAUDE.md sections, and the PRD/SDD bootstrap, and still ends with the "Safe to re-run …" sentence.
- The substance of the catalog row does not contradict the shipped `description`: every capability the row claims is present in the shipped frontmatter, and the row claims no capability the frontmatter does not.
- `### Where bundled helper scripts live` names exactly the script files present under `skills/*/scripts/` after Task 2, and its `context_gauge.py` purpose gloss covers the shipped surface (not just "producer/reader"); the completion note records both the new-file check outcome and the gloss refresh.
- The ASCII workflow diagram, the `## Recommended session settings` table, and the internal-skills catalog table are byte-unchanged.
- The catalog row and the after-install paragraph contain no option list, no caveat text, and no marker path — those live only in the how-to section.
- `docs/prd.md`, `docs/sdd.md`, `CLAUDE.md`, `skills/*`, and `agents/*` are unmodified by this subtask.
