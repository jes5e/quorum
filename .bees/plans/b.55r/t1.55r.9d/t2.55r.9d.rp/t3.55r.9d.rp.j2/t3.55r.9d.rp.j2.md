---
id: t3.55r.9d.rp.j2
type: t3
title: Verify customer-facing consistency with the shipped step, the post-fix wrap semantics, and the cumulative-doc scope fence
up_dependencies:
- t3.55r.9d.rp.79
- t3.55r.9d.rp.29
- t3.55r.9d.rp.mf
parent: t2.55r.9d.rp
reference_materials: null
created_at: '2026-08-19T00:37:30.474480'
status: ready
schema_version: '0.1'
guid: 55r9drpj2htv5uvjk7eqcnahp4e4gveu
---

## Context

This Task writes into three neighbouring customer-facing surfaces (the new how-to section, the catalog and setup-flow prose, and the scratch-files paragraph) that must agree with each other, with Epic 1's already-published gauge-file contract, with Task 1's post-fix wrap paragraph, and with what Task 2 actually shipped. The parent Task's Success Criteria include several conditions that are verification-shaped rather than authoring-shaped, and the parent Bee carries an explicit cross-Epic scope fence: the cumulative `### Feature: Context-window guard for long orchestrator runs` subsection belongs to Epic 3 and must be authored exactly once, there.

Preconditions: the three sibling doc Subtasks of this Task have landed. This Subtask makes only the corrections its checks surface; if every check passes it is a legitimate no-op with a recorded result.

## What Needs to Change

Run the following checks over `README.md` and the Task's diff, and fix any divergence found (fixes stay inside the customer-facing docs; a divergence traceable to shipped skill prose is reported, not silently patched here):

1. **Terminology and path agreement.** `<tempdir>/.quorum/` and its per-OS spellings, `context-guard-opt-out`, `context-usage-<session_id>.json`, `context-usage-quo-setup-self-check.json`, `produce` / `read`, `--wrap-command` / `--session-id`, the producer-configuration argument (checked against the shipped direct-invocation section's trigger, not the `argument-hint` text), and the new installer subcommand names must be spelled identically everywhere they appear across `### Where bundled helper scripts live`, `### Scratch files`, `### The context-usage gauge file`, the new how-to section, and the `/quo-setup` catalog row — and identically to the shipped `skills/quo-setup/SKILL.md` and `skills/quo-setup/scripts/context_gauge.py`.
2. **No stale pre-fix blanking claims.** Grep `README.md` for `blank`, `non-zero`, `exit`, `unparseable`, and `malformed`. Every surviving statement must be reader-scoped (the existing sentence about the *reader* exiting non-zero on a nonconforming file is correct and must not be touched) or must match Task 1's landed path-(b) semantics — `produce` exits 0 on every input shape and reports a malformed payload only as a single stderr line. No sentence may say or imply that the producer fails loudly, exits non-zero, or blanks a wrapped status line.
3. **No duplicated depth, no contradiction.** The how-to section must not restate Task 1's `**Wrapping an existing status line.**` guarantee, and must not contradict it. The scratch-files paragraph must not restate the how-to's opt-out scope. The catalog row must not carry options or caveats. Also check that the pinned-environment precedence-source enumerations in `### The context-usage gauge file` (the shipped `**Restricted or pinned status-line environments.**` paragraph) and the new how-to section agree — this subtask HAS the authority to reconcile them, including rewording that one shipped paragraph, provided its published-contract obligations survive.
4. **Anchor integrity.** Every intra-document anchor link added by this Task resolves to a heading that exists (`#the-context-usage-gauge-file`, `#where-bundled-helper-scripts-live`, and the new how-to section's slug), and the new heading's slug matches what the linking sites use.
5. **Cumulative-doc scope fence.** `docs/prd.md` and `docs/sdd.md` are byte-unmodified by this Task, and neither gains a `### Feature:` subsection. Confirm by diff, not by inspection alone.
6. **Lane fence.** This Task modified `README.md` only. `CLAUDE.md`, `skills/*`, `agents/*`, `tests/*`, and `docs/*` are untouched by this Task's four Subtasks combined.
7. **Style conformance.** `docs/doc-writing-guide.md` `## Inline style`: sentence-case headings, backticked paths/commands/flags/contract keys, no emojis, lists only for enumerable items, no two-fact tables.
8. **Test suite.** Run the project's Full test command from `CLAUDE.md ## Build Commands` and confirm it passes — the shipped-artifact portability guard in `tests/` is the mechanical half of the repo's rule-3 criterion, and a README-only change must not disturb it.
9. **Pre-documentation fence, Task-wide.** Every statement added by this Task — in all three edited surfaces, not only the scratch-files paragraph — about what the opt-out suppresses, about a run being guarded, or about a stop firing is phrased so it is accurate whether or not the boundary consumer has shipped; no added sentence asserts a stop fires today.
10. **Gate-substance agreement.** The three choices described in the how-to correspond, in substance and label, to the three options in the shipped `### Configure the context-usage gauge producer (optional)` gate — including the pre-gate behavior when a higher-precedence source is detected (the configure option is not labeled recommended) and the other-install repoint framing. A divergence is reported (not silently patched) per this subtask's existing rule.

## Key Files

- `README.md` — the only file this subtask may edit, and only to correct divergences the checks surface.
- `skills/quo-setup/SKILL.md`, `skills/quo-setup/scripts/context_gauge.py` — READ-ONLY. Source of truth for every string checked in item 1.
- `CLAUDE.md`, `docs/doc-writing-guide.md` — READ-ONLY. `## Scratch-file convention` and `## The context-gauge file contract` for substance agreement; `## Inline style` for item 7.
- `docs/prd.md`, `docs/sdd.md` — READ-ONLY. Must be unmodified (item 5).

## Acceptance Criteria

- Every string named in item 1 is spelled identically at every occurrence in `README.md` and matches the shipped `skills/quo-setup/SKILL.md` and `skills/quo-setup/scripts/context_gauge.py` byte-for-byte.
- A grep of `README.md` for `blank`, `non-zero`, `exit`, `unparseable`, and `malformed` yields only statements that are reader-scoped or that match the landed path-(b) producer semantics; no statement claims the producer blanks a wrapped status line or exits non-zero.
- The display-preservation guarantee appears exactly once in `README.md`; the opt-out's suppression scope appears exactly once; neither is contradicted elsewhere in the file.
- Every anchor link added by this Task resolves to an existing heading in `README.md`.
- `git diff` for the whole Task shows `docs/prd.md` and `docs/sdd.md` unmodified, and no `### Feature:` heading added anywhere.
- `git diff --name-only` for the whole Task lists `README.md` and no other path.
- The new and amended prose conforms to `docs/doc-writing-guide.md` `## Inline style`: sentence-case headings, backticked identifiers, no emojis.
- The project's Full test command passes.
- The pinned-environment precedence-source enumerations in the gauge-contract section and the how-to section agree after this subtask (reconciled here if they diverged).
- No statement added by this Task asserts that a boundary stop fires today; all consumer-dependent phrasing is accurate before and after the consumer ships.
- The how-to's three described choices match the shipped gate's three options in substance and label, or a divergence is reported.
- If every check passed with no edit required, that outcome is recorded explicitly in the subtask's completion note rather than left implicit.
