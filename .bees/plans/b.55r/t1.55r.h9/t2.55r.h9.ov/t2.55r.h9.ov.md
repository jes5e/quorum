---
id: t2.55r.h9.ov
type: t2
title: Task 4 — Cumulative PRD/SDD Feature subsections and remaining doc items
up_dependencies:
- t2.55r.h9.dc
parent: t1.55r.h9
children:
- t3.55r.h9.ov.bx
- t3.55r.h9.ov.16
- t3.55r.h9.ov.yb
reference_materials: null
created_at: '2026-08-19T20:19:04.474121'
status: ready
schema_version: '0.1'
guid: 55rh9ovupkkkecd3rsqkd179i1252rty
---

## Context

Post-implementation documentation Task for Epic 3, and the **single owner** of the cumulative PRD/SDD `### Feature:` subsections for the whole context-window-guard feature. The feature is complete only when Epic 3 lands, so its cumulative-doc entries are authored here — exactly once — from what actually shipped across Epics 1–3, not from plan-time projections. Epics 1–2 folded in only their own surface docs and did NOT author the cumulative subsection; earlier Tasks in this Epic do the same. Depends on Task 3 (the guard behavior must be landed so the docs describe what was built).

## What Needs to Change

**1. Cumulative SDD `### Feature:` subsection (single owner).** Append/update `### Feature: Context-window guard for long orchestrator runs` under `## Per-feature design` in the SDD (path from CLAUDE.md `## Documentation Locations` `Internal architecture docs (SDD)`). Heading text is the Plan Bee `title` verbatim. Cover: the producer/consumer split, the boundary-gate shape, the unsupported-CLI silent carve-out vs the missing-reading hard-stop, the landed stale-routing decision (Option A window-bump: enlarged freshness window, `stale → stop` retained, the monotonic-floor rework rejected), and the relationship to b.ja9. Use the doc-writer's idempotency rule (replace-if-exists, exact heading match).

**2. Cumulative PRD `### Feature:` subsection (single owner).** Append/update the matching `### Feature: Context-window guard for long orchestrator runs` under `## Per-feature scope` in the PRD (path from CLAUDE.md `## Documentation Locations` `Project requirements doc (PRD)`). Capture the user-facing behavior: stop-before-compaction on long runs, the configure-now gate, the persistent opt-out, the silent single carve-out.

**3. Fifth customer-facing (README) doc element.** The SDD's `## Documentation` customer-facing bullet conditionally requires guard guidance under the README's recommended-session-settings material, and that README section exists (condition fires). Either write that guidance (how the guard behaves on long runs, the fresh-session recommendation, the optional producer setup) or explicitly record that the conditional resolves to "no change needed" with the reason.

**4. README doc-pointer placement (cosmetic).** If the contributor-facing doc-writing-guide pointer still sits mid-section in the README's `### The context-usage gauge file`, relocate it to the section end as a relative markdown link. Verify current state rather than assuming (it may already have been fixed in an Epic 1 close-out pass).

**5. Precedence-source list lockstep (doc-pass item).** The pinned-environment precedence-source enumeration is currently a two-site README fact with no lockstep note. Consider appending that list to the single `**Lockstep rule.**` in the doc-writing guide (which Epic 2 already extended with the marker path and the direct-invocation argument) rather than authoring a second warning.

## Why

The cumulative PRD/SDD are the faithful long-lived record of what the project does and how it's built; the guard feature is only fully documentable now that its acting half has landed. Consolidating the cumulative entries under one owner avoids duplicate/partial `### Feature:` subsections and satisfies the Epic's single-owner acceptance criterion.

## Cross-Task / cross-Epic contract

- Depends on Task 3 — content is drawn from the landed guard behavior, not the plan.
- **Single-owner rule:** this Task is the ONLY place the cumulative `### Feature: Context-window guard for long orchestrator runs` subsection is authored (in both PRD and SDD). Tasks 1–3 and Epics 1–2 must not author it. Observable: exactly one such subsection per cumulative doc.
- Shipped-artifact review criterion still applies to any prose that touches shipped surfaces, but this Task's edits are to repo-only docs (PRD/SDD/README/doc-writing-guide) and CONTRIBUTING is out of scope here.

## Success Criteria

- Exactly one `### Feature: Context-window guard for long orchestrator runs` subsection exists in each of the PRD and SDD, describing what shipped across Epics 1–3 (including the Option A stale-routing decision and the b.ja9 relationship).
- The README's fifth customer-facing element is either written or explicitly recorded as "no change needed" with a reason.
- The README doc-pointer is at the section end as a relative link (or verified already so); the precedence-source lockstep note is resolved.
- All docs are internally consistent with the shipped helper and skill behavior; prose project-neutral; any snippets paired POSIX + PowerShell.
