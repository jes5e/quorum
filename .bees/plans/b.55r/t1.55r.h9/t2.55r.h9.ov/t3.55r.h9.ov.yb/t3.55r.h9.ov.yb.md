---
id: t3.55r.h9.ov.yb
type: t3
title: 'Doc-writing-guide: live STOP_THRESHOLD consumer and precedence-source lockstep'
up_dependencies:
- t3.55r.h9.dc.tp
- t3.55r.h9.dc.cz
- t3.55r.h9.dc.4v
- t3.55r.h9.id.ut
parent: t2.55r.h9.ov
reference_materials: null
created_at: '2026-08-19T20:32:26.718834'
status: done
schema_version: '0.1'
guid: 55rh9ovybvy2bsccfwwjvdob1aqspwxq
---

## Context

The doc-writing guide's `## The context-gauge file contract` section is the contributor-facing source of truth for the gauge/guard mechanism. Two parts go stale the moment Task 3's boundary check lands, and one part invites a lockstep consolidation:

- The `**Named constants.**` paragraph, AFTER Task 2's doc-guide Subtask lands, will describe `STOP_THRESHOLD_PERCENT` as emittable via the new `stop-threshold` subcommand but with the boundary-check consumer wired separately. Once Task 3 lands, the execution-skill boundary check IS that live consumer — so any residual "no consumer reads it yet / ships with a later feature" phrasing must be updated to name the live consumer.
- If Option A raised `FRESHNESS_WINDOW_SECONDS` from 120s, the constant's documented value/derivation needs reconciling — but NOTE Task 2's doc-guide Subtask ("Update doc-writing-guide contract for the stop-threshold seam and enlarged window", t3.55r.h9.id.ut) is the primary owner of the freshness + seam reconciliation. This Subtask must NOT re-do that work; verify it landed and only address what remains (the live-consumer prose and the precedence lockstep).
- The single `**Lockstep rule.**` enumerates the string contract. The pinned-environment precedence-source enumeration currently lives only in the README; the ask is to consider folding that enumeration into the single `**Lockstep rule.**` rather than a second drift-prone warning — but only if Task 3's guard prose newly references those sources.

**Coordination note (cross-Task):** Task 2's doc-guide Subtask (t3.55r.h9.id.ut) lands FIRST (before Task 3) and owns the `stop-threshold` CLI-contract documentation, the Lockstep-rule subcommand enumeration, and the freshness-window reconciliation. This Subtask (Task 4, after Task 3) owns only the two things that are only true once Task 3 lands: (a) updating the `STOP_THRESHOLD_PERCENT` Named-constants prose from "seam, consumer wired separately" to "live consumer = the execution-skill boundary check"; and (b) the precedence-source lockstep decision. Do NOT re-author the seam's CLI-contract paragraph or the freshness figure — verify Task 2's Subtask already did, and only edit what remains.

## What Needs to Change

Read the Task 3 diff and the landed `context_gauge.py` constants first, and read the current state of `docs/doc-writing-guide.md` `## The context-gauge file contract` (which Task 2's Subtask will have already updated).

1. **STOP_THRESHOLD_PERCENT now has a live consumer.** Update the `**Named constants.**` prose so it reflects that the execution-skill boundary check now reads `STOP_THRESHOLD_PERCENT` from the helper (via the `stop-threshold` subcommand), rather than any residual "no consumer reads it yet / ships with a later feature" phrasing. Keep the single-definition-site-tunable framing and the "a retune touches one line" guarantee.

2. **Freshness window — verify only.** Confirm Task 2's doc-guide Subtask already reconciled any `FRESHNESS_WINDOW_SECONDS` / "120 seconds" figure to the landed value. If it did, record "no change needed: Task 2 doc-guide Subtask already reconciled the window". Only edit if a stale figure remains.

3. **Precedence-source lockstep (the "consider" item).** Evaluate whether the pinned-environment precedence-source enumeration should be appended to the single `**Lockstep rule.**`. If Task 3's guard prose (e.g., the missing-reading gate text) newly references those precedence sources such that a second copy would drift from the README's copy, append the enumeration to the single `**Lockstep rule.**` (one governed list, one place to update) rather than authoring a separate warning. If Task 3 introduced no such second reference, record "no change needed: precedence sources remain enumerated once (README); no second copy to keep in lockstep" with that reason. Do NOT create a second standalone precedence warning under any branch.

## Key Files

- `docs/doc-writing-guide.md` (Doc writing guide per CLAUDE.md `## Documentation Locations`) — `## The context-gauge file contract`, specifically `**Named constants.**` and `**Lockstep rule.**`.
- Task 3 diff + `skills/quo-setup/scripts/context_gauge.py` — ground truth for whether guard prose references precedence sources.

## Acceptance Criteria

- The `**Named constants.**` `STOP_THRESHOLD_PERCENT` prose names the execution-skill boundary check as the live consumer (no residual "no consumer reads it yet").
- The `FRESHNESS_WINDOW_SECONDS` figure is confirmed reconciled (by Task 2's Subtask); no stale "120 seconds" remains — verified, not double-edited.
- The precedence-source enumeration is either folded into the single `**Lockstep rule.**` (if a second reference now exists) or explicitly left single-sourced with a recorded reason — and no second standalone precedence warning is introduced.
- All edits stay project-neutral; the single `**Lockstep rule.**` remains the one governed list (no forked second copy).
