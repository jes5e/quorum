---
id: t3.55r.h9.ov.16
type: t3
title: README + CONTRIBUTING customer/contributor-facing guard doc pass
up_dependencies:
- t3.55r.h9.dc.tp
- t3.55r.h9.dc.cz
- t3.55r.h9.dc.4v
- t3.55r.h9.id.bt
parent: t2.55r.h9.ov
reference_materials: null
created_at: '2026-08-19T20:32:25.548792'
status: done
schema_version: '0.1'
guid: 55rh9ov16vam1djb1rmwe7posp8jwv3x
---

## Context

The README already documents the producer/gauge side of the context-window guard comprehensively (`### The context-usage gauge file`, `### Enabling the gauge producer`, the Scratch-files opt-out note). What it does NOT yet carry is user-facing guidance about the CONSUMER behavior that lands with Task 3: that the three execution orchestrators now stop at Epic/Issue boundaries and recommend a fresh session before an ill-timed auto-compaction. This is the fifth customer-facing doc element for the feature. The `## Recommended session settings` section is the natural home because it already frames these orchestrators as the longest-running, longest-context surfaces in a run. README → docs/* links are legitimate (README is not shipped via `cp -r skills/*`).

## What Needs to Change

Read the Task 3 diff first so the guidance matches landed behavior and landed threshold/window figures.

1. **Guard guidance in/near `## Recommended session settings`.** Add a short paragraph (or a sentence woven into the existing long-context framing for `/quo-execute` / `/quo-fix-issue`, extended to `/quo-breakdown-epic`) telling users that on long runs these orchestrators now watch context usage at Epic/Issue boundaries and will stop and recommend starting a fresh session before the harness would auto-compact — and that when no reading is being published they offer to configure the gauge producer inline via `/quo-setup --configure-gauge-producer`. Cross-link the existing `### The context-usage gauge file` and `### Enabling the gauge producer` sections rather than restating the gauge/opt-out mechanics. Keep it project-neutral. If, after reading the diff, you judge the existing opt-out/gauge sections already convey this adequately and a new paragraph would only duplicate, record "no change needed" with that reason instead of padding.

2. **Doc-pointer placement in `### The context-usage gauge file`.** VERIFIED CURRENT STATE (at breakdown): the doc-writing-guide pointer is ALREADY the final paragraph of the section and is already a relative markdown link. No relocation is needed — record "no change needed: pointer already at section end as a relative link". Only act if the Task 3 diff moved it.

3. **Freshness-window consistency.** IF the Task 3 diff (Option A) raised `FRESHNESS_WINDOW_SECONDS` from 120s to a turn-scale value, the README's `### The context-usage gauge file` prose that currently states the helper "trusts a reading written within the last 120 seconds" (and the paired "refresh at least that often" guidance) becomes stale and MUST be reconciled to the landed value. NOTE: Task 2's Subtask "Update README gauge-file-contract freshness prose to the enlarged window" is the primary owner of this reconciliation — if it already landed the fix, this item is a verify-and-record-no-change; only make the edit if it is still stale. Do not double-edit.

4. **CONTRIBUTING.md residual-limitation decision (Engineering best practices — the one otherwise-unowned SDD `## Documentation` element).** The SDD `## Documentation` Engineering-best-practices bullet conditionally calls for a short CONTRIBUTING.md note on the guard's residual limitation (pinned environments requiring a manual producer; the unsupported-CLI silent carve-out) "if warranted, otherwise none." As the single post-implementation doc owner, MAKE and explicitly RECORD this decision (do not leave it implicit). The likely resolution is **"no change needed"**: the residual limitation is already published to operators in README's restricted/pinned-status-line-environment note (FR6 / SR-3.3), and its upstream removal is tracked in Issue `b.cj2`, so a duplicate CONTRIBUTING note is not warranted. If, after reading the landed diff, you judge a contributor-facing note IS warranted (e.g. a maintainer-relevant caveat not present in README), add a short one to CONTRIBUTING.md instead. Either way, record the decision and its reason in the doc-writer pass summary so the SDD conditional is visibly resolved rather than silently dropped. NOTE: this is distinct from Task 1's CONTRIBUTING.md edit (the self-introspection carve-out) — do not disturb that.

## Key Files

- `README.md` (Customer-facing docs per CLAUDE.md `## Documentation Locations`) — `## Recommended session settings`, `### The context-usage gauge file`, `### Enabling the gauge producer`.
- `CONTRIBUTING.md` (Engineering best practices per CLAUDE.md `## Documentation Locations`) — read-only unless item 4 resolves to "warranted"; the residual-limitation decision site.
- Task 3 diff + `skills/quo-setup/scripts/context_gauge.py` — ground truth for the freshness window and stop behavior.

## Acceptance Criteria

- Users reading `## Recommended session settings` learn that the three execution orchestrators stop-and-recommend-fresh-session at boundaries when context nears the auto-compaction point, and that a missing reading triggers the inline configure-now offer — OR a "no change needed" decision is recorded with a concrete reason.
- The `### The context-usage gauge file` doc-writing-guide pointer remains a relative markdown link at the section end (no relocation performed unless the diff moved it).
- Any "120 seconds" freshness figure in the README matches the landed `FRESHNESS_WINDOW_SECONDS` (reconciled here only if Task 2's README Subtask did not already do it; no double-edit).
- The CONTRIBUTING.md residual-limitation decision (SDD `## Documentation` Engineering-best-practices element) is made and RECORDED with a reason — either a short note is added to CONTRIBUTING.md, or "no change needed" is recorded (with the reason: already covered by README's restricted-environment note + tracked in b.cj2). Task 1's self-introspection carve-out in CONTRIBUTING.md is not disturbed.
- New/edited prose is project-neutral and contains no shell snippets that lack paired POSIX + PowerShell variants (this Subtask should not need new snippets).
