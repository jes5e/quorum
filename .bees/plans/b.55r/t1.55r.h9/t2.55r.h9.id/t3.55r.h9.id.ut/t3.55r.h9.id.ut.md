---
id: t3.55r.h9.id.ut
type: t3
title: Update doc-writing-guide contract for the stop-threshold seam and enlarged window
up_dependencies:
- t3.55r.h9.id.vd
- t3.55r.h9.id.xp
down_dependencies:
- t3.55r.h9.ov.yb
parent: t2.55r.h9.id
reference_materials: null
created_at: '2026-08-19T20:28:22.138497'
status: done
schema_version: '0.1'
guid: 55rh9idut7vg75t9af6merpt2riy91xg
---

## Context

The doc-writing guide (`Doc writing guide = docs/doc-writing-guide.md` per CLAUDE.md `## Documentation Locations`) carries the contributor-facing `## The context-gauge file contract` section — the authoritative enumeration of the helper's CLI subcommands, flags, JSON fields, named constants, and the cross-side **Lockstep rule**. This Task changes the shipped helper `skills/quo-setup/scripts/context_gauge.py` in two ways: (1) it raises `FRESHNESS_WINDOW_SECONDS` from 120 to a turn-scale value (~15–20 min), and (2) it adds an additive threshold-emitting seam — a new subcommand `stop-threshold` that prints the integer `STOP_THRESHOLD_PERCENT`. Both are contract-surface changes the guide currently describes in a now-outdated way.

Relevant current prose: the **Named constants.** paragraph describes `FRESHNESS_WINDOW_SECONDS` as live and `STOP_THRESHOLD_PERCENT` as "a reserved seam: no consumer reads it yet" — the new emitting subcommand is exactly the mechanism that begins to make the threshold readable via the CLI, so that characterization needs updating. The **Lockstep rule.** paragraph enumerates the subcommand names (`produce`, `read`, `inspect-statusline`, `install-statusline`, `write-opt-out`) and every flag name as a protected string contract — the new `stop-threshold` subcommand must be added to that enumeration and the count phrase corrected. The guide does NOT currently hardcode the number 120 (it references the constant by name and the window abstractly), so the freshness change is primarily a rationale/lockstep confirmation there rather than a numeric edit.

This Subtask is the contributor-facing half of the README/guide lockstep. It does NOT touch docs/prd.md or docs/sdd.md (Task 4 owns the cumulative PRD/SDD `### Feature:` entry) and does NOT edit the helper docstring (the Engineer's lane — verify it matches but do not author it here).

## What Needs to Change

- **Named constants. paragraph:** update the `STOP_THRESHOLD_PERCENT` characterization to reflect the landed `stop-threshold` seam — document that the integer threshold is now emittable via that subcommand, and adjust the "no consumer reads it yet / reserved seam" wording to match reality (the threshold is now emittable via the CLI even if the boundary-check consumer is wired separately in Task 3). Keep the single-definition-site / one-line-retune framing. If the enlarged `FRESHNESS_WINDOW_SECONDS` value carries new arithmetic rationale (turn-scale vs. the old 120 s), reflect that here without hardcoding a number the guide would then have to keep in lockstep.
- **CLI-contract body:** document the `stop-threshold` subcommand in the same shape the guide documents `produce`/`read`/etc. — a short contract paragraph (prints the integer threshold on stdout, exit 0, read-only). Mirror the exact name the Engineer used.
- **Lockstep rule. paragraph:** add `stop-threshold` to the protected subcommand-name enumeration and correct the subcommand-count phrase (five → six), so the rename-guard covers it.
- Confirm no stale numeric freshness value exists in the section (there is none today); if the edits introduce any explicit number, add a lockstep note tying it to the helper constant, matching the README half.
- Keep `read`'s four-value output and the stale→stop semantics description unchanged. Keep all snippets paired POSIX + PowerShell and the prose project-neutral.

## Key Files

- `docs/doc-writing-guide.md` — `## The context-gauge file contract` (the **Named constants.**, CLI-subcommand, and **Lockstep rule.** paragraphs).
- `skills/quo-setup/scripts/context_gauge.py` — read-only reference for the exact landed seam name and the new `FRESHNESS_WINDOW_SECONDS` value to mirror.

## Acceptance Criteria

- The `stop-threshold` subcommand is documented in the guide's CLI contract and added to the **Lockstep rule.** enumeration; the subcommand-count phrase is corrected (five → six).
- The **Named constants.** paragraph no longer describes `STOP_THRESHOLD_PERCENT` as unreadable/never-read in a way the landed seam contradicts, and the `FRESHNESS_WINDOW_SECONDS` description is consistent with the enlarged turn-scale window.
- No stale freshness number remains; any explicit number carries a lockstep note to the helper constant.
- `read`'s four-value output and stale→stop semantics remain unchanged in the prose.
- No edits to docs/prd.md, docs/sdd.md, or the helper docstring in this Subtask; snippets stay paired POSIX + PowerShell and prose stays project-neutral.
