---
id: t3.55r.h9.id.bt
type: t3
title: Update README gauge-file-contract freshness prose to the enlarged window
up_dependencies:
- t3.55r.h9.id.xp
down_dependencies:
- t3.55r.h9.ov.16
parent: t2.55r.h9.id
reference_materials: null
created_at: '2026-08-19T20:28:20.988236'
status: done
schema_version: '0.1'
guid: 55rh9idbtzz4879by2b6cnw1mtqkbrck
---

## Context

The shipped helper `skills/quo-setup/scripts/context_gauge.py` is changed in this Task (Epic 3) so that `FRESHNESS_WINDOW_SECONDS` rises from 120 to a turn-scale value (~15–20 min). README is the customer-facing docs surface per CLAUDE.md `## Documentation Locations` (`Customer-facing docs = README.md`). The README section `### The context-usage gauge file` is the published file/freshness contract an environment owner reads when writing their own producer, and it hardcodes the old window number in prose — so it drifts the moment the helper constant moves.

Specifically, the **Overwrite and freshness.** paragraph reads: "The bundled helper trusts a reading written within the last 120 seconds, so a producer you write yourself should refresh at least that often." The **Restricted or pinned status-line environments.** paragraph also refers back to "the freshness window above" without a number and needs only consistency.

This Subtask is the customer-facing half of the lockstep. It does NOT touch docs/prd.md or docs/sdd.md (Task 4 owns the cumulative PRD/SDD `### Feature:` entry) and does NOT touch the helper docstring (the Engineer's lane, which carries the helper's own contract summary as a shipped artifact).

## What Needs to Change

- In README `### The context-usage gauge file`, update the **Overwrite and freshness.** sentence so the trusted-reading window matches the helper's new `FRESHNESS_WINDOW_SECONDS` value exactly (read it from the landed helper, do not guess). Phrase it as a turn-scale interval (e.g. minutes) consistent with the new value rather than leaving the stale "120 seconds".
- Add a short lockstep note at that site making explicit that this README number mirrors the helper's `FRESHNESS_WINDOW_SECONDS` constant and must be moved in the same change if that constant is ever retuned — so a future contributor does not let the two drift. (README→docs/* links are permitted; the existing pointer to the doc-writing guide's contract section stays.)
- The threshold-emitting `stop-threshold` subcommand the Engineer adds is contributor-facing CLI surface, and `STOP_THRESHOLD_PERCENT` is not currently surfaced in this README section (README documents publishing/verifying a conforming *file*, not the stop threshold). Do NOT add threshold-seam prose to README unless review of the landed diff shows a genuinely user-observable README-relevant surface; if it does not, leave README's threshold coverage unchanged.
- Do not otherwise restructure the section; keep every paired POSIX + PowerShell snippet and the reader-snippet examples intact and project-neutral.

## Key Files

- `README.md` — section `### The context-usage gauge file` (the **Overwrite and freshness.** paragraph is the load-bearing edit).
- `skills/quo-setup/scripts/context_gauge.py` — read-only reference for the exact landed `FRESHNESS_WINDOW_SECONDS` value to mirror.

## Acceptance Criteria

- The README freshness sentence states an interval numerically equal to the helper's landed `FRESHNESS_WINDOW_SECONDS`; no stale "120 seconds" remains in the section.
- A lockstep note ties the README number to the helper constant.
- `read`'s four-value output (integer / `no-reading` / `stale` / `missing`) and the stale→stop semantics described in the section are unchanged.
- No edits to docs/prd.md, docs/sdd.md, or the helper docstring in this Subtask.
- All shell snippets remain paired POSIX + PowerShell and the prose stays project-neutral.
