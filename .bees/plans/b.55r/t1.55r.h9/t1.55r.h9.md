---
id: t1.55r.h9
type: t1
title: Epic 3 — Epic/Issue-boundary context guard in the execution skills
up_dependencies:
- t1.55r.ec
- t1.55r.9d
parent: b.55r
reference_materials: null
created_at: '2026-08-16T18:29:46.590446'
status: drafted
schema_version: '0.1'
guid: 55rh9j2a1rjwd3stjqhn2xszzydh1hxy
---

## Context

The capability payoff: the boundary guard that actually stops a long run before auto-compaction. Wires the consumer check into the three execution skills' boundary sites. Depends on Epic 1 (the `read` subcommand) and Epic 2 (its missing-reading hard-stop offers the quo-setup step inline, which must exist to be useful).

**SEQUENCING: this Epic must land after Issue b.ja9** (the honesty rewrite of the Epic-boundary discipline). The consumer check co-locates with the exact boundary prose b.ja9 rewrites — quo-execute §4.2, quo-breakdown-epic Mode 2 auto-continue, quo-fix-issue `all`/list inter-issue boundary — and must compose with b.ja9's state-externalization framing (b.ja9 makes any compaction the guard does not prevent lossless; this guard prevents the ill-timed ones). Because b.ja9 is a separate Issue (not an Epic under this Bee), this dependency cannot be wired as an `up_dependency`; coordinate the sequencing manually and re-read the post-b.ja9 boundary prose before editing.

Authoritative spec: Spec Bee **b.8sh** → SDD `## Requirements` SR-2 (consumer), SR-4 (cross-cutting: two-step gate contract, design rules), and the `## Codebase exploration findings` boundary-site + CLAUDE_EFFORT-floor-check citations.

## What Needs to Change

- **Precondition (first Task of this Epic): verify Issue b.ja9 has landed.** Before editing any boundary prose, confirm b.ja9's rewrite is present — detect the state-externalization framing at the three boundary sites (specifically, confirm the removed "clear your working context" instruction is gone from quo-execute §4.2 / quo-breakdown-epic Mode 2 / quo-fix-issue `all`-list), or check that Issue b.ja9 is `done`. If b.ja9 has NOT landed, hard-stop this Epic with a clear message and do not edit the boundary prose — editing prose that still contains the wishful instruction would either conflict with b.ja9's pending rewrite or re-entrench the removed instruction. This converts the manual, memory-dependent sequencing coordination into a checkable gate (plan-review #5).

Then wire the consumer boundary check into each of the three execution skills at the boundary sites named in the SDD, mirroring the existing `CLAUDE_EFFORT` floor-check shape (read first, compare, only then decide whether to fire a gate; silent-skip on unset):

- At each boundary on a **load-bearing run** (quo-execute multi-Epic / Mode 2 §4.2; quo-breakdown-epic Mode 2 multi-Epic; quo-fix-issue `all`/list inter-issue boundary), run `printenv CLAUDE_CODE_SESSION_ID` (paired POSIX/PowerShell), evaluate before creating any gate task, then branch:
  - **session id unset/empty** → skip the guard silently (unsupported-CLI carve-out; the only silent-skip).
  - session id present → run `context_gauge.py read --session-id <id>` (sibling-resolved helper, one literal Bash call) and branch on its FOUR possible outputs: integer **≥ threshold** → stop at the boundary with a fresh-session resume recommendation naming the exact resume command; integer **< threshold** → continue; **`no-reading`** → continue (transient fresh-but-null); **`stale`** → **stop** at the boundary (stalled-producer signal — a stale reading is untrustworthy and biases low, so it must stop, not continue; mirror SR-2.3/SR-2.5), noting the producer appears stalled; **`missing`** → hard-stop via the two-step `TaskCreate` → `AskUserQuestion` contract, publishing the file contract and offering to run `/quo-setup` inline. Do NOT let a `stale` output fall through to a continue.
- Single-unit runs (no boundary crossed) do not invoke the guard.
- Fold in any skill-behavior documentation for the three skills' new stop behavior.

## Cross-Task / cross-Epic contract

Consumes Epic 1's `read` subcommand via sibling resolution (`<base>/../quo-setup/scripts/context_gauge.py`, paired POSIX/PowerShell) and Epic 2's quo-setup step (the inline offer). The new gate conforms to the existing two-step `TaskCreate` → `AskUserQuestion` contract and `gate-askuserquestion-<short-suffix>` naming + yield-control discipline. **External sequencing dependency on Issue b.ja9** (see Context) — not wireable as an up_dependency.

## Acceptance Criteria

- On a boundary-crossing run with an over-threshold reading, each of the three skills stops at the boundary with a correct fresh-session resume command. *(Demo per skill: force a high gauge; observe the stop.)*
- Under-threshold and `no-reading` readings continue the run silently; a `stale` reading stops the run (stalled-producer signal, fresh-session recommendation), never falling through to continue; a `missing` reading fires the hard-stop gate with the file contract + inline `/quo-setup` offer. *(Demo: each of the four read outcomes — over-threshold, under-threshold, no-reading, stale, missing.)*
- An unset `CLAUDE_CODE_SESSION_ID` skips the guard silently (no stop, no prompt). *(Demo: unset the var; run proceeds unguarded.)*
- Single-unit runs show no guard behavior.
- The gate conforms to the two-step contract (a `gate-*` task precedes the `AskUserQuestion`; no stranded gate tasks); all snippets paired POSIX + PowerShell; prose project-neutral.
- Composes cleanly with the post-b.ja9 boundary prose (no reintroduction of the removed "clear your working context" instruction).
- This Epic's first Task verifies b.ja9 has landed and hard-stops if not, before any boundary prose is edited. *(Demo: on a repo where b.ja9 has not landed, this Epic refuses to proceed with a clear message.)*
- **Cumulative-doc ownership (single owner, resolves the SDD RESEARCH NEEDED and plan-review #8):** the cumulative `### Feature: Context-window guard for long orchestrator runs` subsections in the project PRD/SDD are authored exactly once, owned by THIS Epic's post-implementation doc-writer pass (the feature is complete only when Epic 3 lands). Epics 1–2 fold in only their own surface docs (Epic 1 the README file-contract; Epic 2 the setup how-to) and MUST NOT also author the cumulative subsection. *(Observable: exactly one `### Feature:` subsection per cumulative doc, authored under Epic 3.)*

## Dependencies

Depends on Epic 1 (read subcommand) and Epic 2 (inline quo-setup offer). Plus an external, manually-coordinated sequencing dependency on Issue b.ja9.

