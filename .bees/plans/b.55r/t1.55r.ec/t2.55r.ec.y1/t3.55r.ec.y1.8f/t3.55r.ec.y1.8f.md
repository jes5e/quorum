---
id: t3.55r.ec.y1.8f
type: t3
title: Run the full test suite and lint; confirm pre-existing test modules unaffected
up_dependencies:
- t3.55r.ec.y1.za
parent: t2.55r.ec.y1
reference_materials: null
created_at: '2026-08-18T17:27:06.665777'
status: ready
schema_version: '0.1'
guid: 55recy18f7ezpme45qw43kw187kxzmhb
---

## Context

Final Subtask of Task 2 (t2.55r.ec.y1) and the mandatory "run full test suite" step. The two preceding Subtasks each verified their own file in isolation (`tests/conftest.py` edited alone; `tests/test_context_gauge.py` iterated under the Narrow test command). Nothing before this point has run the whole suite together, and the conftest change is the one edit that could plausibly perturb the three pre-existing modules, since all four import from it.

Preconditions: both sibling Subtasks of this Task have landed, and Task 1 (t2.55r.ec.wa) has shipped `skills/quo-setup/scripts/context_gauge.py`.

This Subtask changes nothing except, if the run surfaces a defect, the two test files this Task owns. A failure traced to the helper is a Task 1 defect: surface it, do not fix the helper here and do not relax the assertion to make the suite green.

## What Needs to Change

No new file. Run and verify:

1. Run the repo **Full test** command from CLAUDE.md `## Build Commands` (`python -m pytest tests/`) and confirm a clean run across all four modules.
2. Compare the three pre-existing modules' results against the pre-change baseline counts captured by this Task's first Subtask before any edit landed — primary source: the scratch file it wrote under `<tempdir>/.quorum/` (e.g. `pytest-baseline-t2.55r.ec.y1.txt`); fallback: its completion summary. Same tests collected, same tests passing, no new skips or warnings attributable to this Task.
3. Run the repo **Lint** command (`python -m pyflakes skills/*/scripts/*.py`) and confirm it is clean. That glob covers only helper scripts, so additionally run pyflakes over the two touched test files (`python -m pyflakes tests/conftest.py tests/test_context_gauge.py`) — an unused import or shadowed name in a test file is a real defect the repo Lint key does not catch.
4. Confirm the working tree contains no change under `skills/` and no change to any documentation file: this Task's entire diff is `tests/conftest.py` plus `tests/test_context_gauge.py`.
5. Have the scratch verification script list `<tempdir>/.quorum` BEFORE the pytest run and again AFTER, then assert the after-set is a superset of the before-set (nothing deleted — repo CLAUDE.md `## Scratch-file convention` forbids cleanup) with no new `context-usage-quorum-pytest-*` entries (nothing leaked by the suite).

Any multi-step verification logic must be written to a scratch script file under `<tempdir>/.quorum/` and run with one command rather than composed inline, per this repo's bash-etiquette rule; never delete the scratch file.

## Key Files

- tests/test_context_gauge.py — may be modified ONLY to fix a defect this run surfaces.
- tests/conftest.py — same.
- skills/quo-setup/scripts/context_gauge.py — READ ONLY. A helper defect is surfaced to Task 1, not patched here.

## Acceptance Criteria

- `python -m pytest tests/` exits 0, collects all four test modules, and the completion summary reports the observed pass count per module (not a generic "all tests pass").
- The three pre-existing modules' collected-and-passed counts are identical to the baseline recorded by this Task's conftest Subtask, and both sets of numbers (baseline, with its provenance, and post-change) are stated in the summary.
- `python -m pyflakes skills/*/scripts/*.py` exits clean, and `python -m pyflakes tests/conftest.py tests/test_context_gauge.py` reports nothing.
- `git status --porcelain` shows exactly two changed paths, both under `tests/`; nothing under `skills/`, `docs/`, or `README.md`.
- The before/after listing of the real `<tempdir>/.quorum` shows nothing deleted and no new `context-usage-quorum-pytest-*` entry.
- If any test fails, the completion summary names the failing test, states whether the root cause is in the test or in the Task 1 helper, and — when it is the helper — leaves the test asserting the SDD-specified behavior and reports the divergence rather than weakening the assertion.
