---
id: t3.55r.ec.y1.vi
type: t3
title: Register the CONTEXT_GAUGE relpath constant in tests/conftest.py
down_dependencies:
- t3.55r.ec.y1.za
parent: t2.55r.ec.y1
reference_materials: null
created_at: '2026-08-18T17:26:54.430329'
status: ready
schema_version: '0.1'
guid: 55recy1vifbt5w1p5sqmyt8xwxd3oxak
---

## Context

First Subtask of Task 2 (t2.55r.ec.y1) under Epic 1 (t1.55r.ec, context-window guard). The repo's test harness loads each single-file bundled helper by a repo-root-relative path constant declared once in `tests/conftest.py` (`SCOPED_MARKER_RESOLVER`, `DETECT_FAST_PATH`, `HIVE_COMMIT` at tests/conftest.py:15-17), which every test module then imports (`from conftest import HIVE_COMMIT, load_script, script_path`, tests/test_hive_commit.py:13). The new gauge helper has no such constant, so the sibling test-module Subtask cannot load it.

`tests/conftest.py` is the one file shared by every test module in the suite, so it gets its own Subtask: the edit is a shared-file touch that the test-module Subtask depends on, and isolating it keeps the shared file's diff to a single reviewable line.

Preconditions: Task 1 (t2.55r.ec.wa) has landed `skills/quo-setup/scripts/context_gauge.py`. This Subtask changes test-harness code only — no helper source (Task 1 owns it), no documentation (Task 3, t2.55r.ec.jj, owns it).

Authoritative spec: Spec Bee b.8sh -> SDD child t1.8sh.cg `## Test Fixtures` ("Add a `CONTEXT_GAUGE` relpath constant to `tests/conftest.py` alongside `HIVE_COMMIT` / `DETECT_FAST_PATH` / `SCOPED_MARKER_RESOLVER`").

## What Needs to Change

**Step 0 — toolchain precondition (run FIRST, before any edit).** The repo declares **Full test** (`python -m pytest tests/`) and **Lint** (`python -m pyflakes ...`) in CLAUDE.md `## Build Commands`, but it carries no dependency manifest, and the executing environment may not have either tool installed. Verify with `python -m pytest --version` and `python -m pyflakes --version`; if either fails, run `python -m pip install pytest pyflakes` and re-verify. If installation is not possible, surface a blocker to the orchestrator and stop — do NOT proceed to claim any green run that was not actually observed.

**Step 0.5 — capture the pre-change baseline.** Before editing anything, run the three pre-existing test modules (`python -m pytest tests/`) and record the collected/passed counts per module in BOTH: (a) this Subtask's completion summary, and (b) a scratch file under `<tempdir>/.quorum/` (e.g. `pytest-baseline-t2.55r.ec.y1.txt`; create the directory if absent, never delete the file — repo scratch-file convention). The final Subtask of this Task compares its post-change run against these recorded numbers, reading the scratch file as the primary source — nothing else captures the "before" state once this Subtask's edit lands.

Then, in `tests/conftest.py`, in the relpath-constant block at lines 14-17:

1. Add one module-level constant next to the existing three, preserving the existing style (bare string literal, repo-root-relative, forward slashes):
   `CONTEXT_GAUGE = "skills/quo-setup/scripts/context_gauge.py"`
2. Update the block's leading comment (currently `# Relative paths (from repo root) of the three live helper scripts.` at line 14) so its count is no longer wrong — e.g. drop the numeral rather than hardcode "four", so the comment stops needing maintenance on the next helper.
3. Update the module docstring's phrase `The three scripts under test` (line 3) for the same reason.

Do NOT change `load_script`, `script_path`, `REPO_ROOT`, or anything else in the file. Do not add fixtures here — the gauge tests' fixtures are local to the sibling test module, matching how `test_hive_commit.py` keeps `_env_with_bin` / `_run_cli` local rather than in conftest.

## Key Files

- tests/conftest.py — add the `CONTEXT_GAUGE` constant to the relpath block (lines 14-17); de-numeralize the block comment (line 14) and the docstring phrase (line 3). Line numbers advisory — re-resolve by structure at execute time.

## Acceptance Criteria

- `python -m pytest --version` and `python -m pyflakes --version` both succeed (after installation if needed), and the completion summary states whether an install was performed.
- The completion summary records the pre-change collected/passed counts per pre-existing test module (the baseline the final Subtask compares against).
- `from conftest import CONTEXT_GAUGE` succeeds inside the pytest suite, and `CONTEXT_GAUGE` equals exactly `"skills/quo-setup/scripts/context_gauge.py"`.
- `load_script(CONTEXT_GAUGE)` returns a module object exposing `gauge_path` and `classify_reading`, and `script_path(CONTEXT_GAUGE)` returns an absolute path that exists — i.e. the constant resolves against the file Task 1 actually shipped, not a guessed path.
- The diff to `tests/conftest.py` is the one added constant plus the two count-wording touch-ups; `git diff` shows no change to `REPO_ROOT`, `load_script`, or `script_path`.
- `python -m pytest tests/` (repo CLAUDE.md **Full test**) still collects and passes the three pre-existing modules with no change in their results.
- `python -m pyflakes tests/conftest.py` reports nothing.
- No tracked file in the repo working tree outside `tests/conftest.py` is created or modified by this Subtask; toolchain installation (site-packages) and pytest cache artifacts (`__pycache__`, `.pytest_cache`) are exempt.
