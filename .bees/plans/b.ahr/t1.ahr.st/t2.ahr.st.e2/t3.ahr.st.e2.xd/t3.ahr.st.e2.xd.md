---
id: t3.ahr.st.e2.xd
type: t3
title: Append SDD + PRD cumulative-doc subsections and update README skill-table rows for auto-detect-URLs feature
parent: t2.ahr.st.e2
reference_materials: null
created_at: '2026-05-08T17:08:54.992185'
status: ready
schema_version: '0.1'
guid: ahrste2xd2yovb73nbjoe1zf64hvuu7u
---

## Context

This Subtask ships the customer-visible cumulative-doc updates for the auto-detect-URLs feature (bare URL tokens in `/quo-file-issue` and `/quo-fix-issue`, no flag required). The work was deferred from Epic 1 (`t1.ahr.5a`) to this Epic so the customer-visible doc-state is consistent — no half-shipped middle state where the URL handling has landed in the skills but the docs and README still describe the flag-required model. After this Subtask lands together with sibling Task 1 of Epic `t1.ahr.st`, the feature is fully shipped and documented end-to-end.

The parent Task `t2.ahr.st.e2` calls for all three doc updates in a single commit. This Subtask is the only Subtask under that Task — the work is mechanical (two appends and two README-row edits) and splitting it across Subtasks would manufacture coordination cost without adding review value.

Canonical shapes to mirror:
- SDD `### Feature:` subsection — the existing `Feature: /quo-file-issue: add --reference URL mode for external sources (GitHub, Linear, etc.)` subsection at `docs/sdd.md` line 215 is the closest cousin and the canonical shape (Architecture paragraph; component-level paragraphs; Decomposition paragraph). The most recently-appended subsection (`Feature: Wire /quo-spec-review into /quo-plan, /quo-write-prd, /quo-write-sdd as automatic quality gate`, line 235) follows the same shape and is the immediate predecessor.
- PRD `### Feature:` subsection — the existing `Feature: /quo-file-issue: add --reference URL mode for external sources (GitHub, Linear, etc.)` subsection at `docs/prd.md` line 113 is the canonical shape (`**What.**` / `**Why.**` / `**Acceptance criteria.**` bullet list / `**Out of scope.**` bullet list). The most recently-appended subsection (`Feature: Wire /quo-spec-review ...`, line 136) is the immediate predecessor.
- README skill-table rows — the existing `/quo-file-issue` row already documents the in-conversation-capture and `--reference <url>` / `--from-github <url>` modes; the bare-URL form needs to be folded in alongside the flag forms. The existing `/quo-fix-issue` row currently does not mention URL mode at all and needs to mention `<url>` tokens, mixed `<id>` + `<url>` lists, and the file-then-fix routing through `/quo-file-issue`.

Source content for the new subsections:
- PRD source — Bee `b.q83`'s PRD child (`t1.q83.qb`). Read via `bees show-ticket --ids t1.q83.qb`. Mirror the in-scope acceptance criteria into the new PRD subsection's `**Acceptance criteria.**` bullets and the Non-Goals into `**Out of scope.**` bullets.
- SDD source — Bee `b.q83`'s SDD child (`t1.q83.a4`). Read via `bees show-ticket --ids t1.q83.a4`. Mirror the architecture content into the new SDD subsection's Architecture paragraph; the component-level paragraphs and decomposition paragraph cover what was actually built across Epic 1 (`t1.ahr.5a`, three Tasks: Mode-fork, contract section, dedupe) and Epic 2 (`t1.ahr.st`, Task 1: `/quo-fix-issue` Section 1 token-classifier extension; Task 2: this doc pass).

File insertion locations:
- `docs/sdd.md` — append new subsection at end of file (after the most-recent `### Feature:` subsection). The new subsection is the next `### Feature:` subsection in document order.
- `docs/prd.md` — append new subsection at end of file (after the most-recent `### Feature:` subsection). The new subsection is the next `### Feature:` subsection in document order.

Note on `### Feature: <title>` heading text: this Subtask is part of Bee `b.ahr` (title: "Auto-detect URLs in /quo-file-issue and /quo-fix-issue"). The exact Plan Bee title is the source of truth for the heading text — read it via `bees show-ticket --ids b.ahr` and use the title verbatim (no paraphrasing, no trimming, no case normalization). The Task body's working-text `Auto-detect URLs in /quo-file-issue and /quo-fix-issue (no flag required)` is descriptive prose that may differ slightly from the Plan Bee title; verify against `b.ahr` before writing the heading.

## What Needs to Change

1. **`docs/sdd.md` — append new `### Feature: <verbatim b.ahr title>` subsection at end of file**, following the canonical SDD shape used at line 215. The new subsection MUST cover:
   - **Architecture paragraph.** Short summary of the feature surface: bare-URL Mode-fork extension in `/quo-file-issue`, Section 1 token-classifier extension in `/quo-fix-issue`, dedupe-by-`reference_materials.value` path, the published `## Inline invocation via the Skill tool` contract section in `/quo-file-issue`, and the `file-from-url-<n>` TaskList entries in `/quo-fix-issue`. State explicitly: no agent contracts changed (`agents/engineer.md`, `agents/pm.md`, `agents/doc-writer.md`, etc.) and no bees CLI changes were required.
   - **Component-level paragraphs.** One short paragraph per component covering: `/quo-file-issue` Mode-fork extension (Epic 1 Task `t2.ahr.5a.ir`); `## Inline invocation via the Skill tool` contract section (Epic 1 Task `t2.ahr.5a.s2`); dedupe path (Epic 1 Task `t2.ahr.5a.pf`); `/quo-fix-issue` Section 1 extension (Epic 2 Task `t2.ahr.st.ab`); the `file-from-url-<n>` TaskList naming and indexing semantics; the no-agent-contract-changes / no-bees-CLI-changes note.
   - **Decomposition paragraph.** Two-Epic, four-Task decomposition: Epic 1 (`t1.ahr.5a`) lands `/quo-file-issue` (three Tasks: Mode-fork at `t2.ahr.5a.ir`, contract section at `t2.ahr.5a.s2`, dedupe at `t2.ahr.5a.pf`); Epic 2 (`t1.ahr.st`) lands `/quo-fix-issue` (Task `t2.ahr.st.ab`) plus this cumulative-doc and README pass (Task `t2.ahr.st.e2`).
   - The existing line-215 subsection (foundation: `--reference URL mode`) MUST remain intact, byte-for-byte. The new subsection is additive.
   - **Idempotency.** If a `### Feature:` subsection with the verbatim `b.ahr` title already exists in the file (from a prior partial run), update the existing subsection in place rather than appending a duplicate. Mirrors the SDD `## Documentation` Idempotency wording from `t1.q83.a4`.

2. **`docs/prd.md` — append new `### Feature: <verbatim b.ahr title>` subsection at end of file**, following the canonical PRD shape used at line 113. The new subsection MUST cover:
   - **What.** One-paragraph summary of the feature from the user's perspective — bare URL tokens are accepted as first-class arguments to `/quo-file-issue` and `/quo-fix-issue`, alongside the existing flag-based forms. URL tokens are auto-detected by URL-pattern matching, no flag required. In `/quo-fix-issue`, mixed `<id>` + `<url>` lists are supported; URL tokens trigger file-then-fix routing through `/quo-file-issue`. Existing flag forms (`--reference <url>`, `--from-github <url>`) continue to work unchanged.
   - **Why.** One-paragraph rationale — the flag-required model from the previous feature was friction; users with a URL in hand should be able to paste it directly. This feature closes the gap and makes URL-tracker entry points first-class as quorum is adopted into projects with external trackers.
   - **Acceptance criteria.** Bullet list mirroring the in-scope criteria from `t1.q83.qb` (the PRD child of Bee `b.q83`).
   - **Out of scope.** Bullet list mirroring the Non-Goals from `t1.q83.qb`.
   - The existing line-113 subsection (`/quo-file-issue --reference URL mode`) MUST remain intact, byte-for-byte. The new subsection is additive.
   - **Idempotency.** If a `### Feature:` subsection with the verbatim `b.ahr` title already exists in the file (from a prior partial run), update the existing subsection in place rather than appending a duplicate. Mirrors the SDD `## Documentation` Idempotency wording from `t1.q83.a4`.

3. **`README.md` — update two skill-table rows.**
   - **`/quo-file-issue` row.** Extend the description to note the bare-URL form alongside the existing flag forms. The existing row already documents in-conversation-capture, `--reference <url>`, and `--from-github <url>`; the new row should fold in `/quo-file-issue <url>` (bare URL) as a third invocation form, calling out that URL detection is automatic and the same URL-pattern resolver-name heuristic applies (`github-issue` / `linear-issue` / `url`).
   - **`/quo-fix-issue` row.** Extend the description to mention `<url>` tokens and mixed `<id>` + `<url>` lists, with a one-line note that URL tokens trigger file-then-fix routing via `/quo-file-issue` (same `WebFetch` fallback applies once the Issue is filed).

All three doc updates MUST land in a single commit, per the parent Task's success criteria.

## Key Files

- `docs/sdd.md` — canonical SDD `### Feature:` shape at line 215 (most-recently-appended subsection at line 235 at time of Task drafting; verify against current EOF before inserting). Append new subsection at EOF.
- `docs/prd.md` — canonical PRD `### Feature:` shape at line 113 (most-recently-appended subsection at line 136 at time of Task drafting; verify against current EOF before inserting). Append new subsection at EOF.
- `README.md` — two skill-table rows to edit (`/quo-file-issue` and `/quo-fix-issue`).
- Plan Bee `b.ahr` — read via `bees show-ticket --ids b.ahr` for the verbatim `### Feature: <title>` heading text.
- `t1.q83.qb` — Bee `b.q83`'s PRD child; source content for the new PRD subsection's Acceptance criteria and Out of scope bullets. Read via `bees show-ticket --ids t1.q83.qb`.
- `t1.q83.a4` — Bee `b.q83`'s SDD child; source content for the new SDD subsection's Architecture paragraph. Read via `bees show-ticket --ids t1.q83.a4`.
- Sibling Task `t2.ahr.st.ab` (Epic 2 Task 1, `/quo-fix-issue` Section 1 extension) — its Subtasks (`t3.ahr.st.ab.hc` wiring; `t3.ahr.st.ab.fw` user-visible surface) and the resulting diff are the source of truth for what the SDD subsection's `/quo-fix-issue` component paragraph and the `file-from-url-<n>` TaskList description say. Read via `bees show-ticket --ids t2.ahr.st.ab` plus its Subtask children.
- Epic 1 Tasks `t2.ahr.5a.ir`, `t2.ahr.5a.s2`, `t2.ahr.5a.pf` — already-broken-down (and shipped before this Task in execution order); the diffs on `main` since `t1.ahr.5a` was marked `done` are the source of truth for the SDD subsection's `/quo-file-issue` component paragraphs.

## Acceptance Criteria

- `docs/sdd.md` gains a new `### Feature: <verbatim b.ahr title>` subsection at EOF following the canonical SDD shape (Architecture paragraph; component-level paragraphs covering all four Tasks plus the `file-from-url-<n>` TaskList semantics plus the no-agent-contract-changes / no-bees-CLI-changes note; Decomposition paragraph naming the two Epics and four Tasks).
- `docs/prd.md` gains a new `### Feature: <verbatim b.ahr title>` subsection at EOF following the canonical PRD shape (`**What.**`, `**Why.**`, `**Acceptance criteria.**` bullets mirroring `t1.q83.qb` in-scope, `**Out of scope.**` bullets mirroring `t1.q83.qb` Non-Goals).
- The existing PRD line-113 subsection and SDD line-215 subsection remain intact byte-for-byte; the existing PRD line-136 subsection and SDD line-235 subsection (the most-recent predecessors) also remain intact byte-for-byte. The new subsections are additive at EOF.
- The `### Feature: <title>` heading text is the verbatim Plan Bee `b.ahr` title — no paraphrasing, no trimming, no case normalization. Verified against `bees show-ticket --ids b.ahr`.
- `README.md` `/quo-file-issue` row extended to fold in the bare-URL form alongside the existing in-conversation-capture, `--reference <url>`, and `--from-github <url>` forms.
- `README.md` `/quo-fix-issue` row extended to mention `<url>` tokens, mixed `<id>` + `<url>` lists, and the file-then-fix routing through `/quo-file-issue`.
- All three doc updates land in a single commit (per parent Task success criteria).
- New SDD subsection's component-level paragraphs name the components and conventions established by sibling Task `t2.ahr.st.ab` (Epic 2) and Epic 1's three Tasks (`t2.ahr.5a.ir`, `t2.ahr.5a.s2`, `t2.ahr.5a.pf`) — Mode-fork, `## Inline invocation via the Skill tool` contract section, dedupe path, Section 1 token-classifier extension, `file-from-url-<n>` TaskList.
- New PRD subsection's `**Acceptance criteria.**` bullets mirror the in-scope items from `t1.q83.qb`; `**Out of scope.**` bullets mirror its Non-Goals.
