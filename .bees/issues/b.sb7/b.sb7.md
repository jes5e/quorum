---
id: b.sb7
type: bee
title: Internal docs as map + register with single fact ownership; retire the per-feature PRD/SDD fold-in; archive the PRD
parent: null
reference_materials: null
created_at: '2026-09-10T20:39:45.896745'
status: open
schema_version: '0.1'
guid: sb7ntrreicsvjt89uqrzqrvp4ydmbdgd
---

## Description

The Doc Writer is instructed to describe "what was actually built" from the Engineer's diff and to append or rewrite a `### Feature: <title>` subsection in the project PRD and SDD on every run, in both execute and fix mode. The Doc Reviewer is instructed to grade the result as a current-state cheat sheet with no design decisions and no history, and to hunt for completeness gaps and duplication. The two contracts contradict each other, and neither assigns any kind of fact a single owning document. The result across every repo using quorum is an SDD that restates mechanism already in the code, carries ticket-keyed history, and duplicates facts across three to six carriers, so every fix pays a doc tax and every doc-review round finds something.

Measured on the four downstream repos plus this one (2026-09-10):

| Repo | SDD | Per-feature share | Ticket-id lines | Commits since June touching SDD/PRD |
|---|---|---|---|---|
| live_edit | 7,313 lines / 276k words | 143 lines | 607 | 133 of 349 |
| event_consumer_service | 744 lines / 32k words | 654 lines | 17 | 64 of 403 |
| event_aggregation_service | 783 lines / 6.6k words | (not measured) | | |
| notification_common | 715 lines / 6.5k words | (not measured) | | |
| quorum | 847 lines / 51k words | 742 lines | 86 | 41 of 132 |

live_edit's SDD was 547 KB when first committed on 2026-07-23 and is 1,958 KB today: 72 percent of it is post-plan accretion written by the Doc Writer, not the original seed. Its per-feature bullets contain "superseded later in this same unreleased set by Issue b.7td, retained here as the record of what b.d6b shipped", against the repo's own doc-writing guide, which routes ticket attribution to the changelog and says to remove outdated text rather than annotate it. Its b.is1 entry records that resizing one re-auth interval was declined partly because it meant "eight doc sites". event_consumer_service and quorum show the other shape: the same defect as a per-feature ledger that is 88 percent of the document.

Five independent agent sessions across these repos agreed on what earned its keep: recorded invariants, decisions with rejected alternatives, and verified facts about the outside world with their source (the Contour readiness-selection fact, the WatchGuard slot-pinning rule, the b.f1v log-level lesson). All agreed the PRD contributed nothing after planning and that the cost was concentrated in doc-review rounds spent on descriptive prose.

## Current behavior

- `agents/doc-writer.md` `## Cumulative project doc updates` mandates a `### Feature: <title>` subsection per run in both PRD and SDD, keyed on the Plan Bee title or an Issue-title fallback, replaced idempotently at every Epic. Its categorization table routes every row except pure refactor to the SDD and has no bug-fix row, so every Issue becomes a "Feature".
- `skills/quo-doc-writer-review/SKILL.md` house style forbids design decisions, trade-offs, and history in architecture docs, while its steps 3 and 5 ask for completeness and duplication findings over the whole document. Completeness over a 276k-word document never converges.
- `skills/quo-setup/SKILL.md` seeds `## Per-feature scope` / `## Per-feature design` headers, tells the user "/quo-plan adds a Feature subsection to both docs", and tells the user the PRD is "used by the Product Manager agent to detect spec drift". In the `/quo-plan` flow the PM reads Spec Bee children, and in fix mode it reads the on-disk PRD only when the Issue cites it, so that claim is stale.
- `skills/quo-fix-issue/SKILL.md` and `skills/quo-execute/SKILL.md` §5 surface the Plan Bee title to the Doc Writer for the fold-in, and their §1 hard-fails when the `Project requirements doc (PRD)` key is missing. Writers never receive the Analyst's ratified policy decisions, so the Doc Writer infers what matters from the diff.
- `README.md` promises "Cumulative project docs" as a feature (lines 43, 125, 185).

## Expected behavior

Every kind of fact has exactly one owning carrier, and the Doc Writer routes facts to owners instead of updating every doc the diff touches:

| Kind of fact | Owner | Never in |
|---|---|---|
| Mechanism: what the code does, internal constants, enum arms, field and metric names, step-by-step algorithms | the code and its doc comments | the SDD |
| Rules: current-state invariants, the contracts the code honors, decisions with rejected alternatives, verified external facts with their source | the SDD register, in the subsystem's section, stated as the rule with a pointer to the code and to the test that pins it | changelogs, per-feature narrative |
| Structure: component boundaries, interactions, data flow, where schemas and API surfaces live, and the path of any archived design doc | the SDD map | duplicated elsewhere |
| History: what changed, when, why, ticket ids, superseded claims | the commit message and the bees ticket (already written, never re-reviewed); a project changelog only if the target CLAUDE.md names one | SDD, README, CLAUDE.md |
| Behavior: what operators and users see — install, commands, configuration surface, user-visible defaults | README or the configured customer docs | duplicated tables elsewhere |
| Requirements (pre-implementation): product scope, non-goals, acceptance criteria | Spec Bee `t1=Doc` children and Issue bodies; whatever the `Project requirements doc (PRD)` key names is background, never authority, never a write target | per-Issue entries |

Engineering, test, and doc conventions belong to the four guides named in `## Documentation Locations` and never enter the register. The table lives in exactly one shipped place, `agents/doc-writer.md` `## Fact ownership`; the review skill, the setup skill's generated guide, README, and CONTRIBUTING carry the principle in one sentence and a pointer, never a second copy of the rows.

The maintained SDD is two parts, the map and the register, under a size cap (default 500 lines; the target repo's doc-writing guide may override) so that the Analyst reads it whole on every dispatch. The admission test for a line: would an agent that skipped it make a worse design decision or reintroduce a fixed bug? The map carries one line per archived design document — its path and "historical record, not maintained, verify against code" — so the archive stays reachable by contract rather than by accident.

Register entries are transcribed from ratified sources the orchestrator relays verbatim, not inferred from the diff:

- **`## Ratified decisions`** carries exactly one section, verbatim, on every Doc Writer dispatch in both modes. Fix mode: the approved directive's `### Policy decisions this change implies`, with its fixed line `None — the recommendation leaves no policy question open.` Execute mode: the `## Decisions and rejected alternatives` section of the spec source's SDD (Spec Bee `SDD` child, or a `file-path` SDD when it has one), with the fixed line `None — the spec records no decisions.` when the spec carries no such section or only its explicit-none placeholder. Recipients: Doc Writer, Doc Reviewer, PM — the PM because it invokes the per-Task doc review through the Skill tool in execute mode, and a reviewer without the list cannot check transcription. Relaying to every Doc Writer dispatch is uniform by design: `/quo-breakdown-epic` creates one doc Subtask per code-changing Task without an internal-versus-customer distinction, so routing by Subtask kind would be an orchestrator judgment over ticket bodies.
- **`## Blast radius`** (fix mode only; execute has no such row) gains the Doc Writer as a recipient of the existing verbatim relay, alongside Engineer, PM, Code Reviewer. The Doc Writer reads the invariant lines itself; no orchestrator extracts them. Its contract stays "invariants the change adds, removes, or weakens".
- The Analyst promotes a load-bearing rule or verified external fact it finds in the code or in an archived doc **only** through `### Policy decisions this change implies`, framed as "should this rule enter the register?" with a recommended answer — never through `### Blast radius`, so the Code Reviewer's sweep verification is not fed sites the change does not touch. Archived docs are outside the blast-radius sweep's internal-docs group. The Analyst opens an archived doc the map names when the register lacks a rule the Issue implicates; it does not read the archive on every dispatch.
- **Both modes:** the Doc Writer records an entry when the diff in scope implements the decision; a ratified decision the diff has not implemented yet is listed in the return as not-yet-landed (an observation the orchestrator does not route, since at Subtask 1 of 6 most decisions are pending by design); a decision the diff **contradicts** is reported to the orchestrator. The Doc Writer MAY add an entry the diff plainly establishes on its own when it passes the admission test — the entry is the rule with a pointer to the pinning test or the code comment that records the external fact, never a copy of either. No return marker is needed: the Doc Reviewer derives such entries as register entries the diff touched that are not on the relayed list. This is a deliberate, bounded exception to transcription-only, kept because on the body-as-spec and hand-written-SDD paths it is the only way the register grows during execute, and because it is reviewer-checked.
- The Issue body's `## Doc divergence noted` items are ratified by the Validate-Issue gate and are routed to their owners inside the existing customer-docs and register duties. `/quo-file-issue` step 3b observes divergence only in the SDD and customer-docs keys, never the PRD key; a legacy item that points at a PRD is reported as "background document, no owner, no action" so the `**Doc Sync**` summary line stays true.
- **Size cap, diff-scoped like every other check.** The Doc Writer always writes the entry; when the SDD is over its cap after the write, it reports that in its return. The Doc Reviewer raises the cap as a defect only when the diff under review took the SDD over the cap; a pre-existing over-cap is noted once in the return as a migration follow-up, not a work item. Without this, every fix in an unmigrated repo would produce an out-of-scope finding every round while the Doc Writer refused the transcription the same reviewer then flagged as missing.

The Doc Reviewer checks four finite things: README claims are true against the diff; each relayed decision the diff implements has a normative register entry with a code pointer and no ticket id, and any register entry the diff touched that is not on the relayed list is checked against the code and its test; the SDD did not cross its cap in this diff; no fact appears in two owners. A false claim is a `blocker`; a fact in the wrong owner or in two owners is a `suggestion` with a move-and-pointer fix path; wording inside the right owner is a `nit` unless false.

## Impact

Wall-clock and token cost on every `/quo-fix-issue` and `/quo-execute` run in every downstream repo: five to eight doc-review rounds per Issue were reported, most spent on descriptive prose and multi-carrier drift. Correctness risk: descriptive SDD claims drift and are then trusted by cold agents (§17.2 "OMPF parse failures log at ERROR" was never true; the "Delete refused pre-MULTI" claim was unreachable). A frozen-but-consulted doc would make that permanent, which is why the PRD is archived and taken out of the agents' authoritative read set rather than frozen in place.

## Suggested fix

Hand edits plus cold reviews per CLAUDE.md `## Working on the orchestrator skills`; not a `/quo-fix-issue` run. Ship the skill change first; downstream doc migrations follow separately (item 10). The first worktree (`doc-ownership`) is discarded; implementation restarts from main **after the b.ag8 relay work now in main's working tree has landed**, since it rewrites the same §5 sentences.

1. `agents/doc-writer.md`: replace `## Cumulative project doc updates` with `## Fact ownership` (the table above, the single carrier, plus the guides exclusion) and three duties: customer-facing docs diff review, register transcription from `## Ratified decisions` (and the invariant lines of `## Blast radius` in fix mode) with the not-yet-landed / contradicted / unrelayed-entry rules, map update when a boundary moves. `## Doc divergence noted` items named inside the sources of duties 1 and 2. State the size cap (write, then report), the admission test, and the map's archive-pointer line. Drop the PRD as a write target.
2. `skills/quo-doc-writer-review/SKILL.md`: replace the house-style block with a one-paragraph statement of the map-plus-register contract and a pointer to the ownership table; replace step 3's architecture bullets with the four checks (cap check diff-scoped); make step 5 an ownership check; add the severity mapping; keep README checks, trailer, severity/depth tags, and loop-bounding prose unchanged.
3. `skills/quo-setup/SKILL.md`: drop `## Per-feature scope` / `## Per-feature design` from both skeletons; add `## Register` to the SDD skeleton; the PRD skeleton is labelled a starting point in prose only (no skill keys on a header); rewrite the "how the docs grow" note naming both relay sources; the generated doc-writing guide states the principle, points at the table, and carries the cap. Fix the stale "PM detects spec drift from the PRD" note.
4. `skills/quo-fix-issue/SKILL.md` and `skills/quo-execute/SKILL.md`: §1 makes `Project requirements doc (PRD)` allowed-empty; §5 drops "Surface the Plan Bee title to the Doc Writer" and adds one `## Ratified decisions` dispatch-table row (recipients Doc Writer, Doc Reviewer, PM; fix-mode source `### Policy decisions this change implies`; execute-mode source the spec's decisions section); fix mode adds the Doc Writer to the `## Blast radius` row's recipients. Narrow "Writers never receive the directive" to its `### Recommended approach`. Encode-deferral destinations and `--doc-path` prose name the SDD only.
5. `agents/analyst.md`: SDD read as map and register; whatever the PRD key names is background; promotion of rules only through `### Policy decisions this change implies`; archived docs outside the sweep's internal-docs group and opened on demand via the map's pointer. `agents/pm.md`: forward `## Ratified decisions` verbatim into `/quo-doc-writer-review`; a PRD is background even when cited. `agents/doc-reviewer.md`: the same forward-verbatim bullet, as `agents/code-reviewer.md` already carries for completeness evidence.
6. `skills/quo-plan/SKILL.md` `## Anticipated doc impact` names the map, register, and README. `skills/quo-plan-from-specs/SKILL.md` hard-fail message no longer claims `/quo-plan` adds feature sections to cumulative docs. `skills/quo-file-issue/SKILL.md` step 3b excludes the PRD key. `skills/quo-breakdown-epic/SKILL.md` Encode prose and `hive_commit.py` docstring name the SDD only. `skills/quo-spec-review/SKILL.md` and `skills/quo-write-sdd/SKILL.md` `## Documentation` guidance names SDD and README, never the PRD.
7. `README.md`: replace the three cumulative-docs sentences with the principle and a pointer; name both relay sources; trim the `/quo-plan-from-specs --feature` row so it no longer advertises the cumulative shape. `CONTRIBUTING.md`: principle 5 becomes one sentence plus pointer; the intentional-asymmetries bullet describes the three duties, not diff hunting. This repo's `CLAUDE.md`: PRD joins `Compile/type-check` as allowed-empty; precondition paragraph updated; `docs/doc-writing-guide.md` records the cap and this repo's own pending migration.
8. `docs/inventory/`: amend the rule inventory and `REWRITE-BRIEF.md` (precedent: the D9 amendment) for the retired Plan-Bee-title-to-Doc-Writer rule, the new `## Ratified decisions` row, and the Doc Writer recipient on `## Blast radius`, so the cold-review acceptance criteria A1/A2 pass on future passes.
9. `tests/`: `test_doc_ownership.py` pins the retired vocabulary in every artifact the sweep touched, the `## Ratified decisions` row in both bodies with all three recipients and a single relayed section, the Doc Writer recipient on fix mode's `## Blast radius` row, and the Analyst's Blast-radius scope sentence unchanged. Produce and attach the sweep completeness list (patterns run, every hit, edit or reason untouched).
10. `docs/migration-map-and-register.md` (repo-only): archive-and-rebuild procedure and a copy-paste prompt for the five repos in order — event_consumer_service (validation run), event_aggregation_service, notification_common, live_edit, then quorum itself. Step 2 writes a heading-walk ledger (`<docs-dir>/archive/sdd-<date>-walk.md`: heading → promoted / mechanism / history / unsure) and commits it with the archive, so a second session resumes from it and the unsure list is auditable. The prompt names the installed agents directory generically, never a hardcoded path.
11. Validation before merge: one real `/quo-fix-issue` run on the migrated event_consumer_service against the new Doc Writer and Doc Reviewer contracts, sequenced after the b.pgj run of the current rebuild so one repo is not validating two rewrites at once. Prerequisite to resolve first: the run needs the branch's skills installed in the sandbox, which today are installed from the image at main.
12. Follow-ups to file when this lands: (a) retire the `### Feature:` scoping machinery (`/quo-plan-from-specs --feature`, `scoped_marker_resolver.py`, PM Path A/B), which under this model has no legitimate document to scope into; (b) migrate this repo's own `docs/sdd.md` and `docs/prd.md` last. Annotations: b.c4v annotated now; b.ag8 annotated once the in-flight b.ag8 edits in main's working tree are committed (bees would otherwise overwrite that uncommitted edit).

## Background and rationale

Three truths, and code owns only one. Code is the truth for what the system does; tests are the truth for what it must keep doing; specs are the truth for what it should do for users. A doc sentence describing what the code does is a second copy and drifts. A doc sentence stating what the code must do is normative: when it disagrees with the code, the code is wrong. That is the line between the register and everything that leaves.

The churn decomposes into four kinds: drift corrections on descriptive claims (eliminated when mechanism leaves the docs), multi-carrier edits (eliminated by single ownership), wording rounds (shrunk by a convergent reviewer criterion), and genuine README defects (kept; they are product defects). Round caps, delta-only rounds, and skipped lanes were rejected as speed levers on 2026-09-04 and are not proposed here; this change shrinks what there is to review, not how hard it is reviewed.

The live_edit CLAUDE.md invariant section (one-line rules with SDD section pointers) is already register-shaped and was grown by hand because agents needed it; the SDD behind it is the encyclopedia nobody can read whole. Both downstream repos already carry an IMPLEMENTATION_CHANGELOG (155k and 18k words); moving history there would relocate the churn, since the Doc Reviewer reviews it too, so history's owner is the commit and the ticket.

## Decisions and rejected alternatives

- **Retire the per-feature fold-in entirely** rather than narrow the Doc Writer's question. Rejected because live_edit proves the fold-in is not the only growth surface; the topical sections grew the same way. The ownership table addresses both shapes.
- **Archive the PRD, do not freeze it in place, and key nothing on a header.** A frozen doc still on the agents' read list is read as authority. The signal that a document is background is that the PRD key names it; a starting-point header is human-facing text only, so the setup skeleton and the migration header need not agree.
- **Transcribe register entries from ratified sources; allow one bounded, reviewer-checked exception.** Transcription removes the judgment call that inflated the old SDDs. The unrelayed entry for a rule the diff plainly establishes is kept deliberately (see Expected behavior) and derived by the reviewer rather than marked by the writer — a marker with no consumer is one fewer mechanism.
- **Relay `## Ratified decisions` to every Doc Writer dispatch; not-yet-landed is an unrouted observation.** Routing by Subtask kind was rejected because the breakdown does not distinguish doc Subtasks by kind and classifying bodies is an orchestrator judgment. Moving transcription to the per-Task PM boundary was rejected because the PM does not write docs.
- **One relayed section per heading; the Doc Writer reads Blast radius itself.** "The invariant lines of Blast radius" would be an orchestrator extraction over free-form prose, which the rewrite forbids; adding the Doc Writer to the existing verbatim `## Blast radius` relay is b.ag8's design and dissolves the heading collision, so `## Ratified decisions` is reusable at every hop.
- **Promote rules through `### Policy decisions this change implies`, not `### Blast radius`.** Blast radius feeds the Code Reviewer's sweep verification; a relied-upon-but-unchanged invariant listed there would produce a finding against the Engineer at every unchanged site. A yes/no "enter the register?" question fits the policy-decisions contract exactly and needs no new marker.
- **PM and Doc Reviewer both receive the relay.** In execute mode the per-Task doc review is the PM's Skill invocation; the doc-reviewer subagent runs only at Bee scope. Either alone leaves one path blind.
- **Cap check is diff-scoped.** "Over cap is a defect regardless" strands every unmigrated repo between rebuild and migration with an out-of-scope finding per round. "Defect when the diff grew it" was considered and refined: every register entry grows the file, so the trigger is crossing the cap, and a pre-existing over-cap is a once-noted migration follow-up.
- **The archive is reachable through the map, not through the PRD key or a header.** One map line per archived doc; the Analyst opens it on demand, never on every dispatch (live_edit's archive is 276k words).
- **The ownership table has one shipped carrier.** The first cold review found two mirrors diverged on day one. Every other artifact states the principle in a sentence and points at `agents/doc-writer.md`.
- **Size cap as a review criterion**, project-overridable, default 500 lines. live_edit's guide said "keep it factual and current" and reached 276k words; rules without a bound erode. Precedent: the orchestrator bodies' line budget.
- **History to commit plus ticket, not to a reviewed changelog.** See rationale above.
- **Archive-and-rebuild for every downstream repo**, resumable via the heading-walk ledger. One procedure; anything the extraction misses is recovered lazily when the Analyst promotes it.
- **Leave the `### Feature:` scoping machinery in place in this change and retire it in a follow-up.** It is orthogonal code with its own tests; this change is already a contract change across many files. The README stops advertising it now.
- **Discard the first worktree and restart from main after b.ag8 lands.** The first diff predates four design changes and rewrites §5 sentences b.ag8 is rewriting concurrently; reconciling is costlier than re-implementing from a settled ticket.
- **Cap rounds / batch nits / skip the PRD check by Issue shape.** Rejected: the first two by the 2026-09-04 decision; the third because `agents/pm.md` rejects shape-based short-circuits for good reason.

## Review dispositions

**Round 1 (2026-09-10, two cold reviews of the first worktree diff).** Doc review: (1) CONTRIBUTING asymmetries bullet described diff-hunting — item 7. (2) README attributed execute-mode register content to a gate — item 7, both sources named. (3) Ownership list in three carriers — single carrier. (4) This repo's own docs exceed the cap — follow-up 12b, and the guide records it. (5) "creates and maintains" lead-in — fix. (6) README line 43 repeats line 183 — fix. Engineer review: (1) Doc Reviewer cannot verify transcription — Doc Reviewer and PM receive the relay; unrelayed entries derived as touched-but-not-relayed. (2) Analyst promotion through Blast radius contradicts its contract — policy-decisions section only. (3) `## Doc divergence noted` severed — items named inside duties 1 and 2; file-issue excludes the PRD key. (4) History never-in PRD — dropped. (5) Mechanism never-in README vs defaults — internal constants vs user-visible defaults. (6) `/quo-plan-from-specs` hard-fail message — item 6. (7) Sweep completeness list owed — item 9. (8) Breakdown Encode prose and helper docstring — item 6.

**Round 2 (2026-09-10, ticket re-review by the original reviewer plus a fresh-context agent).** (1) Uniform relay to every Doc Writer — adopted. (2) PM as recipient with forward-verbatim bullet — adopted. (3) One section per heading; Doc Writer joins `## Blast radius` recipients — adopted; b.c4v annotated now, b.ag8 when its in-flight edit is committed. (4) Cap check strands unmigrated repos — adopted, trigger refined to crossing the cap. (5a) contracts double owner — Rules owns contracts the code honors; Requirements is pre-implementation. (5b) constants vs defaults — reworded. (5c) comment-copy exception — entry is the rule with a pointer. (5d) guides — exclusion sentence. (6) archive unreachable — map pointer line; Analyst opens on demand, not every dispatch; archive outside the sweep. (7) header as marker — dropped; the PRD key is the signal. (8) divergence items pointing at the PRD — file-issue exclusion plus no-owner report. (9) migration resumability — heading-walk ledger. (10) unratified marker — dropped. (11a) inventory amendment — item 8. (11b) real validation run before merge — item 11, with the install prerequisite named. (12) fidelity gaps — moot; worktree discarded. (13) merge collision with b.ag8 — restart after it lands.

## Doc divergence noted

- `README.md` lines 43, 125, 185 describe the retired mechanism; rewrite per item 7.
- `skills/quo-setup/SKILL.md` line 489 states the PRD is "used by the Product Manager agent to detect spec drift"; stale since the Specs-hive redesign.
- `skills/quo-plan-from-specs/SKILL.md` hard-fail message claims `/quo-plan` adds feature sections to cumulative docs; it has not since the Specs-hive redesign.
- `docs/sdd.md` in this repo is a per-feature ledger over the cap; migrate last (follow-up 12b).

