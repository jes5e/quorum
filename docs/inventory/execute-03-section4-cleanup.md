# Rule inventory — quo-execute SKILL.md Section 4 (L521–L788)

Slice: `### 4. Per-Task and Per-Epic Cleanup` — 4.1 After Each Task; 4.2 Find next Epic or move to Final Review; `##### Epic-boundary state-externalization checkpoint`; `##### Epic-boundary context-window guard`; `##### Aborted-run close-out`.

| ID | Rule | Kind | Site | Produces | Consumes | Cross-refs | Env | Mirror | Class |
|---|---|---|---|---|---|---|---|---|---|
| E3-001 | Run the 4.1 close-out only when a Task and all its Subtasks are done (all reviewer feedback addressed or ignored). | precondition | L525 `#### 4.1 After Each Task` | - | Subtask/Task completion state; review-feedback disposition | - | none | unknown | procedure |
| E3-002 | Mark the Task `status=done` in bees. | command | L527 `#### 4.1 After Each Task` | Task status flip to `done` | Task ID | - | bees | unknown | procedure |
| E3-003 | Subtasks are already marked done by each agent, except doc Subtasks, which the orchestrator flips on behalf of the doc-writer. | definition | L527 `#### 4.1 After Each Task` | doc-Subtask status flip (orchestrator-side) | `agents/doc-writer.md` no-`Bash` routing | `agents/doc-writer.md` | bees | unknown | procedure |
| E3-004 | Do the Task `status=done` flip before committing so `.bees/` status changes are included in the commit. | ordering | L527 `#### 4.1 After Each Task` | - | E3-002 | - | bees, git | unknown | procedure |
| E3-005 | Create exactly one git commit per Task. | command | L528 `#### 4.1 After Each Task` | git commit | staged files | - | git | unknown | procedure |
| E3-006 | NEVER push to remote — committing only. | invariant | L528 `#### 4.1 After Each Task` | - | - | - | git | unknown | procedure |
| E3-007 | Staging step 1: run the **Format** command from CLAUDE.md `## Build Commands` to normalize formatting. | command | L529 `#### 4.1 After Each Task` | reformatted working tree | CLAUDE.md `## Build Commands` → `Format` | CLAUDE.md `## Build Commands` | Bash | unknown | procedure |
| E3-008 | Do NOT re-run the test suite in the Format/commit step. | invariant | L529 `#### 4.1 After Each Task` | - | - | - | none | unknown | procedure |
| E3-009 | Rationale: the `.T` subtask already validated and the PM confirmed; re-running wastes minutes per Task. | rationale-only | L529 `#### 4.1 After Each Task` | - | - | - | none | unknown | rationale |
| E3-010 | When a final `trivial-tweak` nit pass landed after PM confirmation, the implementer's own lane rungs cover validation: Engineer **Compile/type-check**, **Lint**, **Narrow test**; Test Writer **Narrow test**, **Lint**; Doc Writer owes no rung. | definition | L529 `#### 4.1 After Each Task` | - | lane validation rungs | CLAUDE.md `## Build Commands` keys | none | unknown | rationale |
| E3-011 | Staging step 2: run `git status` to see the full set of modified and untracked files. | command | L530 `#### 4.1 After Each Task` | - | working tree | - | git | unknown | procedure |
| E3-012 | Staging step 3: stage files related to this Task — agent-reported files, formatting changes to files this Task's agents touched, and (only if in-repo) the Plans hive path's contents. | command | L531 `#### 4.1 After Each Task` | staged index | agent file reports; `git status`; E3-013 output | - | git | unknown | procedure |
| E3-013 | To learn the in-repo Plans hive path, run the bundled helper's NON-MUTATING `resolve-hive-paths` mode of `hive_commit.py`. | command | L531 `#### 4.1 After Each Task` | Plans hive absolute path (stdout) or nothing | this skill's base directory | Section 6.5 Encode step | Bash | unknown | procedure |
| E3-014 | Resolve `hive_commit.py`'s path the same way this skill does at its Section 6.5 Encode step. | invariant | L531 `#### 4.1 After Each Task` | - | skill invocation header base directory | Section 6.5 Encode step | none | unknown | procedure |
| E3-015 | The helper emits the Plans hive's absolute path when it lives inside this repo, or nothing when outside — in which case stage no hive path. | definition | L531 `#### 4.1 After Each Task` | - | `resolve-hive-paths` output | - | Bash | unknown | procedure |
| E3-016 | Run the resolver as a single literal Bash call: `python3 "<this skill's base directory>/scripts/hive_commit.py" resolve-hive-paths --hive plans` (POSIX) / `python "<this skill's base directory>\scripts\hive_commit.py" resolve-hive-paths --hive plans` (PowerShell). | command | L531-L541 `#### 4.1 After Each Task` | Plans hive path on stdout | this skill's base directory | - | Bash | unknown | procedure |
| E3-017 | `git add` the emitted Plans hive path (if any) alongside your judgement-selected source files. | command | L543 `#### 4.1 After Each Task` | staged index | E3-016 output | - | git | unknown | procedure |
| E3-018 | Do NOT blindly `git add -A` — other agents or processes may have in-flight changes in the working tree. | invariant | L543 `#### 4.1 After Each Task` | - | - | - | git | unknown | procedure |
| E3-019 | Review each modified file and stage it only if it is plausibly related to this Task. | invariant | L543 `#### 4.1 After Each Task` | - | `git status` output | - | git | unknown | procedure |
| E3-020 | Staging step 4: commit with a descriptive message per system/project git guidance. | command | L544 `#### 4.1 After Each Task` | git commit | staged index | - | git | unknown | procedure |
| E3-021 | Commit subject template: `Plan <bee-id>, Epic N, Task M — <task title> (<task-id>)`; the bare Task ID appears once in trailing parentheses. | field-or-template | L544 `#### 4.1 After Each Task` | commit subject | Plan Bee ID; Epic ordinal; Task ordinal; Task title; Task ID | - | git | no | procedure |
| E3-022 | Example subject: `Plan b.bc7, Epic 1, Task 7 — Document the new auth config surface (t2.rwx.fy.qq)`. | example | L544 `#### 4.1 After Each Task` | - | - | - | none | no | example |
| E3-023 | Source the Plan Bee ID and Epic ordinal from the run's top-level invocation and the Task's parent chain (Task → Epic → Bee); `Epic N` / `Task M` ordinals come from ticket titles. | definition | L544 `#### 4.1 After Each Task` | - | run invocation; ticket titles; parent chain | - | bees | no | procedure |
| E3-024 | If the Epic has no parent Plan Bee (standalone Epic), omit the `Plan <bee-id>, ` prefix and lead with `Epic N, Task M — …`. | field-or-template | L544 `#### 4.1 After Each Task` | commit subject | Epic parent presence | - | git | no | procedure |
| E3-025 | Step 3: mark the per-Task TaskList tasks (named per Section 3's "TaskList naming convention") `completed` and clear them from the active set. | command | L545 `#### 4.1 After Each Task` | TaskList tasks → `completed` | Section 3 TaskList naming convention | Section 3 "TaskList naming convention" | TaskList | unknown | procedure |
| E3-026 | Sweep by name prefix, not by exact name. | invariant | L545 `#### 4.1 After Each Task` | - | TaskList names | - | TaskList | unknown | procedure |
| E3-027 | The prefix sweep must close lanes re-dispatched under the `-r<n>` round discriminator (e.g. `doc-writer-<subtask-id>-r1`, `pm-<task-id>-r1`) alongside their first round. | invariant | L545 `#### 4.1 After Each Task` | round-discriminated tasks → `completed` | `-r<n>` naming | - | TaskList | unknown | procedure |
| E3-028 | The sweep includes any `aborted-<role>-<subtask-id>` redelivery markers the movement-report rung opened for this Task's Subtasks. | invariant | L545 `#### 4.1 After Each Task` | marker tasks → `completed` | `aborted-<role>-<subtask-id>` markers | movement-report rung (Section 3) | TaskList | unknown | procedure |
| E3-029 | Rationale: reaching step 3 means the redelivery landed; a marker left `pending` past close-out would hold a later advance shut. | rationale-only | L545 `#### 4.1 After Each Task` | - | - | - | none | unknown | rationale |
| E3-030 | There is no Agent shutdown to perform — per-Subtask cold dispatches complete-and-exit when each Agent returns. | definition | L545 `#### 4.1 After Each Task` | - | Section 3 dispatch shape | Section 3 | Agent | unknown | procedure |
| E3-031 | The aborted path runs this same prefix sweep at its own site — Section 4.2's `##### Aborted-run close-out` — keep the two sweeps in step. | invariant | L545 `#### 4.1 After Each Task` | - | E3-025..E3-028 | Section 4.2 `##### Aborted-run close-out` | TaskList | unknown | procedure |
| E3-032 | Step 4: output the per-Task summary block to the screen. | command | L546 `#### 4.1 After Each Task` | printed summary | E3-033..E3-041 | - | none | unknown | procedure |
| E3-033 | After the summary, advance to the next Task by dispatching fresh ephemeral implementer Agents per Section 3's dispatch shape. | command | L546 `#### 4.1 After Each Task` | Agent dispatches | next Task | Section 3 | Agent | unknown | procedure |
| E3-034 | Summary heading: `## Task [N] of [total] Complete: [task-title]`. | field-or-template | L549 `#### 4.1 After Each Task` | summary line | Task ordinal, total, title | - | none | unknown | procedure |
| E3-035 | Summary field `**Task ID**: <task-id>`. | field-or-template | L551 `#### 4.1 After Each Task` | summary line | Task ID | - | none | unknown | procedure |
| E3-036 | Summary field `**Files Changed**: [count] files ([list key filenames if < 5, otherwise just count])`. | field-or-template | L552 `#### 4.1 After Each Task` | summary line | commit file set | - | none | unknown | procedure |
| E3-037 | Summary field `**Reviews**: [Code review: X issues found/None needed \| Docs review: Y issues found/None needed \| N nits applied without re-review (or "count unavailable post-compaction"), when any]`. | field-or-template | L553 `#### 4.1 After Each Task` | summary line | review results; nit-pass count | - | none | unknown | procedure |
| E3-038 | Summary field `**Ignored Review Feedback**`: list items flagged by `quo-engineer-review` or `quo-doc-writer-review` the Director chose not to address, or `"None"`. | field-or-template | L554 `#### 4.1 After Each Task` | summary line | ignored review items | `quo-engineer-review`, `quo-doc-writer-review` | none | unknown | procedure |
| E3-039 | Summary field `**Second-order effects**`: the relayed `### Second-order effects` narrative, rendered per the "Second-order effects" logic. | field-or-template | L555 `#### 4.1 After Each Task` | summary line | PM Final report `### Second-order effects` | "Second-order effects" logic (L562-L567) | none | unknown | procedure |
| E3-040 | Summary field `**Follow-up Tasks Created**: [count, if any] [list task-ids if created]`. | field-or-template | L556 `#### 4.1 After Each Task` | summary line | follow-up Task IDs | - | none | unknown | procedure |
| E3-041 | Summary closing line is exactly one of: `Proceeding to next Task: [next-task-title]` or `Final Task, moving on to Final Reviews`. | choice-set | L557-L559 `#### 4.1 After Each Task` | summary line | remaining-Task state | - | none | unknown | procedure |
| E3-042 | The `**Second-order effects**` field is the destination for the narrative `/quo-engineer-review` emits under its `### Second-order effects` heading, relayed by the per-Task PM. | relay | L562 `**Second-order effects (rendered into the summary block above).**` | summary field content | PM Final report | `/quo-engineer-review`, `agents/pm.md` | none | unknown | procedure |
| E3-043 | Relay chain: `/quo-engineer-review` → per-Task PM Agent (`agents/pm.md` relays into its Final report, one labelled sub-block per in-flight invocation) → this field. | definition | L562 `**Second-order effects (rendered into the summary block above).**` | - | - | `/quo-engineer-review`, `agents/pm.md` | Agent | unknown | procedure |
| E3-044 | Rationale: without a named destination the narrative reaches the orchestrator and stops there. | rationale-only | L562 `**Second-order effects (rendered into the summary block above).**` | - | - | - | none | unknown | rationale |
| E3-045 | The Bee-level review in Section 5 has its own destination: the `**Second-order effects**` field of Section 9's `## Bee Execution Complete` block. | definition | L562 `**Second-order effects (rendered into the summary block above).**` | - | - | Section 5; Section 9 `## Bee Execution Complete` | none | no | procedure |
| E3-046 | Rendering rule 1: collect every relayed narrative for this Task — the PM Final report `### Second-order effects` section including each `#### <invocation scope>` sub-block — and render bullets verbatim. | relay | L564 `**Second-order effects (rendered into the summary block above).**` | summary field content | PM Final report | - | none | unknown | procedure |
| E3-047 | Do not re-summarize, re-rank, or fold second-order bullets into the `**Reviews**` line. | invariant | L564 `**Second-order effects (rendered into the summary block above).**` | - | - | - | none | unknown | procedure |
| E3-048 | Rendering rule 2: keep the `#### <invocation scope>` sub-block labels the PM supplied. | relay | L565 `**Second-order effects (rendered into the summary block above).**` | summary field content | PM sub-block labels | - | none | unknown | procedure |
| E3-049 | Rendering rule 3: de-duplicate exact repeats across sources, keeping the earliest attribution; near-duplicates differing in substance are kept separately. | relay | L566 `**Second-order effects (rendered into the summary block above).**` | summary field content | relayed bullets | - | none | unknown | procedure |
| E3-050 | Rendering rule 4: when every source reported `No second-order effects identified.` or no `/quo-engineer-review` ran for this Task, render the single line `None identified.`. | field-or-template | L567 `**Second-order effects (rendered into the summary block above).**` | summary field content | source reports | `/quo-engineer-review` | none | unknown | procedure |
| E3-051 | Do not omit the `**Second-order effects**` field; it is unconditional. | invariant | L567 `**Second-order effects (rendered into the summary block above).**` | - | - | - | none | unknown | procedure |
| E3-052 | Rationale: a section that appears only when there is something to say degrades into one nobody can rely on being asked for. | rationale-only | L567 `**Second-order effects (rendered into the summary block above).**` | - | - | - | none | unknown | rationale |
| E3-053 | Whenever the Director ignores a review-feedback item rather than fix it now, create a `defer-<short-suffix>` TaskList task at the moment of decision. | command | L569 `**Record each ignored-feedback item as a `defer-N` TaskList task at the moment of decision.**` | `defer-*` TaskList task | ignore decision | Section 3 "TaskList naming convention" | TaskList | unknown | procedure |
| E3-054 | The `defer-*` task carries the feedback's one-line description as its `metadata.activity` string, status `pending`. | field-or-template | L569 `**Record each ignored-feedback item …**` | `metadata.activity`; status `pending` | feedback description | - | TaskList | unknown | procedure |
| E3-055 | The PM Final report contract (`agents/pm.md`) annotates each deferred item with a destination: `addressed-now-in-this-Task`, `defer-to-existing-ticket-body: <ticket-id>`, or `defer-to-new-Issue`. | choice-set | L569 `**Record each ignored-feedback item …**` | - | PM Final report | `agents/pm.md` | none | unknown | procedure |
| E3-056 | Record the PM's destination annotation in the same `metadata.activity` string of the `defer-*` task. | field-or-template | L569 `**Record each ignored-feedback item …**` | `metadata.activity` | E3-055 | - | TaskList | unknown | procedure |
| E3-057 | Rationale: this record-creating step is the load-bearing source for Section 6.5's deferral-hygiene gate; without it the gate fires empty. | rationale-only | L569 `**Record each ignored-feedback item …**` | - | - | Section 6.5 deferral-hygiene gate | none | unknown | rationale |
| E3-058 | Items the Director addressed inline this Task do NOT get a `defer-*` task; the ledger tracks only items not addressed now. | invariant | L569 `**Record each ignored-feedback item …**` | - | - | - | TaskList | unknown | procedure |
| E3-059 | When the per-Task PM returns, walk its Final report deferred items (per `agents/pm.md`'s Final report contract). | command | L571 `**Record each PM-deferred item as a `defer-N` TaskList task at the moment of the PM verdict.**` | - | PM Final report | `agents/pm.md` | none | unknown | procedure |
| E3-060 | For every item annotated `defer-to-existing-ticket-body: <ticket-id>` or `defer-to-new-Issue`, create a `defer-<short-suffix>` TaskList task with the one-line description as `metadata.activity`, status `pending`. | command | L571 `**Record each PM-deferred item …**` | `defer-*` TaskList task | PM destination annotations | Section 3 "TaskList naming convention" | TaskList | unknown | procedure |
| E3-061 | Items annotated `addressed-now-in-this-Task` are NOT added to the `defer-*` ledger. | invariant | L571 `**Record each PM-deferred item …**` | - | PM destination annotations | - | TaskList | unknown | procedure |
| E3-062 | Rationale: pairs with the ignored-feedback instruction as the upstream source for Section 6.5's gate; otherwise PM deferrals reach the ledger only via Section 6.5 Step 0's retroactive sweep. | rationale-only | L571 `**Record each PM-deferred item …**` | - | - | Section 6.5 Step 0 | none | unknown | rationale |
| E3-063 | Rationale: walking the PM Final report here mirrors `/quo-breakdown-epic`'s per-Task PM-dispatch site, keeping the two-layer (upstream + Step 0 sweep) pattern symmetric. | rationale-only | L571 `**Record each PM-deferred item …**` | - | - | `/quo-breakdown-epic` per-Task PM dispatch site | none | unknown | rationale |
| E3-064 | The branch-2 Mode 1 continue-or-stop `AskUserQuestion` gate fires through the two-step contract: first `TaskCreate` a `gate-askuserquestion-<short-suffix>` task naming the inter-Epic continuation gate, then `AskUserQuestion` in the same turn. | gate | L575 `#### 4.2 Find next Epic or move to Final Review` | `gate-askuserquestion-*` task; AskUserQuestion | Section 3 gate-task entry | Section 3 TaskList naming convention gate-task entry | TaskList, AskUserQuestion | no | procedure |
| E3-065 | Mark the `gate-*` task `completed` the moment the user's answer is consumed. | command | L575 `#### 4.2 Find next Epic or move to Final Review` | `gate-*` → `completed` | user answer | - | TaskList | unknown | procedure |
| E3-066 | Before moving on from the just-completed Epic, perform the **inter-Epic interaction checkpoint**. | ordering | L577 `#### 4.2 Find next Epic or move to Final Review` | - | Epic completion | - | none | no | procedure |
| E3-067 | Rationale: positioned at the Epic boundary (not the final review) to catch this Epic's interactions with prior Epics' code while context is fresh. | rationale-only | L577 `#### 4.2 Find next Epic or move to Final Review` | - | - | - | none | no | rationale |
| E3-068 | The Director runs the inter-Epic interaction checkpoint directly — no new team. | invariant | L579 `#### 4.2 Find next Epic or move to Final Review` | - | - | - | none | no | procedure |
| E3-069 | Interaction step 1: diff the Epic's landed commits with `git log --oneline <previous-epic-last-commit>..HEAD`. | command | L581 `#### 4.2 Find next Epic or move to Final Review` | commit list | `<previous-epic-last-commit>` | - | git | no | procedure |
| E3-070 | Resolve `<previous-epic-last-commit>` from the run-state manifest: **Pre-Bee SHA** at the first Epic boundary; previous Epic's `last commit <sha>` under **Progress** thereafter. | definition | L581 `#### 4.2 Find next Epic or move to Final Review` | - | run-state manifest **Pre-Bee SHA**, **Progress** | **Epic-boundary state-externalization checkpoint** step 1 | none | no | procedure |
| E3-071 | Interaction step 2: for each file this Epic touched that a prior Epic also touched, scan for **Contract drift** (ordering contracts, docstring claims, "this should never happen" comments). | command | L582-L583 `#### 4.2 Find next Epic or move to Final Review` | findings | commit diffs | - | git | no | procedure |
| E3-072 | Interaction step 2: scan for **Resource compounding** — if this Epic adds acquires from a resource a prior Epic uses, model the aggregate. | command | L584 `#### 4.2 Find next Epic or move to Final Review` | findings | commit diffs | - | git | no | procedure |
| E3-073 | Interaction step 2: scan for **Symmetric-change gaps** — if this Epic added a new resource class, search prior Epics' cleanup paths for missing handling. | command | L585 `#### 4.2 Find next Epic or move to Final Review` | findings | commit diffs | - | git | no | procedure |
| E3-074 | Interaction step 3: if any issue is found, dispatch a fresh ephemeral Engineer Agent per Section 3's dispatch shape to fix it before continuing. | command | L586 `#### 4.2 Find next Epic or move to Final Review` | Engineer dispatch | findings | Section 3 dispatch shape; `agents/engineer.md` | Agent | no | procedure |
| E3-075 | Do not defer interaction-checkpoint fixes to the Final Bee-level review. | invariant | L586 `#### 4.2 Find next Epic or move to Final Review` | - | - | - | none | no | procedure |
| E3-076 | Interaction step 4: record any fixes as additional, clearly labeled commits on the branch. | command | L587 `#### 4.2 Find next Epic or move to Final Review` | git commits | fix edits | - | git | no | procedure |
| E3-077 | After the checkpoint passes (clean or fixed), mark the just-completed Epic `status=done`. | command | L589-L591 `#### 4.2 Find next Epic or move to Final Review` | Epic status flip to `done` | E3-066..E3-076 | - | bees | no | procedure |
| E3-078 | Then re-query all Epics under the Bee to classify the post-Epic state. | command | L591 `#### 4.2 Find next Epic or move to Final Review` | Epic list | Bee ID | - | bees | no | procedure |
| E3-079 | Do not assume "no workable Epic remains" means "Bee is finished"; `status=drafted` Epics must not fall through to final review. | invariant | L591 `#### 4.2 Find next Epic or move to Final Review` | - | Epic statuses | `/quo-breakdown-epic` | none | no | procedure |
| E3-080 | Epic re-query command: `bees execute-freeform-query --query-yaml 'stages: - [parent=<bee-id>, type=t1] report: [title, ticket_status, up_dependencies]'`. | command | L593-L597 `#### 4.2 Find next Epic or move to Final Review` | query result | Bee ID | - | bees | no | procedure |
| E3-081 | `up_dependencies` is returned as a list of ticket IDs only — not statuses. | definition | L599 `#### 4.2 Find next Epic or move to Final Review` | - | query result | - | bees | no | procedure |
| E3-082 | If any `ready` Epic has non-empty `up_dependencies`, batch-look-up those IDs via `bees show-ticket --ids <dep-id-1> <dep-id-2> <...>` to determine workable vs blocked. | command | L599 `#### 4.2 Find next Epic or move to Final Review` | dependency statuses | `up_dependencies` | - | bees | no | procedure |
| E3-083 | Classify the result into exactly one of three branches. | choice-set | L601 `#### 4.2 Find next Epic or move to Final Review` | branch selection | Epic statuses + dependency statuses | - | none | no | procedure |
| E3-084 | The Plans hive status vocabulary `drafted` → `ready` → `in_progress` → `done` is canonical. | definition | L601 `#### 4.2 Find next Epic or move to Final Review` | - | - | - | none | no | procedure |
| E3-085 | Evaluate the branches in listed order; branch 1 takes precedence over branch 2 when both apply. | ordering | L601 `#### 4.2 Find next Epic or move to Final Review` | - | E3-083 | - | none | no | procedure |
| E3-086 | Rationale: any drafted Epic must stop the loop regardless of whether other Epics are workable. | rationale-only | L601 `#### 4.2 Find next Epic or move to Final Review` | - | - | - | none | no | rationale |
| E3-087 | Branch 1 (**Drafted (or blocked-on-drafted) Epics remain**) applies when at least one Epic is `status=drafted`, OR is `status=ready` blocked on a dependency not `done`. | definition | L603 `#### 4.2 Find next Epic or move to Final Review` | - | Epic statuses; dependency statuses | - | none | no | procedure |
| E3-088 | Branch 1: stop the loop. | command | L603 `#### 4.2 Find next Epic or move to Final Review` | run exit | E3-087 | - | none | no | procedure |
| E3-089 | Branch 1: do NOT proceed to Step 5 final review and do NOT offer to mark the Bee done. | invariant | L603 `#### 4.2 Find next Epic or move to Final Review` | - | - | Step 5 | none | no | procedure |
| E3-090 | Branch 1 user message: "Epic `<just-completed-epic-id>` is complete, but Epics `<drafted-or-blocked-ids>` … are still `drafted` … Run `/quo-breakdown-epic <bee-id>` (a fresh session is reasonable …), then re-run `/quo-execute <bee-id>`." | field-or-template | L605 `#### 4.2 Find next Epic or move to Final Review` | printed message | Epic IDs; Bee ID | `/quo-breakdown-epic`, `/quo-execute` | none | no | procedure |
| E3-091 | Branch 1: after the message, run the **Epic-boundary state-externalization checkpoint** (next unit: `none`) and exit the skill. | ordering | L607 `#### 4.2 Find next Epic or move to Final Review` | checkpoint run; skill exit | - | **Epic-boundary state-externalization checkpoint** | none | no | procedure |
| E3-092 | Branch 2 (**Workable Epic remains**) applies when no drafted Epics exist and at least one Epic has `status` in `{ready, in_progress}` with all `up_dependencies` `done`. | definition | L609 `#### 4.2 Find next Epic or move to Final Review` | - | Epic statuses; dependency statuses | - | none | no | procedure |
| E3-093 | Branch 2 branches on the **multi-Epic run mode** captured in Section 2. | ordering | L609 `#### 4.2 Find next Epic or move to Final Review` | - | run-state manifest **Multi-Epic run mode** | Section 2 | none | no | procedure |
| E3-094 | Mode 1 (**Stop after each Epic**), or Section 2's mode prompt was skipped (only one Epic in scope at run start): ask the user whether to continue with the next logical Epic. | gate | L611 `#### 4.2 Find next Epic or move to Final Review` | AskUserQuestion | run mode | Section 2 | TaskList, AskUserQuestion | no | procedure |
| E3-095 | Mode 1 accept: run the **Epic-boundary state-externalization checkpoint** (next unit: next Epic's ID), then the **Epic-boundary context-window guard** (may stop the run), then return to step 2. | ordering | L611 `#### 4.2 Find next Epic or move to Final Review` | checkpoint run; guard run | user answer | **Epic-boundary state-externalization checkpoint**; **Epic-boundary context-window guard** | none | no | procedure |
| E3-096 | Mode 1 decline: run the checkpoint (next unit: `none`) but NOT the context-window guard, then move to final Bee review. | ordering | L611 `#### 4.2 Find next Epic or move to Final Review` | checkpoint run | user answer | **Epic-boundary state-externalization checkpoint** | none | no | procedure |
| E3-097 | The context-window guard never runs on a run-ending path. | invariant | L611 `#### 4.2 Find next Epic or move to Final Review` | - | - | **Epic-boundary context-window guard** | none | no | procedure |
| E3-098 | Mode 2 (**Work through all Epics**): auto-continue without a gate. | command | L612 `#### 4.2 Find next Epic or move to Final Review` | - | run mode | Section 2 | none | no | procedure |
| E3-099 | Mode 2: surface a one-line note announcing the auto-continue and naming the next Epic ID, e.g. `Mode 2 (Work through all Epics): auto-continuing to <next-epic-id> — <title>.` | field-or-template | L612 `#### 4.2 Find next Epic or move to Final Review` | printed note | next Epic ID and title | - | none | no | procedure |
| E3-100 | Mode 2: then run the **Epic-boundary state-externalization checkpoint** (next unit: next Epic's ID), then the **Epic-boundary context-window guard**, then return to step 2. | ordering | L612 `#### 4.2 Find next Epic or move to Final Review` | checkpoint run; guard run | - | **Epic-boundary state-externalization checkpoint**; **Epic-boundary context-window guard** | none | no | procedure |
| E3-101 | Mode 2 auto-continue still respects every other stop: branch 1's drafted-or-blocked stop takes precedence, and any final reviewer-surfaced blocker from Sections 5 and 6 halts the run. | invariant | L612 `#### 4.2 Find next Epic or move to Final Review` | - | - | Sections 5 and 6 | none | no | procedure |
| E3-102 | Branch 3 (**All Epics under this Bee are `done`**): run the **Epic-boundary state-externalization checkpoint** (next unit: `none`), then proceed to Step 5 final Bee review. | ordering | L614 `#### 4.2 Find next Epic or move to Final Review` | checkpoint run | Epic statuses | **Epic-boundary state-externalization checkpoint**; Step 5 | none | no | procedure |
| E3-103 | `##### Epic-boundary state-externalization checkpoint` is the canonical anchor name; other sections refer to it as the *Epic-boundary state-externalization checkpoint*. | definition | L616-L618 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-104 | The three branches invoke the checkpoint by name on all five exit paths, and `##### Aborted-run close-out` invokes it on the run-aborting path. | definition | L618 `##### Epic-boundary state-externalization checkpoint` | - | - | `##### Aborted-run close-out` | none | no | procedure |
| E3-105 | The checkpoint is a definition, not a step in Section 4.2's linear flow — do not run it merely because reading reached its heading. | invariant | L618 `##### Epic-boundary state-externalization checkpoint` | - | - | Section 4.2 | none | no | procedure |
| E3-106 | Run the checkpoint only when an invoking branch calls for it, always after branch classification has resolved (step 2's next unit is unknown until then). | ordering | L618 `##### Epic-boundary state-externalization checkpoint` | - | branch classification | - | none | no | procedure |
| E3-107 | Rationale: Section 3's flat orchestration makes orchestrator context grow monotonically across Epics; nothing in this skill reclaims it; the Epic boundary is where the run is most re-derivable. | rationale-only | L620 `##### Epic-boundary state-externalization checkpoint` | - | - | Section 3 | none | no | rationale |
| E3-108 | On the aborted path, what "re-derivable" means depends on the scope `##### Aborted-run close-out`'s step 2 resolved — read off the entering branch, since the routing-gate `Cancel` branch opens no marker. | definition | L620 `##### Epic-boundary state-externalization checkpoint` | - | entering branch | `##### Aborted-run close-out` step 2 | none | no | procedure |
| E3-109 | A **mid-Epic** abort (per-Task writer lane, or routing-gate `Cancel` at the per-Task review site) leaves the Epic deliberately not complete, with fewer commits than Tasks. | definition | L620 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-110 | A **Bee-level-review** abort (Section 5 Bee-scoped lane, or routing-gate `Cancel` at Section 5's review) leaves every Epic and Task `done` and every per-Task commit landed, stopping the Bee short of final review. | definition | L620 `##### Epic-boundary state-externalization checkpoint` | - | - | Section 5 | none | no | procedure |
| E3-111 | The cross-session carrier a later session reads to see where the run stopped is the bees ticket state plus the landed commits. | definition | L620 `##### Epic-boundary state-externalization checkpoint` | - | - | - | bees, git | no | procedure |
| E3-112 | The manifest's aborted **Progress** entry serves this run's own post-compaction reader, not the next session, because a resuming `/quo-execute <bee-id>` truncates the manifest at run start. | definition | L620 `##### Epic-boundary state-externalization checkpoint` | - | run-state manifest | `/quo-execute` | none | no | rationale |
| E3-113 | The checkpoint's job is to verify the re-derivability invariant and refresh the durable carriers so the run can be re-derived after any harness compaction. | definition | L620 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-114 | Durable carrier 1 — **bees ticket statuses**: just-completed Epic, its Tasks and Subtasks `done`; the Bee `in_progress`; re-readable via `bees show-ticket` / `bees execute-freeform-query`. | definition | L624 `##### Epic-boundary state-externalization checkpoint` | - | bees | - | bees | no | procedure |
| E3-115 | Durable carrier 2 — **git commits**: one per Task per Section 4.1's after-Task commit step plus any inter-Epic interaction-checkpoint fix commits; re-readable via `git log` / `git diff`. | definition | L625 `##### Epic-boundary state-externalization checkpoint` | - | git history | Section 4.1; Section 4.2 inter-Epic interaction checkpoint | git | no | procedure |
| E3-116 | Durable carrier 3 — **the session-scoped compromise tracker**: the on-disk file defined in Section 6.5 `#### Session-scoped compromise tracker`; re-readable via `Read`. | definition | L626 `##### Epic-boundary state-externalization checkpoint` | - | tracker file | Section 6.5 `#### Session-scoped compromise tracker` | none | unknown | procedure |
| E3-117 | Durable carrier 4 — **the `defer-*` TaskList ledger**: the active deferral set Section 6.5's gate reads; re-readable by walking the TaskList. | definition | L627 `##### Epic-boundary state-externalization checkpoint` | - | TaskList | Section 6.5 | TaskList | unknown | procedure |
| E3-118 | Durable carrier 5 — **the run-state manifest**: defined in Section 1 `#### Write the run-state manifest`; carries multi-Epic run mode, isolation strategy, `<pre-bee-sha>`, compromise-tracker path, per-Epic progress; re-readable via `Read`. | definition | L628 `##### Epic-boundary state-externalization checkpoint` | - | manifest file | Section 1 `#### Write the run-state manifest` | none | yes | procedure |
| E3-119 | When invoked, run the checkpoint's three steps before the invoking branch does whatever it does next (return to step 2, move to final Bee review, or exit). | ordering | L630 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-120 | Every checkpoint step is a tool call the orchestrator can actually make — read a file, run a bees query, run a git command, walk the TaskList, write a file. | invariant | L630 `##### Epic-boundary state-externalization checkpoint` | - | - | - | Bash, bees, git, TaskList | no | procedure |
| E3-121 | Step 1 **Verify the carriers**: do all four verifications, subject to three qualifications that each apply only on the path they name; on every other path all four run as written. | command | L632 `##### Epic-boundary state-externalization checkpoint` | verification results | E3-127..E3-130 | - | bees, git, TaskList | no | procedure |
| E3-122 | **Aborted-path qualification — mid-Epic scope**: when invoked from `##### Aborted-run close-out` at mid-Epic scope, the first two verifications invert rather than fail. | precondition | L634 `##### Epic-boundary state-externalization checkpoint` | - | close-out step 2 scope | `##### Aborted-run close-out` | none | no | procedure |
| E3-123 | Mid-Epic inversion 1: the current Epic and its Tasks are not all `done` and are not to be flipped — confirm each reads the status the abort left it at. | command | L634 `##### Epic-boundary state-externalization checkpoint` | - | bees statuses | - | bees | no | procedure |
| E3-124 | Mid-Epic inversion 2: fewer commits exist than completed Tasks — verify only that every Task that did complete has its commit; do not report the aborted Task's missing commit as a gap. | command | L634 `##### Epic-boundary state-externalization checkpoint` | - | git log | - | git | no | procedure |
| E3-125 | Under the mid-Epic qualification, the tracker and TaskList verifications run unchanged. | invariant | L634 `##### Epic-boundary state-externalization checkpoint` | - | - | - | TaskList | no | procedure |
| E3-126 | **Aborted-path qualification — Bee-level scope**: for an aborted Section 5 Bee-scoped re-dispatch (`aborted-<role>-<bee-id>`) or a `Cancel` at Section 5's review, nothing inverts; run all four verifications on their normal path. | precondition | L635 `##### Epic-boundary state-externalization checkpoint` | - | close-out step 2 scope | Section 5; `##### Aborted-run close-out` | bees, git, TaskList | no | procedure |
| E3-127 | Under the Bee-level qualification, treat any genuine shortfall in Epic/Task/Subtask statuses or one-commit-per-Task as a real gap to fix (subject to the precedence carve-out). | invariant | L635 `##### Epic-boundary state-externalization checkpoint` | - | verification results | - | none | no | procedure |
| E3-128 | Do not import the mid-Epic qualification onto the Bee-level path — it would invert two correct checks and mask a real gap. | invariant | L635 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-129 | On the Bee-level path, "just-completed Epic" resolves to the last Epic this run completed; re-verifying it is idempotent and passes whenever this session completed an Epic. | definition | L635 `##### Epic-boundary state-externalization checkpoint` | - | run history | - | none | no | procedure |
| E3-130 | **Precedence**: when the run started with every Epic `done`, went straight to Section 5, and a Bee-scoped abort or `Cancel` occurred there, the all-Epics-already-done exception governs over the Bee-level qualification. | ordering | L635 `##### Epic-boundary state-externalization checkpoint` | - | run history | Section 5; **Exception — the all-Epics-already-done-at-run-start path** | none | no | procedure |
| E3-131 | Under that precedence, the four verifications stay vacuous and are skipped; verify only that Section 2's placeholders resolved; only step 2's Bee-level **Progress** append applies and no `done` Epic entry is touched. | command | L635 `##### Epic-boundary state-externalization checkpoint` | manifest Progress append | manifest placeholders | Section 2 | none | no | procedure |
| E3-132 | **Exception — the all-Epics-already-done-at-run-start path**: when branch 3 is entered because Section 2's query found every Epic already `done` and this session completed none, skip the four verifications. | precondition | L636 `##### Epic-boundary state-externalization checkpoint` | - | Section 2 query result | Section 2 | none | no | procedure |
| E3-133 | On that exception, verify only that Section 2's `#### Resolve the manifest placeholders` step resolved both placeholders. | command | L636 `##### Epic-boundary state-externalization checkpoint` | - | run-state manifest | Section 2 `#### Resolve the manifest placeholders` | none | no | procedure |
| E3-134 | The manifest must read real Epic IDs under **Epics in scope** and `not captured (all Epics already done)` under **Multi-Epic run mode** — never `pending Section 2 query` or a bare `not captured`. | invariant | L636 `##### Epic-boundary state-externalization checkpoint` | - | manifest **Epics in scope**, **Multi-Epic run mode** | Section 2 | none | no | procedure |
| E3-135 | On that exception, then go to step 2 and write next unit `none`. | ordering | L636 `##### Epic-boundary state-externalization checkpoint` | manifest next unit `none` | - | - | none | no | procedure |
| E3-136 | Verification 1: re-query the just-completed Epic and its Task/Subtask children in bees and confirm every one reads `done`. | command | L640 `##### Epic-boundary state-externalization checkpoint` | verification result | bees | - | bees | no | procedure |
| E3-137 | Verification 2: run `git log --oneline <previous-epic-last-commit>..HEAD` and confirm one commit exists per completed Task. | command | L641 `##### Epic-boundary state-externalization checkpoint` | verification result | `<previous-epic-last-commit>` | - | git | no | procedure |
| E3-138 | `<previous-epic-last-commit>` resolves from the run-state manifest, not memory: **Pre-Bee SHA** at the run's first Epic boundary; the previous Epic's `last commit <sha>` under **Progress** at every later boundary. | definition | L641 `##### Epic-boundary state-externalization checkpoint` | - | manifest **Pre-Bee SHA**, **Progress** | Section 4.2 inter-Epic interaction checkpoint step 1 | none | no | procedure |
| E3-139 | Verification 3: `Read` the compromise-tracker file at the path in the manifest's **Compromise tracker** field and confirm every compromise accepted during this Epic has an entry. | command | L642 `##### Epic-boundary state-externalization checkpoint` | verification result | manifest **Compromise tracker**; tracker file | Section 6.5 | none | unknown | procedure |
| E3-140 | A `Read` reporting the tracker file does not exist is not by itself a gap; read it as "nothing was appended" and check that against what this Epic did. | invariant | L642 `##### Epic-boundary state-externalization checkpoint` | - | `Read` result | - | none | unknown | procedure |
| E3-141 | Treat an absent/missing tracker entry as a gap only if a compromise was accepted, or an ungated path pick that Trigger C requires an entry for was dispatched, during this Epic. | invariant | L642 `##### Epic-boundary state-externalization checkpoint` | - | Epic history | Trigger C | none | unknown | procedure |
| E3-142 | A unit whose sole ungated route hit Trigger C's lone-`trivial-tweak` carve-out is correctly entry-less; do not report it as a gap. | invariant | L642 `##### Epic-boundary state-externalization checkpoint` | - | - | Trigger C | none | unknown | procedure |
| E3-143 | Verification 4: walk the TaskList and confirm every per-Subtask, per-Task, and `gate-*` task from this Epic is `completed`. | command | L643 `##### Epic-boundary state-externalization checkpoint` | verification result | TaskList | - | TaskList | no | procedure |
| E3-144 | Fix any gap now — flip the missed status, land the missed commit, append the missed tracker entry, close the stale TaskList task — rather than carrying it forward in conversation. | recovery | L645 `##### Epic-boundary state-externalization checkpoint` | status flip / commit / tracker entry / task completion | verification results | - | bees, git, TaskList | no | procedure |
| E3-145 | Step 2 **Rewrite the run-state manifest** per Section 1 `#### Write the run-state manifest`, recording the just-completed Epic's ID and last commit SHA under progress. | command | L646 `##### Epic-boundary state-externalization checkpoint` | run-state manifest rewrite | Epic ID; last commit SHA | Section 1 `#### Write the run-state manifest` | none | yes | procedure |
| E3-146 | Step 2 records the next unit: the next Epic's ID on paths that pick one up, or `none` on branch 1's exit, branch 2's Mode 1 decline, and branch 3's move to final review. | field-or-template | L646 `##### Epic-boundary state-externalization checkpoint` | manifest next-unit field | branch classification | - | none | no | procedure |
| E3-147 | On the aborted path, the Progress write branches on the scope `##### Aborted-run close-out`'s step 2 resolved, read off the entering branch (both shapes are specified in Section 1's Progress note). | ordering | L646 `##### Epic-boundary state-externalization checkpoint` | manifest **Progress** | entering branch | `##### Aborted-run close-out`; Section 1 Progress note | none | no | procedure |
| E3-148 | Mid-Epic aborted Progress entry: write the current Epic's entry as aborted, e.g. `<epic-id>: aborted mid-Epic at <task-id> — last commit <sha>`. | field-or-template | L648 `##### Epic-boundary state-externalization checkpoint` | manifest **Progress** entry | Epic ID; Task ID; last landed commit | - | none | no | procedure |
| E3-149 | Bee-level aborted Progress: leave every `done — last commit <sha>` Epic entry exactly as it stands and append a Bee-level line, e.g. `<bee-id>: Bee-level review aborted — test-writer-<bee-id>`, or on `Cancel` the review site plus the finding. | field-or-template | L649 `##### Epic-boundary state-externalization checkpoint` | manifest **Progress** append | Bee ID; aborted lane or Cancel finding | Section 5 | none | no | procedure |
| E3-150 | Never overwrite a legitimately-`done` Epic's Progress entry with an aborted one. | invariant | L649 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-151 | On both aborted scopes, write next unit `none`. | field-or-template | L651 `##### Epic-boundary state-externalization checkpoint` | manifest next-unit `none` | - | - | none | no | procedure |
| E3-152 | Step 3 **Re-read, do not recall**: before each Agent dispatch in the next Epic, `bees show-ticket` the Epic/Task/Subtask body and `Read` the manifest for run-scoped values (mode, isolation strategy, tracker path, `<pre-bee-sha>`). | command | L652 `##### Epic-boundary state-externalization checkpoint` | - | bees ticket bodies; run-state manifest | - | bees | no | procedure |
| E3-153 | Carry no value forward from the prior Epic; never reuse a value quoted earlier in the conversation. | invariant | L652 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-154 | If a fact the next Epic needs is not readable from one of the five carriers, stop and write it into one before dispatching anything. | recovery | L652 `##### Epic-boundary state-externalization checkpoint` | carrier write | five carriers | - | none | no | procedure |
| E3-155 | The checkpoint does not clear, compact, or reclaim the orchestrator's context and must never be narrated as if it did. | invariant | L654 `**What this checkpoint does not do.**` | - | - | - | none | no | procedure |
| E3-156 | No model-invocable self-clearing or self-compacting mechanism exists; token reclamation is owned by the harness. | definition | L654 `**What this checkpoint does not do.**` | - | - | - | none | no | rationale |
| E3-157 | A single Epic's working-set footprint is estimated at roughly **~25-30% of a 1M context window**. | definition | L654 `**What this checkpoint does not do.**` | - | - | - | none | no | rationale |
| E3-158 | Treat that ~25-30% figure as an unverified working estimate; never describe it as observed, measured, or benchmarked; it is not a budget this skill measures or enforces. | invariant | L654 `**What this checkpoint does not do.**` | - | - | - | none | no | procedure |
| E3-159 | The orchestrator must not invent or estimate its own remaining context from memory, report a self-measured usage percentage, or gate any branch on such a self-estimate. | invariant | L654 `**What this checkpoint does not do.**` | - | - | - | none | no | procedure |
| E3-160 | Reading an out-of-band, machine-written context-usage gauge produced by a separate status-line process, and stopping at a boundary on it, is permitted — it is not a self-estimate. | definition | L654 `**What this checkpoint does not do.**` | - | gauge file | - | none | no | procedure |
| E3-161 | Neither the gauge read nor the boundary stop reclaims any context. | invariant | L654 `**What this checkpoint does not do.**` | - | - | - | none | no | procedure |
| E3-162 | The only reclamation lever is starting a **fresh session** at an Epic boundary (branch 1 recommends exactly that); the checkpoint makes that lever and any harness compaction lossless. | definition | L654 `**What this checkpoint does not do.**` | - | - | Section 4.2 branch 1 | none | no | rationale |
| E3-163 | Every path out of Section 4.2's branches invokes the checkpoint by name; there is no way to leave that section without running it. | invariant | L656 `##### Epic-boundary state-externalization checkpoint` | - | - | Section 4.2 | none | no | procedure |
| E3-164 | Exit-path enumeration: branch 2 Mode 1 **accept** and branch 2 Mode 2 auto-continue (next unit: next Epic's ID); branch 2 Mode 1 **decline**, branch 1 breakdown exit, branch 3 final review (next unit: `none`). | definition | L658-L660 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-165 | The three `none` paths still run the checkpoint in full — what it verifies and writes outlives the conversation. | invariant | L662 `##### Epic-boundary state-externalization checkpoint` | - | - | - | none | no | procedure |
| E3-166 | One further invoker outside Section 4.2's branches: `##### Aborted-run close-out` (next unit: `none`). | definition | L664 `##### Epic-boundary state-externalization checkpoint` | - | - | `##### Aborted-run close-out` | none | no | procedure |
| E3-167 | The close-out's three entering branches: Section 3's Reconcile-step unexplained-movement gate's **Abort this unit** choice, and the `Cancel` choice at either routing gate (part (d)'s, and part (c)'s in a `blocker`'s choice set) of `### Orchestrator discipline: routing review findings`. | definition | L664 `##### Epic-boundary state-externalization checkpoint` | - | - | Section 3 Reconcile step; `### Orchestrator discipline: routing review findings` parts (c), (d) | none | unknown | procedure |
| E3-168 | `##### Epic-boundary context-window guard` is a definition, not a step in Section 4.2's linear flow; run it only where an invoking branch calls for it by name. | invariant | L666-L668 `##### Epic-boundary context-window guard` | - | - | Section 4.2 | none | no | procedure |
| E3-169 | Exactly two paths invoke the guard: branch 2's Mode 1 **accept** path and branch 2's Mode 2 auto-continue path (next unit: next Epic's ID on both). | definition | L668-L671 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-170 | The guard runs after the state-externalization checkpoint has completed and before control returns to step 2. | ordering | L673 `##### Epic-boundary context-window guard` | - | checkpoint completion | **Epic-boundary state-externalization checkpoint** | none | no | procedure |
| E3-171 | The guard runs on no `next unit: none` run-ending path — not branch 1's exit, not Mode 1 decline, not branch 3's move to final review. | invariant | L673 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-172 | Rationale: on a run ending in-session a fresh-session stop is noise, and a missing-reading gate would fire `AskUserQuestion` right before final review. | rationale-only | L673 `##### Epic-boundary context-window guard` | - | - | - | none | no | rationale |
| E3-173 | A single-Epic run that crosses no Epic boundary never reaches the guard. | definition | L673 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-174 | The state-externalization checkpoint is unconditional on every exit path; the guard deliberately is not — do not inherit its unconditionality. | invariant | L673 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-175 | The guard reads an external gauge a separate status-line producer wrote to a file (the `context_gauge.py` producer/reader contract); it does not measure the orchestrator's own token usage. | definition | L675 `**What this guard is (and is not).**` | - | gauge file | `context_gauge.py`; *What this checkpoint does not do* | Bash | no | procedure |
| E3-176 | Neither the guard's read nor its boundary stop reclaims context; the only reclamation lever is a **fresh session**, which every guard stop recommends. | invariant | L675 `**What this guard is (and is not).**` | - | - | - | none | no | procedure |
| E3-177 | Do not add any "clear your working context" instruction to the guard; no self-clear or self-compact lever exists. | invariant | L675 `**What this guard is (and is not).**` | - | - | - | none | no | procedure |
| E3-178 | Guard **Step 1**: read the session id first and evaluate it before creating any gate task, mirroring Section 1's `#### Check session reasoning effort` ordering rule. | ordering | L679 `##### Epic-boundary context-window guard` | - | `CLAUDE_CODE_SESSION_ID` | Section 1 `#### Check session reasoning effort` | Bash | no | procedure |
| E3-179 | Do NOT `TaskCreate` before the session-id evaluation — a stranded `pending` `gate-*` task violates the two-step contract's yield-control discipline. | invariant | L679 `##### Epic-boundary context-window guard` | - | - | two-step contract | TaskList | no | procedure |
| E3-180 | Session-id read command: `printenv CLAUDE_CODE_SESSION_ID` (POSIX) / `Write-Output $env:CLAUDE_CODE_SESSION_ID` (PowerShell). | command | L681-L689 `##### Epic-boundary context-window guard` | session id (stdout) | environment | - | Bash | no | procedure |
| E3-181 | Trim any trailing whitespace/newline from the session id before use; a trailing newline fails the helper's `--session-id` validation. | invariant | L691 `##### Epic-boundary context-window guard` | trimmed session id | E3-180 output | - | none | no | procedure |
| E3-182 | Guard **Step 2**: session id unset or empty → skip the guard silently and continue to step 2 of branch 2 — no gate, no `TaskCreate`, no output. | precondition | L693 `##### Epic-boundary context-window guard` | - | E3-180 output | Section 1 `CLAUDE_EFFORT`-unset handling | none | no | procedure |
| E3-183 | The unset-session-id path is the only silent-skip path (the unsupported-CLI carve-out). | invariant | L693 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-184 | Guard **Step 3a**: resolve the gauge helper as a sibling of this skill's base directory: `<this skill's base directory>/../quo-setup/scripts/context_gauge.py` (POSIX) / `<this skill's base directory>\..\quo-setup\scripts\context_gauge.py` (PowerShell). | command | L697-L707 `##### Epic-boundary context-window guard` | helper path | skill invocation header base directory | `scoped_marker_resolver.py` sibling-resolution in Section 3; `quo-setup` | none | no | procedure |
| E3-185 | Guard **Step 3b**: obtain the stop threshold with one literal call `python3 "<this skill's base directory>/../quo-setup/scripts/context_gauge.py" stop-threshold` (or `python …\context_gauge.py stop-threshold` on PowerShell); it prints a single integer percentage. | command | L709-L719 `##### Epic-boundary context-window guard` | threshold integer | helper path | `context_gauge.py` | Bash | no | procedure |
| E3-186 | NEVER restate the threshold number in prose; read it from the helper each time so this skill carries no second definition site. | invariant | L709 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-187 | Guard **Step 3c**: read the current reading with one literal call `python3 "<this skill's base directory>/../quo-setup/scripts/context_gauge.py" read --session-id <trimmed-session-id>` (PowerShell: `python …\context_gauge.py read --session-id <trimmed-session-id>`). | command | L721-L731 `##### Epic-boundary context-window guard` | reading (stdout + exit status) | trimmed session id; helper path | `context_gauge.py` | Bash | no | procedure |
| E3-188 | Guard **Step 3d**: branch on the reading's stdout and exit status. | ordering | L733 `##### Epic-boundary context-window guard` | - | E3-187 output | - | none | no | procedure |
| E3-189 | `read` prints exactly one of four values — an integer percentage, `no-reading`, `stale`, or `missing` — and exits `0` for all four. | definition | L733 `##### Epic-boundary context-window guard` | - | - | `context_gauge.py` | Bash | no | procedure |
| E3-190 | `read` exits non-zero on a malformed gauge OR an invalid `--session-id`; do NOT assume a non-zero exit implies a corrupt file. | definition | L733 `##### Epic-boundary context-window guard` | - | - | `context_gauge.py` | Bash | no | procedure |
| E3-191 | Integer ≥ threshold → STOP at this boundary: report the current percentage, recommend resuming in a **fresh session** naming `/quo-execute <bee-id>`, then exit the skill. | choice-set | L735 `##### Epic-boundary context-window guard` | printed report; skill exit | reading; threshold | `/quo-execute` | none | no | procedure |
| E3-192 | Integer < threshold → continue to step 2 of branch 2, no output. | choice-set | L736 `##### Epic-boundary context-window guard` | - | reading; threshold | - | none | no | procedure |
| E3-193 | `no-reading` → continue to step 2 of branch 2, no output (transient fresh-but-null reading; producer alive). | choice-set | L737 `##### Epic-boundary context-window guard` | - | reading | - | none | no | procedure |
| E3-194 | `stale` → STOP at this boundary; note the producer appears stalled and recurring staleness across fresh sessions means the operator should check their status-line producer; recommend `/quo-execute <bee-id>` fresh-session resume. | choice-set | L738 `##### Epic-boundary context-window guard` | printed report; skill exit | reading | `/quo-execute` | none | no | procedure |
| E3-195 | Do NOT let `stale` fall through to continue. | invariant | L738 `##### Epic-boundary context-window guard` | - | - | - | none | no | procedure |
| E3-196 | Non-zero exit or empty stdout → the same fail-safe stop-and-ask as `stale`: STOP and recommend `/quo-execute <bee-id>` fresh-session resume; never fall through to continue. | choice-set | L739 `##### Epic-boundary context-window guard` | printed report; skill exit | exit status; stdout | `/quo-execute` | none | no | procedure |
| E3-197 | `missing` → first check the persistent opt-out marker at `<tempdir>/.quorum/context-guard-opt-out` with one literal existence check. | command | L740 `##### Epic-boundary context-window guard` | marker presence | reading | - | Bash | no | procedure |
| E3-198 | Opt-out existence check command: `test -f /tmp/.quorum/context-guard-opt-out` (POSIX) / `Test-Path "$env:TEMP\.quorum\context-guard-opt-out"` (PowerShell). | command | L742-L750 `##### Epic-boundary context-window guard` | marker presence | filesystem | - | Bash | no | procedure |
| E3-199 | If the opt-out marker is present → skip the guard silently and continue to step 2 of branch 2 (operator's standing choice to run unguarded). | choice-set | L752 `##### Epic-boundary context-window guard` | - | marker presence | - | none | no | procedure |
| E3-200 | If the opt-out marker is absent → hard-stop via the two-step gate in step 4. | choice-set | L752 `##### Epic-boundary context-window guard` | - | marker presence | Step 4 | TaskList, AskUserQuestion | no | procedure |
| E3-201 | Guard **Step 4 — the missing-reading gate**: first `TaskCreate` a `gate-askuserquestion-<short-suffix>` task naming this boundary context-guard gate (distinct per-fire suffix), then `AskUserQuestion` in the same turn. | gate | L754 `##### Epic-boundary context-window guard` | `gate-askuserquestion-*` task; AskUserQuestion | - | Section 3 TaskList naming convention gate-task entry | TaskList, AskUserQuestion | no | procedure |
| E3-202 | Honor the yield-control discipline: do not yield control while the `gate-*` task is `pending`/`in_progress`. | invariant | L754 `##### Epic-boundary context-window guard` | - | - | - | TaskList | no | procedure |
| E3-203 | Mark the missing-reading `gate-*` task `completed` the moment the answer is consumed. | command | L754 `##### Epic-boundary context-window guard` | `gate-*` → `completed` | user answer | - | TaskList | no | procedure |
| E3-204 | Gate question text (a): state that the environment may override the operator's user-level status-line config, so no reading is being published. | field-or-template | L754 `##### Epic-boundary context-window guard` | question text | - | - | AskUserQuestion | no | procedure |
| E3-205 | Gate question text (b): publish the gauge file contract — path `<tempdir>/.quorum/context-usage-<session_id>.json`, required fields `session_id` and a `context_window` object carrying `used_percentage`, and overwrite-per-refresh semantics. | field-or-template | L754 `##### Epic-boundary context-window guard` | question text | - | - | AskUserQuestion | no | procedure |
| E3-206 | Gate question text (c): state that **Configure now** runs `/quo-setup --configure-gauge-producer` inline. | field-or-template | L754 `##### Epic-boundary context-window guard` | question text | - | `/quo-setup` | AskUserQuestion | no | procedure |
| E3-207 | Present the gate options as multi-choice only; do not add fake free-text options duplicating `AskUserQuestion`'s auto-appended `Type something.` / `Chat about this` slot. | invariant | L754 `##### Epic-boundary context-window guard` | - | - | - | AskUserQuestion | no | procedure |
| E3-208 | Option 1 **Configure now**: invoke `/quo-setup --configure-gauge-producer` inline via the Skill tool. | choice-set | L756 `##### Epic-boundary context-window guard` | Skill invocation | user answer | `/quo-setup` | none | no | procedure |
| E3-209 | After a successful Configure now, recommend resuming in a fresh session with `/quo-execute <bee-id>` rather than implying this run is now guarded, then exit the skill. | ordering | L756 `##### Epic-boundary context-window guard` | printed recommendation; skill exit | Configure now result | `/quo-execute` | none | no | procedure |
| E3-210 | Rationale: a freshly-configured producer only begins publishing next session. | rationale-only | L756 `##### Epic-boundary context-window guard` | - | - | - | none | no | rationale |
| E3-211 | Option 2 **Proceed without the guard (this run)**: continue to step 2 of branch 2; write no marker. | choice-set | L757 `##### Epic-boundary context-window guard` | - | user answer | - | none | no | procedure |
| E3-212 | Option 3 **Never guard me (persistent opt-out)**: write the opt-out marker via `python3 "<this skill's base directory>/../quo-setup/scripts/context_gauge.py" write-opt-out` (PowerShell: `python …\context_gauge.py write-opt-out`), then continue to step 2 of branch 2. | choice-set | L758-L768 `##### Epic-boundary context-window guard` | `<tempdir>/.quorum/context-guard-opt-out` marker | user answer; helper path | `context_gauge.py` | Bash | no | procedure |
| E3-213 | Option 4 **Stop here**: exit the skill with the `/quo-execute <bee-id>` fresh-session resume command. | choice-set | L770 `##### Epic-boundary context-window guard` | skill exit | user answer | `/quo-execute` | none | no | procedure |
| E3-214 | `##### Aborted-run close-out` is the canonical anchor name for the path where the run stops without the unit it was working advancing. | definition | L772-L774 `##### Aborted-run close-out` | - | - | - | none | unknown | procedure |
| E3-215 | Three branches route to the close-out: Section 3's Reconcile-step unexplained-movement gate's **Abort this unit**; the routing-decision gate's **Cancel** (part (d)); the scope-bounding gate's **Cancel** (part (c), offered only in a `blocker`'s choice set). | definition | L774 `##### Aborted-run close-out` | - | gate answers | Section 3 Reconcile step; `### Orchestrator discipline: routing review findings` parts (c), (d) | none | unknown | procedure |
| E3-216 | The two `Cancel` branches behave identically here; wherever this skill branches on the routing-gate `Cancel`, read it as covering either gate's `Cancel`. | invariant | L774 `##### Aborted-run close-out` | - | - | - | none | unknown | procedure |
| E3-217 | The close-out is a definition, not a step in Section 4.2's linear flow; run it only where a branch calls for it by name. | invariant | L774 `##### Aborted-run close-out` | - | - | Section 4.2 | none | unknown | procedure |
| E3-218 | Failure narrative: exiting by simply stopping lets an `aborted-*` marker and half-swept TaskList survive into the next session, where part (g)'s **Clause 2** wedges the first Engineer dispatch, and leaves the manifest claiming a next unit never picked up. | rationale-only | L776 `##### Aborted-run close-out` | - | - | part (g) **Clause 2** | none | unknown | failure-narrative |
| E3-219 | Run the close-out's three steps in order. | ordering | L776 `##### Aborted-run close-out` | - | - | - | none | unknown | procedure |
| E3-220 | Close-out step 1 **Close out the TaskList, sweeping by name prefix**; two scopes apply — sweep as applicable to the aborted lane, or on a routing-gate `Cancel`, to the review site the gate fired from. | command | L778 `##### Aborted-run close-out` | TaskList tasks → `completed` | aborted lane / review site | Section 4.1 step 3 | TaskList | unknown | procedure |
| E3-221 | **Per-Task scope** sweep: every `<role>-<subtask-id>` implementer task under the affected Task, that Task's `pm-<task-id>`, every `-r<n>` round of any of them, and the `gate-*` task that fired the branch routing here. | name-class | L780 `##### Aborted-run close-out` | TaskList tasks → `completed` | TaskList names | Section 4.1 step 3 | TaskList | unknown | procedure |
| E3-222 | The Per-Task scope sweep is Section 4.1 step 3's sweep run at an abort instead of at a Task completion. | definition | L780 `##### Aborted-run close-out` | - | - | Section 4.1 step 3 | TaskList | unknown | procedure |
| E3-223 | **Bee scope** sweep applies when the aborted lane was a Section 5 Bee-scoped re-dispatch, or when a routing-gate `Cancel` fired at Section 5's Bee-level review site. | precondition | L781 `##### Aborted-run close-out` | - | aborted lane / review site | Section 5 | TaskList | no | procedure |
| E3-224 | Bee scope sweep: every `*-<bee-id>` name Section 5's **Bee-level TaskList close-out** enumerates (three Bee-scoped reviewers, four Bee-scoped implementer/PM names, their `-r<n>` rounds), and the `gate-*` task that fired the branch. | name-class | L781 `##### Aborted-run close-out` | TaskList tasks → `completed` | TaskList names | Section 5 **Bee-level TaskList close-out** | TaskList | no | procedure |
| E3-225 | Rationale: Section 5's enumeration carries no `gate-*` name, so a `Cancel` at that review would otherwise strand its own gate task `pending`. | rationale-only | L781 `##### Aborted-run close-out` | - | - | Section 5 | none | no | rationale |
| E3-226 | The orchestrator cannot terminate a dispatched background Agent; an `in_progress` task in either scope may still have a live Agent behind it. | definition | L783 `##### Aborted-run close-out` | - | - | - | Agent | unknown | procedure |
| E3-227 | Mark an in-flight sibling lane's task `completed` anyway — the run is ending and no tick will consume its notification. | command | L783 `##### Aborted-run close-out` | TaskList task → `completed` | in-flight task | - | TaskList | unknown | procedure |
| E3-228 | Record in that task's `metadata.activity` that it was in flight at the abort, so a resuming session treats the lane's work as possibly half-landed and re-checks it instead of trusting ticket state. | field-or-template | L783 `##### Aborted-run close-out` | `metadata.activity` | - | - | TaskList | unknown | procedure |
| E3-229 | In both scopes, mark every `aborted-<role>-<id>` redelivery marker still `pending` as `completed` when one is open. | command | L785 `##### Aborted-run close-out` | marker task → `completed` | open markers | - | TaskList | unknown | procedure |
| E3-230 | The movement-gate branch is reached with an `aborted-*` marker open (the abort created it); a routing-gate `Cancel` opens none. | definition | L785 `##### Aborted-run close-out` | - | entering branch | - | TaskList | unknown | procedure |
| E3-231 | Rationale: a marker left `pending` blocks the unit's advance in a later session. | rationale-only | L785 `##### Aborted-run close-out` | - | - | - | none | unknown | rationale |
| E3-232 | Record the abort reason in the marker's `metadata.activity`; that string is informational context for whoever resumes and is never a routing input. | field-or-template | L785 `##### Aborted-run close-out` | `metadata.activity` | abort reason | - | TaskList | unknown | procedure |
| E3-233 | Throughout close-out step 1, "clear from the active set" means mark the task `completed` — never delete it. | invariant | L785 `##### Aborted-run close-out` | - | - | - | TaskList | unknown | procedure |
| E3-234 | Rationale: a deleted task takes the `-r<n>` round history with it; the next round's discriminator is derived by counting rounds already on the list. | rationale-only | L785 `##### Aborted-run close-out` | - | - | - | none | unknown | rationale |
| E3-235 | Close-out step 2: run the **Epic-boundary state-externalization checkpoint** on its aborted path — step 1 with the two aborted-path qualifications, step 2 with the scope-branched aborted **Progress** entry and next unit `none`. | command | L786 `##### Aborted-run close-out` | checkpoint run; manifest rewrite | close-out scope | **Epic-boundary state-externalization checkpoint** | bees, git, TaskList | no | procedure |
| E3-236 | Pick the qualification matching the scope this close-out was entered at, read off the entering branch, not off a marker. | ordering | L786 `##### Aborted-run close-out` | resolved scope | entering branch | - | none | no | procedure |
| E3-237 | Scope resolution on the movement-gate branch: an `aborted-<role>-<subtask-id>` marker ⇒ **mid-Epic**; a Section 5 Bee-scoped lane ⇒ **Bee-level**. | definition | L786 `##### Aborted-run close-out` | resolved scope | aborted lane | Section 5 | none | no | procedure |
| E3-238 | Scope resolution on the routing-gate `Cancel` branch: the per-Task review site ⇒ **mid-Epic**; Section 5's Bee-level review ⇒ **Bee-level**. | definition | L786 `##### Aborted-run close-out` | resolved scope | review site | Section 5 | none | no | procedure |
| E3-239 | Everything else in the checkpoint runs unchanged on the aborted path: verify the compromise tracker, verify the TaskList is clear, rewrite the manifest, re-read rather than recall on any later dispatch. | invariant | L786 `##### Aborted-run close-out` | - | - | **Epic-boundary state-externalization checkpoint** | none | no | procedure |
| E3-240 | Close-out step 3 **Stop the run and name the resume command**: tell the user the run stopped without the unit it was working advancing. | command | L787 `##### Aborted-run close-out` | printed message | - | - | none | unknown | procedure |
| E3-241 | For a per-Task lane abort, name which Subtask and which Task the run stopped in. | field-or-template | L787 `##### Aborted-run close-out` | printed message | Subtask ID; Task ID | - | none | unknown | procedure |
| E3-242 | For a Section 5 Bee-scoped lane abort, name which Bee-level lane aborted (`test-writer-<bee-id>` / `doc-writer-<bee-id>`) and say the run stopped in the Bee-level review, since every Subtask and Task is `done`. | field-or-template | L787 `##### Aborted-run close-out` | printed message | aborted lane name | Section 5 | none | no | procedure |
| E3-243 | When a routing-gate `Cancel` routed here, name which review site the `Cancel` fired from (per-Task review site, or Section 5's Bee-level review) and the finding it fired on, in place of a lane name. | field-or-template | L787 `##### Aborted-run close-out` | printed message | review site; finding | Section 5 | none | unknown | procedure |
| E3-244 | Also name the uncommitted working-tree state the exit leaves behind: at **mid-Epic** scope the in-flight Task's edits; at **Bee-level** scope any Bee-level Engineer round's edits since the last per-Task commit. | field-or-template | L787 `##### Aborted-run close-out` | printed message | resolved scope; working tree | - | git | no | procedure |
| E3-245 | Rationale: a resuming session inherits that tree either way, so say what is in it rather than leaving it to be discovered. | rationale-only | L787 `##### Aborted-run close-out` | - | - | - | none | unknown | rationale |
| E3-246 | Then name the literal resume command **`/quo-execute <bee-id>`** — the same command every other stop site in this skill names — and exit the skill. | command | L787 `##### Aborted-run close-out` | printed resume command; skill exit | Bee ID | `/quo-execute` | none | no | procedure |
| E3-247 | Do NOT run the **Epic-boundary context-window guard** at the aborted-run close-out; it runs only on branch 2's two continuing in-session paths and this is a run-ending path. | invariant | L787 `##### Aborted-run close-out` | - | - | **Epic-boundary context-window guard** | none | no | procedure |

## Anchors defined here

- L521 `### 4. Per-Task and Per-Epic Cleanup`
- L523 `#### 4.1 After Each Task`
- L527 **Do this before committing**
- L528 **NEVER push to remote — committing only.**
- L529 **Format**
- L529 **Compile/type-check**, **Lint**, **Narrow test** (lane rung names)
- L543 **Do NOT blindly `git add -A`**
- L545 **Sweep by name prefix, not by exact name**
- L545 **The sweep includes any `aborted-<role>-<subtask-id>` redelivery markers**
- L545 **The aborted path runs this same sweep at its own site:**
- L549 `## Task [N] of [total] Complete: [task-title]` (summary heading template)
- L551 **Task ID**
- L552 **Files Changed**
- L553 **Reviews**
- L554 **Ignored Review Feedback**
- L555 **Second-order effects** (summary field)
- L556 **Follow-up Tasks Created**
- L562 **Second-order effects (rendered into the summary block above).**
- L562 **destination**; **per-Task**; **Bee-level**
- L564 **Collect every relayed narrative attaching to this Task**; **verbatim**
- L565 **Keep the sub-block labels**
- L566 **De-duplicate exact repeats**
- L567 **When every source reported the fixed empty line**
- L569 **Record each ignored-feedback item as a `defer-N` TaskList task at the moment of decision.**
- L571 **Record each PM-deferred item as a `defer-N` TaskList task at the moment of the PM verdict.**
- L573 `#### 4.2 Find next Epic or move to Final Review`
- L577 **inter-Epic interaction checkpoint**
- L581 **Epic-boundary state-externalization checkpoint** (forward reference); **Pre-Bee SHA**; **Progress**
- L583 **Contract drift**
- L584 **Resource compounding**
- L585 **Symmetric-change gaps**
- L603 **Drafted (or blocked-on-drafted) Epics remain**; **Stop the loop. Do NOT proceed to Step 5 final review and do NOT offer to mark the Bee done.**
- L609 **Workable Epic remains**; **multi-Epic run mode**
- L611 **Mode 1 (Stop after each Epic), or Section 2's mode prompt was skipped**; **Epic-boundary context-window guard** (forward reference)
- L612 **Mode 2 (Work through all Epics)**
- L614 **All Epics under this Bee are `done`**
- L616 `##### Epic-boundary state-externalization checkpoint`
- L618 **It is a definition, not a step in Section 4.2's linear flow**
- L620 **On the aborted path**; **scope that close-out's step 2 resolved**; **mid-Epic**; **Bee-level-review**; **later session**; **bees ticket state plus the landed commits**; **Progress**; **verify that invariant and refresh the durable carriers**
- L624 **bees ticket statuses**
- L625 **git commits**
- L626 **the session-scoped compromise tracker**
- L627 **the `defer-*` TaskList ledger**
- L628 **the run-state manifest**
- L632 **Verify the carriers.**
- L634 **Aborted-path qualification — mid-Epic scope.**; **fewer commits exist than completed Tasks**
- L635 **Aborted-path qualification — Bee-level scope.**; **Section 5 Bee-scoped re-dispatch**; **nothing inverts**; **all four verifications on their normal path**; **last Epic this run completed**; **whenever this session completed an Epic of its own**; **Precedence when it did not**; **the exception governs.**; **Progress**
- L636 **Exception — the all-Epics-already-done-at-run-start path.**; **Epics in scope**; **Multi-Epic run mode**
- L641 **`<previous-epic-last-commit>` resolves from the run-state manifest**; **Pre-Bee SHA**; **Progress**
- L642 **Compromise tracker**; **A `Read` reporting that the file does not exist is not by itself a gap**; **was accepted**; **that Trigger C requires an entry for**
- L645 **now**
- L646 **Rewrite the run-state manifest**; **On the aborted path**; **scope `##### Aborted-run close-out`'s step 2 resolved**
- L648 **Mid-Epic**; **Progress**
- L649 **Bee-level**; **correct**; **append**
- L652 **Re-read, do not recall, at every dispatch in the next Epic.**; **no**
- L654 **What this checkpoint does not do.**; **~25-30% of a 1M context window**; **unverified working estimate carried over from this discipline's original design note**; **not**; **fresh session**
- L658 **accept**
- L659 **decline**
- L664 **outside**; **Abort this unit**
- L666 `##### Epic-boundary context-window guard`
- L668 **definition, not a step in Section 4.2's linear flow**; **two**
- L670 **accept**
- L673 **after**; **before**; **no**; **this guard deliberately is not — do not inherit its unconditionality.**
- L675 **What this guard is (and is not).**; **external**; **fresh session**
- L679 **Step 1 — read the session id first, and evaluate it before creating any gate task.**
- L691 **Trim any trailing whitespace/newline**
- L693 **Step 2 — session id unset or empty → skip the guard silently and continue**; **only**
- L695 **Step 3 — session id present.**
- L709 **Obtain the stop threshold from the helper**; **NEVER restate that number in prose**
- L721 **Read the current reading**
- L735 **integer ≥ threshold → STOP**; **fresh session**
- L736 **integer < threshold → continue**
- L737 **`no-reading` → continue**
- L738 **`stale` → STOP**
- L739 **non-zero exit or empty stdout → the same fail-safe stop-and-ask as `stale`**
- L740 **`missing`**
- L752 **If the marker is present → skip the guard silently and continue**; **hard-stop via the two-step gate**
- L754 **Step 4 — the missing-reading gate.**; **first**; **then**; **Configure now**
- L756 **Configure now**; **next**; **recommends resuming in a fresh session**
- L757 **Proceed without the guard (this run)**
- L758 **Never guard me (persistent opt-out)**
- L770 **Stop here**
- L772 `##### Aborted-run close-out`
- L774 **without the unit it was working advancing**; **Abort this unit**; **Cancel**; **it is a definition, not a step in Section 4.2's linear flow**
- L776 **Clause 2**
- L778 **Close out the TaskList, sweeping by name prefix.**
- L780 **Per-Task scope**
- L781 **Bee scope**; **or when a routing-gate `Cancel` fired at Section 5's Bee-level review site**; **Bee-level TaskList close-out**; **and the `gate-*` task that fired the branch routing here**
- L783 **A sibling lane still in flight when this close-out fires.**
- L785 **every `aborted-<role>-<id>` redelivery marker still `pending`**; **when one is open**; **"clear from the active set" means mark the task `completed`**
- L786 **Run the Epic-boundary state-externalization checkpoint**; **aborted path**; **mid-Epic**; **Bee-level**; **Progress**; **Pick the qualification matching the scope this close-out was entered at**
- L787 **Stop the run and name the resume command.**; **branching on the scope step 2 resolved**; **which Subtask and which Task**; **which Bee-level lane**; **which review site the `Cancel` fired from**; **Also name the uncommitted working-tree state the exit leaves behind**; **mid-Epic**; **Bee-level**; **`/quo-execute <bee-id>`**; **NOT**; **Epic-boundary context-window guard**

## Rationale-only spans

- L529 (second half, "Re-running wastes minutes per Task.") — why not re-run tests (partial line)
- L562 (sentence "Without a named destination …") — why the destination field exists (partial line)
- L567 (sentence "It is unconditional: …") — why field is unconditional (partial line)
- L569 (sentence "This upstream record-creating step …") — why defer ledger is load-bearing (partial line)
- L571 (last two sentences) — two-layer symmetry with breakdown-epic (partial line)
- L577 (sentence "This is a lightweight check …") — why checkpoint sits at boundary (partial line)
- L620 (first two sentences, through "…by the conversation.") — context grows monotonically; boundary re-derivable (partial line)
- L654 (whole paragraph) — checkpoint reclaims nothing; estimate caveats (1 line; mixed rationale and prohibitions)
- L662 (sentence "the run is ending … outlives the conversation") — why `none` paths still run (partial line)
- L673 (clause "because on a run that is ending …") — why guard skips run-ending paths (partial line)
- L776 (first two sentences) — failure narrative: stranded marker wedges resume (partial line)
- L781 (clause "that enumeration carries no `gate-*` name …") — why gate task added to sweep (partial line)
- L785 (clause "a deleted task takes the `-r<n>` round history …") — why never delete tasks (partial line)

No full multi-line span in this slice is rationale-only; every paragraph carries at least one rule. Rationale is interleaved at the sentence level (13 partial-line spans, ~13 lines of the 268-line slice touched).
