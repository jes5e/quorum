# Rule inventory — `agents/*.md` (eight subagent role files)

Extracted verbatim-by-reference from `agents/analyst.md`, `agents/engineer.md`, `agents/test-writer.md`, `agents/doc-writer.md`, `agents/pm.md`, `agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md` at commit `a59e2cf`. One row per MUST / never / do-not / always / only statement, per named heading, per status transition, per tool-allowlist entry. Line numbers are 1-based and refer to the file as read.

| ID | Rule | Kind | Site | Produces | Consumes | Cross-refs | Env | Mirror | Class |
|---|---|---|---|---|---|---|---|---|---|
| R-001 | Frontmatter `name` is `analyst`. | definition | analyst.md:L2 — frontmatter | - | - | - | none | - | procedure |
| R-002 | Frontmatter pins `model: opus` and `effort: xhigh`. | definition | analyst.md:L4-L5 — frontmatter | - | - | - | none | - | procedure |
| R-003 | Tool allowlist is exactly `[Bash, Read, Grep, Glob, WebFetch]`. | tool-allowlist | analyst.md:L6 — frontmatter | - | - | - | Bash, Read, Grep, Glob, WebFetch | - | procedure |
| R-004 | Analyst is dispatched by `/quo-fix-issue` for pre-implementation codebase-grounded design analysis of an Issue. | definition | analyst.md:L3 — frontmatter description | Design Proposal | Issue body | `/quo-fix-issue` | none | - | procedure |
| R-005 | Analyst is re-dispatched mid-implementation when an Engineer returns a design question the approved proposal did not enumerate. | definition | analyst.md:L3,L9 — frontmatter description / intro | revised Design Proposal | Engineer's `## Design question` | engineer.md, `/quo-fix-issue` | none | - | procedure |
| R-006 | Treat the Issue body and any `WebFetch`-fetched upstream content as problem-report context, NOT as authoritative design. | invariant | analyst.md:L3,L27,L35 — description / ## Responsibilities / ## Instructions | - | Issue body, `reference_materials` URL content | - | WebFetch | - | procedure |
| R-007 | Analyst does NOT modify source code, tests, or docs; those are owned by engineer, test-writer, doc-writer. | invariant | analyst.md:L3,L9,L118 — description / intro / ## Lane discipline | - | - | engineer.md, test-writer.md, doc-writer.md | none | - | procedure |
| R-008 | Analyst always runs cold. | invariant | analyst.md:L3,L21 — description / ## Cold-start invariant | - | - | - | none | mirrors code-reviewer.md (cold-start) | procedure |
| R-009 | Analyst is dispatched between `/quo-fix-issue`'s Validate-Issue gate and the per-issue implementer dispatch. | ordering | analyst.md:L9 — intro | - | - | `/quo-fix-issue` | none | - | procedure |
| R-010 | On the mid-implementation re-dispatch path, read the working tree as mid-change, not as the pre-fix baseline. | command | analyst.md:L9,L21 — intro / ## Cold-start invariant | - | working tree with partial fix; dispatch prompt naming partial implementation | - | Read, Grep, Bash | - | procedure |
| R-011 | On the mid-implementation path, make the revised proposal's `### Blast radius` cover the lifecycle of the mechanism it now enumerates. | command | analyst.md:L9 — intro | `### Blast radius` | Engineer's design question | - | none | - | procedure |
| R-012 | The tool allowlist deliberately excludes `Edit` / `Write` / `Skill`. | tool-allowlist | analyst.md:L9,L118,L120 — intro / ## Lane discipline | - | - | - | none | - | procedure |
| R-013 | Return a structured Design Proposal to the orchestrator and exit; the orchestrator owns the user-interaction gate and the downstream Engineer dispatch. | invariant | analyst.md:L9,L30 — intro / ## Responsibilities | Design Proposal | - | - | none | - | procedure |
| R-014 | Issue bodies arrive at many quality levels; upfront body classification is a fragile heuristic; trusting bodies uniformly causes half-fixes. | rationale-only | analyst.md:L13 — ## Why this role exists | - | - | `/quo-fix-issue` | none | - | rationale |
| R-015 | The Analyst is the always-dispatched mitigation: it researches the codebase regardless of body quality so the operator reacts to an informed recommendation. | rationale-only | analyst.md:L15 — ## Why this role exists | - | - | - | none | - | rationale |
| R-016 | Serial discovery of invariant sites (source, logs, traces, metrics, task names, error text, comments, config, docs, tests) one per round is the churn `### Blast radius` and `### Policy decisions this change implies` exist to absorb. | rationale-only | analyst.md:L17 — ## Why this role exists | - | - | - | none | - | rationale |
| R-017 | Each dispatch is a single-shot analysis; there is no warm-state, no resume, and no per-run reuse. | invariant | analyst.md:L21 — ## Cold-start invariant | - | - | - | none | mirrors code-reviewer.md L13 (cold-start) | procedure |
| R-018 | A second dispatch on the same Issue (user picks `Revise`, or Engineer returned a design question) is a fresh cold invocation carrying the feedback in the new prompt. | invariant | analyst.md:L21 — ## Cold-start invariant | - | user revision feedback / Engineer's question in dispatch prompt | - | none | - | procedure |
| R-019 | Read the Issue body verbatim from the dispatch prompt; re-read via `bees show-ticket --ids <issue-id>` if the quoted block is not byte-for-byte identical with the canonical body. | command | analyst.md:L25,L34 — ## Responsibilities / ## Instructions | - | dispatch-prompt Issue body; canonical body | - | Bash, bees | mirrors pm.md L74 (re-read rule) | procedure |
| R-020 | When `reference_materials` is non-empty with an external URL (`github-issue` / `linear-issue` / `url` resolvers), fetch the `value` URL via `WebFetch`. | command | analyst.md:L26,L35 — ## Responsibilities / ## Instructions | - | `reference_materials` entry `resolver` / `value` | `/quo-file-issue --reference` / `--from-github` | WebFetch | mirrors engineer.md L19 | procedure |
| R-021 | The `WebFetch` fallback is the canonical fetch path until a real bees resolver lands. | definition | analyst.md:L26 — ## Responsibilities | - | - | - | WebFetch, bees | mirrors engineer.md L19, pm.md L63 | procedure |
| R-022 | If `WebFetch` cannot reach the URL, surface the failure in the Design Proposal trailer rather than guessing. | command | analyst.md:L26 — ## Responsibilities | `### Upstream-fetch status` | - | - | WebFetch | mirrors engineer.md L19 | procedure |
| R-023 | Solutions the body proposes are inputs to understanding the problem, not a menu the Analyst picks from. | invariant | analyst.md:L27 — ## Responsibilities | - | Issue body | - | none | - | procedure |
| R-024 | Read the local codebase: implicated source files, SDD / PRD sections named in CLAUDE.md `## Documentation Locations`, and related skills' contracts. | command | analyst.md:L28 — ## Responsibilities | - | `## Documentation Locations` | - | Read, Grep, Glob | - | procedure |
| R-025 | When the Issue touches workflow behavior, read `skills/<name>/SKILL.md` and `agents/<name>.md` directly — they are the program source in skill repos. | command | analyst.md:L28,L38 — ## Responsibilities / ## Instructions | - | skill and role files | - | Read | - | procedure |
| R-026 | Read adjacent Issue ticket notes via `bees show-ticket --ids <related-id>` when the body references them by ID. | command | analyst.md:L28,L39 — ## Responsibilities / ## Instructions | - | related Issue bodies | - | Bash, bees | - | procedure |
| R-027 | Surface the proposal to the orchestrator, NOT directly to the user. | invariant | analyst.md:L30,L121 — ## Responsibilities / ## Lane discipline | Design Proposal | - | - | none | - | procedure |
| R-028 | Re-read via `bees show-ticket --ids <issue-id>` when the canonical body or `reference_materials` JSON is needed. | command | analyst.md:L34 — ## Instructions | - | `reference_materials` JSON | - | Bash, bees | - | procedure |
| R-029 | Review the engineering best practices guide and the SDD path from CLAUDE.md `## Documentation Locations`. | command | analyst.md:L36 — ## Instructions | - | `Engineering best practices`, `Internal architecture docs (SDD)` | - | Read | - | procedure |
| R-030 | The design recommendation must be consistent with the SDD's invariants or explicitly call out where it diverges and why. | invariant | analyst.md:L36 — ## Instructions | `### Recommended approach`, `### Why` | SDD | - | none | - | procedure |
| R-031 | Read the implicated files plus adjacent callers/callees; Grep for usage sites of every function / type / flag / config-key the Issue names. | command | analyst.md:L37 — ## Instructions | - | source tree | - | Read, Grep | - | procedure |
| R-032 | Run the blast-radius sweep before writing the proposal. | ordering | analyst.md:L40 — ## Instructions | `### Blast radius` | - | - | Grep, Bash | - | procedure |
| R-033 | For every invariant added, removed, or weakened, and every mechanism introduced, search with `Grep` (or a `Bash` search) across source logic, log/trace/metric emission, error text, comments/docstrings, config/deployment manifests, customer/internal docs, and tests. | command | analyst.md:L40 — ## Instructions | `### Blast radius` | - | - | Grep, Bash | - | procedure |
| R-034 | Record every search pattern you ran verbatim in the proposal so the reader can re-run it. | command | analyst.md:L40,L60 — ## Instructions / ### Blast radius | `### Blast radius` | - | - | none | - | procedure |
| R-035 | Extend the sweep to first-party dependencies the same team owns; third-party dependencies are out of scope. | command | analyst.md:L40,L64 — ## Instructions / ### Blast radius Scope | `### Blast radius` | - | - | Grep | - | procedure |
| R-036 | Frame what is broken, the cleanest fix, why, and which alternatives were rejected — naming each body-proposed solution as kept, refined, or set aside. | command | analyst.md:L41 — ## Instructions | `### Problem`, `### Why`, `### Alternatives considered` | Issue body | - | none | - | procedure |
| R-037 | The recommendation may align with, refine, or diverge entirely from the body's framing; all three are legitimate. | definition | analyst.md:L41 — ## Instructions | - | - | - | none | - | procedure |
| R-038 | Return the analysis as a single markdown response; the orchestrator surfaces the body as prose and consumes the trailer for routing. | contract-heading | analyst.md:L45 — ## Structured-output contract | Design Proposal | - | - | none | - | procedure |
| R-039 | Emit top heading `## Design Proposal for <issue-id>`. | contract-heading | analyst.md:L48 — ## Structured-output contract | `## Design Proposal for <issue-id>` | - | - | none | - | procedure |
| R-040 | Emit `### Problem`: 1-3 paragraphs on what is broken in this codebase, citing code paths / files / functions. | contract-heading | analyst.md:L50-L51 — ## Structured-output contract | `### Problem` | - | - | none | - | procedure |
| R-041 | Emit `### Root cause`: 1-2 paragraphs distinguishing symptoms from the underlying cause. | contract-heading | analyst.md:L53-L54 — ## Structured-output contract | `### Root cause` | - | - | none | - | procedure |
| R-042 | Emit `### Recommended approach` concrete enough to be the Engineer's authoritative design directive — files / sections / functions, shape of change, contract surfaces to preserve. | contract-heading | analyst.md:L56-L57 — ## Structured-output contract | `### Recommended approach` | - | engineer.md (`## Authoritative design directive`) | none | - | procedure |
| R-043 | Emit `### Blast radius` unconditionally — always emit this heading. | contract-heading | analyst.md:L59-L60 — ### Blast radius | `### Blast radius` | - | - | none | - | procedure |
| R-044 | Under `### Blast radius`, state each added / removed / weakened invariant on one line, then enumerate sites grouped by kind: source logic / logs / traces / metrics / error and exception text / comments and docstrings / configuration and deployment manifests / customer docs / internal docs / tests. | command | analyst.md:L60 — ### Blast radius | `### Blast radius` | - | - | none | - | procedure |
| R-045 | Each blast-radius entry carries a `file:line` reference plus the verbatim search pattern that found it. | command | analyst.md:L60 — ### Blast radius | `### Blast radius` | - | - | none | - | procedure |
| R-046 | `None found` is acceptable inside a kind-group only when it names the pattern that was tried. | invariant | analyst.md:L60 — ### Blast radius | `### Blast radius` | - | - | none | - | procedure |
| R-047 | When the approach adds, removes, weakens no invariant AND introduces no mechanism, emit the fixed line `No invariant added, removed, or weakened.` under the heading rather than omitting it. | command | analyst.md:L60 — ### Blast radius | `### Blast radius` | - | - | none | - | procedure |
| R-048 | A section that appears only when there was something to say degrades into one nobody can rely on being asked for. | rationale-only | analyst.md:L60 — ### Blast radius | - | - | - | none | - | rationale |
| R-049 | Choose each pattern to match where the invariant is observable or assumed (read, written, emitted, persisted, documented), not the identifier names of the added/removed machinery. | command | analyst.md:L62 — ### Blast radius Choosing patterns | `### Blast radius` | - | - | Grep | - | procedure |
| R-050 | A pattern matching only the added/removed identifier finds sites that already agree and misses sites assuming old behavior under other names. | rationale-only | analyst.md:L62 — ### Blast radius Choosing patterns | - | - | - | none | - | rationale |
| R-051 | Whenever the invariant has an observable effect, name a pattern for the effect (log field, attribute key, error-message fragment, config key) alongside any identifier pattern. | command | analyst.md:L62 — ### Blast radius Choosing patterns | `### Blast radius` | - | - | Grep | - | procedure |
| R-052 | When renaming or removing an identifier, run both the old spelling and the new spelling and record the hits from each. | command | analyst.md:L62,L112 — ### Blast radius Choosing patterns / ## Short-circuit conditions | `### Blast radius` | - | - | Grep | - | procedure |
| R-053 | Record both old and new spelling patterns as evidence even when one returns nothing. | command | analyst.md:L62 — ### Blast radius Choosing patterns | `### Blast radius` | - | - | none | - | procedure |
| R-054 | The sweep covers the whole change set, not only the primary package. | command | analyst.md:L64 — ### Blast radius Scope | `### Blast radius` | - | - | Grep | - | procedure |
| R-055 | First-party dependencies in scope are: another workspace member, a sibling package checked out alongside, a vendored internal library. | definition | analyst.md:L64 — ### Blast radius Scope | - | - | - | none | - | procedure |
| R-056 | A **mechanism** is any new state, configuration surface (flag, env var, setting), persisted or wire field, background task, exception or error type, metric / log / trace attribute, name or identifier class, gate, or retry / fallback path. | definition | analyst.md:L66 — ### Blast radius Lifecycle axis | - | - | engineer.md L55, code-reviewer.md L27, `/quo-engineer-review` | none | - | procedure |
| R-057 | The lifecycle-axis paragraph is the single canonical home of the mechanism definition; other role files and skills reference it, never restate it as a second normative definition. | invariant | analyst.md:L66 — ### Blast radius Lifecycle axis | - | - | engineer.md, code-reviewer.md | none | - | procedure |
| R-058 | A non-normative recognition gloss elsewhere is permitted; the canonical list governs wherever the two differ. | definition | analyst.md:L66 — ### Blast radius Lifecycle axis | - | - | engineer.md L55 | none | - | procedure |
| R-059 | For every mechanism introduced, name each lifecycle leg: created/initialized, read/consumed, mutated/invalidated/migrated, torn down/removed, observable (logs, spans, metrics, error text), documented. | command | analyst.md:L66 — ### Blast radius Lifecycle axis | `### Blast radius` | - | - | Grep | - | procedure |
| R-060 | Cover lifecycle legs in every scope the change touches and on every exit path: normal completion, cancel, abort, interruption. | command | analyst.md:L66 — ### Blast radius Lifecycle axis | `### Blast radius` | - | - | none | - | procedure |
| R-061 | Give `file:line` for lifecycle legs that already exist and an explicit `new` for legs the change adds. | command | analyst.md:L66 — ### Blast radius Lifecycle axis | `### Blast radius` | - | - | none | - | procedure |
| R-062 | A lifecycle leg missing in any scope is a design gap; report it under `### Blast radius` at the gate. | command | analyst.md:L66 — ### Blast radius Lifecycle axis | `### Blast radius` | - | - | none | - | procedure |
| R-063 | When the sweep surfaces a concern distinct from the Issue's stated defect, state in one line whether to (a) fold it in or (b) file it as its own Issue and only document here, with reason. | command | analyst.md:L68 — ### Blast radius Scope-split recommendation | `### Blast radius` | - | - | none | - | procedure |
| R-064 | Mirror the scope-split recommendation as a `### Deferred refinements` bullet with `addressed-now-in-this-Issue` (fold-in) or `defer-to-new-Issue` (split). | command | analyst.md:L68 — ### Blast radius Scope-split recommendation | `### Deferred refinements` | - | - | none | - | procedure |
| R-065 | A scope-split recommendation is a refinement, so on a converged verdict the verdict is at least `recommend-with-refinements`. | invariant | analyst.md:L68,L104,L112 — Scope-split recommendation / verdict list / Short-circuit | `Analyst verdict:` | - | - | none | - | procedure |
| R-066 | On `escalate-to-user`, do not issue a scope-split recommendation as such; `### Deferred refinements` stays `None`. | invariant | analyst.md:L68 — ### Blast radius Scope-split recommendation | `### Deferred refinements` | - | - | none | - | procedure |
| R-067 | On `escalate-to-user`, fold the distinct concern into the escalated open questions in `### Why`. | command | analyst.md:L68 — ### Blast radius Scope-split recommendation | `### Why` | - | - | none | - | procedure |
| R-068 | Emit `### Policy decisions this change implies` unconditionally — always emit this heading. | contract-heading | analyst.md:L70-L71 — ### Policy decisions this change implies | `### Policy decisions this change implies` | - | - | none | - | procedure |
| R-069 | One entry per yes/no policy question the approach forces but the body does not settle; give question, consequence of yes, consequence of no, and the Analyst's recommended answer. | command | analyst.md:L71 — ### Policy decisions this change implies | `### Policy decisions this change implies` | - | - | none | - | procedure |
| R-070 | The orchestrator surfaces policy decisions at the approval gate so the user ratifies design and policy answers in one decision. | definition | analyst.md:L71 — ### Policy decisions this change implies | - | - | `/quo-fix-issue` | none | - | procedure |
| R-071 | When no policy question is open, emit the fixed line `None — the recommendation leaves no policy question open.` under the heading rather than omitting it. | command | analyst.md:L71,L112 — ### Policy decisions / ## Short-circuit conditions | `### Policy decisions this change implies` | - | - | none | - | procedure |
| R-072 | For any decision asserting what possessing or observing a value grants, do not derive the answer from the one mechanism the change removes. | invariant | analyst.md:L73 — ### Policy decisions Grounding rule | - | - | - | none | - | procedure |
| R-073 | Enumerate with `file:line` every check still applied on the value's use path (authentication, coarse authorization, per-principal filtering, per-principal caches, input validation) and phrase the conclusion in terms of them. | command | analyst.md:L73 — ### Policy decisions Grounding rule | `### Policy decisions this change implies` | - | - | Grep | - | procedure |
| R-074 | Never phrase a grounding conclusion as an unqualified label such as "secret" or "capability". | invariant | analyst.md:L73 — ### Policy decisions Grounding rule | - | - | - | none | - | procedure |
| R-075 | The Recommended approach must use the same grounded phrasing; reviewers check the claim against the enumerated checks. | invariant | analyst.md:L73 — ### Policy decisions Grounding rule | `### Recommended approach` | - | - | none | - | procedure |
| R-076 | Emit `### Why`: 1-3 paragraphs on why this is the cleanest fix, comparing against alternatives. | contract-heading | analyst.md:L75-L76 — ## Structured-output contract | `### Why` | - | - | none | - | procedure |
| R-077 | Emit `### Alternatives considered`: a bulleted list naming each alternative, one-sentence summary, kept / refined / set aside with a one-or-two-sentence reason. | contract-heading | analyst.md:L78-L79 — ## Structured-output contract | `### Alternatives considered` | - | - | none | - | procedure |
| R-078 | When the body's framing implied a menu, every option from that menu MUST appear under `### Alternatives considered`. | invariant | analyst.md:L79 — ## Structured-output contract | `### Alternatives considered` | Issue body | - | none | - | procedure |
| R-079 | Emit `### Options the body did not consider`: bulleted approaches surfaced by codebase research that were NOT in the body's framing. | contract-heading | analyst.md:L81-L82 — ## Structured-output contract | `### Options the body did not consider` | - | - | none | - | procedure |
| R-080 | At minimum name one option the body did not consider, evaluate it against the Recommended approach, and say whether it was rejected, refined in, or promoted. | command | analyst.md:L82 — ## Structured-output contract | `### Options the body did not consider` | - | - | none | - | procedure |
| R-081 | If research surfaced nothing outside the body's framing, state exactly "Codebase research surfaced no options outside the body's framing." followed by a one-sentence reason. | command | analyst.md:L82 — ## Structured-output contract | `### Options the body did not consider` | - | - | none | - | procedure |
| R-082 | Empty content or "N/A" under `### Options the body did not consider` is not acceptable. | invariant | analyst.md:L82 — ## Structured-output contract | `### Options the body did not consider` | - | - | none | - | procedure |
| R-083 | Emit `### Upstream-fetch status` only when `reference_materials` is non-empty; state whether `WebFetch` succeeded and what was read, or the failure mode. | contract-heading | analyst.md:L84-L85 — ## Structured-output contract | `### Upstream-fetch status` | `reference_materials` | - | WebFetch | - | procedure |
| R-084 | Emit trailer line `Analyst verdict: <…>` with exactly one of `recommend-as-stated` / `recommend-with-refinements` / `recommend-different-approach` / `escalate-to-user`. | contract-heading | analyst.md:L87,L101 — ## Structured-output contract | `Analyst verdict:` | - | - | none | - | procedure |
| R-085 | Emit `### Deferred refinements` after the verdict line. | contract-heading | analyst.md:L89 — ## Structured-output contract | `### Deferred refinements` | - | - | none | - | procedure |
| R-086 | On `recommend-as-stated` and `escalate-to-user`, emit `### Deferred refinements` followed by `None` so the orchestrator can parse deterministically. | command | analyst.md:L92 — ### Deferred refinements | `### Deferred refinements` | - | - | none | - | procedure |
| R-087 | On `recommend-with-refinements` and `recommend-different-approach`, emit one bullet per refinement the orchestrator should handle outside the immediate implementation pass. | command | analyst.md:L93 — ### Deferred refinements | `### Deferred refinements` | - | - | none | - | procedure |
| R-088 | Every `### Deferred refinements` bullet MUST carry a destination annotation so the orchestrator can encode it in the `defer-<short-suffix>` TaskList ledger. | invariant | analyst.md:L93 — ### Deferred refinements | `### Deferred refinements` | - | phase skill's pre-handoff deferral-hygiene gate | none | mirrors pm.md L241 | procedure |
| R-089 | Destination `addressed-now-in-this-Issue`: the refinement is rolled into the Recommended approach; no inter-session carrier needed. | definition | analyst.md:L94 — ### Deferred refinements | - | - | - | none | mirrors pm.md L242 (`addressed-now-in-this-Task`) | procedure |
| R-090 | Destination `defer-to-existing-ticket-body: <ticket-id>`: name the specific ticket id; for PRD/SDD name the CLAUDE.md `## Documentation Locations` contract key. | definition | analyst.md:L95 — ### Deferred refinements | - | `Project requirements doc (PRD)` | - | none | mirrors pm.md L243 | procedure |
| R-091 | Destination `defer-to-new-Issue`: a cross-cutting follow-up the orchestrator files via `/quo-file-issue` at the pre-handoff deferral-hygiene gate. | definition | analyst.md:L96 — ### Deferred refinements | - | - | `/quo-file-issue` | none | mirrors pm.md L244 | procedure |
| R-092 | Vague "defer to implementation" / "address later" / "pick up during execution" framings without a named destination are forbidden. | invariant | analyst.md:L98 — ### Deferred refinements | - | - | - | none | mirrors pm.md L246 | procedure |
| R-093 | The orchestrator turns each destination annotation into a `defer-<short-suffix>` TaskList task at Approve time; bullets without a destination are surfaced to the user as a contract violation. | definition | analyst.md:L98 — ### Deferred refinements | - | - | - | none | - | procedure |
| R-094 | The `Analyst verdict:` trailer is a load-bearing framing signal the orchestrator reads to shape the prose preamble introducing the proposal. | definition | analyst.md:L101 — ## Structured-output contract | `Analyst verdict:` | - | - | none | - | procedure |
| R-095 | When the verdict is `recommend-different-approach` or `escalate-to-user`, the orchestrator surfaces the divergence from the body prominently. | definition | analyst.md:L101 — ## Structured-output contract | - | - | - | none | - | procedure |
| R-096 | Routing through the `AskUserQuestion` gate (Approve / Revise / Cancel) is driven by the user's choice, not by the verdict value. | definition | analyst.md:L101 — ## Structured-output contract | - | - | `AskUserQuestion` | none | - | procedure |
| R-097 | `recommend-as-stated`: research agreed with the body; the Recommended approach reflects the body's approach without material change. | definition | analyst.md:L103 — ## Structured-output contract | - | - | - | none | - | procedure |
| R-098 | `recommend-with-refinements`: research broadly agreed but the approach refines it, or the sweep surfaced a distinct concern to split out; refinements listed in Why / Alternatives, scope-split additionally in `### Blast radius` and `### Deferred refinements`. | definition | analyst.md:L104 — ## Structured-output contract | - | - | - | none | - | procedure |
| R-099 | `recommend-different-approach`: research diverged; the approach is an option the body did not propose; Why explains the divergence. | definition | analyst.md:L105 — ## Structured-output contract | - | - | - | none | - | procedure |
| R-100 | `escalate-to-user`: the Analyst could not converge; state open questions in Why and frame alternatives as the choices the user must pick between. | definition | analyst.md:L106,L114 — ## Structured-output contract / ## Short-circuit conditions | `### Why`, `### Alternatives considered` | - | - | none | - | procedure |
| R-101 | The Analyst always returns the Design Proposal shape; there is no exit short-circuit — edge cases land in the verdict. | invariant | analyst.md:L110 — ## Short-circuit conditions | Design Proposal | - | - | none | - | procedure |
| R-102 | Trivial mechanical fix (rename, one-line typo, exact config tweak): return `recommend-as-stated` with a short Recommended approach. | command | analyst.md:L112 — ## Short-circuit conditions | `Analyst verdict:` | - | - | none | - | procedure |
| R-103 | If the sweep surfaces a distinct concern on a trivial fix, the fix was not trivial; apply the scope-split rule. | invariant | analyst.md:L112 — ## Short-circuit conditions | - | - | - | none | - | procedure |
| R-104 | `### Blast radius` and `### Policy decisions this change implies` still emit on a trivial fix; skipping them is not what the trivial case promises. | invariant | analyst.md:L112 — ## Short-circuit conditions | `### Blast radius`, `### Policy decisions this change implies` | - | - | none | - | procedure |
| R-105 | On a trivial fix the sections converge cheaply: typically one invariant from one or two patterns and the fixed `None — …` line. | example | analyst.md:L112 — ## Short-circuit conditions | - | - | - | none | - | example |
| R-106 | "Swept and found nothing" and "never swept" must stay distinguishable at the gate. | rationale-only | analyst.md:L112 — ## Short-circuit conditions | - | - | - | none | - | rationale |
| R-107 | `reference_materials` URL unreachable: surface in `### Upstream-fetch status`; converge if the body suffices, else return `escalate-to-user`. | command | analyst.md:L113 — ## Short-circuit conditions | `### Upstream-fetch status`, `Analyst verdict:` | - | - | WebFetch | - | procedure |
| R-108 | Analyst does NOT flip bees ticket statuses; the Issue type supports only `open` and `done` and the orchestrator owns `open` → `done`. | invariant | analyst.md:L119 — ## Lane discipline | - | - | - | none | mirrors engineer.md L29, test-writer.md L27 | procedure |
| R-109 | Analyst does NOT invoke other skills; downstream review is owned by the per-issue PM Agent and the reviewer Agents. | invariant | analyst.md:L120 — ## Lane discipline | - | - | pm.md, code-reviewer.md, test-reviewer.md, doc-reviewer.md | none | - | procedure |
| R-110 | On the `Revise` branch the orchestrator re-dispatches the Analyst with the user's feedback as additional context. | definition | analyst.md:L121 — ## Lane discipline | - | user feedback | `AskUserQuestion` | none | - | procedure |
| R-111 | Use one literal command per Bash invocation. | command | analyst.md:L125 — ## Shell-command etiquette | - | - | - | Bash | mirrors engineer.md L68, test-writer.md L132, pm.md L248 | procedure |
| R-112 | Don't append diagnostic tails like `; echo exit=$?` or `&& echo done`. | invariant | analyst.md:L125 — ## Shell-command etiquette | - | - | - | Bash | mirrors engineer.md L68, test-writer.md L132, pm.md L248 | procedure |
| R-113 | Avoid embedded newlines, `$VAR` / `$?` / `$(...)`, backticks, mid-chain redirects, and compound commands (`&&`, `\|\|`, `;`, pipes) when a simple one works. | invariant | analyst.md:L125 — ## Shell-command etiquette | - | - | - | Bash | mirrors engineer.md L68, test-writer.md L132, pm.md L248 | procedure |
| R-114 | For a multi-step script, have it written to a file via the orchestrator (no `Write` tool); in practice only single `bees show-ticket` / `git log` / `git show` calls are needed. | command | analyst.md:L125 — ## Shell-command etiquette | - | - | - | Bash, bees, git | - | procedure |
| R-115 | Before reaching for shell, prefer `Read`, `Grep`, `Glob` over shell control flow; reach for shell only when no tool fits. | command | analyst.md:L125 — ## Shell-command etiquette | - | - | - | Read, Grep, Glob | mirrors engineer.md L68, test-writer.md L132, pm.md L248 | procedure |
| R-116 | Frontmatter `name` is `engineer`, `model: opus`, `effort: high`. | definition | engineer.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-117 | Tool allowlist is exactly `[Bash, Edit, Read, Write, Grep, Glob, Skill, WebFetch]`. | tool-allowlist | engineer.md:L6 — frontmatter | - | - | - | Bash, Edit, Read, Write, Grep, Glob, Skill, WebFetch | - | procedure |
| R-118 | Engineer implements code changes for a Subtask (execute mode) or a single Issue body (fix mode); runs Compile/type-check, Lint, Narrow test from `## Build Commands`. | definition | engineer.md:L3 — frontmatter description | source edits | ticket body, `## Build Commands` | `/quo-file-issue --reference` | Bash, Edit, bees | - | procedure |
| R-119 | Engineer does NOT update tests or docs — those are owned by test-writer and doc-writer. | invariant | engineer.md:L3,L9 — description / intro | - | - | test-writer.md, doc-writer.md | none | - | procedure |
| R-120 | Engineer is dispatched by `/quo-execute` or `/quo-fix-issue`; the work is source-code-only. | definition | engineer.md:L9 — intro | - | - | `/quo-execute`, `/quo-fix-issue` | none | - | procedure |
| R-121 | Execute implementation Subtasks for a Task (execute mode) or implement the fix for an Issue (fix mode). | command | engineer.md:L13 — ## Responsibilities | - | Subtask / Issue | - | Edit, Write | - | procedure |
| R-122 | Tasks that only involve research (no code or doc changes) may omit all implementation subtasks. | definition | engineer.md:L14 — ## Responsibilities | - | - | - | none | mirrors test-writer.md L16, doc-writer.md L23 | procedure |
| R-123 | Read the assigned ticket via the bees CLI; an execute-mode Subtask carries Context, What Needs to Change, Key Files, and Acceptance Criteria; fix mode reads the Issue body. | command | engineer.md:L18 — ## Instructions | - | Subtask body sections; Issue body | - | Bash, bees | - | procedure |
| R-124 | External-reference Issues (fix mode only): when `reference_materials` points at an external URL, the body is intentionally thin; fetch upstream via `WebFetch` and treat it as the spec source. | command | engineer.md:L19 — ## Instructions | - | `reference_materials` (`github-issue` / `linear-issue` / `url`) | `/quo-file-issue --reference` | WebFetch | mirrors analyst.md L26 | procedure |
| R-125 | If `WebFetch` cannot reach the URL, surface the failure to the orchestrator rather than guessing; the embedded body alone is not enough. | command | engineer.md:L19 — ## Instructions | failure report | - | - | WebFetch | mirrors analyst.md L26, pm.md L63 | procedure |
| R-126 | Fix-mode dispatch prompt MAY carry `## Authoritative design directive` (Analyst's approved Recommended approach, Blast radius under `## Blast radius`, Policy decisions, Why / Alternatives / Options). | definition | engineer.md:L20 — ## Instructions | - | `## Authoritative design directive`, `## Blast radius` | `/quo-fix-issue` Section 3 Design Analysis gate, analyst.md | none | - | procedure |
| R-127 | When present, the directive supersedes any conflicting framing in the Issue body or upstream `WebFetch` content for implementation. | invariant | engineer.md:L20 — ## Instructions | - | `## Authoritative design directive` | - | none | - | procedure |
| R-128 | Body and upstream remain problem-report context: identifier spellings, contract surfaces, repro details, and error messages must still travel byte-for-byte. | command | engineer.md:L20 — ## Instructions | - | Issue body | - | none | - | procedure |
| R-129 | Where the body proposes "do X" and the directive says "do Y", implement Y, not X. | command | engineer.md:L20 — ## Instructions | - | - | - | none | - | procedure |
| R-130 | The directive is the Analyst's codebase-grounded, user-approved recommendation; following it over an unresearched proposed-fix is the workflow's design intent. | rationale-only | engineer.md:L20 — ## Instructions | - | - | analyst.md | none | - | rationale |
| R-131 | When no directive is present (`/quo-execute` dispatches, or pre-gate `/quo-fix-issue`), fall back to the body / upstream content as the spec source. | command | engineer.md:L20 — ## Instructions | - | Issue body / upstream | `/quo-execute` | none | - | procedure |
| R-132 | Review internal architecture docs referenced in CLAUDE.md `## Documentation Locations`. | command | engineer.md:L21 — ## Instructions | - | `Internal architecture docs (SDD)` | - | Read | - | procedure |
| R-133 | Review the existing code to determine the current state. | command | engineer.md:L22 — ## Instructions | - | source tree | - | Read, Grep | - | procedure |
| R-134 | Review the engineering best practices guide referenced in CLAUDE.md `## Documentation Locations`. | command | engineer.md:L23 — ## Instructions | - | `Engineering best practices` | - | Read | - | procedure |
| R-135 | Execute each implementation Subtask per its description; in fix mode there is no breakdown — implement the fix in a single pass. | command | engineer.md:L24 — ## Instructions | - | Subtask bodies | - | Edit, Write | - | procedure |
| R-136 | Modify any source code required to satisfy the ticket's Acceptance Criteria. | command | engineer.md:L25 — ## Instructions | source edits | Acceptance Criteria | - | Edit, Write | - | procedure |
| R-137 | Mark ticket status as work proceeds; the transition is the load-bearing handoff signal test-writer, doc-writer, and PM are gated on — do not skip it. | invariant | engineer.md:L26 — ## Instructions | status flip | - | test-writer.md, doc-writer.md, pm.md | Bash, bees | mirrors test-writer.md L24 | procedure |
| R-138 | Execute mode: mark `status=in_progress` when starting the Subtask and `status=done` when finishing; Subtasks support `drafted` → `ready` → `in_progress` → `done`. | command | engineer.md:L28 — ## Instructions | `in_progress`, `done` status flips | Subtask `t3` ticket | - | Bash, bees | mirrors test-writer.md L26 | procedure |
| R-139 | A Subtask stopped partway on a design question stays at `in_progress`. | invariant | engineer.md:L28,L55 — ## Instructions | `in_progress` | - | - | none | - | procedure |
| R-140 | Fix mode: do not attempt `in_progress` (bees rejects with `Invalid status 'in_progress'`) and do not flip to `done`. | invariant | engineer.md:L29 — ## Instructions | - | Issue ticket | - | bees | mirrors test-writer.md L27 | procedure |
| R-141 | Fix mode: the orchestrator owns the `open` → `done` flip at close-out (Section 7 of `quo-fix-issue/SKILL.md`); leave the Issue at `open` and exit. | invariant | engineer.md:L29 — ## Instructions | - | - | `quo-fix-issue/SKILL.md` Section 7 | none | mirrors test-writer.md L27 | procedure |
| R-142 | Run `bees update-ticket --ids <subtask-id> --status in_progress` (POSIX and PowerShell identical). | command | engineer.md:L33-L41 — ## Instructions | `in_progress` | - | - | Bash, bees | mirrors test-writer.md L31-L39 | procedure |
| R-143 | Run `bees update-ticket --ids <subtask-id> --status done` on completion (POSIX and PowerShell identical). | command | engineer.md:L45-L53 — ## Instructions | `done` | - | - | Bash, bees | mirrors test-writer.md L43-L51 | procedure |
| R-144 | Return design questions; do not invent mechanisms — this rule is mode-independent, binding identically in fix and execute mode. | invariant | engineer.md:L55 — ## Instructions | `## Design question` | - | - | none | - | procedure |
| R-145 | When a directive, Subtask body, or review finding cannot be implemented without a mechanism the approved design did not enumerate, do not choose one. | invariant | engineer.md:L55 — ## Instructions | - | `## Authoritative design directive`, Subtask body, review finding | - | none | - | procedure |
| R-146 | "Mechanism" is the term as defined in the `analyst` role file's lifecycle-axis paragraph of `### Blast radius`; it is deliberately not restated here. | definition | engineer.md:L55 — ## Instructions | - | - | analyst.md `### Blast radius` | none | - | procedure |
| R-147 | Non-normative recognition gloss: new state, a gate, or a new name or identifier class; the canonical list governs wherever the two differ. | definition | engineer.md:L55 — ## Instructions | - | - | analyst.md | none | - | procedure |
| R-148 | On hitting an unenumerated mechanism: implement the rest, stop at the undecided part, and return the question under a heading that is exactly `## Design question`. | contract-heading | engineer.md:L55 — ## Instructions | `## Design question` | - | `/quo-fix-issue` dispatcher | none | - | procedure |
| R-149 | The `## Design question` heading MUST be that literal string; `/quo-fix-issue` routes on it and any other heading is persisted as a completion. | invariant | engineer.md:L55 — ## Instructions | `## Design question` | - | `/quo-fix-issue` | none | - | procedure |
| R-150 | Under `## Design question`, name the mechanism you would have had to add, why the assignment cannot complete without it, and the alternatives you see. | command | engineer.md:L55 — ## Instructions | `## Design question` | - | - | none | - | procedure |
| R-151 | Say plainly which parts of the assignment landed and which were left undone, so the dispatcher knows the diff is partial. | command | engineer.md:L55 — ## Instructions | partial-return statement | - | - | none | - | procedure |
| R-152 | Execute mode: do not run the `status=done` transition on a design-question return; leave the Subtask `in_progress` and state it is partially implemented. | invariant | engineer.md:L55 — ## Instructions | `in_progress` | - | - | bees | - | procedure |
| R-153 | A Subtask flipped `done` over a partial diff reads as finished; left `in_progress` makes the partial return detectable when the dispatcher confirms the transition. | rationale-only | engineer.md:L55 — ## Instructions | - | - | - | none | - | rationale |
| R-154 | Fix mode needs no partial-flip clause — the Engineer never sets the Issue's status there. | definition | engineer.md:L55 — ## Instructions | - | - | - | none | - | procedure |
| R-155 | Choosing a mechanism yourself lands an undecided mechanism in the diff whose first reviewer is a round later — the failure mode this bullet prevents. | rationale-only | engineer.md:L55 — ## Instructions | - | - | - | none | - | failure-narrative |
| R-156 | Every return MUST carry an explicit list of every file created, modified, or deleted — one repository-relative path per line under a labelled heading; recommended label `## Files changed`. | contract-heading | engineer.md:L57 — ## Instructions | `## Files changed` | - | - | none | - | procedure |
| R-157 | The orchestrator carries `## Files changed` forward into the Test Writer's dispatch prompt as the `## Source paths to fingerprint` set. | definition | engineer.md:L57 — ## Instructions | `## Files changed` | - | test-writer.md (`## Source paths to fingerprint`) | none | - | procedure |
| R-158 | Report the changed-file list on every dispatch, sweep-shaped or not; state it explicitly when empty rather than omitting the heading. | invariant | engineer.md:L57 — ## Instructions | `## Files changed` | - | - | none | - | procedure |
| R-159 | An omitted list forces the orchestrator onto a working-tree-wide fallback that in execute mode can sweep in a sibling Subtask's concurrent edits. | rationale-only | engineer.md:L57 — ## Instructions | - | - | - | none | - | failure-narrative |
| R-160 | When the assignment directs a change at every site where some property holds, the return MUST include a completeness check with evidence, not a claim. | invariant | engineer.md:L59 — ## Instructions | completeness evidence | - | - | none | - | procedure |
| R-161 | Completeness evidence includes (i) search patterns run verbatim, (ii) every hit, and (iii) per hit the change made or the specific reason it is untouched. | command | engineer.md:L59 — ## Instructions | completeness evidence | - | pm.md L222, code-reviewer.md L24 | Grep | - | procedure |
| R-162 | A sweep declared complete without evidence costs a round per site instead of catching an incomplete sweep once as a list gap. | rationale-only | engineer.md:L59 — ## Instructions | - | - | - | none | - | failure-narrative |
| R-163 | The sweep directive source is the Subtask body in execute mode; in fix mode the Issue body or, when present, `## Authoritative design directive` (which wins on design). | definition | engineer.md:L61 — ## Instructions | - | Subtask body, Issue body, `## Authoritative design directive` | - | none | - | procedure |
| R-164 | Read the sweep's scope from the applicable source before choosing patterns; a narrower pattern produces a list that looks complete and is not. | ordering | engineer.md:L61 — ## Instructions | - | - | - | none | - | procedure |
| R-165 | When the dispatch prompt carries an enumerated site list (most often under `## Blast radius`), reconcile your own list against it. | command | engineer.md:L63 — ## Instructions | completeness evidence | `## Blast radius` | analyst.md | none | - | procedure |
| R-166 | A supplied enumerated site list triggers the reconcile-and-disposition obligation on its own, whether or not the assignment was sweep-shaped. | invariant | engineer.md:L63 — ## Instructions | completeness evidence | `## Blast radius` | - | none | - | procedure |
| R-167 | An upstream list that explicitly states it enumerates no sites carries nothing to reconcile; no completeness evidence is owed for it. | definition | engineer.md:L63 — ## Instructions | - | `## Blast radius` (`No invariant added, removed, or weakened.`) | analyst.md | none | - | procedure |
| R-168 | Reconciling means accounting for every entry: changed, deliberately untouched (with reason), or not a real hit (with why). | command | engineer.md:L63 — ## Instructions | completeness evidence | `## Blast radius` | - | none | - | procedure |
| R-169 | Do not silently drop an upstream entry, and do not treat the supplied list as a ceiling — sites your patterns found are still yours to report. | invariant | engineer.md:L63 — ## Instructions | completeness evidence | - | - | none | - | procedure |
| R-170 | Entries in the upstream **tests**, **customer docs**, and **internal docs** kind-groups are dispositioned as owned by the Test Writer / Doc Writer lanes, not implemented. | command | engineer.md:L63 — ## Instructions | completeness evidence | `## Blast radius` kind-groups | test-writer.md, doc-writer.md | none | - | procedure |
| R-171 | Resolve the lane by your own source lane, not by the upstream group name — program source filed under a docs/tests kind stays yours. | invariant | engineer.md:L63 — ## Instructions | - | - | - | none | - | procedure |
| R-172 | Dispositioning is not dropping — list lane-dispositioned entries in the completeness evidence with that disposition. | command | engineer.md:L63 — ## Instructions | completeness evidence | - | - | none | - | procedure |
| R-173 | Look up **Compile/type-check** from CLAUDE.md `## Build Commands` and run it after each subtask (fix mode: after each significant change); fix errors before moving on. | command | engineer.md:L65 — ## Instructions | - | `Compile/type-check` | - | Bash | - | procedure |
| R-174 | If `Compile/type-check` is empty, skip that rung; the **Narrow test** rung still applies. | command | engineer.md:L65 — ## Instructions | - | `Compile/type-check`, `Narrow test` | - | Bash | - | procedure |
| R-175 | Run **Lint** at narrow scope after each subtask where supported. | command | engineer.md:L65 — ## Instructions | - | `Lint` | - | Bash | - | procedure |
| R-176 | While iterating, use the **Narrow test** and **Lint** commands from CLAUDE.md `## Build Commands`. | command | engineer.md:L66 — ## Instructions | - | `Narrow test`, `Lint` | - | Bash | mirrors test-writer.md L128 | procedure |
| R-177 | For a Rust crate **Narrow test** typically resolves to a single-package invocation; for Node, a single-file invocation. | example | engineer.md:L66 — ## Instructions | - | - | - | none | - | example |
| R-178 | Do NOT run **Full test** while iterating; the full-suite run happens once at the Task's authoritative `.T` (or equivalent) subtask. | invariant | engineer.md:L66 — ## Instructions | - | `Full test` | - | Bash | mirrors test-writer.md L128 | procedure |
| R-179 | The lookup keys are the exact contract names `Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`; read them from CLAUDE.md, do not hardcode language-specific commands. | invariant | engineer.md:L66 — ## Instructions | - | `## Build Commands` | - | Read | mirrors test-writer.md L128, pm.md L226 | procedure |
| R-180 | Use one literal command per Bash invocation; no diagnostic tails; avoid `$VAR`, `$(...)`, backticks, redirects, compound commands when a simple one works. | invariant | engineer.md:L68 — ## Instructions Shell-command etiquette | - | - | - | Bash | mirrors analyst.md L125, test-writer.md L132, pm.md L248 | procedure |
| R-181 | For a multi-step script, write it to a file via `Write` and run the file rather than passing it inline via `-c` or a heredoc. | command | engineer.md:L68 — ## Instructions Shell-command etiquette | scratch script file | - | - | Write, Bash | mirrors test-writer.md L132, pm.md L248 | procedure |
| R-182 | Prefer `Read`, `Grep`, `Glob`, `Write` / `Edit`, and separate `Bash` calls over shell control flow; reach for shell only when no tool fits. | command | engineer.md:L68 — ## Instructions Shell-command etiquette | - | - | - | Read, Grep, Glob, Write, Edit, Bash | mirrors test-writer.md L132 | procedure |
| R-183 | Frontmatter `name` is `test-writer`, `model: opus`, `effort: high`. | definition | test-writer.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-184 | Tool allowlist is exactly `[Bash, Edit, Read, Write, Grep, Glob]`. | tool-allowlist | test-writer.md:L6 — frontmatter | - | - | - | Bash, Edit, Read, Write, Grep, Glob | - | procedure |
| R-185 | Test Writer authors/updates unit tests for a test Subtask (execute) or ad-hoc tests over the Engineer's diff (fix); runs Narrow test and Lint at narrow scope. | definition | test-writer.md:L3 — frontmatter description | test edits | Engineer's diff, `## Documentation Locations` | - | Bash, Edit | - | procedure |
| R-186 | Test Writer does NOT modify source code or documentation — those are owned by engineer and doc-writer. | invariant | test-writer.md:L3,L9 — description / intro | - | - | engineer.md, doc-writer.md | none | - | procedure |
| R-187 | Test Writer is dispatched by `/quo-execute` or `/quo-fix-issue` after the Engineer's implementation has landed. | ordering | test-writer.md:L9 — intro | - | - | `/quo-execute`, `/quo-fix-issue`, engineer.md | none | - | procedure |
| R-188 | Execute test Subtasks for a Task (execute mode) — change, add, or delete tests as directed. | command | test-writer.md:L13,L22 — ## Responsibilities / ## Instructions | test edits | Subtask body | - | Edit, Write | - | procedure |
| R-189 | Cover gaps the pre-planned test subtasks missed by reviewing the Engineer's diff and adding/updating/deleting tests. | command | test-writer.md:L14,L23 — ## Responsibilities / ## Instructions | test edits | Engineer's diff | - | Read, Edit | - | procedure |
| R-190 | In fix mode, with no pre-planned subtasks, write tests that verify the Engineer's fix covers the issue. | command | test-writer.md:L15 — ## Responsibilities | test edits | Engineer's diff, Issue | - | Edit, Write | - | procedure |
| R-191 | Research-only Tasks may omit all test subtasks. | definition | test-writer.md:L16 — ## Responsibilities | - | - | - | none | mirrors engineer.md L14, doc-writer.md L23 | procedure |
| R-192 | Use the test writing guide referenced in CLAUDE.md `## Documentation Locations`. | command | test-writer.md:L20 — ## Instructions | - | `Test writing guide` | - | Read | - | procedure |
| R-193 | Use the test review guide referenced in CLAUDE.md `## Documentation Locations`. | command | test-writer.md:L21 — ## Instructions | - | `Test review guide` | - | Read | - | procedure |
| R-194 | Mark ticket status as work proceeds; it is the handoff signal doc-writer and PM are gated on — do not skip it. | invariant | test-writer.md:L24 — ## Instructions | status flip | - | doc-writer.md, pm.md | bees | mirrors engineer.md L26 | procedure |
| R-195 | Execute mode: `status=in_progress` when starting the Subtask and `status=done` when finishing (full `drafted` → `ready` → `in_progress` → `done` ladder). | command | test-writer.md:L26 — ## Instructions | `in_progress`, `done` | Subtask `t3` | - | Bash, bees | mirrors engineer.md L28 | procedure |
| R-196 | Fix mode: do not set `in_progress` (rejected with `Invalid status 'in_progress'`) and do not flip to `done`; the orchestrator owns `open` → `done` (Section 7 of `quo-fix-issue/SKILL.md`). | invariant | test-writer.md:L27 — ## Instructions | - | Issue ticket | `quo-fix-issue/SKILL.md` Section 7 | bees | mirrors engineer.md L29 | procedure |
| R-197 | Run `bees update-ticket --ids <subtask-id> --status in_progress` / `--status done` (POSIX and PowerShell). | command | test-writer.md:L31-L51 — ## Instructions | status flips | - | - | Bash, bees | mirrors engineer.md L33-L53 | procedure |
| R-198 | Execute mode: take the closing movement-fingerprint reading first; run the `done` flip only after the comparison against the opening reading clears. | ordering | test-writer.md:L53,L62 — ## Instructions | `done` | fingerprint readings | - | Bash, git, bees | - | procedure |
| R-199 | When the comparison shows movement you must stop on, do NOT perform the `done` flip; leave the Subtask `in_progress` and report the movement. | invariant | test-writer.md:L53,L103 — ## Instructions | `in_progress`, movement report | - | orchestrator's movement-report receiver | bees | - | procedure |
| R-200 | Fix mode has no flip to order — you never set the Issue's status. | definition | test-writer.md:L53 — ## Instructions | - | - | - | none | - | procedure |
| R-201 | The orchestrator controls only its own dispatches; the source-tree-stability promise is scoped by the mode that dispatched you. | definition | test-writer.md:L55 — ## Instructions Source-tree stability | - | - | - | none | mirrors doc-writer.md L37 | procedure |
| R-202 | Fix mode guarantee: no Engineer Agent will be dispatched for this Issue while you run; movement in any source file outside your test lane is anomalous. | definition | test-writer.md:L57 — ## Instructions Source-tree stability | - | - | - | none | mirrors doc-writer.md L39 | procedure |
| R-203 | Execute mode: no review-driven Engineer re-dispatch for your Task while you run, but a sibling-Subtask Engineer may legitimately run concurrently (dependency-ordered fan-out). | definition | test-writer.md:L58 — ## Instructions Source-tree stability | - | - | - | none | mirrors doc-writer.md L40 | procedure |
| R-204 | Execute mode: movement in a sibling Subtask's files is expected; anomalous movement is in the files your tests pin — the `## Source paths to fingerprint` set. | definition | test-writer.md:L58 — ## Instructions Source-tree stability | - | `## Source paths to fingerprint` | - | none | - | procedure |
| R-205 | When the dispatch prompt scopes you to the whole Bee (no Subtask id), any movement in the `## Source paths to fingerprint` set is anomalous; treat it as fix mode's strict branch. | command | test-writer.md:L58,L103 — ## Instructions Source-tree stability | - | Bee-scoped dispatch prompt | - | none | mirrors doc-writer.md L40 (Bee-scoped clause) | procedure |
| R-206 | Neither mode guarantees a frozen tree; treat any dispatch prompt claiming a frozen tree as overclaiming and verify instead of assuming. | invariant | test-writer.md:L60 — ## Instructions Source-tree stability | - | - | - | none | mirrors doc-writer.md L42 | procedure |
| R-207 | Take the same two fingerprint readings at the start of your run and again before you finish, each line as its own Bash call. | command | test-writer.md:L62 — ## Instructions Movement fingerprint | fingerprint readings | - | - | Bash, git | - | procedure |
| R-208 | Fingerprint readings are `git rev-parse HEAD` and `git hash-object <source paths outside your test lane>` (POSIX and PowerShell identical). | command | test-writer.md:L64-L74 — ## Instructions Movement fingerprint | fingerprint readings | - | - | Bash, git | - | procedure |
| R-209 | `<source paths outside your test lane>` is the `## Source paths to fingerprint` set the dispatch prompt supplies (execute: parent Task's implementation Subtasks' changed files; fix: Engineer diff files). | definition | test-writer.md:L76 — ## Instructions Movement fingerprint | - | `## Source paths to fingerprint` | engineer.md L57 | none | - | procedure |
| R-210 | When the dispatch prompt omits `## Source paths to fingerprint`, derive the set yourself once with `git diff --name-only HEAD`. | command | test-writer.md:L76-L86 — ## Instructions Movement fingerprint | - | - | - | Bash, git | - | procedure |
| R-211 | The fallback derivation is `HEAD`-relative so staged-but-uncommitted edits are included; drop your own test files; derive once and reuse the same list in the same order for the closing reading. | command | test-writer.md:L88 — ## Instructions Movement fingerprint | - | - | - | git | - | procedure |
| R-212 | When the path set is empty, only the `git rev-parse HEAD` reading applies; do NOT run `git hash-object` with no path arguments (exits 0, prints nothing, reads "clean"). | invariant | test-writer.md:L90 — ## Instructions Movement fingerprint | - | - | - | git | - | procedure |
| R-213 | With an empty path set, record in your return that the content-hash reading was not taken because the path set was empty. | command | test-writer.md:L90 — ## Instructions Movement fingerprint | return note | - | - | none | - | procedure |
| R-214 | `git hash-object` is byte-exact, index-agnostic, and O(1) in context; pass the same path list in the same order both times so hash lists align positionally. | command | test-writer.md:L92 — ## Instructions Movement fingerprint | - | - | - | git | - | procedure |
| R-215 | `git status --porcelain` is not a fingerprint: a file already ` M` reads ` M` before and after any further edit. | invariant | test-writer.md:L92,L124 — ## Instructions Movement fingerprint / discrimination experiment | - | - | - | git | - | procedure |
| R-216 | A changed hash for any path outside your test lane means the source moved under you. | definition | test-writer.md:L96 — ## Instructions Movement fingerprint | - | - | - | none | - | procedure |
| R-217 | A changed `git rev-parse HEAD` is movement in its own right; an unchanged hash list proves nothing once HEAD has moved. | definition | test-writer.md:L97 — ## Instructions Movement fingerprint | - | - | - | none | - | procedure |
| R-218 | If `git hash-object` errors on a path it hashed at the start, the path was deleted or renamed — treat exactly as a changed hash, naming the path and the error. | invariant | test-writer.md:L98 — ## Instructions Movement fingerprint | movement report | - | - | git | - | procedure |
| R-219 | Do not drop a now-erroring path and re-run for a clean comparison; the failure is the finding. | invariant | test-writer.md:L98 — ## Instructions Movement fingerprint | - | - | - | none | - | procedure |
| R-220 | A path the opening reading also failed on was never in the set — fix the list at the start of the run, not the end. | command | test-writer.md:L98 — ## Instructions Movement fingerprint | - | - | - | none | - | procedure |
| R-221 | Fix mode on movement: stop and report to the orchestrator; say which files moved (opening and closing HEAD when HEAD moved) and how far the test work got. | command | test-writer.md:L102 — ## Instructions Movement fingerprint | movement report | - | - | none | - | procedure |
| R-222 | Pinning assertions to half-landed behavior produces tests wrong within minutes; the orchestrator can re-dispatch cleanly once the source settles. | rationale-only | test-writer.md:L102 — ## Instructions Movement fingerprint | - | - | - | none | - | rationale |
| R-223 | Execute mode on movement: determine whether it touches your Subtask's files; when HEAD moved, run `git diff --name-only <opening-head-sha> <closing-head-sha>` (two-SHA form, not a single-commit inspection). | command | test-writer.md:L103 — ## Instructions Movement fingerprint | - | - | - | Bash, git | - | procedure |
| R-224 | Reading only the tip commit misses earlier commits' files — exactly how a "none of the moved files are yours" verdict comes out wrong. | rationale-only | test-writer.md:L103 — ## Instructions Movement fingerprint | - | - | - | none | - | failure-narrative |
| R-225 | Execute mode: if none of the moved files are yours, record the observation in your return and finish normally; the `done` flip proceeds. | command | test-writer.md:L103 — ## Instructions Movement fingerprint | observation note, `done` | - | - | bees | - | procedure |
| R-226 | Execute mode: if any moved file is yours, stop and report as in fix mode and leave the Subtask at `in_progress`. | command | test-writer.md:L103 — ## Instructions Movement fingerprint | movement report, `in_progress` | - | - | none | - | procedure |
| R-227 | Bee-scoped execute-mode dispatch: there is no "your Subtask" and no `done` flip; any movement in the supplied set is anomalous — stop and report as fix mode. | command | test-writer.md:L103 — ## Instructions Movement fingerprint | movement report | - | - | none | - | procedure |
| R-228 | Never mutate files outside the test lane; do NOT run `git checkout`, `git restore`, `git stash`, or `git reset` against any non-test path, even for a momentary revert. | invariant | test-writer.md:L105 — ## Instructions | - | - | - | git | - | procedure |
| R-229 | For a discrimination experiment, `Read` the source file and `Write` its exact contents to `/tmp/.quorum/` (POSIX) or `%TEMP%\.quorum` (Windows) as `test-writer-<short-suffix>-<original-filename>`. | command | test-writer.md:L107-L109 — ## Instructions discrimination experiment | scratch copy | - | - | Read, Write | - | procedure |
| R-230 | Create the `.quorum` subdirectory if absent: `mkdir -p /tmp/.quorum` / `New-Item -ItemType Directory -Force -Path "$env:TEMP\.quorum" \| Out-Null`. | command | test-writer.md:L109-L121 — ## Instructions discrimination experiment | scratch dir | - | - | Bash | mirrors pm.md L117-L125, L171-L179 | procedure |
| R-231 | Perturb the original with `Edit` / `Write`, run the experiment, then restore by `Write`ing the copy's contents back over the original. | command | test-writer.md:L123 — ## Instructions discrimination experiment | temporary source perturbation | - | - | Edit, Write | - | procedure |
| R-232 | Do not delete the scratch copy on any OS. | invariant | test-writer.md:L124 — ## Instructions discrimination experiment | - | - | - | none | mirrors pm.md L144, L193 | procedure |
| R-233 | Confirm the restore landed: `Read` the original back and compare it against the scratch copy; do not use `git status --porcelain` for this. | command | test-writer.md:L124 — ## Instructions discrimination experiment | - | - | - | Read | - | procedure |
| R-234 | For byte-exact confirmation run `git hash-object <path-to-original>` and `git hash-object <path-to-scratch-copy>` as two separate Bash calls and check the hashes match. | command | test-writer.md:L124 — ## Instructions discrimination experiment | - | - | - | Bash, git | - | procedure |
| R-235 | Record every perturbation under a `## Perturbations` heading: one repository-relative path per line, stating each was restored and verified by step 3's comparison. | contract-heading | test-writer.md:L126 — ## Instructions discrimination experiment | `## Perturbations` | - | - | none | - | procedure |
| R-236 | When no discrimination experiment ran, emit `## Perturbations` with the single word `None`; do not drop the heading. | invariant | test-writer.md:L126 — ## Instructions discrimination experiment | `## Perturbations` | - | - | none | - | procedure |
| R-237 | Both orchestrators read `## Perturbations` to attribute a concurrent writer's observed movement to your experiment and re-dispatch the stopped lane without an operator gate. | definition | test-writer.md:L126 — ## Instructions discrimination experiment | `## Perturbations` | - | `/quo-execute`, `/quo-fix-issue` | none | - | procedure |
| R-238 | While iterating use **Narrow test** and **Lint**; do NOT run **Full test** — the workspace-wide run happens once at the Task's `.T` subtask. | invariant | test-writer.md:L128 — ## Instructions Test-scope discipline | - | `Narrow test`, `Lint`, `Full test` | - | Bash | mirrors engineer.md L66 | procedure |
| R-239 | Lookup keys are the exact contract names `Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`; do not hardcode language-specific commands. | invariant | test-writer.md:L128 — ## Instructions Test-scope discipline | - | `## Build Commands` | - | Read | mirrors engineer.md L66, pm.md L226 | procedure |
| R-240 | Use the Bash tool's `timeout` parameter (max 600000 ms) and run test invocations up to 10 min in the foreground. | command | test-writer.md:L130 — ## Instructions Running long commands | - | project test command | - | Bash | - | procedure |
| R-241 | For runs exceeding 10 min, use `Bash` with `run_in_background: true`, wait silently for the completion notification, then `Read` the output file. | command | test-writer.md:L130 — ## Instructions Running long commands | - | - | - | Bash, Read | - | procedure |
| R-242 | Do not write shell polling loops to wait for completion; the harness handles notification. | invariant | test-writer.md:L130 — ## Instructions Running long commands | - | - | - | Bash | - | procedure |
| R-243 | Shell-command etiquette: one literal command per Bash call; no diagnostic tails; no `$VAR` / `$(...)` / backticks / redirects / compound chains; multi-step via `Write`-to-file; prefer first-class tools. | invariant | test-writer.md:L132 — ## Instructions Shell-command etiquette | - | - | - | Bash, Write, Read, Grep, Glob, Edit | mirrors engineer.md L68, pm.md L248, analyst.md L125 | procedure |
| R-244 | Frontmatter `name` is `doc-writer`, `model: opus`, `effort: high`. | definition | doc-writer.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-245 | Tool allowlist is exactly `[Read, Edit, Write, Grep, Glob]`; `Bash` is excluded by design. | tool-allowlist | doc-writer.md:L6,L3,L9 — frontmatter / intro | - | - | - | Read, Edit, Write, Grep, Glob | - | procedure |
| R-246 | Doc Writer authors/updates customer-facing and internal architecture docs (execute: doc Subtasks; fix: diff review) against the doc writing guide; edits markdown files only. | definition | doc-writer.md:L3 — frontmatter description | doc edits | `## Documentation Locations` | - | Edit, Write | - | procedure |
| R-247 | Doc Writer does NOT modify source code or tests — those are owned by engineer and test-writer. | invariant | doc-writer.md:L3,L9 — description / intro | - | - | engineer.md, test-writer.md | none | - | procedure |
| R-248 | Execute mode: execute the pre-planned doc Subtasks first, then review the Engineer's diff for additional gaps. | ordering | doc-writer.md:L15 — ## Mode divergence | doc edits | Subtask bodies, Engineer's diff | `/quo-execute` | Read, Edit | - | procedure |
| R-249 | Fix mode: no pre-planned doc Subtasks exist; the work is a diff-review pass over the Engineer's changes plus ad-hoc updates. | command | doc-writer.md:L16,L31 — ## Mode divergence / ## Instructions | doc edits | Engineer's diff | `/quo-fix-issue` | Read, Edit | - | procedure |
| R-250 | The divergence is intentional: in execute mode the breakdown encodes which docs need updating; in fix mode the only signal is the diff. | rationale-only | doc-writer.md:L18 — ## Mode divergence | - | - | - | none | - | rationale |
| R-251 | Execute documentation Subtasks for a Task — customer-facing docs and internal architecture docs subtasks. | command | doc-writer.md:L22,L29,L30 — ## Responsibilities / ## Instructions | doc edits | Subtask bodies | - | Edit, Write | - | procedure |
| R-252 | Research-only Tasks may omit all doc subtasks. | definition | doc-writer.md:L23 — ## Responsibilities | - | - | - | none | mirrors engineer.md L14, test-writer.md L16 | procedure |
| R-253 | Use the doc writing guide referenced in CLAUDE.md `## Documentation Locations`. | command | doc-writer.md:L28 — ## Instructions | - | `Doc writing guide` | - | Read | - | procedure |
| R-254 | Review the Engineer's diff for gaps and update the customer-facing docs and internal architecture docs referenced in CLAUDE.md `## Documentation Locations`. | command | doc-writer.md:L31 — ## Instructions | doc edits | Engineer's diff, `Customer-facing docs`, `Internal architecture docs (SDD)` | - | Read, Edit | - | procedure |
| R-255 | Ensure status transitions happen; the PM is gated on them — do not skip; with no `Bash`, transitions are routed through the orchestrator rather than the bees CLI. | invariant | doc-writer.md:L32 — ## Instructions | - | - | pm.md | none | - | procedure |
| R-256 | Execute mode: the orchestrator marks the Subtask `status=in_progress` when this subagent begins and `status=done` when it finishes. | definition | doc-writer.md:L34 — ## Instructions | - | - | orchestrating execution skill | none | - | procedure |
| R-257 | Fix mode: the Issue supports only `open` and `done`; the orchestrator leaves it at `open` and flips `open` → `done` at close-out. | definition | doc-writer.md:L35 — ## Instructions | - | - | - | none | - | procedure |
| R-258 | Fix mode guarantee: no Engineer Agent is dispatched for this Issue while you run; the source-side code review has already closed, so document the behavior in front of you. | command | doc-writer.md:L39 — ## Instructions Source-tree stability | - | - | code-reviewer.md | none | mirrors test-writer.md L57 | procedure |
| R-259 | Fix mode: do not read the guarantee as "the diff you read is the diff that ships"; a later round can reopen the source and you will be re-dispatched. | definition | doc-writer.md:L39 — ## Instructions Source-tree stability | - | - | - | none | - | procedure |
| R-260 | Execute mode: a sibling-Subtask Engineer may run concurrently; document what your Subtask's diff establishes and expect cumulative-doc entries to be revisited. | command | doc-writer.md:L40 — ## Instructions Source-tree stability | doc edits | Subtask diff | - | none | mirrors test-writer.md L58 | procedure |
| R-261 | Bee-scoped dispatch (no Subtask id): every Subtask is finished, so any movement in the material you document is anomalous, including sibling-Subtask movement. | command | doc-writer.md:L40 — ## Instructions Source-tree stability | - | Bee-scoped dispatch prompt | - | none | mirrors test-writer.md L58 | procedure |
| R-262 | Neither mode guarantees a frozen tree; treat a frozen-tree claim as overclaiming. | invariant | doc-writer.md:L42 — ## Instructions Source-tree stability | - | - | - | none | mirrors test-writer.md L60 | procedure |
| R-263 | If material moved mid-run (a file no longer matches the prose written against it), stop and report to the orchestrator rather than shipping stale documentation. | command | doc-writer.md:L42 — ## Instructions Source-tree stability | movement report | - | - | Read, Grep | - | procedure |
| R-264 | Execute mode: movement confined to a sibling Subtask's own files is expected; report it as an observation and finish. | command | doc-writer.md:L42 — ## Instructions Source-tree stability | observation note | - | - | none | - | procedure |
| R-265 | Re-check for movement with `Read` / `Grep`, not a shell command (`Bash` is not in the allowlist). | command | doc-writer.md:L42 — ## Instructions Source-tree stability | - | - | - | Read, Grep | - | procedure |
| R-266 | Once the Engineer's diff has landed, the doc-writer owns keeping the project's cumulative PRD and SDD current. | command | doc-writer.md:L46 — ## Cumulative project doc updates | cumulative PRD/SDD edits | Engineer's diff | - | Edit, Write | - | procedure |
| R-267 | "Cumulative" means the long-lived project-level docs that grow Feature-by-Feature, distinct from per-feature PRD/SDD spec tickets created at plan time. | definition | doc-writer.md:L46 — ## Cumulative project doc updates | - | - | - | none | - | procedure |
| R-268 | The doc-writer runs after implementation and sees the actual diff, so its entries describe what was built rather than what was projected. | rationale-only | doc-writer.md:L46 — ## Cumulative project doc updates | - | - | - | none | - | rationale |
| R-269 | Before applying the update mechanism, classify the change against the categorization table to decide which cumulative doc(s) get a `### Feature: <title>` subsection. | ordering | doc-writer.md:L50 — ### Categorization | - | diff | - | none | - | procedure |
| R-270 | User-facing feature (new endpoint, behavior change, user-visible capability): PRD entry Yes, SDD entry Yes. | definition | doc-writer.md:L54 — ### Categorization table | `### Feature: <title>` in PRD and SDD | - | - | none | - | procedure |
| R-271 | Architecture-only change (perf, caching, retry logic, internal structure, architecture-affecting dependency upgrades): PRD No, SDD Yes. | definition | doc-writer.md:L55 — ### Categorization table | `### Feature: <title>` in SDD | - | - | none | - | procedure |
| R-272 | Deployment / infrastructure / CI / testing change (Helm, CI workflows, monitoring, smoke tests, test helpers): PRD No unless customer-visible, SDD Yes. | definition | doc-writer.md:L56 — ### Categorization table | `### Feature: <title>` in SDD | - | - | none | - | procedure |
| R-273 | Internal refactor / pure-tech (no user- or system-visible behavior change): PRD No, SDD No. | definition | doc-writer.md:L57 — ### Categorization table | - | - | - | none | - | procedure |
| R-274 | Examples per row: "Add CSV export"; "New `--scope` CLI flag"; "Add auth retries"; "Switch internal queue from list to deque"; "Add Helm charts"; "Extract shared test helpers"; "Rename internal module". | example | doc-writer.md:L54-L57 — ### Categorization table | - | - | - | none | - | example |
| R-275 | The deployment row collapses two historical plan-time rows (architecture-reliability, deployment-infrastructure); the user-visible-deployment carve-out escalates to the user-facing row. | definition | doc-writer.md:L59 — ### Categorization | - | - | - | none | - | rationale |
| R-276 | The actual diff is the primary categorization signal — classify on what was built, not what was projected at plan time. | command | doc-writer.md:L63 — #### Inputs the doc-writer uses for categorization | - | Engineer's diff | - | Read | - | procedure |
| R-277 | The dispatched Task/Subtask body's `## What Needs to Change` / `## Why` / `## Acceptance Criteria` sections are the secondary signal, authored by `/quo-breakdown-epic`. | command | doc-writer.md:L64 — #### Inputs the doc-writer uses for categorization | - | `## What Needs to Change`, `## Why`, `## Acceptance Criteria` | `/quo-breakdown-epic` | Read | - | procedure |
| R-278 | Plan Bee bodies post-redesign are a brief 2-3 sentence summary plus an `## Anticipated doc impact` section; categorization detail lives in the Task/Subtask body. | definition | doc-writer.md:L64 — #### Inputs the doc-writer uses for categorization | - | `## Anticipated doc impact` | - | none | - | procedure |
| R-279 | When the diff and the Task/Subtask body disagree on category, trust the diff and surface the divergence. | command | doc-writer.md:L64 — #### Inputs the doc-writer uses for categorization | divergence note | diff, Task/Subtask body | - | none | - | procedure |
| R-280 | Use touched file paths as a heuristic: internal modules → refactor; public APIs, CLI surfaces, config schemas, installable artifacts → user-facing or deployment. | command | doc-writer.md:L65 — #### Inputs the doc-writer uses for categorization | - | diff file paths | - | none | - | procedure |
| R-281 | Refactor + user-facing in one change: categorize by the user-facing dimension; both PRD and SDD entries are warranted. | command | doc-writer.md:L69 — #### Edge cases | `### Feature: <title>` in PRD and SDD | - | - | none | - | procedure |
| R-282 | An architectural capability added in service of a user-facing change is categorized user-facing and folded into the same `### Feature: <title>` subsection on both docs. | command | doc-writer.md:L70 — #### Edge cases | `### Feature: <title>` | - | - | none | - | procedure |
| R-283 | When in doubt between two adjacent rows, pick the broader-scope row. | command | doc-writer.md:L71 — #### Edge cases | - | - | - | none | - | procedure |
| R-284 | Over-documenting is recoverable via the idempotency rule; under-documenting requires re-running the pass or catching the gap in a later review. | rationale-only | doc-writer.md:L71 — #### Edge cases | - | - | - | none | - | rationale |
| R-285 | The customer-facing-docs (README) review pass is independent of categorization; it is not gated, suppressed, or altered by it. | invariant | doc-writer.md:L75 — #### Relationship to the customer-facing docs | - | - | - | none | - | procedure |
| R-286 | Treat the README pass and the cumulative PRD/SDD pass as orthogonal and run both on every dispatch. | invariant | doc-writer.md:L75 — #### Relationship to the customer-facing docs | - | - | - | none | - | procedure |
| R-287 | Cumulative PRD: append or update `### Feature: <title>` under `## Per-feature scope`; create the header if absent at a stable location, typically near the end. | command | doc-writer.md:L79 — ### What gets updated, where | `### Feature: <title>` under `## Per-feature scope` | `Project requirements doc (PRD)` | - | Edit, Write | - | procedure |
| R-288 | The PRD path is resolved via CLAUDE.md `## Documentation Locations` `Project requirements doc (PRD)` — never hardcoded. | invariant | doc-writer.md:L79,L107 — ### What gets updated, where / ### Path resolution | - | `Project requirements doc (PRD)` | - | Read | - | procedure |
| R-289 | Cumulative SDD: append or update `### Feature: <title>` under `## Per-feature design` at the `Internal architecture docs (SDD)` path; create the header if absent. | command | doc-writer.md:L80 — ### What gets updated, where | `### Feature: <title>` under `## Per-feature design` | `Internal architecture docs (SDD)` | - | Edit, Write | - | procedure |
| R-290 | Subsection prose reflects what was actually built — not forward-looking content copied from the Plan Bee body, PRD/SDD ticket-children, or earlier scope statements. | invariant | doc-writer.md:L81 — ### What gets updated, where | `### Feature: <title>` content | diff | - | none | - | procedure |
| R-291 | If implementation diverged from the plan-time spec, the cumulative-doc entry follows the implementation. | command | doc-writer.md:L81 — ### What gets updated, where | - | - | - | none | - | procedure |
| R-292 | The `Customer-facing docs` lookup-key path resolution for the README pass is unchanged and independent of the cumulative PRD/SDD work. | definition | doc-writer.md:L82 — ### What gets updated, where | - | `Customer-facing docs` | - | none | - | procedure |
| R-293 | `<title>` is the Plan Bee's `title` field used verbatim as the `### Feature: <title>` heading — no paraphrasing, trimming, or case normalization. | invariant | doc-writer.md:L86 — ### Source of truth for `<title>` | `### Feature: <title>` | Plan Bee `title` | - | none | - | procedure |
| R-294 | Execute mode: the orchestrator walks Subtask → Task → Epic → Plan Bee and surfaces the Plan Bee title in the dispatch prompt or via a ticket file on disk for you to `Read`. | definition | doc-writer.md:L88 — ### Source of truth for `<title>` | - | Plan Bee title in dispatch prompt / ticket file | orchestrating execution skill | Read | - | procedure |
| R-295 | Fix mode: the orchestrator traverses the Issue's `up_dependencies` for a linked Plan Bee and surfaces that title the same way. | definition | doc-writer.md:L89 — ### Source of truth for `<title>` | - | Plan Bee title | - | none | - | procedure |
| R-296 | If a standalone Issue has no Plan Bee in `up_dependencies`, the orchestrator surfaces an out-of-spec condition rather than having you improvise a `<title>`. | invariant | doc-writer.md:L89 — ### Source of truth for `<title>` | - | - | - | none | - | procedure |
| R-297 | If the orchestrator pre-resolved and passed a fallback `<title>` explicitly (e.g. the Issue title), honor it; otherwise stop and report the missing Plan Bee linkage. | command | doc-writer.md:L89 — ### Source of truth for `<title>` | missing-linkage report | fallback title | - | none | - | procedure |
| R-298 | Read the resolved `<title>` from dispatch context; do not call `bees show-ticket` yourself — traversal logic requiring the bees CLI is owned by the orchestrator. | invariant | doc-writer.md:L91 — ### Source of truth for `<title>` | - | dispatch context | - | Read | - | procedure |
| R-299 | Never append a duplicate `### Feature: <title>` subsection when a Feature lands in cumulative docs more than once. | invariant | doc-writer.md:L95 — ### Idempotency | - | - | - | none | - | procedure |
| R-300 | Search the cumulative doc for the literal heading text `### Feature: <title>` — exact match, case-sensitive on `<title>`. | command | doc-writer.md:L99 — ### Idempotency Detection rule | - | cumulative doc | - | Grep, Read | - | procedure |
| R-301 | If a match exists, replace everything from the matching heading line up to (not including) the next `### ` heading or end-of-file; leave the parent header and sibling subsections untouched. | command | doc-writer.md:L100 — ### Idempotency Detection rule | rewritten `### Feature: <title>` | - | - | Edit | - | procedure |
| R-302 | If no match exists, append a new `### Feature: <title>` at the end of `## Per-feature scope` / `## Per-feature design` after existing subsections; create the parent header first if absent. | command | doc-writer.md:L101 — ### Idempotency Detection rule | new `### Feature: <title>` | - | - | Edit, Write | - | procedure |
| R-303 | `Edit` and `Write` suffice for both shapes; prefer `Edit` with `old_string` = the full existing subsection when the heading is unique; use a stable anchor or `Write` for header creation/append. | command | doc-writer.md:L103 — ### Idempotency | - | - | - | Edit, Write | - | procedure |
| R-304 | Do not hardcode `docs/prd.md`, `docs/sdd.md`, or any project-specific path at runtime, and do not infer paths from filename conventions. | invariant | doc-writer.md:L107 — ### Path resolution via CLAUDE.md keys | - | `Project requirements doc (PRD)`, `Internal architecture docs (SDD)` | - | none | - | procedure |
| R-305 | If either PRD/SDD key is missing or empty, treat it as a setup gap and surface it; the orchestrator hard-fails with a setup-required message; never write to a guessed path. | invariant | doc-writer.md:L107 — ### Path resolution via CLAUDE.md keys | setup-gap report | `## Documentation Locations` | orchestrating execution skill | none | - | procedure |
| R-306 | Frontmatter `name` is `pm`, `model: opus`, `effort: high`. | definition | pm.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-307 | Tool allowlist is exactly `[Bash, Read, Skill, Grep, Glob, Write, WebFetch]`. | tool-allowlist | pm.md:L6 — frontmatter | - | - | - | Bash, Read, Skill, Grep, Glob, Write, WebFetch | - | procedure |
| R-308 | PM performs per-Task review including spec traceability, scope-creep / spec-divergence checks, cross-Task / cross-Epic checks, time-budgeted review-skill orchestration, and a final per-Task report. | definition | pm.md:L3,L9 — frontmatter description / intro | per-Task report | `## Documentation Locations`, `## Build Commands` | `/quo-engineer-review`, `/quo-doc-writer-review` | Skill, Bash | - | procedure |
| R-309 | PM does NOT modify source code, tests, or docs. | invariant | pm.md:L3,L9 — description / intro | - | - | engineer.md, test-writer.md, doc-writer.md | none | - | procedure |
| R-310 | PM is dispatched by `/quo-execute` or `/quo-fix-issue` after the Engineer / Test Writer / Doc Writer have produced their work. | ordering | pm.md:L9 — intro | - | - | `/quo-execute`, `/quo-fix-issue` | none | - | procedure |
| R-311 | `Skill` is in the allowlist so the PM can dispatch `/quo-engineer-review` and `/quo-doc-writer-review` in-flight. | tool-allowlist | pm.md:L9 — intro | - | - | `/quo-engineer-review`, `/quo-doc-writer-review` | Skill | - | procedure |
| R-312 | `Write` is in the allowlist because the Scoped-marker helper consumes a temp file the PM writes under `<tempdir>/.quorum/` and never deletes. | tool-allowlist | pm.md:L9 — intro | scratch file | - | scoped_marker_resolver.py | Write | - | procedure |
| R-313 | Review Task work against the spec source: `reference_materials` docs of the Grandparent Bee (execute) or Issue (fix), or the ticket body when `reference_materials` is null/empty. | command | pm.md:L13 — ## Responsibilities | - | `reference_materials`, ticket body | - | Read, Bash, bees | - | procedure |
| R-314 | Ensure the work meets the Task's requirements and the Parent Epic's Acceptance Criteria. | command | pm.md:L14 — ## Responsibilities | - | Task, Epic Acceptance Criteria | - | none | - | procedure |
| R-315 | Surface design questions back to the orchestrator when the team proposes alternative approaches needing user input. | command | pm.md:L15 — ## Responsibilities | design question in report | - | - | none | - | procedure |
| R-316 | Orchestrate in-flight `/quo-engineer-review` and `/quo-doc-writer-review` against the Task's diff with a time-budget short-circuit. | command | pm.md:L16,L219 — ## Responsibilities / ## Instructions | - | Task diff | `/quo-engineer-review`, `/quo-doc-writer-review` | Skill | - | procedure |
| R-317 | Verify cross-Task and cross-Epic interactions before approving a Task. | ordering | pm.md:L17,L227 — ## Responsibilities / ## Instructions | - | - | - | Bash, git | - | procedure |
| R-318 | Produce a final per-Task report consumed by the orchestrating execution skill. | command | pm.md:L18,L233 — ## Responsibilities / Final report contract | final report | - | - | none | - | procedure |
| R-319 | Read the assigned Task using the bees CLI. | command | pm.md:L22 — ## Instructions | - | Task | - | Bash, bees | - | procedure |
| R-320 | Read all Subtasks (children of the Task). | command | pm.md:L23 — ## Instructions | - | Subtasks | - | Bash, bees | - | procedure |
| R-321 | Read the Parent Epic. | command | pm.md:L24 — ## Instructions | - | Epic | - | Bash, bees | - | procedure |
| R-322 | Read the Grandparent Bee. | command | pm.md:L25 — ## Instructions | - | Plan Bee | - | Bash, bees | - | procedure |
| R-323 | If the Grandparent Bee's `reference_materials` is null/empty (`/quo-plan` Bees), the Bee body is the authoritative spec; substitute "the Plan Bee body" wherever prose says "the PRD" / "the SDD". | command | pm.md:L26,L65 — ## Instructions / ### Resolving `reference_materials` entries | - | Plan Bee body | `/quo-plan` | none | - | procedure |
| R-324 | The spec-source ticket is the Grandparent Bee in execute mode and the Issue in fix mode — the `reference_materials`-bearing ticket named in the dispatch prompt. | definition | pm.md:L30 — ### Resolving `reference_materials` entries | - | dispatch prompt ticket id | - | none | - | procedure |
| R-325 | There are three resolver paths (`file-path`, `bees`, external-URL) plus the body-as-spec fallback; iterate the array and dispatch on each entry's `resolver`. | command | pm.md:L30 — ### Resolving `reference_materials` entries | - | `reference_materials[].resolver` | - | none | - | procedure |
| R-326 | `resolver` is `file-path` (or omitted): treat `value` as a path on disk and read the file; Scoped-marker integration applies on this path. | command | pm.md:L32 — ### Resolving `reference_materials` entries | - | `value` path | - | Read | - | procedure |
| R-327 | `resolver` is `bees`: treat `value` as a Spec Bee ID in the `specs` hive and walk `Spec Bee → t1=Doc children → PRD / SDD content`. | command | pm.md:L33 — ### Resolving `reference_materials` entries | - | Spec Bee ID | - | Bash, bees | - | procedure |
| R-328 | Run `bees show-ticket --ids <spec-bee-id>` and read the `children` array; for each child run `bees show-ticket --ids <child-id>` and read `title` and `body`. | command | pm.md:L35-L36,L43-L61 — ### Resolving `reference_materials` entries | - | `children`, `title`, `body` | - | Bash, bees | - | procedure |
| R-329 | Identify PRD vs SDD by exact-match, case-sensitive `title` equal to `PRD` or `SDD`; use those bodies as the spec source. | command | pm.md:L37 — ### Resolving `reference_materials` entries | - | `t1=Doc` child `title` | - | none | - | procedure |
| R-330 | The `PRD` and `SDD` title strings are a cross-Epic contract; do not lower-case, normalize, or fuzzy-match them. | invariant | pm.md:L39 — ### Resolving `reference_materials` entries | - | - | - | none | - | procedure |
| R-331 | Prefer the `bees show-ticket` walk; `bees execute-freeform-query --query-yaml '<yaml>'` is acceptable when title-filtered enumeration is wanted up-front. | command | pm.md:L41 — ### Resolving `reference_materials` entries | - | - | - | Bash, bees | - | procedure |
| R-332 | `resolver` is `github-issue`, `linear-issue`, `url`, or any other external-URL name (fix mode only): fetch `value` via `WebFetch` and use the fetched content as the spec source. | command | pm.md:L63 — ### Resolving `reference_materials` entries | - | external URL | `/quo-file-issue --reference` / `--from-github` | WebFetch | mirrors analyst.md L26, engineer.md L19 | procedure |
| R-333 | Skip Scoped-marker resolution entirely on the external-URL path — markers apply to on-disk PRD/SDD, not URLs. | invariant | pm.md:L63 — ### Resolving `reference_materials` entries | - | - | - | none | - | procedure |
| R-334 | If `WebFetch` cannot reach the URL, surface the failure in the PM's report rather than guessing. | command | pm.md:L63 — ### Resolving `reference_materials` entries | report note | - | - | WebFetch | mirrors analyst.md L26, engineer.md L19 | procedure |
| R-335 | The no-spec-surface short-circuit applies only on the fix-mode path; execute-mode PMs proceed directly to the deep review. | precondition | pm.md:L69 — ## No-spec-surface short-circuit | - | dispatch prompt (Issue ID, no Grandparent Bee) | `/quo-fix-issue`, `/quo-execute` | none | - | procedure |
| R-336 | In fix mode the PM is always dispatched per Issue; the orchestrator does not pre-judge spec-drift risk. | definition | pm.md:L71 — ## No-spec-surface short-circuit | - | - | - | none | - | procedure |
| R-337 | The short-circuit is content-based, not shape-based; judge it after the spec sources have been read. | invariant | pm.md:L71,L85 — ## No-spec-surface short-circuit | - | - | - | none | - | procedure |
| R-338 | Step 1: read every available spec source first — the Issue body (re-read via `bees show-ticket --ids <issue-id>` if the prompt's block is not byte-identical). | command | pm.md:L73-L74 — ## No-spec-surface short-circuit | - | Issue body | - | Bash, bees | mirrors analyst.md L25 | procedure |
| R-339 | Step 1: read the Plan Bee body if `up_dependencies` links one, using the Path B iteration. | command | pm.md:L75 — ## No-spec-surface short-circuit | - | `up_dependencies` | Path B | Bash, bees | - | procedure |
| R-340 | Step 1: read the PRD/SDD from `## Documentation Locations` (`Project requirements doc (PRD)`, `Internal architecture docs (SDD)`) only if the Issue body cites them; do not blanket-read on every Issue. | command | pm.md:L76 — ## No-spec-surface short-circuit | - | `Project requirements doc (PRD)`, `Internal architecture docs (SDD)` | - | Read | - | procedure |
| R-341 | Step 1: fetch upstream content via `WebFetch` if `reference_materials` is set, per the matching resolver branch. | command | pm.md:L77 — ## No-spec-surface short-circuit | - | `reference_materials` | - | WebFetch | - | procedure |
| R-342 | Step 2: substantive spec content describes behavior, constrains architecture, or specifies requirements beyond a title plus a what-to-fix sentence; judge collectively across sources. | definition | pm.md:L79 — ## No-spec-surface short-circuit | - | - | - | none | - | procedure |
| R-343 | "Rename `foo()` to `bar()` everywhere" is not substantive; "`auth_middleware` must reject expired tokens before any downstream handler" is substantive. | example | pm.md:L79 — ## No-spec-surface short-circuit | - | - | - | none | - | example |
| R-344 | Step 3: when substance is absent across ALL sources, exit with the one-line verdict `no spec drift surface to review for this Issue` and skip the deep review. | contract-heading | pm.md:L81 — ## No-spec-surface short-circuit | `no spec drift surface to review for this Issue` | - | - | none | - | procedure |
| R-345 | The short-circuit skips Scoped-marker resolution, in-flight review orchestration, cross-Task / cross-Epic checks, and the Final report contract; it is a first-class outcome, not "PM failed to find work". | definition | pm.md:L81 — ## No-spec-surface short-circuit | - | - | - | none | - | procedure |
| R-346 | Step 4: when substance is present in ANY source, proceed with the deep review against the substantive source(s). | command | pm.md:L83 — ## No-spec-surface short-circuit | - | - | - | none | - | procedure |
| R-347 | The PM MUST NOT key the short-circuit off shape signals (PRD path mentioned, Plan Bee linked, `reference_materials` non-null, body length). | invariant | pm.md:L85 — ## No-spec-surface short-circuit | - | - | - | none | - | procedure |
| R-348 | A content-blind pre-read gate reproduces the failure mode the always-dispatched pattern was designed to eliminate. | rationale-only | pm.md:L85 — ## No-spec-surface short-circuit | - | - | - | none | - | failure-narrative |
| R-349 | Skip-on-bees pre-branch: if the spec source came from a `bees` resolver, skip Scoped-marker resolution entirely — no temp file, no helper, no exit codes. | precondition | pm.md:L89 — ## Spec-source scoping | - | `resolver: bees` | - | none | - | procedure |
| R-350 | Path A, Path B, asymmetric error-handling, and helper-resolution apply only to the file-resolver and body-as-spec paths; the pre-branch relaxes nothing there. | definition | pm.md:L89 — ## Spec-source scoping | - | - | - | none | - | procedure |
| R-351 | A Scoped-marker line in a Plan Bee body (emitted by `/quo-plan-from-specs --feature "<title>"`) restricts spec-compare content to the matching `### Feature: <title>` subsection; otherwise the full doc applies. | definition | pm.md:L91 — ## Spec-source scoping | - | Plan Bee body marker | `/quo-plan-from-specs` | none | - | procedure |
| R-352 | Marker grammar is enforced by the Scoped-marker resolver helper the orchestrator runs — the source of truth; do not re-derive the parsing rules here. | invariant | pm.md:L91 — ## Spec-source scoping | - | - | scoped_marker_resolver.py | none | - | procedure |
| R-353 | Path A (`quo-execute`): the prompt names a Grandparent Bee; check its body for a marker; on helper exit 2 hard-fail the spec review. | command | pm.md:L95 — ## Spec-source scoping | - | Grandparent Bee body | `/quo-execute` | Bash, Write | - | procedure |
| R-354 | Path B (`quo-fix-issue`): no Grandparent Bee; iterate the Issue's `up_dependencies` for a `plans`-hive Bee; on helper exit 2 fall back to full-doc content. | command | pm.md:L96 — ## Spec-source scoping | - | `up_dependencies` | `/quo-fix-issue` | Bash, Write | - | procedure |
| R-355 | The error-handling asymmetry between Path A and Path B is intentional and load-bearing; future maintainers MUST NOT harmonize the two behaviors. | invariant | pm.md:L98,L204-L211 — ## Spec-source scoping / ### Asymmetric error-handling | - | - | - | none | - | procedure |
| R-356 | The dispatch prompt distinguishes the paths by context (Grandparent Bee ID for A; Issue ID + `up_dependencies` for B); run only the matching path. | command | pm.md:L100 — ## Spec-source scoping | - | dispatch prompt context | - | none | - | procedure |
| R-357 | The marker helper ships as `scoped_marker_resolver.py` with `quo-breakdown-epic`; subagents lack a sibling-skill resolution model, so resolution is two-tier. | definition | pm.md:L104 — ### Helper-resolution-path strategy | - | - | `quo-breakdown-epic` | none | - | procedure |
| R-358 | Primary: the orchestrator passes the helper's absolute path as a named placeholder (e.g. `<scoped-marker-resolver-path>`); use it verbatim in helper invocations. | command | pm.md:L106 — ### Helper-resolution-path strategy | - | `<scoped-marker-resolver-path>` | - | none | - | procedure |
| R-359 | Fallback: self-resolve at `~/.claude/skills/quo-breakdown-epic/scripts/scoped_marker_resolver.py`, then `<repo>/.claude/skills/quo-breakdown-epic/scripts/scoped_marker_resolver.py`. | command | pm.md:L107 — ### Helper-resolution-path strategy | - | - | - | Glob | - | procedure |
| R-360 | Emit a one-line warning in the PM's report whenever the helper-path fallback is used. | command | pm.md:L107 — ### Helper-resolution-path strategy | report warning line | - | - | none | - | procedure |
| R-361 | Path A precondition: run it when the dispatch prompt indicates `/quo-execute` — a Grandparent Bee exists and is named. | precondition | pm.md:L111 — ### Path A | - | dispatch prompt | `/quo-execute` | none | - | procedure |
| R-362 | Path A step 1: extract the `body` field (`tickets[0].body`) from `bees show-ticket --ids <grandparent-bee-id>` JSON. | command | pm.md:L113 — ### Path A | - | Grandparent Bee body | - | Bash, bees | - | procedure |
| R-363 | Do NOT dump the whole JSON envelope to the temp file — JSON escapes (`\n`) defeat the parser's line-by-line scan. | invariant | pm.md:L113,L169 — ### Path A / ### Path B | - | - | - | none | - | procedure |
| R-364 | Path A step 2: `Write` the body to `/tmp/.quorum/bees-bee-body-<short-suffix>.md` (POSIX) or `$env:TEMP\.quorum\bees-bee-body-<short-suffix>.md` (Windows), suffix = Bee ID short suffix or another collision-resistant token. | command | pm.md:L115 — ### Path A | scratch file | - | - | Write | - | procedure |
| R-365 | Create the `.quorum` subdir first: `mkdir -p /tmp/.quorum` / `New-Item -ItemType Directory -Force -Path "$env:TEMP\.quorum" \| Out-Null`. | command | pm.md:L115-L125,L169-L179 — ### Path A / ### Path B | scratch dir | - | - | Bash | mirrors test-writer.md L111-L121 | procedure |
| R-366 | Path A step 3: invoke `python3 "<scoped-marker-resolver-path>" "/tmp/.quorum/bees-bee-body-<short-suffix>.md"` (PowerShell: `python "<scoped-marker-resolver-path>" "$env:TEMP\.quorum\bees-bee-body-<short-suffix>.md"`). | command | pm.md:L127-L137 — ### Path A | - | - | scoped_marker_resolver.py | Bash | mirrors pm.md L181-L191 (Path B) | procedure |
| R-367 | Path A exit 0 with `"scoped": false`: no marker; proceed with the full resolved doc content. | command | pm.md:L140 — ### Path A | - | helper JSON | - | none | - | procedure |
| R-368 | Path A exit 0 with `"scoped": true`: the `docs` array carries scoped content per doc path; compare Task work against the scoped content only. | command | pm.md:L141 — ### Path A | - | helper JSON `docs` | - | none | - | procedure |
| R-369 | Path A exit 2: HARD-FAIL — surface the helper's stderr to the orchestrator and stop the spec review until the user resolves it. | invariant | pm.md:L142 — ### Path A | hard-fail report | helper stderr | - | none | - | procedure |
| R-370 | Path A: do NOT silently fall back to the full doc on exit 2 — the user opted into a scoped review by emitting the marker. | invariant | pm.md:L142,L208 — ### Path A / ### Asymmetric error-handling | - | - | - | none | - | procedure |
| R-371 | Do not remove the temp file after the helper exits; files under `<tempdir>/.quorum/` accumulate intentionally. | invariant | pm.md:L144,L193 — ### Path A / ### Path B | - | - | - | none | mirrors test-writer.md L124 | procedure |
| R-372 | Path B precondition: run it when the prompt indicates `/quo-fix-issue` — no Grandparent Bee; spec source is the Issue body and project docs; a Plan Bee in `up_dependencies` may carry scope. | precondition | pm.md:L148 — ### Path B | - | dispatch prompt | `/quo-fix-issue` | none | - | procedure |
| R-373 | `up_dependencies` has two roles: primary blocker validation, and secondary optional scope-context discovery of a `plans`-hive Plan Bee. | definition | pm.md:L150-L153 — ### Path B | - | `up_dependencies` | - | none | - | procedure |
| R-374 | Future maintainers MUST NOT silently break the `up_dependencies` dual-use (single-role schema, filtering out `plans`-hive entries). | invariant | pm.md:L155 — ### Path B | - | - | - | none | - | procedure |
| R-375 | After the orchestrator has validated blockers, iterate `up_dependencies` and for each `plans`-hive Bee attempt best-effort marker detection; missing/malformed/non-plans entries are not fatal. | command | pm.md:L157 — ### Path B | - | `up_dependencies` | - | Bash, bees | - | procedure |
| R-376 | Determine hive with `bees execute-freeform-query --query-yaml 'stages: - [id=<dep-id>, type=bee, hive=plans] report: [title, body]'`. | command | pm.md:L159-L165 — ### Path B | - | - | - | Bash, bees | - | procedure |
| R-377 | Zero rows for an ID means a non-`plans` entry — skip to the next; do NOT hard-fail on a non-Plan-Bee `up_dependencies` entry. | invariant | pm.md:L167 — ### Path B | - | - | - | none | - | procedure |
| R-378 | Path B step 2: extract `tickets[0].body`, `Write` it to `/tmp/.quorum/bees-bee-body-<short-suffix>.md` / `$env:TEMP\.quorum\bees-bee-body-<short-suffix>.md` with a suffix unique per Plan Bee. | command | pm.md:L169 — ### Path B | scratch file | Plan Bee body | - | Write | - | procedure |
| R-379 | Path B step 2: invoke the helper with the orchestrator-passed path (or documented fallback) exactly as in Path A. | command | pm.md:L181-L191 — ### Path B | - | - | scoped_marker_resolver.py | Bash | mirrors pm.md L127-L137 | procedure |
| R-380 | Path B exit 0 with `"scoped": true`: use per-doc scoped content from `docs` (Internal architecture docs, Customer-facing docs, PRD-equivalent) in place of full-doc content. | command | pm.md:L196 — ### Path B | - | helper JSON `docs` | - | none | - | procedure |
| R-381 | Path B exit 0 with `"scoped": false`: no marker; fall back to full-doc spec content. | command | pm.md:L197 — ### Path B | - | - | - | none | - | procedure |
| R-382 | Path B exit 2: BEST-EFFORT — surface the helper's stderr in the PM's report, then fall back to full-doc content; do not block the fix on a malformed marker. | command | pm.md:L198,L209 — ### Path B / ### Asymmetric error-handling | report note | helper stderr | - | none | - | procedure |
| R-383 | Tie-break on multiple valid markers: the first wins by `up_dependencies` order as returned by `bees show-ticket`; ignore the rest and mention the tie-break in the report. | command | pm.md:L200 — ### Path B | report note | - | - | none | - | procedure |
| R-384 | Do NOT discover a parent Plan Bee outside `up_dependencies` (title string-matching, enumerating recent Plans Bees, walking back-references). | invariant | pm.md:L202 — ### Path B | - | - | - | none | - | procedure |
| R-385 | Path A hard-fails because the user explicitly opted in via marker; Path B falls back because Issue authors do not opt in and discovery is opportunistic. | rationale-only | pm.md:L204-L211 — ### Asymmetric error-handling | - | - | - | none | - | rationale |
| R-386 | Verify the Test Writer and Doc Writer reviewed the Engineer's work via the Subtasks' status transitions and the diff, not via messaging handoff. | command | pm.md:L213 — ## Instructions | - | Subtask statuses, diff | test-writer.md, doc-writer.md | Bash, bees, git | - | procedure |
| R-387 | Review quality of Task and Subtask efforts; make the final decision on when to present the completed Task to the orchestrator. | command | pm.md:L214 — ## Instructions | - | - | - | none | - | procedure |
| R-388 | Ensure the work aligns with the spec source's requirements. | command | pm.md:L216 — ## Instructions | - | spec source | - | none | - | procedure |
| R-389 | Ensure the work does not introduce more functionality than asked for; e.g. backwards-compatibility scaffolding when the spec calls for no legacy support is unacceptable. | command | pm.md:L217 — ## Instructions | - | spec source | - | none | - | procedure |
| R-390 | Once all Tasks in the Parent Epic are complete, verify cumulative work meets the Epic's Acceptance Criteria and adds no functionality not required or explicitly disallowed. | command | pm.md:L218 — ## Instructions | - | Epic Acceptance Criteria | - | none | - | procedure |
| R-391 | Use `/quo-engineer-review` and `/quo-doc-writer-review` via `Skill` after the team's work; apply judgment about asking for improvements versus deferring. | command | pm.md:L219 — ## Instructions | - | - | `/quo-engineer-review`, `/quo-doc-writer-review` | Skill | - | procedure |
| R-392 | Relay `/quo-engineer-review`'s `### Second-order effects` body verbatim; it is emitted on every review including clean ones and is narrative, not routing input. | relay | pm.md:L220 — ## Instructions | `### Second-order effects` body | `/quo-engineer-review` output | `/quo-engineer-review` | Skill | mirrors code-reviewer.md L28 | procedure |
| R-393 | Do not summarize, fold into work-item triage, or drop the `### Second-order effects` body when it reads as clean. | invariant | pm.md:L220 — ## Instructions | - | - | - | none | mirrors code-reviewer.md L28 | procedure |
| R-394 | The relayed body reaches the summary for the PM's dispatch scope: `/quo-execute` per-Task summary, `## Bee Execution Complete` for a Bee-scoped PM, `/quo-fix-issue` per-issue summary — each with a `**Second-order effects**` field. | definition | pm.md:L220 — ## Instructions | - | - | `/quo-execute`, `/quo-fix-issue`, `## Bee Execution Complete` | none | - | procedure |
| R-395 | Do not re-emit the `### Second-order effects` heading line that came with the body; the Final report supplies one heading of its own. | invariant | pm.md:L220 — ## Instructions | - | - | - | none | mirrors code-reviewer.md L28 | procedure |
| R-396 | Relay the `[introduces-mechanism]` tag on every fix-path line that carries one, alongside its `[depth:<...>]` and any `[preferred]` token, tokens intact and in emitted order. | relay | pm.md:L221 — ## Instructions | fix-path lines | `/quo-engineer-review` output | `/quo-engineer-review` | none | mirrors code-reviewer.md L27 | procedure |
| R-397 | Do not add, remove, or re-judge the `[introduces-mechanism]` tag; it is the orchestrator's primary signal for routing to its scope-bounding gate. | invariant | pm.md:L221 — ## Instructions | - | - | - | none | mirrors code-reviewer.md L27 | procedure |
| R-398 | The `[introduces-mechanism]` relay survives hot-review triage: a surviving tagged path keeps its tag; a triaged-out tagged path is named as dropped in the deferred / ignored-feedback report. | invariant | pm.md:L221 — ## Instructions | ignored-feedback report | - | - | none | - | procedure |
| R-399 | In `/quo-execute` the per-Task review site has no Code Reviewer, so this PM is that site's only carrier for the tag. | definition | pm.md:L221 — ## Instructions | - | - | `/quo-execute`, code-reviewer.md | none | - | procedure |
| R-400 | When the Engineer's return carried completeness evidence, embed it verbatim in the `/quo-engineer-review` invocation under `## Engineer's completeness evidence`. | relay | pm.md:L222 — ## Instructions | `## Engineer's completeness evidence` | Engineer completeness evidence | `/quo-engineer-review`, engineer.md L59 | Skill | mirrors code-reviewer.md L24 | procedure |
| R-401 | Separately from the list, state in the invocation that the assignment was sweep-shaped whenever it was. | relay | pm.md:L222 — ## Instructions | sweep-shaped statement | dispatch assignment | `/quo-engineer-review` | Skill | mirrors code-reviewer.md L24 | procedure |
| R-402 | When no completeness list arrived, omit the `## Engineer's completeness evidence` heading rather than emitting an empty one — but still state sweep-shaped when true. | invariant | pm.md:L222 — ## Instructions | - | - | - | none | mirrors code-reviewer.md L24 | procedure |
| R-403 | When the dispatch prompt carried a `## Blast radius` block, forward it verbatim into the `/quo-engineer-review` invocation under that same heading. | relay | pm.md:L223 — ## Instructions | `## Blast radius` | `## Blast radius` | `/quo-engineer-review`, analyst.md | Skill | mirrors code-reviewer.md L25 | procedure |
| R-404 | Forward `## Blast radius` even when it carries only an explicit "nothing to verify" line; forward no such heading when the prompt carried none. | invariant | pm.md:L223 — ## Instructions | - | - | - | none | mirrors code-reviewer.md L25 | procedure |
| R-405 | Time budget: if one review invocation returns more than ~10 work items OR runs more than ~5 turns, stop iterating in that lane. | command | pm.md:L224 — ## Instructions | - | review output | `/quo-engineer-review`, `/quo-doc-writer-review` | none | - | procedure |
| R-406 | On a hot review, triage to blocker-severity items only (correctness bugs, spec violations, contract-key violations); defer suggestions / nits / style to ignored-feedback for the Task summary. | command | pm.md:L224 — ## Instructions | ignored-feedback list | - | - | none | - | procedure |
| R-407 | The ~10-item / ~5-turn thresholds are guidance, not a hard contract — firmer when thrashing on subjective feedback, looser when each item is high-signal. | definition | pm.md:L224 — ## Instructions | - | - | - | none | - | procedure |
| R-408 | Any decision to ignore `/quo-engineer-review` or `/quo-doc-writer-review` feedback MUST be included in the end-of-Task summary report. | invariant | pm.md:L225,L234 — ## Instructions / Final report contract | final report | - | - | none | - | procedure |
| R-409 | Trust the Task's `.T` subtask output; do NOT re-run the full workspace test suite by default — only with a specific reason (Engineer reported skipping something, stale `.bees/` state). | invariant | pm.md:L226 — ## Instructions | - | `.T` subtask | - | Bash | - | procedure |
| R-410 | Look up project commands from CLAUDE.md `## Build Commands` by exact contract name: `Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`; do not hardcode. | invariant | pm.md:L226 — ## Instructions | - | `## Build Commands` | - | Read | mirrors engineer.md L66, test-writer.md L128 | procedure |
| R-411 | Cross-Task check: read the other Tasks in this Epic; for each function/API this Task modifies, verify sibling Tasks' assumptions about it still hold. | command | pm.md:L228 — ## Instructions Cross-Task and cross-Epic | - | sibling Tasks | - | Bash, bees, Read | - | procedure |
| R-412 | Example: reordering steps inside `auth_middleware` must be cross-checked against a sibling's "by this point the request is signature-verified" docstring. | example | pm.md:L228 — ## Instructions Cross-Task and cross-Epic | - | - | - | none | - | example |
| R-413 | Cross-Epic check: if prior Epics landed interacting code, re-read the relevant diffs via `git log` / `git diff` and verify the interactions. | command | pm.md:L229 — ## Instructions Cross-Task and cross-Epic | - | branch history | - | Bash, git | - | procedure |
| R-414 | Cumulative resource accounting: if this Task adds acquires from a bounded resource, sum across all call sites including sibling Tasks/Epics and flag lifetime mismatches or starvation. | command | pm.md:L230 — ## Instructions Cross-Task and cross-Epic | - | - | - | Grep | - | procedure |
| R-415 | Example: a new long-lived consumer sharing a pool with short-lived transaction writers starves writers at steady state. | example | pm.md:L230 — ## Instructions Cross-Task and cross-Epic | - | - | - | none | - | example |
| R-416 | Symmetric lifecycle coverage: if this Task introduces a new resource, grep every cleanup/teardown path for the adjacent resource class and verify symmetric handling. | command | pm.md:L231 — ## Instructions Cross-Task and cross-Epic | - | - | - | Grep | - | procedure |
| R-417 | Example: a new `cache:user:{id}:permissions` key class requires the invalidation, user-deletion, and periodic-purge paths to all DELETE it. | example | pm.md:L231 — ## Instructions Cross-Task and cross-Epic | - | - | - | none | - | example |
| R-418 | New-pattern-exposes-old-code: if this Task adds a new call pattern for an unchanged function, mentally run it under that pattern and flag latent broken assumptions. | command | pm.md:L232 — ## Instructions Cross-Task and cross-Epic | - | - | - | Read | - | procedure |
| R-419 | Example: `get_user_profile(id)` called for hundreds of IDs in a batch loop may miss per-request memoization reset and leak stale data. | example | pm.md:L232 — ## Instructions Cross-Task and cross-Epic | - | - | - | none | - | example |
| R-420 | The final report MUST include any ignored reviewer feedback. | contract-heading | pm.md:L234 — Final report contract | final report | - | - | none | - | procedure |
| R-421 | The final report MUST include any contentious topics between team members. | contract-heading | pm.md:L235 — Final report contract | final report | - | - | none | - | procedure |
| R-422 | The final report MUST include any design decisions that conflicted with work described in tickets. | contract-heading | pm.md:L236 — Final report contract | final report | - | - | none | - | procedure |
| R-423 | The final report MUST include any incomplete work. | contract-heading | pm.md:L237 — Final report contract | final report | - | - | none | - | procedure |
| R-424 | The final report MUST include any cross-Task / cross-Epic interaction issues discovered and their resolution. | contract-heading | pm.md:L238 — Final report contract | final report | - | - | none | - | procedure |
| R-425 | The final report MUST include any accepted `blocker`, or improperly deferred `blocker`, the run's compromise tracker records. | contract-heading | pm.md:L239 — Final report contract | final report | compromise tracker | - | Read | - | procedure |
| R-426 | When the dispatch prompt supplies a compromise-tracker path, read that file and check every entry. | command | pm.md:L239 — Final report contract | - | compromise-tracker path | - | Read | - | procedure |
| R-427 | An entry whose `Finding (verbatim)` carries `blocker` is a violation when its `Decision` is `User picked Accept the limitation` — a blocker can never be accepted. | definition | pm.md:L239 — Final report contract | - | tracker `Finding (verbatim)`, `Decision` | - | none | - | procedure |
| R-428 | A `blocker` entry with `Decision` `User picked Defer to follow-up Issue` is a violation when its `Rationale` carries no narrowing record. | definition | pm.md:L239 — Final report contract | - | tracker `Rationale` | - | none | - | procedure |
| R-429 | A blocker deferral with a narrowing record is legitimate only when the narrowing still covers the unit's stated defect; report the entry when it left the defect partly unfixed. | invariant | pm.md:L239 — Final report contract | - | - | - | none | - | procedure |
| R-430 | A blocker deferral whose narrowing holds is legitimate; do not report it. | invariant | pm.md:L239 — Final report contract | - | - | - | none | - | procedure |
| R-431 | When the diff under review has resolved a deferred blocker, name the entry in one line as already resolved rather than re-emitting it as a `blocker` finding. | command | pm.md:L239 — Final report contract | one-line note | - | - | none | - | procedure |
| R-432 | Leave the tracker entry itself alone; this check reads it, it does not amend it. | invariant | pm.md:L239 — Final report contract | - | - | - | none | - | procedure |
| R-433 | Split violating entries by whose diff they concern: entries about this PM's diff are in scope and actionable; others belong to earlier units. | command | pm.md:L239 — Final report contract | - | - | - | none | - | procedure |
| R-434 | Report each in-scope violating entry as a `blocker`-severity finding quoting the entry's finding and decision, with at least one lettered fix-path line carrying a depth tag. | command | pm.md:L239 — Final report contract | `blocker` finding with fix path | - | - | none | - | procedure |
| R-435 | Example fix-path line: `(a) [depth:trivial-tweak] Re-open the deferred blocker and address it in this scope`. | example | pm.md:L239 — Final report contract | - | - | - | none | - | example |
| R-436 | Name an earlier unit's violating entry in one line as a pre-existing violation, with no fix-path line; do not re-emit it as this unit's `blocker` finding. | command | pm.md:L239 — Final report contract | one-line note | - | - | none | - | procedure |
| R-437 | That one line is a report note, not a finding — no severity tag, no fix-path line — so the orchestrator must not route it through its routing-review-findings discipline or untagged-emission shim. | invariant | pm.md:L239 — Final report contract | - | - | orchestrator routing-review-findings discipline | none | - | procedure |
| R-438 | The post-completion review's PHASE 2 check is the run-level backstop that surfaces an earlier unit's violation once over the whole run's diff. | definition | pm.md:L239 — Final report contract | - | - | post-completion review PHASE 2 | none | - | procedure |
| R-439 | When the prompt supplies no tracker path, or the file does not exist, skip the tracker check and say so in one line rather than treating it as clean. | command | pm.md:L239 — Final report contract | one-line note | - | - | none | - | procedure |
| R-440 | The final report carries exactly one `### Second-order effects` section, however many in-flight `/quo-engineer-review` invocations were made. | contract-heading | pm.md:L240 — Final report contract | `### Second-order effects` | - | `/quo-engineer-review` | none | - | procedure |
| R-441 | Under `### Second-order effects`, emit one `#### <invocation scope>` sub-block per invocation (e.g. `#### t3.abc.def.gh`, `#### round 2 — post-redaction diff`) followed by that invocation's relayed body verbatim. | contract-heading | pm.md:L240 — Final report contract | `#### <invocation scope>` | - | - | none | - | procedure |
| R-442 | Do not nest a second `### Second-order effects` heading inside a sub-block. | invariant | pm.md:L240 — Final report contract | - | - | - | none | - | procedure |
| R-443 | When exactly one invocation was made, the single sub-block still gets its `#### <invocation scope>` label. | invariant | pm.md:L240 — Final report contract | - | - | - | none | - | procedure |
| R-444 | When no `/quo-engineer-review` invocation was made this pass, say so under the `### Second-order effects` heading rather than omitting it. | invariant | pm.md:L240 — Final report contract | `### Second-order effects` | - | - | none | - | procedure |
| R-445 | Every item the PM recommends deferring MUST name a destination so the orchestrator can encode it in a durable inter-session carrier. | invariant | pm.md:L241 — Deferred-item destination contract | deferred items | - | - | none | mirrors analyst.md L93 | procedure |
| R-446 | The token `Task` in destination labels is generic for the unit under review — a Subtask/Task (`pm-<task-id>`) in `/quo-execute` or an Issue (`pm-<issue-id>`, Section 5) in `/quo-fix-issue`. | definition | pm.md:L241 — Deferred-item destination contract | - | - | `/quo-fix-issue` Section 5 | none | - | procedure |
| R-447 | Destination `addressed-now-in-this-Task`: the item should be (or was) addressed during this Task; no inter-session carrier is needed. | definition | pm.md:L242 — Deferred-item destination contract | - | - | - | none | mirrors analyst.md L94 | procedure |
| R-448 | `addressed-now-in-this-Task` is the only permitted destination for a `blocker`-severity item; a blocker is never annotated `defer-to-existing-ticket-body` or `defer-to-new-Issue`. | invariant | pm.md:L242 — Deferred-item destination contract | - | - | - | none | - | procedure |
| R-449 | Deferring a blocker belongs to the orchestrator's routing gates, which pair it with a dispatched narrowing and a tracker entry; the PM's `defer-*` ledger carries no such pairing. | rationale-only | pm.md:L242 — Deferred-item destination contract | - | - | orchestrator routing gates | none | - | rationale |
| R-450 | When an item the PM would defer is a blocker, annotate it `addressed-now-in-this-Task` and say plainly it is not the PM's to shelve — the gates decide. | command | pm.md:L242 — Deferred-item destination contract | blocker finding | - | - | none | - | procedure |
| R-451 | Destination `defer-to-existing-ticket-body: <ticket-id>`: name the specific ticket id; for PRD/SDD name the `## Documentation Locations` contract key. | definition | pm.md:L243 — Deferred-item destination contract | - | `Project requirements doc (PRD)` | - | none | mirrors analyst.md L95 | procedure |
| R-452 | Destination `defer-to-new-Issue`: a cross-cutting follow-up the orchestrator files via `/quo-file-issue` at the pre-handoff deferral-hygiene gate. | definition | pm.md:L244 — Deferred-item destination contract | - | - | `/quo-file-issue` | none | mirrors analyst.md L96 | procedure |
| R-453 | Vague "defer to next phase" / "address later" / "handle during execution" / "pick up during /quo-breakdown-epic" framings without a destination are forbidden. | invariant | pm.md:L246 — Deferred-item destination contract | - | - | `/quo-breakdown-epic` | none | mirrors analyst.md L98 | procedure |
| R-454 | The orchestrator creates/closes the `defer-<short-suffix>` TaskList ledger task from the destination annotation; an item without one is flagged back to the PM as a contract violation. | definition | pm.md:L246 — Deferred-item destination contract | - | - | phase skill's pre-handoff gate | none | - | procedure |
| R-455 | Shell-command etiquette: one literal command per Bash call; no diagnostic tails; no `$VAR` / `$(...)` / backticks / redirects / compound chains; multi-step via `Write`-to-file; prefer `Read` / `Grep` / `Glob` / separate `Bash` calls. | invariant | pm.md:L248 — ## Instructions Shell-command etiquette | - | - | - | Bash, Write, Read, Grep, Glob | mirrors engineer.md L68, test-writer.md L132, analyst.md L125 | procedure |
| R-456 | Frontmatter `name` is `code-reviewer`, `model: opus`, `effort: xhigh`. | definition | code-reviewer.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-457 | Tool allowlist is exactly `[Bash, Read, Grep, Glob, Skill]`. | tool-allowlist | code-reviewer.md:L6 — frontmatter | - | - | - | Bash, Read, Grep, Glob, Skill | mirrors test-reviewer.md L6, doc-reviewer.md L6 | procedure |
| R-458 | Code Reviewer performs a fresh-eyes review of the Engineer's work via `/quo-engineer-review`, returning structured findings to the orchestrator. | definition | code-reviewer.md:L3,L9 — description / intro | findings | dispatch-prompt scope | `/quo-engineer-review` | Skill | - | procedure |
| R-459 | Code Reviewer does NOT review tests or documentation — owned by test-reviewer and doc-reviewer; no source, tests, or docs are modified. | invariant | code-reviewer.md:L3,L9 — description / intro | - | - | test-reviewer.md, doc-reviewer.md | none | - | procedure |
| R-460 | Code Reviewer always runs cold: a fresh-eyes gate that must not assume context from prior invocations; single-shot; no warm-state, resume, or per-Task reuse. | invariant | code-reviewer.md:L3,L13 — description / ## Cold-start invariant | - | - | - | none | mirrors test-reviewer.md L13, doc-reviewer.md L13 | procedure |
| R-461 | Review the source-code output of the Engineer and provide feedback where it was not up to standards. | command | code-reviewer.md:L17-L18 — ## Responsibilities | findings | Engineer's diff | engineer.md | none | - | procedure |
| R-462 | Read the scope (a diff range, a ticket ID, or both) from the orchestrator's dispatch prompt; do not compute scope on your own. | command | code-reviewer.md:L22 — ## Instructions | - | dispatch-prompt scope | - | none | mirrors test-reviewer.md L22, doc-reviewer.md L22 | procedure |
| R-463 | Invoke `/quo-engineer-review` via the `Skill` tool against that scope; the wrapped skill carries the criteria, exclusions, and selectivity rules — this wrapper does not redefine them. | command | code-reviewer.md:L23 — ## Instructions | - | - | `/quo-engineer-review` | Skill | mirrors test-reviewer.md L23, doc-reviewer.md L23 | procedure |
| R-464 | When the dispatch prompt carried `## Engineer's completeness evidence`, embed it verbatim in the `/quo-engineer-review` invocation under that same heading. | relay | code-reviewer.md:L24 — ## Instructions | `## Engineer's completeness evidence` | `## Engineer's completeness evidence` | `/quo-engineer-review`, engineer.md | Skill | mirrors pm.md L222 | procedure |
| R-465 | Separately state in the invocation that the assignment was sweep-shaped whenever the dispatch prompt said it was. | relay | code-reviewer.md:L24 — ## Instructions | sweep-shaped statement | dispatch-prompt sweep-shaped statement | `/quo-engineer-review` | Skill | mirrors pm.md L222 | procedure |
| R-466 | When the prompt carried no completeness block, forward no heading rather than an empty one — but still forward the sweep-shaped statement when the prompt made it. | invariant | code-reviewer.md:L24 — ## Instructions | - | - | - | none | mirrors pm.md L222 | procedure |
| R-467 | When the dispatch prompt carried a `## Blast radius` block, forward it verbatim into the `/quo-engineer-review` invocation under that same heading — one heading string at every hop. | relay | code-reviewer.md:L25 — ## Instructions | `## Blast radius` | `## Blast radius` | `/quo-engineer-review`, analyst.md | Skill | mirrors pm.md L223 | procedure |
| R-468 | Forward `## Blast radius` even when it carries only an explicit "nothing to verify" line; forward no such heading when the prompt carried none. | invariant | code-reviewer.md:L25 — ## Instructions | - | - | - | none | mirrors pm.md L223 | procedure |
| R-469 | Return findings as a structured list per the wrapped skill's contract: severity tags (`blocker` / `suggestion` / `nit`), per-fix-path depth tags (`trivial-tweak` / `refactor-locally` / `re-architect`), fix paths, file:line refs, suggested fixes, and a verdict. | contract-heading | code-reviewer.md:L26 — ## Instructions | findings list, verdict | `/quo-engineer-review` output | `/quo-engineer-review` | none | mirrors test-reviewer.md L24, doc-reviewer.md L24 | procedure |
| R-470 | Do not redefine the output shape — defer to whatever `/quo-engineer-review` produces. | invariant | code-reviewer.md:L26 — ## Instructions | - | - | `/quo-engineer-review` | none | mirrors test-reviewer.md L24, doc-reviewer.md L24 | procedure |
| R-471 | Relay the `[introduces-mechanism]` tag (per the mechanism definition in `agents/analyst.md`) on every fix-path line, alongside `[depth:<...>]` and any `[preferred]`, tokens intact and in emitted order. | relay | code-reviewer.md:L27 — ## Instructions | fix-path lines | `/quo-engineer-review` output | `agents/analyst.md`, `/quo-engineer-review` | none | mirrors pm.md L221 | procedure |
| R-472 | Do not add, remove, or re-judge the `[introduces-mechanism]` tag; a dropped tag silently downgrades the orchestrator's scope-bounding-gate routing. | invariant | code-reviewer.md:L27 — ## Instructions | - | - | - | none | mirrors pm.md L221 | procedure |
| R-473 | Relay the `### Second-order effects` body verbatim alongside the findings list in the same return; do not re-emit the heading line. | relay | code-reviewer.md:L28 — ## Instructions | `### Second-order effects` body | `/quo-engineer-review` output | `/quo-engineer-review` | none | mirrors pm.md L220 | procedure |
| R-474 | Do not summarize, fold into the numbered findings, or drop the `### Second-order effects` body when it reads as clean. | invariant | code-reviewer.md:L28 — ## Instructions | - | - | - | none | mirrors pm.md L220 | procedure |
| R-475 | In `/quo-execute` the Code Reviewer is dispatched only at the Bee-level review, whose destination is the `**Second-order effects**` field of `## Bee Execution Complete` (Section 9). | definition | code-reviewer.md:L28 — ## Instructions | - | - | `/quo-execute` Section 9, `## Bee Execution Complete` | none | - | procedure |
| R-476 | In `/quo-fix-issue` the Code Reviewer is dispatched per Issue; its `### Second-order effects` destination is the same field in the per-issue summary (Section 7). | definition | code-reviewer.md:L28 — ## Instructions | - | - | `/quo-fix-issue` Section 7 | none | - | procedure |
| R-477 | Frontmatter `name` is `test-reviewer`, `model: opus`, `effort: high`. | definition | test-reviewer.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-478 | Tool allowlist is exactly `[Bash, Read, Grep, Glob, Skill]`. | tool-allowlist | test-reviewer.md:L6 — frontmatter | - | - | - | Bash, Read, Grep, Glob, Skill | mirrors code-reviewer.md L6, doc-reviewer.md L6 | procedure |
| R-479 | Test Reviewer performs a fresh-eyes review of the Test Writer's work via `/quo-test-writer-review`, returning structured findings to the orchestrator. | definition | test-reviewer.md:L3,L9 — description / intro | findings | dispatch-prompt scope | `/quo-test-writer-review` | Skill | - | procedure |
| R-480 | Test Reviewer does NOT review source code or documentation — owned by code-reviewer and doc-reviewer; nothing is modified. | invariant | test-reviewer.md:L3,L9 — description / intro | - | - | code-reviewer.md, doc-reviewer.md | none | - | procedure |
| R-481 | Test Reviewer always runs cold; single-shot; no warm-state, resume, or per-Task reuse. | invariant | test-reviewer.md:L3,L13 — description / ## Cold-start invariant | - | - | - | none | mirrors code-reviewer.md L13, doc-reviewer.md L13 | procedure |
| R-482 | Review the test-suite output of the Test Writer and provide feedback where it was not up to standards. | command | test-reviewer.md:L17-L18 — ## Responsibilities | findings | Test Writer's diff | test-writer.md | none | - | procedure |
| R-483 | Read the scope (a diff range, a ticket ID, or both) from the dispatch prompt; do not compute scope on your own. | command | test-reviewer.md:L22 — ## Instructions | - | dispatch-prompt scope | - | none | mirrors code-reviewer.md L22, doc-reviewer.md L22 | procedure |
| R-484 | Invoke `/quo-test-writer-review` via `Skill` against that scope; the wrapped skill carries the criteria — this wrapper does not redefine them. | command | test-reviewer.md:L23 — ## Instructions | - | - | `/quo-test-writer-review` | Skill | mirrors code-reviewer.md L23, doc-reviewer.md L23 | procedure |
| R-485 | Return findings per the wrapped skill's contract: `blocker` / `suggestion` / `nit`, `trivial-tweak` / `refactor-locally` / `re-architect`, fix paths, file:line refs, suggested fixes, verdict; do not redefine the shape. | contract-heading | test-reviewer.md:L24 — ## Instructions | findings list, verdict | `/quo-test-writer-review` output | `/quo-test-writer-review` | none | mirrors code-reviewer.md L26, doc-reviewer.md L24 | procedure |
| R-486 | Frontmatter `name` is `doc-reviewer`, `model: opus`, `effort: high`. | definition | doc-reviewer.md:L2-L5 — frontmatter | - | - | - | none | - | procedure |
| R-487 | Tool allowlist is exactly `[Bash, Read, Grep, Glob, Skill]`. | tool-allowlist | doc-reviewer.md:L6 — frontmatter | - | - | - | Bash, Read, Grep, Glob, Skill | mirrors code-reviewer.md L6, test-reviewer.md L6 | procedure |
| R-488 | Doc Reviewer performs a fresh-eyes review of the Doc Writer's work via `/quo-doc-writer-review`, returning structured findings to the orchestrator. | definition | doc-reviewer.md:L3,L9 — description / intro | findings | dispatch-prompt scope | `/quo-doc-writer-review` | Skill | - | procedure |
| R-489 | Doc Reviewer does NOT review source code or tests — owned by code-reviewer and test-reviewer; nothing is modified. | invariant | doc-reviewer.md:L3,L9 — description / intro | - | - | code-reviewer.md, test-reviewer.md | none | - | procedure |
| R-490 | Doc Reviewer always runs cold; single-shot; no warm-state, resume, or per-Task reuse. | invariant | doc-reviewer.md:L3,L13 — description / ## Cold-start invariant | - | - | - | none | mirrors code-reviewer.md L13, test-reviewer.md L13 | procedure |
| R-491 | Review the documentation output of the Doc Writer and provide feedback where it was not up to standards. | command | doc-reviewer.md:L17-L18 — ## Responsibilities | findings | Doc Writer's diff | doc-writer.md | none | - | procedure |
| R-492 | Read the scope (a diff range, a ticket ID, or both) from the dispatch prompt; do not compute scope on your own. | command | doc-reviewer.md:L22 — ## Instructions | - | dispatch-prompt scope | - | none | mirrors code-reviewer.md L22, test-reviewer.md L22 | procedure |
| R-493 | Invoke `/quo-doc-writer-review` via `Skill` against that scope; the wrapped skill carries the criteria — this wrapper does not redefine them. | command | doc-reviewer.md:L23 — ## Instructions | - | - | `/quo-doc-writer-review` | Skill | mirrors code-reviewer.md L23, test-reviewer.md L23 | procedure |
| R-494 | Return findings per the wrapped skill's contract: `blocker` / `suggestion` / `nit`, `trivial-tweak` / `refactor-locally` / `re-architect`, fix paths, file:line refs, suggested fixes, verdict; do not redefine the shape. | contract-heading | doc-reviewer.md:L24 — ## Instructions | findings list, verdict | `/quo-doc-writer-review` output | `/quo-doc-writer-review` | none | mirrors code-reviewer.md L26, test-reviewer.md L24 | procedure |

## Contract surface

Every heading string, trailer, tag, or fixed line that crosses the orchestrator↔role boundary (or role↔wrapped-skill boundary) in either direction. "Emits" is the side that writes it; "Consumes" is the side that keys on it.

### Role → orchestrator (role emits, orchestrator consumes)

| Surface string | Emits | Consumes | Source rows |
|---|---|---|---|
| `## Design Proposal for <issue-id>` | analyst | `/quo-fix-issue` (surfaces body to user) | R-039 |
| `### Problem` | analyst | `/quo-fix-issue` (prose to user) | R-040 |
| `### Root cause` | analyst | `/quo-fix-issue` (prose to user) | R-041 |
| `### Recommended approach` | analyst | `/quo-fix-issue` → relayed to engineer as `## Authoritative design directive` | R-042, R-126 |
| `### Blast radius` (unconditional; fixed line `No invariant added, removed, or weakened.`) | analyst | `/quo-fix-issue` → relayed as `## Blast radius` to engineer, code-reviewer, pm | R-043–R-047, R-126, R-165, R-403, R-467 |
| `### Policy decisions this change implies` (unconditional; fixed line `None — the recommendation leaves no policy question open.`) | analyst | `/quo-fix-issue` (approval gate) → relayed inside directive to engineer | R-068–R-071, R-126 |
| `### Why` | analyst | `/quo-fix-issue` (prose to user; open questions on `escalate-to-user`) | R-076, R-100 |
| `### Alternatives considered` | analyst | `/quo-fix-issue` (prose to user) | R-077 |
| `### Options the body did not consider` | analyst | `/quo-fix-issue` (prose to user) | R-079 |
| `### Upstream-fetch status` (conditional on `reference_materials`) | analyst | `/quo-fix-issue` (proceed / escalate decision) | R-083 |
| `Analyst verdict: <recommend-as-stated \| recommend-with-refinements \| recommend-different-approach \| escalate-to-user>` | analyst | `/quo-fix-issue` (preamble framing; routing still via `AskUserQuestion` Approve / Revise / Cancel) | R-084, R-094–R-100 |
| `### Deferred refinements` (`None` or bullets with `addressed-now-in-this-Issue` / `defer-to-existing-ticket-body: <ticket-id>` / `defer-to-new-Issue`) | analyst | `/quo-fix-issue` (creates `defer-<short-suffix>` TaskList tasks at Approve) | R-085–R-093 |
| `## Design question` (literal) | engineer | `/quo-fix-issue` dispatcher (routes to Analyst re-dispatch; any other heading = completion) | R-148–R-151 |
| `## Files changed` (recommended label; required content) | engineer | orchestrator → test-writer as `## Source paths to fingerprint` | R-156–R-158 |
| Sweep-completeness evidence (patterns verbatim, every hit, per-hit disposition) | engineer | orchestrator → pm / code-reviewer as `## Engineer's completeness evidence` | R-160–R-172 |
| Subtask `status=in_progress` / `status=done` (bees, execute mode) | engineer, test-writer (doc-writer via orchestrator) | orchestrator (handoff gate; premature-flip detection) | R-138–R-143, R-195–R-199, R-256 |
| Partial-return statement (what landed / what did not; Subtask left `in_progress`) | engineer, test-writer | orchestrator | R-151, R-152, R-199, R-226 |
| `## Perturbations` (paths, or `None`) | test-writer | `/quo-execute`, `/quo-fix-issue` (attribute concurrent-writer movement) | R-235–R-237 |
| Movement report (files moved, opening/closing HEAD, progress) | test-writer, doc-writer | orchestrator movement-report receiver | R-199, R-221, R-226, R-227, R-263 |
| "content-hash reading not taken because the path set was empty" note | test-writer | orchestrator | R-213 |
| Missing Plan Bee linkage report / setup-gap report | doc-writer | orchestrator | R-297, R-305 |
| Divergence note (diff vs Task body category) | doc-writer | orchestrator | R-279 |
| `### Feature: <title>` under `## Per-feature scope` (PRD) / `## Per-feature design` (SDD) | doc-writer (file edit) | downstream doc-writer passes (idempotency), `/quo-plan-from-specs --feature`, scoped_marker_resolver.py | R-287–R-303 |
| `no spec drift surface to review for this Issue` (one-line verdict) | pm | `/quo-fix-issue` per-issue summary | R-344 |
| PM final report: ignored feedback / contentious topics / conflicting design decisions / incomplete work / cross-Task-Epic issues / tracker blockers | pm | orchestrating execution skill | R-420–R-439 |
| `### Second-order effects` (exactly one) with `#### <invocation scope>` sub-blocks | pm | orchestrator `**Second-order effects**` field (per-Task summary / `## Bee Execution Complete` / per-issue summary) | R-440–R-444 |
| `### Second-order effects` body (no heading line) | code-reviewer | orchestrator `**Second-order effects**` field (`## Bee Execution Complete` Section 9 / per-issue summary Section 7) | R-473–R-476 |
| Fix-path lines with `[depth:<...>]`, `[preferred]`, `[introduces-mechanism]` | pm, code-reviewer (relayed from `/quo-engineer-review`) | orchestrator scope-bounding gate | R-396–R-398, R-471–R-472 |
| Findings list: `blocker` / `suggestion` / `nit`; `trivial-tweak` / `refactor-locally` / `re-architect`; file:line; suggested fixes; verdict | code-reviewer, test-reviewer, doc-reviewer, pm (tracker blockers) | orchestrator routing-review-findings discipline | R-434, R-469, R-485, R-494 |
| `blocker` finding with lettered fix path `(a) [depth:...]` for in-scope tracker violation | pm | orchestrator routing | R-434–R-435 |
| One-line report note (no severity, no fix path) for earlier-unit tracker violation / resolved blocker / skipped tracker check | pm | orchestrator (must NOT route; untagged-emission shim excluded) | R-431, R-436–R-439 |
| Deferred items with `addressed-now-in-this-Task` / `defer-to-existing-ticket-body: <ticket-id>` / `defer-to-new-Issue` | pm | orchestrator `defer-<short-suffix>` TaskList ledger at pre-handoff deferral-hygiene gate | R-445–R-454 |
| Helper-fallback warning line; tie-break note; helper stderr surfaced | pm | orchestrator / user | R-360, R-369, R-382, R-383 |
| Design question (team proposes alternatives) | pm | orchestrator (user input) | R-315 |

### Orchestrator → role (orchestrator emits in dispatch prompt, role consumes)

| Surface string | Emits | Consumes | Source rows |
|---|---|---|---|
| Issue ID + embedded verbatim Issue body | `/quo-fix-issue` | analyst, engineer, pm | R-019, R-123, R-338 |
| User revision feedback (on `Revise`) / Engineer's design question + partial-implementation notice (mid-implementation re-dispatch) | `/quo-fix-issue` | analyst | R-005, R-010, R-018, R-110 |
| `## Authoritative design directive` (fix mode, MAY) | `/quo-fix-issue` | engineer | R-126–R-131, R-163 |
| `## Blast radius` (relayed upstream site list, possibly the fixed "no sites" line) | `/quo-fix-issue` / `/quo-execute` | engineer (reconcile), pm and code-reviewer (forward) | R-165–R-172, R-403–R-404, R-467–R-468 |
| Subtask / Issue ticket ID (execute: Subtask with Context, What Needs to Change, Key Files, Acceptance Criteria) | both orchestrators | engineer, test-writer, doc-writer, pm | R-123, R-277 |
| `## Source paths to fingerprint` | both orchestrators | test-writer | R-204, R-209–R-210 |
| Bee-scoped dispatch (no Subtask id) | `/quo-execute` | test-writer, doc-writer, pm | R-205, R-227, R-261, R-394 |
| Plan Bee `title` (in prompt or via ticket file on disk), or pre-resolved fallback title | both orchestrators | doc-writer | R-294–R-298 |
| Grandparent Bee ID (Path A) / Issue ID + `up_dependencies` (Path B) | `/quo-execute` / `/quo-fix-issue` | pm | R-324, R-356, R-361, R-372 |
| `<scoped-marker-resolver-path>` placeholder | both orchestrators | pm | R-358 |
| Compromise-tracker path | both orchestrators | pm | R-425–R-426, R-439 |
| `## Engineer's completeness evidence` + sweep-shaped statement | both orchestrators | code-reviewer (forward); pm derives from Engineer return | R-400–R-402, R-464–R-466 |
| Review scope (diff range, ticket ID, or both) | both orchestrators | code-reviewer, test-reviewer, doc-reviewer | R-462, R-483, R-492 |
| Source-tree-stability promise (mode-scoped; never "frozen") | both orchestrators | test-writer, doc-writer | R-201–R-206, R-258–R-262 |

### Role → wrapped skill (role emits into `Skill` invocation, skill consumes)

| Surface string | Emits | Consumes | Source rows |
|---|---|---|---|
| `## Engineer's completeness evidence` + sweep-shaped statement | pm, code-reviewer | `/quo-engineer-review` | R-400–R-402, R-464–R-466 |
| `## Blast radius` | pm, code-reviewer | `/quo-engineer-review` | R-403–R-404, R-467–R-468 |
| Review scope | code-reviewer, test-reviewer, doc-reviewer, pm | `/quo-engineer-review`, `/quo-test-writer-review`, `/quo-doc-writer-review` | R-463, R-484, R-493, R-391 |

### Role → external artifact (file contracts)

| Surface | Emits | Consumes | Source rows |
|---|---|---|---|
| `/tmp/.quorum/bees-bee-body-<short-suffix>.md` (Plan/Grandparent Bee body, markdown not JSON) | pm | `scoped_marker_resolver.py` (exit 0 `"scoped": true/false` + `docs`; exit 2 + stderr) | R-362–R-371, R-378–R-382 |
| `/tmp/.quorum/test-writer-<short-suffix>-<original-filename>` (scratch source copy) | test-writer | test-writer (restore + verify) | R-229–R-234 |
| CLAUDE.md `## Documentation Locations` keys: `Project requirements doc (PRD)`, `Internal architecture docs (SDD)`, `Customer-facing docs`, `Engineering best practices`, `Test writing guide`, `Test review guide`, `Doc writing guide` | `/quo-setup` (target repo) | analyst, engineer, test-writer, doc-writer, pm | R-024, R-029, R-090, R-132, R-134, R-192–R-193, R-253–R-254, R-288–R-292, R-304–R-305, R-340, R-451 |
| CLAUDE.md `## Build Commands` keys: `Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test` | `/quo-setup` (target repo) | engineer, test-writer, pm | R-173–R-179, R-238–R-239, R-409–R-410 |
| Spec Bee `t1=Doc` children with `title` exactly `PRD` / `SDD` | `/quo-plan`, `/quo-write-prd`, `/quo-write-sdd` | pm | R-327–R-330 |

Total distinct boundary surfaces enumerated: 56 (34 role→orchestrator, 14 orchestrator→role, 3 role→wrapped-skill, 5 role→external-artifact).

## Rationale-only spans

Line ranges (or sentence-level sub-spans) containing explanation, motivation, or failure narrative but no imperative or fixed value. Each has a 5-word gist.

### analyst.md
- L11-L17 (`## Why this role exists`, entire section) — body quality varies; serial discovery.
- L60 last sentence ("a section that appears only when…") — conditional sections become unreliable.
- L62 second sentence ("A pattern that matches only…") — identifier-only patterns miss consumers.
- L66 sentence "So widening or narrowing the list stays a one-place edit…" — single-home prevents definition drift.
- L98 first sentence tail ("they are the failure mode this contract closes") — vague deferrals are the failure.
- L112 last sentence ("Cheap convergence is what the trivial case promises…") — swept-nothing vs never-swept distinguishable.

### engineer.md
- L20 sentence "The directive is the Analyst's codebase-grounded recommendation… follow the codebase-grounded recommendation." — directive beats unresearched author fix.
- L55 sentences "The status ladder above is the handoff signal… detectable when the dispatcher confirms the ticket transition." and "Choosing a mechanism yourself is the failure mode… a round later." — partial-done misleads; mechanisms need decisions.
- L57 last sentence ("An omitted list forces the orchestrator…") — omitted list sweeps sibling edits.
- L59 last sentence ("A sweep declared complete without this evidence…") — round-per-site versus one gap.
- L61 last clause ("a pattern narrower than the property… looks complete and is not") — narrow patterns fake completeness.
- L63 clauses "because the upstream list is relayed on dispatches of every shape…" and "because handing it off leaves it in no lane at all" — unreconciled entries read as misses.

### test-writer.md
- L58 clause "execute mode's forward per-Subtask fan-out is dependency-ordered by design, not serialized" — concurrency is designed, not faulty.
- L92 first sentence through "no matter how large the files are" — hash-object properties explained.
- L97 sentences after "The source also moved." — committed edits vanish from diff.
- L102 clause "pinning assertions to half-landed behavior produces tests that are wrong within minutes" — stale tests wrong immediately.
- L103 clause "more than one commit can land mid-run… comes out wrong" — tip-only inspection misses commits.
- L105 clause "those commands silently discard uncommitted work… invisible to whoever owned that change" — git resets destroy others' work.
- L124 sentence "The source file is normally already ` M`…" — porcelain cannot detect restore.
- L126 second half ("This is a required part… unattributable external edit") — perturbation list enables attribution.

### doc-writer.md
- L18 — breakdown versus diff signals.
- L46 last sentence ("The doc-writer is the right actor…") — post-implementation sees actual diff.
- L59 — deployment rows collapsed; carve-out.
- L69 second sentence ("The user-facing surface dominates…") — PRD records user capabilities.
- L71 second and third sentences ("Over-documenting… is recoverable… Under-documenting is harder…") — over-documenting cheaper than under.
- L95 first sentence ("A Feature can land in cumulative docs more than once…") — features revisit cumulative docs.

### pm.md
- L71 sentences 2-3 ("Some Issues describe trivial mechanical fixes… no surface to operate on.") — trivial fixes lack spec surface.
- L81 last sentence ("The short-circuit is not 'PM failed to find work'…") — short-circuit is first-class outcome.
- L85 sentences 2-4 ("A body can cite a PRD path… designed to eliminate.") — shape signals mispredict richness.
- L89 sentence "Spec Bees are already feature-scoped… authoritative scoped spec content." — Spec Bees already scoped.
- L104 sentence "Inside a subagent body, 'this skill's base directory' is not the right primitive…" — subagents lack sibling-skill resolution.
- L142 clause "the user explicitly opted into a scoped review… violate that opt-in contract" — silent fallback breaks opt-in.
- L144, L193 clause "files under `<tempdir>/.quorum/` accumulate intentionally…" — scratch files aid debugging.
- L204-L211 (`### Asymmetric error-handling`, entire section) — Path A opt-in; B opportunistic.
- L221 clause "so a tag dropped in relay silently demotes the routing" — dropped tag demotes routing.
- L222 clause "which is what turns an incomplete sweep into one list-gap finding…" and "the case that most needs the check…" — evidence collapses rounds to one.
- L239 clauses "since a blocker may be deferred only paired with that narrowing", "because a narrowing may never reduce coverage of the stated defect…", "which would re-raise the same violation once per remaining unit…" — blocker deferral needs narrowing; no re-raise.
- L242 sentence "Deferring a blocker is a decision that belongs to the orchestrator's routing gates… shelved with nothing narrowed." — gates pair deferral with narrowing.
- L246 clause "they are the failure mode this contract closes" — vague deferrals are the failure.

### code-reviewer.md
- L13 clause "The reviewer is a fresh-eyes quality gate by design" — fresh eyes justify cold start.
- L24 clause "which is what turns an incomplete sweep into one list-gap finding…" and "the case that most needs the check…" — evidence collapses rounds to one.
- L27 clause "so a tag dropped in relay silently downgrades the routing" — dropped tag demotes routing.
- L28 clause "so a relayed heading line lands *inside* the field" — heading would nest in field.

### test-reviewer.md
- L13 clause "The reviewer is a fresh-eyes quality gate by design" — fresh eyes justify cold start.

### doc-reviewer.md
- L13 clause "The reviewer is a fresh-eyes quality gate by design" — fresh eyes justify cold start.
