# Rule inventory — quo-fix-issue SKILL.md, Section 8 (Post-Completion Review) and Section 9 (GitHub close recommendation)

Source slice: `/Users/jesseg/code/quorum/skills/quo-fix-issue/SKILL.md` lines 1114–1342. Rows are extracted, not summarized; identifiers are quoted verbatim.

| ID | Rule | Kind | Site | Produces | Consumes | Cross-refs | Env | Mirror | Class |
|---|---|---|---|---|---|---|---|---|---|
| F7-001 | Section heading `### 8. Post-Completion Review` names this section. | definition | L1114 `### 8. Post-Completion Review` | - | - | - | none | unknown | procedure |
| F7-002 | After all issues are fixed, run a final fresh-context generalist sweep across all changes made during this quo-fix-issue session. | ordering | L1116 `### 8. Post-Completion Review` | fresh-context sweep dispatch | all session changes | - | Agent | unknown | procedure |
| F7-003 | In batch mode the sweep runs after the final issue in the batch; in single mode after the one issue. | ordering | L1116 `### 8. Post-Completion Review` | - | run mode | - | none | no | procedure |
| F7-004 | In batch / `all` mode, before the fresh-context sweep, re-fire Section 7.5's deferral-hygiene gate as the **end-of-batch firing**. | ordering | L1118 **End-of-batch deferral-hygiene firing.** | deferral-hygiene gate firing | run mode; Section 7.5 gate | Section 7.5 | AskUserQuestion, TaskList | no | procedure |
| F7-005 | When re-firing here, treat the invocation as the end-of-batch firing per Section 7.5's batch-firing semantics. | invariant | L1118 **End-of-batch deferral-hygiene firing.** | - | Section 7.5 batch-firing semantics | Section 7.5 | none | no | procedure |
| F7-006 | On an empty active set at the end-of-batch firing, emit the console message `Deferral hygiene (batch close): no deferred items.` (not the per-Issue variant). | field-or-template | L1118 **End-of-batch deferral-hygiene firing.** | console message | deferral active set | Section 7.5 | none | no | procedure |
| F7-007 | Apply the end-of-batch active-set scope: deferrals surfaced *between* Issues at the inter-Issue advance boundary or during batch-level reconciliation, not belonging to any single Issue's per-Issue gate. | definition | L1118 **End-of-batch deferral-hygiene firing.** | - | deferral active set | Section 7.5 | none | no | procedure |
| F7-008 | In single mode, skip the end-of-batch firing — the per-Issue firing already covered the active set; Section 7.5's batch-firing prose is a no-op there. | precondition | L1118 **End-of-batch deferral-hygiene firing.** | - | run mode | Section 7.5 | none | no | procedure |
| F7-009 | The active set at end-of-batch firing should typically be empty; the firing exists as a defensive sweep against boundary / reconciliation deferrals. | rationale-only | L1118 **End-of-batch deferral-hygiene firing.** | - | - | - | none | no | rationale |
| F7-010 | Until the deferral active set is empty, Section 8 cannot proceed to the fresh-context sweep. | precondition | L1118 **End-of-batch deferral-hygiene firing.** | - | deferral active set | - | none | no | procedure |
| F7-011 | Do NOT invoke `/quo-engineer-review`, `/quo-doc-writer-review`, or `/quo-test-writer-review` at the post-completion stage. | invariant | L1120 **Anti-pattern callout — read before acting.** | - | - | `/quo-engineer-review`, `/quo-doc-writer-review`, `/quo-test-writer-review` | none | unknown | procedure |
| F7-012 | Those skills are parallel lanes of an in-flight review with lane-specific scope (`/quo-engineer-review` source code, `/quo-doc-writer-review` user-facing docs, `/quo-test-writer-review` test files), so none runs the cross-lane sweep. | rationale-only | L1120 **Anti-pattern callout — read before acting.** | - | - | `/quo-engineer-review`, `/quo-doc-writer-review`, `/quo-test-writer-review` | none | unknown | rationale |
| F7-013 | Spawn a fresh general-purpose agent with a self-contained prompt instead of invoking the lane review skills. | command | L1120 **Anti-pattern callout — read before acting.** | Agent dispatch | - | - | Agent | unknown | procedure |
| F7-014 | The team-lead must NOT do the post-completion review directly. | invariant | L1122 **Anti-pattern callout, second.** | - | - | - | none | unknown | procedure |
| F7-015 | The team-lead's accumulated framing prompts, agent reports, and reviewer verdicts bias it toward "did the four phases get done correctly?" rather than "is this good?". | rationale-only | L1122 **Anti-pattern callout, second.** | - | - | - | none | unknown | rationale |
| F7-016 | The fresh agent gets the diff and the issue body and nothing else. | invariant | L1122 **Anti-pattern callout, second.** | - | diff; issue body | - | Agent | unknown | procedure |
| F7-017 | Step 1: compute the pre-session diff scope by capturing `<pre-session-sha>` as the HEAD that existed when quo-fix-issue began. | command | L1124 step 1 (`### 8. Post-Completion Review`) | `<pre-session-sha>` | run-state manifest | Section 1 | none | unknown | procedure |
| F7-018 | `Read` the run-state manifest at `<tempdir>/.quorum/run-state-quo-fix-issue-<repo-dir-name>.md` to obtain the SHA. | command | L1124 step 1 | - | run-state manifest file | Section 1 | none | no | procedure |
| F7-019 | The manifest path is recomputed from the working tree root's basename per Section 1, not remembered. | invariant | L1124 step 1 | - | working tree root basename | Section 1 | none | no | procedure |
| F7-020 | Take the manifest's **Pre-session SHA** field — where Section 1's run-start step recorded it. | field-or-template | L1124 step 1 | `<pre-session-sha>` | manifest **Pre-session SHA** field | Section 1 | none | no | procedure |
| F7-021 | If the manifest is missing, fall back to `HEAD~N` where `N` is the number of issues actually fixed this session (one commit per issue per Section 7 step 2.4). | recovery | L1124 step 1 | `<pre-session-sha>` | count of fixed issues | Section 7 step 2.4 | git | no | procedure |
| F7-022 | Before trusting the Pre-session SHA, validate the manifest is this run's by comparing its **Unit scope** ordered Issue batch against the Issue list this session actually fixed. | precondition | L1124 step 1 | - | manifest **Unit scope** field; fixed Issue list | Section 1 `#### Write the run-state manifest` | none | no | procedure |
| F7-023 | On a **Unit scope** mismatch, ignore the manifest entirely and take the same `HEAD~N` fallback rather than scoping the review diff from a foreign SHA. | recovery | L1124 step 1 | `<pre-session-sha>` | manifest comparison result | Section 1 `#### Write the run-state manifest` | git | no | procedure |
| F7-024 | A mismatch is the stale/foreign-manifest tell: a concurrent run in a same-basename checkout truncated the file, leaving a SHA that need not exist in this repository. | rationale-only | L1124 step 1 | - | - | Section 1 `#### Write the run-state manifest` | none | no | rationale |
| F7-025 | Collect the issue ID list as `<issue-id-1> <issue-id-2> ...` (one ID in single-issue mode; the full session list in batch mode). | field-or-template | L1124 step 1 | issue ID list | fixed Issue list | - | none | no | procedure |
| F7-026 | Step 2: spawn the fresh reviewer using the **Agent tool with `subagent_type=general-purpose` and `run_in_background=true`**. | command | L1126 step 2 | reviewer Agent dispatch | - | - | Agent | unknown | procedure |
| F7-027 | The reviewer prompt must be self-contained because the agent sees nothing else from this session. | invariant | L1126 step 2 | - | - | - | Agent | unknown | procedure |
| F7-028 | Pass the compromise-tracker path (Section 7.5's `compromises-<YYYYMMDD-HHMM>-<short-suffix>.md`) as `<compromise-tracker-path>` — the path, NOT the inlined contents. | invariant | L1126 step 2 | `<compromise-tracker-path>` substitution | compromise tracker file path | Section 7.5 | none | unknown | procedure |
| F7-029 | The dispatched Agent reads the tracker file itself via its own `Read` tool. | invariant | L1126 step 2 | - | compromise tracker file | - | Agent | unknown | procedure |
| F7-030 | Substitute `<pre-session-sha>`, `<compromise-tracker-path>`, and the issue ID list into the skeleton before sending. | command | L1126 step 2 | reviewer prompt | `<pre-session-sha>`; `<compromise-tracker-path>`; issue ID list | - | Agent | unknown | procedure |
| F7-031 | Reviewer prompt opens: "You are an independent reviewer for a quorum fix that was just shipped." | field-or-template | L1129 skeleton | - | - | - | none | unknown | procedure |
| F7-032 | Reviewer scope: review the diff `git diff <pre-session-sha>..HEAD`, computing it itself via git. | command | L1131-L1132 skeleton | diff | `<pre-session-sha>` | - | git, Bash | unknown | procedure |
| F7-033 | Reviewer reads each issue body via `bees show-ticket --ids <id>` for the listed Issue IDs. | command | L1132-L1134 skeleton | - | issue ID list | - | bees, Bash | no | procedure |
| F7-034 | Reviewer gives a fresh-eyes review with no context of how the work was done. | invariant | L1135-L1136 skeleton | - | - | - | none | unknown | procedure |
| F7-035 | Reviewer performs the phases IN ORDER; Phases 1–5 lead with challenging accepted compromises; the discrete-defect sweep is the FINAL phase, not the first. | ordering | L1138-L1140 skeleton | - | - | - | none | unknown | procedure |
| F7-036 | PHASE 1: consume the compromise tracker, passed as a FILE PATH at `<compromise-tracker-path>`, reading it via the Read tool. | command | L1142-L1148 skeleton PHASE 1 | - | compromise tracker file | - | none | unknown | procedure |
| F7-037 | The tracker records every compromise the run accepted: deferred-to-Issue findings, accepted limitations, ungated orchestrator path picks, post-completion overrides. | definition | L1143-L1145 skeleton PHASE 1 | - | - | - | none | unknown | procedure |
| F7-038 | Each `## Compromise <n>` entry carries: Finding (verbatim), Fix paths surfaced by reviewer (each with a `[depth:<...>]` tag), Decision, Rationale, Follow-up Issue. | definition | L1148-L1150 skeleton PHASE 1 | - | compromise tracker entry | - | none | unknown | procedure |
| F7-039 | If the tracker file is MISSING or unreadable, the reviewer does NOT abort — it PROCEEDS with the remaining phases. | recovery | L1150-L1152 skeleton PHASE 1 | - | tracker read result | - | none | unknown | procedure |
| F7-040 | On a missing tracker, the reviewer flags the missing tracker explicitly in its output so the dropped hand-off is visible rather than silent. | recovery | L1151-L1153 skeleton PHASE 1 | finding (missing tracker) | tracker read result | - | none | unknown | procedure |
| F7-041 | PHASE 2: challenge each tracked compromise on its merits; push back when the chosen path is not defensible; do not rubber-stamp a compromise because it was accepted. | command | L1155-L1157 skeleton PHASE 2 | findings | compromise tracker entries | - | none | unknown | procedure |
| F7-042 | PHASE 2 includes the deep-asked-but-cheap-shipped check: flag any mismatch between what the tracker records as asked/chosen and what the diff actually shipped. | command | L1157-L1161 skeleton PHASE 2 | finding | tracker Decision; diff | - | none | unknown | procedure |
| F7-043 | A tracker entry whose `Finding (verbatim)` carries the `blocker` severity tag and whose `Decision` is `User picked Accept the limitation` is a contract violation — a blocker can never be accepted. | invariant | L1161-L1164 skeleton PHASE 2 | finding | tracker `Finding (verbatim)`; `Decision` | - | none | unknown | procedure |
| F7-044 | An entry whose `Decision` is `User picked Defer to follow-up Issue` and whose `Rationale` carries **no narrowing record** is a contract violation — a blocker may be deferred only paired with narrowing. | invariant | L1164-L1168 skeleton PHASE 2 | finding | tracker `Decision`; `Rationale` | - | none | unknown | procedure |
| F7-045 | A narrowing record is: what was narrowed out of the change, and why the blocker no longer describes anything that ships. | definition | L1165-L1167 skeleton PHASE 2 | - | - | - | none | unknown | procedure |
| F7-046 | An entry whose narrowing record shows the narrowing **left the unit's stated defect partly unfixed** is a contract violation — narrowing may never reduce coverage of the stated defect. | invariant | L1168-L1172 skeleton PHASE 2 | finding | tracker `Rationale` | - | none | unknown | procedure |
| F7-047 | Emit any PHASE 2 contract violation as a `[compromise-challenge]` finding at `blocker` severity, never lower. | field-or-template | L1172-L1173 skeleton PHASE 2 | `[compromise-challenge]` `blocker` finding | contract-violation detection | - | none | unknown | procedure |
| F7-048 | PHASE 3: for every entry whose Decision is `Orchestrator picked path (<letter>) — highest-quality`, run the ungated-pick plausibility check on TWO axes. | command | L1175-L1179 skeleton PHASE 3 | findings | tracker `Decision` | - | none | unknown | procedure |
| F7-049 | Match the ungated-pick Decision on the surrounding wording, not a literal letter, since the writing step substitutes the chosen path's own letter. | invariant | L1177-L1179 skeleton PHASE 3 | - | tracker `Decision` | - | none | unknown | procedure |
| F7-050 | Axis (i): evaluate whether the depth judgment was plausible, or whether the chosen path's true depth is `re-architect`. | command | L1179-L1180 skeleton PHASE 3 | finding | tracker entry; diff | - | none | unknown | procedure |
| F7-051 | Axis (ii): evaluate whether the chosen path introduced a mechanism the unit's approved design did not enumerate and, if so, whether it should have been deferred or put to the user. | command | L1180-L1187 skeleton PHASE 3 | finding | tracker entry; diff; approved design | - | none | unknown | procedure |
| F7-052 | "Mechanism" means: new state, configuration surface (flag, environment variable, setting), persisted or wire field, background task, exception or error type, metric / log / trace attribute, name or identifier class, gate, or retry / fallback path. | definition | L1181-L1185 skeleton PHASE 3 | - | - | - | none | unknown | procedure |
| F7-053 | Ask the two-axis question of every ungated-pick entry; emit a `[compromise-challenge]` finding on either axis. | command | L1187-L1188 skeleton PHASE 3 | `[compromise-challenge]` finding | axis evaluations | - | none | unknown | procedure |
| F7-054 | Axis (ii) is required, not optional: an ungated pick is an orchestrator judgment no gate reviewed, and PHASE 3 is the only place it gets challenged. | invariant | L1188-L1190 skeleton PHASE 3 | - | - | - | none | unknown | procedure |
| F7-055 | PHASE 4: for EVERY finding the in-flow reviewer surfaced (regardless of severity / depth / path-count), evaluate whether an additional plausible fix path should have been enumerated. | command | L1192-L1195 skeleton PHASE 4 | findings | in-flow reviewer findings | - | none | unknown | procedure |
| F7-056 | On judged under-enumeration, emit a `[compromise-challenge]` finding naming the under-enumeration. | field-or-template | L1195-L1198 skeleton PHASE 4 | `[compromise-challenge]` finding | under-enumeration judgment | - | none | unknown | procedure |
| F7-057 | Under-enumeration means the reviewer surfaced fewer paths than existed, so the pick (orchestrator's or user's at a gate) was made from an incomplete menu. | definition | L1195-L1197 skeleton PHASE 4 | - | - | - | none | unknown | procedure |
| F7-058 | PHASE 3 catches misjudged depth or unnoticed mechanism on a path that WAS surfaced; PHASE 4 catches a plausible path NOT surfaced at all. | rationale-only | L1198-L1201 skeleton PHASE 4 | - | - | - | none | unknown | rationale |
| F7-059 | PHASE 5: make a holistic solution-quality judgment beyond the logged compromises — is the shipped solution actually good, independent of any single tracked decision? | command | L1203-L1205 skeleton PHASE 5 | findings | diff | - | none | unknown | procedure |
| F7-060 | PHASE 6 (final): discrete-defect sweep — flag code defects, prose problems, spec drift between the change and the issue, contract-key violations, cross-file inconsistencies, missing edits the issue called for. | command | L1207-L1211 skeleton PHASE 6 | findings | diff; issue body | - | none | unknown | procedure |
| F7-061 | Do NOT allow renames of keys in CLAUDE.md `## Documentation Locations` or `## Build Commands`. | invariant | L1209-L1210 skeleton PHASE 6 | finding | diff; CLAUDE.md contract keys | CLAUDE.md `## Documentation Locations`, `## Build Commands` | none | unknown | procedure |
| F7-062 | One generalist pass covers code AND docs AND tests — do not lane-scope. | invariant | L1212 skeleton PHASE 6 | - | - | - | none | unknown | procedure |
| F7-063 | In skill repos (diff includes `skills/<name>/SKILL.md` or `agents/<name>.md`), treat those markdown files as skill / subagent program source code, not natural-language documentation. | invariant | L1214-L1216 skeleton note | - | diff file paths | - | none | unknown | procedure |
| F7-064 | Review skill / agent markdown with the same rigor as language-specific source: broken cross-references, drifted contracts, ambiguous prose, and CLAUDE.md design-rule violations are in scope. | command | L1216-L1219 skeleton note | findings | skill / agent markdown in diff | - | none | unknown | procedure |
| F7-065 | Reviewer does NOT do a general repo audit; it stays focused on the diff. | invariant | L1221 skeleton | - | diff | - | none | unknown | procedure |
| F7-066 | Reviewer does NOT invoke /quo-engineer-review, /quo-doc-writer-review, or /quo-test-writer-review. | invariant | L1223-L1226 skeleton | - | - | `/quo-engineer-review`, `/quo-doc-writer-review`, `/quo-test-writer-review` | none | unknown | procedure |
| F7-067 | Reviewer returns findings as a numbered list. | field-or-template | L1228 skeleton | numbered findings list | - | - | none | unknown | procedure |
| F7-068 | Tag EVERY finding with exactly one of `[compromise-challenge]` (challenge to an accepted compromise, from PHASE 2/3/4) / `[design]` (solution-quality concern) / `[defect]` (discrete defect). | field-or-template | L1228-L1231 skeleton | finding class tag | - | - | none | unknown | procedure |
| F7-069 | Each finding also carries the severity tag: one of `blocker` / `suggestion` / `nit`. | field-or-template | L1231 skeleton | severity tag | - | - | none | unknown | procedure |
| F7-070 | Each finding also carries the per-fix-path depth tag and the enumerated fix paths from the in-flight emission contract. | field-or-template | L1232-L1233 skeleton | depth tag; fix paths | in-flight emission contract | - | none | unknown | procedure |
| F7-071 | The depth tag is informative here: the post-completion sweep is the final pre-merge gate, so any finding it emits is a gate candidate regardless of depth. | invariant | L1233-L1235 skeleton | - | - | - | none | unknown | procedure |
| F7-072 | Preserve the `file:line` + severity shape — the new tags are additive to it. | field-or-template | L1235-L1236 skeleton | finding shape | - | - | none | unknown | procedure |
| F7-073 | If clean, the reviewer returns exactly "no issues found". | field-or-template | L1236-L1237 skeleton | clean verdict string | - | - | none | unknown | procedure |
| F7-074 | Wait for the reviewer agent's report before proceeding. | ordering | L1240 step 2 | - | Agent result | - | Agent | unknown | procedure |
| F7-075 | Step 3: synthesize findings before presenting — compare the fresh reviewer's findings against the in-flight per-issue code/test/doc reviewer verdicts still in context. | command | L1242 step 3 | synthesis notes | reviewer report; in-flight verdicts | - | none | unknown | procedure |
| F7-076 | Flag any disagreements explicitly, e.g. "fresh reviewer flagged X but in-flight code reviewer judged X clean." | command | L1242 step 3 | synthesis notes | reviewer report; in-flight verdicts | - | none | unknown | procedure |
| F7-077 | Present the synthesized findings (fresh reviewer's list plus synthesis notes) to the user. | relay | L1242 step 3 | user-facing findings | synthesis | - | none | unknown | procedure |
| F7-078 | When the reviewer returns one or more `[compromise-challenge]` findings, render a one-or-two-sentence prose preamble BEFORE the findings list naming this explicitly. | relay | L1244 **Compromise-challenge preamble (rendered before presenting the findings).** | preamble text | `[compromise-challenge]` findings | - | none | unknown | procedure |
| F7-079 | Mirror the verdict-keyed preamble pattern in `/quo-plan` Step 5e and this skill's Section 3 Analyst-verdict preamble — a short prose lead with the `⚠️`-led divergent-framing convention when a challenge is present. | field-or-template | L1244 **Compromise-challenge preamble** | preamble text | - | `/quo-plan` Step 5e; Section 3 Analyst-verdict preamble | none | unknown | procedure |
| F7-080 | Example preamble: "⚠️ The post-completion reviewer **challenged** [N] compromise(s) accepted during this run — these are not new defects but pushback on decisions you already made; read them before the discrete findings below." | example | L1244 **Compromise-challenge preamble** | - | - | - | none | unknown | example |
| F7-081 | When there are no `[compromise-challenge]` findings, no preamble is needed — present the findings directly. | precondition | L1244 **Compromise-challenge preamble** | - | `[compromise-challenge]` findings count | - | none | unknown | procedure |
| F7-082 | The orchestrator typically creates ad-hoc self-tracking TaskList tasks during Section 8 (e.g., "Get diff scope", per-issue "Verify <id>", "Synthesize findings"). | definition | L1246 **Orchestrator self-tracking close-out (mandatory before yielding).** | self-tracking TaskList tasks | - | Section 7 step 3; step 6 | TaskList | unknown | procedure |
| F7-083 | Before yielding the turn at step 4 / step 5 (to deliver "no issues found" or to fire `AskUserQuestion`), mark every orchestrator self-tracking TaskList task `completed` and clear it from the active set. | ordering | L1246 **Orchestrator self-tracking close-out** | TaskList status flips | self-tracking tasks | step 4; step 5 | TaskList | unknown | procedure |
| F7-084 | The yield is the close-out trigger: when the orchestrator stops responding, the TaskList must show no `in_progress` entries left over from synthesis steps. | invariant | L1246 **Orchestrator self-tracking close-out** | - | TaskList state | - | TaskList | unknown | procedure |
| F7-085 | This self-tracking close-out is the analog of step 6's per-finding close-out (which scopes to `<role>-postcomp-<n>` Agents and `file-issue-postcomp-<n>` entries); the two scopes are complementary, not overlapping. | rationale-only | L1246 **Orchestrator self-tracking close-out** | - | - | step 6; `/quo-file-issue` | TaskList | unknown | rationale |
| F7-086 | `file-issue-postcomp-<n>` entries are created by the "File as issue tickets" branch, which invokes `/quo-file-issue` per finding rather than dispatching an Agent. | definition | L1246 **Orchestrator self-tracking close-out** | - | - | `/quo-file-issue` | TaskList | unknown | procedure |
| F7-087 | Step 4: if the agent returned "no issues found", report "Post-completion review: no issues found" and exit. | relay | L1248 step 4 | console message | reviewer verdict | - | none | unknown | procedure |
| F7-088 | Step 5: if the agent flagged issues, fire the user-facing gate through the two-step `TaskCreate` → `AskUserQuestion` contract. | gate | L1250 step 5 | gate firing | reviewer findings | Section 4 TaskList naming convention | TaskList, AskUserQuestion | unknown | procedure |
| F7-089 | **First** create a `gate-askuserquestion-<short-suffix>` TaskList task naming the post-completion findings gate (per Section 4's TaskList naming convention's gate-task entry). | name-class | L1250 step 5 | `gate-askuserquestion-<short-suffix>` task | - | Section 4 | TaskList | unknown | procedure |
| F7-090 | **Then** call `AskUserQuestion` in the same turn as the `TaskCreate`. | ordering | L1250 step 5 | AskUserQuestion call | gate task | - | AskUserQuestion | unknown | procedure |
| F7-091 | Do not produce a text response describing this gate — fire `TaskCreate` and `AskUserQuestion` directly. | invariant | L1250 step 5 | - | - | - | TaskList, AskUserQuestion | unknown | procedure |
| F7-092 | Mark the `gate-*` task `completed` once the user's answer is consumed and the step 6 routing branch is entered. | ordering | L1250 step 5 | TaskList status flip | user answer | step 6 | TaskList | unknown | procedure |
| F7-093 | Gate question text: "Post-completion review found [N] issues. How would you like to handle them?" | field-or-template | L1251 step 5 | question text | findings count | - | AskUserQuestion | unknown | procedure |
| F7-094 | Gate options are exactly: **Fix in this session** (address the issues now), **File as issue tickets** (create issue tickets via `/quo-file-issue` for each issue), **Skip** (acknowledge and move on without action). | choice-set | L1252-L1255 step 5 | - | - | `/quo-file-issue` | AskUserQuestion | unknown | procedure |
| F7-095 | When the reviewer returned a PHASE 2 contract-violation `[compromise-challenge]` finding, the step 5 question text recommends **`Fix in this session`** (per step 7). | relay | L1257 step 5 | question text recommendation | PHASE 2 findings | step 7 | AskUserQuestion | unknown | procedure |
| F7-096 | Step 6 **Fix in this session**: dispatch fresh ephemeral Agents per Section 4's dispatch shape (Engineer / Test Writer / Doc Writer as needed) to address the findings. | command | L1260 step 6 **Fix in this session** | Agent dispatches | findings | Section 4 | Agent | unknown | procedure |
| F7-097 | Stay in delegate mode during post-completion fixes. | invariant | L1260 step 6 **Fix in this session** | - | - | - | Agent | unknown | procedure |
| F7-098 | Section 4's issue-scoped naming (`<role>-<issue-id>`) does not cover post-completion follow-up Agents; use the **post-completion-scoped** name `<role>-postcomp-<n>`. | name-class | L1260 step 6 **Fix in this session** | `<role>-postcomp-<n>` task | - | Section 4 | TaskList | yes | procedure |
| F7-099 | `<n>` in `<role>-postcomp-<n>` is the 1-based index of the finding in the fresh reviewer's numbered list (e.g., `engineer-postcomp-1`, `doc-writer-postcomp-2`, `engineer-postcomp-3`). | definition | L1260 step 6 **Fix in this session** | - | reviewer numbered list | - | TaskList | yes | procedure |
| F7-100 | Section 4's "exactly one TaskList task per Agent" rule applies: two findings needing Engineer follow-ups dispatch to distinct names (`engineer-postcomp-1`, `engineer-postcomp-3`), never a shared `engineer-postcomp`. | invariant | L1260 step 6 **Fix in this session** | - | - | Section 4 | TaskList | yes | procedure |
| F7-101 | Post-completion names take **no** issue-id suffix and normally no round discriminator — the per-finding index is the discriminator. | invariant | L1260 step 6 **Fix in this session** | - | - | - | TaskList | yes | procedure |
| F7-102 | Carve-out: a round discriminator IS permitted and required when a post-completion writer lane is re-dispatched after a movement abort — append `-r<k>`. | name-class | L1260 step 6 **Fix in this session** | `<role>-postcomp-<n>-r<k>` task | movement abort | - | TaskList | yes | procedure |
| F7-103 | `<k>` is the 1-based re-dispatch count for that role at that finding index (e.g., `test-writer-postcomp-2-r1` for the first Test Writer re-dispatch on finding 2). | definition | L1260 step 6 **Fix in this session** | - | - | - | TaskList | yes | procedure |
| F7-104 | `/quo-execute`'s post-completion naming entry carries the identical `-r<k>` carve-out, so the two skills read as one convention. | invariant | L1260 step 6 **Fix in this session** | - | - | `/quo-execute` | none | yes | procedure |
| F7-105 | Outside the movement-abort re-dispatch case, do not append a round discriminator to a post-completion name. | invariant | L1260 step 6 **Fix in this session** | - | - | - | TaskList | yes | procedure |
| F7-106 | When each follow-up Agent returns, confirm any bees ticket transitions the worker committed to (as Section 4's reconcile-on-completion step does). | command | L1260 step 6 **Fix in this session** | - | Agent result | Section 4 | bees | unknown | procedure |
| F7-107 | Then mark the corresponding `<role>-postcomp-<n>` TaskList task `completed` and clear it from the active set (mirroring Section 7 step 3's per-issue close-out). | ordering | L1260 step 6 **Fix in this session** | TaskList status flip | Agent result | Section 4; Section 7 step 3 | TaskList | unknown | procedure |
| F7-108 | An `engineer-postcomp-<n>` return carrying a `## Design question` is NOT a completion of that finding — its diff is partial by construction. | invariant | L1260 step 6 **Fix in this session** | - | Engineer return `## Design question` | `agents/engineer.md` | Agent | unknown | procedure |
| F7-109 | `agents/engineer.md`'s stop-rather-than-invent rule is mode-independent. | definition | L1260 step 6 **Fix in this session** | - | - | `agents/engineer.md` | none | unknown | rationale |
| F7-110 | On a `## Design question` return: mark that task `completed` (Agent exited), dispatch no further lane for that finding, and route the finding to the **File as issue tickets** branch. | recovery | L1260 step 6 **Fix in this session** | TaskList status flip; routing | Engineer return | - | TaskList | unknown | procedure |
| F7-111 | The follow-up Issue for a design-question finding carries the finding, the Engineer's question verbatim, and its what-landed / what-did-not list. | field-or-template | L1260 step 6 **Fix in this session** | follow-up Issue body | Engineer return | - | bees | unknown | procedure |
| F7-112 | Note in the final report which of the design-question finding's edits are already on disk. | relay | L1260 step 6 **Fix in this session** | final report note | Engineer return | - | none | unknown | procedure |
| F7-113 | There is no Analyst in Section 8, so Section 4's Revise route does not apply to a post-completion design question. | invariant | L1260 step 6 **Fix in this session** | - | - | Section 4 Revise route | none | no | procedure |
| F7-114 | If a tracking entry is created for the design-question filing, reuse the existing `file-issue-postcomp-<n>` name at the same finding index. | name-class | L1260 step 6 **Fix in this session** | `file-issue-postcomp-<n>` task | - | - | TaskList | unknown | procedure |
| F7-115 | Section 4's movement-report rung carries over: a `test-writer-postcomp-<n>` / `doc-writer-postcomp-<n>` return that reports it *stopped* on detected source movement is not a completion. | invariant | L1260 step 6 **Fix in this session** | - | writer Agent return | Section 4 | Agent | unknown | procedure |
| F7-116 | On a movement stop: mark the writer's own `<role>-postcomp-<n>` task `completed` (Agent exited) and open an `aborted-<role>-postcomp-<n>` task `pending` in its place. | recovery | L1260 step 6 **Fix in this session** | `aborted-<role>-postcomp-<n>` task `pending` | writer Agent return | Section 4 | TaskList | unknown | procedure |
| F7-117 | The `aborted-<role>-postcomp-<n>` task carries the same `<n>` as the lane it marks. | invariant | L1260 step 6 **Fix in this session** | - | - | - | TaskList | unknown | procedure |
| F7-118 | The `aborted-*` task's `metadata.activity` string is the writer's "how far I got" report. | field-or-template | L1260 step 6 **Fix in this session** | `metadata.activity` | writer Agent return | - | TaskList | unknown | procedure |
| F7-119 | Re-dispatch the aborted lane once the source has settled, per the rung's three mover branches, under `<role>-postcomp-<n>-r<k>`. | recovery | L1260 step 6 **Fix in this session** | Agent dispatch under `-r<k>` name | source settled | Section 4 rung's three mover branches | Agent, TaskList | unknown | procedure |
| F7-120 | Mark the `aborted-*` task `completed` when the re-dispatch delivers. | ordering | L1260 step 6 **Fix in this session** | TaskList status flip | re-dispatch result | - | TaskList | unknown | procedure |
| F7-121 | While an `aborted-<role>-postcomp-<n>` task is `pending`, do **not** treat the Section 8 pass as finished and do **not** commit — the pending marker is the owed redelivery. | precondition | L1260 step 6 **Fix in this session** | - | TaskList state | Section 4 | TaskList, git | unknown | procedure |
| F7-122 | The rung's unexplained-movement gate applies here; read its abort option's *whole body*, not just its title. | gate | L1262 **The rung's unexplained-movement gate applies here…** | gate firing | - | Section 4 unexplained-movement gate | AskUserQuestion | no | procedure |
| F7-123 | The gate's third option is read as *abort this follow-up lane* rather than *abort this Issue* — the Issue is already `done` and its per-issue commit landed. | invariant | L1262 **The rung's unexplained-movement gate applies here…** | - | - | - | AskUserQuestion | no | procedure |
| F7-124 | The abort option's body routing through `#### Aborted-Issue close-out` does NOT apply on this branch. | invariant | L1262 **That re-titling is not the only thing that changes…** | - | - | `#### Aborted-Issue close-out` | none | no | procedure |
| F7-125 | On this branch the abort closes out via **this branch's OWN prefix sweep** (the "Close out the `aborted-*-postcomp-<n>` markers with the rest" paragraph), **not** `#### Aborted-Issue close-out`. | recovery | L1262 **The rung's unexplained-movement gate applies here…** | prefix sweep | - | `#### Aborted-Issue close-out` | TaskList | no | procedure |
| F7-126 | `#### Aborted-Issue close-out` is scoped to an Issue abandoned *before* its fix landed. | definition | L1262 **The rung's unexplained-movement gate applies here…** | - | - | `#### Aborted-Issue close-out` | none | no | procedure |
| F7-127 | Running `#### Aborted-Issue close-out` from Section 8 would re-fire Section 7.5 for a closed deferral gate, run the Issue-boundary checkpoint's aborted path against a `done` committed Issue, and in batch mode stop the run with a fresh-session resume command. | rationale-only | L1262 **The rung's unexplained-movement gate applies here…** | - | - | Section 7.5; Issue-boundary checkpoint | none | no | rationale |
| F7-128 | After this branch's sweep, **continue Section 8's own flow**: remaining findings' dispositions, then step 7's compromise-challenge recovery gates, then Section 9. | ordering | L1262 **The rung's unexplained-movement gate applies here…** | - | - | step 7; Section 9 | none | no | procedure |
| F7-129 | Once all follow-up lanes have delivered, commit. | command | L1262 **The rung's unexplained-movement gate applies here…** | git commit | all `<role>-postcomp-*` lanes delivered | - | git | unknown | procedure |
| F7-130 | Close out `aborted-*-postcomp-<n>` markers with the rest: the per-Agent close-out sentence covers them too. | ordering | L1264 **Close out the `aborted-*-postcomp-<n>` markers with the rest.** | TaskList status flips | TaskList state | - | TaskList | unknown | procedure |
| F7-131 | Sweep by name **prefix**, so both an `aborted-<role>-postcomp-<n>` marker and any `-r<k>`-discriminated re-dispatch are closed alongside their first round, not left in the active set when Section 8 exits. | invariant | L1264 **Close out the `aborted-*-postcomp-<n>` markers with the rest.** | TaskList status flips | TaskList state | - | TaskList | unknown | procedure |
| F7-132 | This prefix sweep is also the close-out the unexplained-movement gate's abort option routes to on this branch. | definition | L1264 **Close out the `aborted-*-postcomp-<n>` markers with the rest.** | - | - | unexplained-movement gate | TaskList | no | procedure |
| F7-133 | Section 4's Engineer-dispatch precondition keys on issue-scoped names (`test-writer-<issue-id>` and friends), which do not match post-completion names, so it is restated for this branch. | rationale-only | L1266 **The Engineer-dispatch precondition applies here too, on the post-completion names.** | - | - | Section 4 | none | no | rationale |
| F7-134 | The orchestrator MUST NOT dispatch an `engineer-postcomp-<n>` Agent while any `test-writer-postcomp-*` or `doc-writer-postcomp-*` TaskList task is `pending` or `in_progress`. | precondition | L1266 **The Engineer-dispatch precondition applies here too…** | - | TaskList state | Section 4 | TaskList, Agent | unknown | procedure |
| F7-135 | The precondition matches on name prefix plus status at **every** finding index and every `-r<k>` re-dispatch round, not only the `<n>` being addressed. | invariant | L1266 **The Engineer-dispatch precondition applies here too…** | - | TaskList state | - | TaskList | unknown | procedure |
| F7-136 | Reason for the precondition: a writer handed a diff an Engineer is about to rewrite has to redo its work. | rationale-only | L1266 **The Engineer-dispatch precondition applies here too…** | - | - | Section 4 | none | unknown | rationale |
| F7-137 | Section 4's `pending`-with-no-Agent-behind-it clause carries over: dispatch the stalled role, or clear the stale task, rather than waiting on a notification that will never arrive. | recovery | L1266 **The Engineer-dispatch precondition applies here too…** | Agent dispatch or TaskList clear | TaskList state | Section 4 | TaskList, Agent | unknown | procedure |
| F7-138 | When one finding needs both a source change and a test/doc change, dispatch the **Engineer** (`engineer-postcomp-<n>`) first and let it return. | ordering | L1268 **Order the lanes when one finding needs both a source change and a test/doc change.** | Agent dispatch | finding | - | Agent | unknown | procedure |
| F7-139 | Only after the Engineer returns, dispatch the `test-writer-postcomp-<n>` / `doc-writer-postcomp-<n>` lane whose input that source change determines. | ordering | L1268 **Order the lanes…** | Agent dispatch | Engineer result | - | Agent | unknown | procedure |
| F7-140 | Two findings independent of each other may be worked concurrently; the within-finding ordering and the cross-finding precondition are the only constraints. | invariant | L1268 **Order the lanes…** | - | - | - | Agent | unknown | procedure |
| F7-141 | Step 6 **File as issue tickets**: for each issue, invoke `/quo-file-issue` with the issue description. | command | L1269 step 6 **File as issue tickets** | Issue tickets | findings | `/quo-file-issue` | bees | unknown | procedure |
| F7-142 | Report the created ticket IDs to the user. | relay | L1269 step 6 **File as issue tickets** | user-facing report | created ticket IDs | - | none | unknown | procedure |
| F7-143 | TaskList progress entries created for per-finding filing are named `file-issue-postcomp-<n>`, `<n>` being the 1-based index of the finding being filed. | name-class | L1269 step 6 **File as issue tickets** | `file-issue-postcomp-<n>` task | - | - | TaskList | unknown | procedure |
| F7-144 | Mark `file-issue-postcomp-<n>` entries `completed` and clear them from the active set before exiting — same close-out discipline as "Fix in this session". | ordering | L1269 step 6 **File as issue tickets** | TaskList status flips | - | - | TaskList | unknown | procedure |
| F7-145 | Step 6 **Skip**: Done. | command | L1270 step 6 **Skip** | - | - | - | none | unknown | procedure |
| F7-146 | Step 7: a `[compromise-challenge]` finding from PHASE 3 or PHASE 4 is not handled by step 6's Fix / File / Skip alone — each triggers its own recovery gate. | gate | L1272 **Compromise-challenge recovery gates.** | recovery gate firing | PHASE 3 / PHASE 4 findings | step 6 | AskUserQuestion, TaskList | unknown | procedure |
| F7-147 | Each recovery gate fires **before or alongside** the step-6 disposition for that finding. | ordering | L1272 **Compromise-challenge recovery gates.** | - | - | step 6 | AskUserQuestion | unknown | procedure |
| F7-148 | The PHASE 2 accepted-`blocker`-or-unnarrowed-deferral contract violation has NO recovery gate, by design. | invariant | L1272 **Compromise-challenge recovery gates.** | - | PHASE 2 findings | - | none | unknown | procedure |
| F7-149 | A PHASE 2 contract-violation finding is dispositioned by **step 5's Fix / File / Skip gate alone**; step 6 executes whatever that gate returns. | gate | L1272 **Compromise-challenge recovery gates.** | - | step 5 gate answer | step 5; step 6 | AskUserQuestion | unknown | procedure |
| F7-150 | Because the PHASE 2 violation is a `blocker`, the orchestrator recommends **`Fix in this session`** in the step 5 gate's question text. | relay | L1272 **Compromise-challenge recovery gates.** | question text recommendation | PHASE 2 findings | step 5 | AskUserQuestion | unknown | procedure |
| F7-151 | The step 5 gate is **aggregate** — one answer covers every finding — so the recommendation is stated once for the whole finding set. | invariant | L1272 **Compromise-challenge recovery gates.** | - | - | step 5 | AskUserQuestion | unknown | procedure |
| F7-152 | A `Skip` at the step 5 gate leaves the contract violation recorded in the reviewer's output and the compromise tracker and nowhere else. | definition | L1272 **Compromise-challenge recovery gates.** | - | step 5 answer | - | none | unknown | procedure |
| F7-153 | Do not invent a fourth gate for the PHASE 2 contract violation. | invariant | L1272 **Compromise-challenge recovery gates.** | - | - | - | AskUserQuestion | unknown | procedure |
| F7-154 | Both recovery gates use the two-step `TaskCreate` → `AskUserQuestion` contract: **first** create a `gate-askuserquestion-<short-suffix>` TaskList task naming the gate. | name-class | L1272 **Compromise-challenge recovery gates.** | `gate-askuserquestion-<short-suffix>` task | - | - | TaskList | unknown | procedure |
| F7-155 | Use a distinct collision-resistant `<short-suffix>` per gate fire, per the per-fire-uniqueness rule. | invariant | L1272 **Compromise-challenge recovery gates.** | - | - | per-fire-uniqueness rule | TaskList | unknown | procedure |
| F7-156 | **Then** call `AskUserQuestion` with the finite choices in the same turn. | ordering | L1272 **Compromise-challenge recovery gates.** | AskUserQuestion call | gate task | - | AskUserQuestion | unknown | procedure |
| F7-157 | Do not produce a text response describing a recovery gate — fire `TaskCreate` and `AskUserQuestion` directly. | invariant | L1272 **Compromise-challenge recovery gates.** | - | - | - | TaskList, AskUserQuestion | unknown | procedure |
| F7-158 | Mark the recovery gate's `gate-*` task `completed` once the user's answer is consumed and the routing branch is entered. | ordering | L1272 **Compromise-challenge recovery gates.** | TaskList status flip | user answer | - | TaskList | unknown | procedure |
| F7-159 | Recovery gates are multi-choice only — do not add fake free-text options. | invariant | L1272 **Compromise-challenge recovery gates.** | - | - | - | AskUserQuestion | unknown | procedure |
| F7-160 | Each recovery gate **fires per challenged finding** (three such findings ⇒ three gate firings), NOT aggregated into one gate. | invariant | L1272 **Compromise-challenge recovery gates.** | gate firings | `[compromise-challenge]` findings | - | AskUserQuestion | unknown | procedure |
| F7-161 | Recovery gate **firing order equals the reviewer's emission order** in its numbered list. | ordering | L1272 **Compromise-challenge recovery gates.** | - | reviewer numbered list | - | AskUserQuestion | unknown | procedure |
| F7-162 | These gates inherit a prose-adherence fragility — narrowed (not closed) by the two-step contract; do not claim it is fixed. | invariant | L1272 **Compromise-challenge recovery gates.** | - | - | - | none | unknown | rationale |
| F7-163 | **SR-6.7 ungated-route recovery gate** fires when a `[compromise-challenge]` finding flags an ungated orchestrator path pick (Decision `Orchestrator picked path (x) — highest-quality`) on either PHASE 3 axis. | gate | L1274 **SR-6.7 ungated-route recovery gate.** | three-choice AskUserQuestion | PHASE 3 finding; tracker `Decision` | Section 7.5 Trigger D | AskUserQuestion, TaskList | unknown | procedure |
| F7-164 | SR-6.7 triggers cover both axes: a misjudged depth, or a chosen path that introduced a mechanism and should have routed to the scope-bounding gate. | definition | L1274 **SR-6.7 ungated-route recovery gate.** | - | PHASE 3 finding | scope-bounding gate | none | unknown | procedure |
| F7-165 | SR-6.7 choice labels are byte-matched against Section 7.5's Trigger D branches; do not reword them. | invariant | L1274 **SR-6.7 ungated-route recovery gate.** | - | - | Section 7.5 Trigger D | AskUserQuestion | unknown | procedure |
| F7-166 | The pinned strings `File follow-up Issue to revisit the depth decision`, `Accept the misjudgment and proceed`, and `Decision: User overrode auto-route after post-completion challenge (depth misjudgment)` read generically across both axes, naming **the routing misjudgment** — mechanism axis included. | invariant | L1274 **The pinned strings read generically across both axes:** | - | - | Section 7.5 Trigger D | none | unknown | procedure |
| F7-167 | Read "depth decision" / "misjudgment" generically rather than concluding the mechanism axis has no gate. | invariant | L1274 **SR-6.7 ungated-route recovery gate.** | - | - | - | none | unknown | procedure |
| F7-168 | SR-6.7 choices are exactly: `File follow-up Issue to revisit the depth decision`, `Accept the misjudgment and proceed`, `Pause to discuss`. | choice-set | L1274-L1277 **SR-6.7 ungated-route recovery gate.** | - | - | - | AskUserQuestion | unknown | procedure |
| F7-169 | SR-6.7 `File follow-up Issue to revisit the depth decision`: dispatch `/quo-file-issue` via the Skill tool capturing the depth-mismatch finding plus the original compromise-tracker entry as context. | command | L1275 SR-6.7 | Issue ticket | finding; tracker entry | `/quo-file-issue` | bees | unknown | procedure |
| F7-170 | On SR-6.7 `File`, the **original Trigger C tracker entry's `Follow-up Issue` field is updated in place** with the new Issue ID — NO new tracker entry (per Section 7.5's Trigger D `File`-branch note). | field-or-template | L1275 SR-6.7 | tracker `Follow-up Issue` field update | new Issue ID; Trigger C entry | Section 7.5 Trigger D `File`-branch note; Trigger C | none | unknown | procedure |
| F7-171 | SR-6.7 `Accept the misjudgment and proceed`: fires Section 7.5's **Trigger D** append — a NEW tracker entry with `Decision: User overrode auto-route after post-completion challenge (depth misjudgment)` and the reviewer's challenge text as Rationale. | command | L1276 SR-6.7 | tracker entry (Trigger D) | reviewer challenge text | Section 7.5 Trigger D | none | unknown | procedure |
| F7-172 | The SR-6.7 `Accept` step FIRES Trigger D's write — it does not author it; the write mechanism is owned by Section 7.5's Trigger D. | invariant | L1276 SR-6.7 | - | - | Section 7.5 Trigger D | none | unknown | procedure |
| F7-173 | SR-6.7 `Pause to discuss`: stop the post-completion flow at this gate and surface a prose discussion; the user can re-issue any of the three choices afterward. | recovery | L1277 SR-6.7 | prose discussion | - | - | none | unknown | procedure |
| F7-174 | On SR-6.7 `Pause to discuss`, no tracker write fires until a subsequent `Accept` / `File` pick (per Trigger D's `Pause` note). | invariant | L1277 SR-6.7 | - | - | Trigger D `Pause` note | none | unknown | procedure |
| F7-175 | **SR-4.6 under-enumeration recovery gate** fires when a `[compromise-challenge]` finding flags under-enumeration (the fix-path-enumeration plausibility check), with the **same three-choice gate shape** and relabeled choices. | gate | L1278 **SR-4.6 under-enumeration recovery gate.** | three-choice AskUserQuestion | PHASE 4 finding | - | AskUserQuestion, TaskList | unknown | procedure |
| F7-176 | SR-4.6 choices are exactly: `File follow-up Issue to surface the missing path`, `Accept the under-enumeration and proceed`, `Pause to discuss`. | choice-set | L1278-L1281 **SR-4.6 under-enumeration recovery gate.** | - | - | - | AskUserQuestion | unknown | procedure |
| F7-177 | SR-4.6 `File follow-up Issue to surface the missing path`: dispatch `/quo-file-issue` via the Skill tool capturing the under-enumeration finding as context. | command | L1279 SR-4.6 | Issue ticket | finding | `/quo-file-issue` | bees | unknown | procedure |
| F7-178 | On SR-4.6 `File`, the tracker write follows Section 7.5's Trigger D `File`-branch under-enumeration note: append a new entry, or update an existing originating Trigger C entry's `Follow-up Issue` in place when one exists. | field-or-template | L1279 SR-4.6 | tracker entry or `Follow-up Issue` update | new Issue ID | Section 7.5 Trigger D `File`-branch under-enumeration note; Trigger C | none | unknown | procedure |
| F7-179 | SR-4.6 `Accept the under-enumeration and proceed`: fires Section 7.5's **Trigger D** append — a NEW tracker entry with `Decision: User accepted under-enumeration after post-completion challenge` and the reviewer's under-enumeration challenge text as Rationale. | command | L1280 SR-4.6 | tracker entry (Trigger D) | reviewer challenge text | Section 7.5 Trigger D | none | unknown | procedure |
| F7-180 | The SR-4.6 `Accept` step FIRES Trigger D's write — it does not author it. | invariant | L1280 SR-4.6 | - | - | Section 7.5 Trigger D | none | unknown | procedure |
| F7-181 | SR-4.6 `Pause to discuss`: stop the post-completion flow at this gate and surface a prose discussion; the user can re-issue any of the three choices afterward; no tracker write until a subsequent `Accept` / `File`. | recovery | L1281 SR-4.6 | prose discussion | - | - | none | unknown | procedure |
| F7-182 | The SR-4.6 gate's per-branch behavior mirrors the SR-6.7 gate exactly (file follow-up Issue / append explicit-override tracker entry via Trigger D / pause-and-resume). | invariant | L1283 step 7 | - | - | Section 7.5 Trigger D | none | unknown | procedure |
| F7-183 | Like SR-6.7, the SR-4.6 gate fires per challenged finding (non-aggregated) in reviewer emission order. | ordering | L1283 step 7 | - | reviewer numbered list | - | AskUserQuestion | unknown | procedure |
| F7-184 | Section heading `### 9. Recommend upstream GitHub close commands` names this section. | definition | L1285 `### 9. Recommend upstream GitHub close commands` | - | - | - | none | no | procedure |
| F7-185 | After Section 8 either reports clean (step 4) or the step 6 per-finding follow-ups close out, and **before yielding the turn**, emit the GitHub close recommendation block. | ordering | L1287 `### 9.` | recommendation block | Section 8 outcome | Section 8 step 4; step 6 | none | no | procedure |
| F7-186 | The block is a copy-paste-ready set of `gh issue close ...` commands, one for each fixed Issue whose `reference_materials` carries a `github-issue` resolver. | field-or-template | L1287 `### 9.` | recommendation block | fixed Issues' `reference_materials` | - | none | no | procedure |
| F7-187 | **Pure recommendation — the skill NEVER runs `gh issue close` itself.** | invariant | L1287 `### 9.` | - | - | - | none | no | procedure |
| F7-188 | No `gh` auth assumption is baked into the workflow; the user copies and runs the command(s) from an authenticated machine. | rationale-only | L1287 `### 9.` | - | - | - | none | no | rationale |
| F7-189 | The recommendation is scoped to the `github-issue` resolver only; do **not** generalize it to `linear-issue` or `url` resolvers here. | invariant | L1289 `### 9.` | - | resolver type | - | none | no | procedure |
| F7-190 | `linear-issue` close needs Linear's own CLI/API (not bundled); the generic `url` resolver has no close concept; adding either is a separate Issue. | rationale-only | L1289 `### 9.` | - | - | - | none | no | rationale |
| F7-191 | Procedure step 1: **Iterate the run's fixed-issue list** — IDs this run actually marked `done` via Section 7 (single-issue: one ID; list / `all` mode: full session list in fixed order). | command | L1293 **Procedure:** step 1 | fixed-issue iteration | Section 7 done-flips | Section 7 | none | no | procedure |
| F7-192 | Issues skipped (Section 2's blocked-issue handling), cancelled at Section 3's user-approval gate, or never reached (e.g., `bees show-ticket` validation soft-failed in Section 1) are NOT in the fixed-issue list. | invariant | L1293 **Procedure:** step 1 | - | run history | Section 2; Section 3; Section 1 | none | no | procedure |
| F7-193 | Step 2: for each fixed Issue, read its `reference_materials`, re-using the value captured in Section 2 / Section 3 / Section 4's dispatch reads if still in context. | command | L1295 **Procedure:** step 2 | - | `reference_materials` | Section 2; Section 3; Section 4 | none | no | procedure |
| F7-194 | If `reference_materials` is not in context, re-read via `bees show-ticket --ids <issue-id>` (identical POSIX and PowerShell forms). | command | L1295-L1305 **Procedure:** step 2 | - | issue ID | - | bees, Bash | no | procedure |
| F7-195 | `reference_materials` is a JSON array of `{value, resolver}` objects. | definition | L1307 **Procedure:** step 2 | - | - | - | none | no | procedure |
| F7-196 | An empty / null `reference_materials` means the Issue was filed via the in-conversation default capture mode (no upstream URL) and contributes nothing to the block. | precondition | L1307 **Procedure:** step 2 | - | `reference_materials` | - | none | no | procedure |
| F7-197 | Step 3: for each entry whose `resolver == "github-issue"`, parse `value` against `https://github.com/<owner>/<repo>/issues/<n>` to extract `<owner>`, `<repo>`, `<n>`. | command | L1309 **Procedure:** step 3 | `<owner>`, `<repo>`, `<n>` | `reference_materials` entry `value` | - | none | no | procedure |
| F7-198 | HTTP and HTTPS are both acceptable; trailing slashes / fragments / query strings are ignored when matching. | definition | L1309 **Procedure:** step 3 | - | URL | - | none | no | procedure |
| F7-199 | If the URL does not match the canonical shape, skip the entry — a mismatch indicates a malformed entry, since `/quo-file-issue`'s `github-issue` resolver writes only canonical Issue URLs. | recovery | L1309 **Procedure:** step 3 | - | URL match result | `/quo-file-issue` | none | no | procedure |
| F7-200 | Step 4: **Capture the per-issue commit SHA** from Section 7 step 2.4's commit step. | command | L1311 **Procedure:** step 4 | per-issue SHA | Section 7 step 2.4 commit | Section 7 step 2.4 | git | no | procedure |
| F7-201 | Preferred path — **Track at commit time**: record the commit SHA the moment Section 7 step 2.4's `git commit` returns, keyed by issue ID, and re-use it here. | command | L1312 **Track at commit time (preferred).** | SHA keyed by issue ID | `git commit` result | Section 7 step 2.4 | git | no | procedure |
| F7-202 | If the captured SHA is the full 40-char form, abbreviate it (e.g., `git rev-parse --short <full-sha>`) before emitting in step 5. | command | L1312 **Track at commit time (preferred).** | short SHA | full SHA | step 5 | git, Bash | no | procedure |
| F7-203 | Fallback — **Re-derive post-hoc**: resolve the commit per Issue by `--grep`-filtering session commits against the per-issue commit-message contract (subject ends with literal `(<issue-id>)`). | recovery | L1313 **Re-derive post-hoc.** | per-issue SHA | commit subjects | Section 7 step 2.4 | git | no | procedure |
| F7-204 | Run once per fixed Issue: `git log --reverse --format=%h -F --grep='(<issue-id>)' <pre-session-sha>..HEAD` (identical POSIX and PowerShell forms). | command | L1313-L1323 **Re-derive post-hoc.** | abbreviated SHA | `<issue-id>`; `<pre-session-sha>` | - | git, Bash | no | procedure |
| F7-205 | Substitute the literal Issue ID (e.g., `b.abc`) for `<issue-id>` and the session-start SHA captured at Section 8 step 1 for `<pre-session-sha>`. | command | L1325 **Re-derive post-hoc.** | - | issue ID; `<pre-session-sha>` | Section 8 step 1 | none | no | procedure |
| F7-206 | `-F` (`--fixed-strings`) makes the grep pattern literal so `.` and `(` `)` match as themselves, removing regex-vs-literal ambiguity. | rationale-only | L1325 **Re-derive post-hoc.** | - | - | - | none | no | rationale |
| F7-207 | Each `git log` invocation returns at most one match — the per-issue commit — and `%h` emits the abbreviated SHA so no follow-up `git rev-parse --short` is needed. | definition | L1325 **Re-derive post-hoc.** | - | - | - | git | no | procedure |
| F7-208 | Scoping each lookup by the parenthesized `(<issue-id>)` token is load-bearing: it filters out extra commits Section 8 step 6's "Fix in this session" branch may have landed, avoiding positional mis-pairing. | rationale-only | L1325 **Re-derive post-hoc.** | - | - | Section 7 step 2.4; Section 8 step 6 | none | no | rationale |
| F7-209 | Step 5: **Append a bullet** of the form `gh issue close <n> --repo <owner>/<repo> -c "Fixed in <sha>."` to the block, one bullet per matching `reference_materials` entry. | field-or-template | L1327 **Procedure:** step 5 | recommendation bullet | `<n>`, `<owner>`, `<repo>`, `<sha>` | - | none | no | procedure |
| F7-210 | Step 6: **Suppress the entire recommendation block** if zero bullets were collected. | precondition | L1329 **Procedure:** step 6 | - | bullet count | - | none | no | procedure |
| F7-211 | Equivalently, emit nothing when every fixed Issue's `reference_materials` is null/empty or carries only non-`github-issue` resolvers. | definition | L1329 **Procedure:** step 6 | - | `reference_materials` | - | none | no | procedure |
| F7-212 | Output shape: header line `Upstream GitHub Issues to consider closing:`, blank line, then `- gh issue close <n1> --repo <owner1>/<repo1> -c "Fixed in <commit-sha-1>."` bullets. | field-or-template | L1331-L1338 **Output shape (when at least one bullet was collected):** | recommendation block | bullets | - | none | no | procedure |
| F7-213 | Bullet ordering matches the run's fixed-issue iteration order (single-issue: one bullet; list / `all` mode: fixed-list order). | ordering | L1340 `### 9.` | - | fixed-issue list | - | none | no | procedure |
| F7-214 | When a single fixed Issue carries multiple `github-issue` entries in `reference_materials`, emit one bullet per matching entry, in array order. | ordering | L1340 `### 9.` | bullets | `reference_materials` array | - | none | no | procedure |
| F7-215 | The recommendation block is informational console output — NOT an `AskUserQuestion` gate; the user is not asked to confirm. | invariant | L1342 `### 9.` | console output | - | - | none | no | procedure |
| F7-216 | After emitting the block, the orchestrator yields. | ordering | L1342 `### 9.` | - | - | - | none | no | procedure |

## Anchors defined here

- L1114 `### 8. Post-Completion Review`
- L1118 **End-of-batch deferral-hygiene firing.**
- L1118 **end-of-batch firing**
- L1118 **When re-firing here, treat the invocation as the end-of-batch firing per Section 7.5's batch-firing semantics**
- L1120 **Anti-pattern callout — read before acting.**
- L1122 **Anti-pattern callout, second.**
- L1124 **Pre-session SHA** (manifest field)
- L1124 **Before trusting the Pre-session SHA, validate the manifest is this run's:**
- L1124 **Unit scope** (manifest field)
- L1126 **Agent tool with `subagent_type=general-purpose` and `run_in_background=true`**
- L1142 `PHASE 1 — Consume the compromise tracker (passed as a FILE PATH).`
- L1155 `PHASE 2 — Challenge each tracked compromise on its merits.`
- L1165 **no narrowing record**
- L1169 **left the unit's stated defect partly unfixed**
- L1175 `PHASE 3 — Ungated-pick plausibility check, on TWO axes.`
- L1192 `PHASE 4 — Fix-path-enumeration plausibility check.`
- L1203 `PHASE 5 — Holistic solution-quality judgment beyond the logged compromises`
- L1207 `PHASE 6 — Discrete-defect sweep (the final phase).`
- L1244 **Compromise-challenge preamble (rendered before presenting the findings).**
- L1244 **challenged** (inside the example preamble)
- L1246 **Orchestrator self-tracking close-out (mandatory before yielding).**
- L1250 **First** / **then** (two-step gate ordering)
- L1253 **Fix in this session**
- L1254 **File as issue tickets**
- L1255 **Skip**
- L1257 **`Fix in this session`** (recommendation)
- L1260 **Fix in this session** (step 6 branch)
- L1260 **post-completion-scoped**
- L1260 **no** (issue-id suffix)
- L1260 **One carve-out: a round discriminator IS permitted, and required, when a post-completion writer lane is re-dispatched after a movement abort**
- L1260 **An `engineer-postcomp-<n>` return carrying a `## Design question` is not a completion of that finding**
- L1260 **File as issue tickets** (route target)
- L1260 **Section 4's movement-report rung carries over with it**
- L1260 **carrying the same `<n>` as the lane it marks**
- L1260 **not** treat this Section 8 pass as finished / **not** commit
- L1262 **The rung's unexplained-movement gate applies here, but read its abort option's *whole body*, not just its title.**
- L1262 **That re-titling is not the only thing that changes: the option's body routes an aborted Issue through `#### Aborted-Issue close-out`, and none of that routing applies on this branch.**
- L1262 **this branch's OWN prefix sweep**
- L1262 **not** via `#### Aborted-Issue close-out`
- L1262 **continue Section 8's own flow**
- L1264 **Close out the `aborted-*-postcomp-<n>` markers with the rest.**
- L1264 **prefix**
- L1266 **The Engineer-dispatch precondition applies here too, on the post-completion names.**
- L1266 **the orchestrator MUST NOT dispatch an `engineer-postcomp-<n>` Agent while any `test-writer-postcomp-*` or `doc-writer-postcomp-*` TaskList task is `pending` or `in_progress`**
- L1266 **every** (finding index)
- L1268 **Order the lanes when one finding needs both a source change and a test/doc change.**
- L1268 **Engineer**
- L1269 **File as issue tickets** (step 6 branch)
- L1270 **Skip** (step 6 branch)
- L1272 **Compromise-challenge recovery gates.**
- L1272 **before or alongside**
- L1272 **One challenge class has no recovery gate, by design: the accepted-`blocker`-or-unnarrowed-deferral contract violation PHASE 2 emits.**
- L1272 **step 5's Fix / File / Skip gate alone**
- L1272 **`Fix in this session`** (recommendation)
- L1272 **aggregate**
- L1272 **first** / **then** (two-step gate ordering)
- L1272 **fires per challenged finding**
- L1272 **firing order equals the reviewer's emission order**
- L1274 **SR-6.7 ungated-route recovery gate.**
- L1274 **The pinned strings read generically across both axes:**
- L1274 **the routing misjudgment**
- L1275 **original Trigger C tracker entry's `Follow-up Issue` field is updated in place**
- L1276 **Trigger D**
- L1278 **SR-4.6 under-enumeration recovery gate.**
- L1278 **same three-choice gate shape**
- L1280 **Trigger D**
- L1285 `### 9. Recommend upstream GitHub close commands`
- L1287 **before yielding the turn**
- L1287 **Pure recommendation — the skill NEVER runs `gh issue close` itself.**
- L1289 **not** generalize
- L1291 **Procedure:**
- L1293 **Iterate the run's fixed-issue list**
- L1295 **For each fixed Issue, read its `reference_materials`.**
- L1309 **For each `reference_materials` entry whose `resolver == "github-issue"`**
- L1311 **Capture the per-issue commit SHA**
- L1312 **Track at commit time (preferred).**
- L1313 **Re-derive post-hoc.**
- L1327 **Append a bullet**
- L1329 **Suppress the entire recommendation block**
- L1331 **Output shape (when at least one bullet was collected):**

## Rationale-only spans

- L1118 (final two sentences, "The active set … batch-level reconciliation.") — defensive-sweep justification for end-of-batch firing (~1 line-fragment).
- L1120 (parenthetical "e.g. `/quo-engineer-review` is scoped … this step needs") — why lane skills are wrong here (~1 line-fragment).
- L1122 (sentence 2, "By construction … 'is this good?'") — team-lead context bias explanation (~1 line-fragment).
- L1124 (clause "the stale/foreign-manifest tell … in this repository") — foreign-manifest failure narrative (~1 line-fragment).
- L1198–L1201 — PHASE 3 vs PHASE 4 complementarity (4 lines).
- L1233–L1235 (sentence "The depth tag is informative here…") — why depth is informative post-completion (~2 lines).
- L1262 (sentence "Running that close-out from here would … already under way.") — consequences of misrouting to Aborted-Issue close-out (~1 line-fragment).
- L1266 (sentence "The reason is the one Section 4 gives…") — writer-rework justification (~1 line-fragment).
- L1272 (sentence "There is nothing to recover — … partly unfixed") and final sentence ("These gates inherit a prose-adherence fragility…") — why no PHASE 2 gate; residual fragility (~2 line-fragments).
- L1287 (sentence "No `gh` auth assumption … authenticated.") — why recommendation-only (~1 line-fragment).
- L1289 (sentences 2–3) — why linear-issue/url excluded (~1 line).
- L1325 (sentences "The `-F` flag …" and "Scoping each lookup … positional bulk-log pairing.") — grep literalness and uniqueness justification (~1 line, most of the paragraph).

Approximate total rationale-only text: about 15 physical lines out of 229 (most rationale is embedded mid-paragraph in long single lines rather than in standalone line ranges; L1198–L1201 is the only pure multi-line span).
