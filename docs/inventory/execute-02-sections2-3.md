# Rule inventory — quo-execute SKILL.md, Sections 2–3 (L228–L520)

Source: `/Users/jesseg/code/quorum/skills/quo-execute/SKILL.md` lines 228–520.

| ID | Rule | Kind | Site | Produces | Consumes | Cross-refs | Env | Mirror | Class |
|---|---|---|---|---|---|---|---|---|---|
| E2-001 | The multi-Epic run-mode gate fires through the two-step `TaskCreate` → `AskUserQuestion` contract. | gate | L230, `### 2. Find Epic to work on and validate` | `gate-askuserquestion-<short-suffix>` TaskList task; AskUserQuestion call | - | `#### Pick a multi-Epic run mode (only when more than one Epic is in scope)`; Section 3's TaskList naming convention (gate-task entry) | TaskList, AskUserQuestion | no | procedure |
| E2-002 | First `TaskCreate` a `gate-askuserquestion-<short-suffix>` TaskList task naming the gate, then call `AskUserQuestion` in the same turn. | ordering | L230, `### 2.` | `gate-askuserquestion-<short-suffix>` task | - | Section 3's TaskList naming convention's gate-task entry | TaskList, AskUserQuestion | no | procedure |
| E2-003 | Mark the `gate-*` task `completed` the moment the user's answer is consumed. | ordering | L230, `### 2.` | status flip on `gate-*` task | AskUserQuestion result | - | TaskList | unknown | procedure |
| E2-004 | Find all Epics under the chosen Bee with `bees execute-freeform-query --query-yaml 'stages: - [parent=<bee-id>, type=t1] report: [title, ticket_status, up_dependencies]'`. | command | L232-L238, `### 2.` | Epic candidate result set | `<bee-id>` | - | bees, Bash | no | procedure |
| E2-005 | Recommend the best Epic to work on first from the result set. | procedure/relay | L232, `### 2.` | recommendation to user | Epic result set | - | bees | no | procedure |
| E2-006 | The Epic to work on must have status `ready` or `in_progress`. | precondition | L240-L241, `### 2.` | - | `ticket_status` | - | bees | no | procedure |
| E2-007 | The Epic to work on must have all `up_dependencies` in `done` state. | precondition | L240-L242, `### 2.` | - | `up_dependencies`, dependency `ticket_status` | - | bees | no | procedure |
| E2-008 | `up_dependencies` is returned as a list of ticket IDs only, not statuses. | definition | L244, `### 2.` | - | query result | - | bees | no | procedure |
| E2-009 | Collect dependency IDs across all candidate Epics, then batch-look-up their statuses with `bees show-ticket --ids <dep-id-1> <dep-id-2> <...>`. | command | L244-L249, `### 2.` | dependency statuses | `up_dependencies` IDs | - | bees, Bash | no | procedure |
| E2-010 | An Epic is workable only if all its `up_dependencies` are in `done` status. | invariant | L251, `### 2.` | - | dependency `ticket_status` | - | bees | no | procedure |
| E2-011 | An Epic with no `up_dependencies` is unblocked by default. | definition | L251, `### 2.` | - | `up_dependencies` | - | - | no | procedure |
| E2-012 | Before starting the first Epic, count the Epics returned by the `[parent=<bee-id>, type=t1]` query — the full set, not just the workable subset. | precondition | L255, `#### Pick a multi-Epic run mode (only when more than one Epic is in scope)` | Epic count | Epic query result | - | bees | no | procedure |
| E2-013 | Epics in `drafted`/`ready`/`in_progress` plus any already `done` all count toward "scope". | definition | L255, `#### Pick a multi-Epic run mode` | - | Epic statuses | - | - | no | procedure |
| E2-014 | If two or more Epics exist and at least one is workable (`ready` / `in_progress`) or `drafted`, present a one-time mode choice with `AskUserQuestion`. | gate | L255, `#### Pick a multi-Epic run mode` | AskUserQuestion call | Epic count, statuses | Section 4.2 branch 3 | AskUserQuestion, TaskList | no | procedure |
| E2-015 | If every Epic under the Bee is already `done`, skip the mode question entirely. | precondition | L255, `#### Pick a multi-Epic run mode` | - | Epic statuses | Section 4.2 branch 3 | - | no | procedure |
| E2-016 | Section 4.2 branch 3 exits the run with no Epic boundary crossed when all Epics are `done`. | definition | L255, `#### Pick a multi-Epic run mode` | - | - | Section 4.2 branch 3 | - | no | procedure |
| E2-017 | Use the question text "How should this run handle multiple Epics? (You will not be asked again this run.)". | field-or-template | L257, `#### Pick a multi-Epic run mode` | AskUserQuestion question text | - | - | AskUserQuestion | no | procedure |
| E2-018 | Offer option `Stop after each Epic` — pause at every Epic boundary for review and approval of continuation. | choice-set | L259, `#### Pick a multi-Epic run mode` | choice label | - | Section 4.2 branch 2 | AskUserQuestion | no | procedure |
| E2-019 | Under `Stop after each Epic`, Section 4.2 branch 2 asks "do you want to continue with the next logical Epic?" between each Epic. | definition | L259, `#### Pick a multi-Epic run mode` | - | multi-Epic run mode | Section 4.2 branch 2 | AskUserQuestion | no | procedure |
| E2-020 | Offer option `Work through all Epics` — auto-continue across Epics; stop only when proceeding without input risks concrete downstream cost. | choice-set | L260, `#### Pick a multi-Epic run mode` | choice label | - | - | AskUserQuestion | no | procedure |
| E2-021 | Mode 2 still pauses on Section 4.2 branch 1's drafted-or-blocked-on-drafted Epic stop; the loop must exit so the user can run `/quo-breakdown-epic`. | invariant | L260, `#### Pick a multi-Epic run mode` | - | multi-Epic run mode | Section 4.2 branch 1; `/quo-breakdown-epic` | - | no | procedure |
| E2-022 | Mode 2 still pauses on any final reviewer-surfaced blocker the orchestrator escalates from Sections 5 and 6. | invariant | L260, `#### Pick a multi-Epic run mode` | - | multi-Epic run mode | Sections 5 and 6 | - | no | procedure |
| E2-023 | Mode 2 still performs Section 4.2's Epic-boundary state-externalization checkpoint at every Epic boundary. | invariant | L260, `#### Pick a multi-Epic run mode` | - | - | Section 4.2's **Epic-boundary state-externalization checkpoint** | - | no | procedure |
| E2-024 | The checkpoint is a set of verify-and-write steps the orchestrator runs itself, not a pause; it never prompts the user. | definition | L260, `#### Pick a multi-Epic run mode` | - | - | Epic-boundary state-externalization checkpoint | - | no | procedure |
| E2-025 | Capture the user's choice once and store it as the **multi-Epic run mode** for the rest of the run. | field-or-template | L262, `#### Pick a multi-Epic run mode` | `multi-Epic run mode` value | AskUserQuestion result | - | AskUserQuestion | no | procedure |
| E2-026 | The choice persists across Epic boundaries; do not re-prompt at every Epic. | invariant | L262, `#### Pick a multi-Epic run mode` | - | multi-Epic run mode | - | - | no | procedure |
| E2-027 | Section 4.2's branch-2 logic branches on the captured multi-Epic run mode value. | definition | L262, `#### Pick a multi-Epic run mode` | - | multi-Epic run mode | Section 4.2 branch 2 | - | no | procedure |
| E2-028 | If only one Epic exists under the Bee when this step runs, skip the mode question entirely. | precondition | L264, `#### Pick a multi-Epic run mode` | - | Epic count | - | - | no | procedure |
| E2-029 | Rewrite the run-state manifest as soon as the Epic query has returned and the mode gate has either fired or been skipped. | ordering | L268, `#### Resolve the manifest placeholders` | run-state manifest rewrite | Epic query result; gate outcome | Section 1 `#### Write the run-state manifest` | none (file write) | no | procedure |
| E2-030 | This rewrite is unconditional — it runs on all three paths through the gate, not only when a mode was captured. | invariant | L268, `#### Resolve the manifest placeholders` | manifest rewrite | - | Section 1 | none | no | procedure |
| E2-031 | The literal placeholder `pending Section 2 query` in `Epics in scope` must not survive into any Epic's work. | invariant | L268, `#### Resolve the manifest placeholders` | - | `Epics in scope` field | Section 1 | none | no | procedure |
| E2-032 | Write **Epics in scope** as the real Epic IDs returned by the `[parent=<bee-id>, type=t1]` query, on every path. | field-or-template | L270, `#### Resolve the manifest placeholders` | `Epics in scope` field | Epic query result | - | none | no | procedure |
| E2-033 | Write **Multi-Epic run mode** as the captured choice whenever the gate fired at any point in this run, not only on this pass. | field-or-template | L271, `#### Resolve the manifest placeholders` | `Multi-Epic run mode` field | captured mode | - | none | no | procedure |
| E2-034 | Section 2's gate is one-time; on a loop-re-entry pass it does not re-fire; the mode already in the manifest is written back unchanged. | invariant | L271, `#### Resolve the manifest placeholders` | `Multi-Epic run mode` field | manifest | - | none | no | procedure |
| E2-035 | Only when the gate has never fired does the field take one of two skip literals, chosen by a precedence rule. | field-or-template | L271, `#### Resolve the manifest placeholders` | `Multi-Epic run mode` field | gate history | - | none | no | procedure |
| E2-036 | Write `not captured (all Epics already done)` whenever every Epic under the Bee reads `done`. | field-or-template | L271, `#### Resolve the manifest placeholders` | `Multi-Epic run mode` literal | Epic statuses | - | none | no | procedure |
| E2-037 | Reserve `not captured (single Epic in scope)` for exactly one Epic under the Bee that is not yet `done`. | field-or-template | L271, `#### Resolve the manifest placeholders` | `Multi-Epic run mode` literal | Epic count, status | - | none | no | procedure |
| E2-038 | When a Bee holds exactly one already-`done` Epic, the all-done literal wins so the same Bee state always produces the same literal. | invariant | L271, `#### Resolve the manifest placeholders` | - | Epic count, status | - | none | no | rationale |
| E2-039 | On loop re-entry, carry the accumulated manifest fields forward — do not re-emit Section 1's template. | invariant | L273, `#### Resolve the manifest placeholders` | manifest rewrite | manifest | Section 1; Section 4.2 branch 2 | none | no | procedure |
| E2-040 | Section 4.2 branch 2 returns to Section 2 for each subsequent Epic, so the rewrite runs again on every pass after the first. | definition | L273, `#### Resolve the manifest placeholders` | - | - | Section 4.2 branch 2 | none | no | procedure |
| E2-041 | On re-entry passes the rewrite is a two-field refresh, not a fresh run-start write. | invariant | L273, `#### Resolve the manifest placeholders` | manifest rewrite | - | - | none | no | procedure |
| E2-042 | `Read` the current manifest first and write back **Progress**, **Next unit**, **Isolation strategy**, **Pre-Bee SHA**, **Compromise tracker**, and captured **Multi-Epic run mode** unchanged. | ordering | L273, `#### Resolve the manifest placeholders` | manifest rewrite | manifest fields | - | none (Read) | no | procedure |
| E2-043 | On re-entry touch only `Epics in scope` and `Multi-Epic run mode` (in practice only `Epics in scope`). | invariant | L273, `#### Resolve the manifest placeholders` | manifest fields | - | - | none | no | procedure |
| E2-044 | Re-emitting Section 1's blank template would blank **Progress** and **Next unit**, destroying the `last commit <sha>` values the checkpoint's step 1 reads as lower bound. | rationale-only | L273, `#### Resolve the manifest placeholders` | - | `last commit <sha>` | Section 4.2's Epic-boundary checkpoint step 1 | none | no | rationale |
| E2-045 | The manifest, not the conversation, is where the run reads mode and scope back after a compaction. | rationale-only | L275, `#### Resolve the manifest placeholders` | - | manifest | - | none | no | rationale |
| E2-046 | If the Epic has `up_dependencies` that have been completed, review the work actually done in those Epics to check whether this Epic description is stale. | precondition | L278-L279, `#### Check if stale` | - | `up_dependencies`, prior Epic work | - | bees, git | no | procedure |
| E2-047 | Staleness step 1: review the git diff to understand what was actually implemented. | ordering | L281, `#### Check if stale` | - | git diff | - | git | no | procedure |
| E2-048 | Staleness step 2: read the upcoming Epic and its Tasks/Subtasks. | ordering | L282, `#### Check if stale` | - | Epic, Task, Subtask bodies | - | bees | no | procedure |
| E2-049 | Staleness step 3: update any Task or Subtask descriptions now stale given prior Epics (paths changed, signatures differ, new modules). | ordering | L283, `#### Check if stale` | updated ticket bodies | git diff, ticket bodies | - | bees | no | procedure |
| E2-050 | If ready, mark the Epic `status=in_progress` to show work has started. | command | L287, `#### Mark status when ready to start work` | Epic status flip | Epic readiness | - | bees | no | procedure |
| E2-051 | The orchestrator (the Director) drives Tasks through a **reconciliation loop** dispatching fresh, ephemeral background `Agent` invocations against the sibling `agents/` custom subagent types. | definition | L291, `### 3. Execute Tasks via per-Subtask Agent dispatch` | Agent dispatches | `agents/` directory | `agents/` | Agent | unknown | procedure |
| E2-052 | There is no long-lived team, no warmed Agents, and no peer-to-peer messaging between workers. | invariant | L291, `### 3.` | - | - | - | Agent | unknown | procedure |
| E2-053 | The loop is event-driven, not clock-driven; each tick has three phases (Read state, Reconcile, Yield). | definition | L295, `#### Reconciliation loop` | - | - | - | Agent | unknown | procedure |
| E2-054 | Phase 1 (Read state): pull current truth from four sources — bees, TaskList, git state, run-state manifest — before deciding what to do. | ordering | L297, `#### Reconciliation loop` | - | bees, TaskList, git, manifest | - | bees, TaskList, git | unknown | procedure |
| E2-055 | Use `bees show-ticket --ids <epic-id>` to get the Epic's `children` array (Task IDs). | command | L298, `#### Reconciliation loop` | Task ID list | `children` | - | bees | no | procedure |
| E2-056 | For each Task, fetch its full details including its own `children` array (Subtasks). | command | L298, `#### Reconciliation loop` | Subtask ID list | `children` | - | bees | no | procedure |
| E2-057 | Read every Subtask body (Context, What Needs to Change, Key Files, Acceptance Criteria) since these carry the instructions the dispatched Agent will follow. | ordering | L298, `#### Reconciliation loop` | - | Subtask bodies | - | bees | no | procedure |
| E2-058 | Sort Tasks in dependency order by checking each Task's `up_dependencies`. | ordering | L298, `#### Reconciliation loop` | ordered Task list | `up_dependencies` | - | bees | no | procedure |
| E2-059 | Verify at least one Task exists with at least one Subtask and all are non-drafted (`status!=drafted`). | precondition | L298, `#### Reconciliation loop` | - | Task/Subtask statuses | - | bees | no | procedure |
| E2-060 | Use `bees execute-freeform-query --query-yaml 'stages: - [id=<ticket-id>] report: [title, ticket_status]'` for any focused state query. | command | L298-L304, `#### Reconciliation loop` | ticket status | `<ticket-id>` | - | bees, Bash | unknown | procedure |
| E2-061 | Each in-flight Agent has a corresponding TaskList task whose `status` is `pending` (queued), `in_progress` (running), or `completed` (Agent reported done). | definition | L305, `#### Reconciliation loop` | - | TaskList | "TaskList as progress UI" | TaskList | unknown | procedure |
| E2-062 | Git state (the diff on disk) is the only authoritative record of what workers actually did. | invariant | L306, `#### Reconciliation loop` | - | git diff | - | git | unknown | procedure |
| E2-063 | The run-state manifest holds multi-Epic run mode, isolation strategy, `<pre-bee-sha>`, compromise-tracker path, per-Epic progress, and next unit. | definition | L307, `#### Reconciliation loop` | - | manifest | Section 1 `#### Write the run-state manifest` | none | no | procedure |
| E2-064 | `Read` the manifest at `<tempdir>/.quorum/run-state-quo-execute-<bee-id>.md`. | command | L307, `#### Reconciliation loop` | - | manifest file | - | none (Read) | no | procedure |
| E2-065 | The manifest path is derived from this skill's name plus the Bee ID so it stays findable when nothing survives in conversation. | rationale-only | L307, `#### Reconciliation loop` | - | - | - | none | no | rationale |
| E2-066 | Conversation memory is never a substitute for the four sources; re-read rather than recall ticket status, landed commits, run mode, pre-run SHA. | invariant | L309, `#### Reconciliation loop` | - | bees, TaskList, git, manifest | - | bees, TaskList, git | unknown | procedure |
| E2-067 | The re-read rule is unconditional — it holds on every tick, not only after something goes wrong. | invariant | L309, `#### Reconciliation loop` | - | - | - | - | unknown | procedure |
| E2-068 | If a summarization marker is visible in the conversation, treat everything before it as non-authoritative and re-read all four sources in full before dispatching anything else. | recovery | L309, `#### Reconciliation loop` | - | summarization marker; four sources | - | bees, TaskList, git | unknown | procedure |
| E2-069 | Mark the current Task `status=in_progress` and the Bee `status=in_progress` (if not already set) the first time a Task starts. | command | L311, `#### Reconciliation loop` | Task and Bee status flips | - | - | bees | no | procedure |
| E2-070 | Phase 2 (Reconcile): for every Subtask whose dependencies are satisfied and has no Agent in flight, dispatch a fresh Agent. | ordering | L313-L314, `#### Reconciliation loop` | Agent dispatch | Subtask dependencies, TaskList | "Per-Subtask cold dispatch" | Agent, TaskList | no | procedure |
| E2-071 | For every Agent that reported completion: confirm the bees ticket transitioned to `status=done`, mark its TaskList task `completed`, unlock newly-eligible downstream Subtasks. | ordering | L315, `#### Reconciliation loop` | TaskList status flip; unlocks | Agent completion notification; bees status | - | bees, TaskList, Agent | unknown | procedure |
| E2-072 | A Test Writer or Doc Writer return reporting it *stopped* on detected source movement is not a completion; route it through the movement-report rung; it unlocks nothing. | invariant | L315, `#### Reconciliation loop` | - | writer return | movement-report rung | Agent | unknown | procedure |
| E2-073 | The movement rung closes the writer's own TaskList task but opens an `aborted-*` task in its place; the Task does not advance while that task is `pending`. | invariant | L315, `#### Reconciliation loop` | `aborted-*` task | - | - | TaskList | unknown | procedure |
| E2-074 | `agents/test-writer.md` and `agents/doc-writer.md` instruct the writer to stop mid-run and report when the source its work was pinned to moved. | definition | L316, `#### Reconciliation loop` (movement report) | - | writer return | `agents/test-writer.md`, `agents/doc-writer.md` | Agent | unknown | procedure |
| E2-075 | The Test Writer detects movement with a `git rev-parse HEAD` + `git hash-object` fingerprint at start and before finishing; the Doc Writer (no `Bash`) re-reads with `Read` / `Grep`. | definition | L316, movement report | - | - | `agents/test-writer.md`, `agents/doc-writer.md` | git | unknown | procedure |
| E2-076 | Without a movement-report receiver the orchestrator would mark the task `completed` and unlock a per-Task PM review over unfinished tests or docs. | rationale-only | L316, movement report | - | - | - | - | unknown | rationale |
| E2-077 | Read the writer's return before persisting anything. | ordering | L318, movement report — **Recognising it, and the execute-mode narrowing** | - | writer return | - | Agent | unknown | procedure |
| E2-078 | In execute mode, movement confined to a sibling Subtask's own files is designed concurrency; the writer records it as an observation and finishes normally. | definition | L318, movement report | - | writer return | - | - | no | procedure |
| E2-079 | Persist a sibling-movement observation return as a completed return and carry the observation into the Task summary. | ordering | L318, movement report | Task summary entry | writer return | - | TaskList, bees | no | procedure |
| E2-080 | A movement report is the case where the writer stopped because the files its own Subtask pins moved. | definition | L318, movement report | - | writer return | - | - | unknown | procedure |
| E2-081 | What distinguishes observation from movement report is whether the writer stopped, not whether it mentioned movement. | invariant | L318, movement report | - | writer return | - | - | unknown | procedure |
| E2-082 | Do NOT unlock anything downstream of an aborted writer lane — it has not delivered. | invariant | L320, movement report — **What to do** | - | - | - | TaskList | unknown | procedure |
| E2-083 | Record the owed redelivery as a durable TaskList entry, the same obligation-as-pending-task pattern `defer-*` and `gate-*` use. | ordering | L320, movement report | `aborted-*` task | - | `defer-*`, `gate-*` | TaskList | unknown | procedure |
| E2-084 | Rung step 1: mark the writer's own TaskList task `completed` — `test-writer-<subtask-id>` / `doc-writer-<subtask-id>` (or `<role>-<bee-id>` for Section 5 re-dispatch), including any `-r<n>` suffix. | ordering | L322, movement report | TaskList status flip | writer task name | Section 5 | TaskList | unknown | procedure |
| E2-085 | The writer's Agent has exited, so leaving its task active would strand a task no completion notification will ever clear. | rationale-only | L322, movement report | - | - | - | TaskList | unknown | rationale |
| E2-086 | Rung step 2: create a new `aborted-<role>-<id>` task — `aborted-test-writer-<subtask-id>` / `aborted-doc-writer-<subtask-id>`, or `aborted-<role>-<bee-id>` when Bee-scoped — matching the aborted lane's scope. | name-class | L323, movement report | `aborted-*` TaskList task | aborted lane scope | naming convention below | TaskList | unknown | procedure |
| E2-087 | Create the `aborted-*` task with status `pending` and the writer's "how far I got" report as its `metadata.activity` string. | field-or-template | L323, movement report | `aborted-*` task fields | writer return | - | TaskList | unknown | procedure |
| E2-088 | The pending `aborted-*` task is the redelivery-owed marker; the orchestrator reads it off the TaskList next tick rather than holding it in conversation. | invariant | L323, movement report | - | TaskList | - | TaskList | unknown | procedure |
| E2-089 | Routing tests the `aborted-*` task's name prefix and status; `metadata.activity` is informational context only, never a routing input. | invariant | L323, movement report | - | task name, status | - | TaskList | unknown | procedure |
| E2-090 | Rung step 3: the Task must not advance while any `aborted-*` task for it is `pending`. | invariant | L324, movement report | - | `aborted-*` status | "all Subtasks `done`" test | TaskList | unknown | procedure |
| E2-091 | The aborted lane's bees Subtask must not reach `status=done`; a flip set by the writer or by the orchestrator per `agents/doc-writer.md`'s no-`Bash` routing is premature. | invariant | L324, movement report | - | Subtask status | `agents/doc-writer.md` | bees | unknown | procedure |
| E2-092 | When the premature `done` flip already landed, undo it with `bees update-ticket --ids <subtask-id> --status in_progress` (identical on POSIX and PowerShell). | recovery | L324-L334, movement report | Subtask status flip | Subtask status | - | bees, Bash | unknown | procedure |
| E2-093 | `agents/test-writer.md` orders its closing fingerprint reading before the `done` flip and forbids the flip on abort, so the correction is rare; run it when state says otherwise, do not assume. | precondition | L336, movement report | - | Subtask status | `agents/test-writer.md` | bees | unknown | procedure |
| E2-094 | Bee-scoped case: when the aborted lane was a Section 5 Bee-scoped re-dispatch (`test-writer-<bee-id>` / `doc-writer-<bee-id>`), the bees corrective is a no-op — skip it entirely. | precondition | L338, movement report — **Bee-scoped case — the bees corrective is a no-op** | - | lane scope | Section 5; naming convention below | bees | unknown | procedure |
| E2-095 | Bee-scoped re-dispatches answer a Bee-level finding spanning the whole diff and carry no `t3` ticket of their own. | definition | L338, movement report | - | - | Section 5 | - | unknown | procedure |
| E2-096 | On the Bee-scoped path do not hunt for a Subtask to demote, and do not demote a Task or Epic the Bee-level diff touches. | invariant | L338, movement report | - | - | - | bees | unknown | procedure |
| E2-097 | The Bee-scoped `aborted-<role>-<bee-id>` marker holds Section 5's Bee-level review loop from closing; that gate is TaskList-derived so the marker alone is the whole mechanism. | invariant | L338, movement report | - | `aborted-*` status | Section 5 | TaskList | unknown | procedure |
| E2-098 | Re-dispatch the aborted lane according to who moved the source (Engineer, concurrently-dispatched Test Writer, or external actor). | ordering | L340, movement report | re-dispatch | movement attribution | - | Agent | unknown | procedure |
| E2-099 | When the mover was an Engineer, the source must reach a settled state first: let that Engineer's lane finish before re-dispatching the writer. | ordering | L342, movement report | - | Engineer lane status | - | Agent, TaskList | unknown | procedure |
| E2-100 | When the Engineer movement came from a review-finding re-dispatch, let the code review round for that site close per part (g)'s ordering before re-dispatching. | ordering | L342, movement report | - | review round status | part (g) of `### Orchestrator discipline: routing review findings` | TaskList | unknown | procedure |
| E2-101 | The closing review is the Bee-level Code Reviewer when the finding came from Section 5's site (Clause 2), or the per-Task PM's in-flight `/quo-engineer-review` pass for the per-Task site (Clause 1). | definition | L342, movement report | - | - | Section 5; Clause 1; Clause 2; `/quo-engineer-review` | - | unknown | procedure |
| E2-102 | The review-round wait is elided when that Engineer round was the final `trivial-tweak` nit pass, per part (g). | precondition | L342, movement report | - | round type | part (g) | - | unknown | procedure |
| E2-103 | Re-dispatching a writer into a still-moving diff reproduces the abort. | rationale-only | L342, movement report | - | - | - | - | unknown | rationale |
| E2-104 | The `aborted-*` task holds the Task's advance shut throughout but does not block the Engineer round — it is a different name class from part (g)'s prefixes. | invariant | L342, movement report | - | `aborted-*` status | part (g) | TaskList | unknown | procedure |
| E2-105 | The concurrently-dispatched Test Writer candidate set is any sibling Subtask's Test Writer or the Bee-scoped `test-writer-<bee-id>` when the aborted lane is a Section 5 re-dispatch. | definition | L343, movement report | - | in-flight/returned Test Writer dispatches | Section 5 | Agent | unknown | procedure |
| E2-106 | Section 5 can re-dispatch `test-writer-<bee-id>` and `doc-writer-<bee-id>` concurrently. | definition | L343, movement report | - | - | Section 5 | Agent | unknown | procedure |
| E2-107 | `agents/test-writer.md` sanctions perturbing a source file in place with `Edit` / `Write` (restoring from a scratch copy) and requires its return to list every perturbed path under `## Perturbations`, `None` when none. | definition | L343, movement report | - | Test Writer return | `agents/test-writer.md`; `## Perturbations` | - | unknown | procedure |
| E2-108 | Read the `## Perturbations` heading on the Test Writer returns held; when one lists every moved path, re-dispatch the aborted lane once that sibling has returned, with no gate. | ordering | L343, movement report | re-dispatch | `## Perturbations` list; writer movement report | - | Agent | unknown | procedure |
| E2-109 | When `## Perturbations` covers only some of the moved paths, fall through to the external-actor case for the remainder. | ordering | L343, movement report | - | `## Perturbations` list | external-actor case | - | unknown | procedure |
| E2-110 | When a concurrently-dispatched Test Writer has not returned yet, do not classify and do not fire the gate; wait for its completion notification and classify on that tick. | ordering | L343, movement report | - | Test Writer in-flight status | - | Agent | unknown | procedure |
| E2-111 | Once every concurrently-dispatched Test Writer has returned and none lists the moved path, fall through to the external-actor case. | ordering | L343, movement report | - | `## Perturbations` lists | external-actor case | - | unknown | procedure |
| E2-112 | When the mover was an external actor, re-dispatch the writer once the movement is understood — once the orchestrator has read the current diff and sees the tree has settled. | ordering | L344, movement report | re-dispatch | git diff | - | git, Agent | unknown | procedure |
| E2-113 | When the movement is unexplained, fire the **unexplained-movement gate** instead of re-dispatching into it. | gate | L344, movement report | gate | movement attribution | unexplained-movement gate | AskUserQuestion, TaskList | unknown | procedure |
| E2-114 | The writer re-dispatch takes the next `-r<n>` round-discriminated TaskList name per the naming convention. | name-class | L346, movement report | `<role>-<id>-r<n>` task | re-dispatch count | naming convention below | TaskList | unknown | procedure |
| E2-115 | Carry the writer's "how far I got" report (the `aborted-*` task's `metadata.activity`) into the re-dispatch prompt. | relay | L346, movement report | dispatch prompt content | `aborted-*` `metadata.activity` | - | Agent, TaskList | unknown | procedure |
| E2-116 | When the re-dispatched lane returns a normal deliverable, mark the `aborted-*` task `completed` — that releases the Task's advance. | ordering | L346, movement report | `aborted-*` status flip | re-dispatched return | - | TaskList | unknown | procedure |
| E2-117 | If the re-dispatched lane aborts on movement again, repeat the rung from the top: mark its own `-r<n>` task `completed` and refresh the existing `aborted-*` task's `metadata.activity`, never creating a second. | recovery | L346, movement report | TaskList updates | writer return | - | TaskList | unknown | procedure |
| E2-118 | Movement is unexplained when no dispatched Engineer's files overlap the writer's Subtask over the reported window, no returned Test Writer's `## Perturbations` covers the paths, none remain in flight, and the orchestrator cannot otherwise account for the change. | definition | L348, movement report — **Unexplained-movement gate** | - | dispatch history; `## Perturbations`; git diff | - | Agent, git | unknown | procedure |
| E2-119 | Do not re-dispatch blindly into unexplained movement — a blind re-dispatch loop is the failure mode this branch prevents. | invariant | L348, **Unexplained-movement gate** | - | - | - | - | unknown | procedure |
| E2-120 | Fire the unexplained-movement gate via two-step contract: first create a `gate-askuserquestion-<short-suffix>` task naming this gate, then call `AskUserQuestion` in the same turn. | gate | L348, **Unexplained-movement gate** | `gate-askuserquestion-<short-suffix>` task; AskUserQuestion | - | TaskList naming convention below | TaskList, AskUserQuestion | unknown | procedure |
| E2-121 | Do not produce a text response describing the gate — fire the two calls. | invariant | L348, **Unexplained-movement gate** | - | - | - | TaskList, AskUserQuestion | unknown | procedure |
| E2-122 | Do not yield to the harness while the `gate-*` task is `pending` or `in_progress` (yield-control discipline). | invariant | L348, **Unexplained-movement gate** | - | `gate-*` status | - | TaskList | unknown | procedure |
| E2-123 | The gate question text carries the writer's movement report verbatim: files moved, opening and closing HEAD when HEAD moved, and how far the work got. | field-or-template | L350, **Unexplained-movement gate** | question text | writer movement report | - | AskUserQuestion | unknown | procedure |
| E2-124 | The question adds the one-line statement: "the orchestrator dispatched no Engineer for this unit while the writer was running, and no returned concurrently-dispatched Test Writer's `## Perturbations` list names the moved paths, so it cannot attribute the movement." | field-or-template | L350, **Unexplained-movement gate** | question text | - | - | AskUserQuestion | unknown | procedure |
| E2-125 | Offer exactly three options; the gate is multi-choice only — do not add fake free-text options duplicating `Type something.` / `Chat about this`. | choice-set | L350, **Unexplained-movement gate** | option list | - | - | AskUserQuestion | unknown | procedure |
| E2-126 | Option `Re-dispatch the writer now` — movement understood or accepted; re-dispatch the lane under its next `-r<n>` name against the current tree. | choice-set | L352, **Unexplained-movement gate** | re-dispatch | AskUserQuestion result | - | AskUserQuestion, Agent | unknown | procedure |
| E2-127 | Option `Wait` — operator resolves the external change first; the orchestrator yields control. | choice-set | L353, **Unexplained-movement gate** | yield | AskUserQuestion result | - | AskUserQuestion | unknown | procedure |
| E2-128 | After `Wait`, re-fire this gate only on the operator's reply — that reply is the only trigger. | invariant | L353, **Unexplained-movement gate** | fresh gate | operator reply | - | AskUserQuestion, TaskList | unknown | procedure |
| E2-129 | Do not read `Wait` as "no Agent is in flight": sibling Subtask lanes can return completion notifications seconds after the operator picked Wait. | definition | L353, **Unexplained-movement gate** | - | - | - | Agent | no | procedure |
| E2-130 | A sibling lane's completion notification during Wait is a tick that processes that lane normally through Reconcile (persist, mark `completed`, unlock) and MUST NOT re-fire this gate. | invariant | L353, **Unexplained-movement gate** | normal lane processing | Agent completion notification | Reconcile step | Agent, TaskList | no | procedure |
| E2-131 | Re-fire on the operator's reply, never on an Agent notification; the rule is derivable from the tick's trigger and needs no bookkeeping. | invariant | L353, **Unexplained-movement gate** | - | tick trigger | - | - | unknown | procedure |
| E2-132 | Do not record the Wait state in `metadata.activity` and route on it — that string is informational, never a routing input. | invariant | L353, **Unexplained-movement gate** | - | - | - | TaskList | unknown | procedure |
| E2-133 | During Wait the `aborted-*` task stays `pending`, so the Task's advance stays shut. | invariant | L353, **Unexplained-movement gate** | - | `aborted-*` status | - | TaskList | unknown | procedure |
| E2-134 | Option `Abort this unit` — leave every bees ticket at the status the abort found it at and do not advance. | choice-set | L354, **Unexplained-movement gate** | - | AskUserQuestion result | - | AskUserQuestion, bees | unknown | procedure |
| E2-135 | For a Subtask-scoped writer lane, Abort holds back the writer's Subtask and its parent Task; for a Section 5 Bee-scoped lane there is nothing to hold back — every ticket is legitimately `done` and the Bee-level review stops. | definition | L354, **Unexplained-movement gate** | - | lane scope | Section 5 | bees | unknown | procedure |
| E2-136 | On Abort do not simply stop: route the exit through Section 4.2's `##### Aborted-run close-out`. | ordering | L354, **Unexplained-movement gate** | close-out | - | Section 4.2's `##### Aborted-run close-out` | TaskList | no | procedure |
| E2-137 | The Aborted-run close-out sweeps per-Task and (where Bee-scoped) Bee-level TaskList names including the `aborted-*` marker, runs the Epic-boundary state-externalization checkpoint on its aborted path, and stops the run naming `/quo-execute <bee-id>`. | definition | L354, **Unexplained-movement gate** | TaskList sweep; checkpoint; resume command | - | `##### Aborted-run close-out`; **Epic-boundary state-externalization checkpoint**; `/quo-execute <bee-id>` | TaskList | no | procedure |
| E2-138 | Mark the `gate-*` task `completed` the moment the answer is consumed and the chosen branch is entered — including on Wait, where consuming means yielding. | ordering | L356, **Unexplained-movement gate** | `gate-*` status flip | AskUserQuestion result | - | TaskList | unknown | procedure |
| E2-139 | The re-fire after Wait creates a fresh `gate-askuserquestion-<short-suffix>` task rather than reusing the closed one. | name-class | L356, **Unexplained-movement gate** | fresh `gate-*` task | - | - | TaskList | unknown | procedure |
| E2-140 | When all Subtasks of the current Task are `done` and no `aborted-*` TaskList task for this Task is `pending`, advance to the per-Task PM review by dispatching a fresh PM Agent. | ordering | L357, `#### Reconciliation loop` | PM dispatch | Subtask statuses; `aborted-*` status | "Per-Task PM dispatch" | bees, TaskList, Agent | no | procedure |
| E2-141 | The aborted-lane guard matches on the `aborted-` name prefix plus the status. | invariant | L357, `#### Reconciliation loop` | - | TaskList names/status | movement-report rung | TaskList | unknown | procedure |
| E2-142 | When all Tasks of the current Epic are `done`, advance to the inter-Epic interaction checkpoint in Section 4.2. | ordering | L358, `#### Reconciliation loop` | - | Task statuses | Section 4.2 | bees | no | procedure |
| E2-143 | Phase 3 (Yield): the orchestrator does not poll; after dispatching this tick's work, return control to the harness and wait for the Agent completion notification (`run_in_background=true`). | ordering | L360, `#### Reconciliation loop` | yield | - | - | Agent | unknown | procedure |
| E2-144 | The Agent completion notification is what triggers the next tick. | definition | L360, `#### Reconciliation loop` | - | Agent completion notification | - | Agent | unknown | procedure |
| E2-145 | Do not use `/loop` (repeats last turn on wall-clock cadence). | invariant | L364-L366, `##### Anti-pattern: no clock primitives` | - | - | `/loop` | none | unknown | procedure |
| E2-146 | Do not use `ScheduleWakeup` (fires the orchestrator after a delay). | invariant | L367, `##### Anti-pattern: no clock primitives` | - | - | `ScheduleWakeup` | none | unknown | procedure |
| E2-147 | Do not use `CronCreate` (fires the orchestrator on a recurring schedule). | invariant | L368, `##### Anti-pattern: no clock primitives` | - | - | `CronCreate` | none | unknown | procedure |
| E2-148 | Do not poll — re-reading bees / TaskList / git on a sleep-wait cycle without a triggering event. | invariant | L369, `##### Anti-pattern: no clock primitives` | - | - | - | none | unknown | procedure |
| E2-149 | If the tick's work is dispatched and nothing else needs reconciling, yield; background Agents finishing is the only legitimate trigger for the next tick. | ordering | L371, `##### Anti-pattern: no clock primitives` | yield | - | - | Agent | unknown | procedure |
| E2-150 | For each ready implementer Subtask, spawn a fresh Agent at Subtask scope: `Agent(subagent_type=<role>, run_in_background=true, prompt=<dispatch prompt with the Subtask body embedded verbatim>)`. | command | L375-L383, `#### Per-Subtask cold dispatch` | Agent dispatch | Subtask body | - | Agent | no | procedure |
| E2-151 | `subagent_type` is one of `engineer`, `test-writer`, `doc-writer`. | definition | L379, `#### Per-Subtask cold dispatch` | - | - | - | Agent | unknown | procedure |
| E2-152 | Each Subtask gets its own Agent invocation. | invariant | L385, `#### Per-Subtask cold dispatch` | Agent dispatch | - | - | Agent | no | procedure |
| E2-153 | The orchestrator does not name Agents (`Agent(name=...)` is not used) and does not reuse an Agent across Subtasks. | invariant | L385, `#### Per-Subtask cold dispatch` | - | - | - | Agent | unknown | procedure |
| E2-154 | There is no `SendMessage` between Subtasks; the worker reads its assignment from the dispatch prompt, edits files, and exits; the diff is the handoff. | invariant | L385, `#### Per-Subtask cold dispatch` | - | - | - | Agent | unknown | procedure |
| E2-155 | The PM gets a new Agent at every per-Task review boundary; reviewers get a new Agent at every Bee-level review. | invariant | L387, `#### Per-Subtask cold dispatch` | Agent dispatch | - | Section 5 | Agent | unknown | procedure |
| E2-156 | Warm `SendMessage`-based dispatch requires `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` and is not available; the cold-dispatch divergence from the SDD is intentional and tracked by a re-probe issue. | rationale-only | L391, `##### Per-Subtask cold dispatch (vs SDD's warm-Agent intent)` | - | - | Claude Code sub-agents docs; SDD divergence note; "Re-probe SendMessage-without-Agent-Teams as warm-Agent token-cost optimization" | - | unknown | rationale |
| E2-157 | The dispatch prompt must embed the ticket body verbatim; paraphrasing corrupts identifier names the worker uses literally. | invariant | L395, `##### Dispatch prompt: quote the ticket body verbatim` | dispatch prompt | ticket body | - | Agent, bees | unknown | procedure |
| E2-158 | Read the ticket via `bees show-ticket --ids <ticket-id>` and embed the returned body in the prompt as a quoted block. | command | L395, `##### Dispatch prompt: quote the ticket body verbatim` | dispatch prompt | ticket body | - | bees, Bash, Agent | unknown | procedure |
| E2-159 | Do not summarise, paraphrase, or "clean up" identifier spellings in the embedded body. | invariant | L395, `##### Dispatch prompt: quote the ticket body verbatim` | - | - | - | Agent | unknown | procedure |
| E2-160 | Framing prose around the quoted block (e.g., "your gating precondition is met — start now") is fine; the body stays untouched. | definition | L395, `##### Dispatch prompt: quote the ticket body verbatim` | - | - | - | Agent | unknown | procedure |
| E2-161 | The dispatch prompt does not need to ask the worker to ping back; completion notifications are automatic and TaskList is the progress signal. | definition | L395, `##### Dispatch prompt: quote the ticket body verbatim` | - | - | TaskList progress UI | Agent, TaskList | unknown | procedure |
| E2-162 | Test Writer dispatch: supply the fingerprint path list by carrying forward the Engineer return's changed-file list, not by re-deriving from the tree. | relay | L397, **Test Writer dispatch — supply the fingerprint path list** | dispatch prompt heading | Engineer return `## Files changed` | `agents/test-writer.md`, `agents/engineer.md` | Agent | unknown | procedure |
| E2-163 | `agents/engineer.md` requires every Engineer return to carry a `## Files changed` list. | definition | L397, **Test Writer dispatch** | - | Engineer return | `agents/engineer.md`; `## Files changed` | Agent | unknown | procedure |
| E2-164 | Supply the paths under a labelled `## Source paths to fingerprint` heading, one path per line, repository-relative, with test paths dropped. | field-or-template | L397, **Test Writer dispatch** | `## Source paths to fingerprint` heading | `## Files changed` | - | Agent | unknown | procedure |
| E2-165 | Scope the fingerprint list to the lane being dispatched (Subtask-scoped vs Bee-scoped). | invariant | L397, **Test Writer dispatch** | - | lane scope | - | Agent | unknown | procedure |
| E2-166 | For a Subtask-scoped Test Writer the list is resolved, never assumed: a test Subtask has no Engineer diff of its own. | invariant | L399, **Test Writer dispatch** (Subtask-scoped) | - | - | - | bees | no | procedure |
| E2-167 | The primary rule is the parent Task's union: `bees show-ticket --ids <subtask-id>` → `parent`; `bees show-ticket --ids <task-id>` → `children`; union the `## Files changed` lists from Engineer returns for that Task's implementation Subtasks. | command | L399, **Test Writer dispatch** (Subtask-scoped) | fingerprint list | `parent`, `children`, Engineer returns | - | bees, Bash | no | procedure |
| E2-168 | Narrow the union to just the `up_dependencies`-named implementation Subtasks when — and only when — the test Subtask's own `up_dependencies` is non-empty. | precondition | L399, **Test Writer dispatch** (Subtask-scoped) | narrowed list | test Subtask `up_dependencies` | - | bees | no | procedure |
| E2-169 | An empty `up_dependencies` on a test Subtask is legal; the unnarrowed parent-Task union is the answer, not an empty list and not the working-tree fallback. | invariant | L399, **Test Writer dispatch** (Subtask-scoped) | - | `up_dependencies` | Fallback below | bees | no | procedure |
| E2-170 | Implementation Subtasks under other Tasks are designed sibling concurrency and stay out of the list either way. | invariant | L399, **Test Writer dispatch** (Subtask-scoped) | - | - | - | - | no | procedure |
| E2-171 | The union covers only the Engineer returns the orchestrator holds at dispatch time; a same-Task implementation Subtask still in flight contributes nothing. | definition | L399, **Test Writer dispatch** — **A limitation worth naming rather than papering over** | - | held Engineer returns | movement rung above | Agent | no | procedure |
| E2-172 | Paths that entered the union from a same-Task sibling implementation Subtask are supplied; movement in them is anomalous to the writer like any supplied path. | invariant | L399, **Test Writer dispatch** (Subtask-scoped) | - | - | `agents/test-writer.md` execute-mode stop rule | - | no | procedure |
| E2-173 | Treat a writer stop-report naming a same-Task sibling path as legitimate rather than dismissing it as sibling concurrency; the union rule is unchanged. | invariant | L399, **Test Writer dispatch** (Subtask-scoped) | - | writer return | `agents/test-writer.md` | - | no | procedure |
| E2-174 | For a Bee-scoped Test Writer re-dispatched from Section 5, the list is the union of changed-file lists from every Engineer return behind the Bee-level diff, including Bee-scoped `engineer-<bee-id>` rounds. | field-or-template | L400, **Test Writer dispatch** (Bee-scoped) | fingerprint list | Engineer returns | Section 5; `engineer-<bee-id>` | Agent | unknown | procedure |
| E2-175 | Fallback when an Engineer return did not list files: derive the set with `git diff --name-only HEAD` (identical on POSIX and PowerShell) and drop the test paths. | recovery | L402-L412, **Fallback when an Engineer return did not list files** | fingerprint list | git working tree | - | git, Bash | unknown | procedure |
| E2-176 | The fallback derivation is the last resort; it applies only when the dispatched scope yields no usable Engineer-supplied list at all (no return exists, or every return omitted `## Files changed`). | precondition | L414, **This derivation is the last resort, and its trigger is narrow** | - | Engineer returns | - | git | unknown | procedure |
| E2-177 | The fallback does not apply merely because the `up_dependencies` narrowing selected nothing; the parent-Task union answers that case. | invariant | L414, **This derivation is the last resort** | - | - | parent-Task union above | - | no | procedure |
| E2-178 | When only some in-scope returns carried a list, union those and do not fall back — a partial return-carried list beats the whole working tree. | invariant | L414, **This derivation is the last resort** | fingerprint list | Engineer returns | - | - | unknown | procedure |
| E2-179 | An explicitly-empty `## Files changed` list is a list, not a missing one; a research-only Engineer pass contributes nothing and does not trigger the fallback. | invariant | L414, **An explicitly-empty `## Files changed` list is a list, not a missing one** | - | Engineer return | - | - | unknown | procedure |
| E2-180 | In execute mode the fallback reads the whole working tree and can include a sibling Subtask's concurrent edits, which the writer may stop on. | rationale-only | L414, **This derivation is the last resort** | - | - | - | git | no | rationale |
| E2-181 | Omit the `## Source paths to fingerprint` heading rather than emitting an empty one when no usable list exists and the fallback yields nothing outside the writer's own test files. | invariant | L414, **Omit the `## Source paths to fingerprint` heading rather than emitting an empty one** | dispatch prompt | fingerprint list | `agents/test-writer.md` empty-path-set clause | Agent | unknown | procedure |
| E2-182 | Also omit the heading when every in-scope return carried a list but the union across them is empty (fallback never fires). | invariant | L414, **Omit the `## Source paths to fingerprint` heading** | dispatch prompt | Engineer returns | `agents/test-writer.md` | Agent | unknown | procedure |
| E2-183 | `agents/test-writer.md`'s empty-path-set clause keys on the heading being absent; an empty heading would read as a supplied set. | rationale-only | L414, **Omit the `## Source paths to fingerprint` heading** | - | - | `agents/test-writer.md` | - | unknown | rationale |
| E2-184 | Writer dispatch prompts MAY state the orchestrator-side rule scoped to the lane; for a Subtask-scoped writer: "no Engineer Agent will be dispatched for your Subtask's implementation dependency while you are running". | field-or-template | L416, **Test Writer / Doc Writer dispatch — state the orchestrator-side rule, never a freeze guarantee** | dispatch prompt text | lane scope | `agents/test-writer.md`, `agents/doc-writer.md` | Agent | no | procedure |
| E2-185 | The Subtask-scoped promise cannot be widened past that Subtask, because an Engineer on a sibling Subtask may legitimately be running concurrently. | invariant | L416, **state the orchestrator-side rule** | - | - | - | Agent | no | procedure |
| E2-186 | For a Bee-scoped writer re-dispatched from Section 5 the wording is "no Engineer Agent will be dispatched for this Bee while you are running". | field-or-template | L416, **state the orchestrator-side rule** | dispatch prompt text | - | Section 5 | Agent | unknown | procedure |
| E2-187 | Keep the scoping qualifier: the role files scope the guarantee per mode and these are the execute-mode wordings their execute-mode branch expects. | invariant | L416, **state the orchestrator-side rule** | - | - | `agents/test-writer.md`, `agents/doc-writer.md` | Agent | no | procedure |
| E2-188 | The prompts MUST NOT claim "the source tree is frozen" or any close paraphrase promising files will not move. | invariant | L416, **state the orchestrator-side rule** | - | - | - | Agent | unknown | procedure |
| E2-189 | The orchestrator cannot prevent the user, a second session, or a background process from editing the tree; a freeze phrasing promises a lever it does not hold. | rationale-only | L416, **state the orchestrator-side rule** | - | - | - | - | unknown | rationale |
| E2-190 | The writer-side half (capture tree state, re-check, stop and report) lives in `agents/test-writer.md` and `agents/doc-writer.md`, so recovery does not depend on the prompt wording. | definition | L416, **state the orchestrator-side rule** | - | - | `agents/test-writer.md`, `agents/doc-writer.md` | - | unknown | rationale |
| E2-191 | The framing prose MUST NOT loosen the role boundaries defined in the dispatched role's contract file (`agents/<role>.md`). | invariant | L418, `##### Dispatch prompt: quote the ticket body verbatim` | - | `agents/<role>.md` | `agents/<role>.md` | Agent | unknown | procedure |
| E2-192 | The no-loosening rule applies to every dispatched role — implementers (Engineer / Test Writer / Doc Writer) and review-only roles (PM, Code Reviewer, Test Reviewer, Doc Reviewer). | invariant | L418, `##### Dispatch prompt` | - | - | - | Agent | unknown | procedure |
| E2-193 | MUST NOT tell the Engineer it may also write tests or docs. | invariant | L420, `##### Dispatch prompt` | - | - | - | Agent | unknown | example |
| E2-194 | MUST NOT tell the Test Writer it may also modify source code. | invariant | L421, `##### Dispatch prompt` | - | - | - | Agent | unknown | example |
| E2-195 | MUST NOT tell the Doc Writer it may also modify source or test files. | invariant | L422, `##### Dispatch prompt` | - | - | - | Agent | unknown | example |
| E2-196 | MUST NOT tell the PM or any reviewer it may write source, tests, or docs; contract files state "Does NOT modify source code, tests, or docs" (PM) and "Does NOT review <other-lanes>" (reviewers). | invariant | L423, `##### Dispatch prompt` | - | - | agent contract files | Agent | unknown | example |
| E2-197 | MUST NOT tell one reviewer it may also review another reviewer's lane (e.g., Code Reviewer reviewing tests). | invariant | L424, `##### Dispatch prompt` | - | - | - | Agent | unknown | example |
| E2-198 | Temptation to carve a role exception signals a need for orchestrator-level coordination (follow-up Test Writer dispatch, Subtask redirect/re-scope), NOT a softening clause. | invariant | L426, `##### Dispatch prompt` | - | - | - | Agent | unknown | procedure |
| E2-199 | The only handoff is worker → orchestrator (diff in execution mode, JSON return in research mode), never worker-to-worker; "coordinate with the other role's diff" prose cannot make softening safe. | invariant | L426, `##### Dispatch prompt` | - | - | - | Agent | unknown | rationale |
| E2-200 | Workers do not message each other; the orchestrator is the hub, each Agent a spoke that reads its prompt, edits files, and exits; the diff is the handoff. | definition | L430, `#### Hub-and-spoke via substrate` | - | - | - | Agent | unknown | procedure |
| E2-201 | Hub-and-spoke is a structural property of ephemeral background Agents, not a rule the orchestrator must enforce. | rationale-only | L430, `#### Hub-and-spoke via substrate` | - | - | - | Agent | unknown | rationale |
| E2-202 | Subagents cannot spawn other subagents; only the top-level orchestrator may dispatch Agents — the skill ships flat orchestration. | invariant | L434, `#### Recursive delegation: not supported` | - | - | Claude Code sub-agents docs | Agent | unknown | procedure |
| E2-203 | Every Agent invocation originates from this skill's reconciliation loop, never from a worker. | invariant | L434, `#### Recursive delegation: not supported` | - | - | - | Agent | unknown | procedure |
| E2-204 | Orchestrator context grows monotonically within a session and nothing in this skill reclaims those tokens; token reclamation is owned by the harness. | definition | L436, `#### Recursive delegation: not supported` | - | - | - | - | unknown | rationale |
| E2-205 | The skill has no model-invocable way to clear or compact its own context. | definition | L436, `#### Recursive delegation: not supported` | - | - | - | - | unknown | rationale |
| E2-206 | Section 4.2's Epic-boundary state-externalization checkpoint keeps every load-bearing fact in a durable carrier so the run can be re-derived after any compaction. | definition | L436, `#### Recursive delegation: not supported` | - | - | Section 4.2's **Epic-boundary state-externalization checkpoint** | - | no | rationale |
| E2-207 | The actual reclamation lever is starting a fresh session at an Epic boundary, which Section 4.2 branch 1 recommends when the loop exits for breakdown work. | definition | L436, `#### Recursive delegation: not supported` | - | - | Section 4.2 branch 1 | - | no | procedure |
| E2-208 | The orchestrator dispatches four roles during a Task; role contracts live in the role files; the orchestrator invokes the right role at the right time rather than carrying role prose. | invariant | L440, `#### Roles dispatched by the orchestrator` | - | `agents/<role>.md` | - | Agent | unknown | procedure |
| E2-209 | **Engineer** (`agents/engineer.md`) implements source-code Subtasks; Model: Opus (always); does not write tests or docs. | definition | L442, `#### Roles dispatched by the orchestrator` | - | - | `agents/engineer.md` | Agent | unknown | procedure |
| E2-210 | **Test Writer** (`agents/test-writer.md`) implements test Subtasks and reviews the Engineer's diff for missing coverage; Model: Opus (always). | definition | L443, `#### Roles dispatched by the orchestrator` | - | - | `agents/test-writer.md` | Agent | unknown | procedure |
| E2-211 | **Doc Writer** (`agents/doc-writer.md`) implements doc Subtasks, reviews the Engineer's diff for doc gaps, and appends/updates a `### Feature: <title>` subsection in the cumulative PRD and SDD per the categorization heuristic; Model: Opus (always). | definition | L444, `#### Roles dispatched by the orchestrator` | - | - | `agents/doc-writer.md` `## Cumulative project doc updates`; `### Feature: <title>` | Agent | unknown | procedure |
| E2-212 | `<title>` is the verbatim title of the Plan Bee at the top of the Subtask → Task → Epic → Plan Bee chain; the orchestrator surfaces it to the subagent in the dispatch context. | relay | L444, `#### Roles dispatched by the orchestrator` | dispatch context `<title>` | Plan Bee title | `agents/doc-writer.md` | Agent, bees | unknown | procedure |
| E2-213 | `agents/doc-writer.md` is authoritative for the categorization table, `<title>` resolution, idempotency rule, and CLAUDE.md `## Documentation Locations` lookup-key recipe. | definition | L444, `#### Roles dispatched by the orchestrator` | - | - | `agents/doc-writer.md`; CLAUDE.md `## Documentation Locations` | - | unknown | procedure |
| E2-214 | **Product Manager** (`agents/pm.md`) reviews the Task's work against the spec source from the Bee's `reference_materials` (`file-path` resolver or `bees` resolver Spec Bee `t1=Doc` children), or the Bee body when null/empty; Model: Opus (always). | definition | L445, `#### Roles dispatched by the orchestrator` | - | `reference_materials` | `agents/pm.md` `### Resolving reference_materials entries` | Agent | unknown | procedure |
| E2-215 | The PM drives `quo-engineer-review` and `quo-doc-writer-review` per Task and produces the per-Task summary report. | definition | L445, `#### Roles dispatched by the orchestrator` | per-Task summary | - | `quo-engineer-review`, `quo-doc-writer-review` | Agent | unknown | procedure |
| E2-216 | The PM dispatch prompt does not duplicate the resolver-branching logic; `agents/pm.md` `### Resolving reference_materials entries` is authoritative. | invariant | L445, `#### Roles dispatched by the orchestrator` | - | - | `agents/pm.md` `### Resolving reference_materials entries` | Agent | unknown | procedure |
| E2-217 | Reviewer roles (`agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md`) are introduced in Section 5 (final Bee-level reviews). | definition | L447, `#### Roles dispatched by the orchestrator` | - | - | Section 5; `agents/code-reviewer.md`, `agents/test-reviewer.md`, `agents/doc-reviewer.md` | Agent | unknown | procedure |
| E2-218 | Dispatch a fresh PM Agent when all child Subtasks are `status=done` and no `aborted-*` TaskList task for this Task is `pending`. | precondition | L451, `##### Per-Task PM dispatch` | PM dispatch | Subtask statuses; `aborted-*` status | Section 3's Reconcile-step movement-report rung | Agent, bees, TaskList | no | procedure |
| E2-219 | The PM dispatch prompt must include the Task ID and the list of completed Subtask IDs. | field-or-template | L451, `##### Per-Task PM dispatch` | dispatch prompt | Task ID, Subtask IDs | - | Agent | no | procedure |
| E2-220 | The PM dispatch prompt must include `<scoped-marker-resolver-path>`, filled at runtime so `agents/pm.md` can perform its Scoped-marker check. | field-or-template | L451, `##### Per-Task PM dispatch` | dispatch prompt placeholder | resolved helper path | "Scoped-marker PM dispatch wiring"; `agents/pm.md` | Agent | unknown | procedure |
| E2-221 | The PM dispatch prompt must include `<compromise-tracker-path>`, this run's compromise-tracker path — the same value Section 6 passes to the post-completion reviewer. | field-or-template | L451, `##### Per-Task PM dispatch` | dispatch prompt placeholder | compromise-tracker path (manifest) | Section 6 | Agent | unknown | procedure |
| E2-222 | Pass the tracker path, not its contents, exactly as Section 6 does; `agents/pm.md` reads the file itself. | invariant | L451, `##### Per-Task PM dispatch` | - | compromise-tracker path | Section 6; `agents/pm.md` | Agent | unknown | procedure |
| E2-223 | The Bee-scoped PM re-dispatches in Section 5 carry the same two placeholders. | invariant | L451, `##### Per-Task PM dispatch` | dispatch prompt | - | Section 5 | Agent | unknown | procedure |
| E2-224 | `agents/engineer.md` requires an Engineer return to carry a completeness check with evidence whenever its Subtask directed a change at every site where some property holds. | definition | L453, **Relay the Engineer's completeness evidence into the PM dispatch prompt** | - | Engineer return | `agents/engineer.md` | Agent | unknown | procedure |
| E2-225 | When any Engineer return for this Task carried a completeness list, the per-Task PM dispatch prompt MUST embed that list verbatim under a labelled heading; recommended label `## Engineer's completeness evidence`. | relay | L453, **Relay the Engineer's completeness evidence** | dispatch prompt heading | Engineer completeness list | `/quo-engineer-review`; `agents/pm.md` | Agent | unknown | procedure |
| E2-226 | Embed the completeness list as text in the prompt — do not write it to a scratch file and pass a path. | invariant | L453, **Relay the Engineer's completeness evidence** | - | - | - | Agent | unknown | procedure |
| E2-227 | Say which Subtask each completeness list came from when more than one Engineer contributed. | field-or-template | L453, **Relay the Engineer's completeness evidence** | dispatch prompt | Engineer returns | - | Agent | unknown | procedure |
| E2-228 | Separately from the list, state in the PM prompt that the assignment was sweep-shaped whenever it was. | relay | L453, **Relay the Engineer's completeness evidence** | dispatch prompt statement | Subtask body / directive | `/quo-engineer-review` sweep-verification check | Agent | unknown | procedure |
| E2-229 | `/quo-engineer-review`'s sweep-verification check reports a missing list only when the invocation named the assignment as a sweep, so an unnamed sweep silently loses the check. | rationale-only | L453, **Relay the Engineer's completeness evidence** | - | - | `/quo-engineer-review`; `agents/pm.md` | - | unknown | rationale |
| E2-230 | When no completeness list arrived, omit the `## Engineer's completeness evidence` heading rather than emitting an empty one, but still state sweep-shapedness when it applies. | invariant | L453, **Relay the Engineer's completeness evidence** | dispatch prompt | Engineer returns | `/quo-engineer-review` | Agent | unknown | procedure |
| E2-231 | Use Claude Code's native **TaskList** as the visible progress UI; there is no separate display backend to configure. | definition | L457, `#### TaskList as progress UI` | - | - | - | TaskList | unknown | procedure |
| E2-232 | For every Agent the orchestrator dispatches, it creates exactly one TaskList task. | invariant | L459, `#### TaskList as progress UI` | TaskList task | Agent dispatch | - | TaskList, Agent | unknown | procedure |
| E2-233 | Set the task `pending` when the orchestrator decides this Subtask is next, before the Agent invocation lands. | ordering | L461, `#### TaskList as progress UI` | task status | - | - | TaskList | unknown | procedure |
| E2-234 | Set the task `in_progress` the moment the Agent invocation is dispatched (`Agent(...)` returns). | ordering | L462, `#### TaskList as progress UI` | task status | Agent dispatch | - | TaskList, Agent | unknown | procedure |
| E2-235 | Set the task `completed` when the orchestrator processes the Agent's completion notification and confirms the bees ticket transitioned to `status=done`. | ordering | L463, `#### TaskList as progress UI` | task status | Agent completion notification; bees status | - | TaskList, bees | unknown | procedure |
| E2-236 | Exception: a writer lane that returned a movement report has its task set `completed` without a `status=done`; the delivery obligation moves to an `aborted-*` task. | invariant | L463, `#### TaskList as progress UI` | task status; `aborted-*` task | writer return | Section 3's Reconcile-step movement-report rung | TaskList | unknown | procedure |
| E2-237 | `completed` on an Agent task means "this Agent is gone", not "this work shipped". | definition | L463, `#### TaskList as progress UI` | - | - | - | TaskList | unknown | procedure |
| E2-238 | Use `metadata.activity` on the TaskList task to surface finer-grained progress when a worker emits intermediate signal (e.g., `"running narrow tests on package X"`). | field-or-template | L465, `#### TaskList as progress UI` | `metadata.activity` | worker signal | - | TaskList | unknown | procedure |
| E2-239 | The orchestrator updates `metadata.activity` opportunistically; it is informational, not a routing input. | invariant | L465, `#### TaskList as progress UI` | - | - | - | TaskList | unknown | procedure |
| E2-240 | The TaskList naming convention is the canonical cross-reference for downstream Sections and other skills; deterministic (no collision) and unambiguous (maps back to a bee ticket). | definition | L469, `##### TaskList naming convention` | - | - | later Sections; other skills | TaskList | unknown | procedure |
| E2-241 | Implementer Agents (Engineer, Test Writer, Doc Writer) on the forward path use Subtask scope: `<role>-<subtask-id>` (e.g., `engineer-t3.abc.def.gh`, `test-writer-t3.abc.def.ij`, `doc-writer-t3.abc.def.kl`). | name-class | L471, `##### TaskList naming convention` | TaskList task name | `<subtask-id>` | - | TaskList | no | procedure |
| E2-242 | Each Subtask gets its own implementer Agent and its own TaskList task; the subtask-id keeps names unique when sibling Subtasks run concurrently. | invariant | L471, `##### TaskList naming convention` | - | - | - | TaskList | no | procedure |
| E2-243 | Bee-level implementer Agents re-dispatched from Section 5 use Bee scope: `<role>-<bee-id>` (e.g., `engineer-b.abc`, `test-writer-b.abc`, `doc-writer-b.abc`). | name-class | L472, `##### TaskList naming convention` | TaskList task name | `<bee-id>` | Section 5 | TaskList | unknown | procedure |
| E2-244 | Section 5 findings span the whole Bee's diff and are not attributable to one Subtask, so re-dispatches take the Bee id; Subtask and Bee ids are distinct token shapes and never overlap. | rationale-only | L472, `##### TaskList naming convention` | - | - | Section 5 | TaskList | unknown | rationale |
| E2-245 | PM Agents on the forward path use Task scope: `pm-<task-id>` (e.g., `pm-t2.abc.def.gh`). | name-class | L473, `##### TaskList naming convention` | TaskList task name | `<task-id>` | - | TaskList | no | procedure |
| E2-246 | A PM re-dispatched from Section 5's Bee-level review takes the Bee-scoped form `pm-<bee-id>` (e.g., `pm-b.abc`). | name-class | L473, `##### TaskList naming convention` | TaskList task name | `<bee-id>` | Section 5 | TaskList | unknown | procedure |
| E2-247 | Reviewer Agents (Code Reviewer, Test Reviewer, Doc Reviewer) use Bee scope: `<reviewer>-<bee-id>` (e.g., `code-reviewer-b.abc`, `test-reviewer-b.abc`, `doc-reviewer-b.abc`). | name-class | L474, `##### TaskList naming convention` | TaskList task name | `<bee-id>` | Section 5 | TaskList | unknown | procedure |
| E2-248 | Reviewers run once per Bee at the final Bee-level review. | definition | L474, `##### TaskList naming convention` | - | - | Section 5 | Agent | unknown | procedure |
| E2-249 | Every dispatch after the first of an already-named role at the same scope appends `-r<n>`, where `<n>` is the 1-based re-dispatch count for that role at that scope. | name-class | L475, `##### TaskList naming convention` — **Round discriminator** | TaskList task name | re-dispatch count | - | TaskList | yes | procedure |
| E2-250 | Examples: `engineer-b.abc` → `engineer-b.abc-r1` → `engineer-b.abc-r2`; `code-reviewer-b.abc-r1`; `doc-writer-t3.abc.def.kl-r1`; `pm-t2.abc.def.gh-r1`. | example | L475, `##### TaskList naming convention` | - | - | - | TaskList | yes | example |
| E2-251 | The `-r<n>` form is identical to `/quo-fix-issue`'s issue-scoped convention (`engineer-<issue-id>-r<n>`) so readers see one convention. | definition | L475, `##### TaskList naming convention` | - | - | `/quo-fix-issue`; `engineer-<issue-id>-r<n>` | TaskList | yes | rationale |
| E2-252 | Because the discriminator is a suffix, every rule that tests these names — notably part (g)'s Engineer-dispatch preconditions — matches on name prefix plus status, so discriminated names are caught too. | invariant | L475, `##### TaskList naming convention` | - | TaskList names | part (g) of `### Orchestrator discipline: routing review findings` | TaskList | yes | procedure |
| E2-253 | Post-completion follow-up Agents dispatched by Section 6 step 6's `Fix in this session` branch use `<role>-postcomp-<n>`, `<n>` being the 1-based finding index (e.g., `engineer-postcomp-1`, `doc-writer-postcomp-2`). | name-class | L476, `##### TaskList naming convention` | TaskList task name | post-completion finding index | Section 6 step 6; `Fix in this session` | TaskList | yes | procedure |
| E2-254 | Post-completion names take no ticket-id suffix and normally no round discriminator; the per-finding index is the discriminator and distinct findings dispatch under distinct `<n>`. | invariant | L476, `##### TaskList naming convention` | - | - | - | TaskList | yes | procedure |
| E2-255 | Carve-out: a round discriminator `-r<k>` IS required when a post-completion writer lane is re-dispatched after a movement abort; `<k>` is the 1-based re-dispatch count at that finding index (e.g., `test-writer-postcomp-2-r1`). | name-class | L476, `##### TaskList naming convention` | TaskList task name | re-dispatch count | Section 6 step 6; Section 3's movement-report rung | TaskList | yes | procedure |
| E2-256 | Outside the movement-abort carve-out, do not append a round discriminator to a post-completion name. | invariant | L476, `##### TaskList naming convention` | - | - | - | TaskList | yes | procedure |
| E2-257 | Section 6 step 6 is authoritative for post-completion follow-ups' dispatch ordering, Engineer-dispatch freeze precondition, and close-out; it is the same form as `/quo-fix-issue`'s Section 8 step 6. | definition | L476, `##### TaskList naming convention` | - | - | Section 6 step 6; `/quo-fix-issue` Section 8 step 6 | - | yes | procedure |
| E2-258 | Aborted-writer redelivery markers take the scope of the lane that aborted: `aborted-<role>-<subtask-id>`, `aborted-<role>-<bee-id>`, or `aborted-<role>-postcomp-<n>` (e.g., `aborted-test-writer-t3.abc.def.ij`, `aborted-doc-writer-b.abc`). | name-class | L477, `##### TaskList naming convention` | TaskList task name | lane scope | Section 6 | TaskList | unknown | procedure |
| E2-259 | For a post-completion marker, `<n>` is the same finding index as the lane it marks so the marker reads back to its finding. | invariant | L477, `##### TaskList naming convention` | - | finding index | Section 6 | TaskList | unknown | procedure |
| E2-260 | Aborted markers are created by Section 3's movement-report rung: the writer's task is marked `completed`, and this task is created `pending` with the "how far I got" report as `metadata.activity`. | definition | L477, `##### TaskList naming convention` | `aborted-*` task | writer return | Section 3's Reconcile-step **movement-report rung** | TaskList | unknown | procedure |
| E2-261 | An `aborted-*` marker is not an Agent task: the one-task-per-Agent rule and `-r<n>` discriminator do not apply; a second abort refreshes its `metadata.activity` rather than opening another. | invariant | L477, `##### TaskList naming convention` | - | - | - | TaskList | unknown | procedure |
| E2-262 | An `aborted-*` marker is an obligation marker like `defer-*` and `gate-*`: while `pending`, a Subtask-scoped marker holds its parent Task's advance to PM review, and a Bee-scoped marker holds Section 5's review loop from closing. | invariant | L477, `##### TaskList naming convention` | - | `aborted-*` status | `defer-*`, `gate-*`; Section 5 | TaskList | unknown | procedure |
| E2-263 | Mark the `aborted-*` marker `completed` when the re-dispatched lane (`<role>-<subtask-id>-r<n>` / `<role>-<bee-id>-r<n>` / `<role>-postcomp-<n>-r<k>`) returns a normal deliverable. | ordering | L477, `##### TaskList naming convention` | `aborted-*` status flip | re-dispatched return | post-completion round-discriminator carve-out | TaskList | unknown | procedure |
| E2-264 | The `aborted-` prefix is a distinct name class from `<role>-<subtask-id>` / `<role>-<bee-id>`, keeping markers out of part (g)'s Engineer-dispatch clauses without an exemption clause. | invariant | L477, `##### TaskList naming convention` | - | - | part (g) | TaskList | unknown | procedure |
| E2-265 | Deferral-ledger tasks have Run scope: `defer-<short-suffix>` (e.g., `defer-1`, `defer-2`, or any collision-resistant suffix). | name-class | L478, `##### TaskList naming convention` | TaskList task name | - | - | TaskList | unknown | procedure |
| E2-266 | Create a `defer-*` task when an agent's structured return (`agents/pm.md` Final report contract or `agents/analyst.md` `### Deferred refinements`) names `defer-to-existing-ticket-body: <ticket-id>` or `defer-to-new-Issue` that the orchestrator chose not to address inline. | ordering | L478, `##### TaskList naming convention` | `defer-*` task | agent structured return | `agents/pm.md` Final report contract; `agents/analyst.md` `### Deferred refinements` | TaskList | unknown | procedure |
| E2-267 | A `defer-*` task's `metadata.activity` carries the deferral's one-line description so Section 6.5's gate prose can surface the active set. | field-or-template | L478, `##### TaskList naming convention` | `metadata.activity` | deferral description | Section 6.5 | TaskList | unknown | procedure |
| E2-268 | Mark a `defer-*` task `completed` the moment the deferral is encoded in a durable carrier — an updated ticket body, a new Issue, or an explicit in-session resolution (logged in `metadata.activity`). | ordering | L478, `##### TaskList naming convention` | `defer-*` status flip | ticket update / Issue | - | TaskList, bees | unknown | procedure |
| E2-269 | The pre-handoff Section 6.5 gate reads the `defer-*` ledger and refuses to yield control while any remain pending or in-progress. | invariant | L478, `##### TaskList naming convention` | - | `defer-*` statuses | Section 6.5 | TaskList | unknown | procedure |
| E2-270 | Gate-task tasks have Turn scope: `gate-<kind>-<short-suffix>`; the dominant `<kind>` is `askuserquestion` (e.g., `gate-askuserquestion-1`). | name-class | L479, `##### TaskList naming convention` | TaskList task name | - | Section 5; Section 6; Section 6.5 | TaskList | unknown | procedure |
| E2-271 | Create the `gate-*` task via `TaskCreate` immediately before firing the prescribed tool call (typically `AskUserQuestion`), per the two-step contract. | ordering | L479, `##### TaskList naming convention` | `gate-*` task | - | two-step contract | TaskList, AskUserQuestion | unknown | procedure |
| E2-272 | The `<short-suffix>` MUST be unique per fire within the run across every `gate-*` task regardless of `<kind>`, using monotonic integers or gate-specific slugs encoding context. | invariant | L479, `##### TaskList naming convention` | - | prior `gate-*` names | - | TaskList | unknown | procedure |
| E2-273 | The two-step contract applies at every gate this skill fires — trailer-driven review-skill gates (Section 5 escalation) and trailer-less orchestrator-driven gates. | invariant | L479, `##### TaskList naming convention` | - | - | Section 5's review loop | TaskList, AskUserQuestion | unknown | procedure |
| E2-274 | The trailer-less gates are: Section 1's conditional session-effort gate (`Check session reasoning effort`), Section 3's unexplained-movement gate, Section 4.2's continue-or-stop multi-Epic gate (Mode 1), Section 6's post-completion findings gate, Section 6.5's deferral-hygiene gate, Section 7's Acceptance-Criteria sign-off and Bee close-out gates. | definition | L479, `##### TaskList naming convention` | - | - | Section 1 `Check session reasoning effort`; Section 3 **unexplained-movement gate**; Section 4.2; Section 6; Section 6.5; Section 7 | AskUserQuestion | no | procedure |
| E2-275 | A `gate-*` task's `metadata.activity` carries the gate's finite choices verbatim where applicable. | field-or-template | L479, `##### TaskList naming convention` | `metadata.activity` | gate choices | - | TaskList | unknown | procedure |
| E2-276 | Mark a `gate-*` task `completed` the moment the prescribed tool call returns and its result has been consumed (answer routed, next branch entered). | ordering | L479, `##### TaskList naming convention` | `gate-*` status flip | tool result | - | TaskList | unknown | procedure |
| E2-277 | A `gate-*` task normally enters and exits within a single turn — shorter than `defer-*`, which spans the run. | definition | L479, `##### TaskList naming convention` | - | - | - | TaskList | unknown | procedure |
| E2-278 | This skill MUST NOT yield control to the harness while any `gate-*` task is `pending` or `in_progress` (yield-control discipline, mirroring `defer-*`). | invariant | L479, `##### TaskList naming convention` | - | `gate-*` statuses | `defer-*` | TaskList | unknown | procedure |
| E2-279 | If a `gate-*` task is left active when the orchestrator would yield, the next reconciliation tick walks the TaskList, surfaces it, and re-fires the prescribed tool call from the recorded `metadata.activity` choices. | recovery | L479, `##### TaskList naming convention` | re-fired tool call | `gate-*` task; `metadata.activity` | - | TaskList, AskUserQuestion | unknown | procedure |
| E2-280 | The `gate-*` namespace coexists without overlap with `defer-*`, `<role>-<subtask-id>`, `pm-<task-id>`, `<role>-<bee-id>`, `pm-<bee-id>`, reviewer names, `<role>-postcomp-<n>`, and `aborted-*` names, with or without `-r<n>`. | invariant | L479, `##### TaskList naming convention` | - | - | - | TaskList | unknown | procedure |
| E2-281 | The PM dispatch prompt must include the resolved path to the Scoped-marker helper as the `<scoped-marker-resolver-path>` substitution. | field-or-template | L483, `#### Scoped-marker PM dispatch wiring` | dispatch prompt placeholder | resolved helper path | "Per-Task PM dispatch" | Agent | unknown | procedure |
| E2-282 | Resolve the helper path at runtime from this skill's base directory: `<this skill's base directory>/../quo-breakdown-epic/scripts/scoped_marker_resolver.py` (PowerShell: `<this skill's base directory>\..\quo-breakdown-epic\scripts\scoped_marker_resolver.py`). | command | L483-L499, `#### Scoped-marker PM dispatch wiring` | resolved path | skill base directory | `quo-breakdown-epic`; `scoped_marker_resolver.py` | none | unknown | procedure |
| E2-283 | The base directory is shown in the skill invocation header at session start (e.g., `Base directory for this skill: /Users/.../quo-execute`); use the `..` traversal to reach the sibling skill. | definition | L489, `#### Scoped-marker PM dispatch wiring` | - | skill invocation header | - | none | unknown | procedure |
| E2-284 | The orchestrator's responsibility ends at passing the resolved path placeholder; it does not inline the Scoped-marker grammar, the temp-file staging recipe, or the helper invocation — `agents/pm.md` owns those. | invariant | L501, `#### Scoped-marker PM dispatch wiring` | - | - | `agents/pm.md` | Agent | unknown | procedure |
| E2-285 | Test and lint commands form a ladder; stay on the lowest rung that catches the issue you might have introduced. | invariant | L505, `#### Testing discipline — avoid redundant full-workspace runs` | - | - | - | Bash | unknown | procedure |
| E2-286 | Look up actual commands from the project's CLAUDE.md `## Build Commands` using keys `Compile/type-check`, `Format`, `Lint`, `Narrow test`, `Full test`. | command | L505, `#### Testing discipline` | - | CLAUDE.md `## Build Commands` keys | CLAUDE.md `## Build Commands` | Bash | unknown | procedure |
| E2-287 | Rung 1 (while iterating on a file — Engineer, Test Writer): use **Narrow test** and **Lint** at the same scope; do NOT run the full suite. | ordering | L507, `#### Testing discipline` | - | `Narrow test`, `Lint` | - | Bash | unknown | procedure |
| E2-288 | Rung 2 (at subtask boundary): run **Narrow test** + **Lint** for the touched file/package; do NOT run **Full test** yet. | ordering | L508, `#### Testing discipline` | - | `Narrow test`, `Lint` | - | Bash | unknown | procedure |
| E2-289 | Rung 3 (at the Task's final `.T` or equivalent subtask): the single authoritative **Full test** run, plus **Lint** at workspace scope, plus a **Format** check — the only place the full suite must run. | ordering | L509, `#### Testing discipline` | - | `Full test`, `Lint`, `Format` | - | Bash | unknown | procedure |
| E2-290 | Rung 4 (Product Manager review): trust the `.T` subtask's output; do not re-run the full workspace suite by default unless there is a specific reason (skipped work reported, stale `.bees/` state). | invariant | L510, `#### Testing discipline` | - | `.T` output | - | Bash | unknown | procedure |
| E2-291 | Rung 5 (Director, before commit): run **Format** only; do not re-run tests. | ordering | L511, `#### Testing discipline` | - | `Format` | - | Bash | unknown | procedure |
| E2-292 | When a Task only touches one package's tests, do not invoke workspace-wide tests against unrelated packages. | invariant | L513, `#### Testing discipline` | - | - | - | Bash | unknown | procedure |
| E2-293 | Apply the same ladder to **Lint**, **Format**, and any docs-build command: scope narrowly while iterating, run workspace-wide once at `.T`, trust the result downstream. | invariant | L515, `#### Testing discipline` | - | `Lint`, `Format` | - | Bash | unknown | procedure |
| E2-294 | Use the Bash tool's `timeout` parameter (max 600000 ms = 10 min) for long commands. | command | L519, `#### Running long commands` | - | - | - | Bash | unknown | procedure |
| E2-295 | For test invocations up to 10 min, dispatch in the foreground: `Bash(command: "<your project's test command per CLAUDE.md>", timeout: 540000)`. | command | L519, `#### Running long commands` | - | CLAUDE.md test command | - | Bash | unknown | procedure |
| E2-296 | For runs legitimately exceeding 10 min, use `Bash(run_in_background: true)`, wait silently for the task-completion notification, then Read the output file. | command | L519, `#### Running long commands` | - | output file | - | Bash | unknown | procedure |
| E2-297 | Do not write shell polling loops to wait for completion; the harness handles notification. | invariant | L519, `#### Running long commands` | - | - | - | Bash | unknown | procedure |

## Anchors defined here

- L228 `### 2. Find Epic to work on and validate`
- L253 `#### Pick a multi-Epic run mode (only when more than one Epic is in scope)`
- L259 **Stop after each Epic**
- L260 **Work through all Epics**
- L260 **skip the question entirely** (L255 and L264 also)
- L260 **performs** / **Epic-boundary state-externalization checkpoint**
- L262 **multi-Epic run mode**
- L266 `#### Resolve the manifest placeholders`
- L268 **Rewrite the run-state manifest**
- L268 **This rewrite is unconditional — it runs on all three paths through the gate**
- L270 **Epics in scope**
- L271 **Multi-Epic run mode**
- L273 **On loop re-entry, carry the accumulated fields forward — do not re-emit Section 1's template.**
- L273 **two-field refresh, not a fresh run-start write**
- L273 **Progress**, **Next unit**, **Isolation strategy**, **Pre-Bee SHA**, **Compromise tracker**
- L277 `#### Check if stale`
- L285 `#### Mark status when ready to start work`
- L289 `### 3. Execute Tasks via per-Subtask Agent dispatch`
- L291 **reconciliation loop**
- L291 **fresh, ephemeral background `Agent` invocations**
- L293 `#### Reconciliation loop`
- L295 **event-driven, not clock-driven**
- L297 **Read state.**
- L298 **bees**
- L305 **TaskList**
- L306 **git state**
- L307 **the run-state manifest**
- L309 **Conversation memory is never a substitute for these four sources.**
- L309 **if a summarization marker is visible in the conversation**
- L313 **Reconcile.**
- L315 **A Test Writer or Doc Writer return that reports it *stopped* on detected source movement is not a completion**
- L316 **Movement report from a Test Writer or Doc Writer.**
- L316 **stop mid-run and report**
- L318 **Recognising it, and the execute-mode narrowing.**
- L318 **sibling Subtask's** / **movement report** / **its own**
- L320 **What to do.**
- L322 **Mark the writer's own TaskList task `completed`**
- L323 **Create a new `aborted-<role>-<id>` TaskList task**
- L324 **The Task must not advance while any `aborted-*` task for it is `pending`.**
- L336 (bold) **before**
- L338 **Bee-scoped case — the bees corrective is a no-op.**
- L338 **Section 5 Bee-scoped re-dispatch**
- L342 **When the mover was an Engineer**
- L342 **first** / **Bee-level Code Reviewer** / **per-Task PM's in-flight `/quo-engineer-review` pass**
- L343 **When the mover was a concurrently-dispatched Test Writer's discrimination experiment**
- L343 **attributable** / **any concurrently-dispatched Test Writer** / **in place** / **every** / **some** / **re-dispatch the aborted lane once that sibling has returned, with no gate** / **When a concurrently-dispatched Test Writer has not returned yet, do not classify and do not fire the gate**
- L344 **When the mover was an external actor**
- L344 **unexplained** / **unexplained-movement gate**
- L346 **When the re-dispatched lane returns a normal deliverable, mark the `aborted-*` task `completed`**
- L346 **existing**
- L348 **Unexplained-movement gate.**
- L348 **no** / **no returned concurrently-dispatched Test Writer's `## Perturbations` list covers the moved paths** / **first** / **then**
- L350 **verbatim**
- L352 **Re-dispatch the writer now**
- L353 **Wait**
- L353 **only on the operator's reply** / **A sibling lane's completion notification is a tick that processes that lane normally** / **MUST NOT**
- L354 **Abort this unit**
- L354 **`##### Aborted-run close-out`** / **Epic-boundary state-externalization checkpoint**
- L356 **Wait** / **fresh**
- L357 **and no `aborted-*` TaskList task for this Task is `pending`**
- L360 **Yield.** / **Agent completion notification**
- L362 `##### Anti-pattern: no clock primitives`
- L366 **`/loop`**
- L367 **`ScheduleWakeup`**
- L368 **`CronCreate`**
- L369 **Polling**
- L373 `#### Per-Subtask cold dispatch`
- L389 `##### Per-Subtask cold dispatch (vs SDD's warm-Agent intent)`
- L393 `##### Dispatch prompt: quote the ticket body verbatim`
- L395 **verbatim**
- L397 **Test Writer dispatch — supply the fingerprint path list.**
- L397 **not by re-deriving it from the tree, but by carrying forward the changed-file list the Engineer's own return supplied.**
- L399 **Subtask-scoped Test Writer**
- L399 **resolved, never assumed** / **The primary rule is the parent Task's union.** / **union of the `## Files changed` lists from the Engineer returns for that Task's implementation Subtasks** / **Then narrow that union to just the `up_dependencies`-named implementation Subtasks when — and only when — the test Subtask's own `up_dependencies` is non-empty** / **empty `up_dependencies` on a test Subtask is legal** / **A limitation worth naming rather than papering over:** / **possession of an Engineer return** / **at dispatch time** / **same-Task sibling**
- L400 **Bee-scoped Test Writer**
- L402 **Fallback when an Engineer return did not list files.**
- L414 **This derivation is the last resort, and its trigger is narrow:**
- L414 **no usable Engineer-supplied list at all** / **An explicitly-empty `## Files changed` list is a list, not a missing one:** / **whole working tree** / **Omit the `## Source paths to fingerprint` heading rather than emitting an empty one in either of the two ways the set can come out empty:**
- L416 **Test Writer / Doc Writer dispatch — state the orchestrator-side rule, never a freeze guarantee.**
- L416 **Subtask-scoped** / **"no Engineer Agent will be dispatched for your Subtask's implementation dependency while you are running"** / **Bee-scoped** / **"no Engineer Agent will be dispatched for this Bee while you are running"** / **MUST NOT** / **"the source tree is frozen"**
- L418 **every**
- L428 `#### Hub-and-spoke via substrate`
- L430 **structural property**
- L432 `#### Recursive delegation: not supported`
- L434 **flat orchestration**
- L436 **monotonically within a session** / **nothing in this skill reclaims those tokens.** / **survivable** / **Epic-boundary state-externalization checkpoint** / **fresh session**
- L438 `#### Roles dispatched by the orchestrator`
- L442 **Engineer**
- L443 **Test Writer**
- L444 **Doc Writer**
- L445 **Product Manager**
- L449 `##### Per-Task PM dispatch`
- L451 **and no `aborted-*` TaskList task for this Task is `pending`** / **Pass the tracker path, not its contents**
- L453 **Relay the Engineer's completeness evidence into the PM dispatch prompt.**
- L453 **completeness check with evidence** / **when any Engineer return for this Task carried a completeness list, the per-Task PM dispatch prompt MUST embed that list verbatim** / **Separately from the list, state in the prompt that the assignment was sweep-shaped whenever it was**
- L455 `#### TaskList as progress UI`
- L457 **TaskList**
- L459 **one**
- L461 **`pending`**
- L462 **`in_progress`**
- L463 **`completed`** / **One exception:** / **without**
- L467 `##### TaskList naming convention`
- L469 **canonical cross-reference**
- L471 **Implementer Agents** / **Subtask scope**
- L472 **Bee-level implementer Agents** / **Bee scope**
- L473 **PM Agents** / **Task scope** / **Bee-scoped**
- L474 **Reviewer Agents** / **Bee scope**
- L475 **Round discriminator for every repeat dispatch of an already-named role.** / **Every dispatch after the first appends `-r<n>`, where `<n>` is the 1-based re-dispatch count for that role at that scope** / **suffix** / **prefix**
- L476 **Post-completion follow-up Agents** / **post-completion scope** / **no ticket-id suffix** / **is** / **One carve-out: a round discriminator IS permitted, and required, when a post-completion writer lane is re-dispatched after a movement abort**
- L477 **Aborted-writer redelivery markers** / **scope of the lane that aborted** / **the same finding index as the lane it marks** / **movement-report rung** / **This is not an Agent task** / **obligation marker**
- L478 **Deferral-ledger tasks** / **Run scope**
- L479 **Gate-task tasks** / **Turn scope** / **unexplained-movement gate** / **yield-control discipline**
- L481 `#### Scoped-marker PM dispatch wiring`
- L483 **resolved path**
- L503 `#### Testing discipline — avoid redundant full-workspace runs`
- L507 **While iterating on a file** / **Narrow test** / **Lint**
- L508 **At subtask boundary**
- L509 **At the Task's final `.T` (or equivalent) subtask** / **authoritative** / **Full test** / **Format**
- L510 **Product Manager review**
- L511 **Director (before commit)**
- L513 **When a Task only touches one package's tests**
- L515 **Lint**, **Format**
- L517 `#### Running long commands`

## Rationale-only spans

- L275 (1 line) — manifest survives compaction, conversation doesn't
- L389-L391 (3 lines) — warm-Agent SDD divergence explained
- L426 (1 line) — role exceptions signal coordination gap
- L428-L430 (3 lines) — hub-and-spoke is structural, not enforced
- L436 (1 line) — context growth, harness owns reclamation

Total rationale-only lines: 9. (Many other lines mix rule and rationale; the rationale fragments there are captured as `rationale-only` / `rationale` rows rather than as spans.)
